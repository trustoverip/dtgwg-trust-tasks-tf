name: Rust

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
    paths:
      - "trust-tasks-rs/**"
      - "trust-tasks-codegen/**"
      - "trust-tasks-https/**"
      - "trust-tasks-didcomm/**"
      - "trust-tasks-proof-affinidi/**"
      - "Cargo.toml"
      - "Cargo.lock"
      - "specs/**"
      - ".github/workflows/rust.yml"

env:
  CARGO_TERM_COLOR: always
  CARGO_INCREMENTAL: 0
  RUSTFLAGS: "-D warnings"

jobs:
  test:
    name: cargo test (stable, full workspace)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
      # Affinidi's TDK pulls in keyring / secret-storage crates whose
      # build scripts probe for `libdbus-1` via pkg-config. Install the
      # dev headers so libdbus-sys finds the system library.
      - name: Install libdbus-1-dev
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config
      - name: cargo test (default features)
        run: cargo test --workspace --all-targets
      - name: cargo test (--features validate)
        run: cargo test -p trust-tasks-rs --features validate --all-targets
      - name: cargo doc
        run: cargo doc --workspace --no-deps --all-features
        env:
          RUSTDOCFLAGS: "-D warnings"

  msrv:
    name: cargo test (MSRV 1.85 — core crates only)
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@master
        with:
          toolchain: "1.85"
      - uses: Swatinem/rust-cache@v2
      # trust-tasks-rs, trust-tasks-codegen, and trust-tasks-https declare
      # MSRV 1.85 (constrained by the workspace's transitive deps that
      # use `edition2024`). The Affinidi-backed crates separately
      # require Rust 1.94 and don't participate in this job.
      - name: Install libdbus-1-dev
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config
      - name: cargo test (trust-tasks-rs, trust-tasks-codegen)
        run: cargo test -p trust-tasks-rs -p trust-tasks-codegen --all-targets
      - name: cargo test (trust-tasks-https)
        run: cargo test -p trust-tasks-https --all-targets

  fmt:
    name: cargo fmt
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - run: cargo fmt --all -- --check

  clippy:
    name: cargo clippy
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: clippy
      - uses: Swatinem/rust-cache@v2
      - name: Install libdbus-1-dev
        run: sudo apt-get update && sudo apt-get install -y libdbus-1-dev pkg-config
      - name: clippy (default features)
        run: cargo clippy --workspace --all-targets -- -D warnings
      - name: clippy (--all-features)
        run: cargo clippy --workspace --all-targets --all-features -- -D warnings

  codegen-drift:
    name: trust-tasks-codegen drift
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: dtolnay/rust-toolchain@stable
        with:
          components: rustfmt
      - uses: Swatinem/rust-cache@v2
      - name: Run codegen
        run: cargo run -p trust-tasks-codegen
      # Codegen emits prettyplease-canonical output; the committed
      # source has additionally been through `cargo fmt`, so normalise
      # here before diffing.
      - name: Format
        run: cargo fmt --all
      - name: Check for drift
        run: |
          if ! git diff --quiet trust-tasks-rs/src/specs; then
            echo "::error::Codegen output is stale. Run \`cargo run -p trust-tasks-codegen && cargo fmt --all\` and commit the diff."
            git diff trust-tasks-rs/src/specs
            exit 1
          fi

  deny:
    name: cargo deny
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: EmbarkStudios/cargo-deny-action@v2
        with:
          command: check
          arguments: --all-features
