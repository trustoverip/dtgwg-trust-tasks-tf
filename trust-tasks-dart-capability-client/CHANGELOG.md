# Changelog

## 0.1.0

- Initial release: document builders and reply classification for the capability
  Trust Task families (`governance/capability/*` and `git-trust/*`). No crypto —
  sign with `package:trust_tasks_proof`.
