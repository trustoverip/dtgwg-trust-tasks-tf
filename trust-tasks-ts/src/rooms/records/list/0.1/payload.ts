/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/rooms/records/list/0.1/payload.schema.json
 */

import type { AuthorityPresentation, Ext, RecordMetadata } from "../../../../_shared/components.js";


export interface RoomsRecordsListPayload {
  /**
   * The room's identifier.
   */
  roomId: string;
  /**
   * Must confer the `read` action at this room's scope.
   */
  presentation: AuthorityPresentation;
  /**
   * Return only records whose key starts with this. On an `attributed` or `private` room keys are opaque, so a prefix is only useful where the client assigned structure to them.
   */
  prefix?: string;
  /**
   * Return only records changed after this version — the watermark that makes incremental sync possible. Tombstones are returned too: without them a puller learns of every create and update and never of a delete, so deleted records resurrect on the next full rebuild.
   */
  sinceVersion?: number;
  /**
   * Opaque continuation token from a previous response. A host MUST NOT accept a cursor it did not issue.
   */
  cursor?: string;
  /**
   * Maximum records to return. A host MAY return fewer.
   */
  limit?: number;
  /**
   * Ecosystem-defined extension members per SPEC.md §4.5.1.
   */
  ext?: Ext;
}
/**
 * Success response to rooms/records/list. Type https://trusttasks.org/spec/rooms/records/list/0.1#response. Returns METADATA, never bodies: a reader fetches the handful it wants with rooms/records/get.
 */
export interface RoomsRecordsListResponsePayload {
  records: RecordMetadata[];
  /**
   * Present when more records remain.
   */
  cursor?: string;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { AuthorityPresentation, Ext, RecordMetadata };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/rooms/records/list/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = RoomsRecordsListPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/rooms/records/list/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = RoomsRecordsListResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/rooms/records/list/0.1",
  "title": "Rooms Records List — payload",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "roomId",
    "presentation"
  ],
  "properties": {
    "roomId": {
      "type": "string",
      "description": "The room's identifier."
    },
    "presentation": {
      "$ref": "#/$defs/AuthorityPresentation",
      "description": "Must confer the `read` action at this room's scope."
    },
    "prefix": {
      "type": "string",
      "maxLength": 512,
      "description": "Return only records whose key starts with this. On an `attributed` or `private` room keys are opaque, so a prefix is only useful where the client assigned structure to them."
    },
    "sinceVersion": {
      "type": "integer",
      "minimum": 0,
      "description": "Return only records changed after this version — the watermark that makes incremental sync possible. Tombstones are returned too: without them a puller learns of every create and update and never of a delete, so deleted records resurrect on the next full rebuild."
    },
    "cursor": {
      "type": "string",
      "maxLength": 4096,
      "description": "Opaque continuation token from a previous response. A host MUST NOT accept a cursor it did not issue."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 500,
      "description": "Maximum records to return. A host MAY return fewer."
    },
    "ext": {
      "$ref": "#/$defs/Ext",
      "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Rooms Records List — response payload",
      "description": "Success response to rooms/records/list. Type https://trusttasks.org/spec/rooms/records/list/0.1#response. Returns METADATA, never bodies: a reader fetches the handful it wants with rooms/records/get.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "records"
      ],
      "properties": {
        "records": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RecordMetadata"
          }
        },
        "cursor": {
          "type": "string",
          "maxLength": 4096,
          "description": "Present when more records remain."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "RecordMetadata": {
      "title": "RecordMetadata",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "key",
        "version",
        "updatedAt"
      ],
      "description": "What `rooms/records/list` returns. Never the body: ranking happens on the client, and a host that returned every body would make the caller pay for the whole room on every listing.",
      "properties": {
        "key": {
          "type": "string",
          "description": "The record's key within the room. On `attributed` and `private` rooms this MUST be opaque — a random identifier, never a descriptive slug. A key reading `decision/acquire-northwind` defeats the encryption sitting beside it; structured naming belongs inside the sealed body."
        },
        "version": {
          "type": "integer",
          "minimum": 1,
          "description": "Server-assigned, monotonic per room."
        },
        "epoch": {
          "type": "integer",
          "minimum": 1,
          "description": "The epoch the record was sealed under. Absent on an `open` room."
        },
        "status": {
          "type": "string",
          "enum": [
            "active",
            "deprecated",
            "retracted"
          ],
          "description": "Curation state. `retracted` is a tombstone: the body is gone, the key and version remain so incremental sync converges and the audit chain stays intact."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "title": {
          "type": "string",
          "maxLength": 512,
          "description": "Present only on an `open` room; sealed with the body otherwise."
        },
        "description": {
          "type": "string",
          "maxLength": 2048,
          "description": "Present only on an `open` room; sealed with the body otherwise."
        },
        "author": {
          "type": "string",
          "description": "The member who wrote it. Present on `open` and `attributed`; on `private` the author is inside the sealed body, where only members can read it."
        }
      }
    },
    "AuthorityPresentation": {
      "title": "AuthorityPresentation",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "membership",
        "authority"
      ],
      "description": "What a party presents to act on a room. Carries the whole authority chain: a host MUST NOT dereference an authority credential's `parent` to fetch a link it was not given. Resolving over the network would make verification depend on availability, turn every identifier into a request the host can be induced to make against an address the holder chooses, and signal credential use to whoever hosts the identifier.",
      "properties": {
        "membership": {
          "type": "string",
          "description": "The presenter's membership credential for this room, or — on a `private` room — a zero-knowledge presentation of it. Serialized per the governing profile."
        },
        "authority": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "string"
          },
          "description": "The authority chain, LEAF FIRST: the first element is the credential being relied on and the last MUST be one issued by the room itself. Every link the presenter relies on is present, because the host will not fetch one. Capped at 8: verification is linear in chain length and runs on every operation, so an unbounded chain is a denial-of-service surface against the host. The known uses need 2 to 3 — a person attenuating to an agent, and that agent to a sub-agent."
        },
        "subjectBinding": {
          "type": "string",
          "description": "REQUIRED on a `private` room, where the subject identifier is withheld: a proof that the membership credential and the authority chain's leaf describe the SAME subject. Without it two parties pool credentials — one contributes membership, the other authority — and the combination verifies as a single party holding both. A host MUST refuse a private-room presentation that omits this."
        }
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Rooms Records List — response payload",
      "description": "Success response to rooms/records/list. Type https://trusttasks.org/spec/rooms/records/list/0.1#response. Returns METADATA, never bodies: a reader fetches the handful it wants with rooms/records/get.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "records"
      ],
      "properties": {
        "records": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/RecordMetadata"
          }
        },
        "cursor": {
          "type": "string",
          "maxLength": 4096,
          "description": "Present when more records remain."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "RecordMetadata": {
      "title": "RecordMetadata",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "key",
        "version",
        "updatedAt"
      ],
      "description": "What `rooms/records/list` returns. Never the body: ranking happens on the client, and a host that returned every body would make the caller pay for the whole room on every listing.",
      "properties": {
        "key": {
          "type": "string",
          "description": "The record's key within the room. On `attributed` and `private` rooms this MUST be opaque — a random identifier, never a descriptive slug. A key reading `decision/acquire-northwind` defeats the encryption sitting beside it; structured naming belongs inside the sealed body."
        },
        "version": {
          "type": "integer",
          "minimum": 1,
          "description": "Server-assigned, monotonic per room."
        },
        "epoch": {
          "type": "integer",
          "minimum": 1,
          "description": "The epoch the record was sealed under. Absent on an `open` room."
        },
        "status": {
          "type": "string",
          "enum": [
            "active",
            "deprecated",
            "retracted"
          ],
          "description": "Curation state. `retracted` is a tombstone: the body is gone, the key and version remain so incremental sync converges and the audit chain stays intact."
        },
        "updatedAt": {
          "type": "string",
          "format": "date-time"
        },
        "title": {
          "type": "string",
          "maxLength": 512,
          "description": "Present only on an `open` room; sealed with the body otherwise."
        },
        "description": {
          "type": "string",
          "maxLength": 2048,
          "description": "Present only on an `open` room; sealed with the body otherwise."
        },
        "author": {
          "type": "string",
          "description": "The member who wrote it. Present on `open` and `attributed`; on `private` the author is inside the sealed body, where only members can read it."
        }
      }
    },
    "AuthorityPresentation": {
      "title": "AuthorityPresentation",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "membership",
        "authority"
      ],
      "description": "What a party presents to act on a room. Carries the whole authority chain: a host MUST NOT dereference an authority credential's `parent` to fetch a link it was not given. Resolving over the network would make verification depend on availability, turn every identifier into a request the host can be induced to make against an address the holder chooses, and signal credential use to whoever hosts the identifier.",
      "properties": {
        "membership": {
          "type": "string",
          "description": "The presenter's membership credential for this room, or — on a `private` room — a zero-knowledge presentation of it. Serialized per the governing profile."
        },
        "authority": {
          "type": "array",
          "minItems": 1,
          "maxItems": 8,
          "items": {
            "type": "string"
          },
          "description": "The authority chain, LEAF FIRST: the first element is the credential being relied on and the last MUST be one issued by the room itself. Every link the presenter relies on is present, because the host will not fetch one. Capped at 8: verification is linear in chain length and runs on every operation, so an unbounded chain is a denial-of-service surface against the host. The known uses need 2 to 3 — a person attenuating to an agent, and that agent to a sub-agent."
        },
        "subjectBinding": {
          "type": "string",
          "description": "REQUIRED on a `private` room, where the subject identifier is withheld: a proof that the membership credential and the authority chain's leaf describe the SAME subject. Without it two parties pool credentials — one contributes membership, the other authority — and the combination verifies as a single party holding both. A host MUST refuse a private-room presentation that omits this."
        }
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
