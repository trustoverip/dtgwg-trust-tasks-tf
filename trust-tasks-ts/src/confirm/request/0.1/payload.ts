/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/confirm/request/0.1/payload.schema.json
 */

/**
 * A relying party asks a wallet (or other approval agent) to obtain user confirmation for an action. The wallet surfaces the reason to the user and returns a signed confirm/response.
 */
export interface ConfirmRequest {
  /**
   * The VID whose consent the relying party is asking for. The wallet MUST verify it speaks for this VID.
   */
  subject: string;
  /**
   * base64url-encoded nonce ≥128 bits. Echoed and signed in the confirm-response.
   */
  challenge: string;
  /**
   * Human-readable action description shown to the user verbatim. SHOULD be specific ("Confirm transfer of $100 to bob.example" — not "Approve action").
   */
  reason: string;
  /**
   * Optional machine-readable action category the wallet MAY use for UI styling, history grouping, or local policy. Free-form; ecosystems define the vocabulary (e.g. "payment.transfer", "acl.delete", "data.share").
   */
  actionType?: string;
  /**
   * Optional structured payload describing the action — amount, beneficiary, scope, etc. The wallet MAY surface fields to the user. Framework imposes no schema; consumer-defined.
   */
  actionDetails?: {};
  /**
   * Seconds within which the relying party expects a response. The wallet SHOULD treat as advisory.
   */
  ttl?: number;
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}
/**
 * Synchronous acknowledgement that the wallet received the request and will (or will not) deliver a confirm/response. The actual signed confirmation arrives via confirm/response out-of-band. Carried in a Trust Task document whose type is https://trusttasks.org/spec/confirm/request/0.1#response.
 */
export interface ConfirmRequestSynchronousAck {
  status: "accepted" | "refused";
  /**
   * Required when status is `refused`.
   */
  reason?: string;
  ext?: Ext;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/confirm/request/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = ConfirmRequest;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/confirm/request/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = ConfirmRequestSynchronousAck;
