/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/acl/revoke/0.1/payload.schema.json
 */

export interface ACLRevokePayload {
  /**
   * VID of the party being revoked (or scope-reduced).
   */
  subject: string;
  /**
   * If present, the specific scopes to remove from the subject's entry. If absent, the entire entry is removed.
   *
   * @minItems 1
   */
  scopes?: [string, ...string[]];
  /**
   * Optional human-readable rationale for the revocation.
   */
  reason?: string;
  ext?: Ext;
}
/**
 * Ecosystem-defined extension members per SPEC.md §4.5.1.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/acl/revoke/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/acl/revoke/0.1#response" as const;
