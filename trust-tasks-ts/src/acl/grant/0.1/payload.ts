/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/acl/grant/0.1/payload.schema.json
 */

export interface ACLGrantPayload {
  entry: AclEntry;
  /**
   * Optional human-readable rationale.
   */
  reason?: string;
  ext?: Ext1;
}
/**
 * The AclEntry the producer wants the maintainer to hold for the subject (intent). The maintainer's response carries the realized AclEntry it now holds.
 */
export interface AclEntry {
  /**
   * VID of the party in the ACL. Compared by exact string equality (SPEC.md §4.8); producers SHOULD emit canonical form.
   */
  subject: string;
  /**
   * Opaque role identifier interpreted by the ACL maintainer.
   */
  role: string;
  /**
   * Opaque scope identifiers (e.g. contexts, domains, resource prefixes).
   */
  scopes?: string[];
  /**
   * Optional human-readable label.
   */
  label?: string;
  createdAt?: string;
  /**
   * VID of the party that originally added this entry.
   */
  createdBy?: string;
  updatedAt?: string;
  /**
   * VID of the party that last modified this entry.
   */
  updatedBy?: string;
  /**
   * Optional time after which the entry is no longer effective.
   */
  expiresAt?: string;
  ext?: Ext;
}
/**
 * Ecosystem-defined extension members per SPEC.md §4.5.1. Reverse-DNS-namespaced; consumers MUST ignore unrecognized namespaces.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}
/**
 * Ecosystem-defined extension members per SPEC.md §4.5.1.
 */
export interface Ext1 {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/acl/grant/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/acl/grant/0.1#response" as const;
