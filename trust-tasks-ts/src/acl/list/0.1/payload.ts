/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/acl/list/0.1/payload.schema.json
 */

export interface ACLListPayload {
  /**
   * Optional filter — only entries with this role are returned.
   */
  role?: string;
  /**
   * Optional filter — only entries whose scopes include this string are returned.
   */
  scope?: string;
  /**
   * How to read `scope` where the maintainer's scopes are hierarchical: `acting-in` (the default when omitted) returns entries that may act IN the scope — scoped to it or to an ancestor; `subtree` returns entries holding a grant at or BENEATH it; `any` is the union. A consumer without hierarchical scopes MAY treat all three alike. See the specification for why the distinction matters to a revocation sweep.
   */
  direction?: "acting-in" | "subtree" | "any";
  /**
   * Optional filter — only entries whose subject VID starts with this prefix are returned.
   */
  subjectPrefix?: string;
  /**
   * Maximum number of entries to return. Maintainer-defined default and ceiling.
   */
  pageSize?: number;
  /**
   * Opaque continuation token returned by the maintainer in a previous response.
   */
  cursor?: string;
  ext?: Ext;
}
/**
 * Ecosystem-defined extension members per SPEC.md §4.5.1.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/acl/list/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/acl/list/0.1#response" as const;
