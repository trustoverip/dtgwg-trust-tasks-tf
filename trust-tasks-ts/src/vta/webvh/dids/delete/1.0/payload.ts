/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vta/webvh/dids/delete/1.0/payload.schema.json
 */

import type { Ext } from "../../../../../_shared/components.js";


export interface VTAWebVHDIDsDeletePayload {
  did: string;
  ext?: Ext;
}
/**
 * Success response to vta/webvh/dids/delete. Type https://trusttasks.org/spec/vta/webvh/dids/delete/1.0#response.
 */
export interface VTAWebVHDIDsDeleteResponsePayload {
  did: string;
  /**
   * Whether the VTA removed its record. A successful response carrying `false` means it declined.
   */
  deleted: boolean;
  /**
   * Set when the VTA removed its own record but the hosting server did not confirm removal of the published log. **The DID may still resolve.** This is a partial success reported as a success, and a consumer MUST surface it rather than treating the deletion as complete.
   */
  daemonCleanupError?: string;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vta/webvh/dids/delete/1.0" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTAWebVHDIDsDeletePayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vta/webvh/dids/delete/1.0#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTAWebVHDIDsDeleteResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vta/webvh/dids/delete/1.0",
  "title": "VTA WebVH DIDs Delete — payload",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "did"
  ],
  "properties": {
    "did": {
      "type": "string",
      "minLength": 1
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTA WebVH DIDs Delete — response payload",
      "description": "Success response to vta/webvh/dids/delete. Type https://trusttasks.org/spec/vta/webvh/dids/delete/1.0#response.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "did",
        "deleted"
      ],
      "properties": {
        "did": {
          "type": "string"
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the VTA removed its record. A successful response carrying `false` means it declined."
        },
        "daemonCleanupError": {
          "type": "string",
          "description": "Set when the VTA removed its own record but the hosting server did not confirm removal of the published log. **The DID may still resolve.** This is a partial success reported as a success, and a consumer MUST surface it rather than treating the deletion as complete."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTA WebVH DIDs Delete — response payload",
      "description": "Success response to vta/webvh/dids/delete. Type https://trusttasks.org/spec/vta/webvh/dids/delete/1.0#response.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "did",
        "deleted"
      ],
      "properties": {
        "did": {
          "type": "string"
        },
        "deleted": {
          "type": "boolean",
          "description": "Whether the VTA removed its record. A successful response carrying `false` means it declined."
        },
        "daemonCleanupError": {
          "type": "string",
          "description": "Set when the VTA removed its own record but the hosting server did not confirm removal of the published log. **The DID may still resolve.** This is a partial success reported as a success, and a consumer MUST surface it rather than treating the deletion as complete."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
