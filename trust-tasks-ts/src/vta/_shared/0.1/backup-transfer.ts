/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vta/_shared/0.1/backup-transfer.schema.json
 */

/**
 * The shapes that describe how an encrypted backup bundle moves between an operator and an agent. Defined once because `initiate-export` and `initiate-import` return the same descriptor, `initiate-import` and the descriptor carry the same chunk manifest, and `get-chunk` and `put-chunk` carry the same chunk — an inline copy per task would fork the moment one of them gained a member.
 *
 * Two transfer algorithms are defined. `stream` moves the whole bundle in one HTTPS request against a recipient-published address, authorized by a bearer token. `chunkedTrustTask` moves it as a sequence of Trust Task documents over whatever transport already carries the family's control-plane tasks, which is what makes a backup possible for an agent that speaks only DIDComm or TSP and has no HTTPS address to publish. The normative account of `chunkedTrustTask` — the chunk-size bound and why, ordering, resume and expiry — is in the `vta/backup/initiate-export/1.1` specification's Chunked transfer section; these definitions carry its shape.
 */
export interface BackupTransferSharedDefinitionsForTheVtaBackupSpecFamily {
  [k: string]: unknown | undefined;
}

/**
 * The definitions this shared schema publishes, hoisted to one declaration each.
 * See "../../../_shared/components.js".
 */
export type { BundleDescriptor, BundleId, ChunkCount, ChunkData, ChunkedDescriptor, ChunkIndex, ChunkManifest, ChunkSize, ExpectedSha256, ExpectedSizeBytes, ExpiresAt, StreamDescriptor } from "../../../_shared/components.js";
