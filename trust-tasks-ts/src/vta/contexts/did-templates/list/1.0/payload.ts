/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vta/contexts/did-templates/list/1.0/payload.schema.json
 */

import type { Ext } from "../../../../../_shared/components.js";


/**
 * Resolved scope of the stored template.
 */
export type Scope = Builtin | Global | Context;

/**
 * List the DID templates scoped to a context on a VTA. Gated on context access. The success response is the set of persisted DidTemplateRecords for the context.
 */
export interface VTAContextDIDTemplateListPayload {
  /**
   * The context the templates are scoped to.
   */
  contextId: string;
  /**
   * Ecosystem-defined extension members per SPEC.md §4.5.1.
   */
  ext?: Ext;
}
/**
 * The templates scoped to the context.
 */
export interface VTAContextDIDTemplateListResponsePayload {
  /**
   * Templates scoped to the context, sorted by name; empty when none exist.
   */
  templates: DidTemplateRecord[];
  /**
   * Ecosystem-defined extension members per SPEC.md §4.5.1.
   */
  ext?: Ext;
}
/**
 * A persisted template plus provenance. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata.
 */
export interface DidTemplateRecord {
  schemaVersion: 1;
  name: string;
  kind: string;
  description?: string | null;
  methods?: string[];
  requiredVars?: string[];
  optionalVars?: {
    [k: string]: unknown | undefined;
  };
  defaults?: {
    [k: string]: unknown | undefined;
  };
  document: {};
  scope: Scope;
  /**
   * UTC unix-epoch seconds the template was first stored.
   */
  createdAt: number;
  /**
   * UTC unix-epoch seconds of the last write.
   */
  updatedAt: number;
  /**
   * DID of the admin who last wrote the template.
   */
  createdBy: string;
}
export interface Builtin {
  type: "builtin";
}
export interface Global {
  type: "global";
}
export interface Context {
  type: "context";
  contextId: string;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vta/contexts/did-templates/list/1.0" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTAContextDIDTemplateListPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vta/contexts/did-templates/list/1.0#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTAContextDIDTemplateListResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vta/contexts/did-templates/list/1.0",
  "title": "VTA Context DID-Template List — payload",
  "description": "List the DID templates scoped to a context on a VTA. Gated on context access. The success response is the set of persisted DidTemplateRecords for the context.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contextId"
  ],
  "properties": {
    "contextId": {
      "type": "string",
      "minLength": 1,
      "description": "The context the templates are scoped to."
    },
    "ext": {
      "$ref": "#/$defs/Ext",
      "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
    }
  },
  "$defs": {
    "DidTemplateRecord": {
      "title": "DidTemplateRecord",
      "description": "A persisted template plus provenance. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schemaVersion",
        "name",
        "kind",
        "document",
        "scope",
        "createdAt",
        "updatedAt",
        "createdBy"
      ],
      "properties": {
        "schemaVersion": {
          "type": "integer",
          "const": 1
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 64,
          "pattern": "^[a-z0-9-]+$"
        },
        "kind": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "methods": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "requiredVars": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "optionalVars": {
          "type": "object",
          "additionalProperties": true
        },
        "defaults": {
          "type": "object",
          "additionalProperties": true
        },
        "document": {
          "type": "object"
        },
        "scope": {
          "$ref": "#/$defs/Scope",
          "description": "Resolved scope of the stored template."
        },
        "createdAt": {
          "type": "integer",
          "description": "UTC unix-epoch seconds the template was first stored."
        },
        "updatedAt": {
          "type": "integer",
          "description": "UTC unix-epoch seconds of the last write."
        },
        "createdBy": {
          "type": "string",
          "description": "DID of the admin who last wrote the template."
        }
      }
    },
    "Scope": {
      "title": "Scope",
      "description": "Where a stored template lives. Tagged by `type`.",
      "type": "object",
      "oneOf": [
        {
          "title": "Builtin",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "const": "builtin"
            }
          }
        },
        {
          "title": "Global",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "const": "global"
            }
          }
        },
        {
          "title": "Context",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type",
            "contextId"
          ],
          "properties": {
            "type": {
              "const": "context"
            },
            "contextId": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      ]
    },
    "Response": {
      "$anchor": "response",
      "title": "VTA Context DID-Template List — response payload",
      "description": "The templates scoped to the context.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "templates"
      ],
      "properties": {
        "templates": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DidTemplateRecord"
          },
          "description": "Templates scoped to the context, sorted by name; empty when none exist."
        },
        "ext": {
          "$ref": "#/$defs/Ext",
          "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "DidTemplateRecord": {
      "title": "DidTemplateRecord",
      "description": "A persisted template plus provenance. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "schemaVersion",
        "name",
        "kind",
        "document",
        "scope",
        "createdAt",
        "updatedAt",
        "createdBy"
      ],
      "properties": {
        "schemaVersion": {
          "type": "integer",
          "const": 1
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "maxLength": 64,
          "pattern": "^[a-z0-9-]+$"
        },
        "kind": {
          "type": "string",
          "minLength": 1
        },
        "description": {
          "type": [
            "string",
            "null"
          ]
        },
        "methods": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "requiredVars": {
          "type": "array",
          "items": {
            "type": "string"
          }
        },
        "optionalVars": {
          "type": "object",
          "additionalProperties": true
        },
        "defaults": {
          "type": "object",
          "additionalProperties": true
        },
        "document": {
          "type": "object"
        },
        "scope": {
          "$ref": "#/$defs/Scope",
          "description": "Resolved scope of the stored template."
        },
        "createdAt": {
          "type": "integer",
          "description": "UTC unix-epoch seconds the template was first stored."
        },
        "updatedAt": {
          "type": "integer",
          "description": "UTC unix-epoch seconds of the last write."
        },
        "createdBy": {
          "type": "string",
          "description": "DID of the admin who last wrote the template."
        }
      }
    },
    "Scope": {
      "title": "Scope",
      "description": "Where a stored template lives. Tagged by `type`.",
      "type": "object",
      "oneOf": [
        {
          "title": "Builtin",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "const": "builtin"
            }
          }
        },
        {
          "title": "Global",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type"
          ],
          "properties": {
            "type": {
              "const": "global"
            }
          }
        },
        {
          "title": "Context",
          "type": "object",
          "additionalProperties": false,
          "required": [
            "type",
            "contextId"
          ],
          "properties": {
            "type": {
              "const": "context"
            },
            "contextId": {
              "type": "string",
              "minLength": 1
            }
          }
        }
      ]
    },
    "Response": {
      "$anchor": "response",
      "title": "VTA Context DID-Template List — response payload",
      "description": "The templates scoped to the context.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "templates"
      ],
      "properties": {
        "templates": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/DidTemplateRecord"
          },
          "description": "Templates scoped to the context, sorted by name; empty when none exist."
        },
        "ext": {
          "$ref": "#/$defs/Ext",
          "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
