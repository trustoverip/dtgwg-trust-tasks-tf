/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/git-ns/_shared/0.2/git-ns.schema.json
 */

/**
 * Shapes used by more than one git-ns/* task: forge-qualified resources, the five git rights, namespaces, repositories, rights records, forge accounts and drift. 0.2 is wire-identical to 0.1: it adds `DriftType`, naming the values of `DriftItem`'s `type`, so that git-ns/drift/resolve can select a drift item by the same values git-ns/view and git-ns/bridge/event report it with. Not itself a Trust Task specification. A VTC is the source of truth for all of these; the Trust Registry and the forge are projections of them.
 */
export interface GitNamespacesSharedDefinitionsForTheGitNsSpecFamily {
  [k: string]: unknown | undefined;
}

/**
 * The definitions this shared schema publishes, hoisted to one declaration each.
 * See "../../../_shared/components.js".
 */
export type { Bootstrap, Did_GitNsV0_1 as Did, DriftItem, DriftType, ForgeAccount, ForgeHost, ForgeId, GitNamespace, NamespaceId, RepoResource, RepoSummary_GitNsV0_1 as RepoSummary, RepoVisibility, Resource, Right, RightRecord_GitNsV0_1 as RightRecord, Segment, Sync } from "../../../_shared/components.js";
