/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/git-ns/_shared/0.4/git-ns.schema.json
 */

/**
 * Shapes used by more than one git-ns/* task: forge-qualified resources, the five git rights, namespaces, repositories, rights records, forge accounts and drift. 0.4 adds `BreakGlass` and the optional `breakGlass` member of `RightRecord`: a record an actor gave themselves through git-ns/right/break-glass carries who, when and why, and — once another administrator has ratified it with git-ns/right/ratify — who ratified it and when. It also adds `ElevatedRight`, the three rights no actor may grant themselves through git-ns/right/grant. Every other definition is unchanged from 0.3. Not itself a Trust Task specification. A VTC is the source of truth for all of these; the Trust Registry and the forge are projections of them.
 */
export interface GitNamespacesSharedDefinitionsForTheGitNsSpecFamily {
  [k: string]: unknown | undefined;
}

/**
 * The definitions this shared schema publishes, hoisted to one declaration each.
 * See "../../../_shared/components.js".
 */
export type { Bootstrap, BreakGlass, Did_GitNsV0_3 as Did, DriftItem, ElevatedRight, ForgeAccount, ForgeHost, ForgeId, GitNamespace, NamespaceId, RepoResource, RepoSummary_GitNsV0_3 as RepoSummary, RepoVisibility, Resource, Right, RightRecord_GitNsV0_4 as RightRecord, Segment, Sync } from "../../../_shared/components.js";
