/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/git-ns/_shared/0.3/git-ns.schema.json
 */

/**
 * Shapes used by more than one git-ns/* task: forge-qualified resources, the five git rights, namespaces, repositories, rights records, forge accounts and drift. 0.3 narrows `Did` to the W3C DID Core syntax: 0.1 and 0.2 accepted any non-whitespace after `did:<method>:`, which let shell metacharacters, `/`, `?` and `#` through, so a value a consumer later quoted into a command, a URL or a log line was not even a DID. Every other definition is unchanged from 0.2. Not itself a Trust Task specification. A VTC is the source of truth for all of these; the Trust Registry and the forge are projections of them.
 */
export interface GitNamespacesSharedDefinitionsForTheGitNsSpecFamily {
  [k: string]: unknown | undefined;
}

/**
 * The definitions this shared schema publishes, hoisted to one declaration each.
 * See "../../../_shared/components.js".
 */
export type { Bootstrap, Did_GitNsV0_3 as Did, DriftItem, DriftType, ForgeAccount, ForgeHost, ForgeId, GitNamespace, NamespaceId, RepoResource, RepoSummary_GitNsV0_3 as RepoSummary, RepoVisibility, Resource, Right, RightRecord_GitNsV0_3 as RightRecord, Segment, Sync } from "../../../_shared/components.js";
