/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/git-ns/namespace/reseat/0.1/payload.schema.json
 */

import type { Did_GitNsV0_1 as Did, Ext, NamespaceId, Resource, Right, RightRecord_GitNsV0_1 as RightRecord } from "../../../../_shared/components.js";


/**
 * A community administrator recovers a headless bound namespace — one with no live git.ns.admin — by granting git.ns.admin on it to a current member. Refused unless the namespace is headless.
 */
export interface GitNamespacesReseatNamespacePayload {
  /**
   * The headless namespace.
   */
  namespace: NamespaceId;
  /**
   * The current member who receives git.ns.admin on the namespace. May be the administrator themselves.
   */
  subject: Did;
  /**
   * Why the namespace is being reseated and why to this member, in the administrator's words. Kept in the audit record, recorded as the new right's `reason`, and shown to the namespace's repository owners.
   */
  statement: string;
  ext?: Ext;
}
/**
 * The recorded git.ns.admin right.
 */
export interface GitNamespacesReseatNamespaceResponsePayload {
  /**
   * The git.ns.admin record the reseat created. It never carries `expiresAt`.
   */
  right: RightRecord;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Did, Ext, NamespaceId, Resource, Right, RightRecord };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/git-ns/namespace/reseat/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = GitNamespacesReseatNamespacePayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/git-ns/namespace/reseat/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = GitNamespacesReseatNamespaceResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/git-ns/namespace/reseat/0.1",
  "title": "Git Namespaces — Reseat Namespace — payload",
  "description": "A community administrator recovers a headless bound namespace — one with no live git.ns.admin — by granting git.ns.admin on it to a current member. Refused unless the namespace is headless.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "namespace",
    "subject",
    "statement"
  ],
  "properties": {
    "namespace": {
      "$ref": "#/$defs/NamespaceId",
      "description": "The headless namespace."
    },
    "subject": {
      "$ref": "#/$defs/Did",
      "description": "The current member who receives git.ns.admin on the namespace. May be the administrator themselves."
    },
    "statement": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "Why the namespace is being reseated and why to this member, in the administrator's words. Kept in the audit record, recorded as the new right's `reason`, and shown to the namespace's repository owners."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Git Namespaces — Reseat Namespace — response payload",
      "description": "The recorded git.ns.admin right.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "right"
      ],
      "properties": {
        "right": {
          "$ref": "#/$defs/RightRecord",
          "description": "The git.ns.admin record the reseat created. It never carries `expiresAt`."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "RightRecord": {
      "title": "RightRecord",
      "description": "One recorded right. Implied rights (§4.2 of the rights model: `own` implies `maintain` implies `commit.sign` on the same resource; `ns.admin` implies `repo.create` and `own` across its namespace) are not records and never appear as RightRecords.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "subject",
        "right",
        "resource",
        "grantedBy",
        "grantedAt"
      ],
      "properties": {
        "subject": {
          "$ref": "#/$defs/Did",
          "description": "Who holds the right. For `git.commit.sign` this is the DID whose commit signatures the CI check accepts."
        },
        "right": {
          "$ref": "#/$defs/Right"
        },
        "resource": {
          "$ref": "#/$defs/Resource"
        },
        "grantedBy": {
          "$ref": "#/$defs/Did",
          "description": "The actor whose task caused the right: the granter, the creator of a repository (for its first `own`), the adopting admin, the transferring owner, or the binding admin (for the first `git.ns.admin`). The VTC's own DID for a right it derives from its configuration."
        },
        "grantedAt": {
          "type": "string",
          "format": "date-time"
        },
        "expiresAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the right lapses. Absent: no expiry."
        },
        "reason": {
          "type": "string",
          "maxLength": 1024,
          "description": "The granter's free-text reason. Disclosed only to holders of `git.repo.own` on the resource and of `git.ns.admin` over it."
        }
      }
    },
    "Did": {
      "title": "Did",
      "description": "A DID, compared by exact string equality.",
      "type": "string",
      "maxLength": 2048,
      "pattern": "^did:[a-z0-9]+:\\S+$"
    },
    "Resource": {
      "title": "Resource",
      "description": "A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`, `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied: `acme/widgets` alone is not a resource. Containment is by whole segment: `github.com/acme` contains `github.com/acme/widgets` and does not contain `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.",
      "type": "string",
      "maxLength": 455,
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+(?:/[a-z0-9_-][a-z0-9._-]{0,99}){1,2}$"
    },
    "Right": {
      "title": "Right",
      "description": "One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.",
      "type": "string",
      "enum": [
        "git.ns.admin",
        "git.repo.create",
        "git.repo.own",
        "git.repo.maintain",
        "git.commit.sign"
      ]
    },
    "NamespaceId": {
      "title": "NamespaceId",
      "description": "The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Git Namespaces — Reseat Namespace — response payload",
      "description": "The recorded git.ns.admin right.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "right"
      ],
      "properties": {
        "right": {
          "$ref": "#/$defs/RightRecord",
          "description": "The git.ns.admin record the reseat created. It never carries `expiresAt`."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "RightRecord": {
      "title": "RightRecord",
      "description": "One recorded right. Implied rights (§4.2 of the rights model: `own` implies `maintain` implies `commit.sign` on the same resource; `ns.admin` implies `repo.create` and `own` across its namespace) are not records and never appear as RightRecords.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "subject",
        "right",
        "resource",
        "grantedBy",
        "grantedAt"
      ],
      "properties": {
        "subject": {
          "$ref": "#/$defs/Did",
          "description": "Who holds the right. For `git.commit.sign` this is the DID whose commit signatures the CI check accepts."
        },
        "right": {
          "$ref": "#/$defs/Right"
        },
        "resource": {
          "$ref": "#/$defs/Resource"
        },
        "grantedBy": {
          "$ref": "#/$defs/Did",
          "description": "The actor whose task caused the right: the granter, the creator of a repository (for its first `own`), the adopting admin, the transferring owner, or the binding admin (for the first `git.ns.admin`). The VTC's own DID for a right it derives from its configuration."
        },
        "grantedAt": {
          "type": "string",
          "format": "date-time"
        },
        "expiresAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the right lapses. Absent: no expiry."
        },
        "reason": {
          "type": "string",
          "maxLength": 1024,
          "description": "The granter's free-text reason. Disclosed only to holders of `git.repo.own` on the resource and of `git.ns.admin` over it."
        }
      }
    },
    "Did": {
      "title": "Did",
      "description": "A DID, compared by exact string equality.",
      "type": "string",
      "maxLength": 2048,
      "pattern": "^did:[a-z0-9]+:\\S+$"
    },
    "Resource": {
      "title": "Resource",
      "description": "A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`, `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied: `acme/widgets` alone is not a resource. Containment is by whole segment: `github.com/acme` contains `github.com/acme/widgets` and does not contain `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.",
      "type": "string",
      "maxLength": 455,
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+(?:/[a-z0-9_-][a-z0-9._-]{0,99}){1,2}$"
    },
    "Right": {
      "title": "Right",
      "description": "One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.",
      "type": "string",
      "enum": [
        "git.ns.admin",
        "git.repo.create",
        "git.repo.own",
        "git.repo.maintain",
        "git.commit.sign"
      ]
    },
    "NamespaceId": {
      "title": "NamespaceId",
      "description": "The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.",
      "type": "string",
      "minLength": 1,
      "maxLength": 128
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
