/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/git-ns/roles/reproject/0.1/payload.schema.json
 */

import type { Ext, RepoResource, Resource } from "../../../../_shared/components.js";


/**
 * A community administrator, or a namespace admin, has the VTC send its bridge the complete forge roles of one repository, or of every repository in a namespace, again — so the forge comes to match the VTC's rights under the role map the bridge now applies. No right changes.
 */
export interface GitNamespacesReProjectRolesPayload {
  /**
   * A namespace (`<forge>/<owner>`): every active or orphaned repository in it. A repository (`<forge>/<owner>/<repo>`): that repository only.
   */
  resource: Resource;
  /**
   * Why, in the caller's words. Kept with the audit record.
   */
  reason?: string;
  ext?: Ext;
}
/**
 * The repositories the VTC queued a projectRoles job for.
 */
export interface GitNamespacesReProjectRolesResponsePayload {
  /**
   * Each repository a projectRoles job was queued for, with its complete desiredRoles. Empty: the namespace has no active or orphaned repository.
   *
   * @maxItems 10000
   */
  repos: RepoResource[];
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext, RepoResource, Resource };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/git-ns/roles/reproject/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = GitNamespacesReProjectRolesPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/git-ns/roles/reproject/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = GitNamespacesReProjectRolesResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/git-ns/roles/reproject/0.1",
  "title": "Git Namespaces — Re-project Roles — payload",
  "description": "A community administrator, or a namespace admin, has the VTC send its bridge the complete forge roles of one repository, or of every repository in a namespace, again — so the forge comes to match the VTC's rights under the role map the bridge now applies. No right changes.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "resource"
  ],
  "properties": {
    "resource": {
      "$ref": "#/$defs/Resource",
      "description": "A namespace (`<forge>/<owner>`): every active or orphaned repository in it. A repository (`<forge>/<owner>/<repo>`): that repository only."
    },
    "reason": {
      "type": "string",
      "minLength": 1,
      "maxLength": 1024,
      "description": "Why, in the caller's words. Kept with the audit record."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Git Namespaces — Re-project Roles — response payload",
      "description": "The repositories the VTC queued a projectRoles job for.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "repos"
      ],
      "properties": {
        "repos": {
          "type": "array",
          "maxItems": 10000,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/RepoResource"
          },
          "description": "Each repository a projectRoles job was queued for, with its complete desiredRoles. Empty: the namespace has no active or orphaned repository."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "RepoResource": {
      "title": "RepoResource",
      "description": "A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.",
      "type": "string",
      "maxLength": 455,
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}$"
    },
    "Resource": {
      "title": "Resource",
      "description": "A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`, `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied: `acme/widgets` alone is not a resource. Containment is by whole segment: `github.com/acme` contains `github.com/acme/widgets` and does not contain `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.",
      "type": "string",
      "maxLength": 455,
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+(?:/[a-z0-9_-][a-z0-9._-]{0,99}){1,2}$"
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Git Namespaces — Re-project Roles — response payload",
      "description": "The repositories the VTC queued a projectRoles job for.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "repos"
      ],
      "properties": {
        "repos": {
          "type": "array",
          "maxItems": 10000,
          "uniqueItems": true,
          "items": {
            "$ref": "#/$defs/RepoResource"
          },
          "description": "Each repository a projectRoles job was queued for, with its complete desiredRoles. Empty: the namespace has no active or orphaned repository."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "RepoResource": {
      "title": "RepoResource",
      "description": "A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.",
      "type": "string",
      "maxLength": 455,
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}$"
    },
    "Resource": {
      "title": "Resource",
      "description": "A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`, `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied: `acme/widgets` alone is not a resource. Containment is by whole segment: `github.com/acme` contains `github.com/acme/widgets` and does not contain `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.",
      "type": "string",
      "maxLength": 455,
      "pattern": "^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+(?:/[a-z0-9_-][a-z0-9._-]{0,99}){1,2}$"
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
