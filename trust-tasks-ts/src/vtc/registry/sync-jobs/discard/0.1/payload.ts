/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/registry/sync-jobs/discard/0.1/payload.schema.json
 */

import type { Ext } from "../../../../../_shared/components.js";


/**
 * Delete an abandoned trust-registry reconciliation job without dispatching it. The registry's record for that member is left exactly as it is.
 */
export interface VTCRegistrySyncJobsDiscardPayload {
  /**
   * The job to delete, from `vtc/registry/sync-jobs/list`. Required and single: discard is irreversible, so there is deliberately no bulk form.
   */
  jobId: string;
  ext?: Ext;
}
export interface VTCRegistrySyncJobsDiscardResponsePayload {
  /**
   * The deleted job, echoed.
   */
  jobId: string;
  /**
   * The member whose pending change was dropped. Returned because the caller is discarding by identifier and this names what it actually cost.
   */
  memberDid: string;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/registry/sync-jobs/discard/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCRegistrySyncJobsDiscardPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/registry/sync-jobs/discard/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCRegistrySyncJobsDiscardResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/registry/sync-jobs/discard/0.1",
  "title": "VTC Registry Sync Jobs Discard — payload",
  "description": "Delete an abandoned trust-registry reconciliation job without dispatching it. The registry's record for that member is left exactly as it is.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "jobId"
  ],
  "properties": {
    "jobId": {
      "type": "string",
      "minLength": 1,
      "description": "The job to delete, from `vtc/registry/sync-jobs/list`. Required and single: discard is irreversible, so there is deliberately no bulk form."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Registry Sync Jobs Discard — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId",
        "memberDid"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1,
          "description": "The deleted job, echoed."
        },
        "memberDid": {
          "type": "string",
          "minLength": 1,
          "description": "The member whose pending change was dropped. Returned because the caller is discarding by identifier and this names what it actually cost."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Registry Sync Jobs Discard — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId",
        "memberDid"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1,
          "description": "The deleted job, echoed."
        },
        "memberDid": {
          "type": "string",
          "minLength": 1,
          "description": "The member whose pending change was dropped. Returned because the caller is discarding by identifier and this names what it actually cost."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
