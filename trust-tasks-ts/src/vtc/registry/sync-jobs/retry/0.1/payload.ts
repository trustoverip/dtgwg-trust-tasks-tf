/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/registry/sync-jobs/retry/0.1/payload.schema.json
 */

import type { Ext } from "../../../../../_shared/components.js";


/**
 * Requeue an abandoned trust-registry reconciliation job so the community's reconciler dispatches it again.
 */
export type VTCRegistrySyncJobsRetryPayload = {
  /**
   * The single job to requeue, from `vtc/registry/sync-jobs/list`.
   */
  jobId?: string;
  /**
   * Requeue every job in the terminal `failed` state. Spelled as its own member rather than an absent `jobId` so that a client bug which drops the identifier cannot silently become a bulk operation.
   */
  allFailed?: true;
  ext?: Ext;
} & {
  [k: string]: unknown | undefined;
};

export interface VTCRegistrySyncJobsRetryResponsePayload {
  /**
   * Jobs now scheduled for dispatch. Empty is a valid answer and is not an error.
   */
  requeued: Requeued[];
  /**
   * Jobs the maintainer declined to requeue, each with why. Reported rather than failing the task, so a bulk retry is not defeated by one ineligible row.
   */
  skipped: Skipped[];
  ext?: Ext;
}
export interface Requeued {
  jobId: string;
  /**
   * The member whose record the requeued job carries.
   */
  memberDid?: string;
  ext?: Ext;
}
export interface Skipped {
  jobId: string;
  /**
   * `notFailed` — the reconciler still owns the job, so requeueing it would dispatch the same mutation twice. `notFound` — no such job; it may already have been retried, discarded, or swept.
   */
  reason: "notFailed" | "notFound";
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/registry/sync-jobs/retry/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCRegistrySyncJobsRetryPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/registry/sync-jobs/retry/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCRegistrySyncJobsRetryResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/registry/sync-jobs/retry/0.1",
  "title": "VTC Registry Sync Jobs Retry — payload",
  "description": "Requeue an abandoned trust-registry reconciliation job so the community's reconciler dispatches it again.",
  "type": "object",
  "additionalProperties": false,
  "oneOf": [
    {
      "required": [
        "jobId"
      ],
      "not": {
        "required": [
          "allFailed"
        ]
      }
    },
    {
      "required": [
        "allFailed"
      ],
      "not": {
        "required": [
          "jobId"
        ]
      }
    }
  ],
  "properties": {
    "jobId": {
      "type": "string",
      "minLength": 1,
      "description": "The single job to requeue, from `vtc/registry/sync-jobs/list`."
    },
    "allFailed": {
      "type": "boolean",
      "const": true,
      "description": "Requeue every job in the terminal `failed` state. Spelled as its own member rather than an absent `jobId` so that a client bug which drops the identifier cannot silently become a bulk operation."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Requeued": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1
        },
        "memberDid": {
          "type": "string",
          "minLength": 1,
          "description": "The member whose record the requeued job carries."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Skipped": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId",
        "reason"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1
        },
        "reason": {
          "type": "string",
          "enum": [
            "notFailed",
            "notFound"
          ],
          "description": "`notFailed` — the reconciler still owns the job, so requeueing it would dispatch the same mutation twice. `notFound` — no such job; it may already have been retried, discarded, or swept."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Response": {
      "$anchor": "response",
      "title": "VTC Registry Sync Jobs Retry — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "requeued",
        "skipped"
      ],
      "properties": {
        "requeued": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Requeued"
          },
          "description": "Jobs now scheduled for dispatch. Empty is a valid answer and is not an error."
        },
        "skipped": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Skipped"
          },
          "description": "Jobs the maintainer declined to requeue, each with why. Reported rather than failing the task, so a bulk retry is not defeated by one ineligible row."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Requeued": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1
        },
        "memberDid": {
          "type": "string",
          "minLength": 1,
          "description": "The member whose record the requeued job carries."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Skipped": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId",
        "reason"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1
        },
        "reason": {
          "type": "string",
          "enum": [
            "notFailed",
            "notFound"
          ],
          "description": "`notFailed` — the reconciler still owns the job, so requeueing it would dispatch the same mutation twice. `notFound` — no such job; it may already have been retried, discarded, or swept."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Response": {
      "$anchor": "response",
      "title": "VTC Registry Sync Jobs Retry — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "requeued",
        "skipped"
      ],
      "properties": {
        "requeued": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Requeued"
          },
          "description": "Jobs now scheduled for dispatch. Empty is a valid answer and is not an error."
        },
        "skipped": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Skipped"
          },
          "description": "Jobs the maintainer declined to requeue, each with why. Reported rather than failing the task, so a bulk retry is not defeated by one ineligible row."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
