/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/registry/sync-jobs/list/0.1/payload.schema.json
 */

import type { Ext } from "../../../../../_shared/components.js";


/**
 * Which mutation the job carries to the registry.
 */
export type Kind = "publishMember" | "updateMember" | "deleteMember" | "markDeparted";
/**
 * `failed` is terminal: the reconciler has abandoned the job and will not retry it without an operator. `pending` and `inFlight` are moving on their own.
 */
export type State = "pending" | "inFlight" | "failed";

/**
 * Enumerate the community's trust-registry reconciliation queue. Each job is one pending or abandoned attempt to publish a membership change to the registry.
 */
export interface VTCRegistrySyncJobsListPayload {
  /**
   * Filter to jobs in this state. Omit for every state.
   */
  state?: "pending" | "inFlight" | "failed";
  /**
   * Opaque continuation token from a previous page's `nextCursor`.
   */
  cursor?: string;
  /**
   * Page size; the maintainer clamps to 1..=200 (default 50).
   */
  limit?: number;
  ext?: Ext;
}
export interface VTCRegistrySyncJobsListResponsePayload {
  items: Job[];
  /**
   * Continuation token for the next page, or null when this is the last.
   */
  nextCursor?: string | null;
  ext?: Ext;
}
export interface Job {
  /**
   * Identifier of the queued job. The handle `vtc/registry/sync-jobs/retry` and `.../discard` take.
   */
  jobId: string;
  kind: Kind;
  /**
   * The member whose registry record the job carries. In the clear, because an operator holding a hash cannot tell who stopped publishing.
   */
  memberDid: string;
  state: State;
  /**
   * Attempts made so far. `1` on a failed job means the registry answered and refused; a value at the maintainer's retry ceiling means it never answered.
   */
  attempts: number;
  /**
   * The registry's last answer, verbatim. Diagnostic prose for an operator; a consumer MUST NOT parse it.
   */
  lastError?: string | null;
  /**
   * When the job was enqueued.
   */
  createdAt: string;
  /**
   * When it was last dispatched.
   */
  lastAttemptedAt?: string | null;
  /**
   * When it is next due. Null on a failed job: nothing is scheduled.
   */
  nextAttemptAt?: string | null;
  /**
   * When the maintainer's retention sweep will delete this row. A deadline, not a fix — the member stays unpublished.
   */
  purgeDueAt?: string | null;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/registry/sync-jobs/list/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCRegistrySyncJobsListPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/registry/sync-jobs/list/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCRegistrySyncJobsListResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/registry/sync-jobs/list/0.1",
  "title": "VTC Registry Sync Jobs List — payload",
  "description": "Enumerate the community's trust-registry reconciliation queue. Each job is one pending or abandoned attempt to publish a membership change to the registry.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "state": {
      "$ref": "#/$defs/State",
      "description": "Filter to jobs in this state. Omit for every state."
    },
    "cursor": {
      "type": "string",
      "description": "Opaque continuation token from a previous page's `nextCursor`."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 200,
      "description": "Page size; the maintainer clamps to 1..=200 (default 50)."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "State": {
      "type": "string",
      "enum": [
        "pending",
        "inFlight",
        "failed"
      ],
      "description": "`failed` is terminal: the reconciler has abandoned the job and will not retry it without an operator. `pending` and `inFlight` are moving on their own."
    },
    "Kind": {
      "type": "string",
      "enum": [
        "publishMember",
        "updateMember",
        "deleteMember",
        "markDeparted"
      ],
      "description": "Which mutation the job carries to the registry."
    },
    "Job": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId",
        "kind",
        "memberDid",
        "state",
        "attempts",
        "createdAt"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier of the queued job. The handle `vtc/registry/sync-jobs/retry` and `.../discard` take."
        },
        "kind": {
          "$ref": "#/$defs/Kind"
        },
        "memberDid": {
          "type": "string",
          "minLength": 1,
          "description": "The member whose registry record the job carries. In the clear, because an operator holding a hash cannot tell who stopped publishing."
        },
        "state": {
          "$ref": "#/$defs/State"
        },
        "attempts": {
          "type": "integer",
          "minimum": 0,
          "description": "Attempts made so far. `1` on a failed job means the registry answered and refused; a value at the maintainer's retry ceiling means it never answered."
        },
        "lastError": {
          "type": [
            "string",
            "null"
          ],
          "maxLength": 1024,
          "description": "The registry's last answer, verbatim. Diagnostic prose for an operator; a consumer MUST NOT parse it."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the job was enqueued."
        },
        "lastAttemptedAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When it was last dispatched."
        },
        "nextAttemptAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When it is next due. Null on a failed job: nothing is scheduled."
        },
        "purgeDueAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When the maintainer's retention sweep will delete this row. A deadline, not a fix — the member stays unpublished."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Response": {
      "$anchor": "response",
      "title": "VTC Registry Sync Jobs List — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "items"
      ],
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Job"
          }
        },
        "nextCursor": {
          "type": [
            "string",
            "null"
          ],
          "description": "Continuation token for the next page, or null when this is the last."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "State": {
      "type": "string",
      "enum": [
        "pending",
        "inFlight",
        "failed"
      ],
      "description": "`failed` is terminal: the reconciler has abandoned the job and will not retry it without an operator. `pending` and `inFlight` are moving on their own."
    },
    "Kind": {
      "type": "string",
      "enum": [
        "publishMember",
        "updateMember",
        "deleteMember",
        "markDeparted"
      ],
      "description": "Which mutation the job carries to the registry."
    },
    "Job": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "jobId",
        "kind",
        "memberDid",
        "state",
        "attempts",
        "createdAt"
      ],
      "properties": {
        "jobId": {
          "type": "string",
          "minLength": 1,
          "description": "Identifier of the queued job. The handle `vtc/registry/sync-jobs/retry` and `.../discard` take."
        },
        "kind": {
          "$ref": "#/$defs/Kind"
        },
        "memberDid": {
          "type": "string",
          "minLength": 1,
          "description": "The member whose registry record the job carries. In the clear, because an operator holding a hash cannot tell who stopped publishing."
        },
        "state": {
          "$ref": "#/$defs/State"
        },
        "attempts": {
          "type": "integer",
          "minimum": 0,
          "description": "Attempts made so far. `1` on a failed job means the registry answered and refused; a value at the maintainer's retry ceiling means it never answered."
        },
        "lastError": {
          "type": [
            "string",
            "null"
          ],
          "maxLength": 1024,
          "description": "The registry's last answer, verbatim. Diagnostic prose for an operator; a consumer MUST NOT parse it."
        },
        "createdAt": {
          "type": "string",
          "format": "date-time",
          "description": "When the job was enqueued."
        },
        "lastAttemptedAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When it was last dispatched."
        },
        "nextAttemptAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When it is next due. Null on a failed job: nothing is scheduled."
        },
        "purgeDueAt": {
          "type": [
            "string",
            "null"
          ],
          "format": "date-time",
          "description": "When the maintainer's retention sweep will delete this row. A deadline, not a fix — the member stays unpublished."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Response": {
      "$anchor": "response",
      "title": "VTC Registry Sync Jobs List — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "items"
      ],
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "$ref": "#/$defs/Job"
          }
        },
        "nextCursor": {
          "type": [
            "string",
            "null"
          ],
          "description": "Continuation token for the next page, or null when this is the last."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
