/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/vetting/pcs-challenge/0.1/payload.schema.json
 */

import type { Ext } from "../../../../_shared/components.js";


/**
 * An applicant asks the community for the single-use challenge its hidden-vetting proof must be bound to. The community issues one per applicant and spends it when the proof is counted, so a proof cannot be submitted twice.
 */
export interface VTCVettingPCSChallengePayload {
  /**
   * OPTIONAL. The `id` of the admission criterion the coming submission answers. A community running one hidden-vetting criterion needs no hint; one running several may use it to refuse early rather than at submit.
   */
  criterionId?: string;
  ext?: Ext;
}
/**
 * The challenge, and when it stops being accepted.
 */
export interface VTCVettingPCSChallengeResponsePayload {
  /**
   * Lowercase hex, 16 bytes. Compared for equality and never parsed; the applicant binds it into the proof's context.
   */
  challenge: string;
  /**
   * After this instant the challenge is refused. A submission that misses it asks for another.
   */
  expiresAt: string;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/vetting/pcs-challenge/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCVettingPCSChallengePayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/vetting/pcs-challenge/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCVettingPCSChallengeResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/vetting/pcs-challenge/0.1",
  "title": "VTC — Vetting PCS Challenge — payload",
  "description": "An applicant asks the community for the single-use challenge its hidden-vetting proof must be bound to. The community issues one per applicant and spends it when the proof is counted, so a proof cannot be submitted twice.",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "criterionId": {
      "type": "string",
      "minLength": 1,
      "maxLength": 128,
      "description": "OPTIONAL. The `id` of the admission criterion the coming submission answers. A community running one hidden-vetting criterion needs no hint; one running several may use it to refuse early rather than at submit."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Vetting PCS Challenge — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "challenge",
        "expiresAt"
      ],
      "description": "The challenge, and when it stops being accepted.",
      "properties": {
        "challenge": {
          "type": "string",
          "pattern": "^[0-9a-f]{32}$",
          "description": "Lowercase hex, 16 bytes. Compared for equality and never parsed; the applicant binds it into the proof's context."
        },
        "expiresAt": {
          "type": "string",
          "format": "date-time",
          "description": "After this instant the challenge is refused. A submission that misses it asks for another."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Vetting PCS Challenge — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "challenge",
        "expiresAt"
      ],
      "description": "The challenge, and when it stops being accepted.",
      "properties": {
        "challenge": {
          "type": "string",
          "pattern": "^[0-9a-f]{32}$",
          "description": "Lowercase hex, 16 bytes. Compared for equality and never parsed; the applicant binds it into the proof's context."
        },
        "expiresAt": {
          "type": "string",
          "format": "date-time",
          "description": "After this instant the challenge is refused. A submission that misses it asks for another."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
