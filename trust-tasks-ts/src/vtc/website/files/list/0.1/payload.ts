/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/website/files/list/0.1/payload.schema.json
 */

import type { Ext } from "../../../../../_shared/components.js";


export interface VTCWebsiteFilesListPayload {
  /**
   * Opaque continuation token from a prior page.
   */
  cursor?: string;
  /**
   * Page size.
   */
  limit?: number;
  ext?: Ext;
}
export interface VTCWebsiteFilesListResponsePayload {
  /**
   * Files under the site root, excluding hidden files and blocklisted extensions.
   */
  items: {
    /**
     * Path relative to the site root.
     */
    path: string;
    /**
     * File size in bytes.
     */
    size: number;
    /**
     * An opaque content hash for the file, matching the `ETag` a direct read of the same path returns. Lets a client detect a change — or confirm one it just wrote — without downloading the body, which is why the listing carries it rather than making the caller fetch each file to compare.
     */
    etag?: string;
    modifiedAt?: string;
  }[];
  nextCursor?: string | null;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/website/files/list/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCWebsiteFilesListPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/website/files/list/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCWebsiteFilesListResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/website/files/list/0.1",
  "title": "VTC Website Files List — payload",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "cursor": {
      "type": "string",
      "description": "Opaque continuation token from a prior page."
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 1000,
      "description": "Page size."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Website Files List — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "items"
      ],
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "path",
              "size"
            ],
            "properties": {
              "path": {
                "type": "string",
                "minLength": 1,
                "description": "Path relative to the site root."
              },
              "size": {
                "type": "integer",
                "minimum": 0,
                "description": "File size in bytes."
              },
              "etag": {
                "type": "string",
                "minLength": 1,
                "description": "An opaque content hash for the file, matching the `ETag` a direct read of the same path returns. Lets a client detect a change — or confirm one it just wrote — without downloading the body, which is why the listing carries it rather than making the caller fetch each file to compare."
              },
              "modifiedAt": {
                "type": "string",
                "format": "date-time"
              }
            }
          },
          "description": "Files under the site root, excluding hidden files and blocklisted extensions."
        },
        "nextCursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Website Files List — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "items"
      ],
      "properties": {
        "items": {
          "type": "array",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "path",
              "size"
            ],
            "properties": {
              "path": {
                "type": "string",
                "minLength": 1,
                "description": "Path relative to the site root."
              },
              "size": {
                "type": "integer",
                "minimum": 0,
                "description": "File size in bytes."
              },
              "etag": {
                "type": "string",
                "minLength": 1,
                "description": "An opaque content hash for the file, matching the `ETag` a direct read of the same path returns. Lets a client detect a change — or confirm one it just wrote — without downloading the body, which is why the listing carries it rather than making the caller fetch each file to compare."
              },
              "modifiedAt": {
                "type": "string",
                "format": "date-time"
              }
            }
          },
          "description": "Files under the site root, excluding hidden files and blocklisted extensions."
        },
        "nextCursor": {
          "type": [
            "string",
            "null"
          ]
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
