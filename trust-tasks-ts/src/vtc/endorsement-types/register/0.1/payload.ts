/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/endorsement-types/register/0.1/payload.schema.json
 */

export interface VTCEndorsementTypesRegisterPayload {
  typeUri: string;
  description?: string;
  /**
   * Optional JSON Schema the endorsement's claims must satisfy (opaque here).
   */
  claimSchema?: {};
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}
export interface VTCEndorsementTypesRegisterResponsePayload {
  endorsementType: EndorsementType;
  ext?: Ext;
}
export interface EndorsementType {
  /**
   * The endorsement type's URI. Community-scoped; workspace-reserved URIs (e.g. CommunityRole) are refused at registration.
   */
  typeUri: string;
  description?: string;
  /**
   * Optional JSON Schema the endorsement's claims must satisfy (opaque here).
   */
  claimSchema?: {};
  createdAt?: string;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/endorsement-types/register/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCEndorsementTypesRegisterPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/endorsement-types/register/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCEndorsementTypesRegisterResponsePayload;
