/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/policies/test/0.1/payload.schema.json
 */

import type { Ext } from "../../../../_shared/components.js";


export interface VTCPoliciesTestPayload {
  /**
   * Identifier of the stored policy module to evaluate.
   */
  id: string;
  /**
   * Rule to evaluate. Any rule in the module, not only an allow decision.
   */
  query: string;
  /**
   * Facts to evaluate against. Deliberately schema-free: community policies decide over community-shaped input.
   */
  input: {
    [k: string]: unknown | undefined;
  };
  ext?: Ext;
}
export interface VTCPoliciesTestResponsePayload {
  /**
   * The evaluated module's identifier.
   */
  id: string;
  /**
   * Governance purpose the module is classified under.
   */
  purpose: string;
  /**
   * Digest of the evaluated module, so a caller can confirm which bytes ran.
   */
  sha256: string;
  /**
   * Raw result of the queried rule, uninterpreted. May be any JSON value, including absent-as-null.
   */
  result: {
    [k: string]: unknown | undefined;
  };
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/policies/test/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = VTCPoliciesTestPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/policies/test/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = VTCPoliciesTestResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/policies/test/0.1",
  "title": "VTC Policies Test — payload",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "id",
    "query",
    "input"
  ],
  "properties": {
    "id": {
      "type": "string",
      "minLength": 1,
      "description": "Identifier of the stored policy module to evaluate."
    },
    "query": {
      "type": "string",
      "minLength": 1,
      "description": "Rule to evaluate. Any rule in the module, not only an allow decision."
    },
    "input": {
      "description": "Facts to evaluate against. Deliberately schema-free: community policies decide over community-shaped input."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Policies Test — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "purpose",
        "sha256",
        "result"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "The evaluated module's identifier."
        },
        "purpose": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128,
          "description": "Governance purpose the module is classified under."
        },
        "sha256": {
          "type": "string",
          "minLength": 1,
          "description": "Digest of the evaluated module, so a caller can confirm which bytes ran."
        },
        "result": {
          "description": "Raw result of the queried rule, uninterpreted. May be any JSON value, including absent-as-null."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Policies Test — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "id",
        "purpose",
        "sha256",
        "result"
      ],
      "properties": {
        "id": {
          "type": "string",
          "minLength": 1,
          "description": "The evaluated module's identifier."
        },
        "purpose": {
          "type": "string",
          "minLength": 1,
          "maxLength": 128,
          "description": "Governance purpose the module is classified under."
        },
        "sha256": {
          "type": "string",
          "minLength": 1,
          "description": "Digest of the evaluated module, so a caller can confirm which bytes ran."
        },
        "result": {
          "description": "Raw result of the queried rule, uninterpreted. May be any JSON value, including absent-as-null."
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
