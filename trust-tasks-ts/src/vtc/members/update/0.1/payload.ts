/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/vtc/members/update/0.1/payload.schema.json
 */

export interface VTCMembersUpdatePayload {
  /**
   * The member to update.
   */
  did: string;
  /**
   * New role (wire form). MUST NOT be `admin` — promotion to admin is a separate, gated flow, not a metadata patch.
   */
  role?: string;
  /**
   * New directory-publish consent.
   */
  publishConsent?: boolean;
  departurePreference?: "purge" | "tombstone" | "historical" | "policydefault";
  /**
   * Replacement extension bag (maintainer-capped, 16 KiB in the reference implementation).
   */
  extensions?: {};
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/vtc/members/update/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/vtc/members/update/0.1#response" as const;
