/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/persona/persona/list/1.0/payload.schema.json
 */

import type { Ext } from "../../../../_shared/components.js";


/**
 * Enumerate the personas present in one context. This context only — an application learns which identities operate where it does, and nothing about anywhere else the holder operates.
 */
export interface PersonaListPayload {
  contextId: string;
  cursor?: string;
  limit?: number;
  ext?: Ext;
}
/**
 * Success response to persona/persona/list. Type https://trusttasks.org/spec/persona/persona/list/1.0#response. Carries the same thin summary as persona/binding/get, for the same reason: an application needs to know which identity is in use, not what it contains.
 */
export interface PersonaListResponsePayload {
  /**
   * @maxItems 500
   */
  personas: {
    personaDid: string;
    bound: boolean;
    /**
     * The holder's label for the bound profile. Names a composition; reveals none of it.
     */
    profileName?: string;
    claimCount?: number;
    /**
     * True when the bound profile is context-local rather than imported from the holder's pool. Worth surfacing: a local persona is one the application itself may have composed, and an application should be able to tell what it authored from what the holder pushed down.
     */
    isLocal?: boolean;
  }[];
  nextCursor?: string;
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/persona/persona/list/1.0" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = PersonaListPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/persona/persona/list/1.0#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = PersonaListResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/persona/persona/list/1.0",
  "title": "Persona List — payload",
  "description": "Enumerate the personas present in one context. This context only — an application learns which identities operate where it does, and nothing about anywhere else the holder operates.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "contextId"
  ],
  "properties": {
    "contextId": {
      "type": "string",
      "minLength": 1
    },
    "cursor": {
      "type": "string",
      "maxLength": 4096
    },
    "limit": {
      "type": "integer",
      "minimum": 1,
      "maximum": 500,
      "default": 100
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Persona List — response payload",
      "description": "Success response to persona/persona/list. Type https://trusttasks.org/spec/persona/persona/list/1.0#response. Carries the same thin summary as persona/binding/get, for the same reason: an application needs to know which identity is in use, not what it contains.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "personas"
      ],
      "properties": {
        "personas": {
          "type": "array",
          "maxItems": 500,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "personaDid",
              "bound"
            ],
            "properties": {
              "personaDid": {
                "type": "string",
                "minLength": 1
              },
              "bound": {
                "type": "boolean"
              },
              "profileName": {
                "type": "string",
                "maxLength": 128,
                "description": "The holder's label for the bound profile. Names a composition; reveals none of it."
              },
              "claimCount": {
                "type": "integer",
                "minimum": 0
              },
              "isLocal": {
                "type": "boolean",
                "description": "True when the bound profile is context-local rather than imported from the holder's pool. Worth surfacing: a local persona is one the application itself may have composed, and an application should be able to tell what it authored from what the holder pushed down."
              }
            }
          }
        },
        "nextCursor": {
          "type": "string",
          "maxLength": 4096
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Persona List — response payload",
      "description": "Success response to persona/persona/list. Type https://trusttasks.org/spec/persona/persona/list/1.0#response. Carries the same thin summary as persona/binding/get, for the same reason: an application needs to know which identity is in use, not what it contains.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "personas"
      ],
      "properties": {
        "personas": {
          "type": "array",
          "maxItems": 500,
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "personaDid",
              "bound"
            ],
            "properties": {
              "personaDid": {
                "type": "string",
                "minLength": 1
              },
              "bound": {
                "type": "boolean"
              },
              "profileName": {
                "type": "string",
                "maxLength": 128,
                "description": "The holder's label for the bound profile. Names a composition; reveals none of it."
              },
              "claimCount": {
                "type": "integer",
                "minimum": 0
              },
              "isLocal": {
                "type": "boolean",
                "description": "True when the bound profile is context-local rather than imported from the holder's pool. Worth surfacing: a local persona is one the application itself may have composed, and an application should be able to tell what it authored from what the holder pushed down."
              }
            }
          }
        },
        "nextCursor": {
          "type": "string",
          "maxLength": 4096
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
