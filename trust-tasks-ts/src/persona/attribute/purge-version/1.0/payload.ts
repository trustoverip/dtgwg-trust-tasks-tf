/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/persona/attribute/purge-version/1.0/payload.schema.json
 */

import type { Ext, Ulid, Version_PersonaV0_1 as Version } from "../../../../_shared/components.js";


/**
 * Permanently remove earlier versions of one attribute that the maintainer kept because a profile pins them. The holder's override on retention: after a name change, the old name is kept only while something needs it, and this is how the holder says nothing does.
 */
export interface PersonaAttributePurgeVersionPayload {
  attributeId: Ulid;
  /**
   * The retained versions to remove. Omit to remove every retained version of the attribute. The current version is never removed by this task — that is persona/attribute/delete.
   *
   * @minItems 1
   * @maxItems 64
   */
  versions?: [Version, ...Version[]];
  ext?: Ext;
}
/**
 * Success response to persona/attribute/purge-version. Type https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response. Purging a version that is not held is a success that purges nothing, so a repeated purge converges.
 */
export interface PersonaAttributePurgeVersionResponsePayload {
  attributeId: Ulid;
  /**
   * The versions actually removed. Empty when none named was held.
   *
   * @maxItems 64
   */
  purged: Version[];
  /**
   * Profiles that pinned a purged version and now present that entry as stale — which is to say, disclose nothing for it until the holder repins or edits them. Named because a purge that quietly shortened what three faces present is the surprise this family exists to prevent. Absent when none did.
   *
   * @maxItems 256
   */
  stalePins?: {
    profileId: Ulid;
    pinVersion: Version;
  }[];
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext, Ulid, Version };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/persona/attribute/purge-version/1.0" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = PersonaAttributePurgeVersionPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = PersonaAttributePurgeVersionResponsePayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/persona/attribute/purge-version/1.0",
  "title": "Persona Attribute Purge Version — payload",
  "description": "Permanently remove earlier versions of one attribute that the maintainer kept because a profile pins them. The holder's override on retention: after a name change, the old name is kept only while something needs it, and this is how the holder says nothing does.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "attributeId"
  ],
  "properties": {
    "attributeId": {
      "$ref": "#/$defs/Ulid"
    },
    "versions": {
      "type": "array",
      "minItems": 1,
      "maxItems": 64,
      "uniqueItems": true,
      "items": {
        "$ref": "#/$defs/Version"
      },
      "description": "The retained versions to remove. Omit to remove every retained version of the attribute. The current version is never removed by this task — that is persona/attribute/delete."
    },
    "ext": {
      "$ref": "#/$defs/Ext"
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Persona Attribute Purge Version — response payload",
      "description": "Success response to persona/attribute/purge-version. Type https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response. Purging a version that is not held is a success that purges nothing, so a repeated purge converges.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "attributeId",
        "purged"
      ],
      "properties": {
        "attributeId": {
          "$ref": "#/$defs/Ulid"
        },
        "purged": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "$ref": "#/$defs/Version"
          },
          "description": "The versions actually removed. Empty when none named was held."
        },
        "stalePins": {
          "type": "array",
          "maxItems": 256,
          "description": "Profiles that pinned a purged version and now present that entry as stale — which is to say, disclose nothing for it until the holder repins or edits them. Named because a purge that quietly shortened what three faces present is the surprise this family exists to prevent. Absent when none did.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "profileId",
              "pinVersion"
            ],
            "properties": {
              "profileId": {
                "$ref": "#/$defs/Ulid"
              },
              "pinVersion": {
                "$ref": "#/$defs/Version"
              }
            }
          }
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "Version": {
      "title": "Version",
      "description": "A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.",
      "type": "integer",
      "minimum": 1
    },
    "Ulid": {
      "title": "Ulid",
      "description": "A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.",
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"
    }
  }
} as const;

/** As {@link PAYLOAD_SCHEMA}, for the success-response variant. */
export const RESPONSE_PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$ref": "#/$defs/Response",
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "Persona Attribute Purge Version — response payload",
      "description": "Success response to persona/attribute/purge-version. Type https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response. Purging a version that is not held is a success that purges nothing, so a repeated purge converges.",
      "type": "object",
      "additionalProperties": false,
      "required": [
        "attributeId",
        "purged"
      ],
      "properties": {
        "attributeId": {
          "$ref": "#/$defs/Ulid"
        },
        "purged": {
          "type": "array",
          "maxItems": 64,
          "items": {
            "$ref": "#/$defs/Version"
          },
          "description": "The versions actually removed. Empty when none named was held."
        },
        "stalePins": {
          "type": "array",
          "maxItems": 256,
          "description": "Profiles that pinned a purged version and now present that entry as stale — which is to say, disclose nothing for it until the holder repins or edits them. Named because a purge that quietly shortened what three faces present is the surprise this family exists to prevent. Absent when none did.",
          "items": {
            "type": "object",
            "additionalProperties": false,
            "required": [
              "profileId",
              "pinVersion"
            ],
            "properties": {
              "profileId": {
                "$ref": "#/$defs/Ulid"
              },
              "pinVersion": {
                "$ref": "#/$defs/Version"
              }
            }
          }
        },
        "ext": {
          "$ref": "#/$defs/Ext"
        }
      }
    },
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    },
    "Version": {
      "title": "Version",
      "description": "A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.",
      "type": "integer",
      "minimum": 1
    },
    "Ulid": {
      "title": "Ulid",
      "description": "A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.",
      "type": "string",
      "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: RESPONSE_PAYLOAD_SCHEMA,
} as const;
