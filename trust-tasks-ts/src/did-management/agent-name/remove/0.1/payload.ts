/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/did-management/agent-name/remove/0.1/payload.schema.json
 */

export interface DIDManagementRemoveAgentNamePayload {
  /**
   * The hosted DID slot.
   */
  mnemonic: string;
  /**
   * The agent name's local part, without the leading `@`.
   */
  name: string;
  /**
   * A new signed document whose `alsoKnownAs` no longer claims the name; published in the same commit that releases the binding.
   */
  didData: string | {};
  /**
   * Optional explicit hosting domain. See category CONVENTIONS §1.
   */
  domain?: string;
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/did-management/agent-name/remove/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/did-management/agent-name/remove/0.1#response" as const;
