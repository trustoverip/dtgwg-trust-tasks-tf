/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/did-management/_shared/0.1/did-method-extensions/webvh.schema.json
 */

/**
 * Schema for the `ext.vnd.trusttasks.did-method-webvh` extension object. When a did-management operation declares `payload.method = "webvh"`, the producer MAY include this extension at any depth that accepts `ext` (top-level payload, DidRecord, DomainEntry, etc.) to carry webvh-specific metadata. Consumers that aren't webvh-aware MUST ignore unknown namespaces per SPEC.md §4.5.1.
 *
 * Status: draft, evolves with the did:webvh method specification (W3C `did-web-vh` draft).
 */
export interface DIDMethodExtensionDidWebvh {
  /**
   * Self-certifying identifier derived from the initial log entry. Identifies the DID independent of its hosting URL.
   */
  scid?: string;
  /**
   * Multibase-encoded Ed25519 public key currently authorised to sign log entries (`parameters.updateKeys[0]`).
   */
  updateKeyMultibase?: string;
  /**
   * Hashes of next-allowed update keys, when the chain commits to a pre-rotation key set.
   */
  nextKeyHashes?: string[];
  /**
   * URLs of witness services that can attest to the log chain.
   */
  witnessUrls?: string[];
  /**
   * Minimum witness count for a valid attestation.
   */
  witnessThreshold?: number;
  /**
   * VersionId of the most recent published log entry.
   */
  latestVersionId?: string;
  latestVersionTime?: string;
  /**
   * When true, the chain is hosted under a portable identifier and may be moved between hosting URLs without changing the canonical DID.
   */
  portable?: boolean;
  /**
   * True iff the latest log entry deactivates the DID per the webvh deactivation protocol.
   */
  deactivated?: boolean;
}
