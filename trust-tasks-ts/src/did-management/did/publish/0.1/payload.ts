/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/did-management/did/publish/0.1/payload.schema.json
 */

export interface DIDManagementPublishPayload {
  mnemonic: string;
  method: string;
  didData: string | {};
  /**
   * Optional explicit hosting domain. Permits 3rd-party callers (e.g. a VTA managing slots across multiple domains on one shared backplane) to disambiguate the target. Resolution chain: explicit → caller ACL default → system default. See [category conventions §1](../../../_shared/0.1/CONVENTIONS.md#1-domain-resolution). Unknown domains return `did-management:unknown_domain`.
   */
  domain?: string;
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/did-management/did/publish/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/did-management/did/publish/0.1#response" as const;
