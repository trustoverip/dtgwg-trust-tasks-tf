/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/did-management/did/register/0.1/payload.schema.json
 */

export interface DIDManagementRegisterPayload {
  /**
   * Local path under which the DID is to be hosted (e.g. `alice`, `tenant/staff/alice`). `.well-known` reserves the root slot — restricted to admin callers.
   */
  path: string;
  /**
   * DID method identifier (e.g. `webvh`, `web`). The shape of `didData` is method-specific.
   */
  method: string;
  /**
   * Method-specific log content. For `webvh`, a JSONL string containing one or more signed log entries. For `web`, the DID document JSON.
   */
  didData: string | {};
  /**
   * Optional explicit hosting domain. When omitted, the host resolves via caller ACL default → system default.
   */
  domain?: string;
  /**
   * Admin-takeover flag. When `true`, an existing slot owned by another party is replaced; requires administrative authority on the slot's hosting domain. Ignored when the slot is free or the caller is already the owner.
   */
  force?: boolean;
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/did-management/did/register/0.1" as const;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/did-management/did/register/0.1#response" as const;
