/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/did-management/server/register/0.1/payload.schema.json
 */

export interface ServerRegisterPayload {
  instanceId: string;
  did: string;
  publicUrl: string;
  servedDomains: string[];
  label?: string;
  /**
   * DID methods the server is willing to host (e.g. ['webvh', 'web']). Omit to default to ['webvh'].
   */
  enabledMethods?: string[];
  /**
   * Wire-protocol revision the server speaks (e.g. '1.0'). Omit to default to '1.0'.
   */
  protocolVersion?: string;
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}
export interface ServerRegisterResponsePayload {
  instanceId: string;
  accepted: boolean;
  lastSeen: string;
  ext?: Ext;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/did-management/server/register/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = ServerRegisterPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/did-management/server/register/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = ServerRegisterResponsePayload;
