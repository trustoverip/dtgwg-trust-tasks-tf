/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/did-management/server/stats-sync/0.1/payload.schema.json
 */

export interface ServerStatsSyncPayload {
  instanceId: string;
  perMnemonic: {
    mnemonic: string;
    resolves: number;
    updates: number;
  }[];
  buckets?: {
    mnemonic: string;
    epoch: number;
    r: number;
    u: number;
  }[];
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}
export interface ServerStatsSyncResponsePayload {
  instanceId: string;
  accepted: number;
  ext?: Ext;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/did-management/server/stats-sync/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = ServerStatsSyncPayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/did-management/server/stats-sync/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = ServerStatsSyncResponsePayload;
