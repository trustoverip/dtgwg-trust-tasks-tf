/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/governance/capability/enable/0.1/payload.schema.json
 */

/**
 * A community operator enables a pluggable capability for a community. Activation registers the capability's Trust Task handlers, advertises it in discovery, and starts its lifecycle hooks; a community with no enable record carries none of the capability's records, surfaces, or attack surface.
 */
export interface GovernanceCapabilityEnablePayload {
  /**
   * Namespace slug of the capability to enable, e.g. `git-trust`.
   */
  capability: string;
  /**
   * Capability version to enable.
   */
  version: string;
  /**
   * Per-community configuration. Opaque to the framework; the recipient MUST validate it against the schema named by the capability manifest's `configSchema` and reject `config_invalid` on mismatch.
   */
  config?: {};
  /**
   * DID of the companion service that serves this capability, for capabilities not built into the host. When present, the host records a namespace delegation to this DID and `manifest` is REQUIRED (the host cannot know a companion's manifest a priori).
   */
  delegate?: string;
  manifest?: CapabilityManifest;
  ext?: Ext;
}
/**
 * The capability's manifest. OPTIONAL for built-in capabilities (the host knows it); REQUIRED when `delegate` is present.
 */
export interface CapabilityManifest {
  /**
   * The capability's namespace slug, e.g. `git-trust`.
   */
  capability: string;
  /**
   * Manifest/capability version, `MAJOR.MINOR`.
   */
  version: string;
  /**
   * Human-readable capability name for management surfaces.
   */
  title?: string;
  /**
   * One-paragraph description for management surfaces.
   */
  description?: string;
  /**
   * Trust Task spec slugs (or `<family>/*` globs) this capability serves when enabled.
   *
   * @minItems 1
   */
  specs: [string, ...string[]];
  /**
   * The trust-registry tuple vocabulary this capability may read and write. A conforming host MUST reject writes by this capability whose `action` is not listed.
   */
  vocabulary?: {
    /**
     * TRQP `action` values this capability owns, e.g. `git.commit.sign`.
     *
     * @minItems 1
     */
    actions: [string, ...string[]];
    /**
     * Human-readable pattern of the `resource` values used, e.g. `<org>[/<repo>]`.
     */
    resourcePattern?: string;
  };
  /**
   * Map of capability operation (e.g. `grant`, `view`) to the community roles allowed to perform it.
   */
  roles?: {
    [k: string]: string[] | undefined;
  };
  /**
   * Membership lifecycle events the capability consumes, e.g. `member.enrolled`, `member.revoked`, `role.changed`.
   */
  lifecycleHooks?: string[];
  /**
   * Map of capability operation to the consent class its execution requires under the host's delegated-execution policy.
   */
  consentClass?: {
    [k: string]: ("normal" | "elevated" | "destructive") | undefined;
  };
  /**
   * Out-of-stack components that consume this capability's trust decisions.
   */
  externalAdapters?: {
    /**
     * Adapter type, e.g. `github-action`, `webhook`.
     */
    kind: string;
    /**
     * Where the adapter lives, e.g. a repository path or URL.
     */
    ref: string;
  }[];
  /**
   * Spec slug of the JSON schema the per-community `config` document must validate against.
   */
  configSchema?: string;
  ext?: Ext;
}
/**
 * Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
 */
export interface Ext {
  [k: string]: unknown | undefined;
}
export interface GovernanceCapabilityEnableResponsePayload {
  capability: string;
  version?: string;
  /**
   * True once the capability is active for the community.
   */
  enabled: boolean;
  /**
   * When the enablement took effect, per the host clock.
   */
  enabledAt?: string;
  /**
   * Additional human-readable detail.
   */
  message?: string;
  ext?: Ext;
}

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/governance/capability/enable/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = GovernanceCapabilityEnablePayload;

/** Trust Task response type URI (request type URI + "#response"). */
export const RESPONSE_TYPE_URI = "https://trusttasks.org/spec/governance/capability/enable/0.1#response" as const;

/** Stable alias for this specification's success-response payload shape. */
export type Response = GovernanceCapabilityEnableResponsePayload;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
} as const;

/**
 * SPEC.md §7.2 policy for the success-response variant. `isRecipientRequired`
 * tracks the *issuer* party's requirement because a response swaps the
 * parties (§7.3 item 5).
 */
export const RESPONSE_SPEC = {
  typeUri: RESPONSE_TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
} as const;
