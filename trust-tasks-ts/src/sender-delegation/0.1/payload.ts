/**
 * Generated by scripts/build-ts-bindings.mjs — DO NOT EDIT BY HAND.
 * Source: specs/sender-delegation/0.1/payload.schema.json
 */

import type { Ext } from "../../_shared/components.js";


/**
 * The document's signer (this document's `issuer`) names the one transport sender permitted to carry the listed Trust Task documents to this document's `recipient`. It confers no authority on the sender: the consumer still authorizes each carried document as its own issuer. Short-lived by construction — the document's `expiresAt` is REQUIRED and at most 300 seconds after its `issuedAt`.
 */
export interface SenderDelegationPayload {
  /**
   * The bare DID (no fragment) of the transport sender permitted to carry the listed documents. A consumer compares it by exact string equality against the transport-authenticated sender of the message that carried them.
   */
  delegate: string;
  /**
   * The `id` of each Trust Task document this delegation covers. A carried document whose `id` is not listed is not covered, however recently the delegation was issued.
   *
   * @minItems 1
   * @maxItems 16
   */
  documents:
    | [string]
    | [string, string]
    | [string, string, string]
    | [string, string, string, string]
    | [string, string, string, string, string]
    | [string, string, string, string, string, string]
    | [string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string, string, string]
    | [string, string, string, string, string, string, string, string, string, string, string, string, string, string]
    | [
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string
      ]
    | [
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string,
        string
      ];
  /**
   * Ecosystem-defined extension members per SPEC.md §4.5.1.
   */
  ext?: Ext;
}

/** Shared definitions this specification references, re-exported under the names it used to declare them with. */
export type { Ext };

/** Trust Task type URI. */
export const TYPE_URI = "https://trusttasks.org/spec/sender-delegation/0.1" as const;

/** Stable alias for this specification's request payload shape. */
export type Payload = SenderDelegationPayload;

/**
 * This specification's payload schema, as a value.
 *
 * SPEC.md §7.2 item 2 is performed against this. It is shipped as data
 * rather than only as a `.json` file because TypeScript types are erased
 * at runtime: without a schema a consumer has nothing to validate, and
 * every REQUIRED payload member is optional in practice. Cross-file
 * `$ref`s are already inlined, so it needs no resolver.
 */
export const PAYLOAD_SCHEMA = {
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/sender-delegation/0.1",
  "title": "Sender Delegation — payload",
  "description": "The document's signer (this document's `issuer`) names the one transport sender permitted to carry the listed Trust Task documents to this document's `recipient`. It confers no authority on the sender: the consumer still authorizes each carried document as its own issuer. Short-lived by construction — the document's `expiresAt` is REQUIRED and at most 300 seconds after its `issuedAt`.",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "delegate",
    "documents"
  ],
  "properties": {
    "delegate": {
      "type": "string",
      "minLength": 1,
      "description": "The bare DID (no fragment) of the transport sender permitted to carry the listed documents. A consumer compares it by exact string equality against the transport-authenticated sender of the message that carried them."
    },
    "documents": {
      "type": "array",
      "minItems": 1,
      "maxItems": 16,
      "uniqueItems": true,
      "items": {
        "type": "string",
        "minLength": 1
      },
      "description": "The `id` of each Trust Task document this delegation covers. A carried document whose `id` is not listed is not covered, however recently the delegation was issued."
    },
    "ext": {
      "$ref": "#/$defs/Ext",
      "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
    }
  },
  "$defs": {
    "Ext": {
      "title": "Ext",
      "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
      "type": "object",
      "minProperties": 1,
      "additionalProperties": true,
      "propertyNames": {
        "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
      }
    }
  }
} as const;

/**
 * SPEC.md §7.2 policy for the request variant, from this specification's
 * front matter. Pass to `consumeInbound` — items 5b, 7 and 8 are
 * per-specification and cannot be derived from the document alone, and
 * item 2 needs the schema this carries.
 */
export const SPEC = {
  typeUri: TYPE_URI,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: PAYLOAD_SCHEMA,
} as const;
