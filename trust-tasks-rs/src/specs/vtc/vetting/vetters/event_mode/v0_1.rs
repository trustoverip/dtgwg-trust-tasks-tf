//! Generated by `trust-tasks-codegen` — do not edit by hand.
//!
//! Spec slug: `vtc/vetting/vetters/event-mode`. Version: `0.1`.
#[allow(unused_imports)]
use serde::{Deserialize, Serialize};
/// Error types.
pub mod error {
    /// Error from a `TryFrom` or `FromStr` implementation.
    pub struct ConversionError(::std::borrow::Cow<'static, str>);
    impl ::std::error::Error for ConversionError {}
    impl ::std::fmt::Display for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Display::fmt(&self.0, f)
        }
    }
    impl ::std::fmt::Debug for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Debug::fmt(&self.0, f)
        }
    }
    impl From<&'static str> for ConversionError {
        fn from(value: &'static str) -> Self {
            Self(value.into())
        }
    }
    impl From<String> for ConversionError {
        fn from(value: String) -> Self {
            Self(value.into())
        }
    }
}
///Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Ext",
///  "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
///  "type": "object",
///  "minProperties": 1,
///  "additionalProperties": true,
///  "propertyNames": {
///    "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///  }
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(transparent)]
pub struct Ext(pub ::std::collections::HashMap<ExtKey, ::serde_json::Value>);
impl ::std::ops::Deref for Ext {
    type Target = ::std::collections::HashMap<ExtKey, ::serde_json::Value>;
    fn deref(&self) -> &::std::collections::HashMap<ExtKey, ::serde_json::Value> {
        &self.0
    }
}
impl ::std::convert::From<Ext> for ::std::collections::HashMap<ExtKey, ::serde_json::Value> {
    fn from(value: Ext) -> Self {
        value.0
    }
}
impl ::std::convert::From<::std::collections::HashMap<ExtKey, ::serde_json::Value>> for Ext {
    fn from(value: ::std::collections::HashMap<ExtKey, ::serde_json::Value>) -> Self {
        Self(value)
    }
}
///`ExtKey`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ExtKey(::std::string::String);
impl ::std::ops::Deref for ExtKey {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ExtKey> for ::std::string::String {
    fn from(value: ExtKey) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ExtKey {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ExtKey {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///A vetter asks to vet at a named event, at one of the rates the community publishes. The community answers where the request stands; it does not answer whether it was granted, because a vetter cannot raise its own cap and the group it would join may not exist yet.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "$id": "https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1",
///  "title": "Payload",
///  "description": "A vetter asks to vet at a named event, at one of the rates the community publishes. The community answers where the request stands; it does not answer whether it was granted, because a vetter cannot raise its own cap and the group it would join may not exist yet.",
///  "type": "object",
///  "required": [
///    "eventId",
///    "tier",
///    "window"
///  ],
///  "properties": {
///    "eventId": {
///      "description": "The event, as the community names it. It is the whole anonymity set for anything spent under this event's tokens, so it names a gathering people attend — never one desk, one day or one vetter.",
///      "type": "string",
///      "maxLength": 64,
///      "minLength": 1,
///      "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$"
///    },
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "tier": {
///      "description": "One of the tiers the community publishes. A tier is picked from a menu rather than a number being named, so that no vetter's requested rate is itself a distinguishing detail.",
///      "type": "string",
///      "maxLength": 64,
///      "minLength": 1
///    },
///    "window": {
///      "$ref": "#/definitions/Window"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Payload {
    ///The event, as the community names it. It is the whole anonymity set for anything spent under this event's tokens, so it names a gathering people attend — never one desk, one day or one vetter.
    #[serde(rename = "eventId")]
    pub event_id: PayloadEventId,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    ///One of the tiers the community publishes. A tier is picked from a menu rather than a number being named, so that no vetter's requested rate is itself a distinguishing detail.
    pub tier: PayloadTier,
    pub window: Window,
}
impl Payload {
    pub fn builder() -> builder::Payload {
        Default::default()
    }
}
///The event, as the community names it. It is the whole anonymity set for anything spent under this event's tokens, so it names a gathering people attend — never one desk, one day or one vetter.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "The event, as the community names it. It is the whole anonymity set for anything spent under this event's tokens, so it names a gathering people attend — never one desk, one day or one vetter.",
///  "type": "string",
///  "maxLength": 64,
///  "minLength": 1,
///  "pattern": "^[a-z0-9]([a-z0-9-]*[a-z0-9])?$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct PayloadEventId(::std::string::String);
impl ::std::ops::Deref for PayloadEventId {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<PayloadEventId> for ::std::string::String {
    fn from(value: PayloadEventId) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for PayloadEventId {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 64usize {
            return Err("longer than 64 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[a-z0-9]([a-z0-9-]*[a-z0-9])?$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[a-z0-9]([a-z0-9-]*[a-z0-9])?$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for PayloadEventId {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for PayloadEventId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for PayloadEventId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for PayloadEventId {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///One of the tiers the community publishes. A tier is picked from a menu rather than a number being named, so that no vetter's requested rate is itself a distinguishing detail.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "One of the tiers the community publishes. A tier is picked from a menu rather than a number being named, so that no vetter's requested rate is itself a distinguishing detail.",
///  "type": "string",
///  "maxLength": 64,
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct PayloadTier(::std::string::String);
impl ::std::ops::Deref for PayloadTier {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<PayloadTier> for ::std::string::String {
    fn from(value: PayloadTier) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for PayloadTier {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 64usize {
            return Err("longer than 64 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for PayloadTier {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for PayloadTier {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for PayloadTier {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for PayloadTier {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Where the request stands. `pending` is the ordinary answer to a first request and is not a refusal — a refusal is a `trust-task-error`.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Response",
///  "description": "Where the request stands. `pending` is the ordinary answer to a first request and is not a refusal — a refusal is a `trust-task-error`.",
///  "type": "object",
///  "required": [
///    "eventId",
///    "groupFloor",
///    "groupSize",
///    "state",
///    "tier",
///    "window"
///  ],
///  "properties": {
///    "closesAfter": {
///      "description": "Present when `state` is `approved`: the last day this label will be accepted. It is after `window.endDate` by the community's grace period, so an applicant met on the final day still has time to submit.",
///      "type": "string",
///      "format": "date"
///    },
///    "dripPerTick": {
///      "description": "Present when `state` is `approved`: how many tokens a tick under this label yields. It is the tier's rate, and it is the community's cap, not the vetter's request.",
///      "type": "integer",
///      "minimum": 1.0
///    },
///    "eventId": {
///      "description": "The event the request names.",
///      "type": "string",
///      "maxLength": 64,
///      "minLength": 1
///    },
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "groupFloor": {
///      "description": "How many vetters this community requires before an event may be approved at all. A one-vetter event is an event key with one holder, which is a name.",
///      "type": "integer",
///      "minimum": 2.0
///    },
///    "groupSize": {
///      "description": "How many vetters have asked for this event so far, including this one. A count, never a list: who else is at the event is the anonymity set, and naming its members would hand out the very thing the set exists to hide.",
///      "type": "integer",
///      "minimum": 0.0
///    },
///    "label": {
///      "description": "Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.",
///      "type": "string",
///      "maxLength": 128,
///      "minLength": 1
///    },
///    "state": {
///      "description": "`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.",
///      "type": "string",
///      "enum": [
///        "pending",
///        "approved"
///      ]
///    },
///    "tier": {
///      "description": "The tier recorded for this vetter at this event.",
///      "type": "string",
///      "maxLength": 64,
///      "minLength": 1
///    },
///    "window": {
///      "$ref": "#/definitions/Window"
///    }
///  },
///  "additionalProperties": false,
///  "$anchor": "response"
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Response {
    ///Present when `state` is `approved`: the last day this label will be accepted. It is after `window.endDate` by the community's grace period, so an applicant met on the final day still has time to submit.
    #[serde(
        rename = "closesAfter",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub closes_after: ::std::option::Option<::chrono::naive::NaiveDate>,
    ///Present when `state` is `approved`: how many tokens a tick under this label yields. It is the tier's rate, and it is the community's cap, not the vetter's request.
    #[serde(
        rename = "dripPerTick",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub drip_per_tick: ::std::option::Option<::std::num::NonZeroU64>,
    ///The event the request names.
    #[serde(rename = "eventId")]
    pub event_id: ResponseEventId,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    ///How many vetters this community requires before an event may be approved at all. A one-vetter event is an event key with one holder, which is a name.
    #[serde(rename = "groupFloor")]
    pub group_floor: i64,
    ///How many vetters have asked for this event so far, including this one. A count, never a list: who else is at the event is the anonymity set, and naming its members would hand out the very thing the set exists to hide.
    #[serde(rename = "groupSize")]
    pub group_size: u64,
    ///Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub label: ::std::option::Option<ResponseLabel>,
    ///`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.
    pub state: ResponseState,
    ///The tier recorded for this vetter at this event.
    pub tier: ResponseTier,
    pub window: Window,
}
impl Response {
    pub fn builder() -> builder::Response {
        Default::default()
    }
}
///The event the request names.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "The event the request names.",
///  "type": "string",
///  "maxLength": 64,
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseEventId(::std::string::String);
impl ::std::ops::Deref for ResponseEventId {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseEventId> for ::std::string::String {
    fn from(value: ResponseEventId) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseEventId {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 64usize {
            return Err("longer than 64 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseEventId {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseEventId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseEventId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseEventId {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.",
///  "type": "string",
///  "maxLength": 128,
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseLabel(::std::string::String);
impl ::std::ops::Deref for ResponseLabel {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseLabel> for ::std::string::String {
    fn from(value: ResponseLabel) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseLabel {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 128usize {
            return Err("longer than 128 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseLabel {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseLabel {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseLabel {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseLabel {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.",
///  "type": "string",
///  "enum": [
///    "pending",
///    "approved"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum ResponseState {
    #[serde(rename = "pending")]
    Pending,
    #[serde(rename = "approved")]
    Approved,
}
impl ::std::fmt::Display for ResponseState {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::Pending => f.write_str("pending"),
            Self::Approved => f.write_str("approved"),
        }
    }
}
impl ::std::str::FromStr for ResponseState {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "pending" => Ok(Self::Pending),
            "approved" => Ok(Self::Approved),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for ResponseState {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseState {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseState {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
///The tier recorded for this vetter at this event.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "The tier recorded for this vetter at this event.",
///  "type": "string",
///  "maxLength": 64,
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseTier(::std::string::String);
impl ::std::ops::Deref for ResponseTier {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseTier> for ::std::string::String {
    fn from(value: ResponseTier) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseTier {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 64usize {
            return Err("longer than 64 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseTier {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseTier {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseTier {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseTier {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///The days the event runs. Dates, not timestamps: an hour would say when a vetter expects to be at a desk, and the tokens are day-scale anyway.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Event window",
///  "description": "The days the event runs. Dates, not timestamps: an hour would say when a vetter expects to be at a desk, and the tokens are day-scale anyway.",
///  "type": "object",
///  "required": [
///    "endDate",
///    "startDate"
///  ],
///  "properties": {
///    "endDate": {
///      "description": "Last day of the event, inclusive. It is not before `startDate`.",
///      "type": "string",
///      "format": "date"
///    },
///    "startDate": {
///      "description": "First day of the event, inclusive.",
///      "type": "string",
///      "format": "date"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Window {
    ///Last day of the event, inclusive. It is not before `startDate`.
    #[serde(rename = "endDate")]
    pub end_date: ::chrono::naive::NaiveDate,
    ///First day of the event, inclusive.
    #[serde(rename = "startDate")]
    pub start_date: ::chrono::naive::NaiveDate,
}
impl Window {
    pub fn builder() -> builder::Window {
        Default::default()
    }
}
/// Types for composing complex structures.
pub mod builder {
    #[derive(Clone, Debug)]
    pub struct Payload {
        event_id: ::std::result::Result<super::PayloadEventId, ::std::string::String>,
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        tier: ::std::result::Result<super::PayloadTier, ::std::string::String>,
        window: ::std::result::Result<super::Window, ::std::string::String>,
    }
    impl ::std::default::Default for Payload {
        fn default() -> Self {
            Self {
                event_id: Err("no value supplied for event_id".to_string()),
                ext: Ok(Default::default()),
                tier: Err("no value supplied for tier".to_string()),
                window: Err("no value supplied for window".to_string()),
            }
        }
    }
    impl Payload {
        pub fn event_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::PayloadEventId>,
            T::Error: ::std::fmt::Display,
        {
            self.event_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for event_id: {e}"));
            self
        }
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn tier<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::PayloadTier>,
            T::Error: ::std::fmt::Display,
        {
            self.tier = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for tier: {e}"));
            self
        }
        pub fn window<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::Window>,
            T::Error: ::std::fmt::Display,
        {
            self.window = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for window: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Payload> for super::Payload {
        type Error = super::error::ConversionError;
        fn try_from(value: Payload) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                event_id: value.event_id?,
                ext: value.ext?,
                tier: value.tier?,
                window: value.window?,
            })
        }
    }
    impl ::std::convert::From<super::Payload> for Payload {
        fn from(value: super::Payload) -> Self {
            Self {
                event_id: Ok(value.event_id),
                ext: Ok(value.ext),
                tier: Ok(value.tier),
                window: Ok(value.window),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct Response {
        closes_after: ::std::result::Result<
            ::std::option::Option<::chrono::naive::NaiveDate>,
            ::std::string::String,
        >,
        drip_per_tick: ::std::result::Result<
            ::std::option::Option<::std::num::NonZeroU64>,
            ::std::string::String,
        >,
        event_id: ::std::result::Result<super::ResponseEventId, ::std::string::String>,
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        group_floor: ::std::result::Result<i64, ::std::string::String>,
        group_size: ::std::result::Result<u64, ::std::string::String>,
        label: ::std::result::Result<
            ::std::option::Option<super::ResponseLabel>,
            ::std::string::String,
        >,
        state: ::std::result::Result<super::ResponseState, ::std::string::String>,
        tier: ::std::result::Result<super::ResponseTier, ::std::string::String>,
        window: ::std::result::Result<super::Window, ::std::string::String>,
    }
    impl ::std::default::Default for Response {
        fn default() -> Self {
            Self {
                closes_after: Ok(Default::default()),
                drip_per_tick: Ok(Default::default()),
                event_id: Err("no value supplied for event_id".to_string()),
                ext: Ok(Default::default()),
                group_floor: Err("no value supplied for group_floor".to_string()),
                group_size: Err("no value supplied for group_size".to_string()),
                label: Ok(Default::default()),
                state: Err("no value supplied for state".to_string()),
                tier: Err("no value supplied for tier".to_string()),
                window: Err("no value supplied for window".to_string()),
            }
        }
    }
    impl Response {
        pub fn closes_after<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<::chrono::naive::NaiveDate>>,
            T::Error: ::std::fmt::Display,
        {
            self.closes_after = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for closes_after: {e}"));
            self
        }
        pub fn drip_per_tick<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<::std::num::NonZeroU64>>,
            T::Error: ::std::fmt::Display,
        {
            self.drip_per_tick = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for drip_per_tick: {e}"));
            self
        }
        pub fn event_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseEventId>,
            T::Error: ::std::fmt::Display,
        {
            self.event_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for event_id: {e}"));
            self
        }
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn group_floor<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<i64>,
            T::Error: ::std::fmt::Display,
        {
            self.group_floor = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for group_floor: {e}"));
            self
        }
        pub fn group_size<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<u64>,
            T::Error: ::std::fmt::Display,
        {
            self.group_size = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for group_size: {e}"));
            self
        }
        pub fn label<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::ResponseLabel>>,
            T::Error: ::std::fmt::Display,
        {
            self.label = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for label: {e}"));
            self
        }
        pub fn state<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseState>,
            T::Error: ::std::fmt::Display,
        {
            self.state = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for state: {e}"));
            self
        }
        pub fn tier<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseTier>,
            T::Error: ::std::fmt::Display,
        {
            self.tier = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for tier: {e}"));
            self
        }
        pub fn window<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::Window>,
            T::Error: ::std::fmt::Display,
        {
            self.window = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for window: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Response> for super::Response {
        type Error = super::error::ConversionError;
        fn try_from(value: Response) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                closes_after: value.closes_after?,
                drip_per_tick: value.drip_per_tick?,
                event_id: value.event_id?,
                ext: value.ext?,
                group_floor: value.group_floor?,
                group_size: value.group_size?,
                label: value.label?,
                state: value.state?,
                tier: value.tier?,
                window: value.window?,
            })
        }
    }
    impl ::std::convert::From<super::Response> for Response {
        fn from(value: super::Response) -> Self {
            Self {
                closes_after: Ok(value.closes_after),
                drip_per_tick: Ok(value.drip_per_tick),
                event_id: Ok(value.event_id),
                ext: Ok(value.ext),
                group_floor: Ok(value.group_floor),
                group_size: Ok(value.group_size),
                label: Ok(value.label),
                state: Ok(value.state),
                tier: Ok(value.tier),
                window: Ok(value.window),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct Window {
        end_date: ::std::result::Result<::chrono::naive::NaiveDate, ::std::string::String>,
        start_date: ::std::result::Result<::chrono::naive::NaiveDate, ::std::string::String>,
    }
    impl ::std::default::Default for Window {
        fn default() -> Self {
            Self {
                end_date: Err("no value supplied for end_date".to_string()),
                start_date: Err("no value supplied for start_date".to_string()),
            }
        }
    }
    impl Window {
        pub fn end_date<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::chrono::naive::NaiveDate>,
            T::Error: ::std::fmt::Display,
        {
            self.end_date = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for end_date: {e}"));
            self
        }
        pub fn start_date<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::chrono::naive::NaiveDate>,
            T::Error: ::std::fmt::Display,
        {
            self.start_date = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for start_date: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Window> for super::Window {
        type Error = super::error::ConversionError;
        fn try_from(value: Window) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                end_date: value.end_date?,
                start_date: value.start_date?,
            })
        }
    }
    impl ::std::convert::From<super::Window> for Window {
        fn from(value: super::Window) -> Self {
            Self {
                end_date: Ok(value.end_date),
                start_date: Ok(value.start_date),
            }
        }
    }
}
impl crate::Payload for Payload {
    const TYPE_URI: &'static str = "https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_ISSUED_AT_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Where the request stands. `pending` is the ordinary answer to a first request and is not a refusal — a refusal is a `trust-task-error`.\",\n      \"properties\": {\n        \"closesAfter\": {\n          \"description\": \"Present when `state` is `approved`: the last day this label will be accepted. It is after `window.endDate` by the community's grace period, so an applicant met on the final day still has time to submit.\",\n          \"format\": \"date\",\n          \"type\": \"string\"\n        },\n        \"dripPerTick\": {\n          \"description\": \"Present when `state` is `approved`: how many tokens a tick under this label yields. It is the tier's rate, and it is the community's cap, not the vetter's request.\",\n          \"minimum\": 1,\n          \"type\": \"integer\"\n        },\n        \"eventId\": {\n          \"description\": \"The event the request names.\",\n          \"maxLength\": 64,\n          \"minLength\": 1,\n          \"type\": \"string\"\n        },\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"groupFloor\": {\n          \"description\": \"How many vetters this community requires before an event may be approved at all. A one-vetter event is an event key with one holder, which is a name.\",\n          \"minimum\": 2,\n          \"type\": \"integer\"\n        },\n        \"groupSize\": {\n          \"description\": \"How many vetters have asked for this event so far, including this one. A count, never a list: who else is at the event is the anonymity set, and naming its members would hand out the very thing the set exists to hide.\",\n          \"minimum\": 0,\n          \"type\": \"integer\"\n        },\n        \"label\": {\n          \"description\": \"Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.\",\n          \"maxLength\": 128,\n          \"minLength\": 1,\n          \"type\": \"string\"\n        },\n        \"state\": {\n          \"description\": \"`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.\",\n          \"enum\": [\n            \"pending\",\n            \"approved\"\n          ],\n          \"type\": \"string\"\n        },\n        \"tier\": {\n          \"description\": \"The tier recorded for this vetter at this event.\",\n          \"maxLength\": 64,\n          \"minLength\": 1,\n          \"type\": \"string\"\n        },\n        \"window\": {\n          \"$ref\": \"#/$defs/Window\"\n        }\n      },\n      \"required\": [\n        \"eventId\",\n        \"state\",\n        \"tier\",\n        \"window\",\n        \"groupSize\",\n        \"groupFloor\"\n      ],\n      \"title\": \"VTC Vetting Event Mode — response payload\",\n      \"type\": \"object\"\n    },\n    \"Window\": {\n      \"additionalProperties\": false,\n      \"description\": \"The days the event runs. Dates, not timestamps: an hour would say when a vetter expects to be at a desk, and the tokens are day-scale anyway.\",\n      \"properties\": {\n        \"endDate\": {\n          \"description\": \"Last day of the event, inclusive. It is not before `startDate`.\",\n          \"format\": \"date\",\n          \"type\": \"string\"\n        },\n        \"startDate\": {\n          \"description\": \"First day of the event, inclusive.\",\n          \"format\": \"date\",\n          \"type\": \"string\"\n        }\n      },\n      \"required\": [\n        \"startDate\",\n        \"endDate\"\n      ],\n      \"title\": \"Event window\",\n      \"type\": \"object\"\n    }\n  },\n  \"$id\": \"https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"additionalProperties\": false,\n  \"description\": \"A vetter asks to vet at a named event, at one of the rates the community publishes. The community answers where the request stands; it does not answer whether it was granted, because a vetter cannot raise its own cap and the group it would join may not exist yet.\",\n  \"properties\": {\n    \"eventId\": {\n      \"description\": \"The event, as the community names it. It is the whole anonymity set for anything spent under this event's tokens, so it names a gathering people attend — never one desk, one day or one vetter.\",\n      \"maxLength\": 64,\n      \"minLength\": 1,\n      \"pattern\": \"^[a-z0-9]([a-z0-9-]*[a-z0-9])?$\",\n      \"type\": \"string\"\n    },\n    \"ext\": {\n      \"$ref\": \"#/$defs/Ext\"\n    },\n    \"tier\": {\n      \"description\": \"One of the tiers the community publishes. A tier is picked from a menu rather than a number being named, so that no vetter's requested rate is itself a distinguishing detail.\",\n      \"maxLength\": 64,\n      \"minLength\": 1,\n      \"type\": \"string\"\n    },\n    \"window\": {\n      \"$ref\": \"#/$defs/Window\"\n    }\n  },\n  \"required\": [\n    \"eventId\",\n    \"tier\",\n    \"window\"\n  ],\n  \"title\": \"VTC — Vetting Event Mode — payload\",\n  \"type\": \"object\"\n}\n",
    );
}
impl crate::Payload for Response {
    const TYPE_URI: &'static str =
        "https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1#response";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_ISSUED_AT_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Where the request stands. `pending` is the ordinary answer to a first request and is not a refusal — a refusal is a `trust-task-error`.\",\n      \"properties\": {\n        \"closesAfter\": {\n          \"description\": \"Present when `state` is `approved`: the last day this label will be accepted. It is after `window.endDate` by the community's grace period, so an applicant met on the final day still has time to submit.\",\n          \"format\": \"date\",\n          \"type\": \"string\"\n        },\n        \"dripPerTick\": {\n          \"description\": \"Present when `state` is `approved`: how many tokens a tick under this label yields. It is the tier's rate, and it is the community's cap, not the vetter's request.\",\n          \"minimum\": 1,\n          \"type\": \"integer\"\n        },\n        \"eventId\": {\n          \"description\": \"The event the request names.\",\n          \"maxLength\": 64,\n          \"minLength\": 1,\n          \"type\": \"string\"\n        },\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"groupFloor\": {\n          \"description\": \"How many vetters this community requires before an event may be approved at all. A one-vetter event is an event key with one holder, which is a name.\",\n          \"minimum\": 2,\n          \"type\": \"integer\"\n        },\n        \"groupSize\": {\n          \"description\": \"How many vetters have asked for this event so far, including this one. A count, never a list: who else is at the event is the anonymity set, and naming its members would hand out the very thing the set exists to hide.\",\n          \"minimum\": 0,\n          \"type\": \"integer\"\n        },\n        \"label\": {\n          \"description\": \"Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.\",\n          \"maxLength\": 128,\n          \"minLength\": 1,\n          \"type\": \"string\"\n        },\n        \"state\": {\n          \"description\": \"`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.\",\n          \"enum\": [\n            \"pending\",\n            \"approved\"\n          ],\n          \"type\": \"string\"\n        },\n        \"tier\": {\n          \"description\": \"The tier recorded for this vetter at this event.\",\n          \"maxLength\": 64,\n          \"minLength\": 1,\n          \"type\": \"string\"\n        },\n        \"window\": {\n          \"$ref\": \"#/$defs/Window\"\n        }\n      },\n      \"required\": [\n        \"eventId\",\n        \"state\",\n        \"tier\",\n        \"window\",\n        \"groupSize\",\n        \"groupFloor\"\n      ],\n      \"title\": \"VTC Vetting Event Mode — response payload\",\n      \"type\": \"object\"\n    },\n    \"Window\": {\n      \"additionalProperties\": false,\n      \"description\": \"The days the event runs. Dates, not timestamps: an hour would say when a vetter expects to be at a desk, and the tokens are day-scale anyway.\",\n      \"properties\": {\n        \"endDate\": {\n          \"description\": \"Last day of the event, inclusive. It is not before `startDate`.\",\n          \"format\": \"date\",\n          \"type\": \"string\"\n        },\n        \"startDate\": {\n          \"description\": \"First day of the event, inclusive.\",\n          \"format\": \"date\",\n          \"type\": \"string\"\n        }\n      },\n      \"required\": [\n        \"startDate\",\n        \"endDate\"\n      ],\n      \"title\": \"Event window\",\n      \"type\": \"object\"\n    }\n  },\n  \"$ref\": \"#/$defs/Response\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\"\n}\n",
    );
}
impl crate::RequestPayload for Payload {
    type Response = Response;
}
/// The extended error codes this specification declares (SPEC §7.3 item 9,
/// §8.5), in declaration order. Empty when it declares none.
pub const ERROR_CODES: &[crate::DeclaredErrorCode] = &[
    error_codes::NOT_A_VETTER,
    error_codes::UNKNOWN_EVENT,
    error_codes::UNKNOWN_TIER,
    error_codes::BAD_WINDOW,
    error_codes::ALREADY_REQUESTED,
    error_codes::EVENT_CLOSED,
];
/// One constant per extended error code this specification declares
/// (SPEC §7.3 item 9), named for its local part.
///
/// Emit these rather than a string literal: the code is read from the
/// specification, so it cannot name a code the specification never
/// declared.
pub mod error_codes {
    /// `vtc/vetting/vetters/event-mode:notAVetter`
    ///
    /// The requester holds no live vetter grant in this community.
    ///
    /// Declared `retryable: false`.
    pub const NOT_A_VETTER: crate::DeclaredErrorCode = crate::DeclaredErrorCode {
        code: "vtc/vetting/vetters/event-mode:notAVetter",
        retryable: false,
    };
    /// `vtc/vetting/vetters/event-mode:unknownEvent`
    ///
    /// This community is not running an event by that name.
    ///
    /// Declared `retryable: false`.
    pub const UNKNOWN_EVENT: crate::DeclaredErrorCode = crate::DeclaredErrorCode {
        code: "vtc/vetting/vetters/event-mode:unknownEvent",
        retryable: false,
    };
    /// `vtc/vetting/vetters/event-mode:unknownTier`
    ///
    /// `tier` is not one of the tiers this community publishes for this event.
    ///
    /// Declared `retryable: false`.
    pub const UNKNOWN_TIER: crate::DeclaredErrorCode = crate::DeclaredErrorCode {
        code: "vtc/vetting/vetters/event-mode:unknownTier",
        retryable: false,
    };
    /// `vtc/vetting/vetters/event-mode:badWindow`
    ///
    /// `window` is not a window this community will accept for this event — inverted, or outside the event's own dates.
    ///
    /// Declared `retryable: false`.
    pub const BAD_WINDOW: crate::DeclaredErrorCode = crate::DeclaredErrorCode {
        code: "vtc/vetting/vetters/event-mode:badWindow",
        retryable: false,
    };
    /// `vtc/vetting/vetters/event-mode:alreadyRequested`
    ///
    /// This member has already asked for this event. The recorded request stands; nothing was changed.
    ///
    /// Declared `retryable: false`.
    pub const ALREADY_REQUESTED: crate::DeclaredErrorCode = crate::DeclaredErrorCode {
        code: "vtc/vetting/vetters/event-mode:alreadyRequested",
        retryable: false,
    };
    /// `vtc/vetting/vetters/event-mode:eventClosed`
    ///
    /// The event's label has closed. Tokens under it are no longer issued or accepted.
    ///
    /// Declared `retryable: false`.
    pub const EVENT_CLOSED: crate::DeclaredErrorCode = crate::DeclaredErrorCode {
        code: "vtc/vetting/vetters/event-mode:eventClosed",
        retryable: false,
    };
}
#[cfg(test)]
mod conformance {
    //! Round-trip tests harvested from the spec's `spec.md`,
    //! plus a `rejects_invalid_examples` test for any fixtures
    //! in `payload.invalid-examples.json` (validate feature).
    #[test]
    fn request_example_1() {
        const JSON: &str = "{\n  \"id\": \"urn:uuid:00000000-0000-4000-8000-000000000001\",\n  \"type\": \"https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1#request\",\n  \"issuer\": \"did:example:vetter\",\n  \"recipient\": \"did:example:community\",\n  \"issuedAt\": \"2026-10-01T09:00:00Z\",\n  \"threadId\": \"urn:uuid:00000000-0000-4000-8000-0000000000ff\",\n  \"payload\": {\n    \"eventId\": \"kernel-summit-2026\",\n    \"tier\": \"desk\",\n    \"window\": {\n      \"startDate\": \"2026-10-12\",\n      \"endDate\": \"2026-10-14\"\n    }\n  }\n}\n";
        let doc: crate::TrustTask<super::Payload> =
            serde_json::from_str(JSON).expect("deserialize request example");
        let rendered = serde_json::to_value(&doc).expect("re-serialize");
        let expected: serde_json::Value = serde_json::from_str(JSON).expect("re-parse expected");
        assert_eq!(rendered, expected, "request example failed round-trip");
    }
    #[test]
    fn response_example_1() {
        const JSON: &str = "{\n  \"id\": \"urn:uuid:00000000-0000-4000-8000-000000000002\",\n  \"type\": \"https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1#response\",\n  \"issuer\": \"did:example:community\",\n  \"recipient\": \"did:example:vetter\",\n  \"issuedAt\": \"2026-10-01T09:00:01Z\",\n  \"threadId\": \"urn:uuid:00000000-0000-4000-8000-0000000000ff\",\n  \"payload\": {\n    \"eventId\": \"kernel-summit-2026\",\n    \"state\": \"pending\",\n    \"tier\": \"desk\",\n    \"window\": {\n      \"startDate\": \"2026-10-12\",\n      \"endDate\": \"2026-10-14\"\n    },\n    \"groupSize\": 2,\n    \"groupFloor\": 3\n  }\n}\n";
        let doc: crate::TrustTask<super::Response> =
            serde_json::from_str(JSON).expect("deserialize response example");
        let rendered = serde_json::to_value(&doc).expect("re-serialize");
        let expected: serde_json::Value = serde_json::from_str(JSON).expect("re-parse expected");
        assert_eq!(rendered, expected, "response example failed round-trip");
    }
    #[test]
    fn response_example_2() {
        const JSON: &str = "{\n  \"id\": \"urn:uuid:00000000-0000-4000-8000-000000000003\",\n  \"type\": \"https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1#response\",\n  \"issuer\": \"did:example:community\",\n  \"recipient\": \"did:example:vetter\",\n  \"issuedAt\": \"2026-10-03T11:20:00Z\",\n  \"threadId\": \"urn:uuid:00000000-0000-4000-8000-0000000000fe\",\n  \"payload\": {\n    \"eventId\": \"kernel-summit-2026\",\n    \"state\": \"approved\",\n    \"tier\": \"desk\",\n    \"window\": {\n      \"startDate\": \"2026-10-12\",\n      \"endDate\": \"2026-10-14\"\n    },\n    \"groupSize\": 5,\n    \"groupFloor\": 3,\n    \"label\": \"token/event/kernel-summit-2026\",\n    \"dripPerTick\": 20,\n    \"closesAfter\": \"2026-10-28\"\n  }\n}\n";
        let doc: crate::TrustTask<super::Response> =
            serde_json::from_str(JSON).expect("deserialize response example");
        let rendered = serde_json::to_value(&doc).expect("re-serialize");
        let expected: serde_json::Value = serde_json::from_str(JSON).expect("re-parse expected");
        assert_eq!(rendered, expected, "response example failed round-trip");
    }
}
