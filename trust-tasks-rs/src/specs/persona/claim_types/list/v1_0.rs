//! Generated by `trust-tasks-codegen` — do not edit by hand.
//!
//! Spec slug: `persona/claim-types/list`. Version: `1.0`.
#[allow(unused_imports)]
use serde::{Deserialize, Serialize};
/// Error types.
pub mod error {
    /// Error from a `TryFrom` or `FromStr` implementation.
    pub struct ConversionError(::std::borrow::Cow<'static, str>);
    impl ::std::error::Error for ConversionError {}
    impl ::std::fmt::Display for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Display::fmt(&self.0, f)
        }
    }
    impl ::std::fmt::Debug for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Debug::fmt(&self.0, f)
        }
    }
    impl From<&'static str> for ConversionError {
        fn from(value: &'static str) -> Self {
            Self(value.into())
        }
    }
    impl From<String> for ConversionError {
        fn from(value: String) -> Self {
            Self(value.into())
        }
    }
}
/**
The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.

The token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.

The `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.*/
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "ClaimType",
///  "description": "\nThe vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\n\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\n\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.",
///  "type": "string",
///  "maxLength": 128,
///  "minLength": 1,
///  "pattern": "^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ClaimType(::std::string::String);
impl ::std::ops::Deref for ClaimType {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ClaimType> for ::std::string::String {
    fn from(value: ClaimType) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ClaimType {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 128usize {
            return Err("longer than 128 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err(
                "doesn't match pattern \"^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$\"".into(),
            );
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ClaimType {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Ext",
///  "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
///  "type": "object",
///  "minProperties": 1,
///  "additionalProperties": true,
///  "propertyNames": {
///    "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///  }
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(transparent)]
pub struct Ext(pub ::std::collections::HashMap<ExtKey, ::serde_json::Value>);
impl ::std::ops::Deref for Ext {
    type Target = ::std::collections::HashMap<ExtKey, ::serde_json::Value>;
    fn deref(&self) -> &::std::collections::HashMap<ExtKey, ::serde_json::Value> {
        &self.0
    }
}
impl ::std::convert::From<Ext> for ::std::collections::HashMap<ExtKey, ::serde_json::Value> {
    fn from(value: Ext) -> Self {
        value.0
    }
}
impl ::std::convert::From<::std::collections::HashMap<ExtKey, ::serde_json::Value>> for Ext {
    fn from(value: ::std::collections::HashMap<ExtKey, ::serde_json::Value>) -> Self {
        Self(value)
    }
}
///`ExtKey`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ExtKey(::std::string::String);
impl ::std::ops::Deref for ExtKey {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ExtKey> for ::std::string::String {
    fn from(value: ExtKey) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ExtKey {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ExtKey {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Serve the claim-type registry this agent resolves against. A client that cannot read the table has to ship a copy of it, and a copy is a re-sync obligation on every change.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "$id": "https://trusttasks.org/spec/persona/claim-types/list/1.0",
///  "title": "Payload",
///  "description": "Serve the claim-type registry this agent resolves against. A client that cannot read the table has to ship a copy of it, and a copy is a re-sync obligation on every change.",
///  "type": "object",
///  "properties": {
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Payload {
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
}
impl ::std::default::Default for Payload {
    fn default() -> Self {
        Self {
            ext: Default::default(),
        }
    }
}
impl Payload {
    pub fn builder() -> builder::Payload {
        Default::default()
    }
}
/**
What it takes to let a value LEAVE. Distinct from `Sensitivity`, which governs showing it to the holder.

`consent` is the ordinary gate: `persona/disclosure/preview` renders what would leave and `persona/disclosure/present` releases it, so a human sees it once.

`stepUp` additionally requires a fresh authentication bound to THAT preview — not to the session. Without the binding, "each time" degrades into "once per login", which is the failure the requirement exists to prevent; the single-use `previewId` the preview already mints is what an implementation binds to. A maintainer MUST refuse `persona/disclosure/present` for a `stepUp` attribute when no such approval accompanies it.

Absent means *not decided by the holder* and resolves from the claim-type registry, which defaults `payment.*` and `gov.*` to `stepUp`.*/
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "ReleaseRequirement",
///  "description": "\nWhat it takes to let a value LEAVE. Distinct from `Sensitivity`, which governs showing it to the holder.\n\n`consent` is the ordinary gate: `persona/disclosure/preview` renders what would leave and `persona/disclosure/present` releases it, so a human sees it once.\n\n`stepUp` additionally requires a fresh authentication bound to THAT preview — not to the session. Without the binding, \"each time\" degrades into \"once per login\", which is the failure the requirement exists to prevent; the single-use `previewId` the preview already mints is what an implementation binds to. A maintainer MUST refuse `persona/disclosure/present` for a `stepUp` attribute when no such approval accompanies it.\n\nAbsent means *not decided by the holder* and resolves from the claim-type registry, which defaults `payment.*` and `gov.*` to `stepUp`.",
///  "type": "string",
///  "enum": [
///    "consent",
///    "stepUp"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum ReleaseRequirement {
    #[serde(rename = "consent")]
    Consent,
    #[serde(rename = "stepUp")]
    StepUp,
}
impl ::std::fmt::Display for ReleaseRequirement {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::Consent => f.write_str("consent"),
            Self::StepUp => f.write_str("stepUp"),
        }
    }
}
impl ::std::str::FromStr for ReleaseRequirement {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "consent" => Ok(Self::Consent),
            "stepUp" => Ok(Self::StepUp),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for ReleaseRequirement {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ReleaseRequirement {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ReleaseRequirement {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
///Success response to persona/claim-types/list. Type https://trusttasks.org/spec/persona/claim-types/list/1.0#response.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Response",
///  "description": "Success response to persona/claim-types/list. Type https://trusttasks.org/spec/persona/claim-types/list/1.0#response.",
///  "type": "object",
///  "required": [
///    "entries",
///    "registryVersion",
///    "strictness",
///    "unregistered"
///  ],
///  "properties": {
///    "entries": {
///      "description": "The registered tokens. Both exact tokens and family prefixes appear here, undistinguished: which one an entry is follows from whether a given token equals it or descends from it, and marking them would invite a client to walk only one kind.",
///      "type": "array",
///      "items": {
///        "type": "object",
///        "required": [
///          "mask",
///          "release",
///          "sensitivity",
///          "type"
///        ],
///        "properties": {
///          "mask": {
///            "description": "Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.",
///            "type": "string",
///            "maxLength": 32
///          },
///          "minimumSet": {
///            "description": "Marks a type a community may ask for without it being remarkable. The minimum set contains only normal-sensitivity, consent-release types; that is a rule rather than a coincidence.",
///            "type": "boolean"
///          },
///          "oidc": {
///            "description": "OIDC standard claims this type answers. A recorded mapping, applied at presentation — the token stays the maintainer's own and no external vocabulary is primary.",
///            "type": "array",
///            "items": {
///              "type": "string",
///              "maxLength": 64
///            },
///            "maxItems": 16
///          },
///          "release": {
///            "$ref": "#/definitions/ReleaseRequirement"
///          },
///          "sensitivity": {
///            "$ref": "#/definitions/Sensitivity"
///          },
///          "type": {
///            "$ref": "#/definitions/ClaimType"
///          }
///        },
///        "additionalProperties": false
///      },
///      "maxItems": 512
///    },
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "registryVersion": {
///      "description": "Version of the core registry this agent implements — the version of _shared/claim-types.json, not of this task. A client caching the table keys the cache on this.",
///      "type": "string",
///      "maxLength": 32
///    },
///    "strictness": {
///      "description": "Each axis ordered MOST PROTECTIVE FIRST. This is what makes rule 3 computable by a client: resolution takes the longest registered prefix and the unregistered floor and keeps whichever is more protective, which requires an ordering the client did not invent.",
///      "type": "object",
///      "required": [
///        "mask",
///        "release",
///        "sensitivity"
///      ],
///      "properties": {
///        "mask": {
///          "type": "array",
///          "items": {
///            "type": "string",
///            "maxLength": 32
///          },
///          "maxItems": 16
///        },
///        "release": {
///          "type": "array",
///          "items": {
///            "type": "string",
///            "maxLength": 32
///          },
///          "maxItems": 8
///        },
///        "sensitivity": {
///          "type": "array",
///          "items": {
///            "type": "string",
///            "maxLength": 32
///          },
///          "maxItems": 8
///        }
///      },
///      "additionalProperties": false
///    },
///    "unregistered": {
///      "description": "The floor: what a token with no entry and no registered prefix resolves to. Served rather than assumed, because a client that hard-codes it cannot be tightened by a maintainer that raises it.",
///      "type": "object",
///      "required": [
///        "mask",
///        "release",
///        "sensitivity"
///      ],
///      "properties": {
///        "mask": {
///          "type": "string",
///          "maxLength": 32
///        },
///        "release": {
///          "$ref": "#/definitions/ReleaseRequirement"
///        },
///        "sensitivity": {
///          "$ref": "#/definitions/Sensitivity"
///        }
///      },
///      "additionalProperties": false
///    }
///  },
///  "additionalProperties": false,
///  "$anchor": "response"
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Response {
    ///The registered tokens. Both exact tokens and family prefixes appear here, undistinguished: which one an entry is follows from whether a given token equals it or descends from it, and marking them would invite a client to walk only one kind.
    pub entries: ::std::vec::Vec<ResponseEntriesItem>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    ///Version of the core registry this agent implements — the version of _shared/claim-types.json, not of this task. A client caching the table keys the cache on this.
    #[serde(rename = "registryVersion")]
    pub registry_version: ResponseRegistryVersion,
    pub strictness: ResponseStrictness,
    pub unregistered: ResponseUnregistered,
}
impl Response {
    pub fn builder() -> builder::Response {
        Default::default()
    }
}
///`ResponseEntriesItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "object",
///  "required": [
///    "mask",
///    "release",
///    "sensitivity",
///    "type"
///  ],
///  "properties": {
///    "mask": {
///      "description": "Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.",
///      "type": "string",
///      "maxLength": 32
///    },
///    "minimumSet": {
///      "description": "Marks a type a community may ask for without it being remarkable. The minimum set contains only normal-sensitivity, consent-release types; that is a rule rather than a coincidence.",
///      "type": "boolean"
///    },
///    "oidc": {
///      "description": "OIDC standard claims this type answers. A recorded mapping, applied at presentation — the token stays the maintainer's own and no external vocabulary is primary.",
///      "type": "array",
///      "items": {
///        "type": "string",
///        "maxLength": 64
///      },
///      "maxItems": 16
///    },
///    "release": {
///      "$ref": "#/definitions/ReleaseRequirement"
///    },
///    "sensitivity": {
///      "$ref": "#/definitions/Sensitivity"
///    },
///    "type": {
///      "$ref": "#/definitions/ClaimType"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct ResponseEntriesItem {
    ///Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.
    pub mask: ResponseEntriesItemMask,
    ///Marks a type a community may ask for without it being remarkable. The minimum set contains only normal-sensitivity, consent-release types; that is a rule rather than a coincidence.
    #[serde(
        rename = "minimumSet",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub minimum_set: ::std::option::Option<bool>,
    ///OIDC standard claims this type answers. A recorded mapping, applied at presentation — the token stays the maintainer's own and no external vocabulary is primary.
    #[serde(default, skip_serializing_if = "::std::vec::Vec::is_empty")]
    pub oidc: ::std::vec::Vec<ResponseEntriesItemOidcItem>,
    pub release: ReleaseRequirement,
    pub sensitivity: Sensitivity,
    #[serde(rename = "type")]
    pub type_: ClaimType,
}
impl ResponseEntriesItem {
    pub fn builder() -> builder::ResponseEntriesItem {
        Default::default()
    }
}
///Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.",
///  "type": "string",
///  "maxLength": 32
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseEntriesItemMask(::std::string::String);
impl ::std::ops::Deref for ResponseEntriesItemMask {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseEntriesItemMask> for ::std::string::String {
    fn from(value: ResponseEntriesItemMask) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseEntriesItemMask {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 32usize {
            return Err("longer than 32 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseEntriesItemMask {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseEntriesItemMask {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseEntriesItemMask {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseEntriesItemMask {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseEntriesItemOidcItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 64
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseEntriesItemOidcItem(::std::string::String);
impl ::std::ops::Deref for ResponseEntriesItemOidcItem {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseEntriesItemOidcItem> for ::std::string::String {
    fn from(value: ResponseEntriesItemOidcItem) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseEntriesItemOidcItem {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 64usize {
            return Err("longer than 64 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseEntriesItemOidcItem {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseEntriesItemOidcItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseEntriesItemOidcItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseEntriesItemOidcItem {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Version of the core registry this agent implements — the version of _shared/claim-types.json, not of this task. A client caching the table keys the cache on this.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Version of the core registry this agent implements — the version of _shared/claim-types.json, not of this task. A client caching the table keys the cache on this.",
///  "type": "string",
///  "maxLength": 32
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseRegistryVersion(::std::string::String);
impl ::std::ops::Deref for ResponseRegistryVersion {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseRegistryVersion> for ::std::string::String {
    fn from(value: ResponseRegistryVersion) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseRegistryVersion {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 32usize {
            return Err("longer than 32 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseRegistryVersion {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseRegistryVersion {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseRegistryVersion {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseRegistryVersion {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Each axis ordered MOST PROTECTIVE FIRST. This is what makes rule 3 computable by a client: resolution takes the longest registered prefix and the unregistered floor and keeps whichever is more protective, which requires an ordering the client did not invent.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Each axis ordered MOST PROTECTIVE FIRST. This is what makes rule 3 computable by a client: resolution takes the longest registered prefix and the unregistered floor and keeps whichever is more protective, which requires an ordering the client did not invent.",
///  "type": "object",
///  "required": [
///    "mask",
///    "release",
///    "sensitivity"
///  ],
///  "properties": {
///    "mask": {
///      "type": "array",
///      "items": {
///        "type": "string",
///        "maxLength": 32
///      },
///      "maxItems": 16
///    },
///    "release": {
///      "type": "array",
///      "items": {
///        "type": "string",
///        "maxLength": 32
///      },
///      "maxItems": 8
///    },
///    "sensitivity": {
///      "type": "array",
///      "items": {
///        "type": "string",
///        "maxLength": 32
///      },
///      "maxItems": 8
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct ResponseStrictness {
    pub mask: ::std::vec::Vec<ResponseStrictnessMaskItem>,
    pub release: ::std::vec::Vec<ResponseStrictnessReleaseItem>,
    pub sensitivity: ::std::vec::Vec<ResponseStrictnessSensitivityItem>,
}
impl ResponseStrictness {
    pub fn builder() -> builder::ResponseStrictness {
        Default::default()
    }
}
///`ResponseStrictnessMaskItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 32
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseStrictnessMaskItem(::std::string::String);
impl ::std::ops::Deref for ResponseStrictnessMaskItem {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseStrictnessMaskItem> for ::std::string::String {
    fn from(value: ResponseStrictnessMaskItem) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseStrictnessMaskItem {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 32usize {
            return Err("longer than 32 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseStrictnessMaskItem {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseStrictnessMaskItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseStrictnessMaskItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseStrictnessMaskItem {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseStrictnessReleaseItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 32
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseStrictnessReleaseItem(::std::string::String);
impl ::std::ops::Deref for ResponseStrictnessReleaseItem {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseStrictnessReleaseItem> for ::std::string::String {
    fn from(value: ResponseStrictnessReleaseItem) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseStrictnessReleaseItem {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 32usize {
            return Err("longer than 32 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseStrictnessReleaseItem {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseStrictnessReleaseItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseStrictnessReleaseItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseStrictnessReleaseItem {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseStrictnessSensitivityItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 32
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseStrictnessSensitivityItem(::std::string::String);
impl ::std::ops::Deref for ResponseStrictnessSensitivityItem {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseStrictnessSensitivityItem> for ::std::string::String {
    fn from(value: ResponseStrictnessSensitivityItem) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseStrictnessSensitivityItem {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 32usize {
            return Err("longer than 32 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseStrictnessSensitivityItem {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseStrictnessSensitivityItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseStrictnessSensitivityItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseStrictnessSensitivityItem {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///The floor: what a token with no entry and no registered prefix resolves to. Served rather than assumed, because a client that hard-codes it cannot be tightened by a maintainer that raises it.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "The floor: what a token with no entry and no registered prefix resolves to. Served rather than assumed, because a client that hard-codes it cannot be tightened by a maintainer that raises it.",
///  "type": "object",
///  "required": [
///    "mask",
///    "release",
///    "sensitivity"
///  ],
///  "properties": {
///    "mask": {
///      "type": "string",
///      "maxLength": 32
///    },
///    "release": {
///      "$ref": "#/definitions/ReleaseRequirement"
///    },
///    "sensitivity": {
///      "$ref": "#/definitions/Sensitivity"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct ResponseUnregistered {
    pub mask: ResponseUnregisteredMask,
    pub release: ReleaseRequirement,
    pub sensitivity: Sensitivity,
}
impl ResponseUnregistered {
    pub fn builder() -> builder::ResponseUnregistered {
        Default::default()
    }
}
///`ResponseUnregisteredMask`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 32
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseUnregisteredMask(::std::string::String);
impl ::std::ops::Deref for ResponseUnregisteredMask {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseUnregisteredMask> for ::std::string::String {
    fn from(value: ResponseUnregisteredMask) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseUnregisteredMask {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 32usize {
            return Err("longer than 32 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseUnregisteredMask {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseUnregisteredMask {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseUnregisteredMask {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseUnregisteredMask {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
/**
How carefully a value is shown TO ITS OWN HOLDER. `high` means a consumer masks it by default, reveals it one attribute at a time on a deliberate act, and — the half that is not cosmetic — omits it from a listing that did not ask for sensitive values.

Absent means *not decided by the holder*, not `normal`: a consumer resolves it from the claim-type registry (see CLAIM-TYPES.md §4), which is why this member is optional and why an unregistered token resolves conservatively rather than permissively.

Distinct from how linkable the value is. A payment card is highly sensitive and barely linkable — every card number is unique, so knowing one tells a second verifier nothing about the first. Reading either as a proxy for the other produces a consumer that hides the wrong things.*/
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Sensitivity",
///  "description": "\nHow carefully a value is shown TO ITS OWN HOLDER. `high` means a consumer masks it by default, reveals it one attribute at a time on a deliberate act, and — the half that is not cosmetic — omits it from a listing that did not ask for sensitive values.\n\nAbsent means *not decided by the holder*, not `normal`: a consumer resolves it from the claim-type registry (see CLAIM-TYPES.md §4), which is why this member is optional and why an unregistered token resolves conservatively rather than permissively.\n\nDistinct from how linkable the value is. A payment card is highly sensitive and barely linkable — every card number is unique, so knowing one tells a second verifier nothing about the first. Reading either as a proxy for the other produces a consumer that hides the wrong things.",
///  "type": "string",
///  "enum": [
///    "normal",
///    "high"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum Sensitivity {
    #[serde(rename = "normal")]
    Normal,
    #[serde(rename = "high")]
    High,
}
impl ::std::fmt::Display for Sensitivity {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::Normal => f.write_str("normal"),
            Self::High => f.write_str("high"),
        }
    }
}
impl ::std::str::FromStr for Sensitivity {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "normal" => Ok(Self::Normal),
            "high" => Ok(Self::High),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for Sensitivity {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for Sensitivity {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for Sensitivity {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
/// Types for composing complex structures.
pub mod builder {
    #[derive(Clone, Debug)]
    pub struct Payload {
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
    }
    impl ::std::default::Default for Payload {
        fn default() -> Self {
            Self {
                ext: Ok(Default::default()),
            }
        }
    }
    impl Payload {
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Payload> for super::Payload {
        type Error = super::error::ConversionError;
        fn try_from(value: Payload) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self { ext: value.ext? })
        }
    }
    impl ::std::convert::From<super::Payload> for Payload {
        fn from(value: super::Payload) -> Self {
            Self { ext: Ok(value.ext) }
        }
    }
    #[derive(Clone, Debug)]
    pub struct Response {
        entries: ::std::result::Result<
            ::std::vec::Vec<super::ResponseEntriesItem>,
            ::std::string::String,
        >,
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        registry_version:
            ::std::result::Result<super::ResponseRegistryVersion, ::std::string::String>,
        strictness: ::std::result::Result<super::ResponseStrictness, ::std::string::String>,
        unregistered: ::std::result::Result<super::ResponseUnregistered, ::std::string::String>,
    }
    impl ::std::default::Default for Response {
        fn default() -> Self {
            Self {
                entries: Err("no value supplied for entries".to_string()),
                ext: Ok(Default::default()),
                registry_version: Err("no value supplied for registry_version".to_string()),
                strictness: Err("no value supplied for strictness".to_string()),
                unregistered: Err("no value supplied for unregistered".to_string()),
            }
        }
    }
    impl Response {
        pub fn entries<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseEntriesItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.entries = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for entries: {e}"));
            self
        }
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn registry_version<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseRegistryVersion>,
            T::Error: ::std::fmt::Display,
        {
            self.registry_version = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for registry_version: {e}"));
            self
        }
        pub fn strictness<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseStrictness>,
            T::Error: ::std::fmt::Display,
        {
            self.strictness = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for strictness: {e}"));
            self
        }
        pub fn unregistered<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseUnregistered>,
            T::Error: ::std::fmt::Display,
        {
            self.unregistered = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for unregistered: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Response> for super::Response {
        type Error = super::error::ConversionError;
        fn try_from(value: Response) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                entries: value.entries?,
                ext: value.ext?,
                registry_version: value.registry_version?,
                strictness: value.strictness?,
                unregistered: value.unregistered?,
            })
        }
    }
    impl ::std::convert::From<super::Response> for Response {
        fn from(value: super::Response) -> Self {
            Self {
                entries: Ok(value.entries),
                ext: Ok(value.ext),
                registry_version: Ok(value.registry_version),
                strictness: Ok(value.strictness),
                unregistered: Ok(value.unregistered),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct ResponseEntriesItem {
        mask: ::std::result::Result<super::ResponseEntriesItemMask, ::std::string::String>,
        minimum_set: ::std::result::Result<::std::option::Option<bool>, ::std::string::String>,
        oidc: ::std::result::Result<
            ::std::vec::Vec<super::ResponseEntriesItemOidcItem>,
            ::std::string::String,
        >,
        release: ::std::result::Result<super::ReleaseRequirement, ::std::string::String>,
        sensitivity: ::std::result::Result<super::Sensitivity, ::std::string::String>,
        type_: ::std::result::Result<super::ClaimType, ::std::string::String>,
    }
    impl ::std::default::Default for ResponseEntriesItem {
        fn default() -> Self {
            Self {
                mask: Err("no value supplied for mask".to_string()),
                minimum_set: Ok(Default::default()),
                oidc: Ok(Default::default()),
                release: Err("no value supplied for release".to_string()),
                sensitivity: Err("no value supplied for sensitivity".to_string()),
                type_: Err("no value supplied for type_".to_string()),
            }
        }
    }
    impl ResponseEntriesItem {
        pub fn mask<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseEntriesItemMask>,
            T::Error: ::std::fmt::Display,
        {
            self.mask = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for mask: {e}"));
            self
        }
        pub fn minimum_set<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<bool>>,
            T::Error: ::std::fmt::Display,
        {
            self.minimum_set = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for minimum_set: {e}"));
            self
        }
        pub fn oidc<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseEntriesItemOidcItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.oidc = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for oidc: {e}"));
            self
        }
        pub fn release<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ReleaseRequirement>,
            T::Error: ::std::fmt::Display,
        {
            self.release = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for release: {e}"));
            self
        }
        pub fn sensitivity<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::Sensitivity>,
            T::Error: ::std::fmt::Display,
        {
            self.sensitivity = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for sensitivity: {e}"));
            self
        }
        pub fn type_<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ClaimType>,
            T::Error: ::std::fmt::Display,
        {
            self.type_ = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for type_: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<ResponseEntriesItem> for super::ResponseEntriesItem {
        type Error = super::error::ConversionError;
        fn try_from(
            value: ResponseEntriesItem,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                mask: value.mask?,
                minimum_set: value.minimum_set?,
                oidc: value.oidc?,
                release: value.release?,
                sensitivity: value.sensitivity?,
                type_: value.type_?,
            })
        }
    }
    impl ::std::convert::From<super::ResponseEntriesItem> for ResponseEntriesItem {
        fn from(value: super::ResponseEntriesItem) -> Self {
            Self {
                mask: Ok(value.mask),
                minimum_set: Ok(value.minimum_set),
                oidc: Ok(value.oidc),
                release: Ok(value.release),
                sensitivity: Ok(value.sensitivity),
                type_: Ok(value.type_),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct ResponseStrictness {
        mask: ::std::result::Result<
            ::std::vec::Vec<super::ResponseStrictnessMaskItem>,
            ::std::string::String,
        >,
        release: ::std::result::Result<
            ::std::vec::Vec<super::ResponseStrictnessReleaseItem>,
            ::std::string::String,
        >,
        sensitivity: ::std::result::Result<
            ::std::vec::Vec<super::ResponseStrictnessSensitivityItem>,
            ::std::string::String,
        >,
    }
    impl ::std::default::Default for ResponseStrictness {
        fn default() -> Self {
            Self {
                mask: Err("no value supplied for mask".to_string()),
                release: Err("no value supplied for release".to_string()),
                sensitivity: Err("no value supplied for sensitivity".to_string()),
            }
        }
    }
    impl ResponseStrictness {
        pub fn mask<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseStrictnessMaskItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.mask = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for mask: {e}"));
            self
        }
        pub fn release<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseStrictnessReleaseItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.release = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for release: {e}"));
            self
        }
        pub fn sensitivity<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseStrictnessSensitivityItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.sensitivity = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for sensitivity: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<ResponseStrictness> for super::ResponseStrictness {
        type Error = super::error::ConversionError;
        fn try_from(
            value: ResponseStrictness,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                mask: value.mask?,
                release: value.release?,
                sensitivity: value.sensitivity?,
            })
        }
    }
    impl ::std::convert::From<super::ResponseStrictness> for ResponseStrictness {
        fn from(value: super::ResponseStrictness) -> Self {
            Self {
                mask: Ok(value.mask),
                release: Ok(value.release),
                sensitivity: Ok(value.sensitivity),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct ResponseUnregistered {
        mask: ::std::result::Result<super::ResponseUnregisteredMask, ::std::string::String>,
        release: ::std::result::Result<super::ReleaseRequirement, ::std::string::String>,
        sensitivity: ::std::result::Result<super::Sensitivity, ::std::string::String>,
    }
    impl ::std::default::Default for ResponseUnregistered {
        fn default() -> Self {
            Self {
                mask: Err("no value supplied for mask".to_string()),
                release: Err("no value supplied for release".to_string()),
                sensitivity: Err("no value supplied for sensitivity".to_string()),
            }
        }
    }
    impl ResponseUnregistered {
        pub fn mask<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseUnregisteredMask>,
            T::Error: ::std::fmt::Display,
        {
            self.mask = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for mask: {e}"));
            self
        }
        pub fn release<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ReleaseRequirement>,
            T::Error: ::std::fmt::Display,
        {
            self.release = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for release: {e}"));
            self
        }
        pub fn sensitivity<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::Sensitivity>,
            T::Error: ::std::fmt::Display,
        {
            self.sensitivity = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for sensitivity: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<ResponseUnregistered> for super::ResponseUnregistered {
        type Error = super::error::ConversionError;
        fn try_from(
            value: ResponseUnregistered,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                mask: value.mask?,
                release: value.release?,
                sensitivity: value.sensitivity?,
            })
        }
    }
    impl ::std::convert::From<super::ResponseUnregistered> for ResponseUnregistered {
        fn from(value: super::ResponseUnregistered) -> Self {
            Self {
                mask: Ok(value.mask),
                release: Ok(value.release),
                sensitivity: Ok(value.sensitivity),
            }
        }
    }
}
impl crate::Payload for Payload {
    const TYPE_URI: &'static str = "https://trusttasks.org/spec/persona/claim-types/list/1.0";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"ClaimType\": {\n      \"description\": \"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\n      \"maxLength\": 128,\n      \"minLength\": 1,\n      \"pattern\": \"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\",\n      \"title\": \"ClaimType\",\n      \"type\": \"string\"\n    },\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"ReleaseRequirement\": {\n      \"description\": \"What it takes to let a value LEAVE. Distinct from `Sensitivity`, which governs showing it to the holder.\\n\\n`consent` is the ordinary gate: `persona/disclosure/preview` renders what would leave and `persona/disclosure/present` releases it, so a human sees it once.\\n\\n`stepUp` additionally requires a fresh authentication bound to THAT preview — not to the session. Without the binding, \\\"each time\\\" degrades into \\\"once per login\\\", which is the failure the requirement exists to prevent; the single-use `previewId` the preview already mints is what an implementation binds to. A maintainer MUST refuse `persona/disclosure/present` for a `stepUp` attribute when no such approval accompanies it.\\n\\nAbsent means *not decided by the holder* and resolves from the claim-type registry, which defaults `payment.*` and `gov.*` to `stepUp`.\",\n      \"enum\": [\n        \"consent\",\n        \"stepUp\"\n      ],\n      \"title\": \"ReleaseRequirement\",\n      \"type\": \"string\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Success response to persona/claim-types/list. Type https://trusttasks.org/spec/persona/claim-types/list/1.0#response.\",\n      \"properties\": {\n        \"entries\": {\n          \"description\": \"The registered tokens. Both exact tokens and family prefixes appear here, undistinguished: which one an entry is follows from whether a given token equals it or descends from it, and marking them would invite a client to walk only one kind.\",\n          \"items\": {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"mask\": {\n                \"description\": \"Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.\",\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"minimumSet\": {\n                \"description\": \"Marks a type a community may ask for without it being remarkable. The minimum set contains only normal-sensitivity, consent-release types; that is a rule rather than a coincidence.\",\n                \"type\": \"boolean\"\n              },\n              \"oidc\": {\n                \"description\": \"OIDC standard claims this type answers. A recorded mapping, applied at presentation — the token stays the maintainer's own and no external vocabulary is primary.\",\n                \"items\": {\n                  \"maxLength\": 64,\n                  \"type\": \"string\"\n                },\n                \"maxItems\": 16,\n                \"type\": \"array\"\n              },\n              \"release\": {\n                \"$ref\": \"#/$defs/ReleaseRequirement\"\n              },\n              \"sensitivity\": {\n                \"$ref\": \"#/$defs/Sensitivity\"\n              },\n              \"type\": {\n                \"$ref\": \"#/$defs/ClaimType\"\n              }\n            },\n            \"required\": [\n              \"type\",\n              \"sensitivity\",\n              \"release\",\n              \"mask\"\n            ],\n            \"type\": \"object\"\n          },\n          \"maxItems\": 512,\n          \"type\": \"array\"\n        },\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"registryVersion\": {\n          \"description\": \"Version of the core registry this agent implements — the version of _shared/claim-types.json, not of this task. A client caching the table keys the cache on this.\",\n          \"maxLength\": 32,\n          \"type\": \"string\"\n        },\n        \"strictness\": {\n          \"additionalProperties\": false,\n          \"description\": \"Each axis ordered MOST PROTECTIVE FIRST. This is what makes rule 3 computable by a client: resolution takes the longest registered prefix and the unregistered floor and keeps whichever is more protective, which requires an ordering the client did not invent.\",\n          \"properties\": {\n            \"mask\": {\n              \"items\": {\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"maxItems\": 16,\n              \"type\": \"array\"\n            },\n            \"release\": {\n              \"items\": {\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"maxItems\": 8,\n              \"type\": \"array\"\n            },\n            \"sensitivity\": {\n              \"items\": {\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"maxItems\": 8,\n              \"type\": \"array\"\n            }\n          },\n          \"required\": [\n            \"sensitivity\",\n            \"release\",\n            \"mask\"\n          ],\n          \"type\": \"object\"\n        },\n        \"unregistered\": {\n          \"additionalProperties\": false,\n          \"description\": \"The floor: what a token with no entry and no registered prefix resolves to. Served rather than assumed, because a client that hard-codes it cannot be tightened by a maintainer that raises it.\",\n          \"properties\": {\n            \"mask\": {\n              \"maxLength\": 32,\n              \"type\": \"string\"\n            },\n            \"release\": {\n              \"$ref\": \"#/$defs/ReleaseRequirement\"\n            },\n            \"sensitivity\": {\n              \"$ref\": \"#/$defs/Sensitivity\"\n            }\n          },\n          \"required\": [\n            \"sensitivity\",\n            \"release\",\n            \"mask\"\n          ],\n          \"type\": \"object\"\n        }\n      },\n      \"required\": [\n        \"registryVersion\",\n        \"entries\",\n        \"unregistered\",\n        \"strictness\"\n      ],\n      \"title\": \"Persona Claim Types List — response payload\",\n      \"type\": \"object\"\n    },\n    \"Sensitivity\": {\n      \"description\": \"How carefully a value is shown TO ITS OWN HOLDER. `high` means a consumer masks it by default, reveals it one attribute at a time on a deliberate act, and — the half that is not cosmetic — omits it from a listing that did not ask for sensitive values.\\n\\nAbsent means *not decided by the holder*, not `normal`: a consumer resolves it from the claim-type registry (see CLAIM-TYPES.md §4), which is why this member is optional and why an unregistered token resolves conservatively rather than permissively.\\n\\nDistinct from how linkable the value is. A payment card is highly sensitive and barely linkable — every card number is unique, so knowing one tells a second verifier nothing about the first. Reading either as a proxy for the other produces a consumer that hides the wrong things.\",\n      \"enum\": [\n        \"normal\",\n        \"high\"\n      ],\n      \"title\": \"Sensitivity\",\n      \"type\": \"string\"\n    }\n  },\n  \"$id\": \"https://trusttasks.org/spec/persona/claim-types/list/1.0\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"additionalProperties\": false,\n  \"description\": \"Serve the claim-type registry this agent resolves against. A client that cannot read the table has to ship a copy of it, and a copy is a re-sync obligation on every change.\",\n  \"properties\": {\n    \"ext\": {\n      \"$ref\": \"#/$defs/Ext\"\n    }\n  },\n  \"title\": \"Persona Claim Types List — payload\",\n  \"type\": \"object\"\n}\n",
    );
}
impl crate::Payload for Response {
    const TYPE_URI: &'static str =
        "https://trusttasks.org/spec/persona/claim-types/list/1.0#response";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"ClaimType\": {\n      \"description\": \"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\n      \"maxLength\": 128,\n      \"minLength\": 1,\n      \"pattern\": \"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\",\n      \"title\": \"ClaimType\",\n      \"type\": \"string\"\n    },\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"ReleaseRequirement\": {\n      \"description\": \"What it takes to let a value LEAVE. Distinct from `Sensitivity`, which governs showing it to the holder.\\n\\n`consent` is the ordinary gate: `persona/disclosure/preview` renders what would leave and `persona/disclosure/present` releases it, so a human sees it once.\\n\\n`stepUp` additionally requires a fresh authentication bound to THAT preview — not to the session. Without the binding, \\\"each time\\\" degrades into \\\"once per login\\\", which is the failure the requirement exists to prevent; the single-use `previewId` the preview already mints is what an implementation binds to. A maintainer MUST refuse `persona/disclosure/present` for a `stepUp` attribute when no such approval accompanies it.\\n\\nAbsent means *not decided by the holder* and resolves from the claim-type registry, which defaults `payment.*` and `gov.*` to `stepUp`.\",\n      \"enum\": [\n        \"consent\",\n        \"stepUp\"\n      ],\n      \"title\": \"ReleaseRequirement\",\n      \"type\": \"string\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Success response to persona/claim-types/list. Type https://trusttasks.org/spec/persona/claim-types/list/1.0#response.\",\n      \"properties\": {\n        \"entries\": {\n          \"description\": \"The registered tokens. Both exact tokens and family prefixes appear here, undistinguished: which one an entry is follows from whether a given token equals it or descends from it, and marking them would invite a client to walk only one kind.\",\n          \"items\": {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"mask\": {\n                \"description\": \"Mask style, independent of sensitivity: any style that is not `none` is masked on screen whatever the sensitivity. Named rather than enumerated inline so a maintainer may serve a style this version does not list; a client that does not recognise a style MUST treat it as `full` rather than as `none`.\",\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"minimumSet\": {\n                \"description\": \"Marks a type a community may ask for without it being remarkable. The minimum set contains only normal-sensitivity, consent-release types; that is a rule rather than a coincidence.\",\n                \"type\": \"boolean\"\n              },\n              \"oidc\": {\n                \"description\": \"OIDC standard claims this type answers. A recorded mapping, applied at presentation — the token stays the maintainer's own and no external vocabulary is primary.\",\n                \"items\": {\n                  \"maxLength\": 64,\n                  \"type\": \"string\"\n                },\n                \"maxItems\": 16,\n                \"type\": \"array\"\n              },\n              \"release\": {\n                \"$ref\": \"#/$defs/ReleaseRequirement\"\n              },\n              \"sensitivity\": {\n                \"$ref\": \"#/$defs/Sensitivity\"\n              },\n              \"type\": {\n                \"$ref\": \"#/$defs/ClaimType\"\n              }\n            },\n            \"required\": [\n              \"type\",\n              \"sensitivity\",\n              \"release\",\n              \"mask\"\n            ],\n            \"type\": \"object\"\n          },\n          \"maxItems\": 512,\n          \"type\": \"array\"\n        },\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"registryVersion\": {\n          \"description\": \"Version of the core registry this agent implements — the version of _shared/claim-types.json, not of this task. A client caching the table keys the cache on this.\",\n          \"maxLength\": 32,\n          \"type\": \"string\"\n        },\n        \"strictness\": {\n          \"additionalProperties\": false,\n          \"description\": \"Each axis ordered MOST PROTECTIVE FIRST. This is what makes rule 3 computable by a client: resolution takes the longest registered prefix and the unregistered floor and keeps whichever is more protective, which requires an ordering the client did not invent.\",\n          \"properties\": {\n            \"mask\": {\n              \"items\": {\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"maxItems\": 16,\n              \"type\": \"array\"\n            },\n            \"release\": {\n              \"items\": {\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"maxItems\": 8,\n              \"type\": \"array\"\n            },\n            \"sensitivity\": {\n              \"items\": {\n                \"maxLength\": 32,\n                \"type\": \"string\"\n              },\n              \"maxItems\": 8,\n              \"type\": \"array\"\n            }\n          },\n          \"required\": [\n            \"sensitivity\",\n            \"release\",\n            \"mask\"\n          ],\n          \"type\": \"object\"\n        },\n        \"unregistered\": {\n          \"additionalProperties\": false,\n          \"description\": \"The floor: what a token with no entry and no registered prefix resolves to. Served rather than assumed, because a client that hard-codes it cannot be tightened by a maintainer that raises it.\",\n          \"properties\": {\n            \"mask\": {\n              \"maxLength\": 32,\n              \"type\": \"string\"\n            },\n            \"release\": {\n              \"$ref\": \"#/$defs/ReleaseRequirement\"\n            },\n            \"sensitivity\": {\n              \"$ref\": \"#/$defs/Sensitivity\"\n            }\n          },\n          \"required\": [\n            \"sensitivity\",\n            \"release\",\n            \"mask\"\n          ],\n          \"type\": \"object\"\n        }\n      },\n      \"required\": [\n        \"registryVersion\",\n        \"entries\",\n        \"unregistered\",\n        \"strictness\"\n      ],\n      \"title\": \"Persona Claim Types List — response payload\",\n      \"type\": \"object\"\n    },\n    \"Sensitivity\": {\n      \"description\": \"How carefully a value is shown TO ITS OWN HOLDER. `high` means a consumer masks it by default, reveals it one attribute at a time on a deliberate act, and — the half that is not cosmetic — omits it from a listing that did not ask for sensitive values.\\n\\nAbsent means *not decided by the holder*, not `normal`: a consumer resolves it from the claim-type registry (see CLAIM-TYPES.md §4), which is why this member is optional and why an unregistered token resolves conservatively rather than permissively.\\n\\nDistinct from how linkable the value is. A payment card is highly sensitive and barely linkable — every card number is unique, so knowing one tells a second verifier nothing about the first. Reading either as a proxy for the other produces a consumer that hides the wrong things.\",\n      \"enum\": [\n        \"normal\",\n        \"high\"\n      ],\n      \"title\": \"Sensitivity\",\n      \"type\": \"string\"\n    }\n  },\n  \"$ref\": \"#/$defs/Response\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\"\n}\n",
    );
}
impl crate::RequestPayload for Payload {
    type Response = Response;
}
