//! Generated by `trust-tasks-codegen` — do not edit by hand.
//!
//! Spec slug: `persona/disclosure/history`. Version: `1.0`.
#[allow(unused_imports)]
use serde::{Deserialize, Serialize};
/// Error types.
pub mod error {
    /// Error from a `TryFrom` or `FromStr` implementation.
    pub struct ConversionError(::std::borrow::Cow<'static, str>);
    impl ::std::error::Error for ConversionError {}
    impl ::std::fmt::Display for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Display::fmt(&self.0, f)
        }
    }
    impl ::std::fmt::Debug for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Debug::fmt(&self.0, f)
        }
    }
    impl From<&'static str> for ConversionError {
        fn from(value: &'static str) -> Self {
            Self(value.into())
        }
    }
    impl From<String> for ConversionError {
        fn from(value: String) -> Self {
            Self(value.into())
        }
    }
}
/**
The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.

The token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.

The `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.*/
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "ClaimType",
///  "description": "\nThe vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\n\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\n\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.",
///  "type": "string",
///  "maxLength": 128,
///  "minLength": 1,
///  "pattern": "^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ClaimType(::std::string::String);
impl ::std::ops::Deref for ClaimType {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ClaimType> for ::std::string::String {
    fn from(value: ClaimType) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ClaimType {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 128usize {
            return Err("longer than 128 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err(
                "doesn't match pattern \"^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$\"".into(),
            );
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ClaimType {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Ext",
///  "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
///  "type": "object",
///  "minProperties": 1,
///  "additionalProperties": true,
///  "propertyNames": {
///    "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///  }
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(transparent)]
pub struct Ext(pub ::std::collections::HashMap<ExtKey, ::serde_json::Value>);
impl ::std::ops::Deref for Ext {
    type Target = ::std::collections::HashMap<ExtKey, ::serde_json::Value>;
    fn deref(&self) -> &::std::collections::HashMap<ExtKey, ::serde_json::Value> {
        &self.0
    }
}
impl ::std::convert::From<Ext> for ::std::collections::HashMap<ExtKey, ::serde_json::Value> {
    fn from(value: Ext) -> Self {
        value.0
    }
}
impl ::std::convert::From<::std::collections::HashMap<ExtKey, ::serde_json::Value>> for Ext {
    fn from(value: ::std::collections::HashMap<ExtKey, ::serde_json::Value>) -> Self {
        Self(value)
    }
}
///`ExtKey`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ExtKey(::std::string::String);
impl ::std::ops::Deref for ExtKey {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ExtKey> for ::std::string::String {
    fn from(value: ExtKey) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ExtKey {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ExtKey {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///What the holder has shared, with whom, when, and — because the pool sits above the context boundary — which contexts a given attribute has reached.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "$id": "https://trusttasks.org/spec/persona/disclosure/history/1.0",
///  "title": "Payload",
///  "description": "What the holder has shared, with whom, when, and — because the pool sits above the context boundary — which contexts a given attribute has reached.",
///  "type": "object",
///  "properties": {
///    "attributeType": {
///      "description": "Narrow to one claim type — 'where has my home address gone'. Answering this is the obligation the agent-scoped pool takes on: having put the holder's facts above the context boundary, it owes them an account of which contexts each fact has reached.",
///      "$ref": "#/definitions/ClaimType"
///    },
///    "contextId": {
///      "description": "Narrow to one context. Omit to query across all of them, which only the holder can do and which is the whole reason this task is holder-authorized.",
///      "type": "string",
///      "minLength": 1
///    },
///    "cursor": {
///      "type": "string",
///      "maxLength": 4096
///    },
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "limit": {
///      "default": 100,
///      "type": "integer",
///      "maximum": 500.0,
///      "minimum": 1.0
///    },
///    "since": {
///      "type": "string",
///      "format": "date-time"
///    },
///    "verifierDid": {
///      "description": "Narrow to one verifier — 'what does this site have of mine'.",
///      "type": "string",
///      "maxLength": 2048
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Payload {
    ///Narrow to one claim type — 'where has my home address gone'. Answering this is the obligation the agent-scoped pool takes on: having put the holder's facts above the context boundary, it owes them an account of which contexts each fact has reached.
    #[serde(
        rename = "attributeType",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub attribute_type: ::std::option::Option<ClaimType>,
    ///Narrow to one context. Omit to query across all of them, which only the holder can do and which is the whole reason this task is holder-authorized.
    #[serde(
        rename = "contextId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub context_id: ::std::option::Option<PayloadContextId>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub cursor: ::std::option::Option<PayloadCursor>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    #[serde(default = "defaults::default_nzu64::<::std::num::NonZeroU64, 100>")]
    pub limit: ::std::num::NonZeroU64,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub since: ::std::option::Option<::chrono::DateTime<::chrono::offset::Utc>>,
    ///Narrow to one verifier — 'what does this site have of mine'.
    #[serde(
        rename = "verifierDid",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub verifier_did: ::std::option::Option<PayloadVerifierDid>,
}
impl ::std::default::Default for Payload {
    fn default() -> Self {
        Self {
            attribute_type: Default::default(),
            context_id: Default::default(),
            cursor: Default::default(),
            ext: Default::default(),
            limit: defaults::default_nzu64::<::std::num::NonZeroU64, 100>(),
            since: Default::default(),
            verifier_did: Default::default(),
        }
    }
}
impl Payload {
    pub fn builder() -> builder::Payload {
        Default::default()
    }
}
///Narrow to one context. Omit to query across all of them, which only the holder can do and which is the whole reason this task is holder-authorized.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Narrow to one context. Omit to query across all of them, which only the holder can do and which is the whole reason this task is holder-authorized.",
///  "type": "string",
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct PayloadContextId(::std::string::String);
impl ::std::ops::Deref for PayloadContextId {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<PayloadContextId> for ::std::string::String {
    fn from(value: PayloadContextId) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for PayloadContextId {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for PayloadContextId {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for PayloadContextId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for PayloadContextId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for PayloadContextId {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`PayloadCursor`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 4096
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct PayloadCursor(::std::string::String);
impl ::std::ops::Deref for PayloadCursor {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<PayloadCursor> for ::std::string::String {
    fn from(value: PayloadCursor) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for PayloadCursor {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 4096usize {
            return Err("longer than 4096 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for PayloadCursor {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for PayloadCursor {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for PayloadCursor {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for PayloadCursor {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Narrow to one verifier — 'what does this site have of mine'.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Narrow to one verifier — 'what does this site have of mine'.",
///  "type": "string",
///  "maxLength": 2048
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct PayloadVerifierDid(::std::string::String);
impl ::std::ops::Deref for PayloadVerifierDid {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<PayloadVerifierDid> for ::std::string::String {
    fn from(value: PayloadVerifierDid) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for PayloadVerifierDid {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 2048usize {
            return Err("longer than 2048 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for PayloadVerifierDid {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for PayloadVerifierDid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for PayloadVerifierDid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for PayloadVerifierDid {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
/**
How strongly a credential-backed claim is hidden when presented, ordered most private first. `predicate` proves a statement over a claim without disclosing the claim. `derived` discloses exactly the claims needed via an unlinkable derived proof, so two presentations cannot be joined. `selectiveDisclosure` discloses exactly the claims needed but carries the issuer's signature unchanged, so two presentations ARE linkable. `whole` discloses the entire credential.

The distinction between the first two and the last two is of kind, not degree: only `predicate` and `derived` avoid handing two verifiers a join key. A maintainer MUST default to the highest rung the credential's format supports, and MUST NOT silently fall to a lower one — a request that cannot be satisfied at the rung a producer asked for is refused, because a silent privacy downgrade discloses material the holder believed was hidden.*/
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "ProofRung",
///  "description": "\nHow strongly a credential-backed claim is hidden when presented, ordered most private first. `predicate` proves a statement over a claim without disclosing the claim. `derived` discloses exactly the claims needed via an unlinkable derived proof, so two presentations cannot be joined. `selectiveDisclosure` discloses exactly the claims needed but carries the issuer's signature unchanged, so two presentations ARE linkable. `whole` discloses the entire credential.\n\nThe distinction between the first two and the last two is of kind, not degree: only `predicate` and `derived` avoid handing two verifiers a join key. A maintainer MUST default to the highest rung the credential's format supports, and MUST NOT silently fall to a lower one — a request that cannot be satisfied at the rung a producer asked for is refused, because a silent privacy downgrade discloses material the holder believed was hidden.",
///  "type": "string",
///  "enum": [
///    "predicate",
///    "derived",
///    "selectiveDisclosure",
///    "whole"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum ProofRung {
    #[serde(rename = "predicate")]
    Predicate,
    #[serde(rename = "derived")]
    Derived,
    #[serde(rename = "selectiveDisclosure")]
    SelectiveDisclosure,
    #[serde(rename = "whole")]
    Whole,
}
impl ::std::fmt::Display for ProofRung {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::Predicate => f.write_str("predicate"),
            Self::Derived => f.write_str("derived"),
            Self::SelectiveDisclosure => f.write_str("selectiveDisclosure"),
            Self::Whole => f.write_str("whole"),
        }
    }
}
impl ::std::str::FromStr for ProofRung {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "predicate" => Ok(Self::Predicate),
            "derived" => Ok(Self::Derived),
            "selectiveDisclosure" => Ok(Self::SelectiveDisclosure),
            "whole" => Ok(Self::Whole),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for ProofRung {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ProofRung {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ProofRung {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
///Success response to persona/disclosure/history. Type https://trusttasks.org/spec/persona/disclosure/history/1.0#response. Records name claim TYPES and never values: the history says what kind of thing went where, and re-storing the values would double the exposure it exists to describe.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Response",
///  "description": "Success response to persona/disclosure/history. Type https://trusttasks.org/spec/persona/disclosure/history/1.0#response. Records name claim TYPES and never values: the history says what kind of thing went where, and re-storing the values would double the exposure it exists to describe.",
///  "type": "object",
///  "required": [
///    "disclosures"
///  ],
///  "properties": {
///    "disclosures": {
///      "type": "array",
///      "items": {
///        "type": "object",
///        "required": [
///          "claimTypes",
///          "contextId",
///          "disclosedAt",
///          "disclosureId",
///          "personaDid",
///          "verifierDid"
///        ],
///        "properties": {
///          "claimTypes": {
///            "type": "array",
///            "items": {
///              "$ref": "#/definitions/ClaimType"
///            },
///            "maxItems": 128
///          },
///          "contextId": {
///            "type": "string",
///            "minLength": 1
///          },
///          "disclosedAt": {
///            "type": "string",
///            "format": "date-time"
///          },
///          "disclosureId": {
///            "$ref": "#/definitions/Ulid"
///          },
///          "durableCredentialId": {
///            "description": "Present when the disclosure minted a durable credential — the one kind of disclosure that is still live and still revocable, which is why the history names it.",
///            "type": "string"
///          },
///          "personaDid": {
///            "type": "string",
///            "minLength": 1
///          },
///          "purpose": {
///            "type": "string",
///            "maxLength": 512
///          },
///          "renderer": {
///            "type": "string",
///            "maxLength": 64
///          },
///          "rungs": {
///            "description": "Positionally aligned with claimTypes. A holder reviewing history needs to know not only what went but how strongly it was hidden — the same claim type at two rungs is two very different disclosures.",
///            "type": "array",
///            "items": {
///              "$ref": "#/definitions/ProofRung"
///            },
///            "maxItems": 128
///          },
///          "subject": {
///            "description": "The pairwise identifier the disclosure was made under, so a holder can tell two disclosures to the same verifier apart.",
///            "type": "string"
///          },
///          "verifierDid": {
///            "type": "string",
///            "minLength": 1
///          }
///        },
///        "additionalProperties": false
///      },
///      "maxItems": 500
///    },
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "nextCursor": {
///      "type": "string",
///      "maxLength": 4096
///    }
///  },
///  "additionalProperties": false,
///  "$anchor": "response"
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Response {
    pub disclosures: ::std::vec::Vec<ResponseDisclosuresItem>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    #[serde(
        rename = "nextCursor",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub next_cursor: ::std::option::Option<ResponseNextCursor>,
}
impl Response {
    pub fn builder() -> builder::Response {
        Default::default()
    }
}
///`ResponseDisclosuresItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "object",
///  "required": [
///    "claimTypes",
///    "contextId",
///    "disclosedAt",
///    "disclosureId",
///    "personaDid",
///    "verifierDid"
///  ],
///  "properties": {
///    "claimTypes": {
///      "type": "array",
///      "items": {
///        "$ref": "#/definitions/ClaimType"
///      },
///      "maxItems": 128
///    },
///    "contextId": {
///      "type": "string",
///      "minLength": 1
///    },
///    "disclosedAt": {
///      "type": "string",
///      "format": "date-time"
///    },
///    "disclosureId": {
///      "$ref": "#/definitions/Ulid"
///    },
///    "durableCredentialId": {
///      "description": "Present when the disclosure minted a durable credential — the one kind of disclosure that is still live and still revocable, which is why the history names it.",
///      "type": "string"
///    },
///    "personaDid": {
///      "type": "string",
///      "minLength": 1
///    },
///    "purpose": {
///      "type": "string",
///      "maxLength": 512
///    },
///    "renderer": {
///      "type": "string",
///      "maxLength": 64
///    },
///    "rungs": {
///      "description": "Positionally aligned with claimTypes. A holder reviewing history needs to know not only what went but how strongly it was hidden — the same claim type at two rungs is two very different disclosures.",
///      "type": "array",
///      "items": {
///        "$ref": "#/definitions/ProofRung"
///      },
///      "maxItems": 128
///    },
///    "subject": {
///      "description": "The pairwise identifier the disclosure was made under, so a holder can tell two disclosures to the same verifier apart.",
///      "type": "string"
///    },
///    "verifierDid": {
///      "type": "string",
///      "minLength": 1
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct ResponseDisclosuresItem {
    #[serde(rename = "claimTypes")]
    pub claim_types: ::std::vec::Vec<ClaimType>,
    #[serde(rename = "contextId")]
    pub context_id: ResponseDisclosuresItemContextId,
    #[serde(rename = "disclosedAt")]
    pub disclosed_at: ::chrono::DateTime<::chrono::offset::Utc>,
    #[serde(rename = "disclosureId")]
    pub disclosure_id: Ulid,
    ///Present when the disclosure minted a durable credential — the one kind of disclosure that is still live and still revocable, which is why the history names it.
    #[serde(
        rename = "durableCredentialId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub durable_credential_id: ::std::option::Option<::std::string::String>,
    #[serde(rename = "personaDid")]
    pub persona_did: ResponseDisclosuresItemPersonaDid,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub purpose: ::std::option::Option<ResponseDisclosuresItemPurpose>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub renderer: ::std::option::Option<ResponseDisclosuresItemRenderer>,
    ///Positionally aligned with claimTypes. A holder reviewing history needs to know not only what went but how strongly it was hidden — the same claim type at two rungs is two very different disclosures.
    #[serde(default, skip_serializing_if = "::std::vec::Vec::is_empty")]
    pub rungs: ::std::vec::Vec<ProofRung>,
    ///The pairwise identifier the disclosure was made under, so a holder can tell two disclosures to the same verifier apart.
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub subject: ::std::option::Option<::std::string::String>,
    #[serde(rename = "verifierDid")]
    pub verifier_did: ResponseDisclosuresItemVerifierDid,
}
impl ResponseDisclosuresItem {
    pub fn builder() -> builder::ResponseDisclosuresItem {
        Default::default()
    }
}
///`ResponseDisclosuresItemContextId`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseDisclosuresItemContextId(::std::string::String);
impl ::std::ops::Deref for ResponseDisclosuresItemContextId {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseDisclosuresItemContextId> for ::std::string::String {
    fn from(value: ResponseDisclosuresItemContextId) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseDisclosuresItemContextId {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseDisclosuresItemContextId {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseDisclosuresItemContextId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseDisclosuresItemContextId {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseDisclosuresItemContextId {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseDisclosuresItemPersonaDid`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseDisclosuresItemPersonaDid(::std::string::String);
impl ::std::ops::Deref for ResponseDisclosuresItemPersonaDid {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseDisclosuresItemPersonaDid> for ::std::string::String {
    fn from(value: ResponseDisclosuresItemPersonaDid) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseDisclosuresItemPersonaDid {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseDisclosuresItemPersonaDid {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseDisclosuresItemPersonaDid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseDisclosuresItemPersonaDid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseDisclosuresItemPersonaDid {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseDisclosuresItemPurpose`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 512
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseDisclosuresItemPurpose(::std::string::String);
impl ::std::ops::Deref for ResponseDisclosuresItemPurpose {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseDisclosuresItemPurpose> for ::std::string::String {
    fn from(value: ResponseDisclosuresItemPurpose) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseDisclosuresItemPurpose {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 512usize {
            return Err("longer than 512 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseDisclosuresItemPurpose {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseDisclosuresItemPurpose {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseDisclosuresItemPurpose {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseDisclosuresItemPurpose {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseDisclosuresItemRenderer`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 64
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseDisclosuresItemRenderer(::std::string::String);
impl ::std::ops::Deref for ResponseDisclosuresItemRenderer {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseDisclosuresItemRenderer> for ::std::string::String {
    fn from(value: ResponseDisclosuresItemRenderer) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseDisclosuresItemRenderer {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 64usize {
            return Err("longer than 64 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseDisclosuresItemRenderer {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseDisclosuresItemRenderer {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseDisclosuresItemRenderer {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseDisclosuresItemRenderer {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseDisclosuresItemVerifierDid`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "minLength": 1
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseDisclosuresItemVerifierDid(::std::string::String);
impl ::std::ops::Deref for ResponseDisclosuresItemVerifierDid {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseDisclosuresItemVerifierDid> for ::std::string::String {
    fn from(value: ResponseDisclosuresItemVerifierDid) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseDisclosuresItemVerifierDid {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseDisclosuresItemVerifierDid {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseDisclosuresItemVerifierDid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseDisclosuresItemVerifierDid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseDisclosuresItemVerifierDid {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///`ResponseNextCursor`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "maxLength": 4096
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseNextCursor(::std::string::String);
impl ::std::ops::Deref for ResponseNextCursor {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseNextCursor> for ::std::string::String {
    fn from(value: ResponseNextCursor) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseNextCursor {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 4096usize {
            return Err("longer than 4096 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseNextCursor {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseNextCursor {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseNextCursor {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseNextCursor {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Ulid",
///  "description": "A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.",
///  "type": "string",
///  "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct Ulid(::std::string::String);
impl ::std::ops::Deref for Ulid {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<Ulid> for ::std::string::String {
    fn from(value: Ulid) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for Ulid {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[0-9A-HJKMNP-TV-Z]{26}$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[0-9A-HJKMNP-TV-Z]{26}$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for Ulid {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for Ulid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for Ulid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for Ulid {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
/// Types for composing complex structures.
pub mod builder {
    #[derive(Clone, Debug)]
    pub struct Payload {
        attribute_type:
            ::std::result::Result<::std::option::Option<super::ClaimType>, ::std::string::String>,
        context_id: ::std::result::Result<
            ::std::option::Option<super::PayloadContextId>,
            ::std::string::String,
        >,
        cursor: ::std::result::Result<
            ::std::option::Option<super::PayloadCursor>,
            ::std::string::String,
        >,
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        limit: ::std::result::Result<::std::num::NonZeroU64, ::std::string::String>,
        since: ::std::result::Result<
            ::std::option::Option<::chrono::DateTime<::chrono::offset::Utc>>,
            ::std::string::String,
        >,
        verifier_did: ::std::result::Result<
            ::std::option::Option<super::PayloadVerifierDid>,
            ::std::string::String,
        >,
    }
    impl ::std::default::Default for Payload {
        fn default() -> Self {
            Self {
                attribute_type: Ok(Default::default()),
                context_id: Ok(Default::default()),
                cursor: Ok(Default::default()),
                ext: Ok(Default::default()),
                limit: Ok(super::defaults::default_nzu64::<::std::num::NonZeroU64, 100>()),
                since: Ok(Default::default()),
                verifier_did: Ok(Default::default()),
            }
        }
    }
    impl Payload {
        pub fn attribute_type<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::ClaimType>>,
            T::Error: ::std::fmt::Display,
        {
            self.attribute_type = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for attribute_type: {e}"));
            self
        }
        pub fn context_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::PayloadContextId>>,
            T::Error: ::std::fmt::Display,
        {
            self.context_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for context_id: {e}"));
            self
        }
        pub fn cursor<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::PayloadCursor>>,
            T::Error: ::std::fmt::Display,
        {
            self.cursor = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for cursor: {e}"));
            self
        }
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn limit<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::num::NonZeroU64>,
            T::Error: ::std::fmt::Display,
        {
            self.limit = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for limit: {e}"));
            self
        }
        pub fn since<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<
                ::std::option::Option<::chrono::DateTime<::chrono::offset::Utc>>,
            >,
            T::Error: ::std::fmt::Display,
        {
            self.since = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for since: {e}"));
            self
        }
        pub fn verifier_did<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::PayloadVerifierDid>>,
            T::Error: ::std::fmt::Display,
        {
            self.verifier_did = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for verifier_did: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Payload> for super::Payload {
        type Error = super::error::ConversionError;
        fn try_from(value: Payload) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                attribute_type: value.attribute_type?,
                context_id: value.context_id?,
                cursor: value.cursor?,
                ext: value.ext?,
                limit: value.limit?,
                since: value.since?,
                verifier_did: value.verifier_did?,
            })
        }
    }
    impl ::std::convert::From<super::Payload> for Payload {
        fn from(value: super::Payload) -> Self {
            Self {
                attribute_type: Ok(value.attribute_type),
                context_id: Ok(value.context_id),
                cursor: Ok(value.cursor),
                ext: Ok(value.ext),
                limit: Ok(value.limit),
                since: Ok(value.since),
                verifier_did: Ok(value.verifier_did),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct Response {
        disclosures: ::std::result::Result<
            ::std::vec::Vec<super::ResponseDisclosuresItem>,
            ::std::string::String,
        >,
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        next_cursor: ::std::result::Result<
            ::std::option::Option<super::ResponseNextCursor>,
            ::std::string::String,
        >,
    }
    impl ::std::default::Default for Response {
        fn default() -> Self {
            Self {
                disclosures: Err("no value supplied for disclosures".to_string()),
                ext: Ok(Default::default()),
                next_cursor: Ok(Default::default()),
            }
        }
    }
    impl Response {
        pub fn disclosures<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseDisclosuresItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.disclosures = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for disclosures: {e}"));
            self
        }
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn next_cursor<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::ResponseNextCursor>>,
            T::Error: ::std::fmt::Display,
        {
            self.next_cursor = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for next_cursor: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Response> for super::Response {
        type Error = super::error::ConversionError;
        fn try_from(value: Response) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                disclosures: value.disclosures?,
                ext: value.ext?,
                next_cursor: value.next_cursor?,
            })
        }
    }
    impl ::std::convert::From<super::Response> for Response {
        fn from(value: super::Response) -> Self {
            Self {
                disclosures: Ok(value.disclosures),
                ext: Ok(value.ext),
                next_cursor: Ok(value.next_cursor),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct ResponseDisclosuresItem {
        claim_types:
            ::std::result::Result<::std::vec::Vec<super::ClaimType>, ::std::string::String>,
        context_id:
            ::std::result::Result<super::ResponseDisclosuresItemContextId, ::std::string::String>,
        disclosed_at:
            ::std::result::Result<::chrono::DateTime<::chrono::offset::Utc>, ::std::string::String>,
        disclosure_id: ::std::result::Result<super::Ulid, ::std::string::String>,
        durable_credential_id: ::std::result::Result<
            ::std::option::Option<::std::string::String>,
            ::std::string::String,
        >,
        persona_did:
            ::std::result::Result<super::ResponseDisclosuresItemPersonaDid, ::std::string::String>,
        purpose: ::std::result::Result<
            ::std::option::Option<super::ResponseDisclosuresItemPurpose>,
            ::std::string::String,
        >,
        renderer: ::std::result::Result<
            ::std::option::Option<super::ResponseDisclosuresItemRenderer>,
            ::std::string::String,
        >,
        rungs: ::std::result::Result<::std::vec::Vec<super::ProofRung>, ::std::string::String>,
        subject: ::std::result::Result<
            ::std::option::Option<::std::string::String>,
            ::std::string::String,
        >,
        verifier_did:
            ::std::result::Result<super::ResponseDisclosuresItemVerifierDid, ::std::string::String>,
    }
    impl ::std::default::Default for ResponseDisclosuresItem {
        fn default() -> Self {
            Self {
                claim_types: Err("no value supplied for claim_types".to_string()),
                context_id: Err("no value supplied for context_id".to_string()),
                disclosed_at: Err("no value supplied for disclosed_at".to_string()),
                disclosure_id: Err("no value supplied for disclosure_id".to_string()),
                durable_credential_id: Ok(Default::default()),
                persona_did: Err("no value supplied for persona_did".to_string()),
                purpose: Ok(Default::default()),
                renderer: Ok(Default::default()),
                rungs: Ok(Default::default()),
                subject: Ok(Default::default()),
                verifier_did: Err("no value supplied for verifier_did".to_string()),
            }
        }
    }
    impl ResponseDisclosuresItem {
        pub fn claim_types<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ClaimType>>,
            T::Error: ::std::fmt::Display,
        {
            self.claim_types = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for claim_types: {e}"));
            self
        }
        pub fn context_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseDisclosuresItemContextId>,
            T::Error: ::std::fmt::Display,
        {
            self.context_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for context_id: {e}"));
            self
        }
        pub fn disclosed_at<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::chrono::DateTime<::chrono::offset::Utc>>,
            T::Error: ::std::fmt::Display,
        {
            self.disclosed_at = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for disclosed_at: {e}"));
            self
        }
        pub fn disclosure_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::Ulid>,
            T::Error: ::std::fmt::Display,
        {
            self.disclosure_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for disclosure_id: {e}"));
            self
        }
        pub fn durable_credential_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<::std::string::String>>,
            T::Error: ::std::fmt::Display,
        {
            self.durable_credential_id = value.try_into().map_err(|e| {
                format!("error converting supplied value for durable_credential_id: {e}")
            });
            self
        }
        pub fn persona_did<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseDisclosuresItemPersonaDid>,
            T::Error: ::std::fmt::Display,
        {
            self.persona_did = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for persona_did: {e}"));
            self
        }
        pub fn purpose<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<
                ::std::option::Option<super::ResponseDisclosuresItemPurpose>,
            >,
            T::Error: ::std::fmt::Display,
        {
            self.purpose = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for purpose: {e}"));
            self
        }
        pub fn renderer<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<
                ::std::option::Option<super::ResponseDisclosuresItemRenderer>,
            >,
            T::Error: ::std::fmt::Display,
        {
            self.renderer = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for renderer: {e}"));
            self
        }
        pub fn rungs<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ProofRung>>,
            T::Error: ::std::fmt::Display,
        {
            self.rungs = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for rungs: {e}"));
            self
        }
        pub fn subject<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<::std::string::String>>,
            T::Error: ::std::fmt::Display,
        {
            self.subject = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for subject: {e}"));
            self
        }
        pub fn verifier_did<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseDisclosuresItemVerifierDid>,
            T::Error: ::std::fmt::Display,
        {
            self.verifier_did = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for verifier_did: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<ResponseDisclosuresItem> for super::ResponseDisclosuresItem {
        type Error = super::error::ConversionError;
        fn try_from(
            value: ResponseDisclosuresItem,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                claim_types: value.claim_types?,
                context_id: value.context_id?,
                disclosed_at: value.disclosed_at?,
                disclosure_id: value.disclosure_id?,
                durable_credential_id: value.durable_credential_id?,
                persona_did: value.persona_did?,
                purpose: value.purpose?,
                renderer: value.renderer?,
                rungs: value.rungs?,
                subject: value.subject?,
                verifier_did: value.verifier_did?,
            })
        }
    }
    impl ::std::convert::From<super::ResponseDisclosuresItem> for ResponseDisclosuresItem {
        fn from(value: super::ResponseDisclosuresItem) -> Self {
            Self {
                claim_types: Ok(value.claim_types),
                context_id: Ok(value.context_id),
                disclosed_at: Ok(value.disclosed_at),
                disclosure_id: Ok(value.disclosure_id),
                durable_credential_id: Ok(value.durable_credential_id),
                persona_did: Ok(value.persona_did),
                purpose: Ok(value.purpose),
                renderer: Ok(value.renderer),
                rungs: Ok(value.rungs),
                subject: Ok(value.subject),
                verifier_did: Ok(value.verifier_did),
            }
        }
    }
}
/// Generation of default values for serde.
pub mod defaults {
    pub(super) fn default_nzu64<T, const V: u64>() -> T
    where
        T: ::std::convert::TryFrom<::std::num::NonZeroU64>,
        <T as ::std::convert::TryFrom<::std::num::NonZeroU64>>::Error: ::std::fmt::Debug,
    {
        T::try_from(::std::num::NonZeroU64::try_from(V).unwrap()).unwrap()
    }
}
impl crate::Payload for Payload {
    const TYPE_URI: &'static str = "https://trusttasks.org/spec/persona/disclosure/history/1.0";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"ClaimType\": {\n      \"description\": \"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\n      \"maxLength\": 128,\n      \"minLength\": 1,\n      \"pattern\": \"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\",\n      \"title\": \"ClaimType\",\n      \"type\": \"string\"\n    },\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"ProofRung\": {\n      \"description\": \"How strongly a credential-backed claim is hidden when presented, ordered most private first. `predicate` proves a statement over a claim without disclosing the claim. `derived` discloses exactly the claims needed via an unlinkable derived proof, so two presentations cannot be joined. `selectiveDisclosure` discloses exactly the claims needed but carries the issuer's signature unchanged, so two presentations ARE linkable. `whole` discloses the entire credential.\\n\\nThe distinction between the first two and the last two is of kind, not degree: only `predicate` and `derived` avoid handing two verifiers a join key. A maintainer MUST default to the highest rung the credential's format supports, and MUST NOT silently fall to a lower one — a request that cannot be satisfied at the rung a producer asked for is refused, because a silent privacy downgrade discloses material the holder believed was hidden.\",\n      \"enum\": [\n        \"predicate\",\n        \"derived\",\n        \"selectiveDisclosure\",\n        \"whole\"\n      ],\n      \"title\": \"ProofRung\",\n      \"type\": \"string\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Success response to persona/disclosure/history. Type https://trusttasks.org/spec/persona/disclosure/history/1.0#response. Records name claim TYPES and never values: the history says what kind of thing went where, and re-storing the values would double the exposure it exists to describe.\",\n      \"properties\": {\n        \"disclosures\": {\n          \"items\": {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"claimTypes\": {\n                \"items\": {\n                  \"$ref\": \"#/$defs/ClaimType\"\n                },\n                \"maxItems\": 128,\n                \"type\": \"array\"\n              },\n              \"contextId\": {\n                \"minLength\": 1,\n                \"type\": \"string\"\n              },\n              \"disclosedAt\": {\n                \"format\": \"date-time\",\n                \"type\": \"string\"\n              },\n              \"disclosureId\": {\n                \"$ref\": \"#/$defs/Ulid\"\n              },\n              \"durableCredentialId\": {\n                \"description\": \"Present when the disclosure minted a durable credential — the one kind of disclosure that is still live and still revocable, which is why the history names it.\",\n                \"type\": \"string\"\n              },\n              \"personaDid\": {\n                \"minLength\": 1,\n                \"type\": \"string\"\n              },\n              \"purpose\": {\n                \"maxLength\": 512,\n                \"type\": \"string\"\n              },\n              \"renderer\": {\n                \"maxLength\": 64,\n                \"type\": \"string\"\n              },\n              \"rungs\": {\n                \"description\": \"Positionally aligned with claimTypes. A holder reviewing history needs to know not only what went but how strongly it was hidden — the same claim type at two rungs is two very different disclosures.\",\n                \"items\": {\n                  \"$ref\": \"#/$defs/ProofRung\"\n                },\n                \"maxItems\": 128,\n                \"type\": \"array\"\n              },\n              \"subject\": {\n                \"description\": \"The pairwise identifier the disclosure was made under, so a holder can tell two disclosures to the same verifier apart.\",\n                \"type\": \"string\"\n              },\n              \"verifierDid\": {\n                \"minLength\": 1,\n                \"type\": \"string\"\n              }\n            },\n            \"required\": [\n              \"disclosureId\",\n              \"contextId\",\n              \"verifierDid\",\n              \"personaDid\",\n              \"claimTypes\",\n              \"disclosedAt\"\n            ],\n            \"type\": \"object\"\n          },\n          \"maxItems\": 500,\n          \"type\": \"array\"\n        },\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"nextCursor\": {\n          \"maxLength\": 4096,\n          \"type\": \"string\"\n        }\n      },\n      \"required\": [\n        \"disclosures\"\n      ],\n      \"title\": \"Persona Disclosure History — response payload\",\n      \"type\": \"object\"\n    },\n    \"Ulid\": {\n      \"description\": \"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\n      \"pattern\": \"^[0-9A-HJKMNP-TV-Z]{26}$\",\n      \"title\": \"Ulid\",\n      \"type\": \"string\"\n    }\n  },\n  \"$id\": \"https://trusttasks.org/spec/persona/disclosure/history/1.0\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"additionalProperties\": false,\n  \"description\": \"What the holder has shared, with whom, when, and — because the pool sits above the context boundary — which contexts a given attribute has reached.\",\n  \"properties\": {\n    \"attributeType\": {\n      \"$ref\": \"#/$defs/ClaimType\",\n      \"description\": \"Narrow to one claim type — 'where has my home address gone'. Answering this is the obligation the agent-scoped pool takes on: having put the holder's facts above the context boundary, it owes them an account of which contexts each fact has reached.\"\n    },\n    \"contextId\": {\n      \"description\": \"Narrow to one context. Omit to query across all of them, which only the holder can do and which is the whole reason this task is holder-authorized.\",\n      \"minLength\": 1,\n      \"type\": \"string\"\n    },\n    \"cursor\": {\n      \"maxLength\": 4096,\n      \"type\": \"string\"\n    },\n    \"ext\": {\n      \"$ref\": \"#/$defs/Ext\"\n    },\n    \"limit\": {\n      \"default\": 100,\n      \"maximum\": 500,\n      \"minimum\": 1,\n      \"type\": \"integer\"\n    },\n    \"since\": {\n      \"format\": \"date-time\",\n      \"type\": \"string\"\n    },\n    \"verifierDid\": {\n      \"description\": \"Narrow to one verifier — 'what does this site have of mine'.\",\n      \"maxLength\": 2048,\n      \"type\": \"string\"\n    }\n  },\n  \"title\": \"Persona Disclosure History — payload\",\n  \"type\": \"object\"\n}\n",
    );
}
impl crate::Payload for Response {
    const TYPE_URI: &'static str =
        "https://trusttasks.org/spec/persona/disclosure/history/1.0#response";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"ClaimType\": {\n      \"description\": \"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\n      \"maxLength\": 128,\n      \"minLength\": 1,\n      \"pattern\": \"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\",\n      \"title\": \"ClaimType\",\n      \"type\": \"string\"\n    },\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"ProofRung\": {\n      \"description\": \"How strongly a credential-backed claim is hidden when presented, ordered most private first. `predicate` proves a statement over a claim without disclosing the claim. `derived` discloses exactly the claims needed via an unlinkable derived proof, so two presentations cannot be joined. `selectiveDisclosure` discloses exactly the claims needed but carries the issuer's signature unchanged, so two presentations ARE linkable. `whole` discloses the entire credential.\\n\\nThe distinction between the first two and the last two is of kind, not degree: only `predicate` and `derived` avoid handing two verifiers a join key. A maintainer MUST default to the highest rung the credential's format supports, and MUST NOT silently fall to a lower one — a request that cannot be satisfied at the rung a producer asked for is refused, because a silent privacy downgrade discloses material the holder believed was hidden.\",\n      \"enum\": [\n        \"predicate\",\n        \"derived\",\n        \"selectiveDisclosure\",\n        \"whole\"\n      ],\n      \"title\": \"ProofRung\",\n      \"type\": \"string\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Success response to persona/disclosure/history. Type https://trusttasks.org/spec/persona/disclosure/history/1.0#response. Records name claim TYPES and never values: the history says what kind of thing went where, and re-storing the values would double the exposure it exists to describe.\",\n      \"properties\": {\n        \"disclosures\": {\n          \"items\": {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"claimTypes\": {\n                \"items\": {\n                  \"$ref\": \"#/$defs/ClaimType\"\n                },\n                \"maxItems\": 128,\n                \"type\": \"array\"\n              },\n              \"contextId\": {\n                \"minLength\": 1,\n                \"type\": \"string\"\n              },\n              \"disclosedAt\": {\n                \"format\": \"date-time\",\n                \"type\": \"string\"\n              },\n              \"disclosureId\": {\n                \"$ref\": \"#/$defs/Ulid\"\n              },\n              \"durableCredentialId\": {\n                \"description\": \"Present when the disclosure minted a durable credential — the one kind of disclosure that is still live and still revocable, which is why the history names it.\",\n                \"type\": \"string\"\n              },\n              \"personaDid\": {\n                \"minLength\": 1,\n                \"type\": \"string\"\n              },\n              \"purpose\": {\n                \"maxLength\": 512,\n                \"type\": \"string\"\n              },\n              \"renderer\": {\n                \"maxLength\": 64,\n                \"type\": \"string\"\n              },\n              \"rungs\": {\n                \"description\": \"Positionally aligned with claimTypes. A holder reviewing history needs to know not only what went but how strongly it was hidden — the same claim type at two rungs is two very different disclosures.\",\n                \"items\": {\n                  \"$ref\": \"#/$defs/ProofRung\"\n                },\n                \"maxItems\": 128,\n                \"type\": \"array\"\n              },\n              \"subject\": {\n                \"description\": \"The pairwise identifier the disclosure was made under, so a holder can tell two disclosures to the same verifier apart.\",\n                \"type\": \"string\"\n              },\n              \"verifierDid\": {\n                \"minLength\": 1,\n                \"type\": \"string\"\n              }\n            },\n            \"required\": [\n              \"disclosureId\",\n              \"contextId\",\n              \"verifierDid\",\n              \"personaDid\",\n              \"claimTypes\",\n              \"disclosedAt\"\n            ],\n            \"type\": \"object\"\n          },\n          \"maxItems\": 500,\n          \"type\": \"array\"\n        },\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"nextCursor\": {\n          \"maxLength\": 4096,\n          \"type\": \"string\"\n        }\n      },\n      \"required\": [\n        \"disclosures\"\n      ],\n      \"title\": \"Persona Disclosure History — response payload\",\n      \"type\": \"object\"\n    },\n    \"Ulid\": {\n      \"description\": \"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\n      \"pattern\": \"^[0-9A-HJKMNP-TV-Z]{26}$\",\n      \"title\": \"Ulid\",\n      \"type\": \"string\"\n    }\n  },\n  \"$ref\": \"#/$defs/Response\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\"\n}\n",
    );
}
impl crate::RequestPayload for Payload {
    type Response = Response;
}
