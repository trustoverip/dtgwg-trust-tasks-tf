//! Generated by `trust-tasks-codegen` — do not edit by hand.
//!
//! Spec slug: `persona/correlation/analyze`. Version: `1.0`.
#[allow(unused_imports)]
use serde::{Deserialize, Serialize};
/// Error types.
pub mod error {
    /// Error from a `TryFrom` or `FromStr` implementation.
    pub struct ConversionError(::std::borrow::Cow<'static, str>);
    impl ::std::error::Error for ConversionError {}
    impl ::std::fmt::Display for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Display::fmt(&self.0, f)
        }
    }
    impl ::std::fmt::Debug for ConversionError {
        fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> Result<(), ::std::fmt::Error> {
            ::std::fmt::Debug::fmt(&self.0, f)
        }
    }
    impl From<&'static str> for ConversionError {
        fn from(value: &'static str) -> Self {
            Self(value.into())
        }
    }
    impl From<String> for ConversionError {
        fn from(value: String) -> Self {
            Self(value.into())
        }
    }
}
/**
The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.

The token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.

The `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.*/
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "ClaimType",
///  "description": "\nThe vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\n\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\n\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.",
///  "type": "string",
///  "maxLength": 128,
///  "minLength": 1,
///  "pattern": "^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ClaimType(::std::string::String);
impl ::std::ops::Deref for ClaimType {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ClaimType> for ::std::string::String {
    fn from(value: ClaimType) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ClaimType {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 128usize {
            return Err("longer than 128 characters".into());
        }
        if value.chars().count() < 1usize {
            return Err("shorter than 1 characters".into());
        }
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err(
                "doesn't match pattern \"^(x:)?[a-z][a-zA-Z0-9]*(\\.[a-z][a-zA-Z0-9]*)*$\"".into(),
            );
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ClaimType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ClaimType {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Ext",
///  "description": "Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.",
///  "type": "object",
///  "minProperties": 1,
///  "additionalProperties": true,
///  "propertyNames": {
///    "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///  }
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(transparent)]
pub struct Ext(pub ::std::collections::HashMap<ExtKey, ::serde_json::Value>);
impl ::std::ops::Deref for Ext {
    type Target = ::std::collections::HashMap<ExtKey, ::serde_json::Value>;
    fn deref(&self) -> &::std::collections::HashMap<ExtKey, ::serde_json::Value> {
        &self.0
    }
}
impl ::std::convert::From<Ext> for ::std::collections::HashMap<ExtKey, ::serde_json::Value> {
    fn from(value: Ext) -> Self {
        value.0
    }
}
impl ::std::convert::From<::std::collections::HashMap<ExtKey, ::serde_json::Value>> for Ext {
    fn from(value: ::std::collections::HashMap<ExtKey, ::serde_json::Value>) -> Self {
        Self(value)
    }
}
///`ExtKey`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "pattern": "^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ExtKey(::std::string::String);
impl ::std::ops::Deref for ExtKey {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ExtKey> for ::std::string::String {
    fn from(value: ExtKey) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ExtKey {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[a-z][a-z0-9-]*(\\.[a-z0-9-]+)+$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ExtKey {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ExtKey {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///Report where the holder's identities link to one another, and what can be done about each link. The findings task behind the counts the write tasks return.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "$id": "https://trusttasks.org/spec/persona/correlation/analyze/1.0",
///  "title": "Payload",
///  "description": "Report where the holder's identities link to one another, and what can be done about each link. The findings task behind the counts the write tasks return.",
///  "type": "object",
///  "properties": {
///    "attributeId": {
///      "description": "Analyse one attribute. Omit to analyse the whole pool.",
///      "$ref": "#/definitions/Ulid"
///    },
///    "candidate": {
///      "description": "Analyse a value the holder is CONSIDERING but has not written. This is what lets a builder warn before the mistake rather than after it, which is the difference between a guard and a report. A maintainer MUST NOT store a candidate.",
///      "type": "object",
///      "required": [
///        "type",
///        "value",
///        "valueType"
///      ],
///      "properties": {
///        "type": {
///          "$ref": "#/definitions/ClaimType"
///        },
///        "value": {},
///        "valueType": {
///          "$ref": "#/definitions/ValueType"
///        }
///      },
///      "additionalProperties": false
///    },
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "profileId": {
///      "description": "Analyse one composition — what would this profile link if bound.",
///      "$ref": "#/definitions/Ulid"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Payload {
    ///Analyse one attribute. Omit to analyse the whole pool.
    #[serde(
        rename = "attributeId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub attribute_id: ::std::option::Option<Ulid>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub candidate: ::std::option::Option<PayloadCandidate>,
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    ///Analyse one composition — what would this profile link if bound.
    #[serde(
        rename = "profileId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub profile_id: ::std::option::Option<Ulid>,
}
impl ::std::default::Default for Payload {
    fn default() -> Self {
        Self {
            attribute_id: Default::default(),
            candidate: Default::default(),
            ext: Default::default(),
            profile_id: Default::default(),
        }
    }
}
impl Payload {
    pub fn builder() -> builder::Payload {
        Default::default()
    }
}
///Analyse a value the holder is CONSIDERING but has not written. This is what lets a builder warn before the mistake rather than after it, which is the difference between a guard and a report. A maintainer MUST NOT store a candidate.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Analyse a value the holder is CONSIDERING but has not written. This is what lets a builder warn before the mistake rather than after it, which is the difference between a guard and a report. A maintainer MUST NOT store a candidate.",
///  "type": "object",
///  "required": [
///    "type",
///    "value",
///    "valueType"
///  ],
///  "properties": {
///    "type": {
///      "$ref": "#/definitions/ClaimType"
///    },
///    "value": {},
///    "valueType": {
///      "$ref": "#/definitions/ValueType"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct PayloadCandidate {
    #[serde(rename = "type")]
    pub type_: ClaimType,
    pub value: ::serde_json::Value,
    #[serde(rename = "valueType")]
    pub value_type: ValueType,
}
impl PayloadCandidate {
    pub fn builder() -> builder::PayloadCandidate {
        Default::default()
    }
}
///Success response to persona/correlation/analyze. Type https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Response",
///  "description": "Success response to persona/correlation/analyze. Type https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.",
///  "type": "object",
///  "required": [
///    "findings"
///  ],
///  "properties": {
///    "ext": {
///      "$ref": "#/definitions/Ext"
///    },
///    "findings": {
///      "type": "array",
///      "items": {
///        "type": "object",
///        "required": [
///          "remedies",
///          "severity",
///          "why"
///        ],
///        "properties": {
///          "attributeId": {
///            "$ref": "#/definitions/Ulid"
///          },
///          "remedies": {
///            "description": "What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told 'this links your personas' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it.",
///            "type": "array",
///            "items": {
///              "type": "string",
///              "enum": [
///                "useDifferentValue",
///                "reissueCredentialToThisDid",
///                "correlateDeliberately",
///                "proceedAndRecord"
///              ]
///            },
///            "maxItems": 8
///          },
///          "severity": {
///            "type": "string",
///            "enum": [
///              "low",
///              "high"
///            ]
///          },
///          "sharedWith": {
///            "description": "Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.",
///            "type": "array",
///            "items": {
///              "type": "object",
///              "properties": {
///                "contextId": {
///                  "type": "string"
///                },
///                "disclosedTo": {
///                  "type": "array",
///                  "items": {
///                    "type": "string"
///                  },
///                  "maxItems": 64
///                },
///                "personaDid": {
///                  "type": "string"
///                },
///                "profileId": {
///                  "$ref": "#/definitions/Ulid"
///                }
///              },
///              "additionalProperties": false
///            },
///            "maxItems": 128
///          },
///          "why": {
///            "description": "Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.",
///            "type": "string",
///            "maxLength": 1024
///          }
///        },
///        "additionalProperties": false
///      },
///      "maxItems": 256
///    }
///  },
///  "additionalProperties": false,
///  "$anchor": "response"
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct Response {
    #[serde(default, skip_serializing_if = "::std::option::Option::is_none")]
    pub ext: ::std::option::Option<Ext>,
    pub findings: ::std::vec::Vec<ResponseFindingsItem>,
}
impl Response {
    pub fn builder() -> builder::Response {
        Default::default()
    }
}
///`ResponseFindingsItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "object",
///  "required": [
///    "remedies",
///    "severity",
///    "why"
///  ],
///  "properties": {
///    "attributeId": {
///      "$ref": "#/definitions/Ulid"
///    },
///    "remedies": {
///      "description": "What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told 'this links your personas' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it.",
///      "type": "array",
///      "items": {
///        "type": "string",
///        "enum": [
///          "useDifferentValue",
///          "reissueCredentialToThisDid",
///          "correlateDeliberately",
///          "proceedAndRecord"
///        ]
///      },
///      "maxItems": 8
///    },
///    "severity": {
///      "type": "string",
///      "enum": [
///        "low",
///        "high"
///      ]
///    },
///    "sharedWith": {
///      "description": "Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.",
///      "type": "array",
///      "items": {
///        "type": "object",
///        "properties": {
///          "contextId": {
///            "type": "string"
///          },
///          "disclosedTo": {
///            "type": "array",
///            "items": {
///              "type": "string"
///            },
///            "maxItems": 64
///          },
///          "personaDid": {
///            "type": "string"
///          },
///          "profileId": {
///            "$ref": "#/definitions/Ulid"
///          }
///        },
///        "additionalProperties": false
///      },
///      "maxItems": 128
///    },
///    "why": {
///      "description": "Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.",
///      "type": "string",
///      "maxLength": 1024
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct ResponseFindingsItem {
    #[serde(
        rename = "attributeId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub attribute_id: ::std::option::Option<Ulid>,
    ///What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told 'this links your personas' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it.
    pub remedies: ::std::vec::Vec<ResponseFindingsItemRemediesItem>,
    pub severity: ResponseFindingsItemSeverity,
    ///Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.
    #[serde(
        rename = "sharedWith",
        default,
        skip_serializing_if = "::std::vec::Vec::is_empty"
    )]
    pub shared_with: ::std::vec::Vec<ResponseFindingsItemSharedWithItem>,
    ///Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.
    pub why: ResponseFindingsItemWhy,
}
impl ResponseFindingsItem {
    pub fn builder() -> builder::ResponseFindingsItem {
        Default::default()
    }
}
///`ResponseFindingsItemRemediesItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "enum": [
///    "useDifferentValue",
///    "reissueCredentialToThisDid",
///    "correlateDeliberately",
///    "proceedAndRecord"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum ResponseFindingsItemRemediesItem {
    #[serde(rename = "useDifferentValue")]
    UseDifferentValue,
    #[serde(rename = "reissueCredentialToThisDid")]
    ReissueCredentialToThisDid,
    #[serde(rename = "correlateDeliberately")]
    CorrelateDeliberately,
    #[serde(rename = "proceedAndRecord")]
    ProceedAndRecord,
}
impl ::std::fmt::Display for ResponseFindingsItemRemediesItem {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::UseDifferentValue => f.write_str("useDifferentValue"),
            Self::ReissueCredentialToThisDid => f.write_str("reissueCredentialToThisDid"),
            Self::CorrelateDeliberately => f.write_str("correlateDeliberately"),
            Self::ProceedAndRecord => f.write_str("proceedAndRecord"),
        }
    }
}
impl ::std::str::FromStr for ResponseFindingsItemRemediesItem {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "useDifferentValue" => Ok(Self::UseDifferentValue),
            "reissueCredentialToThisDid" => Ok(Self::ReissueCredentialToThisDid),
            "correlateDeliberately" => Ok(Self::CorrelateDeliberately),
            "proceedAndRecord" => Ok(Self::ProceedAndRecord),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for ResponseFindingsItemRemediesItem {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseFindingsItemRemediesItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseFindingsItemRemediesItem {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
///`ResponseFindingsItemSeverity`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "string",
///  "enum": [
///    "low",
///    "high"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum ResponseFindingsItemSeverity {
    #[serde(rename = "low")]
    Low,
    #[serde(rename = "high")]
    High,
}
impl ::std::fmt::Display for ResponseFindingsItemSeverity {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::Low => f.write_str("low"),
            Self::High => f.write_str("high"),
        }
    }
}
impl ::std::str::FromStr for ResponseFindingsItemSeverity {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "low" => Ok(Self::Low),
            "high" => Ok(Self::High),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for ResponseFindingsItemSeverity {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseFindingsItemSeverity {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseFindingsItemSeverity {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
///`ResponseFindingsItemSharedWithItem`
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "type": "object",
///  "properties": {
///    "contextId": {
///      "type": "string"
///    },
///    "disclosedTo": {
///      "type": "array",
///      "items": {
///        "type": "string"
///      },
///      "maxItems": 64
///    },
///    "personaDid": {
///      "type": "string"
///    },
///    "profileId": {
///      "$ref": "#/definitions/Ulid"
///    }
///  },
///  "additionalProperties": false
///}
/// ```
/// </details>
#[derive(::serde::Deserialize, ::serde::Serialize, Clone, Debug)]
#[serde(deny_unknown_fields)]
#[non_exhaustive]
pub struct ResponseFindingsItemSharedWithItem {
    #[serde(
        rename = "contextId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub context_id: ::std::option::Option<::std::string::String>,
    #[serde(
        rename = "disclosedTo",
        default,
        skip_serializing_if = "::std::vec::Vec::is_empty"
    )]
    pub disclosed_to: ::std::vec::Vec<::std::string::String>,
    #[serde(
        rename = "personaDid",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub persona_did: ::std::option::Option<::std::string::String>,
    #[serde(
        rename = "profileId",
        default,
        skip_serializing_if = "::std::option::Option::is_none"
    )]
    pub profile_id: ::std::option::Option<Ulid>,
}
impl ::std::default::Default for ResponseFindingsItemSharedWithItem {
    fn default() -> Self {
        Self {
            context_id: Default::default(),
            disclosed_to: Default::default(),
            persona_did: Default::default(),
            profile_id: Default::default(),
        }
    }
}
impl ResponseFindingsItemSharedWithItem {
    pub fn builder() -> builder::ResponseFindingsItemSharedWithItem {
        Default::default()
    }
}
///Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "description": "Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.",
///  "type": "string",
///  "maxLength": 1024
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct ResponseFindingsItemWhy(::std::string::String);
impl ::std::ops::Deref for ResponseFindingsItemWhy {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<ResponseFindingsItemWhy> for ::std::string::String {
    fn from(value: ResponseFindingsItemWhy) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for ResponseFindingsItemWhy {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        if value.chars().count() > 1024usize {
            return Err("longer than 1024 characters".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for ResponseFindingsItemWhy {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ResponseFindingsItemWhy {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ResponseFindingsItemWhy {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for ResponseFindingsItemWhy {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "Ulid",
///  "description": "A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.",
///  "type": "string",
///  "pattern": "^[0-9A-HJKMNP-TV-Z]{26}$"
///}
/// ```
/// </details>
#[derive(::serde::Serialize, Clone, Debug, Eq, Hash, Ord, PartialEq, PartialOrd)]
#[serde(transparent)]
pub struct Ulid(::std::string::String);
impl ::std::ops::Deref for Ulid {
    type Target = ::std::string::String;
    fn deref(&self) -> &::std::string::String {
        &self.0
    }
}
impl ::std::convert::From<Ulid> for ::std::string::String {
    fn from(value: Ulid) -> Self {
        value.0
    }
}
impl ::std::str::FromStr for Ulid {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        static PATTERN: ::std::sync::LazyLock<::regress::Regex> =
            ::std::sync::LazyLock::new(|| {
                ::regress::Regex::new("^[0-9A-HJKMNP-TV-Z]{26}$").unwrap()
            });
        if PATTERN.find(value).is_none() {
            return Err("doesn't match pattern \"^[0-9A-HJKMNP-TV-Z]{26}$\"".into());
        }
        Ok(Self(value.to_string()))
    }
}
impl ::std::convert::TryFrom<&str> for Ulid {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for Ulid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for Ulid {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl<'de> ::serde::Deserialize<'de> for Ulid {
    fn deserialize<D>(deserializer: D) -> ::std::result::Result<Self, D::Error>
    where
        D: ::serde::Deserializer<'de>,
    {
        ::std::string::String::deserialize(deserializer)?
            .parse()
            .map_err(|e: self::error::ConversionError| {
                <D::Error as ::serde::de::Error>::custom(e.to_string())
            })
    }
}
///The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer's affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer's release.
///
/// <details><summary>JSON schema</summary>
///
/// ```json
///{
///  "title": "ValueType",
///  "description": "The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer's affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer's release.",
///  "type": "string",
///  "enum": [
///    "string",
///    "number",
///    "boolean",
///    "date",
///    "object"
///  ]
///}
/// ```
/// </details>
#[derive(
    ::serde::Deserialize,
    ::serde::Serialize,
    Clone,
    Copy,
    Debug,
    Eq,
    Hash,
    Ord,
    PartialEq,
    PartialOrd,
)]
#[non_exhaustive]
pub enum ValueType {
    #[serde(rename = "string")]
    String,
    #[serde(rename = "number")]
    Number,
    #[serde(rename = "boolean")]
    Boolean,
    #[serde(rename = "date")]
    Date,
    #[serde(rename = "object")]
    Object,
}
impl ::std::fmt::Display for ValueType {
    fn fmt(&self, f: &mut ::std::fmt::Formatter<'_>) -> ::std::fmt::Result {
        match *self {
            Self::String => f.write_str("string"),
            Self::Number => f.write_str("number"),
            Self::Boolean => f.write_str("boolean"),
            Self::Date => f.write_str("date"),
            Self::Object => f.write_str("object"),
        }
    }
}
impl ::std::str::FromStr for ValueType {
    type Err = self::error::ConversionError;
    fn from_str(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        match value {
            "string" => Ok(Self::String),
            "number" => Ok(Self::Number),
            "boolean" => Ok(Self::Boolean),
            "date" => Ok(Self::Date),
            "object" => Ok(Self::Object),
            _ => Err("invalid value".into()),
        }
    }
}
impl ::std::convert::TryFrom<&str> for ValueType {
    type Error = self::error::ConversionError;
    fn try_from(value: &str) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<&::std::string::String> for ValueType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: &::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
impl ::std::convert::TryFrom<::std::string::String> for ValueType {
    type Error = self::error::ConversionError;
    fn try_from(
        value: ::std::string::String,
    ) -> ::std::result::Result<Self, self::error::ConversionError> {
        value.parse()
    }
}
/// Types for composing complex structures.
pub mod builder {
    #[derive(Clone, Debug)]
    pub struct Payload {
        attribute_id:
            ::std::result::Result<::std::option::Option<super::Ulid>, ::std::string::String>,
        candidate: ::std::result::Result<
            ::std::option::Option<super::PayloadCandidate>,
            ::std::string::String,
        >,
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        profile_id:
            ::std::result::Result<::std::option::Option<super::Ulid>, ::std::string::String>,
    }
    impl ::std::default::Default for Payload {
        fn default() -> Self {
            Self {
                attribute_id: Ok(Default::default()),
                candidate: Ok(Default::default()),
                ext: Ok(Default::default()),
                profile_id: Ok(Default::default()),
            }
        }
    }
    impl Payload {
        pub fn attribute_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ulid>>,
            T::Error: ::std::fmt::Display,
        {
            self.attribute_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for attribute_id: {e}"));
            self
        }
        pub fn candidate<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::PayloadCandidate>>,
            T::Error: ::std::fmt::Display,
        {
            self.candidate = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for candidate: {e}"));
            self
        }
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn profile_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ulid>>,
            T::Error: ::std::fmt::Display,
        {
            self.profile_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for profile_id: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Payload> for super::Payload {
        type Error = super::error::ConversionError;
        fn try_from(value: Payload) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                attribute_id: value.attribute_id?,
                candidate: value.candidate?,
                ext: value.ext?,
                profile_id: value.profile_id?,
            })
        }
    }
    impl ::std::convert::From<super::Payload> for Payload {
        fn from(value: super::Payload) -> Self {
            Self {
                attribute_id: Ok(value.attribute_id),
                candidate: Ok(value.candidate),
                ext: Ok(value.ext),
                profile_id: Ok(value.profile_id),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct PayloadCandidate {
        type_: ::std::result::Result<super::ClaimType, ::std::string::String>,
        value: ::std::result::Result<::serde_json::Value, ::std::string::String>,
        value_type: ::std::result::Result<super::ValueType, ::std::string::String>,
    }
    impl ::std::default::Default for PayloadCandidate {
        fn default() -> Self {
            Self {
                type_: Err("no value supplied for type_".to_string()),
                value: Err("no value supplied for value".to_string()),
                value_type: Err("no value supplied for value_type".to_string()),
            }
        }
    }
    impl PayloadCandidate {
        pub fn type_<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ClaimType>,
            T::Error: ::std::fmt::Display,
        {
            self.type_ = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for type_: {e}"));
            self
        }
        pub fn value<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::serde_json::Value>,
            T::Error: ::std::fmt::Display,
        {
            self.value = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for value: {e}"));
            self
        }
        pub fn value_type<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ValueType>,
            T::Error: ::std::fmt::Display,
        {
            self.value_type = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for value_type: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<PayloadCandidate> for super::PayloadCandidate {
        type Error = super::error::ConversionError;
        fn try_from(
            value: PayloadCandidate,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                type_: value.type_?,
                value: value.value?,
                value_type: value.value_type?,
            })
        }
    }
    impl ::std::convert::From<super::PayloadCandidate> for PayloadCandidate {
        fn from(value: super::PayloadCandidate) -> Self {
            Self {
                type_: Ok(value.type_),
                value: Ok(value.value),
                value_type: Ok(value.value_type),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct Response {
        ext: ::std::result::Result<::std::option::Option<super::Ext>, ::std::string::String>,
        findings: ::std::result::Result<
            ::std::vec::Vec<super::ResponseFindingsItem>,
            ::std::string::String,
        >,
    }
    impl ::std::default::Default for Response {
        fn default() -> Self {
            Self {
                ext: Ok(Default::default()),
                findings: Err("no value supplied for findings".to_string()),
            }
        }
    }
    impl Response {
        pub fn ext<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ext>>,
            T::Error: ::std::fmt::Display,
        {
            self.ext = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for ext: {e}"));
            self
        }
        pub fn findings<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseFindingsItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.findings = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for findings: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<Response> for super::Response {
        type Error = super::error::ConversionError;
        fn try_from(value: Response) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                ext: value.ext?,
                findings: value.findings?,
            })
        }
    }
    impl ::std::convert::From<super::Response> for Response {
        fn from(value: super::Response) -> Self {
            Self {
                ext: Ok(value.ext),
                findings: Ok(value.findings),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct ResponseFindingsItem {
        attribute_id:
            ::std::result::Result<::std::option::Option<super::Ulid>, ::std::string::String>,
        remedies: ::std::result::Result<
            ::std::vec::Vec<super::ResponseFindingsItemRemediesItem>,
            ::std::string::String,
        >,
        severity: ::std::result::Result<super::ResponseFindingsItemSeverity, ::std::string::String>,
        shared_with: ::std::result::Result<
            ::std::vec::Vec<super::ResponseFindingsItemSharedWithItem>,
            ::std::string::String,
        >,
        why: ::std::result::Result<super::ResponseFindingsItemWhy, ::std::string::String>,
    }
    impl ::std::default::Default for ResponseFindingsItem {
        fn default() -> Self {
            Self {
                attribute_id: Ok(Default::default()),
                remedies: Err("no value supplied for remedies".to_string()),
                severity: Err("no value supplied for severity".to_string()),
                shared_with: Ok(Default::default()),
                why: Err("no value supplied for why".to_string()),
            }
        }
    }
    impl ResponseFindingsItem {
        pub fn attribute_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ulid>>,
            T::Error: ::std::fmt::Display,
        {
            self.attribute_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for attribute_id: {e}"));
            self
        }
        pub fn remedies<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseFindingsItemRemediesItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.remedies = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for remedies: {e}"));
            self
        }
        pub fn severity<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseFindingsItemSeverity>,
            T::Error: ::std::fmt::Display,
        {
            self.severity = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for severity: {e}"));
            self
        }
        pub fn shared_with<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<super::ResponseFindingsItemSharedWithItem>>,
            T::Error: ::std::fmt::Display,
        {
            self.shared_with = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for shared_with: {e}"));
            self
        }
        pub fn why<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<super::ResponseFindingsItemWhy>,
            T::Error: ::std::fmt::Display,
        {
            self.why = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for why: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<ResponseFindingsItem> for super::ResponseFindingsItem {
        type Error = super::error::ConversionError;
        fn try_from(
            value: ResponseFindingsItem,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                attribute_id: value.attribute_id?,
                remedies: value.remedies?,
                severity: value.severity?,
                shared_with: value.shared_with?,
                why: value.why?,
            })
        }
    }
    impl ::std::convert::From<super::ResponseFindingsItem> for ResponseFindingsItem {
        fn from(value: super::ResponseFindingsItem) -> Self {
            Self {
                attribute_id: Ok(value.attribute_id),
                remedies: Ok(value.remedies),
                severity: Ok(value.severity),
                shared_with: Ok(value.shared_with),
                why: Ok(value.why),
            }
        }
    }
    #[derive(Clone, Debug)]
    pub struct ResponseFindingsItemSharedWithItem {
        context_id: ::std::result::Result<
            ::std::option::Option<::std::string::String>,
            ::std::string::String,
        >,
        disclosed_to:
            ::std::result::Result<::std::vec::Vec<::std::string::String>, ::std::string::String>,
        persona_did: ::std::result::Result<
            ::std::option::Option<::std::string::String>,
            ::std::string::String,
        >,
        profile_id:
            ::std::result::Result<::std::option::Option<super::Ulid>, ::std::string::String>,
    }
    impl ::std::default::Default for ResponseFindingsItemSharedWithItem {
        fn default() -> Self {
            Self {
                context_id: Ok(Default::default()),
                disclosed_to: Ok(Default::default()),
                persona_did: Ok(Default::default()),
                profile_id: Ok(Default::default()),
            }
        }
    }
    impl ResponseFindingsItemSharedWithItem {
        pub fn context_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<::std::string::String>>,
            T::Error: ::std::fmt::Display,
        {
            self.context_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for context_id: {e}"));
            self
        }
        pub fn disclosed_to<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::vec::Vec<::std::string::String>>,
            T::Error: ::std::fmt::Display,
        {
            self.disclosed_to = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for disclosed_to: {e}"));
            self
        }
        pub fn persona_did<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<::std::string::String>>,
            T::Error: ::std::fmt::Display,
        {
            self.persona_did = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for persona_did: {e}"));
            self
        }
        pub fn profile_id<T>(mut self, value: T) -> Self
        where
            T: ::std::convert::TryInto<::std::option::Option<super::Ulid>>,
            T::Error: ::std::fmt::Display,
        {
            self.profile_id = value
                .try_into()
                .map_err(|e| format!("error converting supplied value for profile_id: {e}"));
            self
        }
    }
    impl ::std::convert::TryFrom<ResponseFindingsItemSharedWithItem>
        for super::ResponseFindingsItemSharedWithItem
    {
        type Error = super::error::ConversionError;
        fn try_from(
            value: ResponseFindingsItemSharedWithItem,
        ) -> ::std::result::Result<Self, super::error::ConversionError> {
            Ok(Self {
                context_id: value.context_id?,
                disclosed_to: value.disclosed_to?,
                persona_did: value.persona_did?,
                profile_id: value.profile_id?,
            })
        }
    }
    impl ::std::convert::From<super::ResponseFindingsItemSharedWithItem>
        for ResponseFindingsItemSharedWithItem
    {
        fn from(value: super::ResponseFindingsItemSharedWithItem) -> Self {
            Self {
                context_id: Ok(value.context_id),
                disclosed_to: Ok(value.disclosed_to),
                persona_did: Ok(value.persona_did),
                profile_id: Ok(value.profile_id),
            }
        }
    }
}
impl crate::Payload for Payload {
    const TYPE_URI: &'static str = "https://trusttasks.org/spec/persona/correlation/analyze/1.0";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"ClaimType\": {\n      \"description\": \"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\n      \"maxLength\": 128,\n      \"minLength\": 1,\n      \"pattern\": \"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\",\n      \"title\": \"ClaimType\",\n      \"type\": \"string\"\n    },\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Success response to persona/correlation/analyze. Type https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.\",\n      \"properties\": {\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"findings\": {\n          \"items\": {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"attributeId\": {\n                \"$ref\": \"#/$defs/Ulid\"\n              },\n              \"remedies\": {\n                \"description\": \"What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told 'this links your personas' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it.\",\n                \"items\": {\n                  \"enum\": [\n                    \"useDifferentValue\",\n                    \"reissueCredentialToThisDid\",\n                    \"correlateDeliberately\",\n                    \"proceedAndRecord\"\n                  ],\n                  \"type\": \"string\"\n                },\n                \"maxItems\": 8,\n                \"type\": \"array\"\n              },\n              \"severity\": {\n                \"enum\": [\n                  \"low\",\n                  \"high\"\n                ],\n                \"type\": \"string\"\n              },\n              \"sharedWith\": {\n                \"description\": \"Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.\",\n                \"items\": {\n                  \"additionalProperties\": false,\n                  \"properties\": {\n                    \"contextId\": {\n                      \"type\": \"string\"\n                    },\n                    \"disclosedTo\": {\n                      \"items\": {\n                        \"type\": \"string\"\n                      },\n                      \"maxItems\": 64,\n                      \"type\": \"array\"\n                    },\n                    \"personaDid\": {\n                      \"type\": \"string\"\n                    },\n                    \"profileId\": {\n                      \"$ref\": \"#/$defs/Ulid\"\n                    }\n                  },\n                  \"type\": \"object\"\n                },\n                \"maxItems\": 128,\n                \"type\": \"array\"\n              },\n              \"why\": {\n                \"description\": \"Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.\",\n                \"maxLength\": 1024,\n                \"type\": \"string\"\n              }\n            },\n            \"required\": [\n              \"severity\",\n              \"why\",\n              \"remedies\"\n            ],\n            \"type\": \"object\"\n          },\n          \"maxItems\": 256,\n          \"type\": \"array\"\n        }\n      },\n      \"required\": [\n        \"findings\"\n      ],\n      \"title\": \"Persona Correlation Analyze — response payload\",\n      \"type\": \"object\"\n    },\n    \"Ulid\": {\n      \"description\": \"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\n      \"pattern\": \"^[0-9A-HJKMNP-TV-Z]{26}$\",\n      \"title\": \"Ulid\",\n      \"type\": \"string\"\n    },\n    \"ValueType\": {\n      \"description\": \"The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer's affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer's release.\",\n      \"enum\": [\n        \"string\",\n        \"number\",\n        \"boolean\",\n        \"date\",\n        \"object\"\n      ],\n      \"title\": \"ValueType\",\n      \"type\": \"string\"\n    }\n  },\n  \"$id\": \"https://trusttasks.org/spec/persona/correlation/analyze/1.0\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\",\n  \"additionalProperties\": false,\n  \"description\": \"Report where the holder's identities link to one another, and what can be done about each link. The findings task behind the counts the write tasks return.\",\n  \"properties\": {\n    \"attributeId\": {\n      \"$ref\": \"#/$defs/Ulid\",\n      \"description\": \"Analyse one attribute. Omit to analyse the whole pool.\"\n    },\n    \"candidate\": {\n      \"additionalProperties\": false,\n      \"description\": \"Analyse a value the holder is CONSIDERING but has not written. This is what lets a builder warn before the mistake rather than after it, which is the difference between a guard and a report. A maintainer MUST NOT store a candidate.\",\n      \"properties\": {\n        \"type\": {\n          \"$ref\": \"#/$defs/ClaimType\"\n        },\n        \"value\": {},\n        \"valueType\": {\n          \"$ref\": \"#/$defs/ValueType\"\n        }\n      },\n      \"required\": [\n        \"type\",\n        \"valueType\",\n        \"value\"\n      ],\n      \"type\": \"object\"\n    },\n    \"ext\": {\n      \"$ref\": \"#/$defs/Ext\"\n    },\n    \"profileId\": {\n      \"$ref\": \"#/$defs/Ulid\",\n      \"description\": \"Analyse one composition — what would this profile link if bound.\"\n    }\n  },\n  \"title\": \"Persona Correlation Analyze — payload\",\n  \"type\": \"object\"\n}\n",
    );
}
impl crate::Payload for Response {
    const TYPE_URI: &'static str =
        "https://trusttasks.org/spec/persona/correlation/analyze/1.0#response";
    const IS_PROOF_REQUIRED: bool = true;
    const IS_RECIPIENT_REQUIRED: bool = true;
    const PAYLOAD_SCHEMA: Option<&'static str> = Some(
        "{\n  \"$defs\": {\n    \"ClaimType\": {\n      \"description\": \"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\n      \"maxLength\": 128,\n      \"minLength\": 1,\n      \"pattern\": \"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\",\n      \"title\": \"ClaimType\",\n      \"type\": \"string\"\n    },\n    \"Ext\": {\n      \"additionalProperties\": true,\n      \"description\": \"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\n      \"minProperties\": 1,\n      \"propertyNames\": {\n        \"pattern\": \"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"\n      },\n      \"title\": \"Ext\",\n      \"type\": \"object\"\n    },\n    \"Response\": {\n      \"$anchor\": \"response\",\n      \"additionalProperties\": false,\n      \"description\": \"Success response to persona/correlation/analyze. Type https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.\",\n      \"properties\": {\n        \"ext\": {\n          \"$ref\": \"#/$defs/Ext\"\n        },\n        \"findings\": {\n          \"items\": {\n            \"additionalProperties\": false,\n            \"properties\": {\n              \"attributeId\": {\n                \"$ref\": \"#/$defs/Ulid\"\n              },\n              \"remedies\": {\n                \"description\": \"What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told 'this links your personas' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it.\",\n                \"items\": {\n                  \"enum\": [\n                    \"useDifferentValue\",\n                    \"reissueCredentialToThisDid\",\n                    \"correlateDeliberately\",\n                    \"proceedAndRecord\"\n                  ],\n                  \"type\": \"string\"\n                },\n                \"maxItems\": 8,\n                \"type\": \"array\"\n              },\n              \"severity\": {\n                \"enum\": [\n                  \"low\",\n                  \"high\"\n                ],\n                \"type\": \"string\"\n              },\n              \"sharedWith\": {\n                \"description\": \"Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.\",\n                \"items\": {\n                  \"additionalProperties\": false,\n                  \"properties\": {\n                    \"contextId\": {\n                      \"type\": \"string\"\n                    },\n                    \"disclosedTo\": {\n                      \"items\": {\n                        \"type\": \"string\"\n                      },\n                      \"maxItems\": 64,\n                      \"type\": \"array\"\n                    },\n                    \"personaDid\": {\n                      \"type\": \"string\"\n                    },\n                    \"profileId\": {\n                      \"$ref\": \"#/$defs/Ulid\"\n                    }\n                  },\n                  \"type\": \"object\"\n                },\n                \"maxItems\": 128,\n                \"type\": \"array\"\n              },\n              \"why\": {\n                \"description\": \"Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss.\",\n                \"maxLength\": 1024,\n                \"type\": \"string\"\n              }\n            },\n            \"required\": [\n              \"severity\",\n              \"why\",\n              \"remedies\"\n            ],\n            \"type\": \"object\"\n          },\n          \"maxItems\": 256,\n          \"type\": \"array\"\n        }\n      },\n      \"required\": [\n        \"findings\"\n      ],\n      \"title\": \"Persona Correlation Analyze — response payload\",\n      \"type\": \"object\"\n    },\n    \"Ulid\": {\n      \"description\": \"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\n      \"pattern\": \"^[0-9A-HJKMNP-TV-Z]{26}$\",\n      \"title\": \"Ulid\",\n      \"type\": \"string\"\n    },\n    \"ValueType\": {\n      \"description\": \"The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer's affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer's release.\",\n      \"enum\": [\n        \"string\",\n        \"number\",\n        \"boolean\",\n        \"date\",\n        \"object\"\n      ],\n      \"title\": \"ValueType\",\n      \"type\": \"string\"\n    }\n  },\n  \"$ref\": \"#/$defs/Response\",\n  \"$schema\": \"https://json-schema.org/draft/2020-12/schema\"\n}\n",
    );
}
impl crate::RequestPayload for Payload {
    type Response = Response;
}
