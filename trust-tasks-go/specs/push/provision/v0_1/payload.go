// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/push/provision/0.1/payload.schema.json

package pushprovisionv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Push Provision — response payload
type Response struct {
	Handle string `json:"handle"`

	// The effective allowlist the gateway recorded.
	Policy WakeTriggerPolicy `json:"policy"`
	Ext    *Ext              `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// WakeTriggerPolicy VTA-owned allowlist of the DIDs permitted to trigger a wake for a
// given WakeHandle (push wake-up binding, https://trusttasks.org/binding/push/0.1). The
// VTA is the source of truth for this policy — all device config state resides at the VTA
// — and provisions it to the gateway, which ENFORCES it: a wake request from a DID not on
// the list is refused. Typically holds the device's mediator DID (queue-driven wake,
// where the mediator alone knows the device is offline) and/or the VTA's own DID
// (policy-driven wake, e.g. a step-up the VTA is delegating to this device).
type WakeTriggerPolicy struct {
	// DIDs authorized to trigger a wake for this handle. An empty array means no party may
	// wake the device (push effectively disabled while the handle exists). The gateway
	// authenticates the trigger's DID before checking membership.
	AllowedTriggers []string `json:"allowedTriggers"`
}

// Payload The controller VTA sets a wake handle's trigger allowlist on the push gateway —
// the DIDs permitted to wake the device. The gateway accepts it only from the handle's
// recorded controller VTA and enforces it on every push/wake. See the push wake-up
// binding (https://trusttasks.org/binding/push/0.1).
type Payload struct {
	// The opaque gateway-issued handle (from push/register) whose allowlist is being set.
	Handle string `json:"handle"`

	// The trigger allowlist the VTA computed by its own policy — the DIDs permitted to wake
	// this handle (typically the device's mediator and/or the VTA itself). Empty disables
	// waking.
	Policy WakeTriggerPolicy `json:"policy"`
	Ext    *Ext              `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/push/provision/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/push/provision/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/push/provision/0.1\",\"title\":\"Push Provision — payload\",\"description\":\"The controller VTA sets a wake handle's trigger allowlist on the push gateway — the DIDs permitted to wake the device. The gateway accepts it only from the handle's recorded controller VTA and enforces it on every push/wake. See the push wake-up binding (https://trusttasks.org/binding/push/0.1).\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"handle\",\"policy\"],\"properties\":{\"handle\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The opaque gateway-issued handle (from push/register) whose allowlist is being set.\"},\"policy\":{\"$ref\":\"#/$defs/WakeTriggerPolicy\",\"description\":\"The trigger allowlist the VTA computed by its own policy — the DIDs permitted to wake this handle (typically the device's mediator and/or the VTA itself). Empty disables waking.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Push Provision — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"handle\",\"policy\"],\"properties\":{\"handle\":{\"type\":\"string\",\"minLength\":1},\"policy\":{\"$ref\":\"#/$defs/WakeTriggerPolicy\",\"description\":\"The effective allowlist the gateway recorded.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"WakeTriggerPolicy\":{\"title\":\"WakeTriggerPolicy\",\"description\":\"VTA-owned allowlist of the DIDs permitted to trigger a wake for a given WakeHandle (push wake-up binding, https://trusttasks.org/binding/push/0.1). The VTA is the source of truth for this policy — all device config state resides at the VTA — and provisions it to the gateway, which ENFORCES it: a wake request from a DID not on the list is refused. Typically holds the device's mediator DID (queue-driven wake, where the mediator alone knows the device is offline) and/or the VTA's own DID (policy-driven wake, e.g. a step-up the VTA is delegating to this device).\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"allowedTriggers\"],\"properties\":{\"allowedTriggers\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"minLength\":1},\"uniqueItems\":true,\"description\":\"DIDs authorized to trigger a wake for this handle. An empty array means no party may wake the device (push effectively disabled while the handle exists). The gateway authenticates the trigger's DID before checking membership.\"}}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Push Provision — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"handle\",\"policy\"],\"properties\":{\"handle\":{\"type\":\"string\",\"minLength\":1},\"policy\":{\"$ref\":\"#/$defs/WakeTriggerPolicy\",\"description\":\"The effective allowlist the gateway recorded.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"WakeTriggerPolicy\":{\"title\":\"WakeTriggerPolicy\",\"description\":\"VTA-owned allowlist of the DIDs permitted to trigger a wake for a given WakeHandle (push wake-up binding, https://trusttasks.org/binding/push/0.1). The VTA is the source of truth for this policy — all device config state resides at the VTA — and provisions it to the gateway, which ENFORCES it: a wake request from a DID not on the list is refused. Typically holds the device's mediator DID (queue-driven wake, where the mediator alone knows the device is offline) and/or the VTA's own DID (policy-driven wake, e.g. a step-up the VTA is delegating to this device).\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"allowedTriggers\"],\"properties\":{\"allowedTriggers\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"minLength\":1},\"uniqueItems\":true,\"description\":\"DIDs authorized to trigger a wake for this handle. An empty array means no party may wake the device (push effectively disabled while the handle exists). The gateway authenticates the trigger's DID before checking membership.\"}}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
