// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/rooms/keys/seal/0.1/payload.schema.json

package roomskeyssealv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to rooms/keys/seal. Type
// https://trusttasks.org/spec/rooms/keys/seal/0.1#response. The sealed record, ready to
// hand to a host with rooms/records/put.
type Response struct {
	Sealed SealedRecord `json:"sealed"`
	Ext    *Ext         `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// SealedRecord A record as a host stores it on an `attributed` or `private` room. Title,
// description, body, author and tags are sealed together in one blob rather than
// separately: splitting them would let a host learn the shape of the material from
// ciphertext lengths, for no benefit, since a reader decrypts the whole record either
// way.
type SealedRecord struct {
	// The sealed record, base64url. Bound by AEAD associated data to `roomId`, `key`,
	// `version` and `epoch`, so a host that relocates it to another key, version, epoch or
	// room produces a mismatch and the open fails. The record cannot be moved undetected even
	// though the host holds every byte of it.
	Ciphertext string `json:"ciphertext"`

	// AEAD nonce, base64url.
	Nonce string `json:"nonce"`

	// The key epoch this record was sealed under. Cleartext because the host must serve the
	// right ciphertext, and bound into the associated data so that relabelling it fails
	// authentication rather than causing a reader to try the wrong key.
	Epoch int64 `json:"epoch"`
}

// Payload Rooms Keys Seal — payload
type Payload struct {
	// The room to seal for. The recipient MUST hold group state for it.
	RoomID string `json:"roomId"`

	// The record key this will be stored under. Bound into the associated data, so a record
	// sealed for one key does not open under another. On a sealed tier keys MUST be opaque —
	// a descriptive key defeats the encryption beside it, and structured naming belongs
	// inside the sealed body.
	Key string `json:"key"`

	// The version the writer INTENDS the record to take, bound into the associated data
	// before the host assigns it. A caller that lets the host assign a different one will
	// find the record does not open, which is the correct failure: accepting whatever came
	// back would mean the binding commits to nothing. The shape that works is a create-only
	// write (`expectedVersion: 0`) or a read of the current version before a rewrite.
	Version int64 `json:"version"`

	// The record body to seal, base64url. Sealed whole rather than field by field: splitting
	// it would let a host learn the shape of the material from ciphertext lengths, for no
	// benefit, since a reader decrypts the whole record either way.
	Plaintext string `json:"plaintext"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/rooms/keys/seal/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/rooms/keys/seal/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/rooms/keys/seal/0.1\",\"title\":\"Rooms Keys Seal — payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"roomId\",\"key\",\"version\",\"plaintext\"],\"properties\":{\"roomId\":{\"type\":\"string\",\"description\":\"The room to seal for. The recipient MUST hold group state for it.\"},\"key\":{\"type\":\"string\",\"maxLength\":512,\"description\":\"The record key this will be stored under. Bound into the associated data, so a record sealed for one key does not open under another. On a sealed tier keys MUST be opaque — a descriptive key defeats the encryption beside it, and structured naming belongs inside the sealed body.\"},\"version\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"The version the writer INTENDS the record to take, bound into the associated data before the host assigns it. A caller that lets the host assign a different one will find the record does not open, which is the correct failure: accepting whatever came back would mean the binding commits to nothing. The shape that works is a create-only write (`expectedVersion: 0`) or a read of the current version before a rewrite.\"},\"plaintext\":{\"type\":\"string\",\"description\":\"The record body to seal, base64url. Sealed whole rather than field by field: splitting it would let a host learn the shape of the material from ciphertext lengths, for no benefit, since a reader decrypts the whole record either way.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Rooms Keys Seal — response payload\",\"description\":\"Success response to rooms/keys/seal. Type https://trusttasks.org/spec/rooms/keys/seal/0.1#response. The sealed record, ready to hand to a host with rooms/records/put.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"sealed\"],\"properties\":{\"sealed\":{\"$ref\":\"#/$defs/SealedRecord\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"SealedRecord\":{\"title\":\"SealedRecord\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"ciphertext\",\"nonce\",\"epoch\"],\"description\":\"A record as a host stores it on an `attributed` or `private` room. Title, description, body, author and tags are sealed together in one blob rather than separately: splitting them would let a host learn the shape of the material from ciphertext lengths, for no benefit, since a reader decrypts the whole record either way.\",\"properties\":{\"ciphertext\":{\"type\":\"string\",\"description\":\"The sealed record, base64url. Bound by AEAD associated data to `roomId`, `key`, `version` and `epoch`, so a host that relocates it to another key, version, epoch or room produces a mismatch and the open fails. The record cannot be moved undetected even though the host holds every byte of it.\"},\"nonce\":{\"type\":\"string\",\"description\":\"AEAD nonce, base64url.\"},\"epoch\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"The key epoch this record was sealed under. Cleartext because the host must serve the right ciphertext, and bound into the associated data so that relabelling it fails authentication rather than causing a reader to try the wrong key.\"}}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Rooms Keys Seal — response payload\",\"description\":\"Success response to rooms/keys/seal. Type https://trusttasks.org/spec/rooms/keys/seal/0.1#response. The sealed record, ready to hand to a host with rooms/records/put.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"sealed\"],\"properties\":{\"sealed\":{\"$ref\":\"#/$defs/SealedRecord\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"SealedRecord\":{\"title\":\"SealedRecord\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"ciphertext\",\"nonce\",\"epoch\"],\"description\":\"A record as a host stores it on an `attributed` or `private` room. Title, description, body, author and tags are sealed together in one blob rather than separately: splitting them would let a host learn the shape of the material from ciphertext lengths, for no benefit, since a reader decrypts the whole record either way.\",\"properties\":{\"ciphertext\":{\"type\":\"string\",\"description\":\"The sealed record, base64url. Bound by AEAD associated data to `roomId`, `key`, `version` and `epoch`, so a host that relocates it to another key, version, epoch or room produces a mismatch and the open fails. The record cannot be moved undetected even though the host holds every byte of it.\"},\"nonce\":{\"type\":\"string\",\"description\":\"AEAD nonce, base64url.\"},\"epoch\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"The key epoch this record was sealed under. Cleartext because the host must serve the right ciphertext, and bound into the associated data so that relabelling it fails authentication rather than causing a reader to try the wrong key.\"}}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
