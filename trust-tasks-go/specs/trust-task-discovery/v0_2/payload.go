// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/trust-task-discovery/0.2/payload.schema.json

package trusttaskdiscoveryv0_2

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response List of bare Type URIs the responding party supports. Carried in a Trust Task
// document whose type is https://trusttasks.org/spec/trust-task-discovery/0.2#response.
type Response struct {
	// Each entry's Type URI MUST match at least one of the request's patterns (if any were
	// supplied). Order is not significant. Duplicates by Type URI are not permitted,
	// regardless of whether the entry is in shorthand or expanded form.
	SupportedTypes []json.RawMessage `json:"supportedTypes"`

	// MAJOR.MINOR.PATCH version of the Trust Tasks framework specification (SPEC.md) the
	// responder targets — three-part, as the framework has been versioned since 0.4.0
	// (SPEC.md §5.1.1), and exactly the value a Trust Task specification declares as its
	// target framework version. Lets a discoverer reason about compatibility (SPEC.md §5.2)
	// and select the envelope schema a responder's documents are validated against, which is
	// keyed by the full release: a PATCH is never substituted for another. RECOMMENDED.
	// Two-part in trust-task-discovery/0.1, which could not express a PATCH.
	FrameworkVersion *string `json:"frameworkVersion,omitempty"`
}

// Payload Request payload for the framework-defined trust-task-discovery exchange.
// Carries zero or more slug-glob patterns the discoverer wants the responder to filter
// against. The response payload schema is reachable via $anchor: 'response'.
type Payload struct {
	// Patterns are ORed: a slug matches the query if at least one pattern matches it. If the
	// array is omitted or empty, the responder MUST treat the query as ['*'] — return every
	// supported task. Bounded at 16 entries as parser hardening (SPEC.md §12.2), not as a
	// capability limit: matching an unbounded pattern list against every published slug is
	// work a responder does on an unauthenticated request, and a discoverer wanting more asks
	// for '*' and filters locally.
	Patterns *[]string `json:"patterns,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/trust-task-discovery/0.2"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/trust-task-discovery/0.2#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/trust-task-discovery/0.2\",\"title\":\"Trust Task Discovery — payload\",\"description\":\"Request payload for the framework-defined trust-task-discovery exchange. Carries zero or more slug-glob patterns the discoverer wants the responder to filter against. The response payload schema is reachable via $anchor: 'response'.\",\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"patterns\":{\"type\":\"array\",\"maxItems\":16,\"items\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Slug-glob pattern. Matches: '*' (every slug); '<prefix>/*' (any slug starting with '<prefix>/' — e.g. 'acl/*' matches 'acl/grant', 'acl/revoke', 'acl/grant/sub'); otherwise an exact slug match. Wildcards anywhere other than as a trailing segment are not interpreted — the only sigils are the trailing '/*' and the bare '*'.\"},\"description\":\"Patterns are ORed: a slug matches the query if at least one pattern matches it. If the array is omitted or empty, the responder MUST treat the query as ['*'] — return every supported task. Bounded at 16 entries as parser hardening (SPEC.md §12.2), not as a capability limit: matching an unbounded pattern list against every published slug is work a responder does on an unauthenticated request, and a discoverer wanting more asks for '*' and filters locally.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Trust Task Discovery — response payload\",\"description\":\"List of bare Type URIs the responding party supports. Carried in a Trust Task document whose type is https://trusttasks.org/spec/trust-task-discovery/0.2#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"supportedTypes\"],\"properties\":{\"supportedTypes\":{\"type\":\"array\",\"items\":{\"oneOf\":[{\"type\":\"string\",\"format\":\"uri\",\"description\":\"Shorthand form: a bare Type URI string. Equivalent to an object with only the `type` member set.\"},{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\"],\"properties\":{\"type\":{\"type\":\"string\",\"format\":\"uri\",\"description\":\"A bare Type URI (no #request or #response fragment). The responder supports both request and response variants of every entry it lists.\"},\"requiredExt\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"},\"minItems\":1,\"uniqueItems\":true,\"description\":\"Reverse-DNS `ext` namespaces this responder requires on inbound documents of this Type URI as a matter of local policy (SPEC.md §4.5.1, §7.2). A producer that does not populate every listed namespace will receive a `malformedRequest` rejection. Optional; reserved-but-recognized in 0.1, RECOMMENDED in future revisions for responders that enforce such policies.\"}},\"description\":\"Object form: lists a Type URI together with optional capability annotations.\"}],\"description\":\"An entry in the supportedTypes list. SHORTHAND: a bare Type URI string. EXPANDED: an object with `type` and optional capability annotations (`requiredExt`).\"},\"description\":\"Each entry's Type URI MUST match at least one of the request's patterns (if any were supplied). Order is not significant. Duplicates by Type URI are not permitted, regardless of whether the entry is in shorthand or expanded form.\"},\"frameworkVersion\":{\"type\":\"string\",\"pattern\":\"^(0|[1-9][0-9]*)\\\\.(0|[1-9][0-9]*)\\\\.(0|[1-9][0-9]*)$\",\"description\":\"MAJOR.MINOR.PATCH version of the Trust Tasks framework specification (SPEC.md) the responder targets — three-part, as the framework has been versioned since 0.4.0 (SPEC.md §5.1.1), and exactly the value a Trust Task specification declares as its target framework version. Lets a discoverer reason about compatibility (SPEC.md §5.2) and select the envelope schema a responder's documents are validated against, which is keyed by the full release: a PATCH is never substituted for another. RECOMMENDED. Two-part in trust-task-discovery/0.1, which could not express a PATCH.\"}}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Trust Task Discovery — response payload\",\"description\":\"List of bare Type URIs the responding party supports. Carried in a Trust Task document whose type is https://trusttasks.org/spec/trust-task-discovery/0.2#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"supportedTypes\"],\"properties\":{\"supportedTypes\":{\"type\":\"array\",\"items\":{\"oneOf\":[{\"type\":\"string\",\"format\":\"uri\",\"description\":\"Shorthand form: a bare Type URI string. Equivalent to an object with only the `type` member set.\"},{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\"],\"properties\":{\"type\":{\"type\":\"string\",\"format\":\"uri\",\"description\":\"A bare Type URI (no #request or #response fragment). The responder supports both request and response variants of every entry it lists.\"},\"requiredExt\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"},\"minItems\":1,\"uniqueItems\":true,\"description\":\"Reverse-DNS `ext` namespaces this responder requires on inbound documents of this Type URI as a matter of local policy (SPEC.md §4.5.1, §7.2). A producer that does not populate every listed namespace will receive a `malformedRequest` rejection. Optional; reserved-but-recognized in 0.1, RECOMMENDED in future revisions for responders that enforce such policies.\"}},\"description\":\"Object form: lists a Type URI together with optional capability annotations.\"}],\"description\":\"An entry in the supportedTypes list. SHORTHAND: a bare Type URI string. EXPANDED: an object with `type` and optional capability annotations (`requiredExt`).\"},\"description\":\"Each entry's Type URI MUST match at least one of the request's patterns (if any were supplied). Order is not significant. Duplicates by Type URI are not permitted, regardless of whether the entry is in shorthand or expanded form.\"},\"frameworkVersion\":{\"type\":\"string\",\"pattern\":\"^(0|[1-9][0-9]*)\\\\.(0|[1-9][0-9]*)\\\\.(0|[1-9][0-9]*)$\",\"description\":\"MAJOR.MINOR.PATCH version of the Trust Tasks framework specification (SPEC.md) the responder targets — three-part, as the framework has been versioned since 0.4.0 (SPEC.md §5.1.1), and exactly the value a Trust Task specification declares as its target framework version. Lets a discoverer reason about compatibility (SPEC.md §5.2) and select the envelope schema a responder's documents are validated against, which is keyed by the full release: a PATCH is never substituted for another. RECOMMENDED. Two-part in trust-task-discovery/0.1, which could not express a PATCH.\"}}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
