// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/audit/_shared/0.1/audit.schema.json

package auditsharedauditv0_1

import "encoding/json"

// AuditEnvelope One entry in an append-only audit log. Only eventId/recordedAt/action are
// universal; every other field is populated by maintainers that track it.
// `prevHash`/`entryHash` are present on hash-chained logs (see audit/verify) and absent
// otherwise. Principal DIDs are plaintext and MAY be absent when a right-to-be-forgotten
// redaction has nulled them after the fact.
type AuditEnvelope struct {
	// Stable identifier for this event. Also the tie-breaker component of a cursor's position
	// key.
	EventID string `json:"eventId"`

	// Wall-clock time the entry was written. The primary ordering key.
	RecordedAt string `json:"recordedAt"`

	// The operation this entry records — a maintainer-defined action name (e.g.
	// `member.removed`, `policy.activated`). The discriminator for `detail`.
	Action string `json:"action"`

	// How the operation resolved. Typically `success` or `denied`; a maintainer MAY use
	// others. Absent for events that have no pass/fail sense.
	Outcome *string `json:"outcome,omitempty"`

	// DID of the principal that performed the action. `null` when a right-to-be-forgotten
	// redaction has removed the plaintext; a maintainer that keeps a keyed hash of the actor
	// for correlation carries it in `ext`, not here.
	Actor *string `json:"actor,omitempty"`

	// DID of the principal the action acted upon, when the event has one. `null`/absent for
	// events whose target is the maintainer itself. Same redaction semantics as `actor`.
	Target *string `json:"target,omitempty"`

	// Trust context the event occurred in, for a maintainer that partitions its log per
	// context.
	ContextID *string `json:"contextId,omitempty"`

	// Envelope-shape version at the maintainer, for consumers that need to reason about
	// wire-shape evolution.
	SchemaVersion *int64 `json:"schemaVersion,omitempty"`

	// Hash-chain link: the `entryHash` of the immediately-preceding entry. Present only on
	// chained logs; its integrity is what audit/verify checks. Multibase-encoded multihash
	// over the RFC 8785 (JCS) canonicalization of the predecessor's immutable content — the
	// same derivation as `entryHash`, so a verifier recomputes both the same way.
	PrevHash *DigestMultibase `json:"prevHash,omitempty"`

	// Hash-chain commitment over this entry's immutable content. The next entry's `prevHash`
	// points here. Multibase-encoded multihash over the RFC 8785 (JCS) canonicalization of
	// that content; the canonicalization is what makes the commitment reproducible by a
	// verifier that did not write the entry.
	EntryHash *DigestMultibase `json:"entryHash,omitempty"`

	// Event-specific payload, keyed by `action`. Opaque to the framework; a consumer that
	// does not recognise the action treats it as an unstructured record.
	Detail *map[string]json.RawMessage `json:"detail,omitempty"`
	Ext    *Ext                        `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// DigestMultibase A cryptographic digest as a multibase-encoded multihash — the encoding
// the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the
// one `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
// algorithm in-band, so the value is self-describing and the wire format survives an
// algorithm change without a schema revision; multibase does the same for the base
// encoding, so a verifier never infers base58 from base64url by context. A bare hex
// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and
// is non-conforming here. This definition constrains the *encoding only*. What the digest
// is computed over is stated by each referencing field, because it differs legitimately:
// a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while
// a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON
// document and which does not name a canonicalization is not reproducible. Restricted to
// the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires —
// `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that
// "interoperability is not guaranteed between implementations using such values", and a
// registry whose purpose is interoperability should not mint digests a conforming
// verifier may be unable to read. The alphabets are enforced rather than assumed:
// base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published
// examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for
// consistency with `did:key` and `did:webvh`.
type DigestMultibase = string
