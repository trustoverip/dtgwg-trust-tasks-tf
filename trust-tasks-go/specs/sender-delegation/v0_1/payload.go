// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/sender-delegation/0.1/payload.schema.json

package senderdelegationv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload The document's signer (this document's `issuer`) names the one transport sender
// permitted to carry the listed Trust Task documents to this document's `recipient`. It
// confers no authority on the sender: the consumer still authorizes each carried document
// as its own issuer. Short-lived by construction — the document's `expiresAt` is REQUIRED
// and at most 300 seconds after its `issuedAt`.
type Payload struct {
	// The bare DID (no fragment) of the transport sender permitted to carry the listed
	// documents. A consumer compares it by exact string equality against the
	// transport-authenticated sender of the message that carried them.
	Delegate string `json:"delegate"`

	// The `id` of each Trust Task document this delegation covers. A carried document whose
	// `id` is not listed is not covered, however recently the delegation was issued.
	Documents []string `json:"documents"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/sender-delegation/0.1"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/sender-delegation/0.1\",\"title\":\"Sender Delegation — payload\",\"description\":\"The document's signer (this document's `issuer`) names the one transport sender permitted to carry the listed Trust Task documents to this document's `recipient`. It confers no authority on the sender: the consumer still authorizes each carried document as its own issuer. Short-lived by construction — the document's `expiresAt` is REQUIRED and at most 300 seconds after its `issuedAt`.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"delegate\",\"documents\"],\"properties\":{\"delegate\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The bare DID (no fragment) of the transport sender permitted to carry the listed documents. A consumer compares it by exact string equality against the transport-authenticated sender of the message that carried them.\"},\"documents\":{\"type\":\"array\",\"minItems\":1,\"maxItems\":16,\"uniqueItems\":true,\"items\":{\"type\":\"string\",\"minLength\":1},\"description\":\"The `id` of each Trust Task document this delegation covers. A carried document whose `id` is not listed is not covered, however recently the delegation was issued.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}
