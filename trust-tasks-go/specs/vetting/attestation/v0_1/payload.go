// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vetting/attestation/0.1/payload.schema.json

package vettingattestationv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// StatementMetaMethod How the vetter met the applicant.
type StatementMetaMethod string

// Values StatementMetaMethod may take, per this specification's schema.
const (
	StatementMetaMethodInPerson          StatementMetaMethod = "inPerson"
	StatementMetaMethodVideo             StatementMetaMethod = "video"
	StatementMetaMethodPriorAcquaintance StatementMetaMethod = "priorAcquaintance"
)

// StatementMetaDeclaredRelationship The relationship the vetter declared, which the
// community's independence rules count.
type StatementMetaDeclaredRelationship string

// Values StatementMetaDeclaredRelationship may take, per this specification's schema.
const (
	StatementMetaDeclaredRelationshipNone         StatementMetaDeclaredRelationship = "none"
	StatementMetaDeclaredRelationshipFamily       StatementMetaDeclaredRelationship = "family"
	StatementMetaDeclaredRelationshipSameEmployer StatementMetaDeclaredRelationship = "sameEmployer"
	StatementMetaDeclaredRelationshipCloseFriend  StatementMetaDeclaredRelationship = "closeFriend"
	StatementMetaDeclaredRelationshipOther        StatementMetaDeclaredRelationship = "other"
)

// StatementMeta What a Vetting Statement would have said, minus its issuer.
type StatementMeta struct {
	// The community this attestation is for. An attestation is not portable between
	// communities.
	Community string `json:"community"`

	// The digest of the criterion's requirements as the vetter read them.
	RequirementsDigest string `json:"requirementsDigest"`

	// How the vetter met the applicant.
	Method StatementMetaMethod `json:"method"`

	// The claim types the vetter checked.
	ClaimsVerified []string `json:"claimsVerified"`

	// Whether the vetter confirmed a live person.
	LivenessConfirmed bool `json:"livenessConfirmed"`

	// The relationship the vetter declared, which the community's independence rules count.
	DeclaredRelationship StatementMetaDeclaredRelationship `json:"declaredRelationship"`

	// The commitment on the Vetting Card the applicant showed. Consistency across
	// attestations is what stops two applicants pooling them.
	IdentityCommitment string `json:"identityCommitment"`

	// Digest of that card, as the vetter verified it.
	CardDigestMultibase string `json:"cardDigestMultibase"`

	// A DATE, never a timestamp: an exact time would let a community line an attestation up
	// with a vetter's activity, which undoes the proof without touching it.
	ValidFrom string `json:"validFrom"`

	// A date, for the same reason.
	ValidUntil string `json:"validUntil"`

	// The token label spent, repeated inside the bound facts so it cannot be swapped in
	// transit.
	TokenLabel string `json:"tokenLabel"`

	// The token serial spent, likewise bound.
	TokenSerial string `json:"tokenSerial"`
}

// Response The applicant's receipt. It verifies the attestation on arrival rather than at
// submit, so a vetter learns immediately that what it sent is usable.
type Response struct {
	// Whether the attestation verified against the community's published parameters and the
	// card the applicant showed.
	Accepted bool `json:"accepted"`

	// OPTIONAL. How many attestations the applicant now holds for this criterion — a courtesy
	// so a vetter can tell someone they are done.
	HeldCount *int64 `json:"heldCount,omitempty"`
	Ext       *Ext   `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// PayloadToken The attestation token spent for this attestation. The community records
// the serial, which is how a vetter's rate is capped without anybody learning who they
// are.
type PayloadToken struct {
	// The token label spent under.
	Label string `json:"label"`

	// The serial revealed by the spend. Recorded by the community; a second appearance is a
	// double spend.
	Serial string `json:"serial"`

	// The re-randomised token signature, verifiable under the published token key.
	Shown string `json:"shown"`
}

// Payload A vetter gives an applicant an attestation that names nobody. It carries the
// same facts a Vetting Statement carries and no issuer: what proves it was made by an
// eligible vetter is the proof the applicant later builds over it, not a signature on
// this document.
type Payload struct {
	// The hidden-vetting suite this attestation was made under, exactly as the community's
	// criterion publishes it. An applicant holding attestations under two suites cannot mix
	// them in one proof.
	Suite string `json:"suite"`

	// The attestation itself, multibase base58btc: a tag, a shown class credential and a
	// proof. It carries no identifier of the vetter — the tag is deterministic for this
	// (vetter, applicant) pair and unlinkable across applicants.
	Attestation string `json:"attestation"`

	// The facts the community will count. Bound into the attestation, so changing one here
	// invalidates it.
	Meta StatementMeta `json:"meta"`

	// The attestation token spent for this attestation. The community records the serial,
	// which is how a vetter's rate is capped without anybody learning who they are.
	Token PayloadToken `json:"token"`
	Ext   *Ext         `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vetting/attestation/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vetting/attestation/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vetting/attestation/0.1\",\"title\":\"Vetting — Attestation — payload\",\"description\":\"A vetter gives an applicant an attestation that names nobody. It carries the same facts a Vetting Statement carries and no issuer: what proves it was made by an eligible vetter is the proof the applicant later builds over it, not a signature on this document.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"suite\",\"attestation\",\"meta\",\"token\"],\"properties\":{\"suite\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The hidden-vetting suite this attestation was made under, exactly as the community's criterion publishes it. An applicant holding attestations under two suites cannot mix them in one proof.\"},\"attestation\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The attestation itself, multibase base58btc: a tag, a shown class credential and a proof. It carries no identifier of the vetter — the tag is deterministic for this (vetter, applicant) pair and unlinkable across applicants.\"},\"meta\":{\"$ref\":\"#/$defs/StatementMeta\",\"description\":\"The facts the community will count. Bound into the attestation, so changing one here invalidates it.\"},\"token\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"label\",\"serial\",\"shown\"],\"description\":\"The attestation token spent for this attestation. The community records the serial, which is how a vetter's rate is capped without anybody learning who they are.\",\"properties\":{\"label\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"The token label spent under.\"},\"serial\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The serial revealed by the spend. Recorded by the community; a second appearance is a double spend.\"},\"shown\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The re-randomised token signature, verifiable under the published token key.\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"StatementMeta\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"community\",\"requirementsDigest\",\"method\",\"claimsVerified\",\"livenessConfirmed\",\"declaredRelationship\",\"identityCommitment\",\"cardDigestMultibase\",\"validFrom\",\"validUntil\",\"tokenLabel\",\"tokenSerial\"],\"description\":\"What a Vetting Statement would have said, minus its issuer.\",\"properties\":{\"community\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The community this attestation is for. An attestation is not portable between communities.\"},\"requirementsDigest\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The digest of the criterion's requirements as the vetter read them.\"},\"method\":{\"type\":\"string\",\"enum\":[\"inPerson\",\"video\",\"priorAcquaintance\"],\"description\":\"How the vetter met the applicant.\"},\"claimsVerified\":{\"type\":\"array\",\"minItems\":1,\"items\":{\"type\":\"string\",\"minLength\":1},\"description\":\"The claim types the vetter checked.\"},\"livenessConfirmed\":{\"type\":\"boolean\",\"description\":\"Whether the vetter confirmed a live person.\"},\"declaredRelationship\":{\"type\":\"string\",\"enum\":[\"none\",\"family\",\"sameEmployer\",\"closeFriend\",\"other\"],\"description\":\"The relationship the vetter declared, which the community's independence rules count.\"},\"identityCommitment\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The commitment on the Vetting Card the applicant showed. Consistency across attestations is what stops two applicants pooling them.\"},\"cardDigestMultibase\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"Digest of that card, as the vetter verified it.\"},\"validFrom\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"A DATE, never a timestamp: an exact time would let a community line an attestation up with a vetter's activity, which undoes the proof without touching it.\"},\"validUntil\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"A date, for the same reason.\"},\"tokenLabel\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"The token label spent, repeated inside the bound facts so it cannot be swapped in transit.\"},\"tokenSerial\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The token serial spent, likewise bound.\"}}},\"Response\":{\"$anchor\":\"response\",\"title\":\"Vetting Attestation — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"accepted\"],\"description\":\"The applicant's receipt. It verifies the attestation on arrival rather than at submit, so a vetter learns immediately that what it sent is usable.\",\"properties\":{\"accepted\":{\"type\":\"boolean\",\"description\":\"Whether the attestation verified against the community's published parameters and the card the applicant showed.\"},\"heldCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"OPTIONAL. How many attestations the applicant now holds for this criterion — a courtesy so a vetter can tell someone they are done.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"StatementMeta\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"community\",\"requirementsDigest\",\"method\",\"claimsVerified\",\"livenessConfirmed\",\"declaredRelationship\",\"identityCommitment\",\"cardDigestMultibase\",\"validFrom\",\"validUntil\",\"tokenLabel\",\"tokenSerial\"],\"description\":\"What a Vetting Statement would have said, minus its issuer.\",\"properties\":{\"community\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The community this attestation is for. An attestation is not portable between communities.\"},\"requirementsDigest\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The digest of the criterion's requirements as the vetter read them.\"},\"method\":{\"type\":\"string\",\"enum\":[\"inPerson\",\"video\",\"priorAcquaintance\"],\"description\":\"How the vetter met the applicant.\"},\"claimsVerified\":{\"type\":\"array\",\"minItems\":1,\"items\":{\"type\":\"string\",\"minLength\":1},\"description\":\"The claim types the vetter checked.\"},\"livenessConfirmed\":{\"type\":\"boolean\",\"description\":\"Whether the vetter confirmed a live person.\"},\"declaredRelationship\":{\"type\":\"string\",\"enum\":[\"none\",\"family\",\"sameEmployer\",\"closeFriend\",\"other\"],\"description\":\"The relationship the vetter declared, which the community's independence rules count.\"},\"identityCommitment\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The commitment on the Vetting Card the applicant showed. Consistency across attestations is what stops two applicants pooling them.\"},\"cardDigestMultibase\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"Digest of that card, as the vetter verified it.\"},\"validFrom\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"A DATE, never a timestamp: an exact time would let a community line an attestation up with a vetter's activity, which undoes the proof without touching it.\"},\"validUntil\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"A date, for the same reason.\"},\"tokenLabel\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"The token label spent, repeated inside the bound facts so it cannot be swapped in transit.\"},\"tokenSerial\":{\"type\":\"string\",\"pattern\":\"^z[1-9A-HJ-NP-Za-km-z]+$\",\"description\":\"The token serial spent, likewise bound.\"}}},\"Response\":{\"$anchor\":\"response\",\"title\":\"Vetting Attestation — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"accepted\"],\"description\":\"The applicant's receipt. It verifies the attestation on arrival rather than at submit, so a vetter learns immediately that what it sent is usable.\",\"properties\":{\"accepted\":{\"type\":\"boolean\",\"description\":\"Whether the attestation verified against the community's published parameters and the card the applicant showed.\"},\"heldCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"OPTIONAL. How many attestations the applicant now holds for this criterion — a courtesy so a vetter can tell someone they are done.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
