// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vetting/_shared/0.1/vetting-card.schema.json

package vettingsharedvettingcardv0_1

import "encoding/json"

// VettingCardTypeItem VettingCardTypeItem is a closed set of string values.
type VettingCardTypeItem string

// Values VettingCardTypeItem may take, per this specification's schema.
const (
	VettingCardTypeItemVerifiableDataStructure VettingCardTypeItem = "VerifiableDataStructure"
	VettingCardTypeItemRelationshipCard        VettingCardTypeItem = "RelationshipCard"
	VettingCardTypeItemVettingCard             VettingCardTypeItem = "VettingCard"
)

// VettingCard VettingCard
type VettingCard struct {
	// Exactly `VerifiableDataStructure`, `RelationshipCard` and `VettingCard`, in any order.
	Type []VettingCardTypeItem `json:"type"`

	// Fresh per card. A card is never re-sent to a second session under the same id.
	ID string `json:"id"`

	// The applicant's DID — the issuer of the vetting request and the subject every resulting
	// statement names. The card is signed with this DID's assertion key.
	Publisher   string `json:"publisher"`
	CardVersion int64  `json:"cardVersion"`

	// The vetter's DID — the issuer of the vetting session. A card addressed to anyone else
	// is refused.
	Audience string `json:"audience"`

	// The community the applicant is being vetted for.
	Community string `json:"community"`

	// The session's challenge, copied verbatim.
	Challenge string `json:"challenge"`

	// The session's domain, copied verbatim.
	Domain   string `json:"domain"`
	IssuedAt string `json:"issuedAt"`

	// No later than the session's `expiresAt`.
	ExpiresAt string             `json:"expiresAt"`
	Claims    []VettingCardClaim `json:"claims"`

	// digestMultibase over the RFC 8785 canonicalization of `{ "salt": commitmentSalt,
	// "claims": R }`, where R is `{ "type", "value" }` for every card claim whose type the
	// session lists in `requiredClaims`, ordered by `type` then `value` (code-point order).
	// SHA-256 RECOMMENDED. Because the applicant uses one salt per application, every vetter
	// of that application sees the same value.
	IdentityCommitment DigestMultibase `json:"identityCommitment"`

	// 32 random bytes, base64url without padding, generated once per application. Goes to
	// vetters inside the card and to nobody else: a party holding the commitment without the
	// salt cannot test guesses at the claimed name.
	CommitmentSalt string             `json:"commitmentSalt"`
	Proof          DataIntegrityProof `json:"proof"`
}

// VettingCardClaim VettingCardClaim
type VettingCardClaim struct {
	// The claim type. `person.portrait` is refused: identity vetting works by a vetter
	// looking at the person, not by transmitting their image.
	Type ClaimType `json:"type"`

	// The claimed value, exactly as rendered from the applicant's persona — a string for most
	// claim types, structured JSON for some (e.g. a postal address). Authored by the
	// applicant and asserted under their signature; the vetter's check is what gives it any
	// assurance.
	Value json.RawMessage `json:"value"`

	// Where the value's assurance comes from. `selfAsserted` — the applicant says so, and the
	// vetter's human check is the only assurance added — is the only value this version
	// defines; a verifier MUST NOT treat any other value as adding assurance it does not
	// understand.
	Provenance string `json:"provenance"`
}

// DataIntegrityProof A W3C Data Integrity proof by the card's publisher. Additional Data
// Integrity members (e.g. `created`) are permitted and are covered as the cryptosuite
// defines.
type DataIntegrityProof struct {
	Type string `json:"type"`

	// e.g. `eddsa-jcs-2022`.
	Cryptosuite string `json:"cryptosuite"`

	// A verification method of `publisher`, authorized for `assertionMethod`.
	VerificationMethod string `json:"verificationMethod"`
	ProofPurpose       string `json:"proofPurpose"`
	ProofValue         string `json:"proofValue"`
}

// ClaimType The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`,
// `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a
// consumer with no knowledge of the specific token can still group by its prefix. The
// token is the maintainer's own; no external vocabulary is primary. External vocabularies
// (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by
// a renderer, not at rest, so that a query written in any of them can be matched without
// the store having to live inside any one of them. The `x:` prefix is an open extension
// namespace and is not decoration. The closest prior art — Windows CardSpace's
// self-issued card — supported exactly fifteen predefined claim types with no
// extensibility, and that is the specific way it failed the requirement a holder actually
// has. An `x:` attribute stores, composes, binds and discloses exactly like a known one;
// it renders generically and matches only an explicit query.
type ClaimType = string

// DigestMultibase A cryptographic digest as a multibase-encoded multihash — the encoding
// the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the
// one `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
// algorithm in-band, so the value is self-describing and the wire format survives an
// algorithm change without a schema revision; multibase does the same for the base
// encoding, so a verifier never infers base58 from base64url by context. A bare hex
// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and
// is non-conforming here. This definition constrains the *encoding only*. What the digest
// is computed over is stated by each referencing field, because it differs legitimately:
// a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while
// a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON
// document and which does not name a canonicalization is not reproducible. Restricted to
// the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires —
// `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that
// "interoperability is not guaranteed between implementations using such values", and a
// registry whose purpose is interoperability should not mint digests a conforming
// verifier may be unable to read. The alphabets are enforced rather than assumed:
// base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published
// examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for
// consistency with `did:key` and `did:webvh`.
type DigestMultibase = string
