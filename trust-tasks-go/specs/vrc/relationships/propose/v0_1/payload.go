// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vrc/relationships/propose/0.1/payload.schema.json

package vrcrelationshipsproposev0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The counterparty's acceptance, and its answer on witnessing. A decline is a
// trust-task-error, never a response with accept: false.
type Response struct {
	// Always true: a decline travels as a trust-task-error so that refusals share one path
	// and one set of routing rules (SPEC.md §8.1). Present so the acceptance is explicit
	// rather than inferred from the response's existence.
	Accept json.RawMessage `json:"accept"`

	// The pairwise DID the counterparty will use for this relationship, under the same
	// non-linkability obligation as the request's.
	RelationshipDID string `json:"relationshipDid"`

	// The counterparty's answer to the request's `witnessed`: true where it agrees the
	// exchange will be witnessed. Absent or false accepts an UNWITNESSED exchange, whatever
	// the request asked for — a counterparty unwilling to proceed unwitnessed declines
	// instead, since this specification defines no renegotiation.
	Witnessed *bool `json:"witnessed,omitempty"`
	Ext       *Ext  `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload One person proposes a peer-to-peer relationship to another, naming the pairwise
// DID they will use for it. The counterparty accepts or declines; a decline is a
// trust-task-error carrying `vrc/relationships/propose:declined`.
type Payload struct {
	// The pairwise DID the proposing party will use for this relationship. Minted for this
	// relationship — a producer MUST NOT carry a long-term identifier here, which would make
	// every later presentation of the resulting credentials linkable.
	RelationshipDID string `json:"relationshipDid"`

	// Whether the proposing party asks that the exchange be witnessed. A request, not a fact:
	// it is answered by the same member on the response, and which witness and on what terms
	// is settled under the witness/* specifications, as a separate exchange nested via
	// parentThreadId. Absent means unwitnessed.
	Witnessed *bool `json:"witnessed,omitempty"`

	// Why the proposing party is asking, in their own words. A hint reaching a human; the
	// counterparty is under no obligation to honour it and MUST NOT treat its absence as a
	// defect.
	Reason *string `json:"reason,omitempty"`
	Ext    *Ext    `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vrc/relationships/propose/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vrc/relationships/propose/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vrc/relationships/propose/0.1\",\"title\":\"VRC Relationships Propose — payload\",\"description\":\"One person proposes a peer-to-peer relationship to another, naming the pairwise DID they will use for it. The counterparty accepts or declines; a decline is a trust-task-error carrying `vrc/relationships/propose:declined`.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"relationshipDid\"],\"properties\":{\"relationshipDid\":{\"type\":\"string\",\"pattern\":\"^did:\",\"description\":\"The pairwise DID the proposing party will use for this relationship. Minted for this relationship — a producer MUST NOT carry a long-term identifier here, which would make every later presentation of the resulting credentials linkable.\"},\"witnessed\":{\"type\":\"boolean\",\"description\":\"Whether the proposing party asks that the exchange be witnessed. A request, not a fact: it is answered by the same member on the response, and which witness and on what terms is settled under the witness/* specifications, as a separate exchange nested via parentThreadId. Absent means unwitnessed.\"},\"reason\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":500,\"description\":\"Why the proposing party is asking, in their own words. A hint reaching a human; the counterparty is under no obligation to honour it and MUST NOT treat its absence as a defect.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VRC Relationships Propose — response payload\",\"description\":\"The counterparty's acceptance, and its answer on witnessing. A decline is a trust-task-error, never a response with accept: false.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"accept\",\"relationshipDid\"],\"properties\":{\"accept\":{\"const\":true,\"description\":\"Always true: a decline travels as a trust-task-error so that refusals share one path and one set of routing rules (SPEC.md §8.1). Present so the acceptance is explicit rather than inferred from the response's existence.\"},\"relationshipDid\":{\"type\":\"string\",\"pattern\":\"^did:\",\"description\":\"The pairwise DID the counterparty will use for this relationship, under the same non-linkability obligation as the request's.\"},\"witnessed\":{\"type\":\"boolean\",\"description\":\"The counterparty's answer to the request's `witnessed`: true where it agrees the exchange will be witnessed. Absent or false accepts an UNWITNESSED exchange, whatever the request asked for — a counterparty unwilling to proceed unwitnessed declines instead, since this specification defines no renegotiation.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VRC Relationships Propose — response payload\",\"description\":\"The counterparty's acceptance, and its answer on witnessing. A decline is a trust-task-error, never a response with accept: false.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"accept\",\"relationshipDid\"],\"properties\":{\"accept\":{\"const\":true,\"description\":\"Always true: a decline travels as a trust-task-error so that refusals share one path and one set of routing rules (SPEC.md §8.1). Present so the acceptance is explicit rather than inferred from the response's existence.\"},\"relationshipDid\":{\"type\":\"string\",\"pattern\":\"^did:\",\"description\":\"The pairwise DID the counterparty will use for this relationship, under the same non-linkability obligation as the request's.\"},\"witnessed\":{\"type\":\"boolean\",\"description\":\"The counterparty's answer to the request's `witnessed`: true where it agrees the exchange will be witnessed. Absent or false accepts an UNWITNESSED exchange, whatever the request asked for — a counterparty unwilling to proceed unwitnessed declines instead, since this specification defines no renegotiation.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
