// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/credential-exchange/present/0.1/payload.schema.json

package credentialexchangepresentv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload Holder to verifier: an OID4VP vp_token carrying the selectively-disclosed,
// holder-bound presentation. Replies on the query thread.
type Payload struct {
	// The OID4VP `vp_token`, format-agnostic by design. A JSON **string** is an SD-JWT-VC
	// presentation: the consented disclosures plus a mandatory key-binding JWT over the
	// verifier's nonce and audience. A JSON **object** is a W3C Data-Integrity VP whose proof
	// carries the same nonce and domain. A consumer selects the verification path from the
	// value's type rather than a separate format discriminator, so a new credential format
	// needs no new member here. snake_case is OID4VP's own name for this field.
	VPToken json.RawMessage `json:"vp_token"`
	Ext     *Ext            `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/credential-exchange/present/0.1"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/credential-exchange/present/0.1\",\"title\":\"Credential Exchange Present — payload\",\"description\":\"Holder to verifier: an OID4VP vp_token carrying the selectively-disclosed, holder-bound presentation. Replies on the query thread.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"vp_token\"],\"properties\":{\"vp_token\":{\"type\":[\"string\",\"object\"],\"description\":\"The OID4VP `vp_token`, format-agnostic by design. A JSON **string** is an SD-JWT-VC presentation: the consented disclosures plus a mandatory key-binding JWT over the verifier's nonce and audience. A JSON **object** is a W3C Data-Integrity VP whose proof carries the same nonce and domain. A consumer selects the verification path from the value's type rather than a separate format discriminator, so a new credential format needs no new member here. snake_case is OID4VP's own name for this field.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}
