// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/step-up/approve-request/0.3/payload.schema.json

package authstepupapproverequestv0_3

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseStatus `accepted` means the approver received the request and will return an
// approve-response (typically via DIDComm). `refused` means it will not — `reason` MUST
// be set.
type ResponseStatus string

// Values ResponseStatus may take, per this specification's schema.
const (
	ResponseStatusAccepted ResponseStatus = "accepted"
	ResponseStatusRefused  ResponseStatus = "refused"
)

// Response Synchronous ack from the approver acknowledging it received the request and
// will (or will not) deliver an approve-response. Carried in a Trust Task document whose
// type is https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response.
type Response struct {
	// `accepted` means the approver received the request and will return an approve-response
	// (typically via DIDComm). `refused` means it will not — `reason` MUST be set.
	Status ResponseStatus `json:"status"`

	// Required when status is `refused`.
	Reason *string `json:"reason,omitempty"`
	Ext    *Ext    `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// CredentialRequestOptions Server-issued options for `navigator.credentials.get({
// publicKey: ... })`.
type CredentialRequestOptions struct {
	// Client extension inputs, per the WebAuthn Level 2
	// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
	// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while
	// closing the object with `additionalProperties: false` made the two claims contradict
	// each other: a server emitting standard WebAuthn options could not conform, and the
	// widely-used server libraries emit this member by default. Structure is deliberately
	// unconstrained. The set of extensions is open and registered outside this framework, so
	// enumerating them here would date the schema against a registry it does not own — and a
	// closed list would reproduce the original defect one revision later.
	Extensions *map[string]json.RawMessage `json:"extensions,omitempty"`

	// base64url-encoded one-time nonce.
	Challenge        string                  `json:"challenge"`
	Timeout          *int64                  `json:"timeout,omitempty"`
	RpID             *string                 `json:"rpId,omitempty"`
	AllowCredentials *[]CredentialDescriptor `json:"allowCredentials,omitempty"`
	UserVerification json.RawMessage         `json:"userVerification,omitempty"`
}

// CredentialDescriptor PublicKeyCredentialDescriptor
type CredentialDescriptor struct {
	Type json.RawMessage `json:"type"`

	// base64url-encoded credential id.
	ID         string             `json:"id"`
	Transports *[]json.RawMessage `json:"transports,omitempty"`
}

// PayloadAcceptableEvidenceItem PayloadAcceptableEvidenceItem is a closed set of string
// values.
type PayloadAcceptableEvidenceItem string

// Values PayloadAcceptableEvidenceItem may take, per this specification's schema.
const (
	PayloadAcceptableEvidenceItemDIDSigned PayloadAcceptableEvidenceItem = "didSigned"
	PayloadAcceptableEvidenceItemWebauthn  PayloadAcceptableEvidenceItem = "webauthn"
)

// Payload A relying party asks an approver (typically a wallet or a VTA) to ratify an AAL
// elevation for a subject's session, or — bound to one operation — to approve that one
// act without elevating any session.
type Payload struct {
	// The VID whose session is being elevated, or — for a step-up bound to one operation —
	// the VID whose act that operation is. The approver MUST verify this is a VID it can
	// speak for.
	Subject string `json:"subject"`

	// The session the relying party wants elevated. Opaque to the approver. Absent only when
	// the step-up is bound to one operation that arrived without a session (a signed Trust
	// Task document, for instance); see `boundTo`.
	SessionID *string `json:"sessionId,omitempty"`

	// Present when the step-up authorizes exactly one operation rather than raising a
	// session's assurance level. An opaque relying-party identifier for that operation — for
	// an operation that arrived as a signed document, a digest of its type and payload salted
	// with `challenge`. Echoed by a `recorded` approve-response acknowledgement. Not a
	// capability: the binding is the relying party's own state.
	BoundTo *string `json:"boundTo,omitempty"`

	// base64url-encoded nonce the approver will include in the approve-response signature.
	// ≥128 bits entropy.
	Challenge string `json:"challenge"`

	// Human-readable explanation of WHY the relying party is asking (e.g. "confirm transfer
	// of 1000 USD to bob.example"). Surfaced to the user by the approver for consent. SHOULD
	// be specific enough that a user can refuse intelligently.
	Reason string `json:"reason"`

	// The acr the relying party expects on the elevated session. Approvers MAY refuse if they
	// cannot deliver this level.
	TargetAcr *string `json:"targetAcr,omitempty"`

	// Which approve-response evidence kinds the relying party will accept (see
	// auth/step-up/approve-response `evidence`). When omitted, the approver MAY use any kind
	// it supports. An approver that cannot satisfy any listed kind SHOULD refuse with
	// `methodUnsupported`.
	AcceptableEvidence *[]PayloadAcceptableEvidenceItem `json:"acceptableEvidence,omitempty"`

	// Optional WebAuthn `PublicKeyCredentialRequestOptions` the approver passes to the
	// platform passkey API when producing `webauthn` evidence. When present, its `challenge`
	// MUST equal `payload.challenge` so the resulting assertion binds the same nonce the
	// relying party bound server-side. `rpId`/`allowCredentials` identify which credential
	// the approver should assert with.
	Webauthn *CredentialRequestOptions `json:"webauthn,omitempty"`

	// Seconds within which the relying party expects the approve-response. Approvers SHOULD
	// treat as advisory — the relying party's own expiry policy is authoritative.
	TTL *int64 `json:"ttl,omitempty"`
	Ext *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/auth/step-up/approve-request/0.3"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/auth/step-up/approve-request/0.3\",\"title\":\"Auth — Step-up Approve Request\",\"description\":\"A relying party asks an approver (typically a wallet or a VTA) to ratify an AAL elevation for a subject's session, or — bound to one operation — to approve that one act without elevating any session.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\",\"challenge\",\"reason\"],\"properties\":{\"subject\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The VID whose session is being elevated, or — for a step-up bound to one operation — the VID whose act that operation is. The approver MUST verify this is a VID it can speak for.\"},\"sessionId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The session the relying party wants elevated. Opaque to the approver. Absent only when the step-up is bound to one operation that arrived without a session (a signed Trust Task document, for instance); see `boundTo`.\"},\"boundTo\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":256,\"description\":\"Present when the step-up authorizes exactly one operation rather than raising a session's assurance level. An opaque relying-party identifier for that operation — for an operation that arrived as a signed document, a digest of its type and payload salted with `challenge`. Echoed by a `recorded` approve-response acknowledgement. Not a capability: the binding is the relying party's own state.\"},\"challenge\":{\"type\":\"string\",\"minLength\":16,\"description\":\"base64url-encoded nonce the approver will include in the approve-response signature. ≥128 bits entropy.\"},\"reason\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":500,\"description\":\"Human-readable explanation of WHY the relying party is asking (e.g. \\\"confirm transfer of 1000 USD to bob.example\\\"). Surfaced to the user by the approver for consent. SHOULD be specific enough that a user can refuse intelligently.\"},\"targetAcr\":{\"type\":\"string\",\"description\":\"The acr the relying party expects on the elevated session. Approvers MAY refuse if they cannot deliver this level.\"},\"acceptableEvidence\":{\"type\":\"array\",\"minItems\":1,\"uniqueItems\":true,\"items\":{\"type\":\"string\",\"enum\":[\"didSigned\",\"webauthn\"]},\"description\":\"Which approve-response evidence kinds the relying party will accept (see auth/step-up/approve-response `evidence`). When omitted, the approver MAY use any kind it supports. An approver that cannot satisfy any listed kind SHOULD refuse with `methodUnsupported`.\"},\"webauthn\":{\"$ref\":\"#/$defs/CredentialRequestOptions\",\"description\":\"Optional WebAuthn `PublicKeyCredentialRequestOptions` the approver passes to the platform passkey API when producing `webauthn` evidence. When present, its `challenge` MUST equal `payload.challenge` so the resulting assertion binds the same nonce the relying party bound server-side. `rpId`/`allowCredentials` identify which credential the approver should assert with.\"},\"ttl\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"Seconds within which the relying party expects the approve-response. Approvers SHOULD treat as advisory — the relying party's own expiry policy is authoritative.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Auth Step-up Approve Request — response payload\",\"description\":\"Synchronous ack from the approver acknowledging it received the request and will (or will not) deliver an approve-response. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"status\"],\"properties\":{\"status\":{\"type\":\"string\",\"enum\":[\"accepted\",\"refused\"],\"description\":\"`accepted` means the approver received the request and will return an approve-response (typically via DIDComm). `refused` means it will not — `reason` MUST be set.\"},\"reason\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Required when status is `refused`.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"CredentialRequestOptions\":{\"$anchor\":\"credentialRequestOptions\",\"title\":\"PublicKeyCredentialRequestOptions\",\"description\":\"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"challenge\"],\"properties\":{\"extensions\":{\"type\":\"object\",\"description\":\"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later.\"},\"challenge\":{\"type\":\"string\",\"description\":\"base64url-encoded one-time nonce.\"},\"timeout\":{\"type\":\"integer\",\"minimum\":1},\"rpId\":{\"type\":\"string\",\"minLength\":1},\"allowCredentials\":{\"type\":\"array\",\"items\":{\"$ref\":\"#/$defs/CredentialDescriptor\"}},\"userVerification\":{\"enum\":[\"discouraged\",\"preferred\",\"required\"]}}},\"CredentialDescriptor\":{\"$anchor\":\"credentialDescriptor\",\"title\":\"PublicKeyCredentialDescriptor\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\",\"id\"],\"properties\":{\"type\":{\"const\":\"public-key\"},\"id\":{\"type\":\"string\",\"description\":\"base64url-encoded credential id.\"},\"transports\":{\"type\":\"array\",\"items\":{\"enum\":[\"usb\",\"nfc\",\"ble\",\"internal\",\"hybrid\"]}}}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Auth Step-up Approve Request — response payload\",\"description\":\"Synchronous ack from the approver acknowledging it received the request and will (or will not) deliver an approve-response. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"status\"],\"properties\":{\"status\":{\"type\":\"string\",\"enum\":[\"accepted\",\"refused\"],\"description\":\"`accepted` means the approver received the request and will return an approve-response (typically via DIDComm). `refused` means it will not — `reason` MUST be set.\"},\"reason\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Required when status is `refused`.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"CredentialRequestOptions\":{\"$anchor\":\"credentialRequestOptions\",\"title\":\"PublicKeyCredentialRequestOptions\",\"description\":\"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"challenge\"],\"properties\":{\"extensions\":{\"type\":\"object\",\"description\":\"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later.\"},\"challenge\":{\"type\":\"string\",\"description\":\"base64url-encoded one-time nonce.\"},\"timeout\":{\"type\":\"integer\",\"minimum\":1},\"rpId\":{\"type\":\"string\",\"minLength\":1},\"allowCredentials\":{\"type\":\"array\",\"items\":{\"$ref\":\"#/$defs/CredentialDescriptor\"}},\"userVerification\":{\"enum\":[\"discouraged\",\"preferred\",\"required\"]}}},\"CredentialDescriptor\":{\"$anchor\":\"credentialDescriptor\",\"title\":\"PublicKeyCredentialDescriptor\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\",\"id\"],\"properties\":{\"type\":{\"const\":\"public-key\"},\"id\":{\"type\":\"string\",\"description\":\"base64url-encoded credential id.\"},\"transports\":{\"type\":\"array\",\"items\":{\"enum\":[\"usb\",\"nfc\",\"ble\",\"internal\",\"hybrid\"]}}}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
