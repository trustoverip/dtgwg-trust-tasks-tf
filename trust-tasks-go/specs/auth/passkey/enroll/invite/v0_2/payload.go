// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/passkey/enroll/invite/0.2/payload.schema.json

package authpasskeyenrollinvitev0_2

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseInvite ResponseInvite is a generated payload type.
type ResponseInvite struct {
	// Opaque single-use invite token. The invitee presents it to the consumer during
	// enrollment. ≥128 bits entropy.
	Token string `json:"token"`

	// Operator-shareable URL that, when opened in a WebAuthn-capable browser, drives the
	// redemption ceremony (auth/passkey/enroll/redeem/start). Carries the token. MUST NOT
	// carry the claim code.
	URL string `json:"url"`
}

// ResponsePurpose Echo of the purpose the redeemed credential will have.
type ResponsePurpose string

// Values ResponsePurpose may take, per this specification's schema.
const (
	ResponsePurposeSession ResponsePurpose = "session"
	ResponsePurposeStepUp  ResponsePurpose = "stepUp"
)

// Response The issued invite. Carried in a Trust Task document whose type is
// https://trusttasks.org/spec/auth/passkey/enroll/invite/0.2#response.
type Response struct {
	Invite ResponseInvite `json:"invite"`

	// Echo of the invitee's VID.
	Subject string `json:"subject"`

	// Echo of the purpose the redeemed credential will have.
	Purpose   ResponsePurpose `json:"purpose"`
	ExpiresAt string          `json:"expiresAt"`
	Ext       *Ext            `json:"ext,omitempty"`

	// A short code the invitee types at redemption, returned exactly once. The administrator
	// delivers it over a different channel from `url`, so that intercepting one channel is
	// not enough to redeem. The consumer stores only a slow hash of it.
	ClaimCode string `json:"claimCode"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// PayloadPurpose What the redeemed credential may authenticate. `session` (the default,
// and 0.1's only behaviour): it signs the subject in, with the role and scopes the invite
// carries. `stepUp`: it is accepted only as the user-verification evidence of a step-up
// bound to one operation (auth/step-up/approve-response `evidence.kind: webauthn`), never
// to open or elevate a session; `role` and `scopes` MUST then be absent.
type PayloadPurpose string

// Values PayloadPurpose may take, per this specification's schema.
const (
	PayloadPurposeSession PayloadPurpose = "session"
	PayloadPurposeStepUp  PayloadPurpose = "stepUp"
)

// Payload An administrator issues a single-use invite, redeemed with a separately
// delivered claim code, that binds a passkey to a subject who cannot yet authorise the
// binding themselves: their first passkey, or — for `purpose: stepUp` — a step-up-only
// passkey that never opens a session.
type Payload struct {
	// The VID the invitee will enroll. The admin asserts this binding; the redemption
	// ceremony verifies the invitee controls a passkey and holds the claim code, but does NOT
	// independently verify the subject — trust flows from the admin. For `purpose: stepUp`
	// the consumer MUST already recognise the subject (a member, say).
	Subject string `json:"subject"`

	// What the redeemed credential may authenticate. `session` (the default, and 0.1's only
	// behaviour): it signs the subject in, with the role and scopes the invite carries.
	// `stepUp`: it is accepted only as the user-verification evidence of a step-up bound to
	// one operation (auth/step-up/approve-response `evidence.kind: webauthn`), never to open
	// or elevate a session; `role` and `scopes` MUST then be absent.
	Purpose *PayloadPurpose `json:"purpose,omitempty"`

	// Role the invitee receives on successful enrollment. Consumer-defined vocabulary; if
	// absent the consumer applies its default role.
	Role *string `json:"role,omitempty"`

	// Optional scopes to attach on enrollment.
	Scopes *[]string `json:"scopes,omitempty"`

	// Suggested label for the credential. Used as a default; the invitee MAY override during
	// redemption.
	DeviceLabel *string `json:"deviceLabel,omitempty"`

	// Seconds the invite remains valid. Consumers SHOULD pick 15 min – 24 h, and MAY cap it
	// lower; longer windows widen the attack surface.
	TTL *int64 `json:"ttl,omitempty"`
	Ext *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/auth/passkey/enroll/invite/0.2"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/auth/passkey/enroll/invite/0.2#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/auth/passkey/enroll/invite/0.2\",\"title\":\"Auth — Passkey Enroll (invite)\",\"description\":\"An administrator issues a single-use invite, redeemed with a separately delivered claim code, that binds a passkey to a subject who cannot yet authorise the binding themselves: their first passkey, or — for `purpose: stepUp` — a step-up-only passkey that never opens a session.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\"],\"properties\":{\"subject\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The VID the invitee will enroll. The admin asserts this binding; the redemption ceremony verifies the invitee controls a passkey and holds the claim code, but does NOT independently verify the subject — trust flows from the admin. For `purpose: stepUp` the consumer MUST already recognise the subject (a member, say).\"},\"purpose\":{\"type\":\"string\",\"enum\":[\"session\",\"stepUp\"],\"default\":\"session\",\"description\":\"What the redeemed credential may authenticate. `session` (the default, and 0.1's only behaviour): it signs the subject in, with the role and scopes the invite carries. `stepUp`: it is accepted only as the user-verification evidence of a step-up bound to one operation (auth/step-up/approve-response `evidence.kind: webauthn`), never to open or elevate a session; `role` and `scopes` MUST then be absent.\"},\"role\":{\"type\":\"string\",\"description\":\"Role the invitee receives on successful enrollment. Consumer-defined vocabulary; if absent the consumer applies its default role.\"},\"scopes\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"minLength\":1},\"description\":\"Optional scopes to attach on enrollment.\"},\"deviceLabel\":{\"type\":\"string\",\"maxLength\":256,\"description\":\"Suggested label for the credential. Used as a default; the invitee MAY override during redemption.\"},\"ttl\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"Seconds the invite remains valid. Consumers SHOULD pick 15 min – 24 h, and MAY cap it lower; longer windows widen the attack surface.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Auth Passkey Enroll Invite — response payload\",\"description\":\"The issued invite. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/enroll/invite/0.2#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"invite\",\"subject\",\"purpose\",\"expiresAt\",\"claimCode\"],\"properties\":{\"invite\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"token\",\"url\"],\"properties\":{\"token\":{\"type\":\"string\",\"minLength\":16,\"description\":\"Opaque single-use invite token. The invitee presents it to the consumer during enrollment. ≥128 bits entropy.\"},\"url\":{\"type\":\"string\",\"format\":\"uri\",\"description\":\"Operator-shareable URL that, when opened in a WebAuthn-capable browser, drives the redemption ceremony (auth/passkey/enroll/redeem/start). Carries the token. MUST NOT carry the claim code.\"}}},\"subject\":{\"type\":\"string\",\"description\":\"Echo of the invitee's VID.\"},\"purpose\":{\"type\":\"string\",\"enum\":[\"session\",\"stepUp\"],\"description\":\"Echo of the purpose the redeemed credential will have.\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"},\"claimCode\":{\"type\":\"string\",\"minLength\":8,\"maxLength\":64,\"description\":\"A short code the invitee types at redemption, returned exactly once. The administrator delivers it over a different channel from `url`, so that intercepting one channel is not enough to redeem. The consumer stores only a slow hash of it.\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Auth Passkey Enroll Invite — response payload\",\"description\":\"The issued invite. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/enroll/invite/0.2#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"invite\",\"subject\",\"purpose\",\"expiresAt\",\"claimCode\"],\"properties\":{\"invite\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"token\",\"url\"],\"properties\":{\"token\":{\"type\":\"string\",\"minLength\":16,\"description\":\"Opaque single-use invite token. The invitee presents it to the consumer during enrollment. ≥128 bits entropy.\"},\"url\":{\"type\":\"string\",\"format\":\"uri\",\"description\":\"Operator-shareable URL that, when opened in a WebAuthn-capable browser, drives the redemption ceremony (auth/passkey/enroll/redeem/start). Carries the token. MUST NOT carry the claim code.\"}}},\"subject\":{\"type\":\"string\",\"description\":\"Echo of the invitee's VID.\"},\"purpose\":{\"type\":\"string\",\"enum\":[\"session\",\"stepUp\"],\"description\":\"Echo of the purpose the redeemed credential will have.\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"},\"claimCode\":{\"type\":\"string\",\"minLength\":8,\"maxLength\":64,\"description\":\"A short code the invitee types at redemption, returned exactly once. The administrator delivers it over a different channel from `url`, so that intercepting one channel is not enough to redeem. The consumer stores only a slow hash of it.\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
