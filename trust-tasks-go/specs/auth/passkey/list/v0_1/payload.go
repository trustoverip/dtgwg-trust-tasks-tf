// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/passkey/list/0.1/payload.schema.json

package authpasskeylistv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Carried in a Trust Task document whose type is
// https://trusttasks.org/spec/auth/passkey/list/0.1#response.
type Response struct {
	// Every passkey currently bound to the producer's subject. MAY be empty — a subject who
	// authenticates by other means has no passkeys, which is not an error. Consumers SHOULD
	// sort by registeredAt descending.
	Credentials []RegisteredCredential `json:"credentials"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// RegisteredCredential A passkey already bound to a subject, as listed by
// `auth/passkey/list` and targeted by `auth/passkey/revoke/*`. This is the auth service's
// own management view of a credential — not a WebAuthn dictionary — so its members are
// camelCase per SPEC.md §4.10. The exception is `transports`, whose values are the
// externally-owned WebAuthn transport tokens and are carried verbatim.
type RegisteredCredential struct {
	// Durable identifier for this credential, as issued by `auth/passkey/enroll/finish`: the
	// base64url-encoded WebAuthn credential id. Opaque to the producer — it MUST be echoed
	// verbatim when revoking and MUST NOT be parsed.
	CredentialID string `json:"credentialId"`

	// Operator-facing label captured at enrollment (e.g. "Alice's MacBook Pro"). Absent when
	// the subject enrolled without one. Consumers MUST NOT synthesize a label, because an
	// invented one is indistinguishable from a chosen one to somebody deciding which
	// credential to revoke.
	DeviceLabel *string `json:"deviceLabel,omitempty"`

	// WebAuthn transport hints reported by the authenticator at enrollment (`usb`, `nfc`,
	// `ble`, `internal`, `hybrid`, …). Externally owned and carried verbatim. Deliberately
	// not an enum — unlike `CredentialDescriptor.transports`, which the consumer itself
	// emits, these values were reported by a third-party authenticator, the WebAuthn
	// transport registry grows independently of this specification, and an unrecognized token
	// here is a display concern rather than a validation failure.
	Transports *[]string `json:"transports,omitempty"`

	// When the credential was bound to the subject.
	RegisteredAt string `json:"registeredAt"`

	// When this credential last completed an assertion. Absent if it never has, or if the
	// consumer does not track usage. Those two cases are deliberately NOT distinguished: a
	// nullable variant cannot survive the round-trip through generated bindings, which map
	// absent and null onto the same optional, so a distinction stated here would not be one
	// any conforming implementation could rely on. Consumers that need to advertise "usage is
	// not tracked" SHOULD say so under `ext`.
	LastUsedAt *string `json:"lastUsedAt,omitempty"`
}

// Payload Enumerate the passkeys the auth service holds for the producer's subject. The
// credential-management counterpart to auth/sessions/list: sessions answers "where am I
// signed in?", this answers "what can sign me in?". Its output is the input to
// auth/passkey/revoke/start.
type Payload struct {
	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/auth/passkey/list/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/auth/passkey/list/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/auth/passkey/list/0.1\",\"title\":\"Auth — Passkey List\",\"description\":\"Enumerate the passkeys the auth service holds for the producer's subject. The credential-management counterpart to auth/sessions/list: sessions answers \\\"where am I signed in?\\\", this answers \\\"what can sign me in?\\\". Its output is the input to auth/passkey/revoke/start.\",\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Auth Passkey List — response payload\",\"description\":\"Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/list/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"credentials\"],\"properties\":{\"credentials\":{\"type\":\"array\",\"items\":{\"$ref\":\"#/$defs/RegisteredCredential\"},\"description\":\"Every passkey currently bound to the producer's subject. MAY be empty — a subject who authenticates by other means has no passkeys, which is not an error. Consumers SHOULD sort by registeredAt descending.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"RegisteredCredential\":{\"$anchor\":\"registeredCredential\",\"title\":\"RegisteredCredential\",\"description\":\"A passkey already bound to a subject, as listed by `auth/passkey/list` and targeted by `auth/passkey/revoke/*`. This is the auth service's own management view of a credential — not a WebAuthn dictionary — so its members are camelCase per SPEC.md §4.10. The exception is `transports`, whose values are the externally-owned WebAuthn transport tokens and are carried verbatim.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"credentialId\",\"registeredAt\"],\"properties\":{\"credentialId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Durable identifier for this credential, as issued by `auth/passkey/enroll/finish`: the base64url-encoded WebAuthn credential id. Opaque to the producer — it MUST be echoed verbatim when revoking and MUST NOT be parsed.\"},\"deviceLabel\":{\"type\":\"string\",\"maxLength\":256,\"description\":\"Operator-facing label captured at enrollment (e.g. \\\"Alice's MacBook Pro\\\"). Absent when the subject enrolled without one. Consumers MUST NOT synthesize a label, because an invented one is indistinguishable from a chosen one to somebody deciding which credential to revoke.\"},\"transports\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"uniqueItems\":true,\"description\":\"WebAuthn transport hints reported by the authenticator at enrollment (`usb`, `nfc`, `ble`, `internal`, `hybrid`, …). Externally owned and carried verbatim. Deliberately not an enum — unlike `CredentialDescriptor.transports`, which the consumer itself emits, these values were reported by a third-party authenticator, the WebAuthn transport registry grows independently of this specification, and an unrecognized token here is a display concern rather than a validation failure.\"},\"registeredAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the credential was bound to the subject.\"},\"lastUsedAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When this credential last completed an assertion. Absent if it never has, or if the consumer does not track usage. Those two cases are deliberately NOT distinguished: a nullable variant cannot survive the round-trip through generated bindings, which map absent and null onto the same optional, so a distinction stated here would not be one any conforming implementation could rely on. Consumers that need to advertise \\\"usage is not tracked\\\" SHOULD say so under `ext`.\"}}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Auth Passkey List — response payload\",\"description\":\"Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/list/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"credentials\"],\"properties\":{\"credentials\":{\"type\":\"array\",\"items\":{\"$ref\":\"#/$defs/RegisteredCredential\"},\"description\":\"Every passkey currently bound to the producer's subject. MAY be empty — a subject who authenticates by other means has no passkeys, which is not an error. Consumers SHOULD sort by registeredAt descending.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"RegisteredCredential\":{\"$anchor\":\"registeredCredential\",\"title\":\"RegisteredCredential\",\"description\":\"A passkey already bound to a subject, as listed by `auth/passkey/list` and targeted by `auth/passkey/revoke/*`. This is the auth service's own management view of a credential — not a WebAuthn dictionary — so its members are camelCase per SPEC.md §4.10. The exception is `transports`, whose values are the externally-owned WebAuthn transport tokens and are carried verbatim.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"credentialId\",\"registeredAt\"],\"properties\":{\"credentialId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Durable identifier for this credential, as issued by `auth/passkey/enroll/finish`: the base64url-encoded WebAuthn credential id. Opaque to the producer — it MUST be echoed verbatim when revoking and MUST NOT be parsed.\"},\"deviceLabel\":{\"type\":\"string\",\"maxLength\":256,\"description\":\"Operator-facing label captured at enrollment (e.g. \\\"Alice's MacBook Pro\\\"). Absent when the subject enrolled without one. Consumers MUST NOT synthesize a label, because an invented one is indistinguishable from a chosen one to somebody deciding which credential to revoke.\"},\"transports\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"uniqueItems\":true,\"description\":\"WebAuthn transport hints reported by the authenticator at enrollment (`usb`, `nfc`, `ble`, `internal`, `hybrid`, …). Externally owned and carried verbatim. Deliberately not an enum — unlike `CredentialDescriptor.transports`, which the consumer itself emits, these values were reported by a third-party authenticator, the WebAuthn transport registry grows independently of this specification, and an unrecognized token here is a display concern rather than a validation failure.\"},\"registeredAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the credential was bound to the subject.\"},\"lastUsedAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When this credential last completed an assertion. Absent if it never has, or if the consumer does not track usage. Those two cases are deliberately NOT distinguished: a nullable variant cannot survive the round-trip through generated bindings, which map absent and null onto the same optional, so a distinction stated here would not be one any conforming implementation could rely on. Consumers that need to advertise \\\"usage is not tracked\\\" SHOULD say so under `ext`.\"}}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
