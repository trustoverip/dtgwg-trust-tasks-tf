// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/_shared/0.1/tokens.schema.json

package authsharedtokensv0_1

import "encoding/json"

// TokenBundle An access token (typically short-lived JWT) paired with an optional refresh
// token (typically long-lived opaque string). The shapes follow OAuth 2.0 (RFC 6749 §5.1)
// conventions but are not coupled to any particular OAuth profile.
type TokenBundle struct {
	// Bearer-style access token. Consumers presenting this token to downstream services prove
	// the holder of the original session. Format is consumer-defined — JWT is common, but
	// opaque strings are also valid.
	AccessToken string `json:"accessToken"`

	// Long-lived token redeemable via auth/refresh for a new access token. MAY be absent when
	// the issuer does not support refresh.
	RefreshToken *string `json:"refreshToken,omitempty"`

	// Token presentation scheme. Almost always "Bearer"; reserved for future schemes.
	TokenType string `json:"tokenType"`

	// Seconds from issuance until the access token expires.
	ExpiresIn int64 `json:"expiresIn"`

	// Seconds from issuance until the refresh token expires, when one was issued.
	RefreshExpiresIn *int64 `json:"refreshExpiresIn,omitempty"`

	// Capability tags effective on this token. Format is consumer-defined; the framework
	// imposes no syntax.
	Scope *[]string `json:"scope,omitempty"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage
