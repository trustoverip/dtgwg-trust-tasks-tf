// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/local/profile/put/1.0/payload.schema.json

package personalocalprofileputv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseCorrelationSeverity ResponseCorrelationSeverity is a closed set of string
// values.
type ResponseCorrelationSeverity string

// Values ResponseCorrelationSeverity may take, per this specification's schema.
const (
	ResponseCorrelationSeverityNone ResponseCorrelationSeverity = "none"
	ResponseCorrelationSeverityLow  ResponseCorrelationSeverity = "low"
	ResponseCorrelationSeverityHigh ResponseCorrelationSeverity = "high"
)

// ResponseCorrelation Local profiles ARE correlation-indexed, and the naive
// implementation that skips them loses the guard exactly where a human most needs it: a
// throwaway identity is precisely where somebody reuses a real value. Indexing them is
// not a leak — the index is above the boundary, is keyed by a hash, and only the holder
// can query it.
type ResponseCorrelation struct {
	Severity ResponseCorrelationSeverity `json:"severity"`

	// True when a value here also appears in the holder's pool. The signal that a throwaway
	// is not throwaway.
	MatchesPoolValue *bool `json:"matchesPoolValue,omitempty"`
}

// Response Success response to persona/local/profile/put. Type
// https://trusttasks.org/spec/persona/local/profile/put/1.0#response.
type Response struct {
	ProfileID Ulid    `json:"profileId"`
	Version   Version `json:"version"`
	Created   bool    `json:"created"`

	// Local profiles ARE correlation-indexed, and the naive implementation that skips them
	// loses the guard exactly where a human most needs it: a throwaway identity is precisely
	// where somebody reuses a real value. Indexing them is not a leak — the index is above
	// the boundary, is keyed by a hash, and only the holder can query it.
	Correlation *ResponseCorrelation `json:"correlation,omitempty"`
	Ext         *Ext                 `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Version A value of the store's monotonic write counter. Server-assigned; a producer
// never chooses one.
type Version = int64

// Ulid A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan
// of the store is also creation-ordered and a `list` needs no secondary sort.
// Server-assigned on create; a producer MAY supply one to make a create idempotent, and a
// maintainer MUST reject a supplied value that already exists rather than silently
// overwriting.
type Ulid = string

// ExpectedVersion Optimistic-concurrency precondition. A positive value requires the
// record's current `version` to equal it exactly; zero means create-only and applies only
// when no live record exists at the address.
type ExpectedVersion = int64

// Slot A role a profile entry plays within its profile, so a consumer can find it without
// guessing from its claim type. A profile MAY hold several entries of one type — a legal
// name and a display name, two phone numbers — and only a slot says which answers a given
// question. Unique within a profile. Well-known slots: - `displayName` — what this face
// calls itself. The entry a consumer renders as the face's name to anyone it is shown to.
// Distinct from the profile's own `name`, which is the holder's private label and never
// disclosed. - `primaryEmail`, `primaryPhone`, `primaryAddress` — the entry to use where
// a counterparty asks for one of a kind and the profile holds several. - `avatar` — the
// image this face presents. Other values are the holder's or the producer's own and carry
// no meaning a maintainer interprets.
type Slot = string

// ValueType The JSON shape of `value`, declared so that a consumer can render and compare
// without guessing. The maintainer validates that `value` agrees with this member and
// does nothing further: it does NOT validate a phone number against a phone-number
// grammar. That is a producer's affordance, and a store that grows opinions about the
// contents of its records eventually blocks its consumer's release.
type ValueType string

// Values ValueType may take, per this specification's schema.
const (
	ValueTypeString  ValueType = "string"
	ValueTypeNumber  ValueType = "number"
	ValueTypeBoolean ValueType = "boolean"
	ValueTypeDate    ValueType = "date"
	ValueTypeObject  ValueType = "object"
)

// ClaimType The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`,
// `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a
// consumer with no knowledge of the specific token can still group by its prefix. The
// token is the maintainer's own; no external vocabulary is primary. External vocabularies
// (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by
// a renderer, not at rest, so that a query written in any of them can be matched without
// the store having to live inside any one of them. The `x:` prefix is an open extension
// namespace and is not decoration. The closest prior art — Windows CardSpace's
// self-issued card — supported exactly fifteen predefined claim types with no
// extensibility, and that is the specific way it failed the requirement a holder actually
// has. An `x:` attribute stores, composes, binds and discloses exactly like a known one;
// it renders generically and matches only an explicit query.
type ClaimType = string

// PayloadEntriesItemInline A value the holder keeps in this context and nowhere else.
// Deliberately NARROWER than a pool profile's inline entry, which also carries
// `provenance` and requires it. There is no `provenance` member here, and its absence is
// a rule rather than an omission: a `credentialBacked` provenance names a `credentialId`
// and a `claimPath`, and a value authored inside a context has nowhere to put either. So
// a context-local value is SELF-ASSERTED by construction, and a maintainer MUST present
// it as such. That is the same boundary the missing `ref`, pinned and override forms
// enforce, one member along. Those stop a context-authored object acquiring pool REACH;
// this stops it acquiring an issuer's AUTHORITY — asserting that a value is attested when
// no credential was ever checked, over a value the issuer never saw. A holder who needs a
// context to present an attested claim binds a pool profile, which is holder-authorized,
// rather than authoring one here. Adding a `provenance` member to this object would
// therefore be a privilege escalation dressed as a convenience, not a gap to fill.
type PayloadEntriesItemInline struct {
	Type      ClaimType       `json:"type"`
	ValueType ValueType       `json:"valueType"`
	Value     json.RawMessage `json:"value"`
	Label     *string         `json:"label,omitempty"`
}

// PayloadEntriesItem Inline entries only. The `ref`, pinned and override forms of a pool
// profile are absent from this schema deliberately — a context-local profile that could
// reference the pool would be a context-authored object acquiring pool reach, which is
// exactly what the boundary exists to prevent.
type PayloadEntriesItem struct {
	// A value the holder keeps in this context and nowhere else. Deliberately NARROWER than a
	// pool profile's inline entry, which also carries `provenance` and requires it. There is
	// no `provenance` member here, and its absence is a rule rather than an omission: a
	// `credentialBacked` provenance names a `credentialId` and a `claimPath`, and a value
	// authored inside a context has nowhere to put either. So a context-local value is
	// SELF-ASSERTED by construction, and a maintainer MUST present it as such. That is the
	// same boundary the missing `ref`, pinned and override forms enforce, one member along.
	// Those stop a context-authored object acquiring pool REACH; this stops it acquiring an
	// issuer's AUTHORITY — asserting that a value is attested when no credential was ever
	// checked, over a value the issuer never saw. A holder who needs a context to present an
	// attested claim binds a pool profile, which is holder-authorized, rather than authoring
	// one here. Adding a `provenance` member to this object would therefore be a privilege
	// escalation dressed as a convenience, not a gap to fill.
	Inline PayloadEntriesItemInline `json:"inline"`
	Slot   *Slot                    `json:"slot,omitempty"`
}

// Payload Compose a throwaway profile inside a context, from values supplied here rather
// than drawn from the holder's pool. Inline entries only: a reference is refused, and
// that refusal is what keeps the local surface pool-free.
type Payload struct {
	ContextID       string               `json:"contextId"`
	ProfileID       *Ulid                `json:"profileId,omitempty"`
	Name            string               `json:"name"`
	Entries         []PayloadEntriesItem `json:"entries"`
	ExpectedVersion *ExpectedVersion     `json:"expectedVersion,omitempty"`
	Ext             *Ext                 `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/persona/local/profile/put/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/persona/local/profile/put/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/persona/local/profile/put/1.0\",\"title\":\"Persona Local Profile Put — payload\",\"description\":\"Compose a throwaway profile inside a context, from values supplied here rather than drawn from the holder's pool. Inline entries only: a reference is refused, and that refusal is what keeps the local surface pool-free.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"name\",\"entries\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"profileId\":{\"$ref\":\"#/$defs/Ulid\"},\"name\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128},\"entries\":{\"type\":\"array\",\"maxItems\":64,\"items\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"inline\"],\"description\":\"Inline entries only. The `ref`, pinned and override forms of a pool profile are absent from this schema deliberately — a context-local profile that could reference the pool would be a context-authored object acquiring pool reach, which is exactly what the boundary exists to prevent.\",\"properties\":{\"inline\":{\"description\":\"A value the holder keeps in this context and nowhere else.\\n\\nDeliberately NARROWER than a pool profile's inline entry, which also carries `provenance` and requires it. There is no `provenance` member here, and its absence is a rule rather than an omission: a `credentialBacked` provenance names a `credentialId` and a `claimPath`, and a value authored inside a context has nowhere to put either. So a context-local value is SELF-ASSERTED by construction, and a maintainer MUST present it as such.\\n\\nThat is the same boundary the missing `ref`, pinned and override forms enforce, one member along. Those stop a context-authored object acquiring pool REACH; this stops it acquiring an issuer's AUTHORITY — asserting that a value is attested when no credential was ever checked, over a value the issuer never saw. A holder who needs a context to present an attested claim binds a pool profile, which is holder-authorized, rather than authoring one here.\\n\\nAdding a `provenance` member to this object would therefore be a privilege escalation dressed as a convenience, not a gap to fill.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\",\"valueType\",\"value\"],\"properties\":{\"type\":{\"$ref\":\"#/$defs/ClaimType\"},\"valueType\":{\"$ref\":\"#/$defs/ValueType\"},\"value\":{},\"label\":{\"type\":\"string\",\"maxLength\":128}}},\"slot\":{\"$ref\":\"#/$defs/Slot\"}}}},\"expectedVersion\":{\"$ref\":\"#/$defs/ExpectedVersion\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Local Profile Put — response payload\",\"description\":\"Success response to persona/local/profile/put. Type https://trusttasks.org/spec/persona/local/profile/put/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"profileId\",\"version\",\"created\"],\"properties\":{\"profileId\":{\"$ref\":\"#/$defs/Ulid\"},\"version\":{\"$ref\":\"#/$defs/Version\"},\"created\":{\"type\":\"boolean\"},\"correlation\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"severity\"],\"description\":\"Local profiles ARE correlation-indexed, and the naive implementation that skips them loses the guard exactly where a human most needs it: a throwaway identity is precisely where somebody reuses a real value. Indexing them is not a leak — the index is above the boundary, is keyed by a hash, and only the holder can query it.\",\"properties\":{\"severity\":{\"type\":\"string\",\"enum\":[\"none\",\"low\",\"high\"]},\"matchesPoolValue\":{\"type\":\"boolean\",\"description\":\"True when a value here also appears in the holder's pool. The signal that a throwaway is not throwaway.\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Version\":{\"title\":\"Version\",\"description\":\"A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.\",\"type\":\"integer\",\"minimum\":1},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"},\"ExpectedVersion\":{\"title\":\"ExpectedVersion\",\"description\":\"Optimistic-concurrency precondition. A positive value requires the record's current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.\",\"type\":\"integer\",\"minimum\":0},\"Slot\":{\"title\":\"Slot\",\"type\":\"string\",\"pattern\":\"^[a-z][A-Za-z0-9]{0,31}$\",\"description\":\"A role a profile entry plays within its profile, so a consumer can find it without guessing from its claim type. A profile MAY hold several entries of one type — a legal name and a display name, two phone numbers — and only a slot says which answers a given question. Unique within a profile.\\n\\nWell-known slots:\\n\\n- `displayName` — what this face calls itself. The entry a consumer renders as the face's name to anyone it is shown to. Distinct from the profile's own `name`, which is the holder's private label and never disclosed.\\n- `primaryEmail`, `primaryPhone`, `primaryAddress` — the entry to use where a counterparty asks for one of a kind and the profile holds several.\\n- `avatar` — the image this face presents.\\n\\nOther values are the holder's or the producer's own and carry no meaning a maintainer interprets.\"},\"ValueType\":{\"title\":\"ValueType\",\"description\":\"The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer's affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer's release.\",\"type\":\"string\",\"enum\":[\"string\",\"number\",\"boolean\",\"date\",\"object\"]},\"ClaimType\":{\"title\":\"ClaimType\",\"description\":\"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"pattern\":\"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Local Profile Put — response payload\",\"description\":\"Success response to persona/local/profile/put. Type https://trusttasks.org/spec/persona/local/profile/put/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"profileId\",\"version\",\"created\"],\"properties\":{\"profileId\":{\"$ref\":\"#/$defs/Ulid\"},\"version\":{\"$ref\":\"#/$defs/Version\"},\"created\":{\"type\":\"boolean\"},\"correlation\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"severity\"],\"description\":\"Local profiles ARE correlation-indexed, and the naive implementation that skips them loses the guard exactly where a human most needs it: a throwaway identity is precisely where somebody reuses a real value. Indexing them is not a leak — the index is above the boundary, is keyed by a hash, and only the holder can query it.\",\"properties\":{\"severity\":{\"type\":\"string\",\"enum\":[\"none\",\"low\",\"high\"]},\"matchesPoolValue\":{\"type\":\"boolean\",\"description\":\"True when a value here also appears in the holder's pool. The signal that a throwaway is not throwaway.\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Version\":{\"title\":\"Version\",\"description\":\"A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.\",\"type\":\"integer\",\"minimum\":1},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"},\"ExpectedVersion\":{\"title\":\"ExpectedVersion\",\"description\":\"Optimistic-concurrency precondition. A positive value requires the record's current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.\",\"type\":\"integer\",\"minimum\":0},\"Slot\":{\"title\":\"Slot\",\"type\":\"string\",\"pattern\":\"^[a-z][A-Za-z0-9]{0,31}$\",\"description\":\"A role a profile entry plays within its profile, so a consumer can find it without guessing from its claim type. A profile MAY hold several entries of one type — a legal name and a display name, two phone numbers — and only a slot says which answers a given question. Unique within a profile.\\n\\nWell-known slots:\\n\\n- `displayName` — what this face calls itself. The entry a consumer renders as the face's name to anyone it is shown to. Distinct from the profile's own `name`, which is the holder's private label and never disclosed.\\n- `primaryEmail`, `primaryPhone`, `primaryAddress` — the entry to use where a counterparty asks for one of a kind and the profile holds several.\\n- `avatar` — the image this face presents.\\n\\nOther values are the holder's or the producer's own and carry no meaning a maintainer interprets.\"},\"ValueType\":{\"title\":\"ValueType\",\"description\":\"The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer's affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer's release.\",\"type\":\"string\",\"enum\":[\"string\",\"number\",\"boolean\",\"date\",\"object\"]},\"ClaimType\":{\"title\":\"ClaimType\",\"description\":\"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer's own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"pattern\":\"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*$\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
