// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/attribute/purge-version/1.0/payload.schema.json

package personaattributepurgeversionv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseStalePinsItem ResponseStalePinsItem is a generated payload type.
type ResponseStalePinsItem struct {
	ProfileID  Ulid    `json:"profileId"`
	PinVersion Version `json:"pinVersion"`
}

// Response Success response to persona/attribute/purge-version. Type
// https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response. Purging a
// version that is not held is a success that purges nothing, so a repeated purge
// converges.
type Response struct {
	AttributeID Ulid `json:"attributeId"`

	// The versions actually removed. Empty when none named was held.
	Purged []Version `json:"purged"`

	// Profiles that pinned a purged version and now present that entry as stale — which is to
	// say, disclose nothing for it until the holder repins or edits them. Named because a
	// purge that quietly shortened what three faces present is the surprise this family
	// exists to prevent. Absent when none did.
	StalePins *[]ResponseStalePinsItem `json:"stalePins,omitempty"`
	Ext       *Ext                     `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Version A value of the store's monotonic write counter. Server-assigned; a producer
// never chooses one.
type Version = int64

// Ulid A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan
// of the store is also creation-ordered and a `list` needs no secondary sort.
// Server-assigned on create; a producer MAY supply one to make a create idempotent, and a
// maintainer MUST reject a supplied value that already exists rather than silently
// overwriting.
type Ulid = string

// Payload Permanently remove earlier versions of one attribute that the maintainer kept
// because a profile pins them. The holder's override on retention: after a name change,
// the old name is kept only while something needs it, and this is how the holder says
// nothing does.
type Payload struct {
	AttributeID Ulid `json:"attributeId"`

	// The retained versions to remove. Omit to remove every retained version of the
	// attribute. The current version is never removed by this task — that is
	// persona/attribute/delete.
	Versions *[]Version `json:"versions,omitempty"`
	Ext      *Ext       `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/persona/attribute/purge-version/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/persona/attribute/purge-version/1.0\",\"title\":\"Persona Attribute Purge Version — payload\",\"description\":\"Permanently remove earlier versions of one attribute that the maintainer kept because a profile pins them. The holder's override on retention: after a name change, the old name is kept only while something needs it, and this is how the holder says nothing does.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"attributeId\"],\"properties\":{\"attributeId\":{\"$ref\":\"#/$defs/Ulid\"},\"versions\":{\"type\":\"array\",\"minItems\":1,\"maxItems\":64,\"uniqueItems\":true,\"items\":{\"$ref\":\"#/$defs/Version\"},\"description\":\"The retained versions to remove. Omit to remove every retained version of the attribute. The current version is never removed by this task — that is persona/attribute/delete.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Attribute Purge Version — response payload\",\"description\":\"Success response to persona/attribute/purge-version. Type https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response. Purging a version that is not held is a success that purges nothing, so a repeated purge converges.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"attributeId\",\"purged\"],\"properties\":{\"attributeId\":{\"$ref\":\"#/$defs/Ulid\"},\"purged\":{\"type\":\"array\",\"maxItems\":64,\"items\":{\"$ref\":\"#/$defs/Version\"},\"description\":\"The versions actually removed. Empty when none named was held.\"},\"stalePins\":{\"type\":\"array\",\"maxItems\":256,\"description\":\"Profiles that pinned a purged version and now present that entry as stale — which is to say, disclose nothing for it until the holder repins or edits them. Named because a purge that quietly shortened what three faces present is the surprise this family exists to prevent. Absent when none did.\",\"items\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"profileId\",\"pinVersion\"],\"properties\":{\"profileId\":{\"$ref\":\"#/$defs/Ulid\"},\"pinVersion\":{\"$ref\":\"#/$defs/Version\"}}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Version\":{\"title\":\"Version\",\"description\":\"A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.\",\"type\":\"integer\",\"minimum\":1},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Attribute Purge Version — response payload\",\"description\":\"Success response to persona/attribute/purge-version. Type https://trusttasks.org/spec/persona/attribute/purge-version/1.0#response. Purging a version that is not held is a success that purges nothing, so a repeated purge converges.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"attributeId\",\"purged\"],\"properties\":{\"attributeId\":{\"$ref\":\"#/$defs/Ulid\"},\"purged\":{\"type\":\"array\",\"maxItems\":64,\"items\":{\"$ref\":\"#/$defs/Version\"},\"description\":\"The versions actually removed. Empty when none named was held.\"},\"stalePins\":{\"type\":\"array\",\"maxItems\":256,\"description\":\"Profiles that pinned a purged version and now present that entry as stale — which is to say, disclose nothing for it until the holder repins or edits them. Named because a purge that quietly shortened what three faces present is the surprise this family exists to prevent. Absent when none did.\",\"items\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"profileId\",\"pinVersion\"],\"properties\":{\"profileId\":{\"$ref\":\"#/$defs/Ulid\"},\"pinVersion\":{\"$ref\":\"#/$defs/Version\"}}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Version\":{\"title\":\"Version\",\"description\":\"A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.\",\"type\":\"integer\",\"minimum\":1},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
