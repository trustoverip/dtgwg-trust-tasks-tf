// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/contact/delete/1.0/payload.schema.json

package personacontactdeletev1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to persona/contact/delete. Type
// https://trusttasks.org/spec/persona/contact/delete/1.0#response. Deleting an absent
// contact is a success carrying existed: false, which is what makes a repeated delete
// converge.
type Response struct {
	ContactID Ulid `json:"contactId"`
	Existed   bool `json:"existed"`

	// How many revisions went with it, so a holder deleting a long history is told the size
	// of what they removed.
	RevisionsRemoved *int64 `json:"revisionsRemoved,omitempty"`

	// Revisions NOT removed because a disclosure record still references them. A maintainer
	// that keeps such revisions MUST report the count rather than let the holder believe the
	// deletion was total — an incomplete erasure the holder thinks is complete is worse than
	// one they know about.
	RetainedForDisclosure *int64 `json:"retainedForDisclosure,omitempty"`
	Ext                   *Ext   `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Ulid A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan
// of the store is also creation-ordered and a `list` needs no secondary sort.
// Server-assigned on create; a producer MAY supply one to make a create idempotent, and a
// maintainer MUST reject a supplied value that already exists rather than silently
// overwriting.
type Ulid = string

// Payload Remove a contact and every revision of it.
type Payload struct {
	ContextID string `json:"contextId"`
	ContactID Ulid   `json:"contactId"`
	Ext       *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/persona/contact/delete/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/persona/contact/delete/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/persona/contact/delete/1.0\",\"title\":\"Persona Contact Delete — payload\",\"description\":\"Remove a contact and every revision of it.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"contactId\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"contactId\":{\"$ref\":\"#/$defs/Ulid\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Contact Delete — response payload\",\"description\":\"Success response to persona/contact/delete. Type https://trusttasks.org/spec/persona/contact/delete/1.0#response. Deleting an absent contact is a success carrying existed: false, which is what makes a repeated delete converge.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contactId\",\"existed\"],\"properties\":{\"contactId\":{\"$ref\":\"#/$defs/Ulid\"},\"existed\":{\"type\":\"boolean\"},\"revisionsRemoved\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many revisions went with it, so a holder deleting a long history is told the size of what they removed.\"},\"retainedForDisclosure\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"Revisions NOT removed because a disclosure record still references them. A maintainer that keeps such revisions MUST report the count rather than let the holder believe the deletion was total — an incomplete erasure the holder thinks is complete is worse than one they know about.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Contact Delete — response payload\",\"description\":\"Success response to persona/contact/delete. Type https://trusttasks.org/spec/persona/contact/delete/1.0#response. Deleting an absent contact is a success carrying existed: false, which is what makes a repeated delete converge.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contactId\",\"existed\"],\"properties\":{\"contactId\":{\"$ref\":\"#/$defs/Ulid\"},\"existed\":{\"type\":\"boolean\"},\"revisionsRemoved\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many revisions went with it, so a holder deleting a long history is told the size of what they removed.\"},\"retainedForDisclosure\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"Revisions NOT removed because a disclosure record still references them. A maintainer that keeps such revisions MUST report the count rather than let the holder believe the deletion was total — an incomplete erasure the holder thinks is complete is worse than one they know about.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
