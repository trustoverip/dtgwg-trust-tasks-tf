// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/binding/set/1.0/payload.schema.json

package personabindingsetv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseCorrelationSeverity ResponseCorrelationSeverity is a closed set of string
// values.
type ResponseCorrelationSeverity string

// Values ResponseCorrelationSeverity may take, per this specification's schema.
const (
	ResponseCorrelationSeverityNone ResponseCorrelationSeverity = "none"
	ResponseCorrelationSeverityLow  ResponseCorrelationSeverity = "low"
	ResponseCorrelationSeverityHigh ResponseCorrelationSeverity = "high"
)

// ResponseCorrelation Advisory. Correlation is scored HERE as well as at composition,
// because composing is hypothetical and binding is when a value actually crosses into a
// context. Binding one profile to a second persona is reported at `high` unconditionally:
// that act makes the two personas the same person by construction.
type ResponseCorrelation struct {
	Severity ResponseCorrelationSeverity `json:"severity"`

	// How many other personas are bound to this same profile. A count, not identifiers.
	AlsoBoundPersonaCount *int64 `json:"alsoBoundPersonaCount,omitempty"`
}

// Response Success response to persona/binding/set. Type
// https://trusttasks.org/spec/persona/binding/set/1.0#response.
type Response struct {
	ContextID  string          `json:"contextId"`
	PersonaDID string          `json:"personaDid"`
	ProfileID  json.RawMessage `json:"profileId,omitempty"`
	Version    Version         `json:"version"`

	// How many claims were materialised into the context by this binding. A count, so the
	// holder can see that a push happened and how large it was without the response restating
	// the values.
	MaterialisedClaimCount *int64 `json:"materialisedClaimCount,omitempty"`

	// Advisory. Correlation is scored HERE as well as at composition, because composing is
	// hypothetical and binding is when a value actually crosses into a context. Binding one
	// profile to a second persona is reported at `high` unconditionally: that act makes the
	// two personas the same person by construction.
	Correlation *ResponseCorrelation `json:"correlation,omitempty"`
	BoundAt     string               `json:"boundAt"`
	Ext         *Ext                 `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Version A value of the store's monotonic write counter. Server-assigned; a producer
// never chooses one.
type Version = int64

// Ulid A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan
// of the store is also creation-ordered and a `list` needs no secondary sort.
// Server-assigned on create; a producer MAY supply one to make a create idempotent, and a
// maintainer MUST reject a supplied value that already exists rather than silently
// overwriting.
type Ulid = string

// ExpectedVersion Optimistic-concurrency precondition. A positive value requires the
// record's current `version` to equal it exactly; zero means create-only and applies only
// when no live record exists at the address.
type ExpectedVersion = int64

// Payload Assign a profile to a persona DID within a context, or clear the assignment.
// This is the step that pushes a composition across the context boundary: the maintainer
// materialises the profile into the context, and a context never reaches back the other
// way.
type Payload struct {
	// The context the persona lives in. The binding is context-scoped even though the profile
	// it names is not.
	ContextID string `json:"contextId"`

	// The persona being bound.
	PersonaDID string `json:"personaDid"`

	// The profile to bind, or null to clear. Null is a first-class value and not an omission:
	// a persona with no profile is a legitimate, common state — a throwaway identity that
	// presents nothing — and the schema says so rather than leaving a consumer to infer it
	// from an absent member.
	ProfileID json.RawMessage `json:"profileId,omitempty"`

	// Attributes the holder opts into publishing on the persona's own DID document or
	// equivalent public surface. Empty by default and MUST remain empty unless explicitly
	// set: everything else is a per-verifier projection, and a published value is one
	// document every relying party sees identically — a permanent correlation point the rest
	// of this family exists to avoid.
	PublicEntries *[]Ulid `json:"publicEntries,omitempty"`

	// What this context may call the face the persona wears here, chosen by the holder for
	// this context. Returned by persona/binding/get and persona/binding/list in place of the
	// holder's own name for the face, which is theirs and may say far more than they would
	// tell a context ('the divorce'). Omit to give the context no name at all.
	Label           *string          `json:"label,omitempty"`
	ExpectedVersion *ExpectedVersion `json:"expectedVersion,omitempty"`
	Ext             *Ext             `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/persona/binding/set/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/persona/binding/set/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/persona/binding/set/1.0\",\"title\":\"Persona Binding Set — payload\",\"description\":\"Assign a profile to a persona DID within a context, or clear the assignment. This is the step that pushes a composition across the context boundary: the maintainer materialises the profile into the context, and a context never reaches back the other way.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"personaDid\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The context the persona lives in. The binding is context-scoped even though the profile it names is not.\"},\"personaDid\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":2048,\"description\":\"The persona being bound.\"},\"profileId\":{\"oneOf\":[{\"$ref\":\"#/$defs/Ulid\"},{\"type\":\"null\"}],\"description\":\"The profile to bind, or null to clear. Null is a first-class value and not an omission: a persona with no profile is a legitimate, common state — a throwaway identity that presents nothing — and the schema says so rather than leaving a consumer to infer it from an absent member.\"},\"publicEntries\":{\"type\":\"array\",\"maxItems\":32,\"items\":{\"$ref\":\"#/$defs/Ulid\"},\"description\":\"Attributes the holder opts into publishing on the persona's own DID document or equivalent public surface. Empty by default and MUST remain empty unless explicitly set: everything else is a per-verifier projection, and a published value is one document every relying party sees identically — a permanent correlation point the rest of this family exists to avoid.\"},\"label\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"What this context may call the face the persona wears here, chosen by the holder for this context. Returned by persona/binding/get and persona/binding/list in place of the holder's own name for the face, which is theirs and may say far more than they would tell a context ('the divorce'). Omit to give the context no name at all.\"},\"expectedVersion\":{\"$ref\":\"#/$defs/ExpectedVersion\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Binding Set — response payload\",\"description\":\"Success response to persona/binding/set. Type https://trusttasks.org/spec/persona/binding/set/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"personaDid\",\"version\",\"boundAt\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"personaDid\":{\"type\":\"string\",\"minLength\":1},\"profileId\":{\"oneOf\":[{\"$ref\":\"#/$defs/Ulid\"},{\"type\":\"null\"}]},\"version\":{\"$ref\":\"#/$defs/Version\"},\"materialisedClaimCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many claims were materialised into the context by this binding. A count, so the holder can see that a push happened and how large it was without the response restating the values.\"},\"correlation\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"severity\"],\"description\":\"Advisory. Correlation is scored HERE as well as at composition, because composing is hypothetical and binding is when a value actually crosses into a context. Binding one profile to a second persona is reported at `high` unconditionally: that act makes the two personas the same person by construction.\",\"properties\":{\"severity\":{\"type\":\"string\",\"enum\":[\"none\",\"low\",\"high\"]},\"alsoBoundPersonaCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many other personas are bound to this same profile. A count, not identifiers.\"}}},\"boundAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Version\":{\"title\":\"Version\",\"description\":\"A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.\",\"type\":\"integer\",\"minimum\":1},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"},\"ExpectedVersion\":{\"title\":\"ExpectedVersion\",\"description\":\"Optimistic-concurrency precondition. A positive value requires the record's current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.\",\"type\":\"integer\",\"minimum\":0}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Binding Set — response payload\",\"description\":\"Success response to persona/binding/set. Type https://trusttasks.org/spec/persona/binding/set/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"personaDid\",\"version\",\"boundAt\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"personaDid\":{\"type\":\"string\",\"minLength\":1},\"profileId\":{\"oneOf\":[{\"$ref\":\"#/$defs/Ulid\"},{\"type\":\"null\"}]},\"version\":{\"$ref\":\"#/$defs/Version\"},\"materialisedClaimCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many claims were materialised into the context by this binding. A count, so the holder can see that a push happened and how large it was without the response restating the values.\"},\"correlation\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"severity\"],\"description\":\"Advisory. Correlation is scored HERE as well as at composition, because composing is hypothetical and binding is when a value actually crosses into a context. Binding one profile to a second persona is reported at `high` unconditionally: that act makes the two personas the same person by construction.\",\"properties\":{\"severity\":{\"type\":\"string\",\"enum\":[\"none\",\"low\",\"high\"]},\"alsoBoundPersonaCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many other personas are bound to this same profile. A count, not identifiers.\"}}},\"boundAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Version\":{\"title\":\"Version\",\"description\":\"A value of the store's monotonic write counter. Server-assigned; a producer never chooses one.\",\"type\":\"integer\",\"minimum\":1},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"},\"ExpectedVersion\":{\"title\":\"ExpectedVersion\",\"description\":\"Optimistic-concurrency precondition. A positive value requires the record's current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.\",\"type\":\"integer\",\"minimum\":0}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
