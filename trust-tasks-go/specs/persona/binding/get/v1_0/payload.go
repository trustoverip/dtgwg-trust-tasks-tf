// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/binding/get/1.0/payload.schema.json

package personabindinggetv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to persona/binding/get. Type
// https://trusttasks.org/spec/persona/binding/get/1.0#response. This task is
// context-callable, which is why the response is deliberately thin: an application in the
// context learns THAT a profile is bound and what the holder calls it, and never what it
// contains. Contents reach an application only through the disclosure path, where the
// holder sees a preview first.
type Response struct {
	ContextID  string `json:"contextId"`
	PersonaDID string `json:"personaDid"`

	// Whether a profile is bound. False is a normal state, not an error — a persona need not
	// have a profile.
	Bound bool `json:"bound"`

	// Present when bound. An identifier only; a context-scoped caller cannot resolve it,
	// because profile reads are holder-authorized.
	ProfileID *Ulid `json:"profileId,omitempty"`

	// The holder's label for the bound profile, so an application can show which identity is
	// in use. Names a composition; reveals none of it.
	ProfileName *string `json:"profileName,omitempty"`

	// How many claims the materialised projection holds. Lets an application render "12
	// details available" without obtaining any of them.
	ClaimCount *int64  `json:"claimCount,omitempty"`
	BoundAt    *string `json:"boundAt,omitempty"`
	Ext        *Ext    `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Ulid A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan
// of the store is also creation-ordered and a `list` needs no secondary sort.
// Server-assigned on create; a producer MAY supply one to make a create idempotent, and a
// maintainer MUST reject a supplied value that already exists rather than silently
// overwriting.
type Ulid = string

// Payload Read the binding for one persona in a context: whether a profile is bound, and
// its label — never its contents.
type Payload struct {
	ContextID  string `json:"contextId"`
	PersonaDID string `json:"personaDid"`
	Ext        *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/persona/binding/get/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/persona/binding/get/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/persona/binding/get/1.0\",\"title\":\"Persona Binding Get — payload\",\"description\":\"Read the binding for one persona in a context: whether a profile is bound, and its label — never its contents.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"personaDid\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"personaDid\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":2048},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Binding Get — response payload\",\"description\":\"Success response to persona/binding/get. Type https://trusttasks.org/spec/persona/binding/get/1.0#response. This task is context-callable, which is why the response is deliberately thin: an application in the context learns THAT a profile is bound and what the holder calls it, and never what it contains. Contents reach an application only through the disclosure path, where the holder sees a preview first.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"personaDid\",\"bound\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"personaDid\":{\"type\":\"string\",\"minLength\":1},\"bound\":{\"type\":\"boolean\",\"description\":\"Whether a profile is bound. False is a normal state, not an error — a persona need not have a profile.\"},\"profileId\":{\"$ref\":\"#/$defs/Ulid\",\"description\":\"Present when bound. An identifier only; a context-scoped caller cannot resolve it, because profile reads are holder-authorized.\"},\"profileName\":{\"type\":\"string\",\"maxLength\":128,\"description\":\"The holder's label for the bound profile, so an application can show which identity is in use. Names a composition; reveals none of it.\"},\"claimCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many claims the materialised projection holds. Lets an application render \\\"12 details available\\\" without obtaining any of them.\"},\"boundAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Binding Get — response payload\",\"description\":\"Success response to persona/binding/get. Type https://trusttasks.org/spec/persona/binding/get/1.0#response. This task is context-callable, which is why the response is deliberately thin: an application in the context learns THAT a profile is bound and what the holder calls it, and never what it contains. Contents reach an application only through the disclosure path, where the holder sees a preview first.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"personaDid\",\"bound\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"personaDid\":{\"type\":\"string\",\"minLength\":1},\"bound\":{\"type\":\"boolean\",\"description\":\"Whether a profile is bound. False is a normal state, not an error — a persona need not have a profile.\"},\"profileId\":{\"$ref\":\"#/$defs/Ulid\",\"description\":\"Present when bound. An identifier only; a context-scoped caller cannot resolve it, because profile reads are holder-authorized.\"},\"profileName\":{\"type\":\"string\",\"maxLength\":128,\"description\":\"The holder's label for the bound profile, so an application can show which identity is in use. Names a composition; reveals none of it.\"},\"claimCount\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many claims the materialised projection holds. Lets an application render \\\"12 details available\\\" without obtaining any of them.\"},\"boundAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
