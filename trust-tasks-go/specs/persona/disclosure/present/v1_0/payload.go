// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/disclosure/present/1.0/payload.schema.json

package personadisclosurepresentv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to persona/disclosure/present. Type
// https://trusttasks.org/spec/persona/disclosure/present/1.0#response.
type Response struct {
	// Names the append-only record this disclosure wrote, so the holder can find it again in
	// their history.
	DisclosureID Ulid `json:"disclosureId"`

	// The signed disclosure, in the renderer's format. Opaque to this task: the maintainer
	// signs it, the producer relays it, and neither the schema nor the maintainer
	// reinterprets it once formed.
	Artifact string `json:"artifact"`

	// The identifier the artifact was issued under — pairwise by default, so this disclosure
	// cannot be joined to another by subject alone.
	Subject string `json:"subject"`

	// Present only when a durable credential was minted. It is in the holder's own vault,
	// appears in their credential list, and can be revoked — which is the obligation they
	// took on by asking for it.
	CredentialID *string `json:"credentialId,omitempty"`
	DisclosedAt  string  `json:"disclosedAt"`
	Ext          *Ext    `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Ulid A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan
// of the store is also creation-ordered and a `list` needs no secondary sort.
// Server-assigned on create; a producer MAY supply one to make a create idempotent, and a
// maintainer MUST reject a supplied value that already exists rather than silently
// overwriting.
type Ulid = string

// PayloadMint Optionally mint a durable, revocable self-issued credential instead of an
// ephemeral artifact. Off by default: a presented disclosure is read once and revoking it
// afterwards is meaningless, so status machinery would be maintenance with no benefit. A
// holder who wants a counterparty to be able to re-verify — and themselves to be able to
// revoke — asks for it here and takes on a credential they must now maintain.
type PayloadMint struct {
	Durable    *bool   `json:"durable,omitempty"`
	ValidUntil *string `json:"validUntil,omitempty"`
}

// Payload Produce the signed disclosure a preview described. Consumes a previewId, which
// is what makes the two calls uncollapsible.
type Payload struct {
	ContextID string `json:"contextId"`

	// From a prior preview. Single-use: presenting consumes it. A producer that wants to
	// disclose twice previews twice, which is correct — the second disclosure is a second
	// decision.
	PreviewID Ulid `json:"previewId"`

	// The verifier's nonce, bound into the proof for freshness so the artifact cannot be
	// replayed to another verifier or at another time.
	Challenge *string `json:"challenge,omitempty"`

	// Optionally mint a durable, revocable self-issued credential instead of an ephemeral
	// artifact. Off by default: a presented disclosure is read once and revoking it
	// afterwards is meaningless, so status machinery would be maintenance with no benefit. A
	// holder who wants a counterparty to be able to re-verify — and themselves to be able to
	// revoke — asks for it here and takes on a credential they must now maintain.
	Mint *PayloadMint `json:"mint,omitempty"`
	Ext  *Ext         `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/persona/disclosure/present/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/persona/disclosure/present/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/persona/disclosure/present/1.0\",\"title\":\"Persona Disclosure Present — payload\",\"description\":\"Produce the signed disclosure a preview described. Consumes a previewId, which is what makes the two calls uncollapsible.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"contextId\",\"previewId\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1},\"previewId\":{\"$ref\":\"#/$defs/Ulid\",\"description\":\"From a prior preview. Single-use: presenting consumes it. A producer that wants to disclose twice previews twice, which is correct — the second disclosure is a second decision.\"},\"challenge\":{\"type\":\"string\",\"maxLength\":512,\"description\":\"The verifier's nonce, bound into the proof for freshness so the artifact cannot be replayed to another verifier or at another time.\"},\"mint\":{\"type\":\"object\",\"additionalProperties\":false,\"description\":\"Optionally mint a durable, revocable self-issued credential instead of an ephemeral artifact. Off by default: a presented disclosure is read once and revoking it afterwards is meaningless, so status machinery would be maintenance with no benefit. A holder who wants a counterparty to be able to re-verify — and themselves to be able to revoke — asks for it here and takes on a credential they must now maintain.\",\"properties\":{\"durable\":{\"type\":\"boolean\",\"default\":false},\"validUntil\":{\"type\":\"string\",\"format\":\"date-time\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Disclosure Present — response payload\",\"description\":\"Success response to persona/disclosure/present. Type https://trusttasks.org/spec/persona/disclosure/present/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"disclosureId\",\"artifact\",\"subject\",\"disclosedAt\"],\"properties\":{\"disclosureId\":{\"$ref\":\"#/$defs/Ulid\",\"description\":\"Names the append-only record this disclosure wrote, so the holder can find it again in their history.\"},\"artifact\":{\"type\":\"string\",\"description\":\"The signed disclosure, in the renderer's format. Opaque to this task: the maintainer signs it, the producer relays it, and neither the schema nor the maintainer reinterprets it once formed.\"},\"subject\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The identifier the artifact was issued under — pairwise by default, so this disclosure cannot be joined to another by subject alone.\"},\"credentialId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Present only when a durable credential was minted. It is in the holder's own vault, appears in their credential list, and can be revoked — which is the obligation they took on by asking for it.\"},\"disclosedAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Persona Disclosure Present — response payload\",\"description\":\"Success response to persona/disclosure/present. Type https://trusttasks.org/spec/persona/disclosure/present/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"disclosureId\",\"artifact\",\"subject\",\"disclosedAt\"],\"properties\":{\"disclosureId\":{\"$ref\":\"#/$defs/Ulid\",\"description\":\"Names the append-only record this disclosure wrote, so the holder can find it again in their history.\"},\"artifact\":{\"type\":\"string\",\"description\":\"The signed disclosure, in the renderer's format. Opaque to this task: the maintainer signs it, the producer relays it, and neither the schema nor the maintainer reinterprets it once formed.\"},\"subject\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The identifier the artifact was issued under — pairwise by default, so this disclosure cannot be joined to another by subject alone.\"},\"credentialId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Present only when a durable credential was minted. It is in the holder's own vault, appears in their credential list, and can be revoked — which is the obligation they took on by asking for it.\"},\"disclosedAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Ulid\":{\"title\":\"Ulid\",\"description\":\"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.\",\"type\":\"string\",\"pattern\":\"^[0-9A-HJKMNP-TV-Z]{26}$\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
