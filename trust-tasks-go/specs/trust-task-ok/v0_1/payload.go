// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/trust-task-ok/0.1/payload.schema.json

package trusttaskokv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// PayloadRefsItem PayloadRefsItem is a generated payload type.
type PayloadRefsItem struct {
	// What this reference is, in the consumer's own vocabulary. Not drawn from any registry,
	// and not interoperable — a producer that does not recognize a name ignores it.
	Name string `json:"name"`

	// The reference itself, opaque to this framework.
	Value string `json:"value"`
}

// Payload The courtesy acknowledgement reserved at SPEC.md §8.6: a consumer confirming
// that it received and performed a task which defines no success-response document of its
// own. It is deliberately weak. A producer MUST NOT rely on receiving one, and the
// absence of one carries no information — a consumer may not implement this
// specification, and the document may be lost. Anything a task's contract depends on
// belongs in that task's own #response, not here. Every member is optional: an empty
// payload is the ordinary case, and means exactly 'received and performed'. This
// specification declares no response anchor. An acknowledgement is not answered.
type Payload struct {
	// Human-readable confirmation, for operator UI and logs. Non-normative. A producer MUST
	// NOT parse this for any value it needs, and MUST NOT condition behaviour on its presence
	// or content.
	Message *string `json:"message,omitempty"`

	// Opaque references the consumer chose to surface — a ticket number, a queue position, a
	// processing handle, a retention deadline. Convenience only. A consumer MUST NOT convey
	// through `refs` anything the task's own contract depends on: a value a producer needs in
	// order to proceed is part of that task's semantics and belongs in a response the task
	// itself defines. A producer that finds itself parsing `refs` to continue an exchange is
	// using the wrong document, and the task it is performing should declare its own
	// #response.
	Refs *[]PayloadRefsItem `json:"refs,omitempty"`

	// Vendor-namespaced extension data per SPEC.md §4.5.1. Every immediate child key MUST be
	// a reverse-DNS prefix the producer of this document controls.
	Ext *map[string]json.RawMessage `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/trust-task-ok/0.1"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/trust-task-ok/0.1\",\"title\":\"Trust Task OK — payload\",\"description\":\"The courtesy acknowledgement reserved at SPEC.md §8.6: a consumer confirming that it received and performed a task which defines no success-response document of its own.\\n\\nIt is deliberately weak. A producer MUST NOT rely on receiving one, and the absence of one carries no information — a consumer may not implement this specification, and the document may be lost. Anything a task's contract depends on belongs in that task's own #response, not here.\\n\\nEvery member is optional: an empty payload is the ordinary case, and means exactly 'received and performed'.\\n\\nThis specification declares no response anchor. An acknowledgement is not answered.\",\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"message\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Human-readable confirmation, for operator UI and logs. Non-normative. A producer MUST NOT parse this for any value it needs, and MUST NOT condition behaviour on its presence or content.\"},\"refs\":{\"type\":\"array\",\"description\":\"Opaque references the consumer chose to surface — a ticket number, a queue position, a processing handle, a retention deadline. Convenience only.\\n\\nA consumer MUST NOT convey through `refs` anything the task's own contract depends on: a value a producer needs in order to proceed is part of that task's semantics and belongs in a response the task itself defines. A producer that finds itself parsing `refs` to continue an exchange is using the wrong document, and the task it is performing should declare its own #response.\",\"items\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"name\",\"value\"],\"properties\":{\"name\":{\"type\":\"string\",\"minLength\":1,\"description\":\"What this reference is, in the consumer's own vocabulary. Not drawn from any registry, and not interoperable — a producer that does not recognize a name ignores it.\"},\"value\":{\"type\":\"string\",\"description\":\"The reference itself, opaque to this framework.\"}}}},\"ext\":{\"type\":\"object\",\"description\":\"Vendor-namespaced extension data per SPEC.md §4.5.1. Every immediate child key MUST be a reverse-DNS prefix the producer of this document controls.\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}
