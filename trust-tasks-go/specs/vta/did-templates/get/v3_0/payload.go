// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/did-templates/get/3.0/payload.schema.json

package vtadidtemplatesgetv3_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The persisted DidTemplateRecord. The DidTemplate fields are flattened at the
// top level alongside the resolved scope and provenance metadata. Same shape returned by
// create/update.
type Response = DIDTemplateRecord

// DIDTemplateRecordKeysValuePurpose What the key is for. Deliberately coarser than DID
// Core's five verification relationships: the template says what the key is, and the
// `document` body says where it appears. Encoding the relationship here as well would let
// the two disagree.
type DIDTemplateRecordKeysValuePurpose string

// Values DIDTemplateRecordKeysValuePurpose may take, per this specification's schema.
const (
	DIDTemplateRecordKeysValuePurposeSigning      DIDTemplateRecordKeysValuePurpose = "signing"
	DIDTemplateRecordKeysValuePurposeKeyAgreement DIDTemplateRecordKeysValuePurpose = "keyAgreement"
)

// DIDTemplateRecordKeysValueAlgorithmsItem DIDTemplateRecordKeysValueAlgorithmsItem is a
// closed set of string values.
type DIDTemplateRecordKeysValueAlgorithmsItem string

// Values DIDTemplateRecordKeysValueAlgorithmsItem may take, per this specification's schema.
const (
	DIDTemplateRecordKeysValueAlgorithmsItemEd25519 DIDTemplateRecordKeysValueAlgorithmsItem = "ed25519"
	DIDTemplateRecordKeysValueAlgorithmsItemX25519  DIDTemplateRecordKeysValueAlgorithmsItem = "x25519"
	DIDTemplateRecordKeysValueAlgorithmsItemP256    DIDTemplateRecordKeysValueAlgorithmsItem = "p256"
	DIDTemplateRecordKeysValueAlgorithmsItemMldsa44 DIDTemplateRecordKeysValueAlgorithmsItem = "mldsa44"
	DIDTemplateRecordKeysValueAlgorithmsItemMldsa65 DIDTemplateRecordKeysValueAlgorithmsItem = "mldsa65"
)

// DIDTemplateRecordKeysValue DIDTemplateRecordKeysValue is a generated payload type.
type DIDTemplateRecordKeysValue struct {
	// What the key is for. Deliberately coarser than DID Core's five verification
	// relationships: the template says what the key is, and the `document` body says where it
	// appears. Encoding the relationship here as well would let the two disagree.
	Purpose DIDTemplateRecordKeysValuePurpose `json:"purpose"`

	// Acceptable algorithms, MOST PREFERRED FIRST. A list rather than a single value because
	// a fleet does not migrate atomically: `["mldsa44", "ed25519"]` means mint ML-DSA-44
	// where the implementation can and Ed25519 otherwise, so one template serves a VTA with
	// post-quantum support and one without. An implementation MUST NOT substitute an
	// algorithm absent from this list; running out of candidates is an error, never a silent
	// downgrade — a deployment intended to be post-quantum would otherwise ship classical
	// keys with nothing reporting it. `minItems: 1` for the same reason: an empty list would
	// express no preference and inherit whatever the implementation defaulted to.
	Algorithms []DIDTemplateRecordKeysValueAlgorithmsItem `json:"algorithms"`
}

// DIDTemplateRecord A persisted template. The DidTemplate fields are flattened at the top
// level alongside the resolved scope and provenance metadata. Same shape returned by
// get/create/update and carried per-item by list.
type DIDTemplateRecord struct {
	// Template schema version. 1 is the historical shape, whose keys are implicitly an
	// Ed25519 signing key and an X25519 key-agreement key. 2 adds the `keys` block, which
	// makes those explicit and allows others.
	SchemaVersion int64                       `json:"schemaVersion"`
	Name          string                      `json:"name"`
	Kind          string                      `json:"kind"`
	Description   *string                     `json:"description,omitempty"`
	Methods       *[]string                   `json:"methods,omitempty"`
	RequiredVars  *[]string                   `json:"requiredVars,omitempty"`
	OptionalVars  *map[string]json.RawMessage `json:"optionalVars,omitempty"`
	Defaults      *map[string]json.RawMessage `json:"defaults,omitempty"`

	// The keys this template needs, by slot name. REQUIRES `schemaVersion` 2. A slot's name
	// becomes a document placeholder by an explicit rule — uppercase, `-` to `_`, suffix
	// `_KEY_MB` — so slot `signing` is rendered into `{SIGNING_KEY_MB}` and slot `ka` into
	// `{KA_KEY_MB}`. Those are the two names a `schemaVersion` 1 template already uses, which
	// is deliberate: a v1 template is exactly a v2 template whose `keys` block is `{signing:
	// [ed25519], ka: [x25519]}`, so an implementation can read both through one path and
	// raising the version cannot change how a v1 template renders. An implementation MUST
	// refuse a template that declares a slot whose placeholder never appears in `document` —
	// the key would be minted and never published, so verifiers would continue to see only
	// the keys that are, while the operator believes the template migrated. It MUST likewise
	// refuse a slot placeholder in `document` that no slot declares, which would render as an
	// unsubstituted literal.
	Keys     *map[string]DIDTemplateRecordKeysValue `json:"keys,omitempty"`
	Document map[string]json.RawMessage             `json:"document"`

	// Resolved scope of the stored template.
	Scope Scope `json:"scope"`

	// UTC unix-epoch seconds the template was first stored.
	CreatedAt int64 `json:"createdAt"`

	// UTC unix-epoch seconds of the last write.
	UpdatedAt int64 `json:"updatedAt"`

	// DID of the admin who last wrote the template.
	CreatedBy string `json:"createdBy"`
}

// Scope Where a stored template lives. Tagged by `type`.
type Scope = map[string]json.RawMessage

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload Fetch one DID template on a VTA by name. Omit `contextId` to read from the
// global scope (any authenticated caller); set it to read a template scoped to that
// context (requires access to the context). The success response is the persisted
// DidTemplateRecord.
type Payload struct {
	// Scope selector. Absent: the global scope. Present: the context the template is scoped
	// to; the caller MUST have access to that context.
	ContextID *string `json:"contextId,omitempty"`

	// Template name to fetch within the selected scope.
	Name string `json:"name"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vta/did-templates/get/3.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vta/did-templates/get/3.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vta/did-templates/get/3.0\",\"title\":\"VTA DID-Template Get — payload\",\"description\":\"Fetch one DID template on a VTA by name. Omit `contextId` to read from the global scope (any authenticated caller); set it to read a template scoped to that context (requires access to the context). The success response is the persisted DidTemplateRecord.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"name\"],\"properties\":{\"contextId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Scope selector. Absent: the global scope. Present: the context the template is scoped to; the caller MUST have access to that context.\"},\"name\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Template name to fetch within the selected scope.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA DID-Template Get — response payload\",\"description\":\"The persisted DidTemplateRecord. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata. Same shape returned by create/update.\",\"$ref\":\"#/$defs/DidTemplateRecord\"},\"DidTemplateRecord\":{\"title\":\"DidTemplateRecord\",\"description\":\"A persisted template. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata. Same shape returned by get/create/update and carried per-item by list.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"schemaVersion\",\"name\",\"kind\",\"document\",\"scope\",\"createdAt\",\"updatedAt\",\"createdBy\"],\"properties\":{\"schemaVersion\":{\"type\":\"integer\",\"enum\":[1,2],\"description\":\"Template schema version. 1 is the historical shape, whose keys are implicitly an Ed25519 signing key and an X25519 key-agreement key. 2 adds the `keys` block, which makes those explicit and allows others.\"},\"name\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z0-9-]+$\"},\"kind\":{\"type\":\"string\",\"minLength\":1},\"description\":{\"type\":[\"string\",\"null\"],\"maxLength\":1024},\"methods\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}},\"requiredVars\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}},\"optionalVars\":{\"type\":\"object\",\"additionalProperties\":true},\"defaults\":{\"type\":\"object\",\"additionalProperties\":true},\"keys\":{\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"purpose\",\"algorithms\"],\"properties\":{\"purpose\":{\"type\":\"string\",\"enum\":[\"signing\",\"keyAgreement\"],\"description\":\"What the key is for. Deliberately coarser than DID Core's five verification relationships: the template says what the key is, and the `document` body says where it appears. Encoding the relationship here as well would let the two disagree.\"},\"algorithms\":{\"type\":\"array\",\"minItems\":1,\"items\":{\"type\":\"string\",\"enum\":[\"ed25519\",\"x25519\",\"p256\",\"mldsa44\",\"mldsa65\"]},\"description\":\"Acceptable algorithms, MOST PREFERRED FIRST. A list rather than a single value because a fleet does not migrate atomically: `[\\\"mldsa44\\\", \\\"ed25519\\\"]` means mint ML-DSA-44 where the implementation can and Ed25519 otherwise, so one template serves a VTA with post-quantum support and one without. An implementation MUST NOT substitute an algorithm absent from this list; running out of candidates is an error, never a silent downgrade — a deployment intended to be post-quantum would otherwise ship classical keys with nothing reporting it. `minItems: 1` for the same reason: an empty list would express no preference and inherit whatever the implementation defaulted to.\"}}},\"propertyNames\":{\"pattern\":\"^[a-z0-9-]+$\"},\"description\":\"The keys this template needs, by slot name. REQUIRES `schemaVersion` 2.\\n\\nA slot's name becomes a document placeholder by an explicit rule — uppercase, `-` to `_`, suffix `_KEY_MB` — so slot `signing` is rendered into `{SIGNING_KEY_MB}` and slot `ka` into `{KA_KEY_MB}`. Those are the two names a `schemaVersion` 1 template already uses, which is deliberate: a v1 template is exactly a v2 template whose `keys` block is `{signing: [ed25519], ka: [x25519]}`, so an implementation can read both through one path and raising the version cannot change how a v1 template renders.\\n\\nAn implementation MUST refuse a template that declares a slot whose placeholder never appears in `document` — the key would be minted and never published, so verifiers would continue to see only the keys that are, while the operator believes the template migrated. It MUST likewise refuse a slot placeholder in `document` that no slot declares, which would render as an unsubstituted literal.\"},\"document\":{\"type\":\"object\"},\"scope\":{\"$ref\":\"#/$defs/Scope\",\"description\":\"Resolved scope of the stored template.\"},\"createdAt\":{\"type\":\"integer\",\"description\":\"UTC unix-epoch seconds the template was first stored.\"},\"updatedAt\":{\"type\":\"integer\",\"description\":\"UTC unix-epoch seconds of the last write.\"},\"createdBy\":{\"type\":\"string\",\"description\":\"DID of the admin who last wrote the template.\"}}},\"Scope\":{\"title\":\"Scope\",\"description\":\"Where a stored template lives. Tagged by `type`.\",\"type\":\"object\",\"oneOf\":[{\"title\":\"Builtin\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\"],\"properties\":{\"type\":{\"const\":\"builtin\"}}},{\"title\":\"Global\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\"],\"properties\":{\"type\":{\"const\":\"global\"}}},{\"title\":\"Context\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\",\"contextId\"],\"properties\":{\"type\":{\"const\":\"context\"},\"contextId\":{\"type\":\"string\",\"minLength\":1}}}]},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA DID-Template Get — response payload\",\"description\":\"The persisted DidTemplateRecord. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata. Same shape returned by create/update.\",\"$ref\":\"#/$defs/DidTemplateRecord\"},\"DidTemplateRecord\":{\"title\":\"DidTemplateRecord\",\"description\":\"A persisted template. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata. Same shape returned by get/create/update and carried per-item by list.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"schemaVersion\",\"name\",\"kind\",\"document\",\"scope\",\"createdAt\",\"updatedAt\",\"createdBy\"],\"properties\":{\"schemaVersion\":{\"type\":\"integer\",\"enum\":[1,2],\"description\":\"Template schema version. 1 is the historical shape, whose keys are implicitly an Ed25519 signing key and an X25519 key-agreement key. 2 adds the `keys` block, which makes those explicit and allows others.\"},\"name\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z0-9-]+$\"},\"kind\":{\"type\":\"string\",\"minLength\":1},\"description\":{\"type\":[\"string\",\"null\"],\"maxLength\":1024},\"methods\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}},\"requiredVars\":{\"type\":\"array\",\"items\":{\"type\":\"string\"}},\"optionalVars\":{\"type\":\"object\",\"additionalProperties\":true},\"defaults\":{\"type\":\"object\",\"additionalProperties\":true},\"keys\":{\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"purpose\",\"algorithms\"],\"properties\":{\"purpose\":{\"type\":\"string\",\"enum\":[\"signing\",\"keyAgreement\"],\"description\":\"What the key is for. Deliberately coarser than DID Core's five verification relationships: the template says what the key is, and the `document` body says where it appears. Encoding the relationship here as well would let the two disagree.\"},\"algorithms\":{\"type\":\"array\",\"minItems\":1,\"items\":{\"type\":\"string\",\"enum\":[\"ed25519\",\"x25519\",\"p256\",\"mldsa44\",\"mldsa65\"]},\"description\":\"Acceptable algorithms, MOST PREFERRED FIRST. A list rather than a single value because a fleet does not migrate atomically: `[\\\"mldsa44\\\", \\\"ed25519\\\"]` means mint ML-DSA-44 where the implementation can and Ed25519 otherwise, so one template serves a VTA with post-quantum support and one without. An implementation MUST NOT substitute an algorithm absent from this list; running out of candidates is an error, never a silent downgrade — a deployment intended to be post-quantum would otherwise ship classical keys with nothing reporting it. `minItems: 1` for the same reason: an empty list would express no preference and inherit whatever the implementation defaulted to.\"}}},\"propertyNames\":{\"pattern\":\"^[a-z0-9-]+$\"},\"description\":\"The keys this template needs, by slot name. REQUIRES `schemaVersion` 2.\\n\\nA slot's name becomes a document placeholder by an explicit rule — uppercase, `-` to `_`, suffix `_KEY_MB` — so slot `signing` is rendered into `{SIGNING_KEY_MB}` and slot `ka` into `{KA_KEY_MB}`. Those are the two names a `schemaVersion` 1 template already uses, which is deliberate: a v1 template is exactly a v2 template whose `keys` block is `{signing: [ed25519], ka: [x25519]}`, so an implementation can read both through one path and raising the version cannot change how a v1 template renders.\\n\\nAn implementation MUST refuse a template that declares a slot whose placeholder never appears in `document` — the key would be minted and never published, so verifiers would continue to see only the keys that are, while the operator believes the template migrated. It MUST likewise refuse a slot placeholder in `document` that no slot declares, which would render as an unsubstituted literal.\"},\"document\":{\"type\":\"object\"},\"scope\":{\"$ref\":\"#/$defs/Scope\",\"description\":\"Resolved scope of the stored template.\"},\"createdAt\":{\"type\":\"integer\",\"description\":\"UTC unix-epoch seconds the template was first stored.\"},\"updatedAt\":{\"type\":\"integer\",\"description\":\"UTC unix-epoch seconds of the last write.\"},\"createdBy\":{\"type\":\"string\",\"description\":\"DID of the admin who last wrote the template.\"}}},\"Scope\":{\"title\":\"Scope\",\"description\":\"Where a stored template lives. Tagged by `type`.\",\"type\":\"object\",\"oneOf\":[{\"title\":\"Builtin\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\"],\"properties\":{\"type\":{\"const\":\"builtin\"}}},{\"title\":\"Global\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\"],\"properties\":{\"type\":{\"const\":\"global\"}}},{\"title\":\"Context\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"type\",\"contextId\"],\"properties\":{\"type\":{\"const\":\"context\"},\"contextId\":{\"type\":\"string\",\"minLength\":1}}}]},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
