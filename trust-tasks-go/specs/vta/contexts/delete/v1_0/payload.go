// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/contexts/delete/1.0/payload.schema.json

package vtacontextsdeletev1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to vta/contexts/delete. Type
// https://trusttasks.org/spec/vta/contexts/delete/1.0#response.
type Response struct {
	ID string `json:"id"`

	// Whether the context was removed. A successful response carrying `false` means the VTA
	// declined to act — it is not an error, and a consumer MUST NOT report the deletion as
	// done on the basis of the response status alone.
	Deleted bool `json:"deleted"`

	// One entry per `did:webvh` DID in the deleted subtree whose local record was removed
	// while its hosting server did not confirm removal of the published log. **Those DIDs may
	// still resolve.** This is a partial success reported as a success — the same condition
	// `vta/webvh/dids/delete/1.0` reports as `daemonCleanupError` for a single DID — and a
	// consumer MUST surface it rather than treating the deletion as complete. Absent or empty
	// means every host copy was confirmed gone.
	DaemonCleanupErrors *[]string `json:"daemonCleanupErrors,omitempty"`
	Ext                 *Ext      `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload VTA Contexts Delete — payload
type Payload struct {
	// Context to delete.
	ID string `json:"id"`

	// Delete even though the context still holds keys, DIDs or templates. Default false, and
	// the default is the point: without it a context holding anything is refused, so the
	// destructive case is always something a caller asked for explicitly.
	Force *bool `json:"force,omitempty"`
	Ext   *Ext  `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vta/contexts/delete/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vta/contexts/delete/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vta/contexts/delete/1.0\",\"title\":\"VTA Contexts Delete — payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"id\"],\"properties\":{\"id\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Context to delete.\"},\"force\":{\"type\":\"boolean\",\"default\":false,\"description\":\"Delete even though the context still holds keys, DIDs or templates. Default false, and the default is the point: without it a context holding anything is refused, so the destructive case is always something a caller asked for explicitly.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Contexts Delete — response payload\",\"description\":\"Success response to vta/contexts/delete. Type https://trusttasks.org/spec/vta/contexts/delete/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"id\",\"deleted\"],\"properties\":{\"id\":{\"type\":\"string\"},\"deleted\":{\"type\":\"boolean\",\"description\":\"Whether the context was removed. A successful response carrying `false` means the VTA declined to act — it is not an error, and a consumer MUST NOT report the deletion as done on the basis of the response status alone.\"},\"daemonCleanupErrors\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"One entry per `did:webvh` DID in the deleted subtree whose local record was removed while its hosting server did not confirm removal of the published log. **Those DIDs may still resolve.** This is a partial success reported as a success — the same condition `vta/webvh/dids/delete/1.0` reports as `daemonCleanupError` for a single DID — and a consumer MUST surface it rather than treating the deletion as complete. Absent or empty means every host copy was confirmed gone.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Contexts Delete — response payload\",\"description\":\"Success response to vta/contexts/delete. Type https://trusttasks.org/spec/vta/contexts/delete/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"id\",\"deleted\"],\"properties\":{\"id\":{\"type\":\"string\"},\"deleted\":{\"type\":\"boolean\",\"description\":\"Whether the context was removed. A successful response carrying `false` means the VTA declined to act — it is not an error, and a consumer MUST NOT report the deletion as done on the basis of the response status alone.\"},\"daemonCleanupErrors\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"One entry per `did:webvh` DID in the deleted subtree whose local record was removed while its hosting server did not confirm removal of the published log. **Those DIDs may still resolve.** This is a partial success reported as a success — the same condition `vta/webvh/dids/delete/1.0` reports as `daemonCleanupError` for a single DID — and a consumer MUST surface it rather than treating the deletion as complete. Absent or empty means every host copy was confirmed gone.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
