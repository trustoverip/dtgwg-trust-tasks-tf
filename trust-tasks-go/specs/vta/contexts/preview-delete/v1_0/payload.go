// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/contexts/preview-delete/1.0/payload.schema.json

package vtacontextspreviewdeletev1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to vta/contexts/preview-delete. Every array is what WOULD be
// affected; nothing has been. Type
// https://trusttasks.org/spec/vta/contexts/preview-delete/1.0#response.
type Response struct {
	// The context previewed.
	ID string `json:"id"`

	// Sub-contexts that would be deleted along with this one, as full paths, deepest first.
	// Deleting a context deletes its whole subtree, so every other array in this response
	// covers the resources of these contexts as well as of the context itself — a consumer
	// rendering the preview for a human MUST show this list, because a caller told only what
	// the named context holds cannot see how much of the tree the deletion reaches. Absent or
	// empty means the context is a leaf.
	SubContexts *[]string `json:"subContexts,omitempty"`

	// Key ids that would be destroyed. A derived key can be re-derived from the seed; an
	// internally-generated one cannot be recovered by any means, and this list does not
	// distinguish them — a consumer rendering it for a human SHOULD resolve each key before
	// presenting the choice.
	Keys []string `json:"keys"`

	// did:webvh DIDs that would stop being served. Anything that recorded one as an issuer or
	// subject is left pointing at nothing.
	WebvhDids []string `json:"webvhDids"`

	// Subjects whose ACL entry would be removed outright, because this context was its only
	// scope.
	ACLEntriesRemoved []string `json:"aclEntriesRemoved"`

	// Subjects whose ACL entry would be narrowed — they hold other scopes and keep them.
	ACLEntriesUpdated []string `json:"aclEntriesUpdated"`

	// DID templates scoped to this context that would be removed.
	DIDTemplates *[]string `json:"didTemplates,omitempty"`
	Ext          *Ext      `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload Request payload for vta/contexts/preview-delete: what deleting this context
// would destroy, computed without destroying it.
type Payload struct {
	// Context to preview the deletion of.
	ID  string `json:"id"`
	Ext *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vta/contexts/preview-delete/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vta/contexts/preview-delete/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vta/contexts/preview-delete/1.0\",\"title\":\"VTA Contexts Preview-Delete — payload\",\"description\":\"Request payload for vta/contexts/preview-delete: what deleting this context would destroy, computed without destroying it.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"id\"],\"properties\":{\"id\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Context to preview the deletion of.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Contexts Preview-Delete — response payload\",\"description\":\"Success response to vta/contexts/preview-delete. Every array is what WOULD be affected; nothing has been. Type https://trusttasks.org/spec/vta/contexts/preview-delete/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"id\",\"keys\",\"webvhDids\",\"aclEntriesRemoved\",\"aclEntriesUpdated\"],\"properties\":{\"id\":{\"type\":\"string\",\"description\":\"The context previewed.\"},\"subContexts\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Sub-contexts that would be deleted along with this one, as full paths, deepest first. Deleting a context deletes its whole subtree, so every other array in this response covers the resources of these contexts as well as of the context itself — a consumer rendering the preview for a human MUST show this list, because a caller told only what the named context holds cannot see how much of the tree the deletion reaches. Absent or empty means the context is a leaf.\"},\"keys\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Key ids that would be destroyed. A derived key can be re-derived from the seed; an internally-generated one cannot be recovered by any means, and this list does not distinguish them — a consumer rendering it for a human SHOULD resolve each key before presenting the choice.\"},\"webvhDids\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"did:webvh DIDs that would stop being served. Anything that recorded one as an issuer or subject is left pointing at nothing.\"},\"aclEntriesRemoved\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Subjects whose ACL entry would be removed outright, because this context was its only scope.\"},\"aclEntriesUpdated\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Subjects whose ACL entry would be narrowed — they hold other scopes and keep them.\"},\"didTemplates\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"DID templates scoped to this context that would be removed.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Contexts Preview-Delete — response payload\",\"description\":\"Success response to vta/contexts/preview-delete. Every array is what WOULD be affected; nothing has been. Type https://trusttasks.org/spec/vta/contexts/preview-delete/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"id\",\"keys\",\"webvhDids\",\"aclEntriesRemoved\",\"aclEntriesUpdated\"],\"properties\":{\"id\":{\"type\":\"string\",\"description\":\"The context previewed.\"},\"subContexts\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Sub-contexts that would be deleted along with this one, as full paths, deepest first. Deleting a context deletes its whole subtree, so every other array in this response covers the resources of these contexts as well as of the context itself — a consumer rendering the preview for a human MUST show this list, because a caller told only what the named context holds cannot see how much of the tree the deletion reaches. Absent or empty means the context is a leaf.\"},\"keys\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Key ids that would be destroyed. A derived key can be re-derived from the seed; an internally-generated one cannot be recovered by any means, and this list does not distinguish them — a consumer rendering it for a human SHOULD resolve each key before presenting the choice.\"},\"webvhDids\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"did:webvh DIDs that would stop being served. Anything that recorded one as an issuer or subject is left pointing at nothing.\"},\"aclEntriesRemoved\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Subjects whose ACL entry would be removed outright, because this context was its only scope.\"},\"aclEntriesUpdated\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Subjects whose ACL entry would be narrowed — they hold other scopes and keep them.\"},\"didTemplates\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"DID templates scoped to this context that would be removed.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
