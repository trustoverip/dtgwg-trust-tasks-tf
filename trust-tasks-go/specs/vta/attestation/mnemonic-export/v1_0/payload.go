// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/attestation/mnemonic-export/1.0/payload.schema.json

package vtaattestationmnemonicexportv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response VTA Attestation Mnemonic Export — response payload
type Response struct {
	// The ASCII-armored sealed bundle. It carries the mnemonic sealed to clientDid under a
	// producer assertion whose attestation quote binds the client key, the nonce and the
	// recipient's own sealing key.
	Bundle string `json:"bundle"`

	// SHA-256 of the bundle, lowercase hex. The producer confirms it out of band before
	// opening, so a substituted bundle is caught before any key is applied to it.
	Digest string `json:"digest"`

	// Seconds that were left in the export window when the mnemonic was released.
	// Informational: the window is spent either way.
	WindowRemainingSecs int64 `json:"windowRemainingSecs"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload Asks an agent running in a trusted execution environment to release its seed
// mnemonic, once, sealed to a key only the producer holds. The outer document members
// (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework —
// SPEC §6.3.
type Payload struct {
	// The Ed25519 did:key the mnemonic is sealed to, by its X25519 counterpart, so only the
	// holder of this key's private half can open the answer. Over an end-to-end channel, a
	// key generated for this one export on the machine that will hold the backup, and
	// discarded after. On the signed first-boot path over a hop-by-hop channel, exactly the
	// document's issuer: the key that signed the request.
	ClientDID string `json:"clientDid"`

	// Sixteen random bytes, base64url without padding. It becomes the sealed bundle's
	// identifier, and the recipient binds it into its attestation quote, so an answer cannot
	// be replayed to a different request.
	Nonce string `json:"nonce"`

	// Operator-visible label for the request. Never sealed into the answer and never treated
	// as an instruction.
	Label *string `json:"label,omitempty"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vta/attestation/mnemonic-export/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vta/attestation/mnemonic-export/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vta/attestation/mnemonic-export/1.0\",\"title\":\"VTA Attestation — Mnemonic Export — payload\",\"description\":\"Asks an agent running in a trusted execution environment to release its seed mnemonic, once, sealed to a key only the producer holds. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"clientDid\",\"nonce\"],\"properties\":{\"clientDid\":{\"type\":\"string\",\"pattern\":\"^did:key:z6Mk[1-9A-HJ-NP-Za-km-z]+$\",\"maxLength\":128,\"description\":\"The Ed25519 did:key the mnemonic is sealed to, by its X25519 counterpart, so only the holder of this key's private half can open the answer. Over an end-to-end channel, a key generated for this one export on the machine that will hold the backup, and discarded after. On the signed first-boot path over a hop-by-hop channel, exactly the document's issuer: the key that signed the request.\"},\"nonce\":{\"type\":\"string\",\"pattern\":\"^[A-Za-z0-9_-]{22}$\",\"description\":\"Sixteen random bytes, base64url without padding. It becomes the sealed bundle's identifier, and the recipient binds it into its attestation quote, so an answer cannot be replayed to a different request.\"},\"label\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":256,\"description\":\"Operator-visible label for the request. Never sealed into the answer and never treated as an instruction.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Attestation Mnemonic Export — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"bundle\",\"digest\",\"windowRemainingSecs\"],\"properties\":{\"bundle\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":65536,\"description\":\"The ASCII-armored sealed bundle. It carries the mnemonic sealed to clientDid under a producer assertion whose attestation quote binds the client key, the nonce and the recipient's own sealing key.\"},\"digest\":{\"type\":\"string\",\"pattern\":\"^[0-9a-f]{64}$\",\"description\":\"SHA-256 of the bundle, lowercase hex. The producer confirms it out of band before opening, so a substituted bundle is caught before any key is applied to it.\"},\"windowRemainingSecs\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"Seconds that were left in the export window when the mnemonic was released. Informational: the window is spent either way.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Attestation Mnemonic Export — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"bundle\",\"digest\",\"windowRemainingSecs\"],\"properties\":{\"bundle\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":65536,\"description\":\"The ASCII-armored sealed bundle. It carries the mnemonic sealed to clientDid under a producer assertion whose attestation quote binds the client key, the nonce and the recipient's own sealing key.\"},\"digest\":{\"type\":\"string\",\"pattern\":\"^[0-9a-f]{64}$\",\"description\":\"SHA-256 of the bundle, lowercase hex. The producer confirms it out of band before opening, so a substituted bundle is caught before any key is applied to it.\"},\"windowRemainingSecs\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"Seconds that were left in the export window when the mnemonic was released. Informational: the window is spent either way.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
