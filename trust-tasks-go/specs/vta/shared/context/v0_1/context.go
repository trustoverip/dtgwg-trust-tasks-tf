// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/_shared/0.1/context.schema.json

package vtasharedcontextv0_1

import "encoding/json"

// ContextRecord A context as the VTA holds it.
type ContextRecord struct {
	// Context id. For a nested context this is the full path (`parent/leaf`), not the leaf
	// alone — an ACL scope naming this context must use this value verbatim.
	ID string `json:"id"`

	// Human-readable name. Operator-facing only; carries no authorization meaning, and two
	// contexts may share a name.
	Name string `json:"name"`

	// DID this context acts as, when one has been assigned. Absent means the context has no
	// identity of its own yet — not that it has been denied one.
	DID *string `json:"did,omitempty"`

	// Free-form description.
	Description *string `json:"description,omitempty"`

	// Id of the context this one nests under. Absent for a top-level context. Authority
	// granted at a parent reaches its children, so this member is load-bearing for anyone
	// reasoning about scope.
	Parent *string `json:"parent,omitempty"`

	// Resolved path from the root context, as the VTA derives it from the parent chain.
	// Consumers MUST treat this as derived state: it is recomputed by the maintainer and is
	// not independently settable.
	BasePath string `json:"basePath"`

	// RFC 3339.
	CreatedAt string `json:"createdAt"`

	// RFC 3339.
	UpdatedAt string `json:"updatedAt"`
	Ext       *Ext   `json:"ext,omitempty"`
}

// ContextPolicy Per-context restrictions the VTA enforces. Every member is optional, and
// **absence means unrestricted, not empty** — a policy that omits `presentableTypes`
// permits every type, while one that sets it to `[]` permits none. The two are opposite
// instructions and a consumer MUST NOT collapse them.
type ContextPolicy struct {
	// VIDs permitted to receive presentations from this context. Absent: any verifier.
	// Present-but-empty: none.
	TrustedVerifiers *[]string `json:"trustedVerifiers,omitempty"`

	// Credential types that may be presented from this context. Absent: any type.
	// Present-but-empty: none.
	PresentableTypes *[]string `json:"presentableTypes,omitempty"`

	// Key ids the signing oracle may be invoked on within this context. Absent: every key the
	// context holds. Present-but-empty: no keys.
	SignableKeys *[]string `json:"signableKeys,omitempty"`

	// Whether material in this context may leave it through an export. Defaults to true; set
	// it to false for a context whose contents must not be extractable.
	ExportAllowed *bool `json:"exportAllowed,omitempty"`

	// Resource limits for the context. Shape is maintainer-defined.
	Quotas *map[string]json.RawMessage `json:"quotas,omitempty"`
	Ext    *Ext                        `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage
