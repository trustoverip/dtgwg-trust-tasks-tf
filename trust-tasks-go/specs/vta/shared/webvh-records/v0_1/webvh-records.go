// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/_shared/0.1/webvh-records.schema.json

package vtasharedwebvhrecordsv0_1

import "encoding/json"

// WebvhDIDRecord A did:webvh DID as the VTA holds it. The DID document itself is not here
// — it lives in the log, which `vta/webvh/dids/get` returns on request.
type WebvhDIDRecord struct {
	// The DID, e.g. `did:webvh:<scid>:example.com:alice`.
	DID string `json:"did"`

	// Id of the hosting server serving this DID's log. Empty for a serverless DID, whose
	// location comes from the URL it was created with.
	ServerID string `json:"serverId"`

	// The hosting server's handle for the record. Unrelated to a BIP-39 phrase; the collision
	// of terms is historical.
	Mnemonic string `json:"mnemonic"`

	// Self-certifying identifier: the hash committing to the log's first entry. It is what
	// makes the DID's history tamper-evident, and it changes only when the log is re-created
	// — never in place.
	Scid string `json:"scid"`

	// Context this DID belongs to. Deleting that context destroys this DID.
	ContextID string `json:"contextId"`

	// Whether the DID may be moved to another domain without losing its identity. Fixed at
	// creation: a DID created non-portable cannot be made portable later, because portability
	// is a commitment recorded in the log's first entry.
	Portable bool `json:"portable"`

	// Number of entries in the log.
	LogEntryCount int64 `json:"logEntryCount"`

	// Pre-rotation keys committed by the most recent entry. `0` means pre-rotation is
	// disabled — a compromise of the current key is then unrecoverable by rotation, because
	// the successor was never committed in advance.
	PreRotationCount *int64 `json:"preRotationCount,omitempty"`

	// Next `#key-{n}` fragment id to mint. Monotonic and never decremented, so a fragment is
	// never reused for a different key.
	NextFragmentID *int64 `json:"nextFragmentId,omitempty"`
	CreatedAt      string `json:"createdAt"`
	UpdatedAt      string `json:"updatedAt"`
	Ext            *Ext   `json:"ext,omitempty"`
}

// WebvhServerRecord A hosting server the VTA is registered with. Carries no credential:
// what the VTA holds to authenticate against the server is never part of this record and
// never leaves it.
type WebvhServerRecord struct {
	// Local id for the server, chosen by the operator.
	ID string `json:"id"`

	// The server's own DID, used to authenticate it.
	DID       string  `json:"did"`
	Label     *string `json:"label,omitempty"`
	CreatedAt string  `json:"createdAt"`
	UpdatedAt string  `json:"updatedAt"`
	Ext       *Ext    `json:"ext,omitempty"`
}

// AgentNameEntry An agent name bound to a DID. A name is only ever authoritative in the
// DID→name direction: the DID document's `alsoKnownAs` is what proves the binding,
// because anyone controlling a domain can point a name at somebody else's DID.
type AgentNameEntry struct {
	// The name, without the host — the full form is `<host>/@<name>`.
	Name string `json:"name"`

	// Whether the name currently resolves. A disabled name is still held — it is not
	// available for anyone else to take.
	Enabled bool `json:"enabled"`

	// Unix epoch seconds.
	CreatedAt *int64 `json:"createdAt,omitempty"`
	Ext       *Ext   `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage
