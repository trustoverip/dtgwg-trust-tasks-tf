// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/_shared/0.1/passkey-vm.schema.json

package vtasharedpasskeyvmv0_1

// PasskeyVerificationMethod PasskeyVerificationMethod
type PasskeyVerificationMethod struct {
	// The verificationMethod id as it appears in the DID document:
	// `<did>#passkey-<base64url(sha256(credentialId))>`. The fragment is content-derived so a
	// verifier can locate this VM by recomputing `sha256(credential.id)`.
	ID string `json:"id"`

	// Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID
	// resolvers and verifiers need no WebAuthn-specific knowledge to consume it.
	Type string `json:"type"`

	// The DID this verificationMethod is published on (the DID being augmented).
	Controller string `json:"controller"`

	// W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is the
	// value a verifier validates a WebAuthn assertion against. Re-derived server-side from
	// the attestation at enrolment — never trusted from the browser (see
	// vta/passkey-vms/enroll-submit).
	PublicKeyMultibase string `json:"publicKeyMultibase"`

	// The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute
	// `sha256(credentialId)` and match the assertion to this VM's `id` fragment.
	WebauthnCredentialID string `json:"webauthnCredentialId"`

	// Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`, `nfc`,
	// `ble`). Advisory only — verifiers MUST NOT make trust decisions based on transport
	// hints.
	WebauthnTransports *[]string `json:"webauthnTransports,omitempty"`

	// Optional operator-supplied human-readable label (e.g. "MacBook Touch ID").
	// Informational; not authoritative and not a security input.
	Label *string `json:"label,omitempty"`
}
