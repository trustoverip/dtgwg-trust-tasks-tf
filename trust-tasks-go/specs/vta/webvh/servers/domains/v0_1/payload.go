// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/servers/domains/0.1/payload.schema.json

package vtawebvhserversdomainsv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The success response. Carried in a Trust Task document whose type is
// https://trusttasks.org/spec/vta/webvh/servers/domains/0.1#response.
type Response struct {
	// The domains the agent may use on that server, as the server reported them. Empty is a
	// legitimate answer — a server the agent can reach but holds no domain grant on.
	Domains []DomainEntry `json:"domains"`

	// The domain used when a request names none — the caller's own default where the server
	// records one, else the server's system default. Producers SHOULD omit it when there is
	// neither; `null` is accepted as the same answer, and consumers MUST NOT read a
	// difference between the two.
	Default *string `json:"default,omitempty"`
	Ext     *Ext    `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// DomainEntryStatus Domain lifecycle state. A `disabled` domain still serves existing
// DIDs in read-only mode for the host's configured grace period before purge becomes
// eligible.
type DomainEntryStatus string

// Values DomainEntryStatus may take, per this specification's schema.
const (
	DomainEntryStatusActive   DomainEntryStatus = "active"
	DomainEntryStatusDisabled DomainEntryStatus = "disabled"
)

// DomainEntry DomainEntry
type DomainEntry struct {
	// Hosting domain name (e.g. `did.example.com`). Compared case-insensitively; producers
	// SHOULD emit lowercase canonical form.
	Name string `json:"name"`

	// Optional human-readable label.
	Label *string `json:"label,omitempty"`

	// Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only
	// mode for the host's configured grace period before purge becomes eligible.
	Status DomainEntryStatus `json:"status"`

	// When `true`, this domain is the host's default — new DIDs created without an explicit
	// domain are hosted here. Exactly one entry SHOULD carry `defaultDomain: true`.
	DefaultDomain *bool  `json:"defaultDomain,omitempty"`
	CreatedAt     string `json:"createdAt"`

	// Present iff `status === "disabled"`.
	DisabledAt *string `json:"disabledAt,omitempty"`

	// Earliest RFC3339 timestamp at which the host's background sweep is allowed to purge
	// content for a disabled domain. Operators with administrative authority MAY override the
	// wait via `domain/purge`.
	PurgeAt *string `json:"purgeAt,omitempty"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// Payload Ask an agent which hosting domains it may use on one of the hosting servers it
// has registered. The agent answers by relaying the server's own caller-scoped view; the
// response items are the same DomainEntry the did-management family defines.
type Payload struct {
	// Identifier of the registered hosting server to ask. Required: an agent may know
	// several, and they do not share a domain namespace — omitting it would make the answer
	// ambiguous rather than general.
	ServerID string `json:"serverId"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vta/webvh/servers/domains/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vta/webvh/servers/domains/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vta/webvh/servers/domains/0.1\",\"title\":\"WebVH Server Domains — payload\",\"description\":\"Ask an agent which hosting domains it may use on one of the hosting servers it has registered. The agent answers by relaying the server's own caller-scoped view; the response items are the same DomainEntry the did-management family defines.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"serverId\"],\"properties\":{\"serverId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Identifier of the registered hosting server to ask. Required: an agent may know several, and they do not share a domain namespace — omitting it would make the answer ambiguous rather than general.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"WebVH Server Domains — response payload\",\"description\":\"The success response. Carried in a Trust Task document whose type is https://trusttasks.org/spec/vta/webvh/servers/domains/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"domains\"],\"properties\":{\"domains\":{\"type\":\"array\",\"description\":\"The domains the agent may use on that server, as the server reported them. Empty is a legitimate answer — a server the agent can reach but holds no domain grant on.\",\"items\":{\"$ref\":\"#/$defs/DomainEntry\"}},\"default\":{\"type\":[\"string\",\"null\"],\"description\":\"The domain used when a request names none — the caller's own default where the server records one, else the server's system default. Producers SHOULD omit it when there is neither; `null` is accepted as the same answer, and consumers MUST NOT read a difference between the two.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"DomainEntry\":{\"title\":\"DomainEntry\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"name\",\"status\",\"createdAt\"],\"properties\":{\"name\":{\"type\":\"string\",\"description\":\"Hosting domain name (e.g. `did.example.com`). Compared case-insensitively; producers SHOULD emit lowercase canonical form.\"},\"label\":{\"type\":\"string\",\"description\":\"Optional human-readable label.\"},\"status\":{\"type\":\"string\",\"enum\":[\"active\",\"disabled\"],\"description\":\"Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only mode for the host's configured grace period before purge becomes eligible.\"},\"defaultDomain\":{\"type\":\"boolean\",\"description\":\"When `true`, this domain is the host's default — new DIDs created without an explicit domain are hosted here. Exactly one entry SHOULD carry `defaultDomain: true`.\"},\"createdAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"disabledAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"Present iff `status === \\\"disabled\\\"`.\"},\"purgeAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"Earliest RFC3339 timestamp at which the host's background sweep is allowed to purge content for a disabled domain. Operators with administrative authority MAY override the wait via `domain/purge`.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"WebVH Server Domains — response payload\",\"description\":\"The success response. Carried in a Trust Task document whose type is https://trusttasks.org/spec/vta/webvh/servers/domains/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"domains\"],\"properties\":{\"domains\":{\"type\":\"array\",\"description\":\"The domains the agent may use on that server, as the server reported them. Empty is a legitimate answer — a server the agent can reach but holds no domain grant on.\",\"items\":{\"$ref\":\"#/$defs/DomainEntry\"}},\"default\":{\"type\":[\"string\",\"null\"],\"description\":\"The domain used when a request names none — the caller's own default where the server records one, else the server's system default. Producers SHOULD omit it when there is neither; `null` is accepted as the same answer, and consumers MUST NOT read a difference between the two.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"DomainEntry\":{\"title\":\"DomainEntry\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"name\",\"status\",\"createdAt\"],\"properties\":{\"name\":{\"type\":\"string\",\"description\":\"Hosting domain name (e.g. `did.example.com`). Compared case-insensitively; producers SHOULD emit lowercase canonical form.\"},\"label\":{\"type\":\"string\",\"description\":\"Optional human-readable label.\"},\"status\":{\"type\":\"string\",\"enum\":[\"active\",\"disabled\"],\"description\":\"Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only mode for the host's configured grace period before purge becomes eligible.\"},\"defaultDomain\":{\"type\":\"boolean\",\"description\":\"When `true`, this domain is the host's default — new DIDs created without an explicit domain are hosted here. Exactly one entry SHOULD carry `defaultDomain: true`.\"},\"createdAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"disabledAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"Present iff `status === \\\"disabled\\\"`.\"},\"purgeAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"Earliest RFC3339 timestamp at which the host's background sweep is allowed to purge content for a disabled domain. Operators with administrative authority MAY override the wait via `domain/purge`.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
