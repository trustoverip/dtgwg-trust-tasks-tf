// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/services/rollback/1.0/payload.schema.json

package vtaservicesrollbackv1_0

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response Success response to vta/services/rollback. Type
// https://trusttasks.org/spec/vta/services/rollback/1.0#response.
type Response struct {
	Result RollbackResult `json:"result"`
	Ext    *Ext           `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// RollbackResultKind What the rollback did. `disabled` re-disabled the transport,
// `enabled` re-advertised it at its prior settings, `updated` restored prior settings on
// an entry that stayed advertised, and `noOp` means the previous state already matched —
// nothing was written.
type RollbackResultKind string

// Values RollbackResultKind may take, per this specification's schema.
const (
	RollbackResultKindDisabled RollbackResultKind = "disabled"
	RollbackResultKindEnabled  RollbackResultKind = "enabled"
	RollbackResultKindUpdated  RollbackResultKind = "updated"
	RollbackResultKindNoOp     RollbackResultKind = "noOp"
)

// RollbackResult The outcome of a rollback. Distinct from ServiceMutationResult because a
// rollback can legitimately publish nothing: if the previous state already equals the
// current one there is no change to write, and `kind: "noOp"` says so with
// `logEntryVersionId` absent. Treating that as a failure would be wrong — the requested
// state holds — and treating it as an ordinary success would report a log entry that does
// not exist.
type RollbackResult struct {
	// What the rollback did. `disabled` re-disabled the transport, `enabled` re-advertised it
	// at its prior settings, `updated` restored prior settings on an entry that stayed
	// advertised, and `noOp` means the previous state already matched — nothing was written.
	Kind RollbackResultKind `json:"kind"`

	// Version id of the log entry the rollback wrote. **Absent when `kind` is `noOp`**, and
	// present otherwise.
	LogEntryVersionID *string `json:"logEntryVersionId,omitempty"`

	// RFC 3339 instant the rollback took effect. Absent for `noOp`.
	EffectiveAt *string `json:"effectiveAt,omitempty"`

	// Present when rolling back DIDComm left a mediator draining.
	DrainingMediator *string `json:"drainingMediator,omitempty"`

	// When that drain completes.
	DrainUntil *string `json:"drainUntil,omitempty"`

	// The agent's own DID.
	VtaDID *string `json:"vtaDid,omitempty"`

	// True when the entry was written locally but not published — the operator must redeploy
	// before any verifier sees it.
	Serverless *bool `json:"serverless,omitempty"`
	Ext        *Ext  `json:"ext,omitempty"`
}

// ServiceKind Which transport a task is acting on. This is the discriminator: it selects
// which member of `config` is meaningful, and a payload naming one kind with another's
// config is malformed rather than merely ignored.
type ServiceKind string

// Values ServiceKind may take, per this specification's schema.
const (
	ServiceKindDidcomm  ServiceKind = "didcomm"
	ServiceKindRest     ServiceKind = "rest"
	ServiceKindTsp      ServiceKind = "tsp"
	ServiceKindWebauthn ServiceKind = "webauthn"
)

// Payload Request payload for vta/services/rollback. Reverts a transport to its previous
// advertised settings by writing a NEW log entry — the history is append-only, so a
// rollback is a forward step that restores an earlier state, never an erasure of what
// happened.
type Payload struct {
	Service ServiceKind `json:"service"`
	Ext     *Ext        `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vta/services/rollback/1.0"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vta/services/rollback/1.0#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vta/services/rollback/1.0\",\"title\":\"VTA Services — Rollback — payload\",\"description\":\"Request payload for vta/services/rollback. Reverts a transport to its previous advertised settings by writing a NEW log entry — the history is append-only, so a rollback is a forward step that restores an earlier state, never an erasure of what happened.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"service\"],\"properties\":{\"service\":{\"$ref\":\"#/$defs/ServiceKind\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Services — Rollback — response payload\",\"description\":\"Success response to vta/services/rollback. Type https://trusttasks.org/spec/vta/services/rollback/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"result\"],\"properties\":{\"result\":{\"$ref\":\"#/$defs/RollbackResult\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"RollbackResult\":{\"title\":\"RollbackResult\",\"description\":\"The outcome of a rollback. Distinct from ServiceMutationResult because a rollback can legitimately publish nothing: if the previous state already equals the current one there is no change to write, and `kind: \\\"noOp\\\"` says so with `logEntryVersionId` absent. Treating that as a failure would be wrong — the requested state holds — and treating it as an ordinary success would report a log entry that does not exist.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"kind\"],\"properties\":{\"kind\":{\"type\":\"string\",\"enum\":[\"disabled\",\"enabled\",\"updated\",\"noOp\"],\"description\":\"What the rollback did. `disabled` re-disabled the transport, `enabled` re-advertised it at its prior settings, `updated` restored prior settings on an entry that stayed advertised, and `noOp` means the previous state already matched — nothing was written.\"},\"logEntryVersionId\":{\"type\":\"string\",\"description\":\"Version id of the log entry the rollback wrote. **Absent when `kind` is `noOp`**, and present otherwise.\"},\"effectiveAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"RFC 3339 instant the rollback took effect. Absent for `noOp`.\"},\"drainingMediator\":{\"type\":\"string\",\"description\":\"Present when rolling back DIDComm left a mediator draining.\"},\"drainUntil\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When that drain completes.\"},\"vtaDid\":{\"type\":\"string\",\"description\":\"The agent's own DID.\"},\"serverless\":{\"type\":\"boolean\",\"default\":false,\"description\":\"True when the entry was written locally but not published — the operator must redeploy before any verifier sees it.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"ServiceKind\":{\"title\":\"ServiceKind\",\"description\":\"Which transport a task is acting on. This is the discriminator: it selects which member of `config` is meaningful, and a payload naming one kind with another's config is malformed rather than merely ignored.\",\"type\":\"string\",\"enum\":[\"didcomm\",\"rest\",\"tsp\",\"webauthn\"]}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTA Services — Rollback — response payload\",\"description\":\"Success response to vta/services/rollback. Type https://trusttasks.org/spec/vta/services/rollback/1.0#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"result\"],\"properties\":{\"result\":{\"$ref\":\"#/$defs/RollbackResult\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"RollbackResult\":{\"title\":\"RollbackResult\",\"description\":\"The outcome of a rollback. Distinct from ServiceMutationResult because a rollback can legitimately publish nothing: if the previous state already equals the current one there is no change to write, and `kind: \\\"noOp\\\"` says so with `logEntryVersionId` absent. Treating that as a failure would be wrong — the requested state holds — and treating it as an ordinary success would report a log entry that does not exist.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"kind\"],\"properties\":{\"kind\":{\"type\":\"string\",\"enum\":[\"disabled\",\"enabled\",\"updated\",\"noOp\"],\"description\":\"What the rollback did. `disabled` re-disabled the transport, `enabled` re-advertised it at its prior settings, `updated` restored prior settings on an entry that stayed advertised, and `noOp` means the previous state already matched — nothing was written.\"},\"logEntryVersionId\":{\"type\":\"string\",\"description\":\"Version id of the log entry the rollback wrote. **Absent when `kind` is `noOp`**, and present otherwise.\"},\"effectiveAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"RFC 3339 instant the rollback took effect. Absent for `noOp`.\"},\"drainingMediator\":{\"type\":\"string\",\"description\":\"Present when rolling back DIDComm left a mediator draining.\"},\"drainUntil\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When that drain completes.\"},\"vtaDid\":{\"type\":\"string\",\"description\":\"The agent's own DID.\"},\"serverless\":{\"type\":\"boolean\",\"default\":false,\"description\":\"True when the entry was written locally but not published — the operator must redeploy before any verifier sees it.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"ServiceKind\":{\"title\":\"ServiceKind\",\"description\":\"Which transport a task is acting on. This is the discriminator: it selects which member of `config` is meaningful, and a payload naming one kind with another's config is malformed rather than merely ignored.\",\"type\":\"string\",\"enum\":[\"didcomm\",\"rest\",\"tsp\",\"webauthn\"]}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
