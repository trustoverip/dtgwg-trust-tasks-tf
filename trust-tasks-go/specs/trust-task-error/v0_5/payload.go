// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/trust-task-error/0.5/payload.schema.json

package trusttaskerrorv0_5

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// PayloadInResponseTo Identifies the Trust Task document this error reports on.
// RECOMMENDED on every error response, and REQUIRED in practice for any error a third
// party may later be asked to rely on: threadId correlates the exchange only for a party
// that saw the request, so without this an error retained as evidence names neither the
// task it terminated nor the instance.
type PayloadInResponseTo struct {
	// The Type URI of the document being reported on, including any #request or #response
	// fragment it carried. This is what tells a consumer which specification's semantics
	// apply to an extended code, and which specification declared the requirement that was
	// breached.
	TypeURI string `json:"typeUri"`

	// The id of the specific document being reported on (SPEC §4.3). Globally unique and
	// never reused, so it names one instance where threadId names an exchange. Omit only
	// where echoing it would disclose more than the error already does.
	ID *string `json:"id,omitempty"`
}

// Payload The canonical error-response payload per SPEC.md §8.2.
type Payload struct {
	// Short identifier for the failure category. MUST be either a framework standard code
	// (SPEC.md §8.3) or an extended code namespaced by the originating spec's slug (SPEC.md
	// §8.5, e.g. 'acl/grant:roleNotRecognized').
	Code string `json:"code"`

	// Identifies the Trust Task document this error reports on. RECOMMENDED on every error
	// response, and REQUIRED in practice for any error a third party may later be asked to
	// rely on: threadId correlates the exchange only for a party that saw the request, so
	// without this an error retained as evidence names neither the task it terminated nor the
	// instance.
	InResponseTo *PayloadInResponseTo `json:"inResponseTo,omitempty"`

	// Human-readable description of the error. Non-normative; intended for logs and operator
	// UI.
	Message *string `json:"message,omitempty"`

	// true if the producer of the original document MAY retry; false if retrying with the
	// same document is not expected to succeed.
	Retryable bool `json:"retryable"`

	// RFC 3339 timestamp before which the producer SHOULD NOT retry. Meaningful only when
	// retryable is true.
	RetryAfter *string `json:"retryAfter,omitempty"`

	// Optional task-specific extension data. Per-spec error code declarations may pin the
	// shape via a JSON Schema fragment.
	Details *map[string]json.RawMessage `json:"details,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/trust-task-error/0.5"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/trust-task-error/0.5\",\"title\":\"Trust Task Error — payload\",\"description\":\"The canonical error-response payload per SPEC.md §8.2.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"code\",\"retryable\"],\"properties\":{\"code\":{\"type\":\"string\",\"description\":\"Short identifier for the failure category. MUST be either a framework standard code (SPEC.md §8.3) or an extended code namespaced by the originating spec's slug (SPEC.md §8.5, e.g. 'acl/grant:roleNotRecognized').\",\"anyOf\":[{\"enum\":[\"malformedRequest\",\"unsupportedType\",\"unsupportedVersion\",\"expired\",\"proofRequired\",\"proofInvalid\",\"permissionDenied\",\"wrongRecipient\",\"identityMismatch\",\"idConflict\",\"cancelled\",\"taskFailed\",\"unavailable\",\"internalError\"]},{\"pattern\":\"^[a-z][a-z0-9]*(-[a-z0-9]+)*(/[a-z][a-z0-9]*(-[a-z0-9]+)*)*:[a-z][a-zA-Z0-9_]*$\"}]},\"inResponseTo\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"typeUri\"],\"description\":\"Identifies the Trust Task document this error reports on. RECOMMENDED on every error response, and REQUIRED in practice for any error a third party may later be asked to rely on: threadId correlates the exchange only for a party that saw the request, so without this an error retained as evidence names neither the task it terminated nor the instance.\",\"properties\":{\"typeUri\":{\"type\":\"string\",\"format\":\"uri\",\"minLength\":1,\"description\":\"The Type URI of the document being reported on, including any #request or #response fragment it carried. This is what tells a consumer which specification's semantics apply to an extended code, and which specification declared the requirement that was breached.\"},\"id\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The id of the specific document being reported on (SPEC §4.3). Globally unique and never reused, so it names one instance where threadId names an exchange. Omit only where echoing it would disclose more than the error already does.\"}}},\"message\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Human-readable description of the error. Non-normative; intended for logs and operator UI.\"},\"retryable\":{\"type\":\"boolean\",\"description\":\"true if the producer of the original document MAY retry; false if retrying with the same document is not expected to succeed.\"},\"retryAfter\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"RFC 3339 timestamp before which the producer SHOULD NOT retry. Meaningful only when retryable is true.\"},\"details\":{\"type\":\"object\",\"description\":\"Optional task-specific extension data. Per-spec error code declarations may pin the shape via a JSON Schema fragment.\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  false,
	PayloadSchema:       PayloadSchemaJSON,
}
