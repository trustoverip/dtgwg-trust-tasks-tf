// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/keys/derive-and-sign/0.1/payload.schema.json

package keysderiveandsignv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The success response to a keys/derive-and-sign request. Carried in a Trust
// Task document whose type is
// https://trusttasks.org/spec/keys/derive-and-sign/0.1#response.
type Response struct {
	// Public half of the derived key, multibase-encoded — so the producer learns the identity
	// it just signed as, which it could not otherwise know for a key that was never stored.
	PublicKey string `json:"publicKey"`

	// Signature bytes, base64url-encoded without padding.
	Signature string        `json:"signature"`
	Algorithm SignAlgorithm `json:"algorithm"`
	Ext       *Ext          `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// SignAlgorithm `EdDSA` pairs with an `ed25519` key; `ES256` pairs with a `p256` key;
// `ML-DSA-44` and `ML-DSA-65` pair with `mldsa44` and `mldsa65` keys respectively. An
// `x25519` key performs key agreement and can sign nothing, so no algorithm here is valid
// for one. These are JOSE algorithm identifiers, externally owned, so they are carried
// verbatim and never re-cased (SPEC.md §4.10 rule 5); the ML-DSA names are those RFC 9964
// registers in the JOSE Web Signature and Encryption Algorithms registry, which is why
// their hyphenated casing differs from the `keyType` values beside them — those are
// specification-defined. The set is expected to grow as algorithms are registered. The
// enumeration remains closed: an unrecognised algorithm is refused rather than silently
// substituted with a supported one.
type SignAlgorithm string

// Values SignAlgorithm may take, per this specification's schema.
const (
	SignAlgorithmEdDsa   SignAlgorithm = "EdDSA"
	SignAlgorithmEs256   SignAlgorithm = "ES256"
	SignAlgorithmMlDsa44 SignAlgorithm = "ML-DSA-44"
	SignAlgorithmMlDsa65 SignAlgorithm = "ML-DSA-65"
)

// KeyType Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA),
// `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44`
// and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is
// expected to grow as algorithms are standardised, and growing it is a MINOR change under
// SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a
// constraint rather than narrowing one, and the generated libraries mark this enumeration
// non-exhaustive so that a consumer absorbs a new value rather than failing to compile.
// Two ML-DSA parameter sets are carried because two specifications require different ones
// — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44,
// while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is
// chosen by whatever consumes the key and the two are not redundant. A consumer that does
// not implement a value it receives MUST refuse the document rather than substitute one
// it does support.
type KeyType string

// Values KeyType may take, per this specification's schema.
const (
	KeyTypeEd25519 KeyType = "ed25519"
	KeyTypeX25519  KeyType = "x25519"
	KeyTypeP256    KeyType = "p256"
	KeyTypeMldsa44 KeyType = "mldsa44"
	KeyTypeMldsa65 KeyType = "mldsa65"
)

// Payload Derive a key at a path and sign with it in one step, without adding a stored
// key record.
type Payload struct {
	// Algorithm to derive. MUST be one that can sign.
	KeyType KeyType `json:"keyType"`

	// Hierarchical-deterministic path to derive at. The same path against the same seed
	// always yields the same key, which is what makes this reproducible rather than
	// ephemeral.
	DerivationPath string `json:"derivationPath"`

	// The exact bytes to sign, base64url-encoded without padding. Signed verbatim — not
	// parsed, canonicalized or wrapped.
	Payload string `json:"payload"`

	// Signature algorithm. MUST be compatible with the derived key's type.
	Algorithm SignAlgorithm `json:"algorithm"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/keys/derive-and-sign/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/keys/derive-and-sign/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/keys/derive-and-sign/0.1\",\"title\":\"Keys Derive-and-Sign — payload\",\"description\":\"Derive a key at a path and sign with it in one step, without adding a stored key record.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyType\",\"derivationPath\",\"payload\",\"algorithm\"],\"properties\":{\"keyType\":{\"$ref\":\"#/$defs/KeyType\",\"description\":\"Algorithm to derive. MUST be one that can sign.\"},\"derivationPath\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Hierarchical-deterministic path to derive at. The same path against the same seed always yields the same key, which is what makes this reproducible rather than ephemeral.\"},\"payload\":{\"type\":\"string\",\"description\":\"The exact bytes to sign, base64url-encoded without padding. Signed verbatim — not parsed, canonicalized or wrapped.\"},\"algorithm\":{\"$ref\":\"#/$defs/SignAlgorithm\",\"description\":\"Signature algorithm. MUST be compatible with the derived key's type.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Keys Derive-and-Sign — response payload\",\"description\":\"The success response to a keys/derive-and-sign request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/derive-and-sign/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"publicKey\",\"signature\",\"algorithm\"],\"properties\":{\"publicKey\":{\"type\":\"string\",\"description\":\"Public half of the derived key, multibase-encoded — so the producer learns the identity it just signed as, which it could not otherwise know for a key that was never stored.\"},\"signature\":{\"type\":\"string\",\"description\":\"Signature bytes, base64url-encoded without padding.\"},\"algorithm\":{\"$ref\":\"#/$defs/SignAlgorithm\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"SignAlgorithm\":{\"title\":\"SignAlgorithm\",\"type\":\"string\",\"enum\":[\"EdDSA\",\"ES256\",\"ML-DSA-44\",\"ML-DSA-65\"],\"description\":\"`EdDSA` pairs with an `ed25519` key; `ES256` pairs with a `p256` key; `ML-DSA-44` and `ML-DSA-65` pair with `mldsa44` and `mldsa65` keys respectively. An `x25519` key performs key agreement and can sign nothing, so no algorithm here is valid for one. These are JOSE algorithm identifiers, externally owned, so they are carried verbatim and never re-cased (SPEC.md §4.10 rule 5); the ML-DSA names are those RFC 9964 registers in the JOSE Web Signature and Encryption Algorithms registry, which is why their hyphenated casing differs from the `keyType` values beside them — those are specification-defined. The set is expected to grow as algorithms are registered. The enumeration remains closed: an unrecognised algorithm is refused rather than silently substituted with a supported one.\"},\"KeyType\":{\"title\":\"KeyType\",\"type\":\"string\",\"enum\":[\"ed25519\",\"x25519\",\"p256\",\"mldsa44\",\"mldsa65\"],\"description\":\"Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA), `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is expected to grow as algorithms are standardised, and growing it is a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a constraint rather than narrowing one, and the generated libraries mark this enumeration non-exhaustive so that a consumer absorbs a new value rather than failing to compile. Two ML-DSA parameter sets are carried because two specifications require different ones — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key and the two are not redundant. A consumer that does not implement a value it receives MUST refuse the document rather than substitute one it does support.\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Keys Derive-and-Sign — response payload\",\"description\":\"The success response to a keys/derive-and-sign request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/derive-and-sign/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"publicKey\",\"signature\",\"algorithm\"],\"properties\":{\"publicKey\":{\"type\":\"string\",\"description\":\"Public half of the derived key, multibase-encoded — so the producer learns the identity it just signed as, which it could not otherwise know for a key that was never stored.\"},\"signature\":{\"type\":\"string\",\"description\":\"Signature bytes, base64url-encoded without padding.\"},\"algorithm\":{\"$ref\":\"#/$defs/SignAlgorithm\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"SignAlgorithm\":{\"title\":\"SignAlgorithm\",\"type\":\"string\",\"enum\":[\"EdDSA\",\"ES256\",\"ML-DSA-44\",\"ML-DSA-65\"],\"description\":\"`EdDSA` pairs with an `ed25519` key; `ES256` pairs with a `p256` key; `ML-DSA-44` and `ML-DSA-65` pair with `mldsa44` and `mldsa65` keys respectively. An `x25519` key performs key agreement and can sign nothing, so no algorithm here is valid for one. These are JOSE algorithm identifiers, externally owned, so they are carried verbatim and never re-cased (SPEC.md §4.10 rule 5); the ML-DSA names are those RFC 9964 registers in the JOSE Web Signature and Encryption Algorithms registry, which is why their hyphenated casing differs from the `keyType` values beside them — those are specification-defined. The set is expected to grow as algorithms are registered. The enumeration remains closed: an unrecognised algorithm is refused rather than silently substituted with a supported one.\"},\"KeyType\":{\"title\":\"KeyType\",\"type\":\"string\",\"enum\":[\"ed25519\",\"x25519\",\"p256\",\"mldsa44\",\"mldsa65\"],\"description\":\"Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA), `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is expected to grow as algorithms are standardised, and growing it is a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a constraint rather than narrowing one, and the generated libraries mark this enumeration non-exhaustive so that a consumer absorbs a new value rather than failing to compile. Two ML-DSA parameter sets are carried because two specifications require different ones — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key and the two are not redundant. A consumer that does not implement a value it receives MUST refuse the document rather than substitute one it does support.\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
