// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/keys/sign/0.1/payload.schema.json

package keyssignv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The success response to a keys/sign request. Carried in a Trust Task document
// whose type is https://trusttasks.org/spec/keys/sign/0.1#response.
type Response struct {
	// The key that produced the signature. Echoed so a response separated from its request is
	// still attributable.
	KeyID string `json:"keyId"`

	// Signature bytes, base64url-encoded without padding.
	Signature string        `json:"signature"`
	Algorithm SignAlgorithm `json:"algorithm"`
	Ext       *Ext          `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// SignAlgorithm `EdDSA` pairs with an `ed25519` key; `ES256` pairs with a `p256` key;
// `ML-DSA-44` and `ML-DSA-65` pair with `mldsa44` and `mldsa65` keys respectively. An
// `x25519` key performs key agreement and can sign nothing, so no algorithm here is valid
// for one. These are JOSE algorithm identifiers, externally owned, so they are carried
// verbatim and never re-cased (SPEC.md §4.10 rule 5); the ML-DSA names are those RFC 9964
// registers in the JOSE Web Signature and Encryption Algorithms registry, which is why
// their hyphenated casing differs from the `keyType` values beside them — those are
// specification-defined. The set is expected to grow as algorithms are registered. The
// enumeration remains closed: an unrecognised algorithm is refused rather than silently
// substituted with a supported one.
type SignAlgorithm string

// Values SignAlgorithm may take, per this specification's schema.
const (
	SignAlgorithmEdDsa   SignAlgorithm = "EdDSA"
	SignAlgorithmEs256   SignAlgorithm = "ES256"
	SignAlgorithmMlDsa44 SignAlgorithm = "ML-DSA-44"
	SignAlgorithmMlDsa65 SignAlgorithm = "ML-DSA-65"
)

// Payload Keys Sign — payload
type Payload struct {
	// Identifier of the key to sign with. The key MUST be `active`. Which keys a producer may
	// name is the whole of this task's authorization story — the maintainer signs the bytes
	// it is given without inspecting them.
	KeyID string `json:"keyId"`

	// The exact bytes to sign, base64url-encoded without padding. The maintainer signs these
	// bytes verbatim: it does not parse, canonicalize, or wrap them.
	Payload string `json:"payload"`

	// Signature algorithm to use. MUST be compatible with the named key's `keyType`.
	Algorithm SignAlgorithm `json:"algorithm"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/keys/sign/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/keys/sign/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/keys/sign/0.1\",\"title\":\"Keys Sign — payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyId\",\"payload\",\"algorithm\"],\"properties\":{\"keyId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Identifier of the key to sign with. The key MUST be `active`. Which keys a producer may name is the whole of this task's authorization story — the maintainer signs the bytes it is given without inspecting them.\"},\"payload\":{\"type\":\"string\",\"description\":\"The exact bytes to sign, base64url-encoded without padding. The maintainer signs these bytes verbatim: it does not parse, canonicalize, or wrap them.\"},\"algorithm\":{\"$ref\":\"#/$defs/SignAlgorithm\",\"description\":\"Signature algorithm to use. MUST be compatible with the named key's `keyType`.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\",\"description\":\"Ecosystem-defined extension members per SPEC.md §4.5.1.\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Keys Sign — response payload\",\"description\":\"The success response to a keys/sign request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/sign/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyId\",\"signature\",\"algorithm\"],\"properties\":{\"keyId\":{\"type\":\"string\",\"description\":\"The key that produced the signature. Echoed so a response separated from its request is still attributable.\"},\"signature\":{\"type\":\"string\",\"description\":\"Signature bytes, base64url-encoded without padding.\"},\"algorithm\":{\"$ref\":\"#/$defs/SignAlgorithm\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"SignAlgorithm\":{\"title\":\"SignAlgorithm\",\"type\":\"string\",\"enum\":[\"EdDSA\",\"ES256\",\"ML-DSA-44\",\"ML-DSA-65\"],\"description\":\"`EdDSA` pairs with an `ed25519` key; `ES256` pairs with a `p256` key; `ML-DSA-44` and `ML-DSA-65` pair with `mldsa44` and `mldsa65` keys respectively. An `x25519` key performs key agreement and can sign nothing, so no algorithm here is valid for one. These are JOSE algorithm identifiers, externally owned, so they are carried verbatim and never re-cased (SPEC.md §4.10 rule 5); the ML-DSA names are those RFC 9964 registers in the JOSE Web Signature and Encryption Algorithms registry, which is why their hyphenated casing differs from the `keyType` values beside them — those are specification-defined. The set is expected to grow as algorithms are registered. The enumeration remains closed: an unrecognised algorithm is refused rather than silently substituted with a supported one.\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Keys Sign — response payload\",\"description\":\"The success response to a keys/sign request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/sign/0.1#response.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyId\",\"signature\",\"algorithm\"],\"properties\":{\"keyId\":{\"type\":\"string\",\"description\":\"The key that produced the signature. Echoed so a response separated from its request is still attributable.\"},\"signature\":{\"type\":\"string\",\"description\":\"Signature bytes, base64url-encoded without padding.\"},\"algorithm\":{\"$ref\":\"#/$defs/SignAlgorithm\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"SignAlgorithm\":{\"title\":\"SignAlgorithm\",\"type\":\"string\",\"enum\":[\"EdDSA\",\"ES256\",\"ML-DSA-44\",\"ML-DSA-65\"],\"description\":\"`EdDSA` pairs with an `ed25519` key; `ES256` pairs with a `p256` key; `ML-DSA-44` and `ML-DSA-65` pair with `mldsa44` and `mldsa65` keys respectively. An `x25519` key performs key agreement and can sign nothing, so no algorithm here is valid for one. These are JOSE algorithm identifiers, externally owned, so they are carried verbatim and never re-cased (SPEC.md §4.10 rule 5); the ML-DSA names are those RFC 9964 registers in the JOSE Web Signature and Encryption Algorithms registry, which is why their hyphenated casing differs from the `keyType` values beside them — those are specification-defined. The set is expected to grow as algorithms are registered. The enumeration remains closed: an unrecognised algorithm is refused rather than silently substituted with a supported one.\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
