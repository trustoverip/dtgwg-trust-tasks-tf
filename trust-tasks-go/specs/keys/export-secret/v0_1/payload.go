// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/keys/export-secret/0.1/payload.schema.json

package keysexportsecretv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The key material. Failures use trust-task-error, not this shape. Carried in a
// Trust Task document whose type is
// https://trusttasks.org/spec/keys/export-secret/0.1#response.
type Response struct {
	// The key released, echoed so a consumer can confirm it received the one it named rather
	// than matching on request order.
	KeyID   string  `json:"keyId"`
	KeyType KeyType `json:"keyType"`

	// The public half, multibase-encoded. Returned alongside the private half so a consumer
	// can check the pair agrees before installing it, rather than deriving the public half
	// and hoping.
	PublicKeyMultibase string `json:"publicKeyMultibase"`

	// The private half, multibase-encoded (Base58BTC) over its multicodec-prefixed bytes. The
	// prefix identifies the key material; a consumer decodes by prefix rather than trusting
	// `keyType` alone.
	PrivateKeyMultibase string `json:"privateKeyMultibase"`
}

// KeyType Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA),
// `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44`
// and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is
// expected to grow as algorithms are standardised, and growing it is a MINOR change under
// SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a
// constraint rather than narrowing one, and the generated libraries mark this enumeration
// non-exhaustive so that a consumer absorbs a new value rather than failing to compile.
// Two ML-DSA parameter sets are carried because two specifications require different ones
// — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44,
// while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is
// chosen by whatever consumes the key and the two are not redundant. A consumer that does
// not implement a value it receives MUST refuse the document rather than substitute one
// it does support.
type KeyType string

// Values KeyType may take, per this specification's schema.
const (
	KeyTypeEd25519 KeyType = "ed25519"
	KeyTypeX25519  KeyType = "x25519"
	KeyTypeP256    KeyType = "p256"
	KeyTypeMldsa44 KeyType = "mldsa44"
	KeyTypeMldsa65 KeyType = "mldsa65"
)

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload Names one key whose private half is being asked for. The outer document members
// (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework —
// SPEC §6.3.
type Payload struct {
	// The key to release. Custodian-scoped, as everywhere in `keys/*`. One key per request:
	// there is deliberately no list form, so that each release is one decision the custodian
	// can refuse, record and rate-limit on its own terms.
	KeyID string `json:"keyId"`
	Ext   *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/keys/export-secret/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/keys/export-secret/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/keys/export-secret/0.1\",\"title\":\"Keys Export Secret — payload\",\"description\":\"Names one key whose private half is being asked for. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyId\"],\"properties\":{\"keyId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The key to release. Custodian-scoped, as everywhere in `keys/*`. One key per request: there is deliberately no list form, so that each release is one decision the custodian can refuse, record and rate-limit on its own terms.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Keys Export Secret — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyId\",\"keyType\",\"publicKeyMultibase\",\"privateKeyMultibase\"],\"description\":\"The key material. Failures use trust-task-error, not this shape. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/export-secret/0.1#response.\",\"properties\":{\"keyId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The key released, echoed so a consumer can confirm it received the one it named rather than matching on request order.\"},\"keyType\":{\"$ref\":\"#/$defs/KeyType\"},\"publicKeyMultibase\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The public half, multibase-encoded. Returned alongside the private half so a consumer can check the pair agrees before installing it, rather than deriving the public half and hoping.\"},\"privateKeyMultibase\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The private half, multibase-encoded (Base58BTC) over its multicodec-prefixed bytes. The prefix identifies the key material; a consumer decodes by prefix rather than trusting `keyType` alone.\"}}},\"KeyType\":{\"title\":\"KeyType\",\"type\":\"string\",\"enum\":[\"ed25519\",\"x25519\",\"p256\",\"mldsa44\",\"mldsa65\"],\"description\":\"Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA), `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is expected to grow as algorithms are standardised, and growing it is a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a constraint rather than narrowing one, and the generated libraries mark this enumeration non-exhaustive so that a consumer absorbs a new value rather than failing to compile. Two ML-DSA parameter sets are carried because two specifications require different ones — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key and the two are not redundant. A consumer that does not implement a value it receives MUST refuse the document rather than substitute one it does support.\"},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Keys Export Secret — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"keyId\",\"keyType\",\"publicKeyMultibase\",\"privateKeyMultibase\"],\"description\":\"The key material. Failures use trust-task-error, not this shape. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/export-secret/0.1#response.\",\"properties\":{\"keyId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The key released, echoed so a consumer can confirm it received the one it named rather than matching on request order.\"},\"keyType\":{\"$ref\":\"#/$defs/KeyType\"},\"publicKeyMultibase\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The public half, multibase-encoded. Returned alongside the private half so a consumer can check the pair agrees before installing it, rather than deriving the public half and hoping.\"},\"privateKeyMultibase\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The private half, multibase-encoded (Base58BTC) over its multicodec-prefixed bytes. The prefix identifies the key material; a consumer decodes by prefix rather than trusting `keyType` alone.\"}}},\"KeyType\":{\"title\":\"KeyType\",\"type\":\"string\",\"enum\":[\"ed25519\",\"x25519\",\"p256\",\"mldsa44\",\"mldsa65\"],\"description\":\"Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA), `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is expected to grow as algorithms are standardised, and growing it is a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a constraint rather than narrowing one, and the generated libraries mark this enumeration non-exhaustive so that a consumer absorbs a new value rather than failing to compile. Two ML-DSA parameter sets are carried because two specifications require different ones — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key and the two are not redundant. A consumer that does not implement a value it receives MUST refuse the document rather than substitute one it does support.\"},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
