// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/_shared/0.1/did-record.schema.json

package didmanagementshareddidrecordv0_1

import "encoding/json"

// DIDRecord DidRecord
type DIDRecord struct {
	// Local path under which the DID is hosted (e.g. `alice`, `tenant/staff/alice`,
	// `.well-known`). Compared by exact string equality (SPEC.md §4.8); producers SHOULD emit
	// canonical form.
	Mnemonic string `json:"mnemonic"`

	// VID of the party that currently owns the record. Authorization to mutate the record is
	// anchored on this field.
	Owner string `json:"owner"`

	// RFC3339 timestamp of initial reservation.
	CreatedAt string `json:"createdAt"`

	// RFC3339 timestamp of the most recent record mutation.
	UpdatedAt string `json:"updatedAt"`

	// Number of log entries the host currently holds for the DID. `0` indicates a reservation
	// with no published log yet.
	VersionCount int64 `json:"versionCount"`

	// Fully-qualified DID identifier resolved from the most recent log entry (e.g.
	// `did:webvh:<scid>:host:path`). Absent when `versionCount === 0`.
	DIDID *string `json:"didId,omitempty"`

	// Resolvable URL of the DID's log document (e.g.
	// `https://did.example.com/alice/did.jsonl`). Stable across the record's lifetime:
	// present from the initial reservation (`versionCount === 0`), it tells the owner where
	// to publish the signed log and where resolvers fetch it. Distinct from `didId`, which
	// only exists once a log entry has been published.
	DIDURL *string `json:"didUrl,omitempty"`

	// DID method this record was registered under (e.g. `webvh`, `web`). When omitted,
	// consumers MAY treat the record as legacy; SHOULD default to `webvh` only if their host
	// predates the multi-method era.
	Method *string `json:"method,omitempty"`

	// Hosting domain (hostname) under which the DID resolves. Matches the host segment of the
	// embedded DID identifier.
	Domain *string `json:"domain,omitempty"`

	// When `true`, the DID is administratively disabled — the host serves a deactivation
	// marker but retains content for recovery within the host's retention policy.
	Disabled *bool `json:"disabled,omitempty"`

	// Lifetime resolve counter, when the host exposes per-DID statistics.
	TotalResolves *int64 `json:"totalResolves,omitempty"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage
