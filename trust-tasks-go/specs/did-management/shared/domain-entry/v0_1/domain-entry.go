// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/_shared/0.1/domain-entry.schema.json

package didmanagementshareddomainentryv0_1

import "encoding/json"

// DomainEntryStatus Domain lifecycle state. A `disabled` domain still serves existing
// DIDs in read-only mode for the host's configured grace period before purge becomes
// eligible.
type DomainEntryStatus string

// Values DomainEntryStatus may take, per this specification's schema.
const (
	DomainEntryStatusActive   DomainEntryStatus = "active"
	DomainEntryStatusDisabled DomainEntryStatus = "disabled"
)

// DomainEntry DomainEntry
type DomainEntry struct {
	// Hosting domain name (e.g. `did.example.com`). Compared case-insensitively; producers
	// SHOULD emit lowercase canonical form.
	Name string `json:"name"`

	// Optional human-readable label.
	Label *string `json:"label,omitempty"`

	// Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only
	// mode for the host's configured grace period before purge becomes eligible.
	Status DomainEntryStatus `json:"status"`

	// When `true`, this domain is the host's default — new DIDs created without an explicit
	// domain are hosted here. Exactly one entry SHOULD carry `defaultDomain: true`.
	DefaultDomain *bool  `json:"defaultDomain,omitempty"`
	CreatedAt     string `json:"createdAt"`

	// Present iff `status === "disabled"`.
	DisabledAt *string `json:"disabledAt,omitempty"`

	// Earliest RFC3339 timestamp at which the host's background sweep is allowed to purge
	// content for a disabled domain. Operators with administrative authority MAY override the
	// wait via `domain/purge`.
	PurgeAt *string `json:"purgeAt,omitempty"`

	// Ecosystem-defined extension members per SPEC.md §4.5.1.
	Ext *Ext `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage
