// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/device/wipe/0.1/payload.schema.json

package devicewipev0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseScope ResponseScope is a closed set of string values.
type ResponseScope string

// Values ResponseScope may take, per this specification's schema.
const (
	ResponseScopeCache        ResponseScope = "cache"
	ResponseScopeCacheAndKeys ResponseScope = "cache-and-keys"
	ResponseScopeFull         ResponseScope = "full"
)

// ResponseDiagnostics ResponseDiagnostics is a generated payload type.
type ResponseDiagnostics struct {
	CacheBytesWiped *int64 `json:"cacheBytesWiped,omitempty"`
	KeysWiped       *int64 `json:"keysWiped,omitempty"`

	// OS-level revocation hooks the target managed to invoke (e.g.
	// "navigator.credentials.preventSilentAccess",
	// "ASCredentialIdentityStore.removeAllCredentialIdentities").
	OsHooksInvoked *[]string `json:"osHooksInvoked,omitempty"`

	// Free-form reasons why the wipe was partial (e.g. "os-keychain-unavailable",
	// "extension-storage-quota-exceeded").
	PartialReasons *[]string `json:"partialReasons,omitempty"`
}

// Response Acknowledgement from the target device. Sent only when the target executes the
// wipe; absent if the target was offline or compromised. The maintainer treats the
// absence of a response as 'not confirmed' but considers the device neutralised because
// of the server-side defence-in-depth.
type Response struct {
	DeviceID    string               `json:"deviceId"`
	Scope       ResponseScope        `json:"scope"`
	CompletedAt string               `json:"completedAt"`
	Diagnostics *ResponseDiagnostics `json:"diagnostics,omitempty"`
	Ext         *Ext                 `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// PayloadScope How aggressively the target should wipe: - `cache` — discard the encrypted
// vault cache; consumer can re-sync with valid creds. - `cache-and-keys` — discard cache
// + device-local key material; consumer must re-onboard. - `full` — `cache-and-keys` +
// clear all extension/app storage + revoke OS credential-provider registration where APIs
// permit.
type PayloadScope string

// Values PayloadScope may take, per this specification's schema.
const (
	PayloadScopeCache        PayloadScope = "cache"
	PayloadScopeCacheAndKeys PayloadScope = "cache-and-keys"
	PayloadScopeFull         PayloadScope = "full"
)

// Payload The maintainer issues a wipe to a Companion or Service. The target is expected
// to destroy its local cache and (depending on scope) its device-local key material. The
// action is best-effort — a compromised device may silently drop the wipe — so the
// maintainer additionally revokes ACL access and rotates the device's cache-key
// derivation root, so that defence in depth means a non-compliant device is still
// neutralised.
type Payload struct {
	DeviceID string `json:"deviceId"`

	// How aggressively the target should wipe: - `cache` — discard the encrypted vault cache;
	// consumer can re-sync with valid creds. - `cache-and-keys` — discard cache +
	// device-local key material; consumer must re-onboard. - `full` — `cache-and-keys` +
	// clear all extension/app storage + revoke OS credential-provider registration where APIs
	// permit.
	Scope PayloadScope `json:"scope"`

	// Human-readable reason. Required (not optional) because every wipe is consequential and
	// the audit log must capture intent.
	Reason string `json:"reason"`

	// Wipe-issuance timestamp; identical to the document's `issuedAt`, repeated here so the
	// body is self-contained for offline-queued delivery.
	IssuedAt *string `json:"issuedAt,omitempty"`
	Ext      *Ext    `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/device/wipe/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/device/wipe/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/device/wipe/0.1\",\"title\":\"Device Wipe — payload\",\"description\":\"The maintainer issues a wipe to a Companion or Service. The target is expected to destroy its local cache and (depending on scope) its device-local key material. The action is best-effort — a compromised device may silently drop the wipe — so the maintainer additionally revokes ACL access and rotates the device's cache-key derivation root, so that defence in depth means a non-compliant device is still neutralised.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"deviceId\",\"scope\",\"reason\"],\"properties\":{\"deviceId\":{\"type\":\"string\",\"minLength\":1},\"scope\":{\"type\":\"string\",\"enum\":[\"cache\",\"cache-and-keys\",\"full\"],\"description\":\"How aggressively the target should wipe:\\n- `cache` — discard the encrypted vault cache; consumer can re-sync with valid creds.\\n- `cache-and-keys` — discard cache + device-local key material; consumer must re-onboard.\\n- `full` — `cache-and-keys` + clear all extension/app storage + revoke OS credential-provider registration where APIs permit.\"},\"reason\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":256,\"description\":\"Human-readable reason. Required (not optional) because every wipe is consequential and the audit log must capture intent.\"},\"issuedAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"Wipe-issuance timestamp; identical to the document's `issuedAt`, repeated here so the body is self-contained for offline-queued delivery.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Device Wipe — response payload\",\"description\":\"Acknowledgement from the target device. Sent only when the target executes the wipe; absent if the target was offline or compromised. The maintainer treats the absence of a response as 'not confirmed' but considers the device neutralised because of the server-side defence-in-depth.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"deviceId\",\"scope\",\"completedAt\"],\"properties\":{\"deviceId\":{\"type\":\"string\"},\"scope\":{\"type\":\"string\",\"enum\":[\"cache\",\"cache-and-keys\",\"full\"]},\"completedAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"diagnostics\":{\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"cacheBytesWiped\":{\"type\":\"integer\",\"minimum\":0},\"keysWiped\":{\"type\":\"integer\",\"minimum\":0},\"osHooksInvoked\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"OS-level revocation hooks the target managed to invoke (e.g. \\\"navigator.credentials.preventSilentAccess\\\", \\\"ASCredentialIdentityStore.removeAllCredentialIdentities\\\").\"},\"partialReasons\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Free-form reasons why the wipe was partial (e.g. \\\"os-keychain-unavailable\\\", \\\"extension-storage-quota-exceeded\\\").\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Device Wipe — response payload\",\"description\":\"Acknowledgement from the target device. Sent only when the target executes the wipe; absent if the target was offline or compromised. The maintainer treats the absence of a response as 'not confirmed' but considers the device neutralised because of the server-side defence-in-depth.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"deviceId\",\"scope\",\"completedAt\"],\"properties\":{\"deviceId\":{\"type\":\"string\"},\"scope\":{\"type\":\"string\",\"enum\":[\"cache\",\"cache-and-keys\",\"full\"]},\"completedAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"diagnostics\":{\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"cacheBytesWiped\":{\"type\":\"integer\",\"minimum\":0},\"keysWiped\":{\"type\":\"integer\",\"minimum\":0},\"osHooksInvoked\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"OS-level revocation hooks the target managed to invoke (e.g. \\\"navigator.credentials.preventSilentAccess\\\", \\\"ASCredentialIdentityStore.removeAllCredentialIdentities\\\").\"},\"partialReasons\":{\"type\":\"array\",\"items\":{\"type\":\"string\"},\"description\":\"Free-form reasons why the wipe was partial (e.g. \\\"os-keychain-unavailable\\\", \\\"extension-storage-quota-exceeded\\\").\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
