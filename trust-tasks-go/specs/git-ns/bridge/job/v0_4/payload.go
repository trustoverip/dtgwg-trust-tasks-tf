// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/bridge/job/0.4/payload.schema.json

package gitnsbridgejobv0_4

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// JobID The VTC's identifier for a job, unique across the VTC's jobs.
type JobID = string

// StepName One step of a forge adapter's plan. Forge-neutral names: `create`, `workflow`,
// `keyring`, `variables`, `requiredCheck`, `roles`, `archive`. An adapter MAY add its own
// (`mergeStyle`, `runner`), which a VTC displays but need not understand.
type StepName = string

// DesiredRole DesiredRole is a generated payload type.
type DesiredRole struct {
	Subject DID `json:"subject"`

	// The subject's linked account on this namespace's forge.
	Account ForgeAccount `json:"account"`

	// The highest right recorded for the subject on the repository, or `git.commit.sign`
	// recorded on its namespace — never one held only because `git.ns.admin` implies it. A
	// namespace admin with no right of their own there is listed as `git.ns.admin`, which
	// every forge adapter maps to no role. The forge adapter maps the right to one of its
	// roles.
	Right Right `json:"right"`
}

// RepoSpec RepoSpec is a generated payload type.
type RepoSpec struct {
	Visibility  RepoVisibility `json:"visibility"`
	Description *string        `json:"description,omitempty"`
}

// BindTarget The forge owner whose control the binding admin is to prove.
type BindTarget struct {
	Forge ForgeHost `json:"forge"`
	Owner Segment   `json:"owner"`
}

// ResponseNext For `beginBind` and `beginAccountLink` when `accepted` is `true`: where
// the VTC sends the person.
type ResponseNext struct {
	URL string `json:"url"`

	// The device-flow code, for a forge that uses one.
	UserCode  *string `json:"userCode,omitempty"`
	ExpiresAt string  `json:"expiresAt"`
}

// Response Whether the bridge took the job.
type Response struct {
	JobID JobID `json:"jobId"`

	// `true` — the job is durably queued, or was already queued or running under this
	// `jobId`; a git-ns/bridge/result follows. `false` — the bridge already finished this
	// `jobId`; it does not run it again, and sends its git-ns/bridge/result again.
	Accepted bool `json:"accepted"`

	// For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC sends
	// the person.
	Next *ResponseNext `json:"next,omitempty"`
	Ext  *Ext          `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Segment One lowercased owner or repository name. Forges compare these
// case-insensitively, so the wire form is always lowercase and a producer lowercases
// before sending. A leading `.` is refused, which rules out `.` and `..`.
type Segment = string

// ForgeHost The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server
// host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No
// scheme, no port, no path. The host is a segment of every resource, so a right never
// crosses forges.
type ForgeHost = string

// RepoVisibility Repository visibility on the forge.
type RepoVisibility string

// Values RepoVisibility may take, per this specification's schema.
const (
	RepoVisibilityPublic  RepoVisibility = "public"
	RepoVisibilityPrivate RepoVisibility = "private"
)

// Right One of the five git rights. Each string is also the TRQP `action` the VTC
// publishes the right under in its Trust Registry, so it is carried verbatim.
// `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and
// `git.repo.maintain` to a repository resource; `git.commit.sign` to either.
type Right string

// Values Right may take, per this specification's schema.
const (
	RightGitNsAdmin      Right = "git.ns.admin"
	RightGitRepoCreate   Right = "git.repo.create"
	RightGitRepoOwn      Right = "git.repo.own"
	RightGitRepoMaintain Right = "git.repo.maintain"
	RightGitCommitSign   Right = "git.commit.sign"
)

// ForgeAccount A person's account on one forge. `id` is authoritative; `login` is for
// display only, because logins can be renamed and re-registered.
type ForgeAccount struct {
	Forge ForgeHost `json:"forge"`
	ID    ForgeID   `json:"id"`

	// The account's current login, as the forge reported it when last seen. Display only.
	Login string `json:"login"`
}

// ForgeID An identifier the forge itself assigns — a repository id, a user or
// organisation id — carried as a string so a forge whose ids are not numbers needs no new
// version. GitHub and Forgejo ids are decimal integers written as strings
// (`"812736451"`). Unlike a name, it survives renames and transfers, which is why rights
// and bindings are keyed by it.
type ForgeID = string

// DID A bare DID in the W3C DID Core syntax (§3.1): `did:`, a method name of lowercase
// letters and digits, `:`, and a method-specific id of colon-separated segments drawn
// from `A-Z a-z 0-9 . - _` and percent-encoded octets, the last segment non-empty. A DID
// URL is not a DID: no path, query or fragment (`/`, `?`, `#`), so a verification-method
// id such as `did:key:z6Mk…#z6Mk…` is refused. Compared by exact string equality — no
// case folding or percent-decoding. A consumer MUST still treat the value as data: the
// pattern keeps shell metacharacters, whitespace and quotes out of the wire form, but it
// does not make a DID safe to splice into a command or markup.
type DID = string

// RepoResource A forge-qualified resource naming exactly one repository:
// `<forge-host>/<owner>/<repo>`, lowercase.
type RepoResource = string

// NamespaceID The VTC's opaque identifier for a namespace, assigned when it is bound.
// Stable for the life of the binding; never reused for another binding.
type NamespaceID = string

// PayloadKind What to do. `projectRoles` — converge forge roles to `desiredRoles`, and
// remove whatever role each of `removeAccounts` holds on `repo`. `createRepo` — create
// `repo`, run the bootstrap plan, and project `desiredRoles`. `bootstrap` — run the
// bootstrap plan, or the listed `steps` of it, on an existing `repo`. `archive` — archive
// `repo`. `inspect` — compare the forge with the projection for `repo`, or for the whole
// namespace. `beginBind` — start the proof that the binding admin controls `target`.
// `beginAccountLink` — start linking `subject`'s account on this namespace's forge.
type PayloadKind string

// Values PayloadKind may take, per this specification's schema.
const (
	PayloadKindProjectRoles     PayloadKind = "projectRoles"
	PayloadKindCreateRepo       PayloadKind = "createRepo"
	PayloadKindBootstrap        PayloadKind = "bootstrap"
	PayloadKindArchive          PayloadKind = "archive"
	PayloadKindInspect          PayloadKind = "inspect"
	PayloadKindBeginBind        PayloadKind = "beginBind"
	PayloadKindBeginAccountLink PayloadKind = "beginAccountLink"
)

// Payload The VTC asks its bridge to do one piece of forge work in a namespace. The job
// names what to do (`kind`) and carries the desired state, never forge credentials — the
// bridge holds those. Which optional members a job carries is fixed by its `kind` (see
// the specification's kind table); a bridge refuses a job that carries a member its kind
// does not use, or lacks one its kind requires.
type Payload struct {
	// The VTC's identifier for this job. The bridge reports on it with git-ns/bridge/result.
	JobID JobID `json:"jobId"`

	// The namespace the job acts in. It also selects the bridge's forge credentials for it.
	Namespace NamespaceID `json:"namespace"`

	// What to do. `projectRoles` — converge forge roles to `desiredRoles`, and remove
	// whatever role each of `removeAccounts` holds on `repo`. `createRepo` — create `repo`,
	// run the bootstrap plan, and project `desiredRoles`. `bootstrap` — run the bootstrap
	// plan, or the listed `steps` of it, on an existing `repo`. `archive` — archive `repo`.
	// `inspect` — compare the forge with the projection for `repo`, or for the whole
	// namespace. `beginBind` — start the proof that the binding admin controls `target`.
	// `beginAccountLink` — start linking `subject`'s account on this namespace's forge.
	Kind PayloadKind `json:"kind"`

	// The repository. Required for `projectRoles`, `createRepo`, `bootstrap` and `archive`:
	// there is no namespace-level `projectRoles`, because no right projects to an
	// organisation's own roles. For `inspect`, absent means a sweep of every repository in
	// the namespace.
	Repo *RepoResource `json:"repo,omitempty"`

	// How to create the repository. `createRepo` only.
	Spec *RepoSpec `json:"spec,omitempty"`

	// The complete set of people the projection reaches on the repository, each at the one
	// right they are projected at. The bridge converges to exactly this set among the roles
	// it manages: it adds or changes roles to match and removes managed roles not listed.
	// Each forge account (by `forge` and `id`) appears at most once. `projectRoles` and
	// `createRepo` only.
	DesiredRoles *[]DesiredRole `json:"desiredRoles,omitempty"`

	// Accounts whose direct role on `repo` the bridge removes, whatever that role is —
	// including a role the bridge does not manage and would otherwise only report as drift.
	// Matched by `forge` and `id`; `login` is display only. No account listed here may also
	// appear in `desiredRoles`, except at `git.ns.admin`. `projectRoles` only.
	RemoveAccounts *[]ForgeAccount `json:"removeAccounts,omitempty"`

	// Run only these steps of the adapter's bootstrap plan, in the plan's order. Absent: the
	// whole plan. `bootstrap` only.
	Steps *[]StepName `json:"steps,omitempty"`

	// `beginBind` only.
	Target *BindTarget `json:"target,omitempty"`

	// The member whose forge account is being linked. `beginAccountLink` only.
	Subject *DID `json:"subject,omitempty"`
	Ext     *Ext `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/git-ns/bridge/job/0.4"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/git-ns/bridge/job/0.4#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/git-ns/bridge/job/0.4\",\"title\":\"Git Namespaces — Bridge Job — payload\",\"description\":\"The VTC asks its bridge to do one piece of forge work in a namespace. The job names what to do (`kind`) and carries the desired state, never forge credentials — the bridge holds those. Which optional members a job carries is fixed by its `kind` (see the specification's kind table); a bridge refuses a job that carries a member its kind does not use, or lacks one its kind requires.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"jobId\",\"namespace\",\"kind\"],\"properties\":{\"jobId\":{\"$ref\":\"#/$defs/JobId\",\"description\":\"The VTC's identifier for this job. The bridge reports on it with git-ns/bridge/result.\"},\"namespace\":{\"$ref\":\"#/$defs/NamespaceId\",\"description\":\"The namespace the job acts in. It also selects the bridge's forge credentials for it.\"},\"kind\":{\"type\":\"string\",\"enum\":[\"projectRoles\",\"createRepo\",\"bootstrap\",\"archive\",\"inspect\",\"beginBind\",\"beginAccountLink\"],\"description\":\"What to do. `projectRoles` — converge forge roles to `desiredRoles`, and remove whatever role each of `removeAccounts` holds on `repo`. `createRepo` — create `repo`, run the bootstrap plan, and project `desiredRoles`. `bootstrap` — run the bootstrap plan, or the listed `steps` of it, on an existing `repo`. `archive` — archive `repo`. `inspect` — compare the forge with the projection for `repo`, or for the whole namespace. `beginBind` — start the proof that the binding admin controls `target`. `beginAccountLink` — start linking `subject`'s account on this namespace's forge.\"},\"repo\":{\"$ref\":\"#/$defs/RepoResource\",\"description\":\"The repository. Required for `projectRoles`, `createRepo`, `bootstrap` and `archive`: there is no namespace-level `projectRoles`, because no right projects to an organisation's own roles. For `inspect`, absent means a sweep of every repository in the namespace.\"},\"spec\":{\"$ref\":\"#/$defs/RepoSpec\",\"description\":\"How to create the repository. `createRepo` only.\"},\"desiredRoles\":{\"type\":\"array\",\"uniqueItems\":true,\"items\":{\"$ref\":\"#/$defs/DesiredRole\"},\"description\":\"The complete set of people the projection reaches on the repository, each at the one right they are projected at. The bridge converges to exactly this set among the roles it manages: it adds or changes roles to match and removes managed roles not listed. Each forge account (by `forge` and `id`) appears at most once. `projectRoles` and `createRepo` only.\"},\"removeAccounts\":{\"type\":\"array\",\"minItems\":1,\"uniqueItems\":true,\"items\":{\"$ref\":\"#/$defs/ForgeAccount\"},\"description\":\"Accounts whose direct role on `repo` the bridge removes, whatever that role is — including a role the bridge does not manage and would otherwise only report as drift. Matched by `forge` and `id`; `login` is display only. No account listed here may also appear in `desiredRoles`, except at `git.ns.admin`. `projectRoles` only.\"},\"steps\":{\"type\":\"array\",\"minItems\":1,\"uniqueItems\":true,\"items\":{\"$ref\":\"#/$defs/StepName\"},\"description\":\"Run only these steps of the adapter's bootstrap plan, in the plan's order. Absent: the whole plan. `bootstrap` only.\"},\"target\":{\"$ref\":\"#/$defs/BindTarget\",\"description\":\"`beginBind` only.\"},\"subject\":{\"$ref\":\"#/$defs/Did\",\"description\":\"The member whose forge account is being linked. `beginAccountLink` only.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"JobId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"The VTC's identifier for a job, unique across the VTC's jobs.\"},\"StepName\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z][A-Za-z0-9]*$\",\"description\":\"One step of a forge adapter's plan. Forge-neutral names: `create`, `workflow`, `keyring`, `variables`, `requiredCheck`, `roles`, `archive`. An adapter MAY add its own (`mergeStyle`, `runner`), which a VTC displays but need not understand.\"},\"DesiredRole\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\",\"account\",\"right\"],\"properties\":{\"subject\":{\"$ref\":\"#/$defs/Did\"},\"account\":{\"$ref\":\"#/$defs/ForgeAccount\",\"description\":\"The subject's linked account on this namespace's forge.\"},\"right\":{\"$ref\":\"#/$defs/Right\",\"description\":\"The highest right recorded for the subject on the repository, or `git.commit.sign` recorded on its namespace — never one held only because `git.ns.admin` implies it. A namespace admin with no right of their own there is listed as `git.ns.admin`, which every forge adapter maps to no role. The forge adapter maps the right to one of its roles.\"}}},\"RepoSpec\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"visibility\"],\"properties\":{\"visibility\":{\"$ref\":\"#/$defs/RepoVisibility\"},\"description\":{\"type\":\"string\",\"maxLength\":350}}},\"BindTarget\":{\"title\":\"BindTarget\",\"description\":\"The forge owner whose control the binding admin is to prove.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"forge\",\"owner\"],\"properties\":{\"forge\":{\"$ref\":\"#/$defs/ForgeHost\"},\"owner\":{\"$ref\":\"#/$defs/Segment\"}}},\"Response\":{\"$anchor\":\"response\",\"title\":\"Git Namespaces — Bridge Job — response payload\",\"description\":\"Whether the bridge took the job.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"jobId\",\"accepted\"],\"properties\":{\"jobId\":{\"$ref\":\"#/$defs/JobId\"},\"accepted\":{\"type\":\"boolean\",\"description\":\"`true` — the job is durably queued, or was already queued or running under this `jobId`; a git-ns/bridge/result follows. `false` — the bridge already finished this `jobId`; it does not run it again, and sends its git-ns/bridge/result again.\"},\"next\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"url\",\"expiresAt\"],\"description\":\"For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC sends the person.\",\"properties\":{\"url\":{\"type\":\"string\",\"format\":\"uri\",\"maxLength\":2048,\"pattern\":\"^https://\"},\"userCode\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The device-flow code, for a forge that uses one.\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Segment\":{\"title\":\"Segment\",\"description\":\"One lowercased owner or repository name. Forges compare these case-insensitively, so the wire form is always lowercase and a producer lowercases before sending. A leading `.` is refused, which rules out `.` and `..`.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":100,\"pattern\":\"^[a-z0-9_-][a-z0-9._-]*$\"},\"ForgeHost\":{\"title\":\"ForgeHost\",\"description\":\"The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No scheme, no port, no path. The host is a segment of every resource, so a right never crosses forges.\",\"type\":\"string\",\"minLength\":3,\"maxLength\":253,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+$\"},\"RepoVisibility\":{\"title\":\"RepoVisibility\",\"type\":\"string\",\"enum\":[\"public\",\"private\"],\"description\":\"Repository visibility on the forge.\"},\"Right\":{\"title\":\"Right\",\"description\":\"One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.\",\"type\":\"string\",\"enum\":[\"git.ns.admin\",\"git.repo.create\",\"git.repo.own\",\"git.repo.maintain\",\"git.commit.sign\"]},\"ForgeAccount\":{\"title\":\"ForgeAccount\",\"description\":\"A person's account on one forge. `id` is authoritative; `login` is for display only, because logins can be renamed and re-registered.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"forge\",\"id\",\"login\"],\"properties\":{\"forge\":{\"$ref\":\"#/$defs/ForgeHost\"},\"id\":{\"$ref\":\"#/$defs/ForgeId\"},\"login\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":100,\"description\":\"The account's current login, as the forge reported it when last seen. Display only.\"}}},\"ForgeId\":{\"title\":\"ForgeId\",\"description\":\"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\\"812736451\\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":64},\"Did\":{\"title\":\"Did\",\"description\":\"A bare DID in the W3C DID Core syntax (§3.1): `did:`, a method name of lowercase letters and digits, `:`, and a method-specific id of colon-separated segments drawn from `A-Z a-z 0-9 . - _` and percent-encoded octets, the last segment non-empty. A DID URL is not a DID: no path, query or fragment (`/`, `?`, `#`), so a verification-method id such as `did:key:z6Mk…#z6Mk…` is refused. Compared by exact string equality — no case folding or percent-decoding. A consumer MUST still treat the value as data: the pattern keeps shell metacharacters, whitespace and quotes out of the wire form, but it does not make a DID safe to splice into a command or markup.\",\"type\":\"string\",\"maxLength\":2048,\"pattern\":\"^did:[a-z0-9]+:(?:(?:[A-Za-z0-9._-]|%[0-9A-Fa-f]{2})*:)*(?:[A-Za-z0-9._-]|%[0-9A-Fa-f]{2})+$\"},\"RepoResource\":{\"title\":\"RepoResource\",\"description\":\"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.\",\"type\":\"string\",\"maxLength\":455,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}$\"},\"NamespaceId\":{\"title\":\"NamespaceId\",\"description\":\"The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":128}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"JobId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"The VTC's identifier for a job, unique across the VTC's jobs.\"},\"StepName\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z][A-Za-z0-9]*$\",\"description\":\"One step of a forge adapter's plan. Forge-neutral names: `create`, `workflow`, `keyring`, `variables`, `requiredCheck`, `roles`, `archive`. An adapter MAY add its own (`mergeStyle`, `runner`), which a VTC displays but need not understand.\"},\"DesiredRole\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\",\"account\",\"right\"],\"properties\":{\"subject\":{\"$ref\":\"#/$defs/Did\"},\"account\":{\"$ref\":\"#/$defs/ForgeAccount\",\"description\":\"The subject's linked account on this namespace's forge.\"},\"right\":{\"$ref\":\"#/$defs/Right\",\"description\":\"The highest right recorded for the subject on the repository, or `git.commit.sign` recorded on its namespace — never one held only because `git.ns.admin` implies it. A namespace admin with no right of their own there is listed as `git.ns.admin`, which every forge adapter maps to no role. The forge adapter maps the right to one of its roles.\"}}},\"RepoSpec\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"visibility\"],\"properties\":{\"visibility\":{\"$ref\":\"#/$defs/RepoVisibility\"},\"description\":{\"type\":\"string\",\"maxLength\":350}}},\"BindTarget\":{\"title\":\"BindTarget\",\"description\":\"The forge owner whose control the binding admin is to prove.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"forge\",\"owner\"],\"properties\":{\"forge\":{\"$ref\":\"#/$defs/ForgeHost\"},\"owner\":{\"$ref\":\"#/$defs/Segment\"}}},\"Response\":{\"$anchor\":\"response\",\"title\":\"Git Namespaces — Bridge Job — response payload\",\"description\":\"Whether the bridge took the job.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"jobId\",\"accepted\"],\"properties\":{\"jobId\":{\"$ref\":\"#/$defs/JobId\"},\"accepted\":{\"type\":\"boolean\",\"description\":\"`true` — the job is durably queued, or was already queued or running under this `jobId`; a git-ns/bridge/result follows. `false` — the bridge already finished this `jobId`; it does not run it again, and sends its git-ns/bridge/result again.\"},\"next\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"url\",\"expiresAt\"],\"description\":\"For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC sends the person.\",\"properties\":{\"url\":{\"type\":\"string\",\"format\":\"uri\",\"maxLength\":2048,\"pattern\":\"^https://\"},\"userCode\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The device-flow code, for a forge that uses one.\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\"}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Segment\":{\"title\":\"Segment\",\"description\":\"One lowercased owner or repository name. Forges compare these case-insensitively, so the wire form is always lowercase and a producer lowercases before sending. A leading `.` is refused, which rules out `.` and `..`.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":100,\"pattern\":\"^[a-z0-9_-][a-z0-9._-]*$\"},\"ForgeHost\":{\"title\":\"ForgeHost\",\"description\":\"The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No scheme, no port, no path. The host is a segment of every resource, so a right never crosses forges.\",\"type\":\"string\",\"minLength\":3,\"maxLength\":253,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+$\"},\"RepoVisibility\":{\"title\":\"RepoVisibility\",\"type\":\"string\",\"enum\":[\"public\",\"private\"],\"description\":\"Repository visibility on the forge.\"},\"Right\":{\"title\":\"Right\",\"description\":\"One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.\",\"type\":\"string\",\"enum\":[\"git.ns.admin\",\"git.repo.create\",\"git.repo.own\",\"git.repo.maintain\",\"git.commit.sign\"]},\"ForgeAccount\":{\"title\":\"ForgeAccount\",\"description\":\"A person's account on one forge. `id` is authoritative; `login` is for display only, because logins can be renamed and re-registered.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"forge\",\"id\",\"login\"],\"properties\":{\"forge\":{\"$ref\":\"#/$defs/ForgeHost\"},\"id\":{\"$ref\":\"#/$defs/ForgeId\"},\"login\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":100,\"description\":\"The account's current login, as the forge reported it when last seen. Display only.\"}}},\"ForgeId\":{\"title\":\"ForgeId\",\"description\":\"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\\"812736451\\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":64},\"Did\":{\"title\":\"Did\",\"description\":\"A bare DID in the W3C DID Core syntax (§3.1): `did:`, a method name of lowercase letters and digits, `:`, and a method-specific id of colon-separated segments drawn from `A-Z a-z 0-9 . - _` and percent-encoded octets, the last segment non-empty. A DID URL is not a DID: no path, query or fragment (`/`, `?`, `#`), so a verification-method id such as `did:key:z6Mk…#z6Mk…` is refused. Compared by exact string equality — no case folding or percent-decoding. A consumer MUST still treat the value as data: the pattern keeps shell metacharacters, whitespace and quotes out of the wire form, but it does not make a DID safe to splice into a command or markup.\",\"type\":\"string\",\"maxLength\":2048,\"pattern\":\"^did:[a-z0-9]+:(?:(?:[A-Za-z0-9._-]|%[0-9A-Fa-f]{2})*:)*(?:[A-Za-z0-9._-]|%[0-9A-Fa-f]{2})+$\"},\"RepoResource\":{\"title\":\"RepoResource\",\"description\":\"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.\",\"type\":\"string\",\"maxLength\":455,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}$\"},\"NamespaceId\":{\"title\":\"NamespaceId\",\"description\":\"The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":128}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
