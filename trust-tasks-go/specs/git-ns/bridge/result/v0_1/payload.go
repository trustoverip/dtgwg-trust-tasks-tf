// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/bridge/result/0.1/payload.schema.json

package gitnsbridgeresultv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The VTC has recorded the result.
type Response struct {
	JobID string `json:"jobId"`
	Ext   *Ext   `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// ForgeID An identifier the forge itself assigns — a repository id, a user or
// organisation id — carried as a string so a forge whose ids are not numbers needs no new
// version. GitHub and Forgejo ids are decimal integers written as strings
// (`"812736451"`). Unlike a name, it survives renames and transfers, which is why rights
// and bindings are keyed by it.
type ForgeID = string

// RepoResource A forge-qualified resource naming exactly one repository:
// `<forge-host>/<owner>/<repo>`, lowercase.
type RepoResource = string

// PayloadOutcome `succeeded` — every step was applied. `failed` — nothing was applied.
// `partial` — some steps were applied and at least one failed; `steps` says which. Every
// step is check-then-apply, so the VTC may send the job again.
type PayloadOutcome string

// Values PayloadOutcome may take, per this specification's schema.
const (
	PayloadOutcomeSucceeded PayloadOutcome = "succeeded"
	PayloadOutcomeFailed    PayloadOutcome = "failed"
	PayloadOutcomePartial   PayloadOutcome = "partial"
)

// PayloadRepo The repository the job touched, as the forge now reports it. Present
// whenever the job reached a repository.
type PayloadRepo struct {
	Resource RepoResource `json:"resource"`
	ForgeID  ForgeID      `json:"forgeId"`
}

// PayloadStepsItemOutcome `applied` — changed the forge. `unchanged` — already as
// required. `failed` — could not be applied. `skipped` — not attempted because an earlier
// step failed.
type PayloadStepsItemOutcome string

// Values PayloadStepsItemOutcome may take, per this specification's schema.
const (
	PayloadStepsItemOutcomeApplied   PayloadStepsItemOutcome = "applied"
	PayloadStepsItemOutcomeUnchanged PayloadStepsItemOutcome = "unchanged"
	PayloadStepsItemOutcomeFailed    PayloadStepsItemOutcome = "failed"
	PayloadStepsItemOutcomeSkipped   PayloadStepsItemOutcome = "skipped"
)

// PayloadStepsItem PayloadStepsItem is a generated payload type.
type PayloadStepsItem struct {
	// The step name, from the adapter's plan.
	Step string `json:"step"`

	// `applied` — changed the forge. `unchanged` — already as required. `failed` — could not
	// be applied. `skipped` — not attempted because an earlier step failed.
	Outcome PayloadStepsItemOutcome `json:"outcome"`

	// Human-readable detail, typically for a failure.
	Detail *string `json:"detail,omitempty"`
}

// PayloadError Why the job failed or stopped partway. Present when `outcome` is not
// `succeeded`.
type PayloadError struct {
	// Forge-neutral: `nameTaken`, `notFound`, `forbidden` (the bridge's forge credentials
	// lack a permission), `notCapable` (this forge or namespace cannot do it), `rateLimited`,
	// `expired` (a begin* job's person never completed it), `forgeError`. A bridge MAY use
	// others.
	Code    string  `json:"code"`
	Message *string `json:"message,omitempty"`
}

// Payload The bridge reports how a job ended. Exactly one result is sent per job; the VTC
// updates the repository's state, bootstrap status and forge id from it.
type Payload struct {
	// The job, as git-ns/bridge/job named it.
	JobID string `json:"jobId"`

	// `succeeded` — every step was applied. `failed` — nothing was applied. `partial` — some
	// steps were applied and at least one failed; `steps` says which. Every step is
	// check-then-apply, so the VTC may send the job again.
	Outcome PayloadOutcome `json:"outcome"`

	// The repository the job touched, as the forge now reports it. Present whenever the job
	// reached a repository.
	Repo *PayloadRepo `json:"repo,omitempty"`

	// Per-step outcomes, in the order they ran.
	Steps *[]PayloadStepsItem `json:"steps,omitempty"`

	// Why the job failed or stopped partway. Present when `outcome` is not `succeeded`.
	Error *PayloadError `json:"error,omitempty"`
	Ext   *Ext          `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/git-ns/bridge/result/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/git-ns/bridge/result/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/git-ns/bridge/result/0.1\",\"title\":\"Git Namespaces — Bridge Job Result — payload\",\"description\":\"The bridge reports how a job ended. Exactly one result is sent per job; the VTC updates the repository's state, bootstrap status and forge id from it.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"jobId\",\"outcome\"],\"properties\":{\"jobId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"The job, as git-ns/bridge/job named it.\"},\"outcome\":{\"type\":\"string\",\"enum\":[\"succeeded\",\"failed\",\"partial\"],\"description\":\"`succeeded` — every step was applied. `failed` — nothing was applied. `partial` — some steps were applied and at least one failed; `steps` says which. Every step is check-then-apply, so the VTC may send the job again.\"},\"repo\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"resource\",\"forgeId\"],\"description\":\"The repository the job touched, as the forge now reports it. Present whenever the job reached a repository.\",\"properties\":{\"resource\":{\"$ref\":\"#/$defs/RepoResource\"},\"forgeId\":{\"$ref\":\"#/$defs/ForgeId\"}}},\"steps\":{\"type\":\"array\",\"items\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"step\",\"outcome\"],\"properties\":{\"step\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z][A-Za-z0-9]*$\",\"description\":\"The step name, from the adapter's plan.\"},\"outcome\":{\"type\":\"string\",\"enum\":[\"applied\",\"unchanged\",\"failed\",\"skipped\"],\"description\":\"`applied` — changed the forge. `unchanged` — already as required. `failed` — could not be applied. `skipped` — not attempted because an earlier step failed.\"},\"detail\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Human-readable detail, typically for a failure.\"}}},\"description\":\"Per-step outcomes, in the order they ran.\"},\"error\":{\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"code\"],\"description\":\"Why the job failed or stopped partway. Present when `outcome` is not `succeeded`.\",\"properties\":{\"code\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z][A-Za-z0-9]*$\",\"description\":\"Forge-neutral: `nameTaken`, `notFound`, `forbidden` (the bridge's forge credentials lack a permission), `notCapable` (this forge or namespace cannot do it), `rateLimited`, `expired` (a begin* job's person never completed it), `forgeError`. A bridge MAY use others.\"},\"message\":{\"type\":\"string\",\"maxLength\":1024}}},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Git Namespaces — Bridge Job Result — response payload\",\"description\":\"The VTC has recorded the result.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"jobId\"],\"properties\":{\"jobId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"ForgeId\":{\"title\":\"ForgeId\",\"description\":\"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\\"812736451\\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":64},\"RepoResource\":{\"title\":\"RepoResource\",\"description\":\"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.\",\"type\":\"string\",\"maxLength\":455,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}$\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Git Namespaces — Bridge Job Result — response payload\",\"description\":\"The VTC has recorded the result.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"jobId\"],\"properties\":{\"jobId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"ForgeId\":{\"title\":\"ForgeId\",\"description\":\"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\\"812736451\\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.\",\"type\":\"string\",\"minLength\":1,\"maxLength\":64},\"RepoResource\":{\"title\":\"RepoResource\",\"description\":\"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.\",\"type\":\"string\",\"maxLength\":455,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}$\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
