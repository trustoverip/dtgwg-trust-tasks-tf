// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/right/grant/0.1/payload.schema.json

package gitnsrightgrantv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The recorded right.
type Response struct {
	// The recorded right — newly recorded, or the live record that already existed.
	Right RightRecord `json:"right"`
	Ext   *Ext        `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// RightRecord One recorded right. Implied rights (§4.2 of the rights model: `own` implies
// `maintain` implies `commit.sign` on the same resource; `ns.admin` implies `repo.create`
// and `own` across its namespace) are not records and never appear as RightRecords.
type RightRecord struct {
	// Who holds the right. For `git.commit.sign` this is the DID whose commit signatures the
	// CI check accepts.
	Subject  DID      `json:"subject"`
	Right    Right    `json:"right"`
	Resource Resource `json:"resource"`

	// The actor whose task caused the right: the granter, the creator of a repository (for
	// its first `own`), the adopting admin, the transferring owner, or the binding admin (for
	// the first `git.ns.admin`). The VTC's own DID for a right it derives from its
	// configuration.
	GrantedBy DID    `json:"grantedBy"`
	GrantedAt string `json:"grantedAt"`

	// When the right lapses. Absent: no expiry.
	ExpiresAt *string `json:"expiresAt,omitempty"`

	// The granter's free-text reason. Disclosed only to holders of `git.repo.own` on the
	// resource and of `git.ns.admin` over it.
	Reason *string `json:"reason,omitempty"`
}

// DID A DID, compared by exact string equality.
type DID = string

// Resource A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or
// `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`,
// `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied:
// `acme/widgets` alone is not a resource. Containment is by whole segment:
// `github.com/acme` contains `github.com/acme/widgets` and does not contain
// `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.
type Resource = string

// Right One of the five git rights. Each string is also the TRQP `action` the VTC
// publishes the right under in its Trust Registry, so it is carried verbatim.
// `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and
// `git.repo.maintain` to a repository resource; `git.commit.sign` to either.
type Right string

// Values Right may take, per this specification's schema.
const (
	RightGitNsAdmin      Right = "git.ns.admin"
	RightGitRepoCreate   Right = "git.repo.create"
	RightGitRepoOwn      Right = "git.repo.own"
	RightGitRepoMaintain Right = "git.repo.maintain"
	RightGitCommitSign   Right = "git.commit.sign"
)

// Payload Grants one git right on one forge-qualified resource to a DID. The VTC checks
// the grant against the actor's own rights — scope containment, no escalation,
// members-only namespace rights — and against community policy, records it, and publishes
// it to the Trust Registry.
type Payload struct {
	// Who receives the right.
	Subject DID   `json:"subject"`
	Right   Right `json:"right"`

	// Where the right applies: a namespace resource for `git.ns.admin` and `git.repo.create`,
	// a repository resource for `git.repo.own` and `git.repo.maintain`, either for
	// `git.commit.sign`.
	Resource Resource `json:"resource"`

	// When the right lapses. Absent: it lasts until revoked.
	ExpiresAt *string `json:"expiresAt,omitempty"`

	// Why, in the granter's words. Recorded with the right and disclosed only to the
	// resource's owners and namespace admins.
	Reason *string `json:"reason,omitempty"`
	Ext    *Ext    `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/git-ns/right/grant/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/git-ns/right/grant/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/git-ns/right/grant/0.1\",\"title\":\"Git Namespaces — Grant Right — payload\",\"description\":\"Grants one git right on one forge-qualified resource to a DID. The VTC checks the grant against the actor's own rights — scope containment, no escalation, members-only namespace rights — and against community policy, records it, and publishes it to the Trust Registry.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\",\"right\",\"resource\"],\"properties\":{\"subject\":{\"$ref\":\"#/$defs/Did\",\"description\":\"Who receives the right.\"},\"right\":{\"$ref\":\"#/$defs/Right\"},\"resource\":{\"$ref\":\"#/$defs/Resource\",\"description\":\"Where the right applies: a namespace resource for `git.ns.admin` and `git.repo.create`, a repository resource for `git.repo.own` and `git.repo.maintain`, either for `git.commit.sign`.\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the right lapses. Absent: it lasts until revoked.\"},\"reason\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Why, in the granter's words. Recorded with the right and disclosed only to the resource's owners and namespace admins.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Git Namespaces — Grant Right — response payload\",\"description\":\"The recorded right.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"right\"],\"properties\":{\"right\":{\"$ref\":\"#/$defs/RightRecord\",\"description\":\"The recorded right — newly recorded, or the live record that already existed.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"RightRecord\":{\"title\":\"RightRecord\",\"description\":\"One recorded right. Implied rights (§4.2 of the rights model: `own` implies `maintain` implies `commit.sign` on the same resource; `ns.admin` implies `repo.create` and `own` across its namespace) are not records and never appear as RightRecords.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\",\"right\",\"resource\",\"grantedBy\",\"grantedAt\"],\"properties\":{\"subject\":{\"$ref\":\"#/$defs/Did\",\"description\":\"Who holds the right. For `git.commit.sign` this is the DID whose commit signatures the CI check accepts.\"},\"right\":{\"$ref\":\"#/$defs/Right\"},\"resource\":{\"$ref\":\"#/$defs/Resource\"},\"grantedBy\":{\"$ref\":\"#/$defs/Did\",\"description\":\"The actor whose task caused the right: the granter, the creator of a repository (for its first `own`), the adopting admin, the transferring owner, or the binding admin (for the first `git.ns.admin`). The VTC's own DID for a right it derives from its configuration.\"},\"grantedAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the right lapses. Absent: no expiry.\"},\"reason\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"The granter's free-text reason. Disclosed only to holders of `git.repo.own` on the resource and of `git.ns.admin` over it.\"}}},\"Did\":{\"title\":\"Did\",\"description\":\"A DID, compared by exact string equality.\",\"type\":\"string\",\"maxLength\":2048,\"pattern\":\"^did:[a-z0-9]+:\\\\S+$\"},\"Resource\":{\"title\":\"Resource\",\"description\":\"A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`, `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied: `acme/widgets` alone is not a resource. Containment is by whole segment: `github.com/acme` contains `github.com/acme/widgets` and does not contain `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.\",\"type\":\"string\",\"maxLength\":455,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+(?:/[a-z0-9_-][a-z0-9._-]{0,99}){1,2}$\"},\"Right\":{\"title\":\"Right\",\"description\":\"One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.\",\"type\":\"string\",\"enum\":[\"git.ns.admin\",\"git.repo.create\",\"git.repo.own\",\"git.repo.maintain\",\"git.commit.sign\"]}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"Git Namespaces — Grant Right — response payload\",\"description\":\"The recorded right.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"right\"],\"properties\":{\"right\":{\"$ref\":\"#/$defs/RightRecord\",\"description\":\"The recorded right — newly recorded, or the live record that already existed.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"RightRecord\":{\"title\":\"RightRecord\",\"description\":\"One recorded right. Implied rights (§4.2 of the rights model: `own` implies `maintain` implies `commit.sign` on the same resource; `ns.admin` implies `repo.create` and `own` across its namespace) are not records and never appear as RightRecords.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subject\",\"right\",\"resource\",\"grantedBy\",\"grantedAt\"],\"properties\":{\"subject\":{\"$ref\":\"#/$defs/Did\",\"description\":\"Who holds the right. For `git.commit.sign` this is the DID whose commit signatures the CI check accepts.\"},\"right\":{\"$ref\":\"#/$defs/Right\"},\"resource\":{\"$ref\":\"#/$defs/Resource\"},\"grantedBy\":{\"$ref\":\"#/$defs/Did\",\"description\":\"The actor whose task caused the right: the granter, the creator of a repository (for its first `own`), the adopting admin, the transferring owner, or the binding admin (for the first `git.ns.admin`). The VTC's own DID for a right it derives from its configuration.\"},\"grantedAt\":{\"type\":\"string\",\"format\":\"date-time\"},\"expiresAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the right lapses. Absent: no expiry.\"},\"reason\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"The granter's free-text reason. Disclosed only to holders of `git.repo.own` on the resource and of `git.ns.admin` over it.\"}}},\"Did\":{\"title\":\"Did\",\"description\":\"A DID, compared by exact string equality.\",\"type\":\"string\",\"maxLength\":2048,\"pattern\":\"^did:[a-z0-9]+:\\\\S+$\"},\"Resource\":{\"title\":\"Resource\",\"description\":\"A forge-qualified resource: `<forge-host>/<owner>` for a namespace, or `<forge-host>/<owner>/<repo>` for one repository, all lowercase — `github.com/acme`, `github.com/acme/widgets`, `codeberg.org/acme`. The forge is never implied: `acme/widgets` alone is not a resource. Containment is by whole segment: `github.com/acme` contains `github.com/acme/widgets` and does not contain `github.com/acme-labs/x` or `codeberg.org/acme/widgets`.\",\"type\":\"string\",\"maxLength\":455,\"pattern\":\"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+(?:/[a-z0-9_-][a-z0-9._-]{0,99}){1,2}$\"},\"Right\":{\"title\":\"Right\",\"description\":\"One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.\",\"type\":\"string\",\"enum\":[\"git.ns.admin\",\"git.repo.create\",\"git.repo.own\",\"git.repo.maintain\",\"git.commit.sign\"]}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
