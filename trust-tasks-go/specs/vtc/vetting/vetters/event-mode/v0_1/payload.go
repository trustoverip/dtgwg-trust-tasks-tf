// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/vetting/vetters/event-mode/0.1/payload.schema.json

package vtcvettingvetterseventmodev0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Window The days the event runs. Dates, not timestamps: an hour would say when a vetter
// expects to be at a desk, and the tokens are day-scale anyway.
type Window struct {
	// First day of the event, inclusive.
	StartDate string `json:"startDate"`

	// Last day of the event, inclusive. It is not before `startDate`.
	EndDate string `json:"endDate"`
}

// ResponseState `pending`: recorded, awaiting an approver and the group floor.
// `approved`: the event's label is live and this vetter may draw under it.
type ResponseState string

// Values ResponseState may take, per this specification's schema.
const (
	ResponseStatePending  ResponseState = "pending"
	ResponseStateApproved ResponseState = "approved"
)

// Response Where the request stands. `pending` is the ordinary answer to a first request
// and is not a refusal — a refusal is a `trust-task-error`.
type Response struct {
	// The event the request names.
	EventID string `json:"eventId"`

	// `pending`: recorded, awaiting an approver and the group floor. `approved`: the event's
	// label is live and this vetter may draw under it.
	State ResponseState `json:"state"`

	// The tier recorded for this vetter at this event.
	Tier   string `json:"tier"`
	Window Window `json:"window"`

	// How many vetters have asked for this event so far, including this one. A count, never a
	// list: who else is at the event is the anonymity set, and naming its members would hand
	// out the very thing the set exists to hide.
	GroupSize int64 `json:"groupSize"`

	// How many vetters this community requires before an event may be approved at all. A
	// one-vetter event is an event key with one holder, which is a name.
	GroupFloor int64 `json:"groupFloor"`

	// Present when `state` is `approved`: the token label to draw under, for
	// `vtc/vetting/vetters/pcs-tokens`.
	Label *string `json:"label,omitempty"`

	// Present when `state` is `approved`: how many tokens a tick under this label yields. It
	// is the tier's rate, and it is the community's cap, not the vetter's request.
	DripPerTick *int64 `json:"dripPerTick,omitempty"`

	// Present when `state` is `approved`: the last day this label will be accepted. It is
	// after `window.endDate` by the community's grace period, so an applicant met on the
	// final day still has time to submit.
	ClosesAfter *string `json:"closesAfter,omitempty"`
	Ext         *Ext    `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload A vetter asks to vet at a named event, at one of the rates the community
// publishes. The community answers where the request stands; it does not answer whether
// it was granted, because a vetter cannot raise its own cap and the group it would join
// may not exist yet.
type Payload struct {
	// The event, as the community names it. It is the whole anonymity set for anything spent
	// under this event's tokens, so it names a gathering people attend — never one desk, one
	// day or one vetter.
	EventID string `json:"eventId"`

	// One of the tiers the community publishes. A tier is picked from a menu rather than a
	// number being named, so that no vetter's requested rate is itself a distinguishing
	// detail.
	Tier   string `json:"tier"`
	Window Window `json:"window"`
	Ext    *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vtc/vetting/vetters/event-mode/0.1\",\"title\":\"VTC — Vetting Event Mode — payload\",\"description\":\"A vetter asks to vet at a named event, at one of the rates the community publishes. The community answers where the request stands; it does not answer whether it was granted, because a vetter cannot raise its own cap and the group it would join may not exist yet.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"eventId\",\"tier\",\"window\"],\"properties\":{\"eventId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"pattern\":\"^[a-z0-9]([a-z0-9-]*[a-z0-9])?$\",\"description\":\"The event, as the community names it. It is the whole anonymity set for anything spent under this event's tokens, so it names a gathering people attend — never one desk, one day or one vetter.\"},\"tier\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"One of the tiers the community publishes. A tier is picked from a menu rather than a number being named, so that no vetter's requested rate is itself a distinguishing detail.\"},\"window\":{\"$ref\":\"#/$defs/Window\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Window\":{\"title\":\"Event window\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"startDate\",\"endDate\"],\"description\":\"The days the event runs. Dates, not timestamps: an hour would say when a vetter expects to be at a desk, and the tokens are day-scale anyway.\",\"properties\":{\"startDate\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"First day of the event, inclusive.\"},\"endDate\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"Last day of the event, inclusive. It is not before `startDate`.\"}}},\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Vetting Event Mode — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"eventId\",\"state\",\"tier\",\"window\",\"groupSize\",\"groupFloor\"],\"description\":\"Where the request stands. `pending` is the ordinary answer to a first request and is not a refusal — a refusal is a `trust-task-error`.\",\"properties\":{\"eventId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The event the request names.\"},\"state\":{\"type\":\"string\",\"enum\":[\"pending\",\"approved\"],\"description\":\"`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.\"},\"tier\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The tier recorded for this vetter at this event.\"},\"window\":{\"$ref\":\"#/$defs/Window\"},\"groupSize\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many vetters have asked for this event so far, including this one. A count, never a list: who else is at the event is the anonymity set, and naming its members would hand out the very thing the set exists to hide.\"},\"groupFloor\":{\"type\":\"integer\",\"minimum\":2,\"description\":\"How many vetters this community requires before an event may be approved at all. A one-vetter event is an event key with one holder, which is a name.\"},\"label\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.\"},\"dripPerTick\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"Present when `state` is `approved`: how many tokens a tick under this label yields. It is the tier's rate, and it is the community's cap, not the vetter's request.\"},\"closesAfter\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"Present when `state` is `approved`: the last day this label will be accepted. It is after `window.endDate` by the community's grace period, so an applicant met on the final day still has time to submit.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Window\":{\"title\":\"Event window\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"startDate\",\"endDate\"],\"description\":\"The days the event runs. Dates, not timestamps: an hour would say when a vetter expects to be at a desk, and the tokens are day-scale anyway.\",\"properties\":{\"startDate\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"First day of the event, inclusive.\"},\"endDate\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"Last day of the event, inclusive. It is not before `startDate`.\"}}},\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Vetting Event Mode — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"eventId\",\"state\",\"tier\",\"window\",\"groupSize\",\"groupFloor\"],\"description\":\"Where the request stands. `pending` is the ordinary answer to a first request and is not a refusal — a refusal is a `trust-task-error`.\",\"properties\":{\"eventId\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The event the request names.\"},\"state\":{\"type\":\"string\",\"enum\":[\"pending\",\"approved\"],\"description\":\"`pending`: recorded, awaiting an approver and the group floor. `approved`: the event's label is live and this vetter may draw under it.\"},\"tier\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":64,\"description\":\"The tier recorded for this vetter at this event.\"},\"window\":{\"$ref\":\"#/$defs/Window\"},\"groupSize\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"How many vetters have asked for this event so far, including this one. A count, never a list: who else is at the event is the anonymity set, and naming its members would hand out the very thing the set exists to hide.\"},\"groupFloor\":{\"type\":\"integer\",\"minimum\":2,\"description\":\"How many vetters this community requires before an event may be approved at all. A one-vetter event is an event key with one holder, which is a name.\"},\"label\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":128,\"description\":\"Present when `state` is `approved`: the token label to draw under, for `vtc/vetting/vetters/pcs-tokens`.\"},\"dripPerTick\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"Present when `state` is `approved`: how many tokens a tick under this label yields. It is the tier's rate, and it is the community's cap, not the vetter's request.\"},\"closesAfter\":{\"type\":\"string\",\"format\":\"date\",\"description\":\"Present when `state` is `approved`: the last day this label will be accepted. It is after `window.endDate` by the community's grace period, so an applicant met on the final day still has time to submit.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
