// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/join-requests/withdraw/0.1/payload.schema.json

package vtcjoinrequestswithdrawv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// ResponseStatus The request's state after the withdrawal. Always `withdrawn`; returned
// rather than implied so the response is self-describing to a reader holding only this
// document.
type ResponseStatus string

// Values ResponseStatus may take, per this specification's schema.
const (
	ResponseStatusWithdrawn ResponseStatus = "withdrawn"
)

// Response Confirms which request was closed. Failures use trust-task-error, not this
// shape.
type Response struct {
	// The request that was withdrawn. Returned even when the request omitted it, so an
	// applicant who used the id-less form learns which request they closed.
	RequestID string `json:"requestId"`

	// The request's state after the withdrawal. Always `withdrawn`; returned rather than
	// implied so the response is self-describing to a reader holding only this document.
	Status ResponseStatus `json:"status"`
	Ext    *Ext           `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Payload What an applicant sends to close their own open join request. Every member is
// optional: the authenticated issuer identifies the applicant, and a community that
// allows at most one open request per applicant can resolve the request from that alone.
// The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof)
// are owned by the framework — SPEC §6.3.
type Payload struct {
	// The request to withdraw. Optional: an applicant whose submit response was lost never
	// received an id, and the id-less form — resolved from the authenticated applicant's own
	// identifier — is the only one available to them. Supply it when you have it; a consumer
	// that has it MUST prefer it over inferring the request from the caller.
	RequestID *string `json:"requestId,omitempty"`

	// Free text for the humans who will read the community's record of this withdrawal — a
	// vetter who attested, or a maintainer mid-review. A consumer MUST NOT interpret it or
	// branch on its content. A producer MUST NOT put credentials, secrets, or personal data
	// about anyone other than the applicant into it.
	Reason *string `json:"reason,omitempty"`
	Ext    *Ext    `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vtc/join-requests/withdraw/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vtc/join-requests/withdraw/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vtc/join-requests/withdraw/0.1\",\"title\":\"VTC Join-Requests — Withdraw — payload\",\"description\":\"What an applicant sends to close their own open join request. Every member is optional: the authenticated issuer identifies the applicant, and a community that allows at most one open request per applicant can resolve the request from that alone. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.\",\"type\":\"object\",\"additionalProperties\":false,\"properties\":{\"requestId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The request to withdraw. Optional: an applicant whose submit response was lost never received an id, and the id-less form — resolved from the authenticated applicant's own identifier — is the only one available to them. Supply it when you have it; a consumer that has it MUST prefer it over inferring the request from the caller.\"},\"reason\":{\"type\":\"string\",\"maxLength\":1024,\"description\":\"Free text for the humans who will read the community's record of this withdrawal — a vetter who attested, or a maintainer mid-review. A consumer MUST NOT interpret it or branch on its content. A producer MUST NOT put credentials, secrets, or personal data about anyone other than the applicant into it.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Join-Requests — Withdraw — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"requestId\",\"status\"],\"description\":\"Confirms which request was closed. Failures use trust-task-error, not this shape.\",\"properties\":{\"requestId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The request that was withdrawn. Returned even when the request omitted it, so an applicant who used the id-less form learns which request they closed.\"},\"status\":{\"type\":\"string\",\"enum\":[\"withdrawn\"],\"description\":\"The request's state after the withdrawal. Always `withdrawn`; returned rather than implied so the response is self-describing to a reader holding only this document.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Join-Requests — Withdraw — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"requestId\",\"status\"],\"description\":\"Confirms which request was closed. Failures use trust-task-error, not this shape.\",\"properties\":{\"requestId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The request that was withdrawn. Returned even when the request omitted it, so an applicant who used the id-less form learns which request they closed.\"},\"status\":{\"type\":\"string\",\"enum\":[\"withdrawn\"],\"description\":\"The request's state after the withdrawal. Always `withdrawn`; returned rather than implied so the response is self-describing to a reader holding only this document.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
