// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/join-requests/supplement/0.1/payload.schema.json

package vtcjoinrequestssupplementv0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response The community's fresh decision on the re-presented evidence. Failures use
// trust-task-error, not this shape.
type Response struct {
	// The request that was re-decided (a UUID). Returned even when the request omitted it, so
	// an applicant who used the id-less form learns which request they answered.
	RequestID string `json:"requestId"`

	// What the community decided on the new evidence, in the same shape
	// `vtc/join-requests/submit` returns. MAY be any outcome a submission can produce,
	// including a further request for more evidence and including a refusal.
	Verdict Verdict `json:"verdict"`
	Ext     *Ext    `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Verdict A ceremony decision: the effect, plus its effect-dependent detail.
type Verdict struct {
	Effect VerdictEffect `json:"effect"`
	With   VerdictWith   `json:"with"`
}

// VerdictWith The effect-dependent detail of a verdict. Every member is optional at the
// schema level and which ones are meaningful depends on `effect`: `role` / `obligations`
// / `bundleRef` on `allow`, `code` / `reason` on `deny`, `queue` / `reason` on `refer`,
// `needs` / `presentationDefinition` on `requestMore`. The dependency is stated here
// rather than enforced by `if`/`then` per effect, so that the shape stays a single flat
// object a generated type can carry without a discriminated union per family — a
// deliberate trade of schema strictness for implementability, and the reason a consumer
// MUST branch on `effect` rather than on which members happen to be present.
type VerdictWith struct {
	// The granted local role. `allow` only.
	Role *string `json:"role,omitempty"`

	// Conditions attached to the grant. `allow` only.
	Obligations *map[string]json.RawMessage `json:"obligations,omitempty"`

	// Pointer to a sealed credential bundle, added by the community where issuance occurred
	// rather than emitted by the policy. `allow` only.
	BundleRef *map[string]json.RawMessage `json:"bundleRef,omitempty"`

	// Stable refusal code, safe to branch on. `deny` only.
	Code *string `json:"code,omitempty"`

	// Elaboration in prose, when the decider gave one. `deny` and `refer`.
	Reason *string `json:"reason,omitempty"`

	// Which review queue the decision was parked in, so an applicant can be told who now
	// holds it. `refer` only.
	Queue *string `json:"queue,omitempty"`

	// What further evidence is required, named so the applicant can act without a support
	// conversation. `requestMore` only.
	Needs *[]string `json:"needs,omitempty"`

	// A machine-readable statement of the same request, so a wallet can satisfy it without a
	// human reading `needs`. `requestMore` only.
	PresentationDefinition *map[string]json.RawMessage `json:"presentationDefinition,omitempty"`
}

// VerdictEffect What the policy decided. `allow` — admitted. `deny` — refused, terminally
// for this submission. `refer` — parked for a human or quorum decision; the applicant is
// neither in nor out. `requestMore` — the policy cannot decide yet and names what further
// evidence it needs. The four are not reducible to a pending/decided pair. `refer` and
// `requestMore` are both 'not decided', but they place the next action with different
// parties: `refer` waits on the community, `requestMore` waits on the applicant. A
// consumer that cannot tell them apart cannot tell a user whether to wait or to act.
type VerdictEffect string

// Values VerdictEffect may take, per this specification's schema.
const (
	VerdictEffectAllow       VerdictEffect = "allow"
	VerdictEffectDeny        VerdictEffect = "deny"
	VerdictEffectRefer       VerdictEffect = "refer"
	VerdictEffectRequestMore VerdictEffect = "requestMore"
)

// Payload The evidence an applicant offers in answer to a community's request for more,
// against the join request they already have open. The outer document members (id, type,
// issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.
type Payload struct {
	// The Verifiable Presentation satisfying the `presentationDefinition` the community
	// returned when it deferred. It REPLACES the presentation on the request rather than
	// adding to it, so it carries everything the community requires and not only the
	// shortfall — see the spec's Definitions.
	VP map[string]json.RawMessage `json:"vp"`

	// The request to supplement. OPTIONAL: an applicant whose submit response was lost never
	// received one, and the consumer then resolves the request from the authenticated
	// applicant. A consumer given this value MUST prefer it over inferring the request from
	// the caller.
	RequestID *string `json:"requestId,omitempty"`

	// An opaque bag the community stores verbatim and this task does not interpret. Replaces
	// the bag on the request on the same terms as `vp`.
	Extensions *map[string]json.RawMessage `json:"extensions,omitempty"`
	Ext        *Ext                        `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vtc/join-requests/supplement/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vtc/join-requests/supplement/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vtc/join-requests/supplement/0.1\",\"title\":\"VTC Join-Requests Supplement — payload\",\"description\":\"The evidence an applicant offers in answer to a community's request for more, against the join request they already have open. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"vp\"],\"properties\":{\"vp\":{\"type\":\"object\",\"description\":\"The Verifiable Presentation satisfying the `presentationDefinition` the community returned when it deferred. It REPLACES the presentation on the request rather than adding to it, so it carries everything the community requires and not only the shortfall — see the spec's Definitions.\"},\"requestId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The request to supplement. OPTIONAL: an applicant whose submit response was lost never received one, and the consumer then resolves the request from the authenticated applicant. A consumer given this value MUST prefer it over inferring the request from the caller.\"},\"extensions\":{\"type\":\"object\",\"description\":\"An opaque bag the community stores verbatim and this task does not interpret. Replaces the bag on the request on the same terms as `vp`.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Join-Requests Supplement — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"requestId\",\"verdict\"],\"description\":\"The community's fresh decision on the re-presented evidence. Failures use trust-task-error, not this shape.\",\"properties\":{\"requestId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The request that was re-decided (a UUID). Returned even when the request omitted it, so an applicant who used the id-less form learns which request they answered.\"},\"verdict\":{\"$ref\":\"#/$defs/Verdict\",\"description\":\"What the community decided on the new evidence, in the same shape `vtc/join-requests/submit` returns. MAY be any outcome a submission can produce, including a further request for more evidence and including a refusal.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Verdict\":{\"$anchor\":\"verdict\",\"title\":\"Verdict\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"effect\",\"with\"],\"description\":\"A ceremony decision: the effect, plus its effect-dependent detail.\",\"properties\":{\"effect\":{\"$ref\":\"#/$defs/VerdictEffect\"},\"with\":{\"$ref\":\"#/$defs/VerdictWith\"}}},\"VerdictWith\":{\"$anchor\":\"verdictWith\",\"title\":\"VerdictWith\",\"type\":\"object\",\"additionalProperties\":false,\"description\":\"The effect-dependent detail of a verdict.\\n\\nEvery member is optional at the schema level and which ones are meaningful depends on `effect`: `role` / `obligations` / `bundleRef` on `allow`, `code` / `reason` on `deny`, `queue` / `reason` on `refer`, `needs` / `presentationDefinition` on `requestMore`. The dependency is stated here rather than enforced by `if`/`then` per effect, so that the shape stays a single flat object a generated type can carry without a discriminated union per family — a deliberate trade of schema strictness for implementability, and the reason a consumer MUST branch on `effect` rather than on which members happen to be present.\",\"properties\":{\"role\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The granted local role. `allow` only.\"},\"obligations\":{\"type\":\"object\",\"description\":\"Conditions attached to the grant. `allow` only.\"},\"bundleRef\":{\"type\":\"object\",\"description\":\"Pointer to a sealed credential bundle, added by the community where issuance occurred rather than emitted by the policy. `allow` only.\"},\"code\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Stable refusal code, safe to branch on. `deny` only.\"},\"reason\":{\"type\":[\"string\",\"null\"],\"maxLength\":1024,\"description\":\"Elaboration in prose, when the decider gave one. `deny` and `refer`.\"},\"queue\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Which review queue the decision was parked in, so an applicant can be told who now holds it. `refer` only.\"},\"needs\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"minLength\":1},\"description\":\"What further evidence is required, named so the applicant can act without a support conversation. `requestMore` only.\"},\"presentationDefinition\":{\"type\":\"object\",\"description\":\"A machine-readable statement of the same request, so a wallet can satisfy it without a human reading `needs`. `requestMore` only.\"}}},\"VerdictEffect\":{\"$anchor\":\"verdictEffect\",\"title\":\"VerdictEffect\",\"type\":\"string\",\"enum\":[\"allow\",\"deny\",\"refer\",\"requestMore\"],\"description\":\"What the policy decided.\\n\\n`allow` — admitted. `deny` — refused, terminally for this submission. `refer` — parked for a human or quorum decision; the applicant is neither in nor out. `requestMore` — the policy cannot decide yet and names what further evidence it needs.\\n\\nThe four are not reducible to a pending/decided pair. `refer` and `requestMore` are both 'not decided', but they place the next action with different parties: `refer` waits on the community, `requestMore` waits on the applicant. A consumer that cannot tell them apart cannot tell a user whether to wait or to act.\"}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Join-Requests Supplement — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"requestId\",\"verdict\"],\"description\":\"The community's fresh decision on the re-presented evidence. Failures use trust-task-error, not this shape.\",\"properties\":{\"requestId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The request that was re-decided (a UUID). Returned even when the request omitted it, so an applicant who used the id-less form learns which request they answered.\"},\"verdict\":{\"$ref\":\"#/$defs/Verdict\",\"description\":\"What the community decided on the new evidence, in the same shape `vtc/join-requests/submit` returns. MAY be any outcome a submission can produce, including a further request for more evidence and including a refusal.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Verdict\":{\"$anchor\":\"verdict\",\"title\":\"Verdict\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"effect\",\"with\"],\"description\":\"A ceremony decision: the effect, plus its effect-dependent detail.\",\"properties\":{\"effect\":{\"$ref\":\"#/$defs/VerdictEffect\"},\"with\":{\"$ref\":\"#/$defs/VerdictWith\"}}},\"VerdictWith\":{\"$anchor\":\"verdictWith\",\"title\":\"VerdictWith\",\"type\":\"object\",\"additionalProperties\":false,\"description\":\"The effect-dependent detail of a verdict.\\n\\nEvery member is optional at the schema level and which ones are meaningful depends on `effect`: `role` / `obligations` / `bundleRef` on `allow`, `code` / `reason` on `deny`, `queue` / `reason` on `refer`, `needs` / `presentationDefinition` on `requestMore`. The dependency is stated here rather than enforced by `if`/`then` per effect, so that the shape stays a single flat object a generated type can carry without a discriminated union per family — a deliberate trade of schema strictness for implementability, and the reason a consumer MUST branch on `effect` rather than on which members happen to be present.\",\"properties\":{\"role\":{\"type\":\"string\",\"minLength\":1,\"description\":\"The granted local role. `allow` only.\"},\"obligations\":{\"type\":\"object\",\"description\":\"Conditions attached to the grant. `allow` only.\"},\"bundleRef\":{\"type\":\"object\",\"description\":\"Pointer to a sealed credential bundle, added by the community where issuance occurred rather than emitted by the policy. `allow` only.\"},\"code\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Stable refusal code, safe to branch on. `deny` only.\"},\"reason\":{\"type\":[\"string\",\"null\"],\"maxLength\":1024,\"description\":\"Elaboration in prose, when the decider gave one. `deny` and `refer`.\"},\"queue\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Which review queue the decision was parked in, so an applicant can be told who now holds it. `refer` only.\"},\"needs\":{\"type\":\"array\",\"items\":{\"type\":\"string\",\"minLength\":1},\"description\":\"What further evidence is required, named so the applicant can act without a support conversation. `requestMore` only.\"},\"presentationDefinition\":{\"type\":\"object\",\"description\":\"A machine-readable statement of the same request, so a wallet can satisfy it without a human reading `needs`. `requestMore` only.\"}}},\"VerdictEffect\":{\"$anchor\":\"verdictEffect\",\"title\":\"VerdictEffect\",\"type\":\"string\",\"enum\":[\"allow\",\"deny\",\"refer\",\"requestMore\"],\"description\":\"What the policy decided.\\n\\n`allow` — admitted. `deny` — refused, terminally for this submission. `refer` — parked for a human or quorum decision; the applicant is neither in nor out. `requestMore` — the policy cannot decide yet and names what further evidence it needs.\\n\\nThe four are not reducible to a pending/decided pair. `refer` and `requestMore` are both 'not decided', but they place the next action with different parties: `refer` waits on the community, `requestMore` waits on the applicant. A consumer that cannot tell them apart cannot tell a user whether to wait or to act.\"}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     false,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
