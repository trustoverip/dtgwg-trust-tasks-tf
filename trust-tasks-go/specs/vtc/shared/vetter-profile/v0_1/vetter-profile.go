// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/_shared/0.1/vetter-profile.schema.json

package vtcsharedvetterprofilev0_1

// VetterDisplayName The name the vetter chooses to be listed under. Self-asserted and
// unverified: it is not the name on any document, and a client MUST attribute it to the
// vetter.
type VetterDisplayName = string

// LanguageTag A BCP 47 language tag, e.g. `en`, `de-AT`. Compared case-insensitively.
type LanguageTag = string

// CountryCode An ISO 3166-1 alpha-2 country code, upper case, e.g. `DE`.
type CountryCode = string

// PlaceName A region or city name as the vetter writes it. Compared case-insensitively
// and otherwise exactly.
type PlaceName = string

// CalendarDate A calendar date, `YYYY-MM-DD` (RFC 3339 full-date), with no time or zone.
// Compared as a UTC date.
type CalendarDate = string

// VetterLocation Where a vetter can meet people, as coarse as the vetter chooses: a
// country, optionally a region, optionally a city. Never a street address.
type VetterLocation struct {
	Country CountryCode `json:"country"`
	Region  *PlaceName  `json:"region,omitempty"`
	City    *PlaceName  `json:"city,omitempty"`
}

// VetterEvent An event the vetter will attend and vet at. `endDate` is on or after
// `startDate` and no more than 31 days after it; JSON Schema cannot compare two members,
// so the community checks both and refuses a violation with `malformedRequest`.
type VetterEvent struct {
	// The event's name, e.g. `Linux Plumbers Conference 2026`.
	Name      string          `json:"name"`
	StartDate CalendarDate    `json:"startDate"`
	EndDate   CalendarDate    `json:"endDate"`
	Location  *VetterLocation `json:"location,omitempty"`

	// OPTIONAL. The event's own https page.
	URL *string `json:"url,omitempty"`
}

// VetterMethods The methods the vetter offers, from `inPerson`, `video`,
// `priorAcquaintance`.
type VetterMethods = []VettingMethod

// VetterAcceptsDocumentation What documentation the vetter relies on, in the same tokens
// as vetting/request's `acceptsDocumentation` — `passport`, `nationalId`,
// `driverLicence`, `none`, or another lowerCamelCase class. The vetter's own choice;
// empty says nothing either way.
type VetterAcceptsDocumentation = []VettingDocumentation

// VetterAvailability Vetter-authored free text on when they are available to vet.
// Attributed to the vetter.
type VetterAvailability = string

// VetterContactHint Vetter-authored free text on how to obtain a ticket from them, e.g.
// `Find me at the OpenVTC booth`. A hint, not an address a request can be sent to:
// vetting/request still needs a ticket or an introduction the vetter accepts.
type VetterContactHint = string

// VettingDocumentation A class of documentation, named in lowerCamelCase. Open rather
// than enumerated, because what documentation a vetter accepts is each vetter's own
// choice. Well-known values: `passport`, `nationalId`, `driverLicence`, and `none` — the
// vetter will attest without a document, which is the `priorAcquaintance` case. Only the
// class ever travels — never a document number, an image, an issuing authority or an
// expiry date. `none` states a policy (what a vetter accepts); a record of what was
// relied on expresses 'no document' as an empty list instead.
type VettingDocumentation = string

// VettingMethod How the vetter established that the person they checked is the person
// controlling the applicant's DID. `inPerson` — both people were physically together.
// `video` — a live, two-way video call. `priorAcquaintance` — the vetter has known or
// worked with this person over a period, and attests from that knowledge rather than from
// a document. A method is a description of what happened, not an assurance level: which
// methods count, and how many of each, is community policy.
type VettingMethod string

// Values VettingMethod may take, per this specification's schema.
const (
	VettingMethodInPerson          VettingMethod = "inPerson"
	VettingMethodVideo             VettingMethod = "video"
	VettingMethodPriorAcquaintance VettingMethod = "priorAcquaintance"
)
