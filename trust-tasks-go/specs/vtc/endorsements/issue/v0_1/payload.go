// Code generated by scripts/build-go-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/endorsements/issue/0.1/payload.schema.json

package vtcendorsementsissuev0_1

import (
	"encoding/json"
	"github.com/trustoverip/dtgwg-trust-tasks-tf/trust-tasks-go/trusttasks"
)

// Response VTC Endorsements Issue — response payload
type Response struct {
	Endorsement Endorsement `json:"endorsement"`

	// The signed VEC just minted. Present only here: handing back the credential is the point
	// of an issue call, and the issuer is the only party that can. Reads carry
	// `endorsement.issued` — the reference — instead.
	Credential map[string]json.RawMessage `json:"credential"`
	Ext        *Ext                       `json:"ext,omitempty"`
}

// Ext Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
// reverse-DNS namespace; structure under each namespace is opaque to the framework.
type Ext = map[string]json.RawMessage

// Endorsement Endorsement
type Endorsement struct {
	// Community-scoped identifier for this endorsement row.
	EndorsementID string `json:"endorsementId"`

	// The registered endorsement type this VEC asserts; see vtc/endorsement-types/*.
	TypeURI string `json:"typeUri"`

	// DID of the endorsement's subject (becomes credentialSubject.id).
	SubjectDID string `json:"subjectDid"`

	// The attested claim body, validated against the endorsement type's claimSchema when it
	// declares one.
	Claim *map[string]json.RawMessage `json:"claim,omitempty"`

	// A pointer to the issued VEC — its identifier and lifetime, not its bytes.
	// `endorsements/issue` additionally returns the credential itself, because that is the
	// one call whose caller has no other way to receive it.
	Issued CredentialReference `json:"issued"`

	// The endorsement's slot on the community's shared Revocation status list. Published, so
	// a foreign verifier can check revocation without contacting this community.
	StatusListIndex int64 `json:"statusListIndex"`

	// When the endorsement was revoked, or null while live.
	RevokedAt *string `json:"revokedAt,omitempty"`
}

// CredentialReference A pointer to an issued credential, without the credential itself.
// The counterpart to IssuedCredential, for the far more common case of *reading about* a
// credential rather than being handed one. A listing that embedded the signed credential
// in every row would grow with the size of the credentials rather than the number of them
// — a page of fifty is megabytes — and a reader that only needs to know a credential
// exists, when it lapses, and how to revoke it does not need the bytes. The holder can
// always fetch the credential itself by `credentialId`, and a verifier can check
// revocation from the row's status-list slot without either. Reach for IssuedCredential
// only at the moment of minting, where the caller has no other way to receive what was
// just made for them.
type CredentialReference struct {
	CredentialID CredentialID `json:"credentialId"`

	// When the credential was minted.
	IssuedAt *string `json:"issuedAt,omitempty"`

	// When it lapses, or null when it does not.
	ExpiresAt *string `json:"expiresAt,omitempty"`
}

// CredentialID Stable identifier for an issued credential — the handle for revocation and
// audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be
// parsed.
type CredentialID = string

// Payload VTC Endorsements Issue — payload
type Payload struct {
	// DID of the endorsement's subject (becomes credentialSubject.id).
	SubjectDID string `json:"subjectDid"`

	// A type already registered via vtc/endorsement-types/register; unregistered types are
	// refused.
	TypeURI string `json:"typeUri"`

	// The claim body to attest. Capped at 8 KiB serialised, and validated against the type's
	// claimSchema when it declares one.
	Claim map[string]json.RawMessage `json:"claim"`

	// Credential lifetime in seconds from issuance. The community MAY cap this.
	ValiditySeconds *int64 `json:"validitySeconds,omitempty"`
	Ext             *Ext   `json:"ext,omitempty"`
}

// TypeURI is the Trust Task type URI this package's Payload is carried under.
const TypeURI = "https://trusttasks.org/spec/vtc/endorsements/issue/0.1"

// ResponseTypeURI is the success-response form of TypeURI (SPEC §4.4.1).
const ResponseTypeURI = "https://trusttasks.org/spec/vtc/endorsements/issue/0.1#response"

// PayloadSchemaJSON is this specification's payload schema, as JSON text.
//
// SPEC §7.2 item 2 is performed against this. It ships with the package rather
// than only as a file under specs/ because a Go struct carries none of the
// schema's constraints — not minLength, not pattern, not the oneOf mutual
// exclusion — so without it every such rule is unenforced. Cross-file $refs are
// already inlined, so it needs no resolver.
const PayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$id\":\"https://trusttasks.org/spec/vtc/endorsements/issue/0.1\",\"title\":\"VTC Endorsements Issue — payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"subjectDid\",\"typeUri\",\"claim\"],\"properties\":{\"subjectDid\":{\"type\":\"string\",\"pattern\":\"^did:\",\"description\":\"DID of the endorsement's subject (becomes credentialSubject.id).\"},\"typeUri\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":512,\"description\":\"A type already registered via vtc/endorsement-types/register; unregistered types are refused.\"},\"claim\":{\"type\":\"object\",\"minProperties\":1,\"description\":\"The claim body to attest. Capped at 8 KiB serialised, and validated against the type's claimSchema when it declares one.\"},\"validitySeconds\":{\"type\":\"integer\",\"minimum\":1,\"description\":\"Credential lifetime in seconds from issuance. The community MAY cap this.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}},\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Endorsements Issue — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"endorsement\",\"credential\"],\"properties\":{\"endorsement\":{\"$ref\":\"#/$defs/Endorsement\"},\"credential\":{\"type\":\"object\",\"description\":\"The signed VEC just minted. Present only here: handing back the credential is the point of an issue call, and the issuer is the only party that can. Reads carry `endorsement.issued` — the reference — instead.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Endorsement\":{\"$anchor\":\"endorsement\",\"title\":\"Endorsement\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"endorsementId\",\"typeUri\",\"subjectDid\",\"issued\",\"statusListIndex\"],\"properties\":{\"endorsementId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Community-scoped identifier for this endorsement row.\"},\"typeUri\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":512,\"description\":\"The registered endorsement type this VEC asserts; see vtc/endorsement-types/*.\"},\"subjectDid\":{\"type\":\"string\",\"pattern\":\"^did:\",\"description\":\"DID of the endorsement's subject (becomes credentialSubject.id).\"},\"claim\":{\"type\":\"object\",\"description\":\"The attested claim body, validated against the endorsement type's claimSchema when it declares one.\"},\"issued\":{\"$ref\":\"#/$defs/CredentialReference\",\"description\":\"A pointer to the issued VEC — its identifier and lifetime, not its bytes. `endorsements/issue` additionally returns the credential itself, because that is the one call whose caller has no other way to receive it.\"},\"statusListIndex\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"The endorsement's slot on the community's shared Revocation status list. Published, so a foreign verifier can check revocation without contacting this community.\"},\"revokedAt\":{\"type\":[\"string\",\"null\"],\"format\":\"date-time\",\"description\":\"When the endorsement was revoked, or null while live.\"}}},\"CredentialReference\":{\"$anchor\":\"credentialReference\",\"title\":\"CredentialReference\",\"type\":\"object\",\"additionalProperties\":false,\"description\":\"A pointer to an issued credential, without the credential itself.\\n\\nThe counterpart to IssuedCredential, for the far more common case of *reading about* a credential rather than being handed one. A listing that embedded the signed credential in every row would grow with the size of the credentials rather than the number of them — a page of fifty is megabytes — and a reader that only needs to know a credential exists, when it lapses, and how to revoke it does not need the bytes.\\n\\nThe holder can always fetch the credential itself by `credentialId`, and a verifier can check revocation from the row's status-list slot without either. Reach for IssuedCredential only at the moment of minting, where the caller has no other way to receive what was just made for them.\",\"required\":[\"credentialId\"],\"properties\":{\"credentialId\":{\"$ref\":\"#/$defs/CredentialId\"},\"issuedAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the credential was minted.\"},\"expiresAt\":{\"type\":[\"string\",\"null\"],\"format\":\"date-time\",\"description\":\"When it lapses, or null when it does not.\"}}},\"CredentialId\":{\"$anchor\":\"credentialId\",\"title\":\"CredentialId\",\"description\":\"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.\",\"type\":\"string\",\"minLength\":1}}}"

// ResponsePayloadSchemaJSON is PayloadSchemaJSON for the success-response variant.
const ResponsePayloadSchemaJSON = "{\"$schema\":\"https://json-schema.org/draft/2020-12/schema\",\"$ref\":\"#/$defs/Response\",\"$defs\":{\"Response\":{\"$anchor\":\"response\",\"title\":\"VTC Endorsements Issue — response payload\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"endorsement\",\"credential\"],\"properties\":{\"endorsement\":{\"$ref\":\"#/$defs/Endorsement\"},\"credential\":{\"type\":\"object\",\"description\":\"The signed VEC just minted. Present only here: handing back the credential is the point of an issue call, and the issuer is the only party that can. Reads carry `endorsement.issued` — the reference — instead.\"},\"ext\":{\"$ref\":\"#/$defs/Ext\"}}},\"Ext\":{\"title\":\"Ext\",\"description\":\"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.\",\"type\":\"object\",\"minProperties\":1,\"additionalProperties\":true,\"propertyNames\":{\"pattern\":\"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+$\"}},\"Endorsement\":{\"$anchor\":\"endorsement\",\"title\":\"Endorsement\",\"type\":\"object\",\"additionalProperties\":false,\"required\":[\"endorsementId\",\"typeUri\",\"subjectDid\",\"issued\",\"statusListIndex\"],\"properties\":{\"endorsementId\":{\"type\":\"string\",\"minLength\":1,\"description\":\"Community-scoped identifier for this endorsement row.\"},\"typeUri\":{\"type\":\"string\",\"minLength\":1,\"maxLength\":512,\"description\":\"The registered endorsement type this VEC asserts; see vtc/endorsement-types/*.\"},\"subjectDid\":{\"type\":\"string\",\"pattern\":\"^did:\",\"description\":\"DID of the endorsement's subject (becomes credentialSubject.id).\"},\"claim\":{\"type\":\"object\",\"description\":\"The attested claim body, validated against the endorsement type's claimSchema when it declares one.\"},\"issued\":{\"$ref\":\"#/$defs/CredentialReference\",\"description\":\"A pointer to the issued VEC — its identifier and lifetime, not its bytes. `endorsements/issue` additionally returns the credential itself, because that is the one call whose caller has no other way to receive it.\"},\"statusListIndex\":{\"type\":\"integer\",\"minimum\":0,\"description\":\"The endorsement's slot on the community's shared Revocation status list. Published, so a foreign verifier can check revocation without contacting this community.\"},\"revokedAt\":{\"type\":[\"string\",\"null\"],\"format\":\"date-time\",\"description\":\"When the endorsement was revoked, or null while live.\"}}},\"CredentialReference\":{\"$anchor\":\"credentialReference\",\"title\":\"CredentialReference\",\"type\":\"object\",\"additionalProperties\":false,\"description\":\"A pointer to an issued credential, without the credential itself.\\n\\nThe counterpart to IssuedCredential, for the far more common case of *reading about* a credential rather than being handed one. A listing that embedded the signed credential in every row would grow with the size of the credentials rather than the number of them — a page of fifty is megabytes — and a reader that only needs to know a credential exists, when it lapses, and how to revoke it does not need the bytes.\\n\\nThe holder can always fetch the credential itself by `credentialId`, and a verifier can check revocation from the row's status-list slot without either. Reach for IssuedCredential only at the moment of minting, where the caller has no other way to receive what was just made for them.\",\"required\":[\"credentialId\"],\"properties\":{\"credentialId\":{\"$ref\":\"#/$defs/CredentialId\"},\"issuedAt\":{\"type\":\"string\",\"format\":\"date-time\",\"description\":\"When the credential was minted.\"},\"expiresAt\":{\"type\":[\"string\",\"null\"],\"format\":\"date-time\",\"description\":\"When it lapses, or null when it does not.\"}}},\"CredentialId\":{\"$anchor\":\"credentialId\",\"title\":\"CredentialId\",\"description\":\"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.\",\"type\":\"string\",\"minLength\":1}}}"

// Spec is the SPEC §7.2 policy for the request variant, taken from this
// specification's front matter. Pass it to trusttasks.ConsumeInbound — items 5b,
// 7 and 8 are per-specification and cannot be derived from the document alone,
// and item 2 needs the schema it carries.
var Spec = trusttasks.SpecPolicy{
	TypeURI:             TypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       PayloadSchemaJSON,
}

// ResponseSpec is the SPEC §7.2 policy for the success-response variant.
// IsRecipientRequired tracks the *issuer* party's requirement, because a response
// swaps the parties (§7.3 item 5).
var ResponseSpec = trusttasks.SpecPolicy{
	TypeURI:             ResponseTypeURI,
	IsBearer:            false,
	IsProofRequired:     true,
	IsRecipientRequired: true,
	IsIssuedAtRequired:  true,
	PayloadSchema:       ResponsePayloadSchemaJSON,
}
