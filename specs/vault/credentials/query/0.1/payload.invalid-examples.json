[
  {
    "note": "Unknown top-level member \u2014 additionalProperties: false catches `frobnicate`.",
    "payload": {
      "purpose": "membership",
      "frobnicate": true
    }
  },
  {
    "note": "Bare/unnamespaced ext key \u2014 SPEC \u00a74.5.1 requires every immediate child of ext to be reverse-DNS namespaced.",
    "payload": {
      "purpose": "membership",
      "ext": {
        "bare-key": {
          "anything": "here"
        }
      }
    }
  },
  {
    "note": "status outside the validity enum. `archived` is a lifecycle state, and conflating the two axes is the misreading this schema exists to prevent.",
    "payload": {
      "status": "archived"
    }
  },
  {
    "note": "issuerDid that is not a DID.",
    "payload": {
      "issuerDid": "https://issuer.example"
    }
  }
]
