[
  {
    "note": "Neither credential nor credentialBase64. There is nothing to store, and an empty receive must not read as a no-op success.",
    "payload": {
      "contextId": "ctx-a"
    }
  },
  {
    "note": "Both forms at once. oneOf refuses it \u2014 a maintainer given two bodies has no rule for which it verified.",
    "payload": {
      "credential": {
        "type": [
          "VerifiableCredential"
        ]
      },
      "credentialBase64": "abc"
    }
  },
  {
    "note": "`purpose` supplied by the consumer. Purpose is derived from the credential's type tags; letting a consumer declare it lets a credential be filed under a classification its contents do not support.",
    "payload": {
      "credential": {
        "type": [
          "VerifiableCredential"
        ]
      },
      "purpose": "membership"
    }
  },
  {
    "note": "credentialBase64 with standard-alphabet padding. The member is base64url without padding; `+`, `/` and `=` are refused by the pattern.",
    "payload": {
      "credentialBase64": "ab+c/d=="
    }
  },
  {
    "note": "Bare/unnamespaced ext key \u2014 SPEC \u00a74.5.1 requires every immediate child of ext to be reverse-DNS namespaced.",
    "payload": {
      "credential": {
        "type": [
          "VerifiableCredential"
        ]
      },
      "ext": {
        "bare-key": {
          "anything": "here"
        }
      }
    }
  },
  {
    "note": "Unknown top-level member \u2014 additionalProperties: false catches `status`. A consumer does not get to assert validity; the maintainer computes it.",
    "payload": {
      "credential": {
        "type": [
          "VerifiableCredential"
        ]
      },
      "status": "valid"
    }
  }
]
