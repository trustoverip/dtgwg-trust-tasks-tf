[
  {
    "note": "The request takes no parameters; a stray field is rejected.",
    "payload": { "communityDid": "did:webvh:x" }
  },
  {
    "note": "Nor does the request name a criterion to fetch. A per-criterion read would let the community see which requirement a caller cares about — the one thing discovery must not reveal.",
    "payload": { "criterionId": "kernel-developer" }
  },
  {
    "note": "`ext` keys must be reverse-DNS namespaces (SPEC §4.5.1); a bare word is not one.",
    "payload": { "ext": { "mine": { "a": 1 } } }
  }
]
