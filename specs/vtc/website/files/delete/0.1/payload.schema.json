{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/website/files/delete/0.1",
  "title": "VTC Website Files Delete — payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["path"],
  "properties": {
    "path": { "type": "string", "minLength": 1, "description": "Path of the file to delete, relative to the site root." },
    "ext": { "$ref": "../../../../../_framework/0.2/framework.schema.json#/$defs/Ext" }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Website Files Delete — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": ["path", "deleted"],
      "properties": {
        "path": { "type": "string", "minLength": 1 },
        "deleted": { "type": "boolean" },
        "ext": { "$ref": "../../../../../_framework/0.2/framework.schema.json#/$defs/Ext" }
      }
    }
  }
}
