{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vtc/members/personhood/assert/0.1",
  "title": "VTC Members Personhood-Assert — payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["did", "presentation"],
  "properties": {
    "did": { "type": "string", "minLength": 1, "description": "The member asserting personhood; MUST equal the presentation's holder." },
    "presentation": { "type": "object", "description": "A W3C Verifiable Presentation (opaque here). Its holder MUST match did, its proof.challenge MUST be the paired challengeId, and it MUST carry at least one credential satisfying the community's active personhood policy." },
    "ext": { "$ref": "../../../../../_framework/0.2/framework.schema.json#/$defs/Ext" }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTC Members Personhood-Assert — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": ["did", "personhood", "vmc", "roleVec"],
      "properties": {
        "did": { "type": "string", "minLength": 1 },
        "personhood": { "type": "boolean", "const": true, "description": "Always true on success — the flag flipped or stayed asserted." },
        "vmc": { "type": "object", "description": "Re-issued membership credential carrying the asserted personhood (opaque here)." },
        "roleVec": { "type": "object", "description": "Re-issued role credential (opaque here)." },
        "ext": { "$ref": "../../../../../_framework/0.2/framework.schema.json#/$defs/Ext" }
      }
    }
  }
}
