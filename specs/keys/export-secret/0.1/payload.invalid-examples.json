[
  {
    "note": "`keyId` is required — a request naming no key has nothing to release.",
    "payload": {}
  },
  {
    "note": "`keyId` must not be empty. An empty string is what a caller lands on when its own key configuration is unset, and a custodian must refuse rather than resolve it to something.",
    "payload": { "keyId": "" }
  },
  {
    "note": "No list form. One key per request is the point: a batch would make many releases one decision, where each ought to be its own.",
    "payload": { "keyId": ["did:example:issuer#key-0", "did:example:issuer#key-1"] }
  },
  {
    "note": "Nor a `keyIds` member arriving alongside, which is the shape a batch would most likely take if someone added one.",
    "payload": { "keyId": "did:example:issuer#key-0", "keyIds": ["did:example:issuer#key-1"] }
  },
  {
    "note": "No context selector. Exporting 'every key in a context' is a different request with a different risk, and it must not be reachable by widening this one.",
    "payload": { "keyId": "did:example:issuer#key-0", "contextId": "rooms" }
  },
  {
    "note": "A request must not ask the custodian to override the key's own exportability. That decision belongs to `keys/set-exportability` and to an authority beyond the one that imposed it, not to a member on the request that reads it.",
    "payload": { "keyId": "did:example:issuer#key-0", "exportable": true }
  },
  {
    "note": "Nor to name where the key should be sent. The response goes to the producer that asked, established by the envelope; a destination member would be an open redirect for key material.",
    "payload": { "keyId": "did:example:issuer#key-0", "deliverTo": "did:example:elsewhere" }
  },
  {
    "note": "`keyId` must be a string, not a number.",
    "payload": { "keyId": 42 }
  },
  {
    "note": "`ext` keys must be reverse-DNS namespaces (SPEC §4.5.1); a bare word is not one.",
    "payload": { "keyId": "did:example:issuer#key-0", "ext": { "mine": { "a": 1 } } }
  }
]
