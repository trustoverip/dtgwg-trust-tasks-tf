{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/kyc-handoff/1.0",
  "title": "KYC Handoff — payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["subject", "result"],
  "properties": {
    "subject": {
      "type": "string",
      "description": "VID of the natural or legal person verified."
    },
    "result": {
      "enum": ["passed", "failed", "review"],
      "description": "Outcome of the verification."
    },
    "level": {
      "enum": ["LOA1", "LOA2", "LOA3"],
      "description": "Assurance level assigned to the verification."
    },
    "evidence": {
      "type": "array",
      "items": { "type": "string", "format": "uri" },
      "description": "Opaque references to underlying evidence the relying party may dereference under its own trust framework."
    }
  }
}
