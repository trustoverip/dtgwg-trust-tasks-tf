[
  {
    "note": "`id` is required — a request that names no context has nothing to authorize against, and must not be read as \"all of them\".",
    "payload": {}
  },
  {
    "note": "`id` must not be empty. An empty string is the shape a caller lands on when its own context configuration is unset, and the recipient must refuse it rather than resolve it to a default.",
    "payload": {
      "id": ""
    }
  },
  {
    "note": "`id` must be a string, not an array. A caller operating several contexts fetches each one separately; the task deliberately has no batch form.",
    "payload": {
      "id": [
        "rooms/host-1",
        "rooms/host-2"
      ]
    }
  },
  {
    "note": "`id` must be a string, not a number.",
    "payload": {
      "id": 42
    }
  },
  {
    "note": "Unknown top-level member is rejected (additionalProperties: false). A `did` on the *request* is the likeliest one to be invented — the recipient chooses which DID answers, and a caller must not be able to ask for one.",
    "payload": {
      "id": "rooms/host-1",
      "did": "did:webvh:QmExample:example.com:rooms:host-1"
    }
  },
  {
    "note": "Nor may a request narrow the answer by verification method. The response is the context's key material or an error; there is no per-key form.",
    "payload": {
      "id": "rooms/host-1",
      "keyId": "did:webvh:QmExample:example.com:rooms:host-1#key-0"
    }
  },
  {
    "note": "`ext` keys must be reverse-DNS namespaces (SPEC §4.5.1); a bare word is not one.",
    "payload": {
      "id": "rooms/host-1",
      "ext": {
        "mine": {
          "anything": true
        }
      }
    }
  },
  {
    "note": "`ext` must not be empty — an empty extension object carries no vendor namespace and says nothing.",
    "payload": {
      "id": "rooms/host-1",
      "ext": {}
    }
  }
]
