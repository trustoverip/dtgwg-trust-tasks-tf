[
  {
    "note": "Unknown top-level member \u2014 additionalProperties: false catches `includeBodies`. A list never returns credential bodies, so a member asking for them must not silently be ignored.",
    "payload": {
      "includeBodies": true
    }
  },
  {
    "note": "status outside the derived enum. `suspended` is not a state this task computes, and admitting it would let a consumer filter on something no agent can answer.",
    "payload": {
      "status": "suspended"
    }
  },
  {
    "note": "holder that is not a DID.",
    "payload": {
      "holder": "holder@example.com"
    }
  },
  {
    "note": "pageSize of zero. A page of nothing is not a page, and minimum: 1 refuses the shape that would otherwise loop a consumer forever.",
    "payload": {
      "pageSize": 0
    }
  },
  {
    "note": "Bare/unnamespaced ext key \u2014 SPEC \u00a74.5.1 requires every immediate child of ext to be reverse-DNS namespaced.",
    "payload": {
      "ext": {
        "bare-key": {
          "anything": "here"
        }
      }
    }
  }
]
