{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/vta/app-state/get-many/1.0",
  "title": "VTA Application State Get-Many — payload",
  "description": "Read up to 256 application-state records from one namespace in a single round trip. A rebuild or a reconnect is N records, and round-tripping each one separately is the difference between a usable reconnect and an unusable one.",
  "type": "object",
  "additionalProperties": false,
  "required": ["contextId", "namespace", "keys"],
  "properties": {
    "contextId": {
      "type": "string",
      "minLength": 1,
      "description": "The VTA context the records are scoped to; the isolation boundary."
    },
    "namespace": {
      "$ref": "../../../_shared/0.1/app-state-record.schema.json#/$defs/Namespace",
      "description": "Required, unlike in list: a batch read addresses records by key, and a key is only unique within a namespace."
    },
    "keys": {
      "type": "array",
      "minItems": 1,
      "maxItems": 256,
      "uniqueItems": true,
      "items": {
        "$ref": "../../../_shared/0.1/app-state-record.schema.json#/$defs/Key"
      },
      "description": "The keys to read. Duplicates are refused rather than deduplicated, because a caller that sent one did not mean to and would otherwise never find out."
    },
    "includeDeleted": {
      "type": "boolean",
      "description": "Defaults to false. When true, an address holding a tombstone yields a record with `deleted: true` rather than appearing in `missing`."
    },
    "ext": {
      "$ref": "../../../../_framework/0.2/framework.schema.json#/$defs/Ext",
      "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
    }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "VTA Application State Get-Many — response payload",
      "description": "Success response to vta/app-state/get-many. Type https://trusttasks.org/spec/vta/app-state/get-many/1.0#response. Every requested key appears in exactly one of `records`, `missing` or `deferred`, so a caller can account for all of them without comparing sets itself.",
      "type": "object",
      "additionalProperties": false,
      "required": ["records", "missing"],
      "properties": {
        "records": {
          "type": "array",
          "items": {
            "$ref": "../../../_shared/0.1/app-state-record.schema.json#/$defs/AppStateRecord"
          },
          "description": "The records found, each with its `value`, in the order the keys were requested. May be empty."
        },
        "missing": {
          "type": "array",
          "items": {
            "$ref": "../../../_shared/0.1/app-state-record.schema.json#/$defs/Key"
          },
          "description": "Requested keys holding no record — and, unless `includeDeleted` was set, keys holding only a tombstone. Reported rather than silently omitted, so a caller need not diff the request against the response to notice."
        },
        "deferred": {
          "type": "array",
          "items": {
            "$ref": "../../../_shared/0.1/app-state-record.schema.json#/$defs/Key"
          },
          "description": "Requested keys the maintainer did not evaluate because the response had reached its size budget. The caller re-requests exactly these. This exists because the per-record cap multiplied by the key ceiling exceeds any sane response limit, and the alternative — refusing the whole batch — would make a caller guess at a batch size instead of being told one."
        },
        "ext": {
          "$ref": "../../../../_framework/0.2/framework.schema.json#/$defs/Ext",
          "description": "Ecosystem-defined extension members per SPEC.md §4.5.1."
        }
      }
    }
  }
}
