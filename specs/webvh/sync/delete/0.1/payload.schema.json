{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://trusttasks.org/spec/webvh/sync/delete/0.1",
  "title": "WebVH Sync Delete — payload",
  "type": "object",
  "additionalProperties": false,
  "required": ["mnemonic"],
  "properties": {
    "mnemonic": { "type": "string", "minLength": 1 },
    "ext": { "$ref": "../../../../_framework/0.1/framework.schema.json#/$defs/Ext" }
  },
  "$defs": {
    "Response": {
      "$anchor": "response",
      "title": "WebVH Sync Delete — response payload",
      "type": "object",
      "additionalProperties": false,
      "required": ["mnemonic", "status"],
      "properties": {
        "mnemonic": { "type": "string" },
        "status": { "type": "string", "enum": ["deleted"] },
        "ext": { "$ref": "../../../../_framework/0.1/framework.schema.json#/$defs/Ext" }
      }
    }
  }
}
