// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/chat/message/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A cryptographic digest as a multibase-encoded multihash — the encoding the W3C
/// Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one
/// `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
/// algorithm in-band, so the value is self-describing and the wire format survives an
/// algorithm change without a schema revision; multibase does the same for the base
/// encoding, so a verifier never infers base58 from base64url by context. A bare hex
/// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract
/// and is non-conforming here. This definition constrains the *encoding only*. What
/// the digest is computed over is stated by each referencing field, because it differs
/// legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS)
/// canonicalization, while a digest over an opaque artifact is taken over its bytes. A
/// field whose input is a JSON document and which does not name a canonicalization is
/// not reproducible. Restricted to the two multibase headers W3C Controlled
/// Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u`
/// (base64url-no-pad). CID permits others but states that "interoperability is not
/// guaranteed between implementations using such values", and a registry whose purpose
/// is interoperability should not mint digests a conforming verifier may be unable to
/// read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I
/// and l, and an earlier permissive pattern let three published examples carry digests
/// that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with
/// `did:key` and `did:webvh`.
typedef DigestMultibase = String;

/// AttachmentRef, generated from its schema.
class AttachmentRef {
  const AttachmentRef({
    required this.id,
    this.filename,
    required this.mediaType,
    this.sizeBytes,
    this.digest,
  });

  /// Read this payload from a decoded JSON object.
  factory AttachmentRef.fromJson(Map<String, dynamic> json) => AttachmentRef(
        id: json['id'] as String,
        filename: json['filename'] as String?,
        mediaType: json['mediaType'] as String,
        sizeBytes: json['sizeBytes'] as int?,
        digest: json['digest'] as String?,
      );

  /// Opaque attachment id, resolvable via the bridge's attachment fetch.
  final String id;

  /// OPTIONAL. Suggested filename.
  final String? filename;

  /// IANA media type (e.g. `image/jpeg`).
  final String mediaType;

  /// OPTIONAL. Size in bytes, if known ahead of fetch.
  final int? sizeBytes;

  /// OPTIONAL. Multibase-encoded multihash over the attachment bytes, so the reference
  /// is itself verifiable and tamper-evident. Taken over the bytes as transferred, not
  /// over any JSON wrapper.
  final DigestMultibase? digest;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (filename != null) 'filename': filename!,
        'mediaType': mediaType,
        if (sizeBytes != null) 'sizeBytes': sizeBytes!,
        if (digest != null) 'digest': digest!,
      };
}

/// ChainLink, generated from its schema.
class ChainLink {
  const ChainLink({
    required this.id,
    required this.digest,
  });

  /// Read this payload from a decoded JSON object.
  factory ChainLink.fromJson(Map<String, dynamic> json) => ChainLink(
        id: json['id'] as String,
        digest: json['digest'] as String,
      );

  /// The `id` of the previous `chat/message` Trust Task document in this conversation.
  final String id;

  /// Multibase-encoded multihash over the RFC 8785 (JCS) canonicalization of the
  /// previous document, so a gap, reorder, or removal in the chain is detectable. The
  /// algorithm travels in the multihash rather than being fixed here.
  final DigestMultibase digest;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'digest': digest,
      };
}

/// Mention, generated from its schema.
class Mention {
  const Mention({
    required this.participant,
    this.displayName,
    required this.start,
    required this.length,
  });

  /// Read this payload from a decoded JSON object.
  factory Mention.fromJson(Map<String, dynamic> json) => Mention(
        participant: json['participant'] as String,
        displayName: json['displayName'] as String?,
        start: json['start'] as int,
        length: json['length'] as int,
      );

  /// Opaque, bridge-issued handle for the mentioned participant. MUST NOT be a raw
  /// platform address; like `conversationId` it carries no platform-native identifier
  /// (phone number, UUID, member id) upstream.
  final String participant;

  /// OPTIONAL. Human-readable name the source platform supplied for the participant. A
  /// non-authoritative rendering hint — never used for identity. Absent when the
  /// platform exposes no name in the mention.
  final String? displayName;

  /// Source-platform native start offset of the sentinel span in `text`. Advisory:
  /// offset units differ across platforms, so the authoritative binding is positional
  /// (Nth U+FFFC → Nth mention), not by offset.
  final int start;

  /// Source-platform native length of the sentinel span in `text`. Advisory; see
  /// `start`.
  final int length;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'participant': participant,
        if (displayName != null) 'displayName': displayName!,
        'start': start,
        'length': length,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// `inbound` = platform → agent (the bridge attests what it received and normalized);
/// `outbound` = agent → platform (authored by the agent).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadDirection(String value) {
  static const PayloadDirection inbound = PayloadDirection('inbound');
  static const PayloadDirection outbound = PayloadDirection('outbound');

  /// Every value this specification's schema permits.
  static const List<PayloadDirection> values = <PayloadDirection>[
    inbound,
    outbound
  ];
}

/// A conversational message exchanged between an AI agent and a messaging-platform
/// bridge. Signed by its author (via the document `proof`) and hash-linked to the
/// previous message in the conversation (`prev`), so a third party can verify each
/// message's author and ordering after the transport has closed — for audit and
/// dispute resolution. Conversations and contacts are referenced by opaque,
/// bridge-issued handles, never raw platform addresses.
class Payload {
  const Payload({
    required this.conversationId,
    required this.direction,
    this.platform,
    this.text,
    this.mentions,
    this.attachments,
    this.replyToId,
    this.isGroup,
    this.isMention,
    this.prev,
    required this.sentAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        conversationId: json['conversationId'] as String,
        direction: PayloadDirection(json['direction'] as String),
        platform: json['platform'] as String?,
        text: json['text'] as String?,
        mentions: json['mentions'] == null
            ? null
            : (json['mentions'] as List<dynamic>)
                .map((e) => Mention.fromJson(e as Map<String, dynamic>))
                .toList(),
        attachments: json['attachments'] == null
            ? null
            : (json['attachments'] as List<dynamic>)
                .map((e) => AttachmentRef.fromJson(e as Map<String, dynamic>))
                .toList(),
        replyToId: json['replyToId'] as String?,
        isGroup: json['isGroup'] as bool?,
        isMention: json['isMention'] as bool?,
        prev: json['prev'] == null
            ? null
            : ChainLink.fromJson(json['prev'] as Map<String, dynamic>),
        sentAt: json['sentAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque, bridge-issued conversation handle. MUST NOT be a raw platform address
  /// (phone number, chat id).
  final String conversationId;

  /// `inbound` = platform → agent (the bridge attests what it received and normalized);
  /// `outbound` = agent → platform (authored by the agent).
  final PayloadDirection direction;

  /// OPTIONAL. Platform key the message belongs to (e.g. `signal`, `whatsapp`).
  /// Advisory.
  final String? platform;

  /// OPTIONAL. Plain-text body. Absent for attachment-only messages.
  final String? text;

  /// OPTIONAL. @-mentions carried by the message, ordered to match the U+FFFC sentinel
  /// placeholders in `text`: the Nth U+FFFC binds to the Nth entry. Absent/empty when
  /// the message mentions no one. The producer is responsible for emitting one U+FFFC
  /// sentinel per mention, in order — translating each platform's native mention
  /// encoding into this neutral form.
  final List<Mention>? mentions;

  /// OPTIONAL. Attachments carried by reference, never inline.
  final List<AttachmentRef>? attachments;

  /// OPTIONAL. The `id` of the message this one replies to.
  final String? replyToId;

  /// OPTIONAL. True when the conversation is a group/channel, false (or absent) for a
  /// 1:1 DM. Part of the signed record so the audit chain captures where a message was
  /// sent, not just its text.
  final bool? isGroup;

  /// OPTIONAL. Platform-confirmed signal that this (inbound) message addresses the agent
  /// — e.g. an @-mention of the agent, or any DM. Lets a group-aware consumer decide
  /// whether a group message is for it without agent-name heuristics. Distinct from
  /// `mentions`, which lists the participants referenced in the body. Outbound messages
  /// leave this absent/false.
  final bool? isMention;

  /// OPTIONAL on the first message in a conversation; present on every message
  /// thereafter. Links to the previous message so the conversation forms a verifiable,
  /// ordered chain.
  final ChainLink? prev;

  /// RFC 3339 timestamp the author asserts for this message.
  final String sentAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'conversationId': conversationId,
        'direction': direction.value,
        if (platform != null) 'platform': platform!,
        if (text != null) 'text': text!,
        if (mentions != null)
          'mentions': mentions!.map((e) => e.toJson()).toList(),
        if (attachments != null)
          'attachments': attachments!.map((e) => e.toJson()).toList(),
        if (replyToId != null) 'replyToId': replyToId!,
        if (isGroup != null) 'isGroup': isGroup!,
        if (isMention != null) 'isMention': isMention!,
        if (prev != null) 'prev': prev!.toJson(),
        'sentAt': sentAt,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/chat/message/0.1';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/chat/message/0.1","title":"Chat Message — payload","description":"A conversational message exchanged between an AI agent and a messaging-platform bridge. Signed by its author (via the document `proof`) and hash-linked to the previous message in the conversation (`prev`), so a third party can verify each message\'s author and ordering after the transport has closed — for audit and dispute resolution. Conversations and contacts are referenced by opaque, bridge-issued handles, never raw platform addresses.","type":"object","additionalProperties":false,"required":["conversationId","direction","sentAt"],"properties":{"conversationId":{"type":"string","minLength":1,"description":"Opaque, bridge-issued conversation handle. MUST NOT be a raw platform address (phone number, chat id)."},"direction":{"type":"string","enum":["inbound","outbound"],"description":"`inbound` = platform → agent (the bridge attests what it received and normalized); `outbound` = agent → platform (authored by the agent)."},"platform":{"type":"string","minLength":1,"description":"OPTIONAL. Platform key the message belongs to (e.g. `signal`, `whatsapp`). Advisory."},"text":{"type":"string","maxLength":16384,"description":"OPTIONAL. Plain-text body. Absent for attachment-only messages."},"mentions":{"type":"array","items":{"\$ref":"#/\$defs/Mention"},"description":"OPTIONAL. @-mentions carried by the message, ordered to match the U+FFFC sentinel placeholders in `text`: the Nth U+FFFC binds to the Nth entry. Absent/empty when the message mentions no one. The producer is responsible for emitting one U+FFFC sentinel per mention, in order — translating each platform\'s native mention encoding into this neutral form."},"attachments":{"type":"array","items":{"\$ref":"#/\$defs/AttachmentRef"},"description":"OPTIONAL. Attachments carried by reference, never inline."},"replyToId":{"type":"string","minLength":1,"description":"OPTIONAL. The `id` of the message this one replies to."},"isGroup":{"type":"boolean","description":"OPTIONAL. True when the conversation is a group/channel, false (or absent) for a 1:1 DM. Part of the signed record so the audit chain captures where a message was sent, not just its text."},"isMention":{"type":"boolean","description":"OPTIONAL. Platform-confirmed signal that this (inbound) message addresses the agent — e.g. an @-mention of the agent, or any DM. Lets a group-aware consumer decide whether a group message is for it without agent-name heuristics. Distinct from `mentions`, which lists the participants referenced in the body. Outbound messages leave this absent/false."},"prev":{"\$ref":"#/\$defs/ChainLink","description":"OPTIONAL on the first message in a conversation; present on every message thereafter. Links to the previous message so the conversation forms a verifiable, ordered chain."},"sentAt":{"type":"string","format":"date-time","description":"RFC 3339 timestamp the author asserts for this message."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"AttachmentRef":{"type":"object","additionalProperties":false,"required":["id","mediaType"],"properties":{"id":{"type":"string","minLength":1,"description":"Opaque attachment id, resolvable via the bridge\'s attachment fetch."},"filename":{"type":"string","maxLength":256,"description":"OPTIONAL. Suggested filename."},"mediaType":{"type":"string","minLength":1,"description":"IANA media type (e.g. `image/jpeg`)."},"sizeBytes":{"type":"integer","minimum":0,"description":"OPTIONAL. Size in bytes, if known ahead of fetch."},"digest":{"\$ref":"#/\$defs/DigestMultibase","description":"OPTIONAL. Multibase-encoded multihash over the attachment bytes, so the reference is itself verifiable and tamper-evident. Taken over the bytes as transferred, not over any JSON wrapper."}}},"ChainLink":{"type":"object","additionalProperties":false,"required":["id","digest"],"properties":{"id":{"type":"string","minLength":1,"description":"The `id` of the previous `chat/message` Trust Task document in this conversation."},"digest":{"\$ref":"#/\$defs/DigestMultibase","description":"Multibase-encoded multihash over the RFC 8785 (JCS) canonicalization of the previous document, so a gap, reorder, or removal in the chain is detectable. The algorithm travels in the multihash rather than being fixed here."}}},"Mention":{"type":"object","additionalProperties":false,"required":["participant","start","length"],"properties":{"participant":{"type":"string","minLength":1,"description":"Opaque, bridge-issued handle for the mentioned participant. MUST NOT be a raw platform address; like `conversationId` it carries no platform-native identifier (phone number, UUID, member id) upstream."},"displayName":{"type":"string","maxLength":256,"description":"OPTIONAL. Human-readable name the source platform supplied for the participant. A non-authoritative rendering hint — never used for identity. Absent when the platform exposes no name in the mention."},"start":{"type":"integer","minimum":0,"description":"Source-platform native start offset of the sentinel span in `text`. Advisory: offset units differ across platforms, so the authoritative binding is positional (Nth U+FFFC → Nth mention), not by offset."},"length":{"type":"integer","minimum":0,"description":"Source-platform native length of the sentinel span in `text`. Advisory; see `start`."}}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);
