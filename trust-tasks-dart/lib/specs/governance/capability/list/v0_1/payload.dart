// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/governance/capability/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The trust-registry tuple vocabulary this capability may read and write. A
/// conforming host MUST reject writes by this capability whose `action` is not listed.
class CapabilityManifestVocabulary {
  const CapabilityManifestVocabulary({
    required this.actions,
    this.resourcePattern,
  });

  /// Read this payload from a decoded JSON object.
  factory CapabilityManifestVocabulary.fromJson(Map<String, dynamic> json) =>
      CapabilityManifestVocabulary(
        actions:
            (json['actions'] as List<dynamic>).map((e) => e as String).toList(),
        resourcePattern: json['resourcePattern'] as String?,
      );

  /// TRQP `action` values this capability owns, e.g. `git.commit.sign`.
  final List<String> actions;

  /// Human-readable pattern of the `resource` values used, e.g. `&lt;org&gt;\[/&lt;repo&gt;\]`.
  final String? resourcePattern;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'actions': actions,
        if (resourcePattern != null) 'resourcePattern': resourcePattern!,
      };
}

/// CapabilityManifestConsentClassValue is a closed set of string values defined by
/// this specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const CapabilityManifestConsentClassValue(String value) {
  static const CapabilityManifestConsentClassValue normal =
      CapabilityManifestConsentClassValue('normal');
  static const CapabilityManifestConsentClassValue elevated =
      CapabilityManifestConsentClassValue('elevated');
  static const CapabilityManifestConsentClassValue destructive =
      CapabilityManifestConsentClassValue('destructive');

  /// Every value this specification's schema permits.
  static const List<CapabilityManifestConsentClassValue> values =
      <CapabilityManifestConsentClassValue>[normal, elevated, destructive];
}

/// CapabilityManifestExternalAdaptersItem, generated from its schema.
class CapabilityManifestExternalAdaptersItem {
  const CapabilityManifestExternalAdaptersItem({
    required this.kind,
    required this.ref,
  });

  /// Read this payload from a decoded JSON object.
  factory CapabilityManifestExternalAdaptersItem.fromJson(
          Map<String, dynamic> json) =>
      CapabilityManifestExternalAdaptersItem(
        kind: json['kind'] as String,
        ref: json['ref'] as String,
      );

  /// Adapter type, e.g. `github-action`, `webhook`.
  final String kind;

  /// Where the adapter lives, e.g. a repository path or URL.
  final String ref;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'ref': ref,
      };
}

/// The self-description of a pluggable community capability: the Trust Task families
/// it serves, the trust-registry vocabulary it reads and writes, the roles that may
/// operate it, the membership lifecycle hooks it consumes, and the external adapters
/// that act on its decisions. The manifest is what governance approves, what discovery
/// advertises, and what a management UX renders.
class CapabilityManifest {
  const CapabilityManifest({
    required this.capability,
    required this.version,
    this.title,
    this.description,
    required this.specs,
    this.vocabulary,
    this.roles,
    this.lifecycleHooks,
    this.consentClass,
    this.externalAdapters,
    this.configSchema,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory CapabilityManifest.fromJson(Map<String, dynamic> json) =>
      CapabilityManifest(
        capability: json['capability'] as String,
        version: json['version'] as String,
        title: json['title'] as String?,
        description: json['description'] as String?,
        specs:
            (json['specs'] as List<dynamic>).map((e) => e as String).toList(),
        vocabulary: json['vocabulary'] == null
            ? null
            : CapabilityManifestVocabulary.fromJson(
                json['vocabulary'] as Map<String, dynamic>),
        roles: json['roles'] == null
            ? null
            : (json['roles'] as Map<String, dynamic>).map((k, v) => MapEntry(
                k, (v as List<dynamic>).map((e) => e as String).toList())),
        lifecycleHooks: json['lifecycleHooks'] == null
            ? null
            : (json['lifecycleHooks'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        consentClass: json['consentClass'] == null
            ? null
            : (json['consentClass'] as Map<String, dynamic>).map((k, v) =>
                MapEntry(k, CapabilityManifestConsentClassValue(v as String))),
        externalAdapters: json['externalAdapters'] == null
            ? null
            : (json['externalAdapters'] as List<dynamic>)
                .map((e) => CapabilityManifestExternalAdaptersItem.fromJson(
                    e as Map<String, dynamic>))
                .toList(),
        configSchema: json['configSchema'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The capability's namespace slug, e.g. `git-trust`.
  final String capability;

  /// Manifest/capability version, `MAJOR.MINOR`.
  final String version;

  /// Human-readable capability name for management surfaces.
  final String? title;

  /// One-paragraph description for management surfaces.
  final String? description;

  /// Trust Task spec slugs (or `&lt;family&gt;/*` globs) this capability serves when enabled.
  final List<String> specs;

  /// The trust-registry tuple vocabulary this capability may read and write. A
  /// conforming host MUST reject writes by this capability whose `action` is not listed.
  final CapabilityManifestVocabulary? vocabulary;

  /// Map of capability operation (e.g. `grant`, `view`) to the community roles allowed
  /// to perform it.
  final Map<String, List<String>>? roles;

  /// Membership lifecycle events the capability consumes, e.g. `member.enrolled`,
  /// `member.revoked`, `role.changed`.
  final List<String>? lifecycleHooks;

  /// Map of capability operation to the consent class its execution requires under the
  /// host's delegated-execution policy.
  final Map<String, CapabilityManifestConsentClassValue>? consentClass;

  /// Out-of-stack components that consume this capability's trust decisions.
  final List<CapabilityManifestExternalAdaptersItem>? externalAdapters;

  /// Spec slug of the JSON schema the per-community `config` document must validate
  /// against.
  final String? configSchema;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'capability': capability,
        'version': version,
        if (title != null) 'title': title!,
        if (description != null) 'description': description!,
        'specs': specs,
        if (vocabulary != null) 'vocabulary': vocabulary!.toJson(),
        if (roles != null) 'roles': roles!,
        if (lifecycleHooks != null) 'lifecycleHooks': lifecycleHooks!,
        if (consentClass != null)
          'consentClass': consentClass!.map((k, v) => MapEntry(k, v.value)),
        if (externalAdapters != null)
          'externalAdapters': externalAdapters!.map((e) => e.toJson()).toList(),
        if (configSchema != null) 'configSchema': configSchema!,
        if (ext != null) 'ext': ext!,
      };
}

/// ResponseCapabilitiesItem, generated from its schema.
class ResponseCapabilitiesItem {
  const ResponseCapabilitiesItem({
    required this.manifest,
    required this.enabled,
    this.enabledAt,
    this.delegate,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseCapabilitiesItem.fromJson(Map<String, dynamic> json) =>
      ResponseCapabilitiesItem(
        manifest: CapabilityManifest.fromJson(
            json['manifest'] as Map<String, dynamic>),
        enabled: json['enabled'] as bool,
        enabledAt: json['enabledAt'] as String?,
        delegate: json['delegate'] as String?,
      );

  final CapabilityManifest manifest;
  final bool enabled;

  /// When the capability was enabled, for enabled entries.
  final String? enabledAt;

  /// DID of the companion serving this capability, when not built in.
  final String? delegate;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'manifest': manifest.toJson(),
        'enabled': enabled,
        if (enabledAt != null) 'enabledAt': enabledAt!,
        if (delegate != null) 'delegate': delegate!,
      };
}

/// Governance Capability List — response payload
class Response {
  const Response({
    required this.capabilities,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        capabilities: (json['capabilities'] as List<dynamic>)
            .map((e) =>
                ResponseCapabilitiesItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<ResponseCapabilitiesItem> capabilities;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'capabilities': capabilities.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Which capabilities to list: `enabled` (the default when absent), `available` (known
/// to the host but not enabled), or `all`.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadStatus(String value) {
  static const PayloadStatus enabled = PayloadStatus('enabled');
  static const PayloadStatus available = PayloadStatus('available');
  static const PayloadStatus all = PayloadStatus('all');

  /// Every value this specification's schema permits.
  static const List<PayloadStatus> values = <PayloadStatus>[
    enabled,
    available,
    all
  ];
}

/// List a community's capabilities and their manifests — the query behind a capability
/// management surface. Defaults to enabled capabilities only.
class Payload {
  const Payload({
    this.status,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        status: json['status'] == null
            ? null
            : PayloadStatus(json['status'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Which capabilities to list: `enabled` (the default when absent), `available` (known
  /// to the host but not enabled), or `all`.
  final PayloadStatus? status;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (status != null) 'status': status!.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/governance/capability/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/governance/capability/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/governance/capability/list/0.1","title":"Governance Capability List — payload","description":"List a community\'s capabilities and their manifests — the query behind a capability management surface. Defaults to enabled capabilities only.","type":"object","additionalProperties":false,"properties":{"status":{"type":"string","enum":["enabled","available","all"],"description":"Which capabilities to list: `enabled` (the default when absent), `available` (known to the host but not enabled), or `all`."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Governance Capability List — response payload","type":"object","additionalProperties":false,"required":["capabilities"],"properties":{"capabilities":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["manifest","enabled"],"properties":{"manifest":{"\$ref":"#/\$defs/CapabilityManifest"},"enabled":{"type":"boolean"},"enabledAt":{"type":"string","format":"date-time","description":"When the capability was enabled, for enabled entries."},"delegate":{"type":"string","description":"DID of the companion serving this capability, when not built in."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CapabilityManifest":{"title":"CapabilityManifest","description":"The self-description of a pluggable community capability: the Trust Task families it serves, the trust-registry vocabulary it reads and writes, the roles that may operate it, the membership lifecycle hooks it consumes, and the external adapters that act on its decisions. The manifest is what governance approves, what discovery advertises, and what a management UX renders.","type":"object","additionalProperties":false,"required":["capability","version","specs"],"properties":{"capability":{"type":"string","pattern":"^[a-z0-9][a-z0-9-]*\$","description":"The capability\'s namespace slug, e.g. `git-trust`."},"version":{"type":"string","pattern":"^[0-9]+\\\\.[0-9]+\$","description":"Manifest/capability version, `MAJOR.MINOR`."},"title":{"type":"string","maxLength":256,"description":"Human-readable capability name for management surfaces."},"description":{"type":"string","maxLength":1024,"description":"One-paragraph description for management surfaces."},"specs":{"type":"array","minItems":1,"items":{"type":"string"},"description":"Trust Task spec slugs (or `<family>/*` globs) this capability serves when enabled."},"vocabulary":{"type":"object","additionalProperties":false,"description":"The trust-registry tuple vocabulary this capability may read and write. A conforming host MUST reject writes by this capability whose `action` is not listed.","required":["actions"],"properties":{"actions":{"type":"array","minItems":1,"items":{"type":"string"},"description":"TRQP `action` values this capability owns, e.g. `git.commit.sign`."},"resourcePattern":{"type":"string","description":"Human-readable pattern of the `resource` values used, e.g. `<org>[/<repo>]`."}}},"roles":{"type":"object","additionalProperties":{"type":"array","items":{"type":"string"}},"description":"Map of capability operation (e.g. `grant`, `view`) to the community roles allowed to perform it."},"lifecycleHooks":{"type":"array","items":{"type":"string"},"description":"Membership lifecycle events the capability consumes, e.g. `member.enrolled`, `member.revoked`, `role.changed`."},"consentClass":{"type":"object","additionalProperties":{"type":"string","enum":["normal","elevated","destructive"]},"description":"Map of capability operation to the consent class its execution requires under the host\'s delegated-execution policy."},"externalAdapters":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["kind","ref"],"properties":{"kind":{"type":"string","description":"Adapter type, e.g. `github-action`, `webhook`."},"ref":{"type":"string","description":"Where the adapter lives, e.g. a repository path or URL."}}},"description":"Out-of-stack components that consume this capability\'s trust decisions."},"configSchema":{"type":"string","description":"Spec slug of the JSON schema the per-community `config` document must validate against."},"ext":{"\$ref":"#/\$defs/Ext"}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Governance Capability List — response payload","type":"object","additionalProperties":false,"required":["capabilities"],"properties":{"capabilities":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["manifest","enabled"],"properties":{"manifest":{"\$ref":"#/\$defs/CapabilityManifest"},"enabled":{"type":"boolean"},"enabledAt":{"type":"string","format":"date-time","description":"When the capability was enabled, for enabled entries."},"delegate":{"type":"string","description":"DID of the companion serving this capability, when not built in."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CapabilityManifest":{"title":"CapabilityManifest","description":"The self-description of a pluggable community capability: the Trust Task families it serves, the trust-registry vocabulary it reads and writes, the roles that may operate it, the membership lifecycle hooks it consumes, and the external adapters that act on its decisions. The manifest is what governance approves, what discovery advertises, and what a management UX renders.","type":"object","additionalProperties":false,"required":["capability","version","specs"],"properties":{"capability":{"type":"string","pattern":"^[a-z0-9][a-z0-9-]*\$","description":"The capability\'s namespace slug, e.g. `git-trust`."},"version":{"type":"string","pattern":"^[0-9]+\\\\.[0-9]+\$","description":"Manifest/capability version, `MAJOR.MINOR`."},"title":{"type":"string","maxLength":256,"description":"Human-readable capability name for management surfaces."},"description":{"type":"string","maxLength":1024,"description":"One-paragraph description for management surfaces."},"specs":{"type":"array","minItems":1,"items":{"type":"string"},"description":"Trust Task spec slugs (or `<family>/*` globs) this capability serves when enabled."},"vocabulary":{"type":"object","additionalProperties":false,"description":"The trust-registry tuple vocabulary this capability may read and write. A conforming host MUST reject writes by this capability whose `action` is not listed.","required":["actions"],"properties":{"actions":{"type":"array","minItems":1,"items":{"type":"string"},"description":"TRQP `action` values this capability owns, e.g. `git.commit.sign`."},"resourcePattern":{"type":"string","description":"Human-readable pattern of the `resource` values used, e.g. `<org>[/<repo>]`."}}},"roles":{"type":"object","additionalProperties":{"type":"array","items":{"type":"string"}},"description":"Map of capability operation (e.g. `grant`, `view`) to the community roles allowed to perform it."},"lifecycleHooks":{"type":"array","items":{"type":"string"},"description":"Membership lifecycle events the capability consumes, e.g. `member.enrolled`, `member.revoked`, `role.changed`."},"consentClass":{"type":"object","additionalProperties":{"type":"string","enum":["normal","elevated","destructive"]},"description":"Map of capability operation to the consent class its execution requires under the host\'s delegated-execution policy."},"externalAdapters":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["kind","ref"],"properties":{"kind":{"type":"string","description":"Adapter type, e.g. `github-action`, `webhook`."},"ref":{"type":"string","description":"Where the adapter lives, e.g. a repository path or URL."}}},"description":"Out-of-stack components that consume this capability\'s trust decisions."},"configSchema":{"type":"string","description":"Spec slug of the JSON schema the per-community `config` document must validate against."},"ext":{"\$ref":"#/\$defs/Ext"}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
