// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/task-consent/request/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// `prompted` = the request was verified and an approval prompt raised. `refused` =
/// the device will not prompt; `reason` MUST be set.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus prompted = ResponseStatus('prompted');
  static const ResponseStatus refused = ResponseStatus('refused');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[
    prompted,
    refused
  ];
}

/// Synchronous acknowledgement from the approver device that a prompt was raised. The
/// decision itself arrives separately as a task-consent/decision — a human is in the
/// loop, so it cannot be a synchronous reply.
class Response {
  const Response({
    required this.status,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        status: ResponseStatus(json['status'] as String),
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// `prompted` = the request was verified and an approval prompt raised. `refused` =
  /// the device will not prompt; `reason` MUST be set.
  final ResponseStatus status;

  /// REQUIRED when status is `refused`.
  final String? reason;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'status': status.value,
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
      };
}

/// The prior state the effects were computed against. A human in the loop makes the
/// approval window minutes wide, so the state can move underneath a pending approval.
/// The executor asserts this pin still holds at execution and refuses otherwise —
/// without it, a lost update silently executes against state the approver never saw.
class StatePin {
  const StatePin({
    required this.resource,
    required this.version,
  });

  /// Read this payload from a decoded JSON object.
  factory StatePin.fromJson(Map<String, dynamic> json) => StatePin(
        resource: json['resource'] as String,
        version: json['version'] as String,
      );

  /// Identifier of the pinned resource (usually the subject DID).
  final String resource;

  /// Opaque version identifier of the prior state (e.g. a did:webvh `versionId`).
  /// Compared for equality at execution; the executor MUST NOT attempt to order or
  /// interpret it.
  final String version;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'resource': resource,
        'version': version,
      };
}

/// One consequence of executing the pending task, authored by the executor that is
/// about to run it. An effect is produced by dry-running the real handler against the
/// executor's own prior state — never by the requester, and never by re-implementing
/// the handler's semantics elsewhere. A consent surface renders ONLY effects it
/// received under the executor's signature.
class Effect {
  const Effect({
    required this.kind,
    required this.summary,
    this.path,
    this.before,
    this.after,
    this.detail,
  });

  /// Read this payload from a decoded JSON object.
  factory Effect.fromJson(Map<String, dynamic> json) => Effect(
        kind: json['kind'] as String,
        summary: json['summary'] as String,
        path: json['path'] as String?,
        before: json['before'],
        after: json['after'],
        detail: json['detail'] as Map<String, dynamic>?,
      );

  /// Machine discriminator for rich rendering. Well-known kinds are registered in the
  /// prose (`documentChange`, `keyRotation`, `preRotationRefresh`, `resourceDelete`,
  /// `disclosure`), but the set is OPEN: handlers evolve faster than this schema, and an
  /// executor MUST be able to describe a consequence this version does not name. A
  /// surface that does not recognise a `kind` MUST still render `summary`.
  final String kind;

  /// REQUIRED human-facing sentence describing this consequence, authored by the
  /// executor. This is the ONLY member a consent surface is guaranteed to be able to
  /// render, and it is what makes an unrecognised `kind` degrade to something truthful
  /// rather than something invisible. A surface MUST render it verbatim; it MUST NOT
  /// substitute its own prose.
  final String summary;

  /// OPTIONAL RFC 6901 JSON Pointer locating the change within the subject resource
  /// (e.g. `/service/0`).
  final String? path;

  /// OPTIONAL prior value at `path`, from the executor's authoritative state. ABSENT
  /// means there was no prior value (a creation); an explicit `null` is treated
  /// identically, so an executor MUST NOT rely on the two being distinguishable.
  /// Structured members are for rich rendering only — `summary` is what a surface is
  /// obliged to show, and it is where an executor states a distinction this shape cannot
  /// carry.
  final Object? before;

  /// OPTIONAL resulting value at `path` once the task executes. ABSENT means the value
  /// is removed (a deletion), under the same rule as `before`.
  final Object? after;

  /// OPTIONAL kind-specific structured extras (e.g. `{ "commitments": 2 }` for
  /// `preRotationRefresh`).
  final Map<String, dynamic>? detail;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'summary': summary,
        if (path != null) 'path': path!,
        if (before != null) 'before': before!,
        if (after != null) 'after': after!,
        if (detail != null) 'detail': detail!,
      };
}

/// Sensitivity of data the task returns to its caller.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ExposureDiscloses(String value) {
  static const ExposureDiscloses none = ExposureDiscloses('none');
  static const ExposureDiscloses metadata = ExposureDiscloses('metadata');
  static const ExposureDiscloses secret = ExposureDiscloses('secret');

  /// Every value this specification's schema permits.
  static const List<ExposureDiscloses> values = <ExposureDiscloses>[
    none,
    metadata,
    secret
  ];
}

/// The authoritative SPEC §7.3 item 14 exposure class of the pending task, derived by
/// the executor from the compiled handler it is about to invoke — never from the
/// registry's declared value.
class Exposure {
  const Exposure({
    required this.discloses,
    required this.actsAsSubject,
  });

  /// Read this payload from a decoded JSON object.
  factory Exposure.fromJson(Map<String, dynamic> json) => Exposure(
        discloses: ExposureDiscloses(json['discloses'] as String),
        actsAsSubject: json['actsAsSubject'] as bool,
      );

  /// Sensitivity of data the task returns to its caller.
  final ExposureDiscloses discloses;

  /// Whether execution exercises the subject's own authority to produce an attributable
  /// effect in the subject's name.
  final bool actsAsSubject;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'discloses': discloses.value,
        'actsAsSubject': actsAsSubject,
      };
}

/// A cryptographic digest as a multibase-encoded multihash — the encoding the W3C
/// Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one
/// `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
/// algorithm in-band, so the value is self-describing and the wire format survives an
/// algorithm change without a schema revision; multibase does the same for the base
/// encoding, so a verifier never infers base58 from base64url by context. A bare hex
/// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract
/// and is non-conforming here. This definition constrains the *encoding only*. What
/// the digest is computed over is stated by each referencing field, because it differs
/// legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS)
/// canonicalization, while a digest over an opaque artifact is taken over its bytes. A
/// field whose input is a JSON document and which does not name a canonicalization is
/// not reproducible. Restricted to the two multibase headers W3C Controlled
/// Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u`
/// (base64url-no-pad). CID permits others but states that "interoperability is not
/// guaranteed between implementations using such values", and a registry whose purpose
/// is interoperability should not mint digests a conforming verifier may be unable to
/// read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I
/// and l, and an earlier permissive pattern let three published examples carry digests
/// that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with
/// `did:key` and `did:webvh`.
typedef DigestMultibase = String;

/// Authoritative SPEC §7.3 item 13 side-effect class of the pending task, derived by
/// the executor from the compiled handler it is about to invoke. NEVER taken from the
/// registry: a registry that decided this would be a consent kill-switch,
/// downgradeable by publishing a new version with a weaker class.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadSideEffects(String value) {
  static const PayloadSideEffects none = PayloadSideEffects('none');
  static const PayloadSideEffects mutating = PayloadSideEffects('mutating');
  static const PayloadSideEffects destructive =
      PayloadSideEffects('destructive');

  /// Every value this specification's schema permits.
  static const List<PayloadSideEffects> values = <PayloadSideEffects>[
    none,
    mutating,
    destructive
  ];
}

/// The executor asks an enrolled approver device to authorize one pending privileged
/// task, presenting the effects it computed by dry-running the real handler against
/// its own prior state. The executor signs this document; the approver renders only
/// what it verifies under that signature.
class Payload {
  const Payload({
    required this.challenge,
    required this.taskType,
    required this.payloadDigest,
    required this.sideEffects,
    required this.exposure,
    required this.effects,
    this.consequences,
    this.subject,
    required this.requester,
    this.requesterDeviceId,
    this.origin,
    this.note,
    this.statePin,
    required this.approverSet,
    required this.minApprovals,
    required this.excludeRequester,
    required this.expiresAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        challenge: json['challenge'] as String,
        taskType: json['taskType'] as String,
        payloadDigest: json['payloadDigest'] as String,
        sideEffects: PayloadSideEffects(json['sideEffects'] as String),
        exposure: Exposure.fromJson(json['exposure'] as Map<String, dynamic>),
        effects: (json['effects'] as List<dynamic>)
            .map((e) => Effect.fromJson(e as Map<String, dynamic>))
            .toList(),
        consequences: json['consequences'] == null
            ? null
            : (json['consequences'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        subject: json['subject'] as String?,
        requester: json['requester'] as String,
        requesterDeviceId: json['requesterDeviceId'] as String?,
        origin: json['origin'] as String?,
        note: json['note'] as String?,
        statePin: json['statePin'] == null
            ? null
            : StatePin.fromJson(json['statePin'] as Map<String, dynamic>),
        approverSet: json['approverSet'] as String,
        minApprovals: json['minApprovals'] as int,
        excludeRequester: json['excludeRequester'] as bool,
        expiresAt: json['expiresAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Per-request nonce (≥128 bits entropy) echoed and signed by the matching
  /// task-consent/decision. It is also the salt in `payloadDigest`, so a party that
  /// holds this request can re-derive the digest and a party that does not cannot invert
  /// it.
  final String challenge;

  /// Type URI of the task awaiting approval. It is bound into `payloadDigest`: without
  /// that binding, two tasks whose payloads canonicalize identically would share a
  /// digest, and an approval for a benign task would authorize a destructive one.
  final String taskType;

  /// The binding between what the approver sees and what executes. Multibase-encoded
  /// multihash over the RFC 8785 (JCS) canonicalization of the payload, the task type,
  /// and the `challenge` as salt. The decision echoes it; the executor re-derives it
  /// from the payload it is about to run and refuses on mismatch. Salted because an
  /// unsalted digest over a low-entropy payload is a confirmation oracle for anyone who
  /// observes it in transit.
  final DigestMultibase payloadDigest;

  /// Authoritative SPEC §7.3 item 13 side-effect class of the pending task, derived by
  /// the executor from the compiled handler it is about to invoke. NEVER taken from the
  /// registry: a registry that decided this would be a consent kill-switch,
  /// downgradeable by publishing a new version with a weaker class.
  final PayloadSideEffects sideEffects;
  final Exposure exposure;

  /// What executing this task will actually do, computed by dry-running the real handler
  /// against the executor's prior state. MAY be empty when the executor has no dry-run
  /// for this handler — in which case `consequences` carries the specification's static
  /// fallback text. When BOTH are empty the surface MUST tell the approver the
  /// consequences could not be determined, and MUST NOT present the task as though it
  /// had none.
  final List<Effect> effects;

  /// Static, human-facing second-order effects declared in the task's specification
  /// (SPEC §7.3 item 13). A fallback for handlers with no dry-run — per-task, not
  /// per-request, so it can say 'any document change rotates the update keys' but not
  /// which keys. Prefer `effects`.
  final List<String>? consequences;

  /// OPTIONAL identifier the task acts on — the value at the specification's
  /// `subjectPath`, usually a DID. Absent for subjectless tasks.
  final String? subject;

  /// DID that submitted the pending task. Shown to the approver, and compared against
  /// the approver when `excludeRequester` is set.
  final String requester;

  /// OPTIONAL enrolled device the task was submitted from.
  final String? requesterDeviceId;

  /// OPTIONAL web origin that proposed the task, when it arrived via a relying party.
  /// This MUST be the origin the consumer's own runtime attested (e.g. the
  /// browser-supplied sender origin) — never a value the proposing page supplied, and
  /// never one the relying party could author.
  final String? origin;

  /// OPTIONAL requester-authored display text — the one member of this payload whose
  /// prose the executor did NOT author, carried verbatim for context ('why I am
  /// asking'). EXPLICITLY UNTRUSTED: it is written by the least trusted party in the
  /// system and MUST NOT be treated as a statement of the task's effects. A surface that
  /// renders it MUST attribute it to `requester`, MUST present it visually distinct from
  /// `effects`, and MUST NOT let it substitute for, reorder, or obscure them. The
  /// executor's signature on this document attests only that the requester supplied this
  /// text, never that it is true.
  final String? note;
  final StatePin? statePin;

  /// Name of the approver set the policy's `requireConsent` named.
  final String approverSet;

  /// Distinct approvals from the set required before the task may execute.
  final int minApprovals;

  /// When true, `requester` may not count toward the threshold — so a single compromised
  /// device cannot both propose and approve. A surface MUST NOT offer to approve a
  /// request whose `requester` is its own subject when this is set.
  final bool excludeRequester;

  /// After this instant the pending request lapses and no decision is accepted for it.
  final String expiresAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'challenge': challenge,
        'taskType': taskType,
        'payloadDigest': payloadDigest,
        'sideEffects': sideEffects.value,
        'exposure': exposure.toJson(),
        'effects': effects.map((e) => e.toJson()).toList(),
        if (consequences != null) 'consequences': consequences!,
        if (subject != null) 'subject': subject!,
        'requester': requester,
        if (requesterDeviceId != null) 'requesterDeviceId': requesterDeviceId!,
        if (origin != null) 'origin': origin!,
        if (note != null) 'note': note!,
        if (statePin != null) 'statePin': statePin!.toJson(),
        'approverSet': approverSet,
        'minApprovals': minApprovals,
        'excludeRequester': excludeRequester,
        'expiresAt': expiresAt,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/task-consent/request/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/task-consent/request/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/task-consent/request/0.1","title":"Task Consent Request — payload","description":"The executor asks an enrolled approver device to authorize one pending privileged task, presenting the effects it computed by dry-running the real handler against its own prior state. The executor signs this document; the approver renders only what it verifies under that signature.","type":"object","additionalProperties":false,"required":["challenge","taskType","payloadDigest","sideEffects","exposure","effects","requester","approverSet","minApprovals","excludeRequester","expiresAt"],"properties":{"challenge":{"type":"string","minLength":16,"description":"Per-request nonce (≥128 bits entropy) echoed and signed by the matching task-consent/decision. It is also the salt in `payloadDigest`, so a party that holds this request can re-derive the digest and a party that does not cannot invert it."},"taskType":{"type":"string","format":"uri","description":"Type URI of the task awaiting approval. It is bound into `payloadDigest`: without that binding, two tasks whose payloads canonicalize identically would share a digest, and an approval for a benign task would authorize a destructive one."},"payloadDigest":{"\$ref":"#/\$defs/DigestMultibase","description":"The binding between what the approver sees and what executes. Multibase-encoded multihash over the RFC 8785 (JCS) canonicalization of the payload, the task type, and the `challenge` as salt. The decision echoes it; the executor re-derives it from the payload it is about to run and refuses on mismatch. Salted because an unsalted digest over a low-entropy payload is a confirmation oracle for anyone who observes it in transit."},"sideEffects":{"type":"string","enum":["none","mutating","destructive"],"description":"Authoritative SPEC §7.3 item 13 side-effect class of the pending task, derived by the executor from the compiled handler it is about to invoke. NEVER taken from the registry: a registry that decided this would be a consent kill-switch, downgradeable by publishing a new version with a weaker class."},"exposure":{"\$ref":"#/\$defs/Exposure"},"effects":{"type":"array","description":"What executing this task will actually do, computed by dry-running the real handler against the executor\'s prior state. MAY be empty when the executor has no dry-run for this handler — in which case `consequences` carries the specification\'s static fallback text. When BOTH are empty the surface MUST tell the approver the consequences could not be determined, and MUST NOT present the task as though it had none.","items":{"\$ref":"#/\$defs/Effect"}},"consequences":{"type":"array","description":"Static, human-facing second-order effects declared in the task\'s specification (SPEC §7.3 item 13). A fallback for handlers with no dry-run — per-task, not per-request, so it can say \'any document change rotates the update keys\' but not which keys. Prefer `effects`.","items":{"type":"string","minLength":1,"maxLength":500}},"subject":{"type":"string","description":"OPTIONAL identifier the task acts on — the value at the specification\'s `subjectPath`, usually a DID. Absent for subjectless tasks."},"requester":{"type":"string","description":"DID that submitted the pending task. Shown to the approver, and compared against the approver when `excludeRequester` is set."},"requesterDeviceId":{"type":"string","description":"OPTIONAL enrolled device the task was submitted from."},"origin":{"type":"string","description":"OPTIONAL web origin that proposed the task, when it arrived via a relying party. This MUST be the origin the consumer\'s own runtime attested (e.g. the browser-supplied sender origin) — never a value the proposing page supplied, and never one the relying party could author."},"note":{"type":"string","minLength":1,"maxLength":500,"description":"OPTIONAL requester-authored display text — the one member of this payload whose prose the executor did NOT author, carried verbatim for context (\'why I am asking\'). EXPLICITLY UNTRUSTED: it is written by the least trusted party in the system and MUST NOT be treated as a statement of the task\'s effects. A surface that renders it MUST attribute it to `requester`, MUST present it visually distinct from `effects`, and MUST NOT let it substitute for, reorder, or obscure them. The executor\'s signature on this document attests only that the requester supplied this text, never that it is true."},"statePin":{"\$ref":"#/\$defs/StatePin"},"approverSet":{"type":"string","description":"Name of the approver set the policy\'s `requireConsent` named."},"minApprovals":{"type":"integer","minimum":1,"description":"Distinct approvals from the set required before the task may execute."},"excludeRequester":{"type":"boolean","description":"When true, `requester` may not count toward the threshold — so a single compromised device cannot both propose and approve. A surface MUST NOT offer to approve a request whose `requester` is its own subject when this is set."},"expiresAt":{"type":"string","format":"date-time","description":"After this instant the pending request lapses and no decision is accepted for it."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Task Consent Request — response payload","description":"Synchronous acknowledgement from the approver device that a prompt was raised. The decision itself arrives separately as a task-consent/decision — a human is in the loop, so it cannot be a synchronous reply.","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["prompted","refused"],"description":"`prompted` = the request was verified and an approval prompt raised. `refused` = the device will not prompt; `reason` MUST be set."},"reason":{"type":"string","maxLength":1024,"description":"REQUIRED when status is `refused`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"StatePin":{"title":"StatePin","description":"The prior state the effects were computed against. A human in the loop makes the approval window minutes wide, so the state can move underneath a pending approval. The executor asserts this pin still holds at execution and refuses otherwise — without it, a lost update silently executes against state the approver never saw.","type":"object","additionalProperties":false,"required":["resource","version"],"properties":{"resource":{"type":"string","minLength":1,"description":"Identifier of the pinned resource (usually the subject DID)."},"version":{"type":"string","minLength":1,"description":"Opaque version identifier of the prior state (e.g. a did:webvh `versionId`). Compared for equality at execution; the executor MUST NOT attempt to order or interpret it."}}},"Effect":{"title":"Effect","description":"One consequence of executing the pending task, authored by the executor that is about to run it. An effect is produced by dry-running the real handler against the executor\'s own prior state — never by the requester, and never by re-implementing the handler\'s semantics elsewhere. A consent surface renders ONLY effects it received under the executor\'s signature.","type":"object","additionalProperties":false,"required":["kind","summary"],"properties":{"kind":{"type":"string","pattern":"^[a-z][a-zA-Z0-9]*\$","description":"Machine discriminator for rich rendering. Well-known kinds are registered in the prose (`documentChange`, `keyRotation`, `preRotationRefresh`, `resourceDelete`, `disclosure`), but the set is OPEN: handlers evolve faster than this schema, and an executor MUST be able to describe a consequence this version does not name. A surface that does not recognise a `kind` MUST still render `summary`."},"summary":{"type":"string","minLength":1,"maxLength":500,"description":"REQUIRED human-facing sentence describing this consequence, authored by the executor. This is the ONLY member a consent surface is guaranteed to be able to render, and it is what makes an unrecognised `kind` degrade to something truthful rather than something invisible. A surface MUST render it verbatim; it MUST NOT substitute its own prose."},"path":{"type":"string","description":"OPTIONAL RFC 6901 JSON Pointer locating the change within the subject resource (e.g. `/service/0`)."},"before":{"description":"OPTIONAL prior value at `path`, from the executor\'s authoritative state. ABSENT means there was no prior value (a creation); an explicit `null` is treated identically, so an executor MUST NOT rely on the two being distinguishable. Structured members are for rich rendering only — `summary` is what a surface is obliged to show, and it is where an executor states a distinction this shape cannot carry."},"after":{"description":"OPTIONAL resulting value at `path` once the task executes. ABSENT means the value is removed (a deletion), under the same rule as `before`."},"detail":{"type":"object","description":"OPTIONAL kind-specific structured extras (e.g. `{ \\"commitments\\": 2 }` for `preRotationRefresh`).","additionalProperties":true}}},"Exposure":{"title":"Exposure","description":"The authoritative SPEC §7.3 item 14 exposure class of the pending task, derived by the executor from the compiled handler it is about to invoke — never from the registry\'s declared value.","type":"object","additionalProperties":false,"required":["discloses","actsAsSubject"],"properties":{"discloses":{"type":"string","enum":["none","metadata","secret"],"description":"Sensitivity of data the task returns to its caller."},"actsAsSubject":{"type":"boolean","description":"Whether execution exercises the subject\'s own authority to produce an attributable effect in the subject\'s name."}}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Task Consent Request — response payload","description":"Synchronous acknowledgement from the approver device that a prompt was raised. The decision itself arrives separately as a task-consent/decision — a human is in the loop, so it cannot be a synchronous reply.","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["prompted","refused"],"description":"`prompted` = the request was verified and an approval prompt raised. `refused` = the device will not prompt; `reason` MUST be set."},"reason":{"type":"string","maxLength":1024,"description":"REQUIRED when status is `refused`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"StatePin":{"title":"StatePin","description":"The prior state the effects were computed against. A human in the loop makes the approval window minutes wide, so the state can move underneath a pending approval. The executor asserts this pin still holds at execution and refuses otherwise — without it, a lost update silently executes against state the approver never saw.","type":"object","additionalProperties":false,"required":["resource","version"],"properties":{"resource":{"type":"string","minLength":1,"description":"Identifier of the pinned resource (usually the subject DID)."},"version":{"type":"string","minLength":1,"description":"Opaque version identifier of the prior state (e.g. a did:webvh `versionId`). Compared for equality at execution; the executor MUST NOT attempt to order or interpret it."}}},"Effect":{"title":"Effect","description":"One consequence of executing the pending task, authored by the executor that is about to run it. An effect is produced by dry-running the real handler against the executor\'s own prior state — never by the requester, and never by re-implementing the handler\'s semantics elsewhere. A consent surface renders ONLY effects it received under the executor\'s signature.","type":"object","additionalProperties":false,"required":["kind","summary"],"properties":{"kind":{"type":"string","pattern":"^[a-z][a-zA-Z0-9]*\$","description":"Machine discriminator for rich rendering. Well-known kinds are registered in the prose (`documentChange`, `keyRotation`, `preRotationRefresh`, `resourceDelete`, `disclosure`), but the set is OPEN: handlers evolve faster than this schema, and an executor MUST be able to describe a consequence this version does not name. A surface that does not recognise a `kind` MUST still render `summary`."},"summary":{"type":"string","minLength":1,"maxLength":500,"description":"REQUIRED human-facing sentence describing this consequence, authored by the executor. This is the ONLY member a consent surface is guaranteed to be able to render, and it is what makes an unrecognised `kind` degrade to something truthful rather than something invisible. A surface MUST render it verbatim; it MUST NOT substitute its own prose."},"path":{"type":"string","description":"OPTIONAL RFC 6901 JSON Pointer locating the change within the subject resource (e.g. `/service/0`)."},"before":{"description":"OPTIONAL prior value at `path`, from the executor\'s authoritative state. ABSENT means there was no prior value (a creation); an explicit `null` is treated identically, so an executor MUST NOT rely on the two being distinguishable. Structured members are for rich rendering only — `summary` is what a surface is obliged to show, and it is where an executor states a distinction this shape cannot carry."},"after":{"description":"OPTIONAL resulting value at `path` once the task executes. ABSENT means the value is removed (a deletion), under the same rule as `before`."},"detail":{"type":"object","description":"OPTIONAL kind-specific structured extras (e.g. `{ \\"commitments\\": 2 }` for `preRotationRefresh`).","additionalProperties":true}}},"Exposure":{"title":"Exposure","description":"The authoritative SPEC §7.3 item 14 exposure class of the pending task, derived by the executor from the compiled handler it is about to invoke — never from the registry\'s declared value.","type":"object","additionalProperties":false,"required":["discloses","actsAsSubject"],"properties":{"discloses":{"type":"string","enum":["none","metadata","secret"],"description":"Sensitivity of data the task returns to its caller."},"actsAsSubject":{"type":"boolean","description":"Whether execution exercises the subject\'s own authority to produce an attributable effect in the subject\'s name."}}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
