// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/keys/revoke/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Lifecycle state. Only an `active` key may be named in a signing request; a
/// `revoked` key is retained so historic signatures remain attributable, and MUST NOT
/// be reactivated.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const KeyStatus(String value) {
  static const KeyStatus active = KeyStatus('active');
  static const KeyStatus revoked = KeyStatus('revoked');

  /// Every value this specification's schema permits.
  static const List<KeyStatus> values = <KeyStatus>[active, revoked];
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a keys/revoke request. Carried in a Trust Task document
/// whose type is https://trusttasks.org/spec/keys/revoke/0.1#response.
class Response {
  const Response({
    required this.keyId,
    required this.status,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        keyId: json['keyId'] as String,
        status: KeyStatus(json['status'] as String),
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The key that was retired.
  final String keyId;

  /// The lifecycle state now in effect — `revoked` on success. Returned rather than
  /// assumed so a caller reading only the response can see the realized state.
  final KeyStatus status;

  /// RFC 3339 timestamp of the revocation. The boundary between signatures made while
  /// the key was valid and requests refused afterwards.
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'keyId': keyId,
        'status': status.value,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// Retire a key from further use. The record is retained so historic signatures stay
/// attributable; this is not a delete.
class Payload {
  const Payload({
    required this.keyId,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        keyId: json['keyId'] as String,
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Identifier of the key to retire.
  final String keyId;

  /// Optional human-readable rationale, recorded with the revocation.
  final String? reason;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'keyId': keyId,
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/keys/revoke/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/keys/revoke/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/keys/revoke/0.1","title":"Keys Revoke — payload","description":"Retire a key from further use. The record is retained so historic signatures stay attributable; this is not a delete.","type":"object","additionalProperties":false,"required":["keyId"],"properties":{"keyId":{"type":"string","minLength":1,"description":"Identifier of the key to retire."},"reason":{"type":"string","maxLength":1024,"description":"Optional human-readable rationale, recorded with the revocation."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Keys Revoke — response payload","description":"The success response to a keys/revoke request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/revoke/0.1#response.","type":"object","additionalProperties":false,"required":["keyId","status","updatedAt"],"properties":{"keyId":{"type":"string","description":"The key that was retired."},"status":{"\$ref":"#/\$defs/KeyStatus","description":"The lifecycle state now in effect — `revoked` on success. Returned rather than assumed so a caller reading only the response can see the realized state."},"updatedAt":{"type":"string","format":"date-time","description":"RFC 3339 timestamp of the revocation. The boundary between signatures made while the key was valid and requests refused afterwards."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"KeyStatus":{"title":"KeyStatus","type":"string","enum":["active","revoked"],"description":"Lifecycle state. Only an `active` key may be named in a signing request; a `revoked` key is retained so historic signatures remain attributable, and MUST NOT be reactivated."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Keys Revoke — response payload","description":"The success response to a keys/revoke request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/keys/revoke/0.1#response.","type":"object","additionalProperties":false,"required":["keyId","status","updatedAt"],"properties":{"keyId":{"type":"string","description":"The key that was retired."},"status":{"\$ref":"#/\$defs/KeyStatus","description":"The lifecycle state now in effect — `revoked` on success. Returned rather than assumed so a caller reading only the response can see the realized state."},"updatedAt":{"type":"string","format":"date-time","description":"RFC 3339 timestamp of the revocation. The boundary between signatures made while the key was valid and requests refused afterwards."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"KeyStatus":{"title":"KeyStatus","type":"string","enum":["active","revoked"],"description":"Lifecycle state. Only an `active` key may be named in a signing request; a `revoked` key is retained so historic signatures remain attributable, and MUST NOT be reactivated."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
