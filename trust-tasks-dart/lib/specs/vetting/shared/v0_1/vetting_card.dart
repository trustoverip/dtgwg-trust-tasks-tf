// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vetting/_shared/0.1/vetting-card.schema.json

/// The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`,
/// `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a
/// consumer with no knowledge of the specific token can still group by its prefix. The
/// token is the maintainer's own; no external vocabulary is primary. External
/// vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied
/// at PRESENTATION by a renderer, not at rest, so that a query written in any of them
/// can be matched without the store having to live inside any one of them. The `x:`
/// prefix is an open extension namespace and is not decoration. The closest prior art
/// — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim
/// types with no extensibility, and that is the specific way it failed the requirement
/// a holder actually has. An `x:` attribute stores, composes, binds and discloses
/// exactly like a known one; it renders generically and matches only an explicit
/// query.
typedef ClaimType = String;

/// VettingCardClaim
class VettingCardClaim {
  const VettingCardClaim({
    required this.type,
    required this.value,
    required this.provenance,
  });

  /// Read this payload from a decoded JSON object.
  factory VettingCardClaim.fromJson(Map<String, dynamic> json) =>
      VettingCardClaim(
        type: json['type'] as String,
        value: json['value'],
        provenance: json['provenance'] as String,
      );

  /// The claim type. `person.portrait` is refused: identity vetting works by a vetter
  /// looking at the person, not by transmitting their image.
  final ClaimType type;

  /// The claimed value, exactly as rendered from the applicant's persona — a string for
  /// most claim types, structured JSON for some (e.g. a postal address). Authored by the
  /// applicant and asserted under their signature; the vetter's check is what gives it
  /// any assurance.
  final Object? value;

  /// Where the value's assurance comes from. `selfAsserted` — the applicant says so, and
  /// the vetter's human check is the only assurance added — is the only value this
  /// version defines; a verifier MUST NOT treat any other value as adding assurance it
  /// does not understand.
  final String provenance;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'value': value,
        'provenance': provenance,
      };
}

/// A cryptographic digest as a multibase-encoded multihash — the encoding the W3C
/// Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one
/// `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
/// algorithm in-band, so the value is self-describing and the wire format survives an
/// algorithm change without a schema revision; multibase does the same for the base
/// encoding, so a verifier never infers base58 from base64url by context. A bare hex
/// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract
/// and is non-conforming here. This definition constrains the *encoding only*. What
/// the digest is computed over is stated by each referencing field, because it differs
/// legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS)
/// canonicalization, while a digest over an opaque artifact is taken over its bytes. A
/// field whose input is a JSON document and which does not name a canonicalization is
/// not reproducible. Restricted to the two multibase headers W3C Controlled
/// Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u`
/// (base64url-no-pad). CID permits others but states that "interoperability is not
/// guaranteed between implementations using such values", and a registry whose purpose
/// is interoperability should not mint digests a conforming verifier may be unable to
/// read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I
/// and l, and an earlier permissive pattern let three published examples carry digests
/// that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with
/// `did:key` and `did:webvh`.
typedef DigestMultibase = String;

/// A W3C Data Integrity proof by the card's publisher. Additional Data Integrity
/// members (e.g. `created`) are permitted and are covered as the cryptosuite defines.
class DataIntegrityProof {
  const DataIntegrityProof({
    required this.type,
    required this.cryptosuite,
    required this.verificationMethod,
    required this.proofPurpose,
    required this.proofValue,
  });

  /// Read this payload from a decoded JSON object.
  factory DataIntegrityProof.fromJson(Map<String, dynamic> json) =>
      DataIntegrityProof(
        type: json['type'] as String,
        cryptosuite: json['cryptosuite'] as String,
        verificationMethod: json['verificationMethod'] as String,
        proofPurpose: json['proofPurpose'] as String,
        proofValue: json['proofValue'] as String,
      );

  final String type;

  /// e.g. `eddsa-jcs-2022`.
  final String cryptosuite;

  /// A verification method of `publisher`, authorized for `assertionMethod`.
  final String verificationMethod;
  final String proofPurpose;
  final String proofValue;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'cryptosuite': cryptosuite,
        'verificationMethod': verificationMethod,
        'proofPurpose': proofPurpose,
        'proofValue': proofValue,
      };
}

/// VettingCardTypeItem is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const VettingCardTypeItem(String value) {
  static const VettingCardTypeItem verifiableDataStructure =
      VettingCardTypeItem('VerifiableDataStructure');
  static const VettingCardTypeItem relationshipCard =
      VettingCardTypeItem('RelationshipCard');
  static const VettingCardTypeItem vettingCard =
      VettingCardTypeItem('VettingCard');

  /// Every value this specification's schema permits.
  static const List<VettingCardTypeItem> values = <VettingCardTypeItem>[
    verifiableDataStructure,
    relationshipCard,
    vettingCard
  ];
}

/// VettingCard
class VettingCard {
  const VettingCard({
    required this.type,
    required this.id,
    required this.publisher,
    required this.cardVersion,
    required this.audience,
    required this.community,
    required this.challenge,
    required this.domain,
    required this.issuedAt,
    required this.expiresAt,
    required this.claims,
    required this.identityCommitment,
    required this.commitmentSalt,
    required this.proof,
  });

  /// Read this payload from a decoded JSON object.
  factory VettingCard.fromJson(Map<String, dynamic> json) => VettingCard(
        type: (json['type'] as List<dynamic>)
            .map((e) => VettingCardTypeItem(e as String))
            .toList(),
        id: json['id'] as String,
        publisher: json['publisher'] as String,
        cardVersion: json['cardVersion'] as int,
        audience: json['audience'] as String,
        community: json['community'] as String,
        challenge: json['challenge'] as String,
        domain: json['domain'] as String,
        issuedAt: json['issuedAt'] as String,
        expiresAt: json['expiresAt'] as String,
        claims: (json['claims'] as List<dynamic>)
            .map((e) => VettingCardClaim.fromJson(e as Map<String, dynamic>))
            .toList(),
        identityCommitment: json['identityCommitment'] as String,
        commitmentSalt: json['commitmentSalt'] as String,
        proof:
            DataIntegrityProof.fromJson(json['proof'] as Map<String, dynamic>),
      );

  /// Exactly `VerifiableDataStructure`, `RelationshipCard` and `VettingCard`, in any
  /// order.
  final List<VettingCardTypeItem> type;

  /// Fresh per card. A card is never re-sent to a second session under the same id.
  final String id;

  /// The applicant's DID — the issuer of the vetting request and the subject every
  /// resulting statement names. The card is signed with this DID's assertion key.
  final String publisher;
  final int cardVersion;

  /// The vetter's DID — the issuer of the vetting session. A card addressed to anyone
  /// else is refused.
  final String audience;

  /// The community the applicant is being vetted for.
  final String community;

  /// The session's challenge, copied verbatim.
  final String challenge;

  /// The session's domain, copied verbatim.
  final String domain;
  final String issuedAt;

  /// No later than the session's `expiresAt`.
  final String expiresAt;
  final List<VettingCardClaim> claims;

  /// digestMultibase over the RFC 8785 canonicalization of `{ "salt": commitmentSalt,
  /// "claims": R }`, where R is `{ "type", "value" }` for every card claim whose type
  /// the session lists in `requiredClaims`, ordered by `type` then `value` (code-point
  /// order). SHA-256 RECOMMENDED. Because the applicant uses one salt per application,
  /// every vetter of that application sees the same value.
  final DigestMultibase identityCommitment;

  /// 32 random bytes, base64url without padding, generated once per application. Goes to
  /// vetters inside the card and to nobody else: a party holding the commitment without
  /// the salt cannot test guesses at the claimed name.
  final String commitmentSalt;
  final DataIntegrityProof proof;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type.map((e) => e.value).toList(),
        'id': id,
        'publisher': publisher,
        'cardVersion': cardVersion,
        'audience': audience,
        'community': community,
        'challenge': challenge,
        'domain': domain,
        'issuedAt': issuedAt,
        'expiresAt': expiresAt,
        'claims': claims.map((e) => e.toJson()).toList(),
        'identityCommitment': identityCommitment,
        'commitmentSalt': commitmentSalt,
        'proof': proof.toJson(),
      };
}
