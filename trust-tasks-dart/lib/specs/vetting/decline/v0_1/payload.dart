// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vetting/decline/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// OPTIONAL. `couldNotVerify` — the vetter could not establish the claimed identity.
/// `documentMismatch` — the documentation did not match the card or the person.
/// `livenessFailed` — the match code could not be confirmed with the person.
/// `notComfortable` — the vetter prefers not to attest. `other` — none of these.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadCode(String value) {
  static const PayloadCode couldNotVerify = PayloadCode('couldNotVerify');
  static const PayloadCode documentMismatch = PayloadCode('documentMismatch');
  static const PayloadCode livenessFailed = PayloadCode('livenessFailed');
  static const PayloadCode notComfortable = PayloadCode('notComfortable');
  static const PayloadCode other = PayloadCode('other');

  /// Every value this specification's schema permits.
  static const List<PayloadCode> values = <PayloadCode>[
    couldNotVerify,
    documentMismatch,
    livenessFailed,
    notComfortable,
    other
  ];
}

/// A vetter tells an applicant it will not issue a Vetting Statement for an accepted
/// request. The reason code is optional: a vetter never has to justify declining. Sent
/// to the applicant only — never to the community.
class Payload {
  const Payload({
    required this.requestId,
    this.code,
    this.message,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        requestId: json['requestId'] as String,
        code: json['code'] == null ? null : PayloadCode(json['code'] as String),
        message: json['message'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The `requestId` from the vetter's acceptance of the applicant's vetting request.
  final String requestId;

  /// OPTIONAL. `couldNotVerify` — the vetter could not establish the claimed identity.
  /// `documentMismatch` — the documentation did not match the card or the person.
  /// `livenessFailed` — the match code could not be confirmed with the person.
  /// `notComfortable` — the vetter prefers not to attest. `other` — none of these.
  final PayloadCode? code;

  /// OPTIONAL vetter-authored text for the applicant. Attributed to the vetter on every
  /// surface that renders it; read by the applicant only.
  final String? message;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'requestId': requestId,
        if (code != null) 'code': code!.value,
        if (message != null) 'message': message!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vetting/decline/0.1';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vetting/decline/0.1","title":"Vetting Decline — payload","description":"A vetter tells an applicant it will not issue a Vetting Statement for an accepted request. The reason code is optional: a vetter never has to justify declining. Sent to the applicant only — never to the community.","type":"object","additionalProperties":false,"required":["requestId"],"properties":{"requestId":{"type":"string","minLength":1,"maxLength":128,"description":"The `requestId` from the vetter\'s acceptance of the applicant\'s vetting request."},"code":{"type":"string","enum":["couldNotVerify","documentMismatch","livenessFailed","notComfortable","other"],"description":"OPTIONAL. `couldNotVerify` — the vetter could not establish the claimed identity. `documentMismatch` — the documentation did not match the card or the person. `livenessFailed` — the match code could not be confirmed with the person. `notComfortable` — the vetter prefers not to attest. `other` — none of these."},"message":{"type":"string","minLength":1,"maxLength":500,"description":"OPTIONAL vetter-authored text for the applicant. Attributed to the vetter on every surface that renders it; read by the applicant only."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);
