// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/stats/show/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The depth of one queue and its effective limit.
class QueueDepth {
  const QueueDepth({
    required this.count,
    required this.bytes,
    this.limit,
    this.saturation,
    this.oldestAgeSeconds,
    this.deliveredUnacked,
  });

  /// Read this payload from a decoded JSON object.
  factory QueueDepth.fromJson(Map<String, dynamic> json) => QueueDepth(
        count: json['count'] as int,
        bytes: json['bytes'] as int,
        limit: json['limit'] as int?,
        saturation: json['saturation'] == null
            ? null
            : (json['saturation'] as num).toDouble(),
        oldestAgeSeconds: json['oldestAgeSeconds'] as int?,
        deliveredUnacked: json['deliveredUnacked'] as int?,
      );

  /// Messages currently in the queue.
  final int count;

  /// Total stored size of those messages, in bytes.
  final int bytes;

  /// The effective message-count limit for this queue after account overrides and
  /// mediator defaults are applied; -1 = unlimited.
  final int? limit;

  /// count ÷ limit. Absent when the limit is unlimited. May exceed 1 when a limit was
  /// lowered below the current depth.
  final double? saturation;

  /// Age of the oldest message in the queue, in seconds. Absent when the queue is empty
  /// or the mediator did not measure it.
  final int? oldestAgeSeconds;

  /// Messages in the queue whose state is `delivered` — handed out but not yet deleted
  /// by the recipient. Absent when the mediator did not measure it.
  final int? deliveredUnacked;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'count': count,
        'bytes': bytes,
        if (limit != null) 'limit': limit!,
        if (saturation != null) 'saturation': saturation!,
        if (oldestAgeSeconds != null) 'oldestAgeSeconds': oldestAgeSeconds!,
        if (deliveredUnacked != null) 'deliveredUnacked': deliveredUnacked!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseConnections, generated from its schema.
class ResponseConnections {
  const ResponseConnections({
    required this.websocketActive,
    this.websocketMax,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseConnections.fromJson(Map<String, dynamic> json) =>
      ResponseConnections(
        websocketActive: json['websocketActive'] as int,
        websocketMax: json['websocketMax'] as int?,
      );

  final int websocketActive;

  /// Configured ceiling; absent when unlimited.
  final int? websocketMax;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'websocketActive': websocketActive,
        if (websocketMax != null) 'websocketMax': websocketMax!,
      };
}

/// Lifetime counters since the mediator's store was created. Monotonic; a client
/// derives rates from successive readings.
class ResponseTotals {
  const ResponseTotals({
    required this.receivedCount,
    required this.receivedBytes,
    required this.sentCount,
    required this.sentBytes,
    required this.deletedCount,
    this.deletedBytes,
    this.websocketOpened,
    this.websocketClosed,
    this.sessionsCreated,
    this.sessionsAuthenticated,
    this.invitationsCreated,
    this.invitationsClaimed,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseTotals.fromJson(Map<String, dynamic> json) => ResponseTotals(
        receivedCount: json['receivedCount'] as int,
        receivedBytes: json['receivedBytes'] as int,
        sentCount: json['sentCount'] as int,
        sentBytes: json['sentBytes'] as int,
        deletedCount: json['deletedCount'] as int,
        deletedBytes: json['deletedBytes'] as int?,
        websocketOpened: json['websocketOpened'] as int?,
        websocketClosed: json['websocketClosed'] as int?,
        sessionsCreated: json['sessionsCreated'] as int?,
        sessionsAuthenticated: json['sessionsAuthenticated'] as int?,
        invitationsCreated: json['invitationsCreated'] as int?,
        invitationsClaimed: json['invitationsClaimed'] as int?,
      );

  final int receivedCount;
  final int receivedBytes;
  final int sentCount;
  final int sentBytes;
  final int deletedCount;
  final int? deletedBytes;
  final int? websocketOpened;
  final int? websocketClosed;
  final int? sessionsCreated;
  final int? sessionsAuthenticated;
  final int? invitationsCreated;
  final int? invitationsClaimed;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'receivedCount': receivedCount,
        'receivedBytes': receivedBytes,
        'sentCount': sentCount,
        'sentBytes': sentBytes,
        'deletedCount': deletedCount,
        if (deletedBytes != null) 'deletedBytes': deletedBytes!,
        if (websocketOpened != null) 'websocketOpened': websocketOpened!,
        if (websocketClosed != null) 'websocketClosed': websocketClosed!,
        if (sessionsCreated != null) 'sessionsCreated': sessionsCreated!,
        if (sessionsAuthenticated != null)
          'sessionsAuthenticated': sessionsAuthenticated!,
        if (invitationsCreated != null)
          'invitationsCreated': invitationsCreated!,
        if (invitationsClaimed != null)
          'invitationsClaimed': invitationsClaimed!,
      };
}

/// State of the store circuit breaker. `open` means the mediator is shedding store
/// operations.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseForwardingCircuitBreaker(String value) {
  static const ResponseForwardingCircuitBreaker closed =
      ResponseForwardingCircuitBreaker('closed');
  static const ResponseForwardingCircuitBreaker open =
      ResponseForwardingCircuitBreaker('open');
  static const ResponseForwardingCircuitBreaker halfOpen =
      ResponseForwardingCircuitBreaker('halfOpen');

  /// Every value this specification's schema permits.
  static const List<ResponseForwardingCircuitBreaker> values =
      <ResponseForwardingCircuitBreaker>[closed, open, halfOpen];
}

/// ResponseForwarding, generated from its schema.
class ResponseForwarding {
  const ResponseForwarding({
    required this.queueLength,
    this.queueLimit,
    this.circuitBreaker,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseForwarding.fromJson(Map<String, dynamic> json) =>
      ResponseForwarding(
        queueLength: json['queueLength'] as int,
        queueLimit: json['queueLimit'] as int?,
        circuitBreaker: json['circuitBreaker'] == null
            ? null
            : ResponseForwardingCircuitBreaker(
                json['circuitBreaker'] as String),
      );

  /// Messages waiting to be forwarded to other mediators.
  final int queueLength;
  final int? queueLimit;

  /// State of the store circuit breaker. `open` means the mediator is shedding store
  /// operations.
  final ResponseForwardingCircuitBreaker? circuitBreaker;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'queueLength': queueLength,
        if (queueLimit != null) 'queueLimit': queueLimit!,
        if (circuitBreaker != null) 'circuitBreaker': circuitBreaker!.value,
      };
}

/// Aggregate of the mediator's most recent queue survey. Absent when no survey has
/// completed.
class ResponseQueues {
  const ResponseQueues({
    required this.surveyedAt,
    required this.accounts,
    this.truncated,
    this.receive,
    this.send,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseQueues.fromJson(Map<String, dynamic> json) => ResponseQueues(
        surveyedAt: json['surveyedAt'] as String,
        accounts: json['accounts'] as int,
        truncated: json['truncated'] as bool?,
        receive: json['receive'] == null
            ? null
            : QueueDepth.fromJson(json['receive'] as Map<String, dynamic>),
        send: json['send'] == null
            ? null
            : QueueDepth.fromJson(json['send'] as Map<String, dynamic>),
      );

  final String surveyedAt;

  /// Accounts the survey covered.
  final int accounts;

  /// The survey stopped before covering every account.
  final bool? truncated;

  /// Totals across every surveyed receive queue; `saturation` is the maximum of any
  /// single queue.
  final QueueDepth? receive;

  /// Totals across every surveyed send queue; `saturation` is the maximum of any single
  /// queue.
  final QueueDepth? send;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'surveyedAt': surveyedAt,
        'accounts': accounts,
        if (truncated != null) 'truncated': truncated!,
        if (receive != null) 'receive': receive!.toJson(),
        if (send != null) 'send': send!.toJson(),
      };
}

/// The success response to a messaging/stats/show request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/messaging/stats/show/0.1#response.
class Response {
  const Response({
    required this.version,
    this.startedAt,
    required this.uptimeSeconds,
    required this.connections,
    required this.totals,
    required this.forwarding,
    this.queues,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        version: json['version'] as String,
        startedAt: json['startedAt'] as String?,
        uptimeSeconds: json['uptimeSeconds'] as int,
        connections: ResponseConnections.fromJson(
            json['connections'] as Map<String, dynamic>),
        totals: ResponseTotals.fromJson(json['totals'] as Map<String, dynamic>),
        forwarding: ResponseForwarding.fromJson(
            json['forwarding'] as Map<String, dynamic>),
        queues: json['queues'] == null
            ? null
            : ResponseQueues.fromJson(json['queues'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The mediator software's version string.
  final String version;
  final String? startedAt;
  final int uptimeSeconds;
  final ResponseConnections connections;

  /// Lifetime counters since the mediator's store was created. Monotonic; a client
  /// derives rates from successive readings.
  final ResponseTotals totals;
  final ResponseForwarding forwarding;

  /// Aggregate of the mediator's most recent queue survey. Absent when no survey has
  /// completed.
  final ResponseQueues? queues;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'version': version,
        if (startedAt != null) 'startedAt': startedAt!,
        'uptimeSeconds': uptimeSeconds,
        'connections': connections.toJson(),
        'totals': totals.toJson(),
        'forwarding': forwarding.toJson(),
        if (queues != null) 'queues': queues!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging — Show Mediator Statistics — payload
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/messaging/stats/show/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/stats/show/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/stats/show/0.1","title":"Messaging — Show Mediator Statistics — payload","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Show Mediator Statistics — response payload","description":"The success response to a messaging/stats/show request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/stats/show/0.1#response.","type":"object","additionalProperties":false,"properties":{"version":{"type":"string","minLength":1,"description":"The mediator software\'s version string."},"startedAt":{"type":"string","format":"date-time"},"uptimeSeconds":{"type":"integer","minimum":0},"connections":{"type":"object","additionalProperties":false,"required":["websocketActive"],"properties":{"websocketActive":{"type":"integer","minimum":0},"websocketMax":{"type":"integer","minimum":0,"description":"Configured ceiling; absent when unlimited."}}},"totals":{"type":"object","additionalProperties":false,"description":"Lifetime counters since the mediator\'s store was created. Monotonic; a client derives rates from successive readings.","required":["receivedCount","receivedBytes","sentCount","sentBytes","deletedCount"],"properties":{"receivedCount":{"type":"integer","minimum":0},"receivedBytes":{"type":"integer","minimum":0},"sentCount":{"type":"integer","minimum":0},"sentBytes":{"type":"integer","minimum":0},"deletedCount":{"type":"integer","minimum":0},"deletedBytes":{"type":"integer","minimum":0},"websocketOpened":{"type":"integer","minimum":0},"websocketClosed":{"type":"integer","minimum":0},"sessionsCreated":{"type":"integer","minimum":0},"sessionsAuthenticated":{"type":"integer","minimum":0},"invitationsCreated":{"type":"integer","minimum":0},"invitationsClaimed":{"type":"integer","minimum":0}}},"forwarding":{"type":"object","additionalProperties":false,"required":["queueLength"],"properties":{"queueLength":{"type":"integer","minimum":0,"description":"Messages waiting to be forwarded to other mediators."},"queueLimit":{"type":"integer","minimum":0},"circuitBreaker":{"type":"string","enum":["closed","open","halfOpen"],"description":"State of the store circuit breaker. `open` means the mediator is shedding store operations."}}},"queues":{"type":"object","additionalProperties":false,"description":"Aggregate of the mediator\'s most recent queue survey. Absent when no survey has completed.","required":["surveyedAt","accounts"],"properties":{"surveyedAt":{"type":"string","format":"date-time"},"accounts":{"type":"integer","minimum":0,"description":"Accounts the survey covered."},"truncated":{"type":"boolean","description":"The survey stopped before covering every account."},"receive":{"\$ref":"#/\$defs/QueueDepth","description":"Totals across every surveyed receive queue; `saturation` is the maximum of any single queue."},"send":{"\$ref":"#/\$defs/QueueDepth","description":"Totals across every surveyed send queue; `saturation` is the maximum of any single queue."}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["version","uptimeSeconds","connections","totals","forwarding"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"QueueDepth":{"title":"QueueDepth","type":"object","additionalProperties":false,"required":["count","bytes"],"description":"The depth of one queue and its effective limit.","properties":{"count":{"type":"integer","minimum":0,"description":"Messages currently in the queue."},"bytes":{"type":"integer","minimum":0,"description":"Total stored size of those messages, in bytes."},"limit":{"type":"integer","minimum":-1,"description":"The effective message-count limit for this queue after account overrides and mediator defaults are applied; -1 = unlimited."},"saturation":{"type":"number","minimum":0,"description":"count ÷ limit. Absent when the limit is unlimited. May exceed 1 when a limit was lowered below the current depth."},"oldestAgeSeconds":{"type":"integer","minimum":0,"description":"Age of the oldest message in the queue, in seconds. Absent when the queue is empty or the mediator did not measure it."},"deliveredUnacked":{"type":"integer","minimum":0,"description":"Messages in the queue whose state is `delivered` — handed out but not yet deleted by the recipient. Absent when the mediator did not measure it."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Show Mediator Statistics — response payload","description":"The success response to a messaging/stats/show request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/stats/show/0.1#response.","type":"object","additionalProperties":false,"properties":{"version":{"type":"string","minLength":1,"description":"The mediator software\'s version string."},"startedAt":{"type":"string","format":"date-time"},"uptimeSeconds":{"type":"integer","minimum":0},"connections":{"type":"object","additionalProperties":false,"required":["websocketActive"],"properties":{"websocketActive":{"type":"integer","minimum":0},"websocketMax":{"type":"integer","minimum":0,"description":"Configured ceiling; absent when unlimited."}}},"totals":{"type":"object","additionalProperties":false,"description":"Lifetime counters since the mediator\'s store was created. Monotonic; a client derives rates from successive readings.","required":["receivedCount","receivedBytes","sentCount","sentBytes","deletedCount"],"properties":{"receivedCount":{"type":"integer","minimum":0},"receivedBytes":{"type":"integer","minimum":0},"sentCount":{"type":"integer","minimum":0},"sentBytes":{"type":"integer","minimum":0},"deletedCount":{"type":"integer","minimum":0},"deletedBytes":{"type":"integer","minimum":0},"websocketOpened":{"type":"integer","minimum":0},"websocketClosed":{"type":"integer","minimum":0},"sessionsCreated":{"type":"integer","minimum":0},"sessionsAuthenticated":{"type":"integer","minimum":0},"invitationsCreated":{"type":"integer","minimum":0},"invitationsClaimed":{"type":"integer","minimum":0}}},"forwarding":{"type":"object","additionalProperties":false,"required":["queueLength"],"properties":{"queueLength":{"type":"integer","minimum":0,"description":"Messages waiting to be forwarded to other mediators."},"queueLimit":{"type":"integer","minimum":0},"circuitBreaker":{"type":"string","enum":["closed","open","halfOpen"],"description":"State of the store circuit breaker. `open` means the mediator is shedding store operations."}}},"queues":{"type":"object","additionalProperties":false,"description":"Aggregate of the mediator\'s most recent queue survey. Absent when no survey has completed.","required":["surveyedAt","accounts"],"properties":{"surveyedAt":{"type":"string","format":"date-time"},"accounts":{"type":"integer","minimum":0,"description":"Accounts the survey covered."},"truncated":{"type":"boolean","description":"The survey stopped before covering every account."},"receive":{"\$ref":"#/\$defs/QueueDepth","description":"Totals across every surveyed receive queue; `saturation` is the maximum of any single queue."},"send":{"\$ref":"#/\$defs/QueueDepth","description":"Totals across every surveyed send queue; `saturation` is the maximum of any single queue."}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["version","uptimeSeconds","connections","totals","forwarding"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"QueueDepth":{"title":"QueueDepth","type":"object","additionalProperties":false,"required":["count","bytes"],"description":"The depth of one queue and its effective limit.","properties":{"count":{"type":"integer","minimum":0,"description":"Messages currently in the queue."},"bytes":{"type":"integer","minimum":0,"description":"Total stored size of those messages, in bytes."},"limit":{"type":"integer","minimum":-1,"description":"The effective message-count limit for this queue after account overrides and mediator defaults are applied; -1 = unlimited."},"saturation":{"type":"number","minimum":0,"description":"count ÷ limit. Absent when the limit is unlimited. May exceed 1 when a limit was lowered below the current depth."},"oldestAgeSeconds":{"type":"integer","minimum":0,"description":"Age of the oldest message in the queue, in seconds. Absent when the queue is empty or the mediator did not measure it."},"deliveredUnacked":{"type":"integer","minimum":0,"description":"Messages in the queue whose state is `delivered` — handed out but not yet deleted by the recipient. Absent when the mediator did not measure it."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
