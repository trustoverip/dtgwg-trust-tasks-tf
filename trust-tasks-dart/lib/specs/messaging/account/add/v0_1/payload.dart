// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/account/add/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the
/// account's controlling DID, carried verbatim and compared by exact string equality.
/// For privacy — and because some mediators key accounts by a one-way hash and never
/// hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an
/// equally valid value here: producer and consumer simply agree on the same opaque
/// identifier and compare by exact string equality. The field carries whichever form
/// the issuing mediator uses.
typedef Vid = String;

/// The account's role at the mediator. `standard` is an ordinary served account;
/// `admin`/`rootAdmin` may administer other accounts; `mediator` is the mediator's own
/// account. Only a rootAdmin may assign or modify the rootAdmin role.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const AccountType(String value) {
  static const AccountType standard = AccountType('standard');
  static const AccountType admin = AccountType('admin');
  static const AccountType rootAdmin = AccountType('rootAdmin');
  static const AccountType mediator = AccountType('mediator');

  /// Every value this specification's schema permits.
  static const List<AccountType> values = <AccountType>[
    standard,
    admin,
    rootAdmin,
    mediator
  ];
}

/// How the account's access list is interpreted. `explicitAllow` = an allowlist (empty
/// denies everyone); `explicitDeny` = a denylist (empty allows everyone).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const MediatorAclAccessListMode(String value) {
  static const MediatorAclAccessListMode explicitAllow =
      MediatorAclAccessListMode('explicitAllow');
  static const MediatorAclAccessListMode explicitDeny =
      MediatorAclAccessListMode('explicitDeny');

  /// Every value this specification's schema permits.
  static const List<MediatorAclAccessListMode> values =
      <MediatorAclAccessListMode>[explicitAllow, explicitDeny];
}

/// The mediator's per-account access-control capability set, expressed as named
/// booleans (the transport-agnostic form of the mediator's internal capability flags).
/// On a set request, members omitted are left unchanged; a get/response carries the
/// full realized set.
class MediatorAcl {
  const MediatorAcl({
    this.accessListMode,
    this.blocked,
    this.local,
    this.sendMessages,
    this.receiveMessages,
    this.sendForwarded,
    this.receiveForwarded,
    this.createInvites,
    this.anonReceive,
    this.selfManageList,
    this.selfManageSendQueueLimit,
    this.selfManageReceiveQueueLimit,
    this.didcommEnabled,
    this.tspEnabled,
  });

  /// Read this payload from a decoded JSON object.
  factory MediatorAcl.fromJson(Map<String, dynamic> json) => MediatorAcl(
        accessListMode: json['accessListMode'] == null
            ? null
            : MediatorAclAccessListMode(json['accessListMode'] as String),
        blocked: json['blocked'] as bool?,
        local: json['local'] as bool?,
        sendMessages: json['sendMessages'] as bool?,
        receiveMessages: json['receiveMessages'] as bool?,
        sendForwarded: json['sendForwarded'] as bool?,
        receiveForwarded: json['receiveForwarded'] as bool?,
        createInvites: json['createInvites'] as bool?,
        anonReceive: json['anonReceive'] as bool?,
        selfManageList: json['selfManageList'] as bool?,
        selfManageSendQueueLimit: json['selfManageSendQueueLimit'] as bool?,
        selfManageReceiveQueueLimit:
            json['selfManageReceiveQueueLimit'] as bool?,
        didcommEnabled: json['didcommEnabled'] as bool?,
        tspEnabled: json['tspEnabled'] as bool?,
      );

  /// How the account's access list is interpreted. `explicitAllow` = an allowlist (empty
  /// denies everyone); `explicitDeny` = a denylist (empty allows everyone).
  final MediatorAclAccessListMode? accessListMode;

  /// The account is blocked from authenticating and transacting.
  final bool? blocked;

  /// Messages for this account may be stored locally at this mediator for pickup.
  final bool? local;

  /// May send direct messages through the mediator.
  final bool? sendMessages;

  /// May receive direct messages.
  final bool? receiveMessages;

  /// May send routing/forward (relay) messages.
  final bool? sendForwarded;

  /// May be the next hop of a forwarded message.
  final bool? receiveForwarded;

  /// May create out-of-band invitations.
  final bool? createInvites;

  /// Accepts anonymous (no authenticated sender) messages.
  final bool? anonReceive;

  /// May self-manage its own access list.
  final bool? selfManageList;

  /// May self-manage its own send-queue limit.
  final bool? selfManageSendQueueLimit;

  /// May self-manage its own receive-queue limit.
  final bool? selfManageReceiveQueueLimit;

  /// The account accepts DIDComm-protocol delivery. Default true; set false for a
  /// TSP-only node.
  final bool? didcommEnabled;

  /// The account accepts TSP-protocol delivery. Default true; set false for a
  /// DIDComm-only node.
  final bool? tspEnabled;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (accessListMode != null) 'accessListMode': accessListMode!.value,
        if (blocked != null) 'blocked': blocked!,
        if (local != null) 'local': local!,
        if (sendMessages != null) 'sendMessages': sendMessages!,
        if (receiveMessages != null) 'receiveMessages': receiveMessages!,
        if (sendForwarded != null) 'sendForwarded': sendForwarded!,
        if (receiveForwarded != null) 'receiveForwarded': receiveForwarded!,
        if (createInvites != null) 'createInvites': createInvites!,
        if (anonReceive != null) 'anonReceive': anonReceive!,
        if (selfManageList != null) 'selfManageList': selfManageList!,
        if (selfManageSendQueueLimit != null)
          'selfManageSendQueueLimit': selfManageSendQueueLimit!,
        if (selfManageReceiveQueueLimit != null)
          'selfManageReceiveQueueLimit': selfManageReceiveQueueLimit!,
        if (didcommEnabled != null) 'didcommEnabled': didcommEnabled!,
        if (tspEnabled != null) 'tspEnabled': tspEnabled!,
      };
}

/// Per-account queued-message limits. A value of -1 means unlimited; a member omitted
/// on a change request leaves that limit unchanged.
class QueueLimits {
  const QueueLimits({
    this.sendQueueLimit,
    this.receiveQueueLimit,
  });

  /// Read this payload from a decoded JSON object.
  factory QueueLimits.fromJson(Map<String, dynamic> json) => QueueLimits(
        sendQueueLimit: json['sendQueueLimit'] as int?,
        receiveQueueLimit: json['receiveQueueLimit'] as int?,
      );

  /// Maximum queued send messages; -1 = unlimited.
  final int? sendQueueLimit;

  /// Maximum queued receive messages; -1 = unlimited.
  final int? receiveQueueLimit;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (sendQueueLimit != null) 'sendQueueLimit': sendQueueLimit!,
        if (receiveQueueLimit != null) 'receiveQueueLimit': receiveQueueLimit!,
      };
}

/// Message counts split by wire protocol. An absent member is a protocol the mediator
/// counted nothing for. The member names match the WireProtocol values the traffic
/// monitor reports.
class ProtocolCounts {
  const ProtocolCounts({
    this.didcomm,
    this.didcommV1,
    this.tsp,
    this.other,
  });

  /// Read this payload from a decoded JSON object.
  factory ProtocolCounts.fromJson(Map<String, dynamic> json) => ProtocolCounts(
        didcomm: json['didcomm'] as int?,
        didcommV1: json['didcommV1'] as int?,
        tsp: json['tsp'] as int?,
        other: json['other'] as int?,
      );

  /// DIDComm v2 (JWE/JWS).
  final int? didcomm;

  /// A DIDComm v1 envelope.
  final int? didcommV1;

  /// A Trust Spanning Protocol message.
  final int? tsp;

  /// Anything the mediator could not classify.
  final int? other;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (didcomm != null) 'didcomm': didcomm!,
        if (didcommV1 != null) 'didcommV1': didcommV1!,
        if (tsp != null) 'tsp': tsp!,
        if (other != null) 'other': other!,
      };
}

/// What an account has sent and received over its lifetime, as the mediator counted
/// it. Every member is optional: a mediator reports what it keeps, and a counter
/// absent from a response was not kept rather than zero. Counters survive restarts and
/// are not reset by reading them; removing an account discards them.
class AccountStats {
  const AccountStats({
    this.messagesReceived,
    this.messagesSent,
    this.bytesReceived,
    this.bytesSent,
    this.receivedByProtocol,
    this.sentByProtocol,
  });

  /// Read this payload from a decoded JSON object.
  factory AccountStats.fromJson(Map<String, dynamic> json) => AccountStats(
        messagesReceived: json['messagesReceived'] as int?,
        messagesSent: json['messagesSent'] as int?,
        bytesReceived: json['bytesReceived'] as int?,
        bytesSent: json['bytesSent'] as int?,
        receivedByProtocol: json['receivedByProtocol'] == null
            ? null
            : ProtocolCounts.fromJson(
                json['receivedByProtocol'] as Map<String, dynamic>),
        sentByProtocol: json['sentByProtocol'] == null
            ? null
            : ProtocolCounts.fromJson(
                json['sentByProtocol'] as Map<String, dynamic>),
      );

  /// Messages the mediator accepted addressed to this account.
  final int? messagesReceived;

  /// Messages the mediator accepted from this account.
  final int? messagesSent;

  /// Total size of the messages counted by messagesReceived.
  final int? bytesReceived;

  /// Total size of the messages counted by messagesSent.
  final int? bytesSent;

  /// messagesReceived split by the wire protocol the message arrived in.
  final ProtocolCounts? receivedByProtocol;

  /// messagesSent split by the wire protocol the message was sent in.
  final ProtocolCounts? sentByProtocol;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (messagesReceived != null) 'messagesReceived': messagesReceived!,
        if (messagesSent != null) 'messagesSent': messagesSent!,
        if (bytesReceived != null) 'bytesReceived': bytesReceived!,
        if (bytesSent != null) 'bytesSent': bytesSent!,
        if (receivedByProtocol != null)
          'receivedByProtocol': receivedByProtocol!.toJson(),
        if (sentByProtocol != null) 'sentByProtocol': sentByProtocol!.toJson(),
      };
}

/// The mediator's view of one served account.
class Account {
  const Account({
    required this.did,
    required this.accountType,
    required this.acl,
    this.queueLimits,
    this.sendQueueCount,
    this.sendQueueBytes,
    this.receiveQueueCount,
    this.receiveQueueBytes,
    this.accessListCount,
    this.lastReceivedAt,
    this.lastAuthenticatedAt,
    this.stats,
  });

  /// Read this payload from a decoded JSON object.
  factory Account.fromJson(Map<String, dynamic> json) => Account(
        did: json['did'] as String,
        accountType: AccountType(json['accountType'] as String),
        acl: MediatorAcl.fromJson(json['acl'] as Map<String, dynamic>),
        queueLimits: json['queueLimits'] == null
            ? null
            : QueueLimits.fromJson(json['queueLimits'] as Map<String, dynamic>),
        sendQueueCount: json['sendQueueCount'] as int?,
        sendQueueBytes: json['sendQueueBytes'] as int?,
        receiveQueueCount: json['receiveQueueCount'] as int?,
        receiveQueueBytes: json['receiveQueueBytes'] as int?,
        accessListCount: json['accessListCount'] as int?,
        lastReceivedAt: json['lastReceivedAt'] as int?,
        lastAuthenticatedAt: json['lastAuthenticatedAt'] as int?,
        stats: json['stats'] == null
            ? null
            : AccountStats.fromJson(json['stats'] as Map<String, dynamic>),
      );

  /// The account's controlling DID, or — for privacy, and for mediators that key
  /// accounts by a one-way hash and never hold the full DID — a stable hash of that DID
  /// (see `Vid`). Whichever form is used, it is the opaque account identifier the other
  /// `account/*`, `acl/*`, and `access-list/*` tasks accept.
  final Vid did;
  final AccountType accountType;
  final MediatorAcl acl;
  final QueueLimits? queueLimits;

  /// Current count of queued send messages.
  final int? sendQueueCount;

  /// Current byte size of queued send messages.
  final int? sendQueueBytes;

  /// Current count of queued receive messages.
  final int? receiveQueueCount;

  /// Current byte size of queued receive messages.
  final int? receiveQueueBytes;

  /// Number of entries in the account's access list.
  final int? accessListCount;

  /// Unix epoch seconds at which the mediator last accepted a message addressed to this
  /// account. Present only when the request set `includeActivity` and the mediator has
  /// recorded such a message; MAY lag the true time by up to 60 seconds.
  final int? lastReceivedAt;

  /// Unix epoch seconds at which this account last completed authentication with the
  /// mediator, over any transport. Present only when the request set `includeActivity`
  /// and the mediator has recorded an authentication.
  final int? lastAuthenticatedAt;

  /// Lifetime counters the mediator keeps for this account. Present only when the
  /// request set `includeStats`.
  final AccountStats? stats;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'accountType': accountType.value,
        'acl': acl.toJson(),
        if (queueLimits != null) 'queueLimits': queueLimits!.toJson(),
        if (sendQueueCount != null) 'sendQueueCount': sendQueueCount!,
        if (sendQueueBytes != null) 'sendQueueBytes': sendQueueBytes!,
        if (receiveQueueCount != null) 'receiveQueueCount': receiveQueueCount!,
        if (receiveQueueBytes != null) 'receiveQueueBytes': receiveQueueBytes!,
        if (accessListCount != null) 'accessListCount': accessListCount!,
        if (lastReceivedAt != null) 'lastReceivedAt': lastReceivedAt!,
        if (lastAuthenticatedAt != null)
          'lastAuthenticatedAt': lastAuthenticatedAt!,
        if (stats != null) 'stats': stats!.toJson(),
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a messaging/account/add request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/messaging/account/add/0.1#response.
class Response {
  const Response({
    required this.account,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        account: Account.fromJson(json['account'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The full realized mediator view of the newly created account.
  final Account account;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'account': account.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging Add Account — payload
class Payload {
  const Payload({
    required this.did,
    this.accountType,
    this.acl,
    this.queueLimits,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        accountType: json['accountType'] == null
            ? null
            : AccountType(json['accountType'] as String),
        acl: json['acl'] == null
            ? null
            : MediatorAcl.fromJson(json['acl'] as Map<String, dynamic>),
        queueLimits: json['queueLimits'] == null
            ? null
            : QueueLimits.fromJson(json['queueLimits'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID of the account to register.
  final Vid did;

  /// The role to assign the new account. Defaults to `standard`; assigning a
  /// non-standard role requires administrative standing (only a rootAdmin may assign
  /// rootAdmin).
  final AccountType? accountType;

  /// Initial access-control capabilities. Members omitted take the mediator's configured
  /// defaults.
  final MediatorAcl? acl;

  /// Initial queued-message limits. Members omitted take the mediator's configured
  /// defaults.
  final QueueLimits? queueLimits;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (accountType != null) 'accountType': accountType!.value,
        if (acl != null) 'acl': acl!.toJson(),
        if (queueLimits != null) 'queueLimits': queueLimits!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/messaging/account/add/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/account/add/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/account/add/0.1","title":"Messaging Add Account — payload","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"\$ref":"#/\$defs/Vid","description":"The DID of the account to register."},"accountType":{"\$ref":"#/\$defs/AccountType","default":"standard","description":"The role to assign the new account. Defaults to `standard`; assigning a non-standard role requires administrative standing (only a rootAdmin may assign rootAdmin)."},"acl":{"\$ref":"#/\$defs/MediatorAcl","description":"Initial access-control capabilities. Members omitted take the mediator\'s configured defaults."},"queueLimits":{"\$ref":"#/\$defs/QueueLimits","description":"Initial queued-message limits. Members omitted take the mediator\'s configured defaults."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging Add Account — response payload","description":"The success response to a messaging/account/add request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/account/add/0.1#response.","type":"object","additionalProperties":false,"required":["account"],"properties":{"account":{"\$ref":"#/\$defs/Account","description":"The full realized mediator view of the newly created account."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Account":{"title":"Account","type":"object","additionalProperties":false,"required":["did","accountType","acl"],"description":"The mediator\'s view of one served account.","properties":{"did":{"\$ref":"#/\$defs/Vid","description":"The account\'s controlling DID, or — for privacy, and for mediators that key accounts by a one-way hash and never hold the full DID — a stable hash of that DID (see `Vid`). Whichever form is used, it is the opaque account identifier the other `account/*`, `acl/*`, and `access-list/*` tasks accept."},"accountType":{"\$ref":"#/\$defs/AccountType"},"acl":{"\$ref":"#/\$defs/MediatorAcl"},"queueLimits":{"\$ref":"#/\$defs/QueueLimits"},"sendQueueCount":{"type":"integer","minimum":0,"description":"Current count of queued send messages."},"sendQueueBytes":{"type":"integer","minimum":0,"description":"Current byte size of queued send messages."},"receiveQueueCount":{"type":"integer","minimum":0,"description":"Current count of queued receive messages."},"receiveQueueBytes":{"type":"integer","minimum":0,"description":"Current byte size of queued receive messages."},"accessListCount":{"type":"integer","minimum":0,"description":"Number of entries in the account\'s access list."},"lastReceivedAt":{"type":"integer","minimum":0,"description":"Unix epoch seconds at which the mediator last accepted a message addressed to this account. Present only when the request set `includeActivity` and the mediator has recorded such a message; MAY lag the true time by up to 60 seconds."},"lastAuthenticatedAt":{"type":"integer","minimum":0,"description":"Unix epoch seconds at which this account last completed authentication with the mediator, over any transport. Present only when the request set `includeActivity` and the mediator has recorded an authentication."},"stats":{"\$ref":"#/\$defs/AccountStats","description":"Lifetime counters the mediator keeps for this account. Present only when the request set `includeStats`."}}},"AccountStats":{"title":"AccountStats","type":"object","additionalProperties":false,"description":"What an account has sent and received over its lifetime, as the mediator counted it. Every member is optional: a mediator reports what it keeps, and a counter absent from a response was not kept rather than zero. Counters survive restarts and are not reset by reading them; removing an account discards them.","properties":{"messagesReceived":{"type":"integer","minimum":0,"description":"Messages the mediator accepted addressed to this account."},"messagesSent":{"type":"integer","minimum":0,"description":"Messages the mediator accepted from this account."},"bytesReceived":{"type":"integer","minimum":0,"description":"Total size of the messages counted by messagesReceived."},"bytesSent":{"type":"integer","minimum":0,"description":"Total size of the messages counted by messagesSent."},"receivedByProtocol":{"\$ref":"#/\$defs/ProtocolCounts","description":"messagesReceived split by the wire protocol the message arrived in."},"sentByProtocol":{"\$ref":"#/\$defs/ProtocolCounts","description":"messagesSent split by the wire protocol the message was sent in."}}},"ProtocolCounts":{"title":"ProtocolCounts","type":"object","additionalProperties":false,"description":"Message counts split by wire protocol. An absent member is a protocol the mediator counted nothing for. The member names match the WireProtocol values the traffic monitor reports.","properties":{"didcomm":{"type":"integer","minimum":0,"description":"DIDComm v2 (JWE/JWS)."},"didcommV1":{"type":"integer","minimum":0,"description":"A DIDComm v1 envelope."},"tsp":{"type":"integer","minimum":0,"description":"A Trust Spanning Protocol message."},"other":{"type":"integer","minimum":0,"description":"Anything the mediator could not classify."}}},"QueueLimits":{"title":"QueueLimits","type":"object","additionalProperties":false,"description":"Per-account queued-message limits. A value of -1 means unlimited; a member omitted on a change request leaves that limit unchanged.","properties":{"sendQueueLimit":{"type":"integer","minimum":-1,"description":"Maximum queued send messages; -1 = unlimited."},"receiveQueueLimit":{"type":"integer","minimum":-1,"description":"Maximum queued receive messages; -1 = unlimited."}}},"MediatorAcl":{"title":"MediatorAcl","type":"object","additionalProperties":false,"description":"The mediator\'s per-account access-control capability set, expressed as named booleans (the transport-agnostic form of the mediator\'s internal capability flags). On a set request, members omitted are left unchanged; a get/response carries the full realized set.","properties":{"accessListMode":{"type":"string","enum":["explicitAllow","explicitDeny"],"description":"How the account\'s access list is interpreted. `explicitAllow` = an allowlist (empty denies everyone); `explicitDeny` = a denylist (empty allows everyone)."},"blocked":{"type":"boolean","description":"The account is blocked from authenticating and transacting."},"local":{"type":"boolean","description":"Messages for this account may be stored locally at this mediator for pickup."},"sendMessages":{"type":"boolean","description":"May send direct messages through the mediator."},"receiveMessages":{"type":"boolean","description":"May receive direct messages."},"sendForwarded":{"type":"boolean","description":"May send routing/forward (relay) messages."},"receiveForwarded":{"type":"boolean","description":"May be the next hop of a forwarded message."},"createInvites":{"type":"boolean","description":"May create out-of-band invitations."},"anonReceive":{"type":"boolean","description":"Accepts anonymous (no authenticated sender) messages."},"selfManageList":{"type":"boolean","description":"May self-manage its own access list."},"selfManageSendQueueLimit":{"type":"boolean","description":"May self-manage its own send-queue limit."},"selfManageReceiveQueueLimit":{"type":"boolean","description":"May self-manage its own receive-queue limit."},"didcommEnabled":{"type":"boolean","description":"The account accepts DIDComm-protocol delivery. Default true; set false for a TSP-only node."},"tspEnabled":{"type":"boolean","description":"The account accepts TSP-protocol delivery. Default true; set false for a DIDComm-only node."}}},"AccountType":{"title":"AccountType","type":"string","enum":["standard","admin","rootAdmin","mediator"],"description":"The account\'s role at the mediator. `standard` is an ordinary served account; `admin`/`rootAdmin` may administer other accounts; `mediator` is the mediator\'s own account. Only a rootAdmin may assign or modify the rootAdmin role."},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging Add Account — response payload","description":"The success response to a messaging/account/add request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/account/add/0.1#response.","type":"object","additionalProperties":false,"required":["account"],"properties":{"account":{"\$ref":"#/\$defs/Account","description":"The full realized mediator view of the newly created account."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Account":{"title":"Account","type":"object","additionalProperties":false,"required":["did","accountType","acl"],"description":"The mediator\'s view of one served account.","properties":{"did":{"\$ref":"#/\$defs/Vid","description":"The account\'s controlling DID, or — for privacy, and for mediators that key accounts by a one-way hash and never hold the full DID — a stable hash of that DID (see `Vid`). Whichever form is used, it is the opaque account identifier the other `account/*`, `acl/*`, and `access-list/*` tasks accept."},"accountType":{"\$ref":"#/\$defs/AccountType"},"acl":{"\$ref":"#/\$defs/MediatorAcl"},"queueLimits":{"\$ref":"#/\$defs/QueueLimits"},"sendQueueCount":{"type":"integer","minimum":0,"description":"Current count of queued send messages."},"sendQueueBytes":{"type":"integer","minimum":0,"description":"Current byte size of queued send messages."},"receiveQueueCount":{"type":"integer","minimum":0,"description":"Current count of queued receive messages."},"receiveQueueBytes":{"type":"integer","minimum":0,"description":"Current byte size of queued receive messages."},"accessListCount":{"type":"integer","minimum":0,"description":"Number of entries in the account\'s access list."},"lastReceivedAt":{"type":"integer","minimum":0,"description":"Unix epoch seconds at which the mediator last accepted a message addressed to this account. Present only when the request set `includeActivity` and the mediator has recorded such a message; MAY lag the true time by up to 60 seconds."},"lastAuthenticatedAt":{"type":"integer","minimum":0,"description":"Unix epoch seconds at which this account last completed authentication with the mediator, over any transport. Present only when the request set `includeActivity` and the mediator has recorded an authentication."},"stats":{"\$ref":"#/\$defs/AccountStats","description":"Lifetime counters the mediator keeps for this account. Present only when the request set `includeStats`."}}},"AccountStats":{"title":"AccountStats","type":"object","additionalProperties":false,"description":"What an account has sent and received over its lifetime, as the mediator counted it. Every member is optional: a mediator reports what it keeps, and a counter absent from a response was not kept rather than zero. Counters survive restarts and are not reset by reading them; removing an account discards them.","properties":{"messagesReceived":{"type":"integer","minimum":0,"description":"Messages the mediator accepted addressed to this account."},"messagesSent":{"type":"integer","minimum":0,"description":"Messages the mediator accepted from this account."},"bytesReceived":{"type":"integer","minimum":0,"description":"Total size of the messages counted by messagesReceived."},"bytesSent":{"type":"integer","minimum":0,"description":"Total size of the messages counted by messagesSent."},"receivedByProtocol":{"\$ref":"#/\$defs/ProtocolCounts","description":"messagesReceived split by the wire protocol the message arrived in."},"sentByProtocol":{"\$ref":"#/\$defs/ProtocolCounts","description":"messagesSent split by the wire protocol the message was sent in."}}},"ProtocolCounts":{"title":"ProtocolCounts","type":"object","additionalProperties":false,"description":"Message counts split by wire protocol. An absent member is a protocol the mediator counted nothing for. The member names match the WireProtocol values the traffic monitor reports.","properties":{"didcomm":{"type":"integer","minimum":0,"description":"DIDComm v2 (JWE/JWS)."},"didcommV1":{"type":"integer","minimum":0,"description":"A DIDComm v1 envelope."},"tsp":{"type":"integer","minimum":0,"description":"A Trust Spanning Protocol message."},"other":{"type":"integer","minimum":0,"description":"Anything the mediator could not classify."}}},"QueueLimits":{"title":"QueueLimits","type":"object","additionalProperties":false,"description":"Per-account queued-message limits. A value of -1 means unlimited; a member omitted on a change request leaves that limit unchanged.","properties":{"sendQueueLimit":{"type":"integer","minimum":-1,"description":"Maximum queued send messages; -1 = unlimited."},"receiveQueueLimit":{"type":"integer","minimum":-1,"description":"Maximum queued receive messages; -1 = unlimited."}}},"MediatorAcl":{"title":"MediatorAcl","type":"object","additionalProperties":false,"description":"The mediator\'s per-account access-control capability set, expressed as named booleans (the transport-agnostic form of the mediator\'s internal capability flags). On a set request, members omitted are left unchanged; a get/response carries the full realized set.","properties":{"accessListMode":{"type":"string","enum":["explicitAllow","explicitDeny"],"description":"How the account\'s access list is interpreted. `explicitAllow` = an allowlist (empty denies everyone); `explicitDeny` = a denylist (empty allows everyone)."},"blocked":{"type":"boolean","description":"The account is blocked from authenticating and transacting."},"local":{"type":"boolean","description":"Messages for this account may be stored locally at this mediator for pickup."},"sendMessages":{"type":"boolean","description":"May send direct messages through the mediator."},"receiveMessages":{"type":"boolean","description":"May receive direct messages."},"sendForwarded":{"type":"boolean","description":"May send routing/forward (relay) messages."},"receiveForwarded":{"type":"boolean","description":"May be the next hop of a forwarded message."},"createInvites":{"type":"boolean","description":"May create out-of-band invitations."},"anonReceive":{"type":"boolean","description":"Accepts anonymous (no authenticated sender) messages."},"selfManageList":{"type":"boolean","description":"May self-manage its own access list."},"selfManageSendQueueLimit":{"type":"boolean","description":"May self-manage its own send-queue limit."},"selfManageReceiveQueueLimit":{"type":"boolean","description":"May self-manage its own receive-queue limit."},"didcommEnabled":{"type":"boolean","description":"The account accepts DIDComm-protocol delivery. Default true; set false for a TSP-only node."},"tspEnabled":{"type":"boolean","description":"The account accepts TSP-protocol delivery. Default true; set false for a DIDComm-only node."}}},"AccountType":{"title":"AccountType","type":"string","enum":["standard","admin","rootAdmin","mediator"],"description":"The account\'s role at the mediator. `standard` is an ordinary served account; `admin`/`rootAdmin` may administer other accounts; `mediator` is the mediator\'s own account. Only a rootAdmin may assign or modify the rootAdmin role."},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
