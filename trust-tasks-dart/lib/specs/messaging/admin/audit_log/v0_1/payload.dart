// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/admin/audit-log/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the
/// account's controlling DID, carried verbatim and compared by exact string equality.
/// For privacy — and because some mediators key accounts by a one-way hash and never
/// hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an
/// equally valid value here: producer and consumer simply agree on the same opaque
/// identifier and compare by exact string equality. The field carries whichever form
/// the issuing mediator uses.
typedef Vid = String;

/// The kind of privileged change recorded in the audit log.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const AuditAction(String value) {
  static const AuditAction setAcl = AuditAction('setAcl');
  static const AuditAction accessListAdd = AuditAction('accessListAdd');
  static const AuditAction accessListRemove = AuditAction('accessListRemove');
  static const AuditAction accessListClear = AuditAction('accessListClear');
  static const AuditAction accountAdd = AuditAction('accountAdd');
  static const AuditAction accountRemove = AuditAction('accountRemove');
  static const AuditAction accountChangeType = AuditAction('accountChangeType');
  static const AuditAction accountChangeQueueLimits =
      AuditAction('accountChangeQueueLimits');
  static const AuditAction adminAdd = AuditAction('adminAdd');
  static const AuditAction adminStrip = AuditAction('adminStrip');

  /// Every value this specification's schema permits.
  static const List<AuditAction> values = <AuditAction>[
    setAcl,
    accessListAdd,
    accessListRemove,
    accessListClear,
    accountAdd,
    accountRemove,
    accountChangeType,
    accountChangeQueueLimits,
    adminAdd,
    adminStrip
  ];
}

/// One record in the mediator's privileged-change audit log: one change, by one actor,
/// at one time.
class AuditEntry {
  const AuditEntry({
    required this.timestamp,
    required this.actor,
    required this.target,
    required this.action,
    this.detail,
  });

  /// Read this payload from a decoded JSON object.
  factory AuditEntry.fromJson(Map<String, dynamic> json) => AuditEntry(
        timestamp: json['timestamp'] as int,
        actor: json['actor'] as String,
        target: json['target'] as String,
        action: AuditAction(json['action'] as String),
        detail: json['detail'] as String?,
      );

  /// Unix epoch seconds at which the change was recorded.
  final int timestamp;

  /// The authenticated account that made the change (an admin, or the owner for a
  /// self-service change).
  final Vid actor;

  /// The account whose record changed.
  final Vid target;
  final AuditAction action;

  /// Short human-readable summary of the change; not machine-parsed.
  final String? detail;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'timestamp': timestamp,
        'actor': actor,
        'target': target,
        'action': action.value,
        if (detail != null) 'detail': detail!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a messaging/admin/audit-log request (newest-first). Carried
/// in a Trust Task document whose type is
/// https://trusttasks.org/spec/messaging/admin/audit-log/0.1#response.
class Response {
  const Response({
    required this.entries,
    this.nextCursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        entries: (json['entries'] as List<dynamic>)
            .map((e) => AuditEntry.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The page of audit-log records, newest first.
  final List<AuditEntry> entries;

  /// Opaque continuation token. Present only when further entries remain beyond this
  /// page; omitted on the final page.
  final String? nextCursor;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entries': entries.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging Audit Log — payload
class Payload {
  const Payload({
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque continuation token from a prior page's nextCursor. Echoed verbatim; treated
  /// as unstructured by the requester.
  final String? cursor;

  /// Maximum number of audit entries to return in this page. The mediator chooses a
  /// default when omitted.
  final int? limit;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/messaging/admin/audit-log/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/admin/audit-log/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/admin/audit-log/0.1","title":"Messaging Audit Log — payload","type":"object","additionalProperties":false,"properties":{"cursor":{"type":"string","minLength":1,"description":"Opaque continuation token from a prior page\'s nextCursor. Echoed verbatim; treated as unstructured by the requester."},"limit":{"type":"integer","minimum":1,"maximum":1000,"description":"Maximum number of audit entries to return in this page. The mediator chooses a default when omitted."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging Audit Log — response payload","description":"The success response to a messaging/admin/audit-log request (newest-first). Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/admin/audit-log/0.1#response.","type":"object","additionalProperties":false,"required":["entries"],"properties":{"entries":{"type":"array","description":"The page of audit-log records, newest first.","items":{"\$ref":"#/\$defs/AuditEntry"}},"nextCursor":{"type":"string","minLength":1,"description":"Opaque continuation token. Present only when further entries remain beyond this page; omitted on the final page."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"AuditEntry":{"title":"AuditEntry","type":"object","additionalProperties":false,"required":["timestamp","actor","target","action"],"description":"One record in the mediator\'s privileged-change audit log: one change, by one actor, at one time.","properties":{"timestamp":{"type":"integer","minimum":0,"description":"Unix epoch seconds at which the change was recorded."},"actor":{"\$ref":"#/\$defs/Vid","description":"The authenticated account that made the change (an admin, or the owner for a self-service change)."},"target":{"\$ref":"#/\$defs/Vid","description":"The account whose record changed."},"action":{"\$ref":"#/\$defs/AuditAction"},"detail":{"type":"string","description":"Short human-readable summary of the change; not machine-parsed."}}},"AuditAction":{"title":"AuditAction","type":"string","description":"The kind of privileged change recorded in the audit log.","enum":["setAcl","accessListAdd","accessListRemove","accessListClear","accountAdd","accountRemove","accountChangeType","accountChangeQueueLimits","adminAdd","adminStrip"]},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging Audit Log — response payload","description":"The success response to a messaging/admin/audit-log request (newest-first). Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/admin/audit-log/0.1#response.","type":"object","additionalProperties":false,"required":["entries"],"properties":{"entries":{"type":"array","description":"The page of audit-log records, newest first.","items":{"\$ref":"#/\$defs/AuditEntry"}},"nextCursor":{"type":"string","minLength":1,"description":"Opaque continuation token. Present only when further entries remain beyond this page; omitted on the final page."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"AuditEntry":{"title":"AuditEntry","type":"object","additionalProperties":false,"required":["timestamp","actor","target","action"],"description":"One record in the mediator\'s privileged-change audit log: one change, by one actor, at one time.","properties":{"timestamp":{"type":"integer","minimum":0,"description":"Unix epoch seconds at which the change was recorded."},"actor":{"\$ref":"#/\$defs/Vid","description":"The authenticated account that made the change (an admin, or the owner for a self-service change)."},"target":{"\$ref":"#/\$defs/Vid","description":"The account whose record changed."},"action":{"\$ref":"#/\$defs/AuditAction"},"detail":{"type":"string","description":"Short human-readable summary of the change; not machine-parsed."}}},"AuditAction":{"title":"AuditAction","type":"string","description":"The kind of privileged change recorded in the audit log.","enum":["setAcl","accessListAdd","accessListRemove","accessListClear","accountAdd","accountRemove","accountChangeType","accountChangeQueueLimits","adminAdd","adminStrip"]},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
