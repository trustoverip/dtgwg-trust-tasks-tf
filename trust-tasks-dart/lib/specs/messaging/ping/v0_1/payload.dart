// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/ping/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Coarse health. `ok` = able to accept and route messages; `degraded` = reachable but
/// operating with reduced function. A mediator that cannot serve at all returns the
/// framework `unavailable` error instead of a response.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus ok = ResponseStatus('ok');
  static const ResponseStatus degraded = ResponseStatus('degraded');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[ok, degraded];
}

/// The success response to a messaging/ping request. Carried in a Trust Task document
/// whose type is https://trusttasks.org/spec/messaging/ping/0.1#response.
class Response {
  const Response({
    required this.serverTime,
    required this.status,
    this.protocols,
    this.nonce,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        serverTime: json['serverTime'] as String,
        status: ResponseStatus(json['status'] as String),
        protocols: json['protocols'] == null
            ? null
            : (json['protocols'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        nonce: json['nonce'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The mediator's current time (RFC 3339). Advisory; not a trusted time source.
  final String serverTime;

  /// Coarse health. `ok` = able to accept and route messages; `degraded` = reachable but
  /// operating with reduced function. A mediator that cannot serve at all returns the
  /// framework `unavailable` error instead of a response.
  final ResponseStatus status;

  /// The transport protocols the mediator currently serves (e.g. `didcomm`, `tsp`). Open
  /// set; requesters ignore unrecognized tokens. MAY be omitted in a reduced response to
  /// an unauthenticated requester.
  final List<String>? protocols;

  /// Echoed verbatim from the request when one was supplied.
  final String? nonce;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'serverTime': serverTime,
        'status': status.value,
        if (protocols != null) 'protocols': protocols!,
        if (nonce != null) 'nonce': nonce!,
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging Ping — payload
class Payload {
  const Payload({
    this.nonce,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        nonce: json['nonce'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Optional opaque value the requester may supply to correlate the response to this
  /// probe. The mediator echoes it verbatim in the response.
  final String? nonce;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (nonce != null) 'nonce': nonce!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/messaging/ping/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/ping/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/ping/0.1","title":"Messaging Ping — payload","type":"object","additionalProperties":false,"properties":{"nonce":{"type":"string","description":"Optional opaque value the requester may supply to correlate the response to this probe. The mediator echoes it verbatim in the response."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging Ping — response payload","description":"The success response to a messaging/ping request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/ping/0.1#response.","type":"object","additionalProperties":false,"required":["serverTime","status"],"properties":{"serverTime":{"type":"string","format":"date-time","description":"The mediator\'s current time (RFC 3339). Advisory; not a trusted time source."},"status":{"type":"string","enum":["ok","degraded"],"description":"Coarse health. `ok` = able to accept and route messages; `degraded` = reachable but operating with reduced function. A mediator that cannot serve at all returns the framework `unavailable` error instead of a response."},"protocols":{"type":"array","items":{"type":"string"},"description":"The transport protocols the mediator currently serves (e.g. `didcomm`, `tsp`). Open set; requesters ignore unrecognized tokens. MAY be omitted in a reduced response to an unauthenticated requester."},"nonce":{"type":"string","description":"Echoed verbatim from the request when one was supplied."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging Ping — response payload","description":"The success response to a messaging/ping request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/ping/0.1#response.","type":"object","additionalProperties":false,"required":["serverTime","status"],"properties":{"serverTime":{"type":"string","format":"date-time","description":"The mediator\'s current time (RFC 3339). Advisory; not a trusted time source."},"status":{"type":"string","enum":["ok","degraded"],"description":"Coarse health. `ok` = able to accept and route messages; `degraded` = reachable but operating with reduced function. A mediator that cannot serve at all returns the framework `unavailable` error instead of a response."},"protocols":{"type":"array","items":{"type":"string"},"description":"The transport protocols the mediator currently serves (e.g. `didcomm`, `tsp`). Open set; requesters ignore unrecognized tokens. MAY be omitted in a reduced response to an unauthenticated requester."},"nonce":{"type":"string","description":"Echoed verbatim from the request when one was supplied."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
