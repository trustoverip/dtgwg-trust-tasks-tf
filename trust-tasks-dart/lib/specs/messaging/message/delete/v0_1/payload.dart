// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/message/delete/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Why it was not deleted. `notFound`: no such message. `notInAccount`: the message
/// exists but is not in the named account's queues.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseResultsItemReason(String value) {
  static const ResponseResultsItemReason notFound =
      ResponseResultsItemReason('notFound');
  static const ResponseResultsItemReason notInAccount =
      ResponseResultsItemReason('notInAccount');

  /// Every value this specification's schema permits.
  static const List<ResponseResultsItemReason> values =
      <ResponseResultsItemReason>[notFound, notInAccount];
}

/// ResponseResultsItem, generated from its schema.
class ResponseResultsItem {
  const ResponseResultsItem({
    required this.msgId,
    required this.deleted,
    this.reason,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseResultsItem.fromJson(Map<String, dynamic> json) =>
      ResponseResultsItem(
        msgId: json['msgId'] as String,
        deleted: json['deleted'] as bool,
        reason: json['reason'] == null
            ? null
            : ResponseResultsItemReason(json['reason'] as String),
      );

  final String msgId;
  final bool deleted;

  /// Why it was not deleted. `notFound`: no such message. `notInAccount`: the message
  /// exists but is not in the named account's queues.
  final ResponseResultsItemReason? reason;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'msgId': msgId,
        'deleted': deleted,
        if (reason != null) 'reason': reason!.value,
      };
}

/// The success response to a messaging/message/delete request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/messaging/message/delete/0.1#response.
class Response {
  const Response({
    required this.results,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        results: (json['results'] as List<dynamic>)
            .map((e) => ResponseResultsItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// One entry per requested msgId, in request order.
  final List<ResponseResultsItem> results;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'results': results.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the
/// account's controlling DID, carried verbatim and compared by exact string equality.
/// For privacy — and because some mediators key accounts by a one-way hash and never
/// hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an
/// equally valid value here: producer and consumer simply agree on the same opaque
/// identifier and compare by exact string equality. The field carries whichever form
/// the issuing mediator uses.
typedef Vid = String;

/// Messaging — Delete Messages — payload
class Payload {
  const Payload({
    this.did,
    required this.msgIds,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String?,
        msgIds:
            (json['msgIds'] as List<dynamic>).map((e) => e as String).toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The account whose queue holds the messages. Omitted = the requester's own account.
  /// A requester without administrative standing may name only itself.
  final Vid? did;
  final List<String> msgIds;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (did != null) 'did': did!,
        'msgIds': msgIds,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/messaging/message/delete/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/message/delete/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/message/delete/0.1","title":"Messaging — Delete Messages — payload","type":"object","additionalProperties":false,"required":["msgIds"],"properties":{"did":{"\$ref":"#/\$defs/Vid","description":"The account whose queue holds the messages. Omitted = the requester\'s own account. A requester without administrative standing may name only itself."},"msgIds":{"type":"array","minItems":1,"maxItems":100,"uniqueItems":true,"items":{"type":"string","minLength":1}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Delete Messages — response payload","description":"The success response to a messaging/message/delete request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/message/delete/0.1#response.","type":"object","additionalProperties":false,"properties":{"results":{"type":"array","description":"One entry per requested msgId, in request order.","items":{"type":"object","additionalProperties":false,"required":["msgId","deleted"],"properties":{"msgId":{"type":"string","minLength":1},"deleted":{"type":"boolean"},"reason":{"type":"string","enum":["notFound","notInAccount"],"description":"Why it was not deleted. `notFound`: no such message. `notInAccount`: the message exists but is not in the named account\'s queues."}}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["results"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Delete Messages — response payload","description":"The success response to a messaging/message/delete request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/message/delete/0.1#response.","type":"object","additionalProperties":false,"properties":{"results":{"type":"array","description":"One entry per requested msgId, in request order.","items":{"type":"object","additionalProperties":false,"required":["msgId","deleted"],"properties":{"msgId":{"type":"string","minLength":1},"deleted":{"type":"boolean"},"reason":{"type":"string","enum":["notFound","notInAccount"],"description":"Why it was not deleted. `notFound`: no such message. `notInAccount`: the message exists but is not in the named account\'s queues."}}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["results"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
