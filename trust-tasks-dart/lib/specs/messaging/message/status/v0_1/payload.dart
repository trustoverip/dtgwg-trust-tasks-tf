// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/message/status/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Where a sent message stands. `queued`: held for the recipient and not yet handed
/// over. `delivered`: handed to the recipient (pickup or live stream) and not yet
/// removed by it. `collected`: removed by the recipient — acknowledged after pickup,
/// fetched with delete, or deleted or purged by it. `withdrawn`: removed by the
/// sender. `discarded`: removed by the mediator before the recipient took it —
/// expired, or its account was removed. `unknown`: the mediator holds neither the
/// message nor a receipt for it under the requester.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const MessageState(String value) {
  static const MessageState queued = MessageState('queued');
  static const MessageState delivered = MessageState('delivered');
  static const MessageState collected = MessageState('collected');
  static const MessageState withdrawn = MessageState('withdrawn');
  static const MessageState discarded = MessageState('discarded');
  static const MessageState unknown = MessageState('unknown');

  /// Every value this specification's schema permits.
  static const List<MessageState> values = <MessageState>[
    queued,
    delivered,
    collected,
    withdrawn,
    discarded,
    unknown
  ];
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseStatusesItem, generated from its schema.
class ResponseStatusesItem {
  const ResponseStatusesItem({
    required this.msgId,
    required this.state,
    this.at,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseStatusesItem.fromJson(Map<String, dynamic> json) =>
      ResponseStatusesItem(
        msgId: json['msgId'] as String,
        state: MessageState(json['state'] as String),
        at: json['at'] as String?,
      );

  final String msgId;
  final MessageState state;

  /// When the message reached this state: first handed over for `delivered`, removed for
  /// `collected`, `withdrawn` and `discarded`. Absent for `queued` and `unknown`, and
  /// wherever the mediator does not disclose it.
  final String? at;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'msgId': msgId,
        'state': state.value,
        if (at != null) 'at': at!,
      };
}

/// The success response to a messaging/message/status request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/messaging/message/status/0.1#response.
class Response {
  const Response({
    required this.statuses,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        statuses: (json['statuses'] as List<dynamic>)
            .map(
                (e) => ResponseStatusesItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// One entry per requested msgId, in request order.
  final List<ResponseStatusesItem> statuses;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'statuses': statuses.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging — Message Status — payload
class Payload {
  const Payload({
    required this.msgIds,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        msgIds:
            (json['msgIds'] as List<dynamic>).map((e) => e as String).toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The messages the requester sent and wants the status of, by the mediator's message
  /// identifier.
  final List<String> msgIds;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'msgIds': msgIds,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/messaging/message/status/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/message/status/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/message/status/0.1","title":"Messaging — Message Status — payload","type":"object","additionalProperties":false,"required":["msgIds"],"properties":{"msgIds":{"type":"array","description":"The messages the requester sent and wants the status of, by the mediator\'s message identifier.","minItems":1,"maxItems":100,"uniqueItems":true,"items":{"type":"string","minLength":1,"maxLength":256}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"MessageState":{"title":"MessageState","type":"string","enum":["queued","delivered","collected","withdrawn","discarded","unknown"],"description":"Where a sent message stands. `queued`: held for the recipient and not yet handed over. `delivered`: handed to the recipient (pickup or live stream) and not yet removed by it. `collected`: removed by the recipient — acknowledged after pickup, fetched with delete, or deleted or purged by it. `withdrawn`: removed by the sender. `discarded`: removed by the mediator before the recipient took it — expired, or its account was removed. `unknown`: the mediator holds neither the message nor a receipt for it under the requester."},"Response":{"\$anchor":"response","title":"Messaging — Message Status — response payload","description":"The success response to a messaging/message/status request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/message/status/0.1#response.","type":"object","additionalProperties":false,"properties":{"statuses":{"type":"array","description":"One entry per requested msgId, in request order.","items":{"type":"object","additionalProperties":false,"required":["msgId","state"],"properties":{"msgId":{"type":"string","minLength":1,"maxLength":256},"state":{"\$ref":"#/\$defs/MessageState"},"at":{"type":"string","format":"date-time","description":"When the message reached this state: first handed over for `delivered`, removed for `collected`, `withdrawn` and `discarded`. Absent for `queued` and `unknown`, and wherever the mediator does not disclose it."}}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["statuses"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"MessageState":{"title":"MessageState","type":"string","enum":["queued","delivered","collected","withdrawn","discarded","unknown"],"description":"Where a sent message stands. `queued`: held for the recipient and not yet handed over. `delivered`: handed to the recipient (pickup or live stream) and not yet removed by it. `collected`: removed by the recipient — acknowledged after pickup, fetched with delete, or deleted or purged by it. `withdrawn`: removed by the sender. `discarded`: removed by the mediator before the recipient took it — expired, or its account was removed. `unknown`: the mediator holds neither the message nor a receipt for it under the requester."},"Response":{"\$anchor":"response","title":"Messaging — Message Status — response payload","description":"The success response to a messaging/message/status request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/message/status/0.1#response.","type":"object","additionalProperties":false,"properties":{"statuses":{"type":"array","description":"One entry per requested msgId, in request order.","items":{"type":"object","additionalProperties":false,"required":["msgId","state"],"properties":{"msgId":{"type":"string","minLength":1,"maxLength":256},"state":{"\$ref":"#/\$defs/MessageState"},"at":{"type":"string","format":"date-time","description":"When the message reached this state: first handed over for `delivered`, removed for `collected`, `withdrawn` and `discarded`. Absent for `queued` and `unknown`, and wherever the mediator does not disclose it."}}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["statuses"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
