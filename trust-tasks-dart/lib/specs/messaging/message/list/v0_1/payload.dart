// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/message/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Which of an account's two queues. `receive` holds messages addressed to the account
/// awaiting its pickup. `send` holds messages the account sent that are still held
/// against it until their recipient deletes them — so a stalled recipient fills its
/// senders' send queues.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Queue(String value) {
  static const Queue receive = Queue('receive');
  static const Queue send = Queue('send');

  /// Every value this specification's schema permits.
  static const List<Queue> values = <Queue>[receive, send];
}

/// A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the
/// account's controlling DID, carried verbatim and compared by exact string equality.
/// For privacy — and because some mediators key accounts by a one-way hash and never
/// hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an
/// equally valid value here: producer and consumer simply agree on the same opaque
/// identifier and compare by exact string equality. The field carries whichever form
/// the issuing mediator uses.
typedef Vid = String;

/// The protocol a message travelled in, as the mediator detected it from the wire
/// form. `didcomm` is DIDComm v2 (JWE/JWS); `didcommV1` is a DIDComm v1 envelope;
/// `tsp` is a Trust Spanning Protocol message; `other` is anything the mediator could
/// not classify.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const WireProtocol(String value) {
  static const WireProtocol didcomm = WireProtocol('didcomm');
  static const WireProtocol didcommV1 = WireProtocol('didcommV1');
  static const WireProtocol tsp = WireProtocol('tsp');
  static const WireProtocol other = WireProtocol('other');

  /// Every value this specification's schema permits.
  static const List<WireProtocol> values = <WireProtocol>[
    didcomm,
    didcommV1,
    tsp,
    other
  ];
}

/// `queued`: stored and not yet handed to the recipient. `delivered`: handed to the
/// recipient (pickup or live stream) but not yet deleted by it — the recipient has not
/// acknowledged it.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DeliveryState(String value) {
  static const DeliveryState queued = DeliveryState('queued');
  static const DeliveryState delivered = DeliveryState('delivered');

  /// Every value this specification's schema permits.
  static const List<DeliveryState> values = <DeliveryState>[queued, delivered];
}

/// Metadata of one stored message. Never carries the message body.
class MessageMeta {
  const MessageMeta({
    required this.msgId,
    required this.queue,
    required this.size,
    required this.receivedAt,
    this.expiresAt,
    this.from,
    this.to,
    this.protocol,
    this.deliveryState,
    this.deliveredAt,
  });

  /// Read this payload from a decoded JSON object.
  factory MessageMeta.fromJson(Map<String, dynamic> json) => MessageMeta(
        msgId: json['msgId'] as String,
        queue: Queue(json['queue'] as String),
        size: json['size'] as int,
        receivedAt: json['receivedAt'] as String,
        expiresAt: json['expiresAt'] as String?,
        from: json['from'] as String?,
        to: json['to'] as String?,
        protocol: json['protocol'] == null
            ? null
            : WireProtocol(json['protocol'] as String),
        deliveryState: json['deliveryState'] == null
            ? null
            : DeliveryState(json['deliveryState'] as String),
        deliveredAt: json['deliveredAt'] as String?,
      );

  /// The mediator's identifier for the stored message; the handle messaging/message/get
  /// and messaging/message/delete accept.
  final String msgId;
  final Queue queue;

  /// Stored size in bytes.
  final int size;

  /// When the mediator stored the message.
  final String receivedAt;

  /// When the mediator will expire the message if it is not deleted first.
  final String? expiresAt;

  /// The sender, where the mediator knows it. Absent for anonymous messages.
  final Vid? from;

  /// The recipient account.
  final Vid? to;
  final WireProtocol? protocol;
  final DeliveryState? deliveryState;

  /// When the message was first handed to the recipient. Present only when deliveryState
  /// is `delivered`.
  final String? deliveredAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'msgId': msgId,
        'queue': queue.value,
        'size': size,
        'receivedAt': receivedAt,
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (from != null) 'from': from!,
        if (to != null) 'to': to!,
        if (protocol != null) 'protocol': protocol!.value,
        if (deliveryState != null) 'deliveryState': deliveryState!.value,
        if (deliveredAt != null) 'deliveredAt': deliveredAt!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a messaging/message/list request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/messaging/message/list/0.1#response.
class Response {
  const Response({
    required this.messages,
    this.nextCursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        messages: (json['messages'] as List<dynamic>)
            .map((e) => MessageMeta.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The page, oldest first.
  final List<MessageMeta> messages;

  /// Opaque continuation token. Present only when further items remain beyond this page;
  /// omitted on the final page.
  final String? nextCursor;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'messages': messages.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging — List Messages — payload
class Payload {
  const Payload({
    this.did,
    required this.queue,
    this.peer,
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String?,
        queue: Queue(json['queue'] as String),
        peer: json['peer'] as String?,
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The account whose queue to list. Omitted = the requester's own account. A requester
  /// without administrative standing may name only itself.
  final Vid? did;
  final Queue queue;

  /// Only messages exchanged with this counterparty.
  final Vid? peer;

  /// Opaque continuation token from a prior page's nextCursor. Echoed verbatim; treated
  /// as unstructured by the requester.
  final String? cursor;

  /// Maximum messages per page. The mediator chooses a default when omitted.
  final int? limit;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (did != null) 'did': did!,
        'queue': queue.value,
        if (peer != null) 'peer': peer!,
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/messaging/message/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/message/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/message/list/0.1","title":"Messaging — List Messages — payload","type":"object","additionalProperties":false,"required":["queue"],"properties":{"did":{"\$ref":"#/\$defs/Vid","description":"The account whose queue to list. Omitted = the requester\'s own account. A requester without administrative standing may name only itself."},"queue":{"\$ref":"#/\$defs/Queue"},"peer":{"\$ref":"#/\$defs/Vid","description":"Only messages exchanged with this counterparty."},"cursor":{"type":"string","minLength":1,"description":"Opaque continuation token from a prior page\'s nextCursor. Echoed verbatim; treated as unstructured by the requester."},"limit":{"type":"integer","minimum":1,"maximum":500,"description":"Maximum messages per page. The mediator chooses a default when omitted."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging — List Messages — response payload","description":"The success response to a messaging/message/list request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/message/list/0.1#response.","type":"object","additionalProperties":false,"properties":{"messages":{"type":"array","items":{"\$ref":"#/\$defs/MessageMeta"},"description":"The page, oldest first."},"nextCursor":{"type":"string","minLength":1,"description":"Opaque continuation token. Present only when further items remain beyond this page; omitted on the final page."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["messages"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"MessageMeta":{"title":"MessageMeta","type":"object","additionalProperties":false,"required":["msgId","queue","size","receivedAt"],"description":"Metadata of one stored message. Never carries the message body.","properties":{"msgId":{"type":"string","minLength":1,"description":"The mediator\'s identifier for the stored message; the handle messaging/message/get and messaging/message/delete accept.","maxLength":256},"queue":{"\$ref":"#/\$defs/Queue"},"size":{"type":"integer","minimum":0,"description":"Stored size in bytes."},"receivedAt":{"type":"string","format":"date-time","description":"When the mediator stored the message."},"expiresAt":{"type":"string","format":"date-time","description":"When the mediator will expire the message if it is not deleted first."},"from":{"\$ref":"#/\$defs/Vid","description":"The sender, where the mediator knows it. Absent for anonymous messages."},"to":{"\$ref":"#/\$defs/Vid","description":"The recipient account."},"protocol":{"\$ref":"#/\$defs/WireProtocol"},"deliveryState":{"\$ref":"#/\$defs/DeliveryState"},"deliveredAt":{"type":"string","format":"date-time","description":"When the message was first handed to the recipient. Present only when deliveryState is `delivered`."}}},"DeliveryState":{"title":"DeliveryState","type":"string","enum":["queued","delivered"],"description":"`queued`: stored and not yet handed to the recipient. `delivered`: handed to the recipient (pickup or live stream) but not yet deleted by it — the recipient has not acknowledged it."},"WireProtocol":{"title":"WireProtocol","type":"string","enum":["didcomm","didcommV1","tsp","other"],"description":"The protocol a message travelled in, as the mediator detected it from the wire form. `didcomm` is DIDComm v2 (JWE/JWS); `didcommV1` is a DIDComm v1 envelope; `tsp` is a Trust Spanning Protocol message; `other` is anything the mediator could not classify."},"Queue":{"title":"Queue","type":"string","enum":["receive","send"],"description":"Which of an account\'s two queues. `receive` holds messages addressed to the account awaiting its pickup. `send` holds messages the account sent that are still held against it until their recipient deletes them — so a stalled recipient fills its senders\' send queues."},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging — List Messages — response payload","description":"The success response to a messaging/message/list request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/message/list/0.1#response.","type":"object","additionalProperties":false,"properties":{"messages":{"type":"array","items":{"\$ref":"#/\$defs/MessageMeta"},"description":"The page, oldest first."},"nextCursor":{"type":"string","minLength":1,"description":"Opaque continuation token. Present only when further items remain beyond this page; omitted on the final page."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["messages"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"MessageMeta":{"title":"MessageMeta","type":"object","additionalProperties":false,"required":["msgId","queue","size","receivedAt"],"description":"Metadata of one stored message. Never carries the message body.","properties":{"msgId":{"type":"string","minLength":1,"description":"The mediator\'s identifier for the stored message; the handle messaging/message/get and messaging/message/delete accept.","maxLength":256},"queue":{"\$ref":"#/\$defs/Queue"},"size":{"type":"integer","minimum":0,"description":"Stored size in bytes."},"receivedAt":{"type":"string","format":"date-time","description":"When the mediator stored the message."},"expiresAt":{"type":"string","format":"date-time","description":"When the mediator will expire the message if it is not deleted first."},"from":{"\$ref":"#/\$defs/Vid","description":"The sender, where the mediator knows it. Absent for anonymous messages."},"to":{"\$ref":"#/\$defs/Vid","description":"The recipient account."},"protocol":{"\$ref":"#/\$defs/WireProtocol"},"deliveryState":{"\$ref":"#/\$defs/DeliveryState"},"deliveredAt":{"type":"string","format":"date-time","description":"When the message was first handed to the recipient. Present only when deliveryState is `delivered`."}}},"DeliveryState":{"title":"DeliveryState","type":"string","enum":["queued","delivered"],"description":"`queued`: stored and not yet handed to the recipient. `delivered`: handed to the recipient (pickup or live stream) but not yet deleted by it — the recipient has not acknowledged it."},"WireProtocol":{"title":"WireProtocol","type":"string","enum":["didcomm","didcommV1","tsp","other"],"description":"The protocol a message travelled in, as the mediator detected it from the wire form. `didcomm` is DIDComm v2 (JWE/JWS); `didcommV1` is a DIDComm v1 envelope; `tsp` is a Trust Spanning Protocol message; `other` is anything the mediator could not classify."},"Queue":{"title":"Queue","type":"string","enum":["receive","send"],"description":"Which of an account\'s two queues. `receive` holds messages addressed to the account awaiting its pickup. `send` holds messages the account sent that are still held against it until their recipient deletes them — so a stalled recipient fills its senders\' send queues."},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
