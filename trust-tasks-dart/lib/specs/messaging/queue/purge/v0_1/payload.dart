// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/queue/purge/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a messaging/queue/purge request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/messaging/queue/purge/0.1#response.
class Response {
  const Response({
    required this.matched,
    this.matchedBytes,
    required this.purged,
    required this.dryRun,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        matched: json['matched'] as int,
        matchedBytes: json['matchedBytes'] as int?,
        purged: json['purged'] as int,
        dryRun: json['dryRun'] as bool,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Messages that matched the selection.
  final int matched;
  final int? matchedBytes;

  /// Messages actually removed. 0 on a dry run.
  final int purged;
  final bool dryRun;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'matched': matched,
        if (matchedBytes != null) 'matchedBytes': matchedBytes!,
        'purged': purged,
        'dryRun': dryRun,
        if (ext != null) 'ext': ext!,
      };
}

/// A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the
/// account's controlling DID, carried verbatim and compared by exact string equality.
/// For privacy — and because some mediators key accounts by a one-way hash and never
/// hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an
/// equally valid value here: producer and consumer simply agree on the same opaque
/// identifier and compare by exact string equality. The field carries whichever form
/// the issuing mediator uses.
typedef Vid = String;

/// Which of an account's two queues. `receive` holds messages addressed to the account
/// awaiting its pickup. `send` holds messages the account sent that are still held
/// against it until their recipient deletes them — so a stalled recipient fills its
/// senders' send queues.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Queue(String value) {
  static const Queue receive = Queue('receive');
  static const Queue send = Queue('send');

  /// Every value this specification's schema permits.
  static const List<Queue> values = <Queue>[receive, send];
}

/// Messaging — Purge Queue — payload
class Payload {
  const Payload({
    this.did,
    required this.queue,
    this.peer,
    this.olderThanSeconds,
    this.dryRun,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String?,
        queue: Queue(json['queue'] as String),
        peer: json['peer'] as String?,
        olderThanSeconds: json['olderThanSeconds'] as int?,
        dryRun: json['dryRun'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The account whose queue to purge. Omitted = the requester's own account. A
  /// requester without administrative standing may name only itself.
  final Vid? did;
  final Queue queue;

  /// Only messages exchanged with this counterparty: in a send queue, the recipient; in
  /// a receive queue, the sender.
  final Vid? peer;

  /// Only messages queued at least this long.
  final int? olderThanSeconds;

  /// Report what would be purged and purge nothing. Default false.
  final bool? dryRun;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (did != null) 'did': did!,
        'queue': queue.value,
        if (peer != null) 'peer': peer!,
        if (olderThanSeconds != null) 'olderThanSeconds': olderThanSeconds!,
        if (dryRun != null) 'dryRun': dryRun!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/messaging/queue/purge/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/queue/purge/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/queue/purge/0.1","title":"Messaging — Purge Queue — payload","type":"object","additionalProperties":false,"required":["queue"],"properties":{"did":{"\$ref":"#/\$defs/Vid","description":"The account whose queue to purge. Omitted = the requester\'s own account. A requester without administrative standing may name only itself."},"queue":{"\$ref":"#/\$defs/Queue"},"peer":{"\$ref":"#/\$defs/Vid","description":"Only messages exchanged with this counterparty: in a send queue, the recipient; in a receive queue, the sender."},"olderThanSeconds":{"type":"integer","minimum":0,"description":"Only messages queued at least this long."},"dryRun":{"type":"boolean","description":"Report what would be purged and purge nothing. Default false."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Purge Queue — response payload","description":"The success response to a messaging/queue/purge request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/queue/purge/0.1#response.","type":"object","additionalProperties":false,"properties":{"matched":{"type":"integer","minimum":0,"description":"Messages that matched the selection."},"matchedBytes":{"type":"integer","minimum":0},"purged":{"type":"integer","minimum":0,"description":"Messages actually removed. 0 on a dry run."},"dryRun":{"type":"boolean"},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["matched","purged","dryRun"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."},"Queue":{"title":"Queue","type":"string","enum":["receive","send"],"description":"Which of an account\'s two queues. `receive` holds messages addressed to the account awaiting its pickup. `send` holds messages the account sent that are still held against it until their recipient deletes them — so a stalled recipient fills its senders\' send queues."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Purge Queue — response payload","description":"The success response to a messaging/queue/purge request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/queue/purge/0.1#response.","type":"object","additionalProperties":false,"properties":{"matched":{"type":"integer","minimum":0,"description":"Messages that matched the selection."},"matchedBytes":{"type":"integer","minimum":0},"purged":{"type":"integer","minimum":0,"description":"Messages actually removed. 0 on a dry run."},"dryRun":{"type":"boolean"},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["matched","purged","dryRun"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."},"Queue":{"title":"Queue","type":"string","enum":["receive","send"],"description":"Which of an account\'s two queues. `receive` holds messages addressed to the account awaiting its pickup. `send` holds messages the account sent that are still held against it until their recipient deletes them — so a stalled recipient fills its senders\' send queues."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
