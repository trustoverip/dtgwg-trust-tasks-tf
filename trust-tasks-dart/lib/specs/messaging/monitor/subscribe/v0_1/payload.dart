// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/messaging/monitor/subscribe/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the
/// account's controlling DID, carried verbatim and compared by exact string equality.
/// For privacy — and because some mediators key accounts by a one-way hash and never
/// hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an
/// equally valid value here: producer and consumer simply agree on the same opaque
/// identifier and compare by exact string equality. The field carries whichever form
/// the issuing mediator uses.
typedef Vid = String;

/// `inbound`: arriving at the mediator. `outbound`: leaving it (delivery to a
/// recipient, or a forward to another mediator). `internal`: a state change with no
/// wire traffic (expiry, deletion, purge).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const TrafficDirection(String value) {
  static const TrafficDirection inbound = TrafficDirection('inbound');
  static const TrafficDirection outbound = TrafficDirection('outbound');
  static const TrafficDirection internal = TrafficDirection('internal');

  /// Every value this specification's schema permits.
  static const List<TrafficDirection> values = <TrafficDirection>[
    inbound,
    outbound,
    internal
  ];
}

/// What happened. `received`: a frame arrived. `stored`: it was queued for a local
/// account. `delivered`: handed to its recipient. `forwarded`: relayed to another
/// mediator. `refused`: rejected (see the event's `outcome`). `deleted`: removed by
/// its recipient or an administrator. `expired`: removed by expiry. `purged`: removed
/// by a queue purge.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const TrafficStage(String value) {
  static const TrafficStage received = TrafficStage('received');
  static const TrafficStage stored = TrafficStage('stored');
  static const TrafficStage delivered = TrafficStage('delivered');
  static const TrafficStage forwarded = TrafficStage('forwarded');
  static const TrafficStage refused = TrafficStage('refused');
  static const TrafficStage deleted = TrafficStage('deleted');
  static const TrafficStage expired = TrafficStage('expired');
  static const TrafficStage purged = TrafficStage('purged');

  /// Every value this specification's schema permits.
  static const List<TrafficStage> values = <TrafficStage>[
    received,
    stored,
    delivered,
    forwarded,
    refused,
    deleted,
    expired,
    purged
  ];
}

/// The protocol a message travelled in, as the mediator detected it from the wire
/// form. `didcomm` is DIDComm v2 (JWE/JWS); `didcommV1` is a DIDComm v1 envelope;
/// `tsp` is a Trust Spanning Protocol message; `other` is anything the mediator could
/// not classify.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const WireProtocol(String value) {
  static const WireProtocol didcomm = WireProtocol('didcomm');
  static const WireProtocol didcommV1 = WireProtocol('didcommV1');
  static const WireProtocol tsp = WireProtocol('tsp');
  static const WireProtocol other = WireProtocol('other');

  /// Every value this specification's schema permits.
  static const List<WireProtocol> values = <WireProtocol>[
    didcomm,
    didcommV1,
    tsp,
    other
  ];
}

/// How the traffic reached or left the mediator. `peerMediator` is
/// mediator-to-mediator forwarding.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const TrafficChannel(String value) {
  static const TrafficChannel websocket = TrafficChannel('websocket');
  static const TrafficChannel rest = TrafficChannel('rest');
  static const TrafficChannel peerMediator = TrafficChannel('peerMediator');
  static const TrafficChannel internal = TrafficChannel('internal');

  /// Every value this specification's schema permits.
  static const List<TrafficChannel> values = <TrafficChannel>[
    websocket,
    rest,
    peerMediator,
    internal
  ];
}

/// Selects which events a monitor subscription receives. Every member present narrows
/// the selection (members are ANDed; values within one member are ORed). An empty
/// filter selects everything the requester is entitled to see.
class MonitorFilter {
  const MonitorFilter({
    this.dids,
    this.directions,
    this.stages,
    this.protocols,
    this.channels,
    this.messageTypePrefixes,
    this.failuresOnly,
  });

  /// Read this payload from a decoded JSON object.
  factory MonitorFilter.fromJson(Map<String, dynamic> json) => MonitorFilter(
        dids: json['dids'] == null
            ? null
            : (json['dids'] as List<dynamic>).map((e) => e as String).toList(),
        directions: json['directions'] == null
            ? null
            : (json['directions'] as List<dynamic>)
                .map((e) => TrafficDirection(e as String))
                .toList(),
        stages: json['stages'] == null
            ? null
            : (json['stages'] as List<dynamic>)
                .map((e) => TrafficStage(e as String))
                .toList(),
        protocols: json['protocols'] == null
            ? null
            : (json['protocols'] as List<dynamic>)
                .map((e) => WireProtocol(e as String))
                .toList(),
        channels: json['channels'] == null
            ? null
            : (json['channels'] as List<dynamic>)
                .map((e) => TrafficChannel(e as String))
                .toList(),
        messageTypePrefixes: json['messageTypePrefixes'] == null
            ? null
            : (json['messageTypePrefixes'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        failuresOnly: json['failuresOnly'] as bool?,
      );

  /// Only events whose `from` or `to` is one of these accounts.
  final List<Vid>? dids;
  final List<TrafficDirection>? directions;
  final List<TrafficStage>? stages;
  final List<WireProtocol>? protocols;
  final List<TrafficChannel>? channels;

  /// Only events whose `messageType` starts with one of these strings. Events with no
  /// `messageType` never match when this member is present.
  final List<String>? messageTypePrefixes;

  /// Only events that carry an `outcome`.
  final bool? failuresOnly;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (dids != null) 'dids': dids!,
        if (directions != null)
          'directions': directions!.map((e) => e.value).toList(),
        if (stages != null) 'stages': stages!.map((e) => e.value).toList(),
        if (protocols != null)
          'protocols': protocols!.map((e) => e.value).toList(),
        if (channels != null)
          'channels': channels!.map((e) => e.value).toList(),
        if (messageTypePrefixes != null)
          'messageTypePrefixes': messageTypePrefixes!,
        if (failuresOnly != null) 'failuresOnly': failuresOnly!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a messaging/monitor/subscribe request. Carried in a Trust
/// Task document whose type is
/// https://trusttasks.org/spec/messaging/monitor/subscribe/0.1#response.
class Response {
  const Response({
    required this.subscriptionId,
    required this.expiresAt,
    required this.filter,
    required this.maxEventsPerSecond,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        subscriptionId: json['subscriptionId'] as String,
        expiresAt: json['expiresAt'] as String,
        filter: MonitorFilter.fromJson(json['filter'] as Map<String, dynamic>),
        maxEventsPerSecond: json['maxEventsPerSecond'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String subscriptionId;
  final String expiresAt;

  /// The filter in force, after the mediator narrowed it to what the requester is
  /// entitled to see.
  final MonitorFilter filter;

  /// The rate ceiling in force.
  final int maxEventsPerSecond;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subscriptionId': subscriptionId,
        'expiresAt': expiresAt,
        'filter': filter.toJson(),
        'maxEventsPerSecond': maxEventsPerSecond,
        if (ext != null) 'ext': ext!,
      };
}

/// Messaging — Subscribe to Traffic Monitor — payload
class Payload {
  const Payload({
    this.filter,
    this.leaseSeconds,
    this.maxEventsPerSecond,
    this.subscriptionId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        filter: json['filter'] == null
            ? null
            : MonitorFilter.fromJson(json['filter'] as Map<String, dynamic>),
        leaseSeconds: json['leaseSeconds'] as int?,
        maxEventsPerSecond: json['maxEventsPerSecond'] as int?,
        subscriptionId: json['subscriptionId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final MonitorFilter? filter;

  /// How long the subscription lives unless renewed. Default 300.
  final int? leaseSeconds;

  /// Rate ceiling the subscriber can absorb; the mediator MAY apply a lower one. Events
  /// over the ceiling are counted as dropped, never queued.
  final int? maxEventsPerSecond;

  /// Renew or re-filter an existing subscription held by the same requester. Omitted =
  /// create a new one.
  final String? subscriptionId;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (filter != null) 'filter': filter!.toJson(),
        if (leaseSeconds != null) 'leaseSeconds': leaseSeconds!,
        if (maxEventsPerSecond != null)
          'maxEventsPerSecond': maxEventsPerSecond!,
        if (subscriptionId != null) 'subscriptionId': subscriptionId!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/messaging/monitor/subscribe/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/messaging/monitor/subscribe/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/messaging/monitor/subscribe/0.1","title":"Messaging — Subscribe to Traffic Monitor — payload","type":"object","additionalProperties":false,"properties":{"filter":{"\$ref":"#/\$defs/MonitorFilter"},"leaseSeconds":{"type":"integer","minimum":10,"maximum":3600,"description":"How long the subscription lives unless renewed. Default 300."},"maxEventsPerSecond":{"type":"integer","minimum":1,"maximum":1000,"description":"Rate ceiling the subscriber can absorb; the mediator MAY apply a lower one. Events over the ceiling are counted as dropped, never queued."},"subscriptionId":{"type":"string","minLength":1,"description":"Renew or re-filter an existing subscription held by the same requester. Omitted = create a new one."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Subscribe to Traffic Monitor — response payload","description":"The success response to a messaging/monitor/subscribe request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/monitor/subscribe/0.1#response.","type":"object","additionalProperties":false,"properties":{"subscriptionId":{"type":"string","minLength":1},"expiresAt":{"type":"string","format":"date-time"},"filter":{"\$ref":"#/\$defs/MonitorFilter","description":"The filter in force, after the mediator narrowed it to what the requester is entitled to see."},"maxEventsPerSecond":{"type":"integer","minimum":1,"description":"The rate ceiling in force."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["subscriptionId","expiresAt","filter","maxEventsPerSecond"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"MonitorFilter":{"title":"MonitorFilter","type":"object","additionalProperties":false,"description":"Selects which events a monitor subscription receives. Every member present narrows the selection (members are ANDed; values within one member are ORed). An empty filter selects everything the requester is entitled to see.","properties":{"dids":{"type":"array","minItems":1,"maxItems":100,"uniqueItems":true,"items":{"\$ref":"#/\$defs/Vid"},"description":"Only events whose `from` or `to` is one of these accounts."},"directions":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/TrafficDirection"}},"stages":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/TrafficStage"}},"protocols":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/WireProtocol"}},"channels":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/TrafficChannel"}},"messageTypePrefixes":{"type":"array","minItems":1,"maxItems":20,"uniqueItems":true,"items":{"type":"string","minLength":1,"maxLength":512},"description":"Only events whose `messageType` starts with one of these strings. Events with no `messageType` never match when this member is present."},"failuresOnly":{"type":"boolean","description":"Only events that carry an `outcome`."}}},"TrafficChannel":{"title":"TrafficChannel","type":"string","enum":["websocket","rest","peerMediator","internal"],"description":"How the traffic reached or left the mediator. `peerMediator` is mediator-to-mediator forwarding."},"WireProtocol":{"title":"WireProtocol","type":"string","enum":["didcomm","didcommV1","tsp","other"],"description":"The protocol a message travelled in, as the mediator detected it from the wire form. `didcomm` is DIDComm v2 (JWE/JWS); `didcommV1` is a DIDComm v1 envelope; `tsp` is a Trust Spanning Protocol message; `other` is anything the mediator could not classify."},"TrafficStage":{"title":"TrafficStage","type":"string","enum":["received","stored","delivered","forwarded","refused","deleted","expired","purged"],"description":"What happened. `received`: a frame arrived. `stored`: it was queued for a local account. `delivered`: handed to its recipient. `forwarded`: relayed to another mediator. `refused`: rejected (see the event\'s `outcome`). `deleted`: removed by its recipient or an administrator. `expired`: removed by expiry. `purged`: removed by a queue purge."},"TrafficDirection":{"title":"TrafficDirection","type":"string","enum":["inbound","outbound","internal"],"description":"`inbound`: arriving at the mediator. `outbound`: leaving it (delivery to a recipient, or a forward to another mediator). `internal`: a state change with no wire traffic (expiry, deletion, purge)."},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Messaging — Subscribe to Traffic Monitor — response payload","description":"The success response to a messaging/monitor/subscribe request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/messaging/monitor/subscribe/0.1#response.","type":"object","additionalProperties":false,"properties":{"subscriptionId":{"type":"string","minLength":1},"expiresAt":{"type":"string","format":"date-time"},"filter":{"\$ref":"#/\$defs/MonitorFilter","description":"The filter in force, after the mediator narrowed it to what the requester is entitled to see."},"maxEventsPerSecond":{"type":"integer","minimum":1,"description":"The rate ceiling in force."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"required":["subscriptionId","expiresAt","filter","maxEventsPerSecond"]},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"MonitorFilter":{"title":"MonitorFilter","type":"object","additionalProperties":false,"description":"Selects which events a monitor subscription receives. Every member present narrows the selection (members are ANDed; values within one member are ORed). An empty filter selects everything the requester is entitled to see.","properties":{"dids":{"type":"array","minItems":1,"maxItems":100,"uniqueItems":true,"items":{"\$ref":"#/\$defs/Vid"},"description":"Only events whose `from` or `to` is one of these accounts."},"directions":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/TrafficDirection"}},"stages":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/TrafficStage"}},"protocols":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/WireProtocol"}},"channels":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/TrafficChannel"}},"messageTypePrefixes":{"type":"array","minItems":1,"maxItems":20,"uniqueItems":true,"items":{"type":"string","minLength":1,"maxLength":512},"description":"Only events whose `messageType` starts with one of these strings. Events with no `messageType` never match when this member is present."},"failuresOnly":{"type":"boolean","description":"Only events that carry an `outcome`."}}},"TrafficChannel":{"title":"TrafficChannel","type":"string","enum":["websocket","rest","peerMediator","internal"],"description":"How the traffic reached or left the mediator. `peerMediator` is mediator-to-mediator forwarding."},"WireProtocol":{"title":"WireProtocol","type":"string","enum":["didcomm","didcommV1","tsp","other"],"description":"The protocol a message travelled in, as the mediator detected it from the wire form. `didcomm` is DIDComm v2 (JWE/JWS); `didcommV1` is a DIDComm v1 envelope; `tsp` is a Trust Spanning Protocol message; `other` is anything the mediator could not classify."},"TrafficStage":{"title":"TrafficStage","type":"string","enum":["received","stored","delivered","forwarded","refused","deleted","expired","purged"],"description":"What happened. `received`: a frame arrived. `stored`: it was queued for a local account. `delivered`: handed to its recipient. `forwarded`: relayed to another mediator. `refused`: rejected (see the event\'s `outcome`). `deleted`: removed by its recipient or an administrator. `expired`: removed by expiry. `purged`: removed by a queue purge."},"TrafficDirection":{"title":"TrafficDirection","type":"string","enum":["inbound","outbound","internal"],"description":"`inbound`: arriving at the mediator. `outbound`: leaving it (delivery to a recipient, or a forward to another mediator). `internal`: a state change with no wire traffic (expiry, deletion, purge)."},"Vid":{"title":"Vid","type":"string","minLength":1,"description":"A Verifiable Identifier (SPEC §4.8). For a mediator-served account this is the account\'s controlling DID, carried verbatim and compared by exact string equality. For privacy — and because some mediators key accounts by a one-way hash and never hold the full DID — a stable hash of the DID (e.g. its SHA-256 digest) is an equally valid value here: producer and consumer simply agree on the same opaque identifier and compare by exact string equality. The field carries whichever form the issuing mediator uses."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
