// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/device/_shared/0.1/device-binding.schema.json

/// Discriminator: is this consumer a user-driven Companion or a headless Service?
typedef ConsumerKind = Object?;

/// Producer-supplied attestation at registration time, verifiable by the maintainer
/// against the platform's attestation infrastructure. Tagged union over the
/// discriminator `kind`.
typedef DeviceAttestation = Object?;

/// `hardware`: the key is non-exportable in the secure keystore (iOS Secure Enclave /
/// Android StrongBox) and every signing / key-agreement operation runs in-chip —
/// achievable only with P-256. `software`: the key is held in app memory during use,
/// stored hardware-wrapped at rest. Maintainers MAY apply stricter policy (shorter
/// sessions, more frequent step-up) to `software`-tier devices.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const KeyCustodyTier(String value) {
  static const KeyCustodyTier hardware = KeyCustodyTier('hardware');
  static const KeyCustodyTier software = KeyCustodyTier('software');

  /// Every value this specification's schema permits.
  static const List<KeyCustodyTier> values = <KeyCustodyTier>[
    hardware,
    software
  ];
}

/// How a device custodies its private key material — maintainer policy input,
/// mirroring `attestation`. See docs/design-notes/mobile-key-custody-profile.md.
class KeyCustody {
  const KeyCustody({
    required this.tier,
    this.signingAlg,
    this.keyAgreementCurve,
  });

  /// Read this payload from a decoded JSON object.
  factory KeyCustody.fromJson(Map<String, dynamic> json) => KeyCustody(
        tier: KeyCustodyTier(json['tier'] as String),
        signingAlg: json['signingAlg'] as String?,
        keyAgreementCurve: json['keyAgreementCurve'] as String?,
      );

  /// `hardware`: the key is non-exportable in the secure keystore (iOS Secure Enclave /
  /// Android StrongBox) and every signing / key-agreement operation runs in-chip —
  /// achievable only with P-256. `software`: the key is held in app memory during use,
  /// stored hardware-wrapped at rest. Maintainers MAY apply stricter policy (shorter
  /// sessions, more frequent step-up) to `software`-tier devices.
  final KeyCustodyTier tier;

  /// JOSE `alg` of the holder's signing key, e.g. `ES256` (hardware-custodiable on
  /// mobile) or `EdDSA` (not).
  final String? signingAlg;

  /// Curve of the holder's keyAgreement key, e.g. `P-256` (hardware-custodiable on
  /// mobile) or `X25519` (not).
  final String? keyAgreementCurve;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'tier': tier.value,
        if (signingAlg != null) 'signingAlg': signingAlg!,
        if (keyAgreementCurve != null) 'keyAgreementCurve': keyAgreementCurve!,
      };
}

/// Fine-grained capability flag scoped to the device's allowed contexts. See SPEC.md
/// for the full semantics of each. Capability values are additive: a consumer MUST
/// ignore a value it does not recognise rather than reject the binding, and MUST NOT
/// treat an unrecognised value as conferring anything.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Capability(String value) {
  static const Capability vaultRead = Capability('vault-read');
  static const Capability vaultWrite = Capability('vault-write');
  static const Capability proxyLogin = Capability('proxy-login');
  static const Capability fillRelease = Capability('fill-release');
  static const Capability policyAdmin = Capability('policy-admin');
  static const Capability deviceAdmin = Capability('device-admin');
  static const Capability sign = Capability('sign');
  static const Capability keyMint = Capability('key-mint');
  static const Capability signTrustTask = Capability('sign-trust-task');
  static const Capability credentialWrite = Capability('credential-write');
  static const Capability memoryRead = Capability('memory-read');
  static const Capability memoryWrite = Capability('memory-write');
  static const Capability roomPresent = Capability('room-present');
  static const Capability roomOpen = Capability('room-open');

  /// Every value this specification's schema permits.
  static const List<Capability> values = <Capability>[
    vaultRead,
    vaultWrite,
    proxyLogin,
    fillRelease,
    policyAdmin,
    deviceAdmin,
    sign,
    keyMint,
    signTrustTask,
    credentialWrite,
    memoryRead,
    memoryWrite,
    roomPresent,
    roomOpen
  ];
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// DeviceBinding
class DeviceBinding {
  const DeviceBinding({
    required this.deviceId,
    required this.consumerDid,
    required this.consumerKind,
    required this.displayName,
    this.platform,
    this.attestation,
    this.pushCapable,
    this.keyCustody,
    this.capabilities,
    required this.registeredAt,
    this.lastSeenAt,
    this.disabledAt,
    this.wipedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory DeviceBinding.fromJson(Map<String, dynamic> json) => DeviceBinding(
        deviceId: json['deviceId'] as String,
        consumerDid: json['consumerDid'] as String,
        consumerKind: json['consumerKind'],
        displayName: json['displayName'] as String,
        platform: json['platform'] as String?,
        attestation: json['attestation'],
        pushCapable: json['pushCapable'] as bool?,
        keyCustody: json['keyCustody'] == null
            ? null
            : KeyCustody.fromJson(json['keyCustody'] as Map<String, dynamic>),
        capabilities: json['capabilities'] == null
            ? null
            : (json['capabilities'] as List<dynamic>)
                .map((e) => Capability(e as String))
                .toList(),
        registeredAt: json['registeredAt'] as String,
        lastSeenAt: json['lastSeenAt'] as String?,
        disabledAt: json['disabledAt'] as String?,
        wipedAt: json['wipedAt'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Maintainer-assigned opaque id for this device. Stable across the device's lifetime
  /// — never re-used after disable or wipe.
  final String deviceId;

  /// The long-term VTA-derived key (DID) the device authenticates with. Established via
  /// the ACL-swap pattern at registration.
  final String consumerDid;
  final ConsumerKind consumerKind;

  /// Human-readable name (e.g. "Glenn's MacBook — Chrome", "iPhone 17").
  final String displayName;

  /// Free-form platform descriptor (e.g. "macOS 16 / Chrome 142", "iOS 19.1", "Android
  /// 16", "Linux/x86_64"). Producer-supplied at registration; consumer-supplied updates
  /// are accepted on heartbeat.
  final String? platform;
  final DeviceAttestation? attestation;

  /// Whether this device has a usable push channel — i.e. it has registered a push token
  /// with a push gateway and conveyed the resulting opaque WakeHandle to its VTA via
  /// device/set-wake (https://trusttasks.org/binding/push/0.1). Informational visibility
  /// for device/list only. The raw platform push token is held ONLY by the gateway; the
  /// maintainer/VTA holds the opaque WakeHandle and the VTA-owned trigger allowlist,
  /// never the token. Set from the presence of a current WakeHandle for this device.
  final bool? pushCapable;

  /// How the device custodies its private key material (tier + algorithms). Maintainer
  /// policy input, mirroring `attestation` — a maintainer MAY apply stricter policy to
  /// `software`-tier devices. See docs/design-notes/mobile-key-custody-profile.md.
  final KeyCustody? keyCustody;

  /// Capability bitset granted to this device (mirrors the ACL-side scope). Returned for
  /// inspection; mutated only via acl/change-role or device/disable.
  final List<Capability>? capabilities;
  final String registeredAt;

  /// Updated on every device/heartbeat and on any successful auth.
  final String? lastSeenAt;

  /// Present when the device has been disabled (device/disable). Disabled devices cannot
  /// authenticate but their record is retained for audit.
  final String? disabledAt;

  /// Present when a wipe has been issued (device/wipe). Distinct from disabledAt — a
  /// wiped device is also disabled, but wipe additionally communicates a wipe-cache
  /// instruction that the device may or may not have executed (see device/wipe).
  final String? wipedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'deviceId': deviceId,
        'consumerDid': consumerDid,
        'consumerKind': consumerKind,
        'displayName': displayName,
        if (platform != null) 'platform': platform!,
        if (attestation != null) 'attestation': attestation!,
        if (pushCapable != null) 'pushCapable': pushCapable!,
        if (keyCustody != null) 'keyCustody': keyCustody!.toJson(),
        if (capabilities != null)
          'capabilities': capabilities!.map((e) => e.value).toList(),
        'registeredAt': registeredAt,
        if (lastSeenAt != null) 'lastSeenAt': lastSeenAt!,
        if (disabledAt != null) 'disabledAt': disabledAt!,
        if (wipedAt != null) 'wipedAt': wipedAt!,
        if (ext != null) 'ext': ext!,
      };
}

/// An opaque, gateway-issued reference to a device's push channel (push wake-up
/// binding, https://trusttasks.org/binding/push/0.1). The push gateway returns it to
/// the device at registration; the device conveys it to its VTA (device/set-wake), and
/// the VTA provisions it to authorized triggers (its mediator and/or itself). The raw
/// platform push token (APNs/FCM/WebPush) is held ONLY by the gateway and is never
/// represented here — the handle abstracts the platform, so adding new push methods
/// (e.g. PWA Web Push) needs no change to triggers or VTA config. A handle is a bearer
/// capability to *request* a wake (subject to the gateway's allowlist), never to read
/// the channel.
class WakeHandle {
  const WakeHandle({
    required this.gateway,
    required this.handle,
  });

  /// Read this payload from a decoded JSON object.
  factory WakeHandle.fromJson(Map<String, dynamic> json) => WakeHandle(
        gateway: json['gateway'] as String,
        handle: json['handle'] as String,
      );

  /// The push gateway that issued this handle and acts on it — a DID (DIDComm-reachable
  /// gateway) or an https URL (REST gateway). A trigger sends its contentless wake
  /// request here.
  final String gateway;

  /// Opaque gateway-issued identifier for the device's push channel. Reveals no platform
  /// token. Rotates whenever the device re-registers a new platform token with the
  /// gateway; the device then re-conveys the fresh handle via device/set-wake.
  final String handle;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'gateway': gateway,
        'handle': handle,
      };
}

/// VTA-owned allowlist of the DIDs permitted to trigger a wake for a given WakeHandle
/// (push wake-up binding, https://trusttasks.org/binding/push/0.1). The VTA is the
/// source of truth for this policy — all device config state resides at the VTA — and
/// provisions it to the gateway, which ENFORCES it: a wake request from a DID not on
/// the list is refused. Typically holds the device's mediator DID (queue-driven wake,
/// where the mediator alone knows the device is offline) and/or the VTA's own DID
/// (policy-driven wake, e.g. a step-up the VTA is delegating to this device).
class WakeTriggerPolicy {
  const WakeTriggerPolicy({
    required this.allowedTriggers,
  });

  /// Read this payload from a decoded JSON object.
  factory WakeTriggerPolicy.fromJson(Map<String, dynamic> json) =>
      WakeTriggerPolicy(
        allowedTriggers: (json['allowedTriggers'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
      );

  /// DIDs authorized to trigger a wake for this handle. An empty array means no party
  /// may wake the device (push effectively disabled while the handle exists). The
  /// gateway authenticates the trigger's DID before checking membership.
  final List<String> allowedTriggers;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'allowedTriggers': allowedTriggers,
      };
}

/// A device's platform push channel — the body the device registers with its push
/// GATEWAY (push wake-up binding, https://trusttasks.org/binding/push/0.1; modeled on
/// Aries RFC 0699/0734). The gateway holds this token and returns an opaque WakeHandle
/// in exchange; the token is held by the gateway ONLY, never by the mediator or the
/// maintainer/VTA. The gateway uses it to send a contentless wake-up when an
/// authorized trigger asks — the push payload never carries Trust Task content. Tagged
/// union over the discriminator `platform`.
typedef PushRegistration = Object?;
