// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/device/heartbeat/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Discriminator. The framework currently supports wipe; future versions may add
/// others.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const QueuedOperationKind(String value) {
  static const QueuedOperationKind wipe = QueuedOperationKind('wipe');
  static const QueuedOperationKind policyReload =
      QueuedOperationKind('policyReload');
  static const QueuedOperationKind configUpdate =
      QueuedOperationKind('configUpdate');

  /// Every value this specification's schema permits.
  static const List<QueuedOperationKind> values = <QueuedOperationKind>[
    wipe,
    policyReload,
    configUpdate
  ];
}

/// QueuedOperation
class QueuedOperation {
  const QueuedOperation({
    required this.kind,
    required this.task,
  });

  /// Read this payload from a decoded JSON object.
  factory QueuedOperation.fromJson(Map<String, dynamic> json) =>
      QueuedOperation(
        kind: QueuedOperationKind(json['kind'] as String),
        task: json['task'],
      );

  /// Discriminator. The framework currently supports wipe; future versions may add
  /// others.
  final QueuedOperationKind kind;

  /// The full Trust Task document the consumer would have received if it had been online
  /// (e.g. a device/wipe/0.1 document). Consumer verifies and executes as if it had been
  /// received normally.
  final Object? task;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind.value,
        'task': task,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Tells the consumer whether to call vault/sync.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseSyncHint(String value) {
  static const ResponseSyncHint upToDate = ResponseSyncHint('upToDate');
  static const ResponseSyncHint syncDue = ResponseSyncHint('syncDue');
  static const ResponseSyncHint fullResyncRequired =
      ResponseSyncHint('fullResyncRequired');

  /// Every value this specification's schema permits.
  static const List<ResponseSyncHint> values = <ResponseSyncHint>[
    upToDate,
    syncDue,
    fullResyncRequired
  ];
}

/// Device Heartbeat — response payload
class Response {
  const Response({
    required this.serverTime,
    this.queuedOperations,
    this.syncHint,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        serverTime: json['serverTime'] as String,
        queuedOperations: json['queuedOperations'] == null
            ? null
            : (json['queuedOperations'] as List<dynamic>)
                .map((e) => QueuedOperation.fromJson(e as Map<String, dynamic>))
                .toList(),
        syncHint: json['syncHint'] == null
            ? null
            : ResponseSyncHint(json['syncHint'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Authoritative timestamp — consumers MAY use to detect clock drift.
  final String serverTime;

  /// Operations the maintainer queued for this device while it was offline. The consumer
  /// MUST execute these in order before any other op.
  final List<QueuedOperation>? queuedOperations;

  /// Tells the consumer whether to call vault/sync.
  final ResponseSyncHint? syncHint;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'serverTime': serverTime,
        if (queuedOperations != null)
          'queuedOperations': queuedOperations!.map((e) => e.toJson()).toList(),
        if (syncHint != null) 'syncHint': syncHint!.value,
        if (ext != null) 'ext': ext!,
      };
}

/// Periodic check-in from a Companion or Service. Refreshes `lastSeenAt`, carries
/// optional state digests, and gives the maintainer a chance to deliver queued
/// operations (notably queued wipes for targets that were offline at issuance).
class Payload {
  const Payload({
    this.platform,
    this.vaultSeq,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        platform: json['platform'] as String?,
        vaultSeq: json['vaultSeq'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Updated platform descriptor if it changed since registration (e.g. browser
  /// updated).
  final String? platform;

  /// Optional — consumer's current sync baseline. If the maintainer notices a gap
  /// (consumer is behind), the response can hint that a vault/sync is due.
  final int? vaultSeq;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (platform != null) 'platform': platform!,
        if (vaultSeq != null) 'vaultSeq': vaultSeq!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/device/heartbeat/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/device/heartbeat/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/device/heartbeat/0.2","title":"Device Heartbeat — payload","description":"Periodic check-in from a Companion or Service. Refreshes `lastSeenAt`, carries optional state digests, and gives the maintainer a chance to deliver queued operations (notably queued wipes for targets that were offline at issuance).","type":"object","additionalProperties":false,"properties":{"platform":{"type":"string","description":"Updated platform descriptor if it changed since registration (e.g. browser updated)."},"vaultSeq":{"type":"integer","minimum":0,"description":"Optional — consumer\'s current sync baseline. If the maintainer notices a gap (consumer is behind), the response can hint that a vault/sync is due."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Device Heartbeat — response payload","type":"object","additionalProperties":false,"required":["serverTime"],"properties":{"serverTime":{"type":"string","format":"date-time","description":"Authoritative timestamp — consumers MAY use to detect clock drift."},"queuedOperations":{"type":"array","items":{"\$ref":"#/\$defs/QueuedOperation"},"description":"Operations the maintainer queued for this device while it was offline. The consumer MUST execute these in order before any other op."},"syncHint":{"type":"string","enum":["upToDate","syncDue","fullResyncRequired"],"description":"Tells the consumer whether to call vault/sync."},"ext":{"\$ref":"#/\$defs/Ext"}}},"QueuedOperation":{"title":"QueuedOperation","type":"object","additionalProperties":false,"required":["kind","task"],"properties":{"kind":{"type":"string","enum":["wipe","policyReload","configUpdate"],"description":"Discriminator. The framework currently supports wipe; future versions may add others."},"task":{"description":"The full Trust Task document the consumer would have received if it had been online (e.g. a device/wipe/0.1 document). Consumer verifies and executes as if it had been received normally."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Device Heartbeat — response payload","type":"object","additionalProperties":false,"required":["serverTime"],"properties":{"serverTime":{"type":"string","format":"date-time","description":"Authoritative timestamp — consumers MAY use to detect clock drift."},"queuedOperations":{"type":"array","items":{"\$ref":"#/\$defs/QueuedOperation"},"description":"Operations the maintainer queued for this device while it was offline. The consumer MUST execute these in order before any other op."},"syncHint":{"type":"string","enum":["upToDate","syncDue","fullResyncRequired"],"description":"Tells the consumer whether to call vault/sync."},"ext":{"\$ref":"#/\$defs/Ext"}}},"QueuedOperation":{"title":"QueuedOperation","type":"object","additionalProperties":false,"required":["kind","task"],"properties":{"kind":{"type":"string","enum":["wipe","policyReload","configUpdate"],"description":"Discriminator. The framework currently supports wipe; future versions may add others."},"task":{"description":"The full Trust Task document the consumer would have received if it had been online (e.g. a device/wipe/0.1 document). Consumer verifies and executes as if it had been received normally."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
