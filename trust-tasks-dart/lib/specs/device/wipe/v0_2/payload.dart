// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/device/wipe/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseScope is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseScope(String value) {
  static const ResponseScope cache = ResponseScope('cache');
  static const ResponseScope cacheAndKeys = ResponseScope('cacheAndKeys');
  static const ResponseScope full = ResponseScope('full');

  /// Every value this specification's schema permits.
  static const List<ResponseScope> values = <ResponseScope>[
    cache,
    cacheAndKeys,
    full
  ];
}

/// ResponseDiagnostics, generated from its schema.
class ResponseDiagnostics {
  const ResponseDiagnostics({
    this.cacheBytesWiped,
    this.keysWiped,
    this.osHooksInvoked,
    this.partialReasons,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseDiagnostics.fromJson(Map<String, dynamic> json) =>
      ResponseDiagnostics(
        cacheBytesWiped: json['cacheBytesWiped'] as int?,
        keysWiped: json['keysWiped'] as int?,
        osHooksInvoked: json['osHooksInvoked'] == null
            ? null
            : (json['osHooksInvoked'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        partialReasons: json['partialReasons'] == null
            ? null
            : (json['partialReasons'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
      );

  final int? cacheBytesWiped;
  final int? keysWiped;

  /// OS-level revocation hooks the target managed to invoke (e.g.
  /// "navigator.credentials.preventSilentAccess",
  /// "ASCredentialIdentityStore.removeAllCredentialIdentities").
  final List<String>? osHooksInvoked;

  /// Free-form reasons why the wipe was partial (e.g. "os-keychain-unavailable",
  /// "extension-storage-quota-exceeded").
  final List<String>? partialReasons;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (cacheBytesWiped != null) 'cacheBytesWiped': cacheBytesWiped!,
        if (keysWiped != null) 'keysWiped': keysWiped!,
        if (osHooksInvoked != null) 'osHooksInvoked': osHooksInvoked!,
        if (partialReasons != null) 'partialReasons': partialReasons!,
      };
}

/// Acknowledgement from the target device. Sent only when the target executes the
/// wipe; absent if the target was offline or compromised. The maintainer treats the
/// absence of a response as 'not confirmed' but considers the device neutralised
/// because of the server-side defence-in-depth.
class Response {
  const Response({
    required this.deviceId,
    required this.scope,
    required this.completedAt,
    this.diagnostics,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        deviceId: json['deviceId'] as String,
        scope: ResponseScope(json['scope'] as String),
        completedAt: json['completedAt'] as String,
        diagnostics: json['diagnostics'] == null
            ? null
            : ResponseDiagnostics.fromJson(
                json['diagnostics'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String deviceId;
  final ResponseScope scope;
  final String completedAt;
  final ResponseDiagnostics? diagnostics;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'deviceId': deviceId,
        'scope': scope.value,
        'completedAt': completedAt,
        if (diagnostics != null) 'diagnostics': diagnostics!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// How aggressively the target should wipe: - `cache` — discard the encrypted vault
/// cache; consumer can re-sync with valid creds. - `cacheAndKeys` — discard cache +
/// device-local key material; consumer must re-onboard. - `full` — `cacheAndKeys` +
/// clear all extension/app storage + revoke OS credential-provider registration where
/// APIs permit.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadScope(String value) {
  static const PayloadScope cache = PayloadScope('cache');
  static const PayloadScope cacheAndKeys = PayloadScope('cacheAndKeys');
  static const PayloadScope full = PayloadScope('full');

  /// Every value this specification's schema permits.
  static const List<PayloadScope> values = <PayloadScope>[
    cache,
    cacheAndKeys,
    full
  ];
}

/// The maintainer issues a wipe to a Companion or Service. The target is expected to
/// destroy its local cache and (depending on scope) its device-local key material. The
/// action is best-effort — a compromised device may silently drop the wipe — so the
/// maintainer additionally revokes ACL access and rotates the device's cache-key
/// derivation root, so that defence in depth means a non-compliant device is still
/// neutralised.
class Payload {
  const Payload({
    required this.deviceId,
    required this.scope,
    required this.reason,
    this.issuedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        deviceId: json['deviceId'] as String,
        scope: PayloadScope(json['scope'] as String),
        reason: json['reason'] as String,
        issuedAt: json['issuedAt'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String deviceId;

  /// How aggressively the target should wipe: - `cache` — discard the encrypted vault
  /// cache; consumer can re-sync with valid creds. - `cacheAndKeys` — discard cache +
  /// device-local key material; consumer must re-onboard. - `full` — `cacheAndKeys` +
  /// clear all extension/app storage + revoke OS credential-provider registration where
  /// APIs permit.
  final PayloadScope scope;

  /// Human-readable reason. Required (not optional) because every wipe is consequential
  /// and the audit log must capture intent.
  final String reason;

  /// Wipe-issuance timestamp; identical to the document's `issuedAt`, repeated here so
  /// the body is self-contained for offline-queued delivery.
  final String? issuedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'deviceId': deviceId,
        'scope': scope.value,
        'reason': reason,
        if (issuedAt != null) 'issuedAt': issuedAt!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/device/wipe/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/device/wipe/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/device/wipe/0.2","title":"Device Wipe — payload","description":"The maintainer issues a wipe to a Companion or Service. The target is expected to destroy its local cache and (depending on scope) its device-local key material. The action is best-effort — a compromised device may silently drop the wipe — so the maintainer additionally revokes ACL access and rotates the device\'s cache-key derivation root, so that defence in depth means a non-compliant device is still neutralised.","type":"object","additionalProperties":false,"required":["deviceId","scope","reason"],"properties":{"deviceId":{"type":"string","minLength":1},"scope":{"type":"string","enum":["cache","cacheAndKeys","full"],"description":"How aggressively the target should wipe:\\n- `cache` — discard the encrypted vault cache; consumer can re-sync with valid creds.\\n- `cacheAndKeys` — discard cache + device-local key material; consumer must re-onboard.\\n- `full` — `cacheAndKeys` + clear all extension/app storage + revoke OS credential-provider registration where APIs permit."},"reason":{"type":"string","minLength":1,"maxLength":256,"description":"Human-readable reason. Required (not optional) because every wipe is consequential and the audit log must capture intent."},"issuedAt":{"type":"string","format":"date-time","description":"Wipe-issuance timestamp; identical to the document\'s `issuedAt`, repeated here so the body is self-contained for offline-queued delivery."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Device Wipe — response payload","description":"Acknowledgement from the target device. Sent only when the target executes the wipe; absent if the target was offline or compromised. The maintainer treats the absence of a response as \'not confirmed\' but considers the device neutralised because of the server-side defence-in-depth.","type":"object","additionalProperties":false,"required":["deviceId","scope","completedAt"],"properties":{"deviceId":{"type":"string"},"scope":{"type":"string","enum":["cache","cacheAndKeys","full"]},"completedAt":{"type":"string","format":"date-time"},"diagnostics":{"type":"object","additionalProperties":false,"properties":{"cacheBytesWiped":{"type":"integer","minimum":0},"keysWiped":{"type":"integer","minimum":0},"osHooksInvoked":{"type":"array","items":{"type":"string"},"description":"OS-level revocation hooks the target managed to invoke (e.g. \\"navigator.credentials.preventSilentAccess\\", \\"ASCredentialIdentityStore.removeAllCredentialIdentities\\")."},"partialReasons":{"type":"array","items":{"type":"string"},"description":"Free-form reasons why the wipe was partial (e.g. \\"os-keychain-unavailable\\", \\"extension-storage-quota-exceeded\\")."}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Device Wipe — response payload","description":"Acknowledgement from the target device. Sent only when the target executes the wipe; absent if the target was offline or compromised. The maintainer treats the absence of a response as \'not confirmed\' but considers the device neutralised because of the server-side defence-in-depth.","type":"object","additionalProperties":false,"required":["deviceId","scope","completedAt"],"properties":{"deviceId":{"type":"string"},"scope":{"type":"string","enum":["cache","cacheAndKeys","full"]},"completedAt":{"type":"string","format":"date-time"},"diagnostics":{"type":"object","additionalProperties":false,"properties":{"cacheBytesWiped":{"type":"integer","minimum":0},"keysWiped":{"type":"integer","minimum":0},"osHooksInvoked":{"type":"array","items":{"type":"string"},"description":"OS-level revocation hooks the target managed to invoke (e.g. \\"navigator.credentials.preventSilentAccess\\", \\"ASCredentialIdentityStore.removeAllCredentialIdentities\\")."},"partialReasons":{"type":"array","items":{"type":"string"},"description":"Free-form reasons why the wipe was partial (e.g. \\"os-keychain-unavailable\\", \\"extension-storage-quota-exceeded\\")."}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
