// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/sync/_shared/0.1/sync-event.schema.json

/// A single binding target for a vault entry. Tagged union over the discriminator
/// `kind`. A VaultEntry's `targets` array MAY mix any number of these.
typedef SiteTarget = Object?;

/// Discriminator for the secret type stored in the entry. Definitions: - `password` —
/// username + password (+ optional TOTP seed). - `passkey` — WebAuthn discoverable
/// credential (private key + rpId + userHandle). - `oauth-tokens` — OAuth 2.0 refresh
/// + access token bundle for a specific provider. - `did-self-issued` — Self-Issued
/// OpenID Provider v2 (SIOP) credential: the entry points at a DID + signing key
/// already managed by the VTA. - `didcomm-peer` — DIDComm peer identity used to
/// authenticate against a DIDComm-speaking relying party. - `bearer-token` — opaque
/// bearer token carried in a maintainer-named header (covers API tokens, long-lived
/// JWTs, personal-access tokens). - `ssh-key` — SSH private key + comment. - `custom`
/// — arbitrary structured fields; release-time consumer responsible for
/// interpretation.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const SecretKind(String value) {
  static const SecretKind password = SecretKind('password');
  static const SecretKind passkey = SecretKind('passkey');
  static const SecretKind oauthTokens = SecretKind('oauth-tokens');
  static const SecretKind didSelfIssued = SecretKind('did-self-issued');
  static const SecretKind didcommPeer = SecretKind('didcomm-peer');
  static const SecretKind bearerToken = SecretKind('bearer-token');
  static const SecretKind sshKey = SecretKind('ssh-key');
  static const SecretKind custom = SecretKind('custom');

  /// Every value this specification's schema permits.
  static const List<SecretKind> values = <SecretKind>[
    password,
    passkey,
    oauthTokens,
    didSelfIssued,
    didcommPeer,
    bearerToken,
    sshKey,
    custom
  ];
}

/// AttachmentRef
class AttachmentRef {
  const AttachmentRef({
    required this.id,
    required this.name,
    required this.sizeBytes,
    required this.sha256,
    this.contentType,
  });

  /// Read this payload from a decoded JSON object.
  factory AttachmentRef.fromJson(Map<String, dynamic> json) => AttachmentRef(
        id: json['id'] as String,
        name: json['name'] as String,
        sizeBytes: json['sizeBytes'] as int,
        sha256: json['sha256'] as String,
        contentType: json['contentType'] as String?,
      );

  /// Opaque maintainer-assigned id for this attachment; used to fetch the blob via a
  /// separate mechanism.
  final String id;

  /// User-supplied filename (e.g. "recovery-codes.txt").
  final String name;

  /// Size of the encrypted blob in bytes. Maintainers MAY enforce a maximum per
  /// attachment and per entry.
  final int sizeBytes;

  /// Hex-encoded SHA-256 of the encrypted blob bytes (post-encryption). Lets the
  /// consumer verify integrity after fetch.
  final String sha256;

  /// Optional MIME type hint for the consumer UI (e.g. "text/plain",
  /// "application/x-pem-file").
  final String? contentType;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
        'sizeBytes': sizeBytes,
        'sha256': sha256,
        if (contentType != null) 'contentType': contentType!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Archival-lifecycle state of this entry. Absent means `active`, so a maintainer that
/// models no lifecycle omits it and every existing document stays valid. Deliberately
/// carries no JSON Schema `default`: a declared default is materialised by generated
/// bindings, which would make an absent `status` reappear as an explicit `"active"` on
/// re-serialisation and break round-trip idempotence for every existing document. This
/// member is what makes `vault/list`'s `status: "all"` selector usable: that view
/// returns entries in every state by design, and without a state on the entry itself a
/// consumer cannot tell which is which. An `archived` entry is hidden from ordinary
/// listings but intact; a `deleted` entry is a tombstone inside its grace window,
/// still restorable until `graceUntil` passes.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const VaultEntryStatus(String value) {
  static const VaultEntryStatus active = VaultEntryStatus('active');
  static const VaultEntryStatus archived = VaultEntryStatus('archived');
  static const VaultEntryStatus deleted = VaultEntryStatus('deleted');

  /// Every value this specification's schema permits.
  static const List<VaultEntryStatus> values = <VaultEntryStatus>[
    active,
    archived,
    deleted
  ];
}

/// VaultEntry
class VaultEntry {
  const VaultEntry({
    required this.id,
    required this.contextId,
    required this.targets,
    required this.label,
    required this.secretKind,
    this.tags,
    this.notes,
    this.favicon,
    this.selectors,
    this.customFieldNames,
    this.attachments,
    this.expiresAt,
    this.breachedAt,
    this.passwordChangedAt,
    required this.createdAt,
    this.createdBy,
    required this.updatedAt,
    this.updatedBy,
    this.lastUsedAt,
    required this.version,
    this.principalDid,
    this.status,
    this.archivedAt,
    this.deletedAt,
    this.graceUntil,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory VaultEntry.fromJson(Map<String, dynamic> json) => VaultEntry(
        id: json['id'] as String,
        contextId: json['contextId'] as String,
        targets: (json['targets'] as List<dynamic>).map((e) => e).toList(),
        label: json['label'] as String,
        secretKind: SecretKind(json['secretKind'] as String),
        tags: json['tags'] == null
            ? null
            : (json['tags'] as List<dynamic>).map((e) => e as String).toList(),
        notes: json['notes'] as String?,
        favicon: json['favicon'] as String?,
        selectors: json['selectors'] == null
            ? null
            : (json['selectors'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        customFieldNames: json['customFieldNames'] == null
            ? null
            : (json['customFieldNames'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        attachments: json['attachments'] == null
            ? null
            : (json['attachments'] as List<dynamic>)
                .map((e) => AttachmentRef.fromJson(e as Map<String, dynamic>))
                .toList(),
        expiresAt: json['expiresAt'] as String?,
        breachedAt: json['breachedAt'] as String?,
        passwordChangedAt: json['passwordChangedAt'] as String?,
        createdAt: json['createdAt'] as String,
        createdBy: json['createdBy'] as String?,
        updatedAt: json['updatedAt'] as String,
        updatedBy: json['updatedBy'] as String?,
        lastUsedAt: json['lastUsedAt'] as String?,
        version: json['version'] as int,
        principalDid: json['principalDid'] as String?,
        status: json['status'] == null
            ? null
            : VaultEntryStatus(json['status'] as String),
        archivedAt: json['archivedAt'] as String?,
        deletedAt: json['deletedAt'] as String?,
        graceUntil: json['graceUntil'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque vault-maintainer-assigned identifier for the entry. ULID/UUID/base32 are
  /// common; the wire spec only requires non-empty string equality.
  final String id;

  /// Identifier of the trust context (persona) the entry belongs to. Opaque string
  /// interpreted by the vault maintainer; corresponds to a single ContextRecord on the
  /// VTA side.
  final String contextId;

  /// One or more binding targets — web origins, mobile app identifiers, and/or DIDs —
  /// that this credential applies to. A request from any matching target uses this
  /// entry. A typical entry for a service that exists as both a website and mobile apps
  /// will list a web origin, an iOS bundle id, and an Android package id; passkeys for
  /// that service typically list only the origin (because iOS Associated Domains and
  /// Android Asset Links bind apps to the domain at the OS level).
  final List<SiteTarget> targets;

  /// Human-readable display name (e.g. "Work GitHub", "Personal bank — checking").
  /// Bounded on the wire at 256 characters; a maintainer MAY enforce a shorter limit.
  final String label;

  /// Discriminator for the kind of secret this entry holds. The secret material itself
  /// is NEVER returned in metadata views; the kind is exposed so consumers can render an
  /// appropriate UI affordance and so policy decisions can route by kind.
  final SecretKind secretKind;

  /// User-defined tags for organisation and filtering (e.g. \["family", "finance"\]).
  /// Maintainers MAY enforce a maximum count; the wire spec does not.
  final List<String>? tags;

  /// Non-sensitive notes the user attached to the entry. Visible in metadata view
  /// (suitable for support contact, account number, expiry policy memos). SENSITIVE
  /// notes belong in the secret payload as a `secureNotes` field — those are only
  /// released by vault/release/0.1.
  final String? notes;

  /// Optional URI of an icon to display in the consumer UI. Maintainers MAY fetch and
  /// cache; consumers SHOULD treat as untrusted content and fetch via a sandboxed
  /// pipeline.
  final String? favicon;

  /// Opaque maintainer-defined selector strings fed to the policy engine when this entry
  /// is requested (e.g. "recent_uv_required", "network_class=corp", "step_up_push").
  /// Consumers MUST treat selectors as opaque; they exist for policy authoring on the
  /// maintainer side.
  final List<String>? selectors;

  /// Names of additional fields the user has attached (e.g. \["security-question-1",
  /// "account-number"\]). The VALUES live in the secret payload and are only delivered by
  /// vault/release/0.1. Exposing names in metadata lets the consumer render the right
  /// form layout before requesting release.
  final List<String>? customFieldNames;

  /// References to encrypted blobs associated with the entry (recovery codes, PEM files,
  /// screenshots of authenticator setup). The blobs themselves are fetched via a
  /// separate mechanism the maintainer documents; metadata view exposes only the
  /// descriptor.
  final List<AttachmentRef>? attachments;

  /// Optional time after which the credential is no longer expected to be valid (e.g. an
  /// OAuth refresh token's known expiry, a time-limited API token, an enterprise
  /// password rotation policy). Maintainers MAY surface this in the consumer UI as a
  /// warning.
  final String? expiresAt;

  /// Set by the maintainer (via HIBP integration or equivalent) when the password
  /// material associated with this entry is known to appear in a public breach.
  /// Consumers SHOULD surface this prominently. Cleared when the user rotates the
  /// password and the new password is not in any known breach.
  final String? breachedAt;

  /// Set whenever the password component of the secret payload is rotated. Maintainers
  /// MUST update this on every secret-material change for entries of kind `password` (or
  /// any kind that carries a password component). Used by consumers to surface
  /// rotation-overdue warnings.
  final String? passwordChangedAt;
  final String createdAt;

  /// VID of the consumer that originally created the entry.
  final String? createdBy;
  final String updatedAt;

  /// VID of the consumer that last modified the entry.
  final String? updatedBy;

  /// Most recent time the entry was used (either released or proxy-login performed).
  /// Maintainers MAY return this with reduced precision (e.g. hour-floored) when
  /// releasing to a less-trusted consumer.
  final String? lastUsedAt;

  /// Monotonic version counter incremented on every mutation. Used by consumers for
  /// optimistic-concurrency checks on vault/upsert and as the seq baseline for
  /// vault/sync.
  final int version;

  /// Optional cached DID the entry will act AS for DID-shaped flows — mirrors the `did`
  /// field of the entry's secret payload when `secretKind` carries one
  /// (`did-self-issued`, `didcomm-peer`). Absent for kinds that have no DID concept
  /// (`password`, `passkey`, `oauth-tokens`, `bearer-token`, `ssh-key`, `custom`).
  /// MAINTAINER-DERIVED, NOT CONSUMER-SUPPLIED: the maintainer MUST recompute this from
  /// the canonical secret at every upsert / secret rotation; a producer-supplied value
  /// on `vault/upsert/0.1` MUST be ignored (no error, but no honour). Read-only on the
  /// wire, present in metadata views so consumers can drive RP-side flows (e.g. fetch
  /// `/auth/challenge` keyed on the principal DID before requesting a proxy-login)
  /// without releasing the secret.
  final String? principalDid;

  /// Archival-lifecycle state of this entry. Absent means `active`, so a maintainer that
  /// models no lifecycle omits it and every existing document stays valid. Deliberately
  /// carries no JSON Schema `default`: a declared default is materialised by generated
  /// bindings, which would make an absent `status` reappear as an explicit `"active"` on
  /// re-serialisation and break round-trip idempotence for every existing document. This
  /// member is what makes `vault/list`'s `status: "all"` selector usable: that view
  /// returns entries in every state by design, and without a state on the entry itself a
  /// consumer cannot tell which is which. An `archived` entry is hidden from ordinary
  /// listings but intact; a `deleted` entry is a tombstone inside its grace window,
  /// still restorable until `graceUntil` passes.
  final VaultEntryStatus? status;

  /// When the entry was archived. Present iff `status` is `archived`.
  final String? archivedAt;

  /// When the entry was soft-deleted. Present iff `status` is `deleted`. A soft delete
  /// is reversible: this records when the grace window opened, not when the entry ceased
  /// to exist.
  final String? deletedAt;

  /// Deadline after which a `deleted` entry is irreversibly purged. Present iff `status`
  /// is `deleted`. It is the one member a consumer showing a trash view actually needs —
  /// "deleted" without a deadline cannot be distinguished from "gone", and the
  /// difference is whether the holder can still get their credential back.
  final String? graceUntil;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1. Reverse-DNS-namespaced;
  /// consumers MUST ignore unrecognized namespaces.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'contextId': contextId,
        'targets': targets,
        'label': label,
        'secretKind': secretKind.value,
        if (tags != null) 'tags': tags!,
        if (notes != null) 'notes': notes!,
        if (favicon != null) 'favicon': favicon!,
        if (selectors != null) 'selectors': selectors!,
        if (customFieldNames != null) 'customFieldNames': customFieldNames!,
        if (attachments != null)
          'attachments': attachments!.map((e) => e.toJson()).toList(),
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (breachedAt != null) 'breachedAt': breachedAt!,
        if (passwordChangedAt != null) 'passwordChangedAt': passwordChangedAt!,
        'createdAt': createdAt,
        if (createdBy != null) 'createdBy': createdBy!,
        'updatedAt': updatedAt,
        if (updatedBy != null) 'updatedBy': updatedBy!,
        if (lastUsedAt != null) 'lastUsedAt': lastUsedAt!,
        'version': version,
        if (principalDid != null) 'principalDid': principalDid!,
        if (status != null) 'status': status!.value,
        if (archivedAt != null) 'archivedAt': archivedAt!,
        if (deletedAt != null) 'deletedAt': deletedAt!,
        if (graceUntil != null) 'graceUntil': graceUntil!,
        if (ext != null) 'ext': ext!,
      };
}

/// VaultUpsertedEvent
class VaultUpsertedEvent {
  const VaultUpsertedEvent({
    required this.kind,
    required this.seq,
    required this.occurredAt,
    required this.entry,
  });

  /// Read this payload from a decoded JSON object.
  factory VaultUpsertedEvent.fromJson(Map<String, dynamic> json) =>
      VaultUpsertedEvent(
        kind: json['kind'],
        seq: json['seq'] as int,
        occurredAt: json['occurredAt'] as String,
        entry: VaultEntry.fromJson(json['entry'] as Map<String, dynamic>),
      );

  final Object? kind;
  final int seq;
  final String occurredAt;
  final VaultEntry entry;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'seq': seq,
        'occurredAt': occurredAt,
        'entry': entry.toJson(),
      };
}

/// VaultDeletedEvent
class VaultDeletedEvent {
  const VaultDeletedEvent({
    required this.kind,
    required this.seq,
    required this.occurredAt,
    required this.id,
    required this.contextId,
    required this.graceUntil,
  });

  /// Read this payload from a decoded JSON object.
  factory VaultDeletedEvent.fromJson(Map<String, dynamic> json) =>
      VaultDeletedEvent(
        kind: json['kind'],
        seq: json['seq'] as int,
        occurredAt: json['occurredAt'] as String,
        id: json['id'] as String,
        contextId: json['contextId'] as String,
        graceUntil: json['graceUntil'] as String,
      );

  final Object? kind;
  final int seq;
  final String occurredAt;
  final String id;
  final String contextId;
  final String graceUntil;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'seq': seq,
        'occurredAt': occurredAt,
        'id': id,
        'contextId': contextId,
        'graceUntil': graceUntil,
      };
}

/// AclChangedEventChange is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const AclChangedEventChange(String value) {
  static const AclChangedEventChange granted = AclChangedEventChange('granted');
  static const AclChangedEventChange revoked = AclChangedEventChange('revoked');
  static const AclChangedEventChange roleChanged =
      AclChangedEventChange('role_changed');
  static const AclChangedEventChange swapped = AclChangedEventChange('swapped');
  static const AclChangedEventChange deviceDisabled =
      AclChangedEventChange('device_disabled');
  static const AclChangedEventChange deviceWiped =
      AclChangedEventChange('device_wiped');

  /// Every value this specification's schema permits.
  static const List<AclChangedEventChange> values = <AclChangedEventChange>[
    granted,
    revoked,
    roleChanged,
    swapped,
    deviceDisabled,
    deviceWiped
  ];
}

/// AclChangedEvent
class AclChangedEvent {
  const AclChangedEvent({
    required this.kind,
    required this.seq,
    required this.occurredAt,
    required this.subject,
    required this.change,
  });

  /// Read this payload from a decoded JSON object.
  factory AclChangedEvent.fromJson(Map<String, dynamic> json) =>
      AclChangedEvent(
        kind: json['kind'],
        seq: json['seq'] as int,
        occurredAt: json['occurredAt'] as String,
        subject: json['subject'] as String,
        change: AclChangedEventChange(json['change'] as String),
      );

  final Object? kind;
  final int seq;
  final String occurredAt;

  /// DID of the consumer whose ACL entry changed.
  final String subject;
  final AclChangedEventChange change;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'seq': seq,
        'occurredAt': occurredAt,
        'subject': subject,
        'change': change.value,
      };
}

/// PolicyChangedEventChange is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PolicyChangedEventChange(String value) {
  static const PolicyChangedEventChange created =
      PolicyChangedEventChange('created');
  static const PolicyChangedEventChange updated =
      PolicyChangedEventChange('updated');
  static const PolicyChangedEventChange deleted =
      PolicyChangedEventChange('deleted');

  /// Every value this specification's schema permits.
  static const List<PolicyChangedEventChange> values =
      <PolicyChangedEventChange>[created, updated, deleted];
}

/// PolicyChangedEvent
class PolicyChangedEvent {
  const PolicyChangedEvent({
    required this.kind,
    required this.seq,
    required this.occurredAt,
    required this.policyId,
    required this.change,
  });

  /// Read this payload from a decoded JSON object.
  factory PolicyChangedEvent.fromJson(Map<String, dynamic> json) =>
      PolicyChangedEvent(
        kind: json['kind'],
        seq: json['seq'] as int,
        occurredAt: json['occurredAt'] as String,
        policyId: json['policyId'] as String,
        change: PolicyChangedEventChange(json['change'] as String),
      );

  final Object? kind;
  final int seq;
  final String occurredAt;
  final String policyId;
  final PolicyChangedEventChange change;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'seq': seq,
        'occurredAt': occurredAt,
        'policyId': policyId,
        'change': change.value,
      };
}

/// SyncEvent, defined by this specification's schema.
typedef SyncEvent = Object?;
