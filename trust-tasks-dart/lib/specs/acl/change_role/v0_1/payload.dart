// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/acl/change-role/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Minimum step-up mode this subject MUST satisfy for gated operations, raising the
/// system floor. `self` = the subject re-authenticates its own session; `delegated` =
/// a separate `approver` MUST ratify. Omitted → the system floor applies unchanged. A
/// value weaker than the resolved floor is ignored (additive-only).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const AclEntryStepUpRequire(String value) {
  static const AclEntryStepUpRequire self = AclEntryStepUpRequire('self');
  static const AclEntryStepUpRequire delegated =
      AclEntryStepUpRequire('delegated');

  /// Every value this specification's schema permits.
  static const List<AclEntryStepUpRequire> values = <AclEntryStepUpRequire>[
    self,
    delegated
  ];
}

/// Per-entry authentication step-up configuration, consumed by the ACL maintainer when
/// it gates an operation behind a step-up (see auth/step-up/policy/0.1).
/// ADDITIVE-ONLY: a per-entry setting MAY raise the assurance required of this subject
/// above the maintainer's system-wide floor, but MUST NOT lower it. The maintainer
/// resolves the effective requirement as the strictest of (system floor, this entry).
class AclEntryStepUp {
  const AclEntryStepUp({
    this.approver,
    this.require,
  });

  /// Read this payload from a decoded JSON object.
  factory AclEntryStepUp.fromJson(Map<String, dynamic> json) => AclEntryStepUp(
        approver: json['approver'] as String?,
        require: json['require'] == null
            ? null
            : AclEntryStepUpRequire(json['require'] as String),
      );

  /// VID authorized to ratify step-up for this subject — the `recipient` the maintainer
  /// addresses an auth/step-up/approve-request to (e.g. the holder's mobile
  /// authenticator or browser companion). Absent → the subject is its own approver (mode
  /// `self`) when it holds a usable authenticator; if neither an `approver` nor a self
  /// authenticator exists, no step-up method is available for this subject and the
  /// maintainer's fail-closed rule applies.
  final String? approver;

  /// Minimum step-up mode this subject MUST satisfy for gated operations, raising the
  /// system floor. `self` = the subject re-authenticates its own session; `delegated` =
  /// a separate `approver` MUST ratify. Omitted → the system floor applies unchanged. A
  /// value weaker than the resolved floor is ignored (additive-only).
  final AclEntryStepUpRequire? require;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (approver != null) 'approver': approver!,
        if (require != null) 'require': require!.value,
      };
}

/// Approve-authority: what this subject may **confer on others** by ratifying an
/// approval, as distinct from `scopes`, which is what it may **exercise itself**. The
/// two axes are independent, and that independence is the point — it is what lets a
/// maintainer configure a least-privilege approver who can authorize an operation in a
/// scope it has no authority to perform. OMISSION MEANS NOTHING IS CONFERRED. An
/// absent `approve`, an absent `all`, and an empty `scopes` are all equivalent to
/// "this subject may ratify nothing". A consumer that does not implement this member
/// therefore confers less than the producer intended rather than more, which is the
/// direction a missed member has to fail in. A subject with approve-authority is NOT
/// thereby authorized to act. Consumers MUST resolve the two axes separately: reading
/// `approve` to answer "may this party ratify X" and `scopes` to answer "may this
/// party do X". Collapsing them grants an approver the ability to perform what it was
/// only meant to sign off on.
class AclEntryApprove {
  const AclEntryApprove({
    this.all,
    this.scopes,
  });

  /// Read this payload from a decoded JSON object.
  factory AclEntryApprove.fromJson(Map<String, dynamic> json) =>
      AclEntryApprove(
        all: json['all'] as bool?,
        scopes: json['scopes'] == null
            ? null
            : (json['scopes'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
      );

  /// The subject may confer ANY scope. Takes precedence over `scopes`, which a consumer
  /// MUST ignore when this is true. Absent or false → only the scopes listed below, if
  /// any.
  final bool? all;

  /// Opaque scope identifiers this subject may confer, drawn from the same vocabulary as
  /// the entry's own `scopes`. Where a maintainer's scopes are hierarchical, conferring
  /// a scope confers its descendants — the same containment rule the maintainer already
  /// applies to `scopes`, so the two axes cannot disagree about what a scope means. An
  /// empty array confers nothing; it is not a wildcard.
  final List<String>? scopes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (all != null) 'all': all!,
        if (scopes != null) 'scopes': scopes!,
      };
}

/// AclEntry
class AclEntry {
  const AclEntry({
    required this.subject,
    required this.role,
    this.scopes,
    this.allowedKeys,
    this.label,
    this.createdAt,
    this.createdBy,
    this.updatedAt,
    this.updatedBy,
    this.expiresAt,
    this.stepUp,
    this.approve,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory AclEntry.fromJson(Map<String, dynamic> json) => AclEntry(
        subject: json['subject'] as String,
        role: json['role'] as String,
        scopes: json['scopes'] == null
            ? null
            : (json['scopes'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        allowedKeys: json['allowedKeys'] == null
            ? null
            : (json['allowedKeys'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        label: json['label'] as String?,
        createdAt: json['createdAt'] as String?,
        createdBy: json['createdBy'] as String?,
        updatedAt: json['updatedAt'] as String?,
        updatedBy: json['updatedBy'] as String?,
        expiresAt: json['expiresAt'] as String?,
        stepUp: json['stepUp'] == null
            ? null
            : AclEntryStepUp.fromJson(json['stepUp'] as Map<String, dynamic>),
        approve: json['approve'] == null
            ? null
            : AclEntryApprove.fromJson(json['approve'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// VID of the party in the ACL. Compared by exact string equality (SPEC.md §4.8);
  /// producers SHOULD emit canonical form.
  final String subject;

  /// Opaque role identifier interpreted by the ACL maintainer.
  final String role;

  /// Opaque scope identifiers (e.g. contexts, domains, resource prefixes).
  final List<String>? scopes;

  /// Key identifiers this subject may invoke the maintainer's signing oracle on.
  /// INTERSECTS WITH `scopes` — it can only narrow, never widen: a key named here that
  /// lies outside the entry's scopes remains unreachable, exactly as if it were not
  /// named. ABSENT means every key within the entry's scopes (the behaviour of entries
  /// that pre-date this member); explicit `null` is equivalent to absent, and producers
  /// SHOULD omit the member instead. PRESENT-BUT-EMPTY means authorized on NO keys — the
  /// opposite of absent, and deliberately so: emptiness is never a wildcard
  /// (CONVENTIONS.md §5). A consumer MUST preserve and enforce the absent-vs-empty
  /// distinction end to end; collapsing the two (e.g. by testing emptiness alone)
  /// re-creates the empty-means-unrestricted class of privilege-escalation defect this
  /// family's conventions exist to prevent.
  final List<String>? allowedKeys;

  /// Optional human-readable label.
  final String? label;
  final String? createdAt;

  /// VID of the party that originally added this entry.
  final String? createdBy;
  final String? updatedAt;

  /// VID of the party that last modified this entry.
  final String? updatedBy;

  /// Optional time after which the entry is no longer effective.
  final String? expiresAt;

  /// Per-entry authentication step-up configuration, consumed by the ACL maintainer when
  /// it gates an operation behind a step-up (see auth/step-up/policy/0.1).
  /// ADDITIVE-ONLY: a per-entry setting MAY raise the assurance required of this subject
  /// above the maintainer's system-wide floor, but MUST NOT lower it. The maintainer
  /// resolves the effective requirement as the strictest of (system floor, this entry).
  final AclEntryStepUp? stepUp;

  /// Approve-authority: what this subject may **confer on others** by ratifying an
  /// approval, as distinct from `scopes`, which is what it may **exercise itself**. The
  /// two axes are independent, and that independence is the point — it is what lets a
  /// maintainer configure a least-privilege approver who can authorize an operation in a
  /// scope it has no authority to perform. OMISSION MEANS NOTHING IS CONFERRED. An
  /// absent `approve`, an absent `all`, and an empty `scopes` are all equivalent to
  /// "this subject may ratify nothing". A consumer that does not implement this member
  /// therefore confers less than the producer intended rather than more, which is the
  /// direction a missed member has to fail in. A subject with approve-authority is NOT
  /// thereby authorized to act. Consumers MUST resolve the two axes separately: reading
  /// `approve` to answer "may this party ratify X" and `scopes` to answer "may this
  /// party do X". Collapsing them grants an approver the ability to perform what it was
  /// only meant to sign off on.
  final AclEntryApprove? approve;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1. Reverse-DNS-namespaced;
  /// consumers MUST ignore unrecognized namespaces.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        'role': role,
        if (scopes != null) 'scopes': scopes!,
        if (allowedKeys != null) 'allowedKeys': allowedKeys!,
        if (label != null) 'label': label!,
        if (createdAt != null) 'createdAt': createdAt!,
        if (createdBy != null) 'createdBy': createdBy!,
        if (updatedAt != null) 'updatedAt': updatedAt!,
        if (updatedBy != null) 'updatedBy': updatedBy!,
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (stepUp != null) 'stepUp': stepUp!.toJson(),
        if (approve != null) 'approve': approve!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The success response to an acl/change-role request. Carried in a Trust Task
/// document whose type is https://trusttasks.org/spec/acl/change-role/0.1#response.
class Response {
  const Response({
    required this.entry,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        entry: AclEntry.fromJson(json['entry'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The AclEntry the maintainer now holds for the subject. entry.role MUST equal the
  /// request's payload.toRole.
  final AclEntry entry;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entry': entry.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// ACL Change Role — payload
class Payload {
  const Payload({
    required this.subject,
    required this.fromRole,
    required this.toRole,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subject: json['subject'] as String,
        fromRole: json['fromRole'] as String,
        toRole: json['toRole'] as String,
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// VID of the party whose role is changing.
  final String subject;

  /// The subject's prior role. Used by the maintainer for the optimistic concurrency
  /// check.
  final String fromRole;

  /// The role to transition the subject to.
  final String toRole;

  /// Optional human-readable rationale.
  final String? reason;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        'fromRole': fromRole,
        'toRole': toRole,
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/acl/change-role/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/acl/change-role/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/acl/change-role/0.1","title":"ACL Change Role — payload","type":"object","additionalProperties":false,"required":["subject","fromRole","toRole"],"properties":{"subject":{"type":"string","description":"VID of the party whose role is changing."},"fromRole":{"type":"string","minLength":1,"description":"The subject\'s prior role. Used by the maintainer for the optimistic concurrency check."},"toRole":{"type":"string","minLength":1,"description":"The role to transition the subject to."},"reason":{"type":"string","maxLength":1024,"description":"Optional human-readable rationale."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"ACL Change Role — response payload","description":"The success response to an acl/change-role request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/acl/change-role/0.1#response.","type":"object","additionalProperties":false,"required":["entry"],"properties":{"entry":{"description":"The AclEntry the maintainer now holds for the subject. entry.role MUST equal the request\'s payload.toRole.","\$ref":"#/\$defs/AclEntry"},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"AclEntry":{"title":"AclEntry","type":"object","additionalProperties":false,"required":["subject","role"],"properties":{"subject":{"type":"string","description":"VID of the party in the ACL. Compared by exact string equality (SPEC.md §4.8); producers SHOULD emit canonical form."},"role":{"type":"string","description":"Opaque role identifier interpreted by the ACL maintainer."},"scopes":{"type":"array","items":{"type":"string"},"description":"Opaque scope identifiers (e.g. contexts, domains, resource prefixes)."},"allowedKeys":{"type":["array","null"],"items":{"type":"string"},"description":"Key identifiers this subject may invoke the maintainer\'s signing oracle on. INTERSECTS WITH `scopes` — it can only narrow, never widen: a key named here that lies outside the entry\'s scopes remains unreachable, exactly as if it were not named. ABSENT means every key within the entry\'s scopes (the behaviour of entries that pre-date this member); explicit `null` is equivalent to absent, and producers SHOULD omit the member instead. PRESENT-BUT-EMPTY means authorized on NO keys — the opposite of absent, and deliberately so: emptiness is never a wildcard (CONVENTIONS.md §5). A consumer MUST preserve and enforce the absent-vs-empty distinction end to end; collapsing the two (e.g. by testing emptiness alone) re-creates the empty-means-unrestricted class of privilege-escalation defect this family\'s conventions exist to prevent."},"label":{"type":"string","maxLength":256,"description":"Optional human-readable label."},"createdAt":{"type":"string","format":"date-time"},"createdBy":{"type":"string","description":"VID of the party that originally added this entry."},"updatedAt":{"type":"string","format":"date-time"},"updatedBy":{"type":"string","description":"VID of the party that last modified this entry."},"expiresAt":{"type":"string","format":"date-time","description":"Optional time after which the entry is no longer effective."},"stepUp":{"type":"object","additionalProperties":false,"description":"Per-entry authentication step-up configuration, consumed by the ACL maintainer when it gates an operation behind a step-up (see auth/step-up/policy/0.1). ADDITIVE-ONLY: a per-entry setting MAY raise the assurance required of this subject above the maintainer\'s system-wide floor, but MUST NOT lower it. The maintainer resolves the effective requirement as the strictest of (system floor, this entry).","properties":{"approver":{"type":"string","description":"VID authorized to ratify step-up for this subject — the `recipient` the maintainer addresses an auth/step-up/approve-request to (e.g. the holder\'s mobile authenticator or browser companion). Absent → the subject is its own approver (mode `self`) when it holds a usable authenticator; if neither an `approver` nor a self authenticator exists, no step-up method is available for this subject and the maintainer\'s fail-closed rule applies."},"require":{"type":"string","enum":["self","delegated"],"description":"Minimum step-up mode this subject MUST satisfy for gated operations, raising the system floor. `self` = the subject re-authenticates its own session; `delegated` = a separate `approver` MUST ratify. Omitted → the system floor applies unchanged. A value weaker than the resolved floor is ignored (additive-only)."}}},"approve":{"type":"object","additionalProperties":false,"description":"Approve-authority: what this subject may **confer on others** by ratifying an approval, as distinct from `scopes`, which is what it may **exercise itself**. The two axes are independent, and that independence is the point — it is what lets a maintainer configure a least-privilege approver who can authorize an operation in a scope it has no authority to perform.\\n\\nOMISSION MEANS NOTHING IS CONFERRED. An absent `approve`, an absent `all`, and an empty `scopes` are all equivalent to \\"this subject may ratify nothing\\". A consumer that does not implement this member therefore confers less than the producer intended rather than more, which is the direction a missed member has to fail in.\\n\\nA subject with approve-authority is NOT thereby authorized to act. Consumers MUST resolve the two axes separately: reading `approve` to answer \\"may this party ratify X\\" and `scopes` to answer \\"may this party do X\\". Collapsing them grants an approver the ability to perform what it was only meant to sign off on.","properties":{"all":{"type":"boolean","default":false,"description":"The subject may confer ANY scope. Takes precedence over `scopes`, which a consumer MUST ignore when this is true. Absent or false → only the scopes listed below, if any."},"scopes":{"type":"array","items":{"type":"string"},"description":"Opaque scope identifiers this subject may confer, drawn from the same vocabulary as the entry\'s own `scopes`. Where a maintainer\'s scopes are hierarchical, conferring a scope confers its descendants — the same containment rule the maintainer already applies to `scopes`, so the two axes cannot disagree about what a scope means. An empty array confers nothing; it is not a wildcard."}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1. Reverse-DNS-namespaced; consumers MUST ignore unrecognized namespaces."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"ACL Change Role — response payload","description":"The success response to an acl/change-role request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/acl/change-role/0.1#response.","type":"object","additionalProperties":false,"required":["entry"],"properties":{"entry":{"description":"The AclEntry the maintainer now holds for the subject. entry.role MUST equal the request\'s payload.toRole.","\$ref":"#/\$defs/AclEntry"},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"AclEntry":{"title":"AclEntry","type":"object","additionalProperties":false,"required":["subject","role"],"properties":{"subject":{"type":"string","description":"VID of the party in the ACL. Compared by exact string equality (SPEC.md §4.8); producers SHOULD emit canonical form."},"role":{"type":"string","description":"Opaque role identifier interpreted by the ACL maintainer."},"scopes":{"type":"array","items":{"type":"string"},"description":"Opaque scope identifiers (e.g. contexts, domains, resource prefixes)."},"allowedKeys":{"type":["array","null"],"items":{"type":"string"},"description":"Key identifiers this subject may invoke the maintainer\'s signing oracle on. INTERSECTS WITH `scopes` — it can only narrow, never widen: a key named here that lies outside the entry\'s scopes remains unreachable, exactly as if it were not named. ABSENT means every key within the entry\'s scopes (the behaviour of entries that pre-date this member); explicit `null` is equivalent to absent, and producers SHOULD omit the member instead. PRESENT-BUT-EMPTY means authorized on NO keys — the opposite of absent, and deliberately so: emptiness is never a wildcard (CONVENTIONS.md §5). A consumer MUST preserve and enforce the absent-vs-empty distinction end to end; collapsing the two (e.g. by testing emptiness alone) re-creates the empty-means-unrestricted class of privilege-escalation defect this family\'s conventions exist to prevent."},"label":{"type":"string","maxLength":256,"description":"Optional human-readable label."},"createdAt":{"type":"string","format":"date-time"},"createdBy":{"type":"string","description":"VID of the party that originally added this entry."},"updatedAt":{"type":"string","format":"date-time"},"updatedBy":{"type":"string","description":"VID of the party that last modified this entry."},"expiresAt":{"type":"string","format":"date-time","description":"Optional time after which the entry is no longer effective."},"stepUp":{"type":"object","additionalProperties":false,"description":"Per-entry authentication step-up configuration, consumed by the ACL maintainer when it gates an operation behind a step-up (see auth/step-up/policy/0.1). ADDITIVE-ONLY: a per-entry setting MAY raise the assurance required of this subject above the maintainer\'s system-wide floor, but MUST NOT lower it. The maintainer resolves the effective requirement as the strictest of (system floor, this entry).","properties":{"approver":{"type":"string","description":"VID authorized to ratify step-up for this subject — the `recipient` the maintainer addresses an auth/step-up/approve-request to (e.g. the holder\'s mobile authenticator or browser companion). Absent → the subject is its own approver (mode `self`) when it holds a usable authenticator; if neither an `approver` nor a self authenticator exists, no step-up method is available for this subject and the maintainer\'s fail-closed rule applies."},"require":{"type":"string","enum":["self","delegated"],"description":"Minimum step-up mode this subject MUST satisfy for gated operations, raising the system floor. `self` = the subject re-authenticates its own session; `delegated` = a separate `approver` MUST ratify. Omitted → the system floor applies unchanged. A value weaker than the resolved floor is ignored (additive-only)."}}},"approve":{"type":"object","additionalProperties":false,"description":"Approve-authority: what this subject may **confer on others** by ratifying an approval, as distinct from `scopes`, which is what it may **exercise itself**. The two axes are independent, and that independence is the point — it is what lets a maintainer configure a least-privilege approver who can authorize an operation in a scope it has no authority to perform.\\n\\nOMISSION MEANS NOTHING IS CONFERRED. An absent `approve`, an absent `all`, and an empty `scopes` are all equivalent to \\"this subject may ratify nothing\\". A consumer that does not implement this member therefore confers less than the producer intended rather than more, which is the direction a missed member has to fail in.\\n\\nA subject with approve-authority is NOT thereby authorized to act. Consumers MUST resolve the two axes separately: reading `approve` to answer \\"may this party ratify X\\" and `scopes` to answer \\"may this party do X\\". Collapsing them grants an approver the ability to perform what it was only meant to sign off on.","properties":{"all":{"type":"boolean","default":false,"description":"The subject may confer ANY scope. Takes precedence over `scopes`, which a consumer MUST ignore when this is true. Absent or false → only the scopes listed below, if any."},"scopes":{"type":"array","items":{"type":"string"},"description":"Opaque scope identifiers this subject may confer, drawn from the same vocabulary as the entry\'s own `scopes`. Where a maintainer\'s scopes are hierarchical, conferring a scope confers its descendants — the same containment rule the maintainer already applies to `scopes`, so the two axes cannot disagree about what a scope means. An empty array confers nothing; it is not a wildcard."}}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1. Reverse-DNS-namespaced; consumers MUST ignore unrecognized namespaces."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
