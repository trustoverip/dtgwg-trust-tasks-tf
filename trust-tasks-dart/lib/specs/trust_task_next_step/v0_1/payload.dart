// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/trust-task-next-step/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// PayloadExpectsItem, generated from its schema.
class PayloadExpectsItem {
  const PayloadExpectsItem({
    required this.typeUri,
    this.reason,
    this.hint,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadExpectsItem.fromJson(Map<String, dynamic> json) =>
      PayloadExpectsItem(
        typeUri: json['typeUri'] as String,
        reason: json['reason'] as String?,
        hint: json['hint'] as Map<String, dynamic>?,
      );

  /// The Type URI of the Trust Task the recipient expects next, including any #request
  /// fragment. A suggestion only: it confers no authorization to perform that task, and
  /// the producer applies its own policy before acting (see the specification's Security
  /// & Privacy section).
  final String typeUri;

  /// Human-readable explanation of why this continuation is expected. Non-normative;
  /// intended for operator UI and logs.
  final String? reason;

  /// Optional structured data the producer MAY use when composing the expected document
  /// — a challenge to echo, an identifier to quote, a presentation definition to
  /// satisfy. Its shape is governed by the specification named in typeUri, not by this
  /// one. A producer MUST NOT treat a hint as authoritative for any value it can
  /// determine itself.
  final Map<String, dynamic>? hint;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'typeUri': typeUri,
        if (reason != null) 'reason': reason!,
        if (hint != null) 'hint': hint!,
      };
}

/// Identifies the Trust Task document this next step answers. SHOULD be populated, for
/// the reason SPEC §8.2 gives for the identical member on an error response: threadId
/// correlates the exchange only for a party that already saw the request, so a
/// retained next step otherwise names neither the task it interrupted nor the
/// instance.
class PayloadInResponseTo {
  const PayloadInResponseTo({
    required this.typeUri,
    this.id,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadInResponseTo.fromJson(Map<String, dynamic> json) =>
      PayloadInResponseTo(
        typeUri: json['typeUri'] as String,
        id: json['id'] as String?,
      );

  /// The Type URI of the document being answered, including any fragment it carried.
  final String typeUri;

  /// The id of the specific document being answered (SPEC §4.3). Globally unique and
  /// never reused, so it names one instance where threadId names an exchange.
  final String? id;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'typeUri': typeUri,
        if (id != null) 'id': id!,
      };
}

/// The recipient-suggested continuation reserved at SPEC.md §8.6: the original task
/// was understood, but cannot complete in isolation, and this names what the recipient
/// party expects in order to proceed. A next step is neither a success response nor a
/// failure. The originating task is left open — a consumer that means 'no' returns a
/// trust-task-error instead, and one that means 'done' returns the originating
/// specification's #response variant. This specification declares no response anchor:
/// a producer answers a next step by issuing a document of the expected type, not by
/// responding to this one.
class Payload {
  const Payload({
    required this.expects,
    this.continuation,
    this.inResponseTo,
    this.message,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        expects: (json['expects'] as List<dynamic>)
            .map((e) => PayloadExpectsItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        continuation: json['continuation'],
        inResponseTo: json['inResponseTo'] == null
            ? null
            : PayloadInResponseTo.fromJson(
                json['inResponseTo'] as Map<String, dynamic>),
        message: json['message'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The continuations the recipient will accept, in the producer's order of preference
  /// where the recipient has one. More than one entry means ALTERNATIVES — satisfying
  /// any single entry unblocks the exchange, never all of them. A recipient that needs
  /// several things done first names the one it wants next and issues a further next
  /// step afterwards; expressing a conjunction here would be a flow definition, which
  /// belongs to a ceremony rather than to a single response.
  final List<PayloadExpectsItem> expects;

  /// What happens once an expected task completes. `resubmit` (the default) means the
  /// expected task is a PREREQUISITE and the producer re-issues the originating request
  /// to continue — a new document with a fresh id and the same threadId, which SPEC §8.4
  /// distinguishes from a retry, since a retry is bit-for-bit identical and is not what
  /// happens here; this matches the established pattern of task-consent/granted, where
  /// the requester re-submits once approval lands. `proceed` means the expected task IS
  /// the continuation and the originating request is not re-issued, as in an offer
  /// answered by a request.
  final Object? continuation;

  /// Identifies the Trust Task document this next step answers. SHOULD be populated, for
  /// the reason SPEC §8.2 gives for the identical member on an error response: threadId
  /// correlates the exchange only for a party that already saw the request, so a
  /// retained next step otherwise names neither the task it interrupted nor the
  /// instance.
  final PayloadInResponseTo? inResponseTo;

  /// Human-readable description of why the task cannot complete in isolation.
  /// Non-normative. Subject to the same restraint as an error message: it reaches a
  /// party that may not be entitled to learn why.
  final String? message;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'expects': expects.map((e) => e.toJson()).toList(),
        if (continuation != null) 'continuation': continuation!,
        if (inResponseTo != null) 'inResponseTo': inResponseTo!.toJson(),
        if (message != null) 'message': message!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/trust-task-next-step/0.1';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/trust-task-next-step/0.1","title":"Trust Task Next Step — payload","description":"The recipient-suggested continuation reserved at SPEC.md §8.6: the original task was understood, but cannot complete in isolation, and this names what the recipient party expects in order to proceed.\\n\\nA next step is neither a success response nor a failure. The originating task is left open — a consumer that means \'no\' returns a trust-task-error instead, and one that means \'done\' returns the originating specification\'s #response variant.\\n\\nThis specification declares no response anchor: a producer answers a next step by issuing a document of the expected type, not by responding to this one.","type":"object","additionalProperties":false,"required":["expects"],"properties":{"expects":{"type":"array","minItems":1,"description":"The continuations the recipient will accept, in the producer\'s order of preference where the recipient has one. More than one entry means ALTERNATIVES — satisfying any single entry unblocks the exchange, never all of them. A recipient that needs several things done first names the one it wants next and issues a further next step afterwards; expressing a conjunction here would be a flow definition, which belongs to a ceremony rather than to a single response.","items":{"type":"object","additionalProperties":false,"required":["typeUri"],"properties":{"typeUri":{"type":"string","format":"uri","minLength":1,"description":"The Type URI of the Trust Task the recipient expects next, including any #request fragment. A suggestion only: it confers no authorization to perform that task, and the producer applies its own policy before acting (see the specification\'s Security & Privacy section)."},"reason":{"type":"string","maxLength":1024,"description":"Human-readable explanation of why this continuation is expected. Non-normative; intended for operator UI and logs."},"hint":{"type":"object","description":"Optional structured data the producer MAY use when composing the expected document — a challenge to echo, an identifier to quote, a presentation definition to satisfy. Its shape is governed by the specification named in typeUri, not by this one. A producer MUST NOT treat a hint as authoritative for any value it can determine itself."}}}},"continuation":{"enum":["resubmit","proceed"],"default":"resubmit","description":"What happens once an expected task completes. `resubmit` (the default) means the expected task is a PREREQUISITE and the producer re-issues the originating request to continue — a new document with a fresh id and the same threadId, which SPEC §8.4 distinguishes from a retry, since a retry is bit-for-bit identical and is not what happens here; this matches the established pattern of task-consent/granted, where the requester re-submits once approval lands. `proceed` means the expected task IS the continuation and the originating request is not re-issued, as in an offer answered by a request.","\$comment":"Two values rather than a boolean, because \'resubmit: false\' reads as a negation of something the reader has to reconstruct."},"inResponseTo":{"type":"object","additionalProperties":false,"required":["typeUri"],"description":"Identifies the Trust Task document this next step answers. SHOULD be populated, for the reason SPEC §8.2 gives for the identical member on an error response: threadId correlates the exchange only for a party that already saw the request, so a retained next step otherwise names neither the task it interrupted nor the instance.","properties":{"typeUri":{"type":"string","format":"uri","minLength":1,"description":"The Type URI of the document being answered, including any fragment it carried."},"id":{"type":"string","minLength":1,"description":"The id of the specific document being answered (SPEC §4.3). Globally unique and never reused, so it names one instance where threadId names an exchange."}}},"message":{"type":"string","maxLength":1024,"description":"Human-readable description of why the task cannot complete in isolation. Non-normative. Subject to the same restraint as an error message: it reaches a party that may not be entitled to learn why."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);
