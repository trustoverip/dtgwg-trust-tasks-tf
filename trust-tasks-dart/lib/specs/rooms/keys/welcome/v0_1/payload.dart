// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/rooms/keys/welcome/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to rooms/keys/welcome. Type
/// https://trusttasks.org/spec/rooms/keys/welcome/0.1#response.
class Response {
  const Response({
    required this.epoch,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        epoch: json['epoch'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The MLS epoch the recipient joined at. The sender needs this to know which commits
  /// the recipient still lacks.
  final int epoch;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'epoch': epoch,
        if (ext != null) 'ext': ext!,
      };
}

/// A room's owner delivers an MLS Welcome to the agent that will hold a new member's
/// room keys, so it can join the group. The Welcome carries the group's secrets sealed
/// to the recipient's KeyPackage; a recipient that accepts one it was not invited to
/// has been handed key material it should have refused.
class Payload {
  const Payload({
    required this.roomId,
    required this.welcome,
    this.ratchetTree,
    this.invitation,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        roomId: json['roomId'] as String,
        welcome: json['welcome'] as String,
        ratchetTree: json['ratchetTree'] as String?,
        invitation: json['invitation'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The room being joined.
  final String roomId;

  /// The MLS Welcome message, base64url.
  final String welcome;

  /// The group's ratchet tree, base64url, where the Welcome does not carry it as an
  /// extension. A joiner needs the tree to derive the group's secrets; omit it only
  /// where the group uses the ratchet-tree extension.
  final String? ratchetTree;

  /// The invitation credential (VIC) the room issued to the joining party, serialized
  /// per the governing profile. This is what makes the Welcome acceptable: joining is a
  /// two-party act, and a recipient with no matching invitation MUST refuse.
  final String? invitation;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'roomId': roomId,
        'welcome': welcome,
        if (ratchetTree != null) 'ratchetTree': ratchetTree!,
        if (invitation != null) 'invitation': invitation!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/rooms/keys/welcome/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/rooms/keys/welcome/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/rooms/keys/welcome/0.1","title":"Rooms Keys Welcome — payload","description":"A room\'s owner delivers an MLS Welcome to the agent that will hold a new member\'s room keys, so it can join the group. The Welcome carries the group\'s secrets sealed to the recipient\'s KeyPackage; a recipient that accepts one it was not invited to has been handed key material it should have refused.","type":"object","additionalProperties":false,"required":["roomId","welcome"],"properties":{"roomId":{"type":"string","description":"The room being joined."},"welcome":{"type":"string","description":"The MLS Welcome message, base64url."},"ratchetTree":{"type":"string","description":"The group\'s ratchet tree, base64url, where the Welcome does not carry it as an extension. A joiner needs the tree to derive the group\'s secrets; omit it only where the group uses the ratchet-tree extension."},"invitation":{"type":"string","description":"The invitation credential (VIC) the room issued to the joining party, serialized per the governing profile. This is what makes the Welcome acceptable: joining is a two-party act, and a recipient with no matching invitation MUST refuse."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Rooms Keys Welcome — response payload","description":"Success response to rooms/keys/welcome. Type https://trusttasks.org/spec/rooms/keys/welcome/0.1#response.","type":"object","additionalProperties":false,"required":["epoch"],"properties":{"epoch":{"type":"integer","minimum":0,"description":"The MLS epoch the recipient joined at. The sender needs this to know which commits the recipient still lacks."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Rooms Keys Welcome — response payload","description":"Success response to rooms/keys/welcome. Type https://trusttasks.org/spec/rooms/keys/welcome/0.1#response.","type":"object","additionalProperties":false,"required":["epoch"],"properties":{"epoch":{"type":"integer","minimum":0,"description":"The MLS epoch the recipient joined at. The sender needs this to know which commits the recipient still lacks."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
