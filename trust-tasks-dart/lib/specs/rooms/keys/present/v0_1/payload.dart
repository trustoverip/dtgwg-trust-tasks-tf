// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/rooms/keys/present/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to rooms/keys/present. Type
/// https://trusttasks.org/spec/rooms/keys/present/0.1#response.
class Response {
  const Response({
    required this.presentation,
    this.expiresAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        presentation: json['presentation'] as Map<String, dynamic>,
        expiresAt: json['expiresAt'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The presentation to send to the host: a membership credential, an authority chain,
  /// and — where the room withholds the subject — a same-subject binding.
  final Map<String, dynamic> presentation;

  /// When the presentation stops being accepted, so a caller can avoid presenting a
  /// stale one.
  final String? expiresAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'presentation': presentation,
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (ext != null) 'ext': ext!,
      };
}

/// The action the presentation must confer. Named here so the produced presentation
/// can be scoped to it: a presentation minted for `read` should not be reusable for
/// `write`, and a holder that mints one covering everything has handed the agent its
/// own standing.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadAction(String value) {
  static const PayloadAction read = PayloadAction('read');
  static const PayloadAction write = PayloadAction('write');
  static const PayloadAction curate = PayloadAction('curate');
  static const PayloadAction admin = PayloadAction('admin');

  /// Every value this specification's schema permits.
  static const List<PayloadAction> values = <PayloadAction>[
    read,
    write,
    curate,
    admin
  ];
}

/// An agent asks the party holding its principal's room credentials to produce a
/// presentation for one room operation. The credentials never cross to the agent; only
/// the presentation does, and it is bound to the operation it was asked for.
class Payload {
  const Payload({
    required this.roomId,
    required this.action,
    this.audience,
    this.nonce,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        roomId: json['roomId'] as String,
        action: PayloadAction(json['action'] as String),
        audience: json['audience'] as String?,
        nonce: json['nonce'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The room to present for.
  final String roomId;

  /// The action the presentation must confer. Named here so the produced presentation
  /// can be scoped to it: a presentation minted for `read` should not be reusable for
  /// `write`, and a holder that mints one covering everything has handed the agent its
  /// own standing.
  final PayloadAction action;

  /// The party the presentation is for, so it cannot be replayed elsewhere. A host's
  /// identifier, normally.
  final String? audience;

  /// Freshness value from the verifier, echoed into the presentation. Without one a
  /// captured presentation is replayable for as long as its credentials live.
  final String? nonce;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'roomId': roomId,
        'action': action.value,
        if (audience != null) 'audience': audience!,
        if (nonce != null) 'nonce': nonce!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/rooms/keys/present/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/rooms/keys/present/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/rooms/keys/present/0.1","title":"Rooms Keys Present — payload","description":"An agent asks the party holding its principal\'s room credentials to produce a presentation for one room operation. The credentials never cross to the agent; only the presentation does, and it is bound to the operation it was asked for.","type":"object","additionalProperties":false,"required":["roomId","action"],"properties":{"roomId":{"type":"string","description":"The room to present for."},"action":{"type":"string","enum":["read","write","curate","admin"],"description":"The action the presentation must confer. Named here so the produced presentation can be scoped to it: a presentation minted for `read` should not be reusable for `write`, and a holder that mints one covering everything has handed the agent its own standing."},"audience":{"type":"string","description":"The party the presentation is for, so it cannot be replayed elsewhere. A host\'s identifier, normally."},"nonce":{"type":"string","maxLength":512,"description":"Freshness value from the verifier, echoed into the presentation. Without one a captured presentation is replayable for as long as its credentials live."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Rooms Keys Present — response payload","description":"Success response to rooms/keys/present. Type https://trusttasks.org/spec/rooms/keys/present/0.1#response.","type":"object","additionalProperties":false,"required":["presentation"],"properties":{"presentation":{"type":"object","additionalProperties":true,"description":"The presentation to send to the host: a membership credential, an authority chain, and — where the room withholds the subject — a same-subject binding."},"expiresAt":{"type":"string","format":"date-time","description":"When the presentation stops being accepted, so a caller can avoid presenting a stale one."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Rooms Keys Present — response payload","description":"Success response to rooms/keys/present. Type https://trusttasks.org/spec/rooms/keys/present/0.1#response.","type":"object","additionalProperties":false,"required":["presentation"],"properties":{"presentation":{"type":"object","additionalProperties":true,"description":"The presentation to send to the host: a membership credential, an authority chain, and — where the room withholds the subject — a same-subject binding."},"expiresAt":{"type":"string","format":"date-time","description":"When the presentation stops being accepted, so a caller can avoid presenting a stale one."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
