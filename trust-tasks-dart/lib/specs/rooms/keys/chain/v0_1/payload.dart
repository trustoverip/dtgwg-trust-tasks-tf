// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/rooms/keys/chain/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to rooms/keys/chain. Type
/// https://trusttasks.org/spec/rooms/keys/chain/0.1#response.
class Response {
  const Response({
    required this.roomId,
    required this.earliestReadableEpoch,
    this.stored,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        roomId: json['roomId'] as String,
        earliestReadableEpoch: json['earliestReadableEpoch'] as int,
        stored: json['stored'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String roomId;

  /// The earliest epoch the recipient can now derive a key for, having walked the chain
  /// it holds. **This is the answer worth having**, and it is not a restatement of what
  /// was sent: a rung only extends reach if every rung above it is present too, so a
  /// caller that supplied a set with a gap in it learns that here rather than at the
  /// first record that will not open. `1` means the room's whole history is reachable.
  final int earliestReadableEpoch;

  /// How many rungs the recipient did not already hold. Zero is a success, not a no-op:
  /// it means the caller's chain was already delivered, which is what a retry looks
  /// like.
  final int? stored;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'roomId': roomId,
        'earliestReadableEpoch': earliestReadableEpoch,
        if (stored != null) 'stored': stored!,
        if (ext != null) 'ext': ext!,
      };
}

/// One rung of a room's epoch key chain: the storage key of epoch `epoch - 1`, sealed
/// under the storage key of `epoch`. A group key schedule offers no way to derive an
/// earlier epoch's key from a later one — that property is what makes removing a
/// member mean something — so without a chain the first membership change makes every
/// record already in the room unopenable by everyone, including whoever wrote it. The
/// chain is the one-way street run deliberately the other way: a member holding the
/// current key walks it backwards to any retained epoch, and a member holding an
/// earlier key still derives nothing later. Removal stays forward-only; reading stays
/// possible. What a chain costs is stated where it is chosen, in the room's retention
/// policy.
class EpochLink {
  const EpochLink({
    required this.epoch,
    required this.wrapped,
    required this.nonce,
  });

  /// Read this payload from a decoded JSON object.
  factory EpochLink.fromJson(Map<String, dynamic> json) => EpochLink(
        epoch: json['epoch'] as int,
        wrapped: json['wrapped'] as String,
        nonce: json['nonce'] as String,
      );

  /// The epoch whose storage key opens this link; it wraps the storage key of `epoch -
  /// 1`. Never 1: a room's first epoch has no predecessor, so a link claiming one wraps
  /// something that is not an earlier epoch's key.
  final int epoch;

  /// The wrapped predecessor key, base64url. Bound by AEAD associated data to `roomId`
  /// and to this link's own position in the chain, so a link lifted to another rung, or
  /// served under another room, fails to open rather than yielding a key that is wrong.
  /// The binding is load-bearing rather than decorative: every rung is a fixed-length
  /// key sealed under a fixed-length key, so nothing about the ciphertext itself says
  /// where it belongs.
  final String wrapped;

  /// AEAD nonce, base64url.
  final String nonce;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'epoch': epoch,
        'wrapped': wrapped,
        'nonce': nonce,
      };
}

/// Rooms Keys Chain — payload
class Payload {
  const Payload({
    required this.roomId,
    required this.links,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        roomId: json['roomId'] as String,
        links: (json['links'] as List<dynamic>)
            .map((e) => EpochLink.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The room whose chain these rungs belong to. The recipient MUST hold group state for
  /// it, and MUST NOT create any on the strength of this request: rungs are inert
  /// without an epoch key, so accepting them for a room one is not in would retain key
  /// material for nothing.
  final String roomId;

  /// The rungs, in any order. A recipient stores them and reports how far back it can
  /// now reach — see the response's `earliestReadableEpoch`.
  final List<EpochLink> links;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'roomId': roomId,
        'links': links.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/rooms/keys/chain/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/rooms/keys/chain/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/rooms/keys/chain/0.1","title":"Rooms Keys Chain — payload","type":"object","additionalProperties":false,"required":["roomId","links"],"properties":{"roomId":{"type":"string","description":"The room whose chain these rungs belong to. The recipient MUST hold group state for it, and MUST NOT create any on the strength of this request: rungs are inert without an epoch key, so accepting them for a room one is not in would retain key material for nothing."},"links":{"type":"array","minItems":1,"description":"The rungs, in any order. A recipient stores them and reports how far back it can now reach — see the response\'s `earliestReadableEpoch`.","items":{"\$ref":"#/\$defs/EpochLink"}},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Rooms Keys Chain — response payload","description":"Success response to rooms/keys/chain. Type https://trusttasks.org/spec/rooms/keys/chain/0.1#response.","type":"object","additionalProperties":false,"required":["roomId","earliestReadableEpoch"],"properties":{"roomId":{"type":"string"},"earliestReadableEpoch":{"type":"integer","minimum":1,"description":"The earliest epoch the recipient can now derive a key for, having walked the chain it holds. **This is the answer worth having**, and it is not a restatement of what was sent: a rung only extends reach if every rung above it is present too, so a caller that supplied a set with a gap in it learns that here rather than at the first record that will not open. `1` means the room\'s whole history is reachable."},"stored":{"type":"integer","minimum":0,"description":"How many rungs the recipient did not already hold. Zero is a success, not a no-op: it means the caller\'s chain was already delivered, which is what a retry looks like."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"EpochLink":{"title":"EpochLink","type":"object","additionalProperties":false,"required":["epoch","wrapped","nonce"],"description":"One rung of a room\'s epoch key chain: the storage key of epoch `epoch - 1`, sealed under the storage key of `epoch`. A group key schedule offers no way to derive an earlier epoch\'s key from a later one — that property is what makes removing a member mean something — so without a chain the first membership change makes every record already in the room unopenable by everyone, including whoever wrote it. The chain is the one-way street run deliberately the other way: a member holding the current key walks it backwards to any retained epoch, and a member holding an earlier key still derives nothing later. Removal stays forward-only; reading stays possible. What a chain costs is stated where it is chosen, in the room\'s retention policy.","properties":{"epoch":{"type":"integer","minimum":2,"description":"The epoch whose storage key opens this link; it wraps the storage key of `epoch - 1`. Never 1: a room\'s first epoch has no predecessor, so a link claiming one wraps something that is not an earlier epoch\'s key."},"wrapped":{"type":"string","description":"The wrapped predecessor key, base64url. Bound by AEAD associated data to `roomId` and to this link\'s own position in the chain, so a link lifted to another rung, or served under another room, fails to open rather than yielding a key that is wrong. The binding is load-bearing rather than decorative: every rung is a fixed-length key sealed under a fixed-length key, so nothing about the ciphertext itself says where it belongs."},"nonce":{"type":"string","description":"AEAD nonce, base64url."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Rooms Keys Chain — response payload","description":"Success response to rooms/keys/chain. Type https://trusttasks.org/spec/rooms/keys/chain/0.1#response.","type":"object","additionalProperties":false,"required":["roomId","earliestReadableEpoch"],"properties":{"roomId":{"type":"string"},"earliestReadableEpoch":{"type":"integer","minimum":1,"description":"The earliest epoch the recipient can now derive a key for, having walked the chain it holds. **This is the answer worth having**, and it is not a restatement of what was sent: a rung only extends reach if every rung above it is present too, so a caller that supplied a set with a gap in it learns that here rather than at the first record that will not open. `1` means the room\'s whole history is reachable."},"stored":{"type":"integer","minimum":0,"description":"How many rungs the recipient did not already hold. Zero is a success, not a no-op: it means the caller\'s chain was already delivered, which is what a retry looks like."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"EpochLink":{"title":"EpochLink","type":"object","additionalProperties":false,"required":["epoch","wrapped","nonce"],"description":"One rung of a room\'s epoch key chain: the storage key of epoch `epoch - 1`, sealed under the storage key of `epoch`. A group key schedule offers no way to derive an earlier epoch\'s key from a later one — that property is what makes removing a member mean something — so without a chain the first membership change makes every record already in the room unopenable by everyone, including whoever wrote it. The chain is the one-way street run deliberately the other way: a member holding the current key walks it backwards to any retained epoch, and a member holding an earlier key still derives nothing later. Removal stays forward-only; reading stays possible. What a chain costs is stated where it is chosen, in the room\'s retention policy.","properties":{"epoch":{"type":"integer","minimum":2,"description":"The epoch whose storage key opens this link; it wraps the storage key of `epoch - 1`. Never 1: a room\'s first epoch has no predecessor, so a link claiming one wraps something that is not an earlier epoch\'s key."},"wrapped":{"type":"string","description":"The wrapped predecessor key, base64url. Bound by AEAD associated data to `roomId` and to this link\'s own position in the chain, so a link lifted to another rung, or served under another room, fails to open rather than yielding a key that is wrong. The binding is load-bearing rather than decorative: every rung is a fixed-length key sealed under a fixed-length key, so nothing about the ciphertext itself says where it belongs."},"nonce":{"type":"string","description":"AEAD nonce, base64url."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
