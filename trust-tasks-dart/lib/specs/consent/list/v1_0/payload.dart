// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/consent/list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The interaction kind: a 1:1 direct message, a multi-party group, or a broadcast
/// channel.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Kind(String value) {
  static const Kind dm = Kind('dm');
  static const Kind group = Kind('group');
  static const Kind channel = Kind('channel');

  /// Every value this specification's schema permits.
  static const List<Kind> values = <Kind>[dm, group, channel];
}

/// The platform-agnostic identifier of WHAT is being consented to: one conversation,
/// for one agent.
class ConsentSubject {
  const ConsentSubject({
    required this.platform,
    required this.conversationRef,
    required this.kind,
    required this.agent,
  });

  /// Read this payload from a decoded JSON object.
  factory ConsentSubject.fromJson(Map<String, dynamic> json) => ConsentSubject(
        platform: json['platform'] as String,
        conversationRef: json['conversationRef'] as String,
        kind: Kind(json['kind'] as String),
        agent: json['agent'] as String,
      );

  /// Messaging-platform tag, e.g. "signal", "whatsapp", "slack".
  final String platform;

  /// The bridge's OPAQUE conversation handle (e.g. "sig-1a2b3c4d"). NEVER the raw
  /// platform address — the VTA never learns the phone number / chat id.
  final String conversationRef;
  final Kind kind;

  /// VID (DID) of the AI agent the conversation would reach.
  final String agent;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'platform': platform,
        'conversationRef': conversationRef,
        'kind': kind.value,
        'agent': agent,
      };
}

/// Whether the subject is permitted. The ABSENCE of any grant is treated as `deny`
/// (default-deny).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Effect(String value) {
  static const Effect allow = Effect('allow');
  static const Effect deny = Effect('deny');

  /// Every value this specification's schema permits.
  static const List<Effect> values = <Effect>[allow, deny];
}

/// What the agent may do: `receive` = read inbound on this conversation; `converse` =
/// read and reply.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Scope(String value) {
  static const Scope receive = Scope('receive');
  static const Scope converse = Scope('converse');

  /// Every value this specification's schema permits.
  static const List<Scope> values = <Scope>[receive, converse];
}

/// A recorded consent decision over a ConsentSubject.
class ConsentGrant {
  const ConsentGrant({
    required this.subject,
    required this.effect,
    this.scope,
    required this.grantedBy,
    required this.grantedAt,
    this.expiresAt,
    this.evidence,
  });

  /// Read this payload from a decoded JSON object.
  factory ConsentGrant.fromJson(Map<String, dynamic> json) => ConsentGrant(
        subject:
            ConsentSubject.fromJson(json['subject'] as Map<String, dynamic>),
        effect: Effect(json['effect'] as String),
        scope: json['scope'] == null ? null : Scope(json['scope'] as String),
        grantedBy: json['grantedBy'] as String,
        grantedAt: json['grantedAt'] as String,
        expiresAt: json['expiresAt'] as String?,
        evidence: json['evidence'] as String?,
      );

  final ConsentSubject subject;
  final Effect effect;

  /// Granted scope. REQUIRED when `effect` is `allow`; absent for `deny`.
  final Scope? scope;

  /// VID of the approver who made the decision.
  final String grantedBy;

  /// RFC 3339 time the decision was recorded.
  final String grantedAt;

  /// Optional. After this time the grant lapses and the subject must be re-consented.
  final String? expiresAt;

  /// How the decision was authorized — e.g. "did-signed" (approver's DID signed the
  /// consent/decision) or "bridge-attested" (an enrolled bridge relayed the operator's
  /// out-of-band choice).
  final String? evidence;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject.toJson(),
        'effect': effect.value,
        if (scope != null) 'scope': scope!.value,
        'grantedBy': grantedBy,
        'grantedAt': grantedAt,
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (evidence != null) 'evidence': evidence!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The grants the requesting bridge is authorized to enforce.
class Response {
  const Response({
    required this.grants,
    this.cursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        grants: (json['grants'] as List<dynamic>)
            .map((e) => ConsentGrant.fromJson(e as Map<String, dynamic>))
            .toList(),
        cursor: json['cursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Matching consent grants.
  final List<ConsentGrant> grants;

  /// Optional cursor for incremental follow-up.
  final String? cursor;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'grants': grants.map((e) => e.toJson()).toList(),
        if (cursor != null) 'cursor': cursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// A bridge fetches the consent grants it should enforce.
class Payload {
  const Payload({
    this.agent,
    this.platform,
    this.subject,
    this.since,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        agent: json['agent'] as String?,
        platform: json['platform'] as String?,
        subject: json['subject'] == null
            ? null
            : ConsentSubject.fromJson(json['subject'] as Map<String, dynamic>),
        since: json['since'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Optional. Restrict to grants for this agent VID.
  final String? agent;

  /// Optional. Restrict to this platform tag.
  final String? platform;

  /// Optional. A full subject, to point-check a single conversation.
  final ConsentSubject? subject;

  /// Optional opaque cursor; return only grants changed after it.
  final String? since;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (agent != null) 'agent': agent!,
        if (platform != null) 'platform': platform!,
        if (subject != null) 'subject': subject!.toJson(),
        if (since != null) 'since': since!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/consent/list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/consent/list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/consent/list/1.0","title":"Consent List — payload","description":"A bridge fetches the consent grants it should enforce.","type":"object","additionalProperties":false,"properties":{"agent":{"type":"string","minLength":1,"description":"Optional. Restrict to grants for this agent VID."},"platform":{"type":"string","minLength":1,"description":"Optional. Restrict to this platform tag."},"subject":{"\$ref":"#/\$defs/ConsentSubject","description":"Optional. A full subject, to point-check a single conversation."},"since":{"type":"string","description":"Optional opaque cursor; return only grants changed after it."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Consent List — response payload","description":"The grants the requesting bridge is authorized to enforce.","type":"object","additionalProperties":false,"required":["grants"],"properties":{"grants":{"type":"array","items":{"\$ref":"#/\$defs/ConsentGrant"},"description":"Matching consent grants."},"cursor":{"type":"string","description":"Optional cursor for incremental follow-up."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConsentGrant":{"type":"object","additionalProperties":false,"required":["subject","effect","grantedBy","grantedAt"],"description":"A recorded consent decision over a ConsentSubject.","properties":{"subject":{"\$ref":"#/\$defs/ConsentSubject"},"effect":{"\$ref":"#/\$defs/Effect"},"scope":{"\$ref":"#/\$defs/Scope","description":"Granted scope. REQUIRED when `effect` is `allow`; absent for `deny`."},"grantedBy":{"type":"string","minLength":1,"description":"VID of the approver who made the decision."},"grantedAt":{"type":"string","format":"date-time","description":"RFC 3339 time the decision was recorded."},"expiresAt":{"type":"string","format":"date-time","description":"Optional. After this time the grant lapses and the subject must be re-consented."},"evidence":{"type":"string","description":"How the decision was authorized — e.g. \\"did-signed\\" (approver\'s DID signed the consent/decision) or \\"bridge-attested\\" (an enrolled bridge relayed the operator\'s out-of-band choice)."}}},"Scope":{"type":"string","enum":["receive","converse"],"description":"What the agent may do: `receive` = read inbound on this conversation; `converse` = read and reply."},"Effect":{"type":"string","enum":["allow","deny"],"description":"Whether the subject is permitted. The ABSENCE of any grant is treated as `deny` (default-deny)."},"ConsentSubject":{"type":"object","additionalProperties":false,"required":["platform","conversationRef","kind","agent"],"description":"The platform-agnostic identifier of WHAT is being consented to: one conversation, for one agent.","properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\", \\"whatsapp\\", \\"slack\\"."},"conversationRef":{"type":"string","minLength":1,"description":"The bridge\'s OPAQUE conversation handle (e.g. \\"sig-1a2b3c4d\\"). NEVER the raw platform address — the VTA never learns the phone number / chat id."},"kind":{"\$ref":"#/\$defs/Kind"},"agent":{"type":"string","minLength":1,"description":"VID (DID) of the AI agent the conversation would reach."}}},"Kind":{"type":"string","enum":["dm","group","channel"],"description":"The interaction kind: a 1:1 direct message, a multi-party group, or a broadcast channel."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Consent List — response payload","description":"The grants the requesting bridge is authorized to enforce.","type":"object","additionalProperties":false,"required":["grants"],"properties":{"grants":{"type":"array","items":{"\$ref":"#/\$defs/ConsentGrant"},"description":"Matching consent grants."},"cursor":{"type":"string","description":"Optional cursor for incremental follow-up."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConsentGrant":{"type":"object","additionalProperties":false,"required":["subject","effect","grantedBy","grantedAt"],"description":"A recorded consent decision over a ConsentSubject.","properties":{"subject":{"\$ref":"#/\$defs/ConsentSubject"},"effect":{"\$ref":"#/\$defs/Effect"},"scope":{"\$ref":"#/\$defs/Scope","description":"Granted scope. REQUIRED when `effect` is `allow`; absent for `deny`."},"grantedBy":{"type":"string","minLength":1,"description":"VID of the approver who made the decision."},"grantedAt":{"type":"string","format":"date-time","description":"RFC 3339 time the decision was recorded."},"expiresAt":{"type":"string","format":"date-time","description":"Optional. After this time the grant lapses and the subject must be re-consented."},"evidence":{"type":"string","description":"How the decision was authorized — e.g. \\"did-signed\\" (approver\'s DID signed the consent/decision) or \\"bridge-attested\\" (an enrolled bridge relayed the operator\'s out-of-band choice)."}}},"Scope":{"type":"string","enum":["receive","converse"],"description":"What the agent may do: `receive` = read inbound on this conversation; `converse` = read and reply."},"Effect":{"type":"string","enum":["allow","deny"],"description":"Whether the subject is permitted. The ABSENCE of any grant is treated as `deny` (default-deny)."},"ConsentSubject":{"type":"object","additionalProperties":false,"required":["platform","conversationRef","kind","agent"],"description":"The platform-agnostic identifier of WHAT is being consented to: one conversation, for one agent.","properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\", \\"whatsapp\\", \\"slack\\"."},"conversationRef":{"type":"string","minLength":1,"description":"The bridge\'s OPAQUE conversation handle (e.g. \\"sig-1a2b3c4d\\"). NEVER the raw platform address — the VTA never learns the phone number / chat id."},"kind":{"\$ref":"#/\$defs/Kind"},"agent":{"type":"string","minLength":1,"description":"VID (DID) of the AI agent the conversation would reach."}}},"Kind":{"type":"string","enum":["dm","group","channel"],"description":"The interaction kind: a 1:1 direct message, a multi-party group, or a broadcast channel."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
