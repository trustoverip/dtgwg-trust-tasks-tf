// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/consent/approver-list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// How a consent prompt reaches the approver: `wake` pushes to the approver's device
/// for a DID-signed decision; `bridge-relay` renders it through an enrolled bridge
/// (e.g. a numbered card in the operator's messaging app) for a bridge-attested
/// decision.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Route(String value) {
  static const Route wake = Route('wake');
  static const Route bridgeRelay = Route('bridge-relay');

  /// Every value this specification's schema permits.
  static const List<Route> values = <Route>[wake, bridgeRelay];
}

/// Who approves inbound-messaging consent for a given platform within a VTA context,
/// and how the prompt reaches them.
class ApproverBinding {
  const ApproverBinding({
    required this.platform,
    required this.context,
    required this.approver,
    this.route,
    this.routeHint,
  });

  /// Read this payload from a decoded JSON object.
  factory ApproverBinding.fromJson(Map<String, dynamic> json) =>
      ApproverBinding(
        platform: json['platform'] as String,
        context: json['context'] as String,
        approver: json['approver'] as String,
        route: json['route'] == null ? null : Route(json['route'] as String),
        routeHint: json['routeHint'] as String?,
      );

  /// Messaging-platform tag, e.g. "signal", "whatsapp", "slack".
  final String platform;

  /// The VTA context path this binding applies to.
  final String context;

  /// VID (DID) of the operator authorized to decide consent for this platform/context.
  final String approver;

  /// Optional. How to deliver the prompt; defaults to bridge-relay when omitted.
  final Route? route;

  /// Optional routing detail — e.g. the operator's opaque conversationRef for
  /// `bridge-relay`.
  final String? routeHint;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'platform': platform,
        'context': context,
        'approver': approver,
        if (route != null) 'route': route!.value,
        if (routeHint != null) 'routeHint': routeHint!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Consent List Approvers — response payload
class Response {
  const Response({
    required this.approvers,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        approvers: (json['approvers'] as List<dynamic>)
            .map((e) => ApproverBinding.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Matching approver bindings.
  final List<ApproverBinding> approvers;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'approvers': approvers.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Fetch approver bindings, optionally filtered by platform or context.
class Payload {
  const Payload({
    this.platform,
    this.context,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        platform: json['platform'] as String?,
        context: json['context'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Optional. Restrict to this platform tag.
  final String? platform;

  /// Optional. Restrict to this context path.
  final String? context;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (platform != null) 'platform': platform!,
        if (context != null) 'context': context!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/consent/approver-list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/consent/approver-list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/consent/approver-list/1.0","title":"Consent List Approvers — payload","description":"Fetch approver bindings, optionally filtered by platform or context.","type":"object","additionalProperties":false,"properties":{"platform":{"type":"string","minLength":1,"description":"Optional. Restrict to this platform tag."},"context":{"type":"string","minLength":1,"description":"Optional. Restrict to this context path."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Consent List Approvers — response payload","type":"object","additionalProperties":false,"required":["approvers"],"properties":{"approvers":{"type":"array","items":{"\$ref":"#/\$defs/ApproverBinding"},"description":"Matching approver bindings."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ApproverBinding":{"type":"object","additionalProperties":false,"required":["platform","context","approver"],"description":"Who approves inbound-messaging consent for a given platform within a VTA context, and how the prompt reaches them.","properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\", \\"whatsapp\\", \\"slack\\"."},"context":{"type":"string","minLength":1,"description":"The VTA context path this binding applies to."},"approver":{"type":"string","minLength":1,"description":"VID (DID) of the operator authorized to decide consent for this platform/context."},"route":{"\$ref":"#/\$defs/Route","description":"Optional. How to deliver the prompt; defaults to bridge-relay when omitted."},"routeHint":{"type":"string","description":"Optional routing detail — e.g. the operator\'s opaque conversationRef for `bridge-relay`."}}},"Route":{"type":"string","enum":["wake","bridge-relay"],"description":"How a consent prompt reaches the approver: `wake` pushes to the approver\'s device for a DID-signed decision; `bridge-relay` renders it through an enrolled bridge (e.g. a numbered card in the operator\'s messaging app) for a bridge-attested decision."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Consent List Approvers — response payload","type":"object","additionalProperties":false,"required":["approvers"],"properties":{"approvers":{"type":"array","items":{"\$ref":"#/\$defs/ApproverBinding"},"description":"Matching approver bindings."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ApproverBinding":{"type":"object","additionalProperties":false,"required":["platform","context","approver"],"description":"Who approves inbound-messaging consent for a given platform within a VTA context, and how the prompt reaches them.","properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\", \\"whatsapp\\", \\"slack\\"."},"context":{"type":"string","minLength":1,"description":"The VTA context path this binding applies to."},"approver":{"type":"string","minLength":1,"description":"VID (DID) of the operator authorized to decide consent for this platform/context."},"route":{"\$ref":"#/\$defs/Route","description":"Optional. How to deliver the prompt; defaults to bridge-relay when omitted."},"routeHint":{"type":"string","description":"Optional routing detail — e.g. the operator\'s opaque conversationRef for `bridge-relay`."}}},"Route":{"type":"string","enum":["wake","bridge-relay"],"description":"How a consent prompt reaches the approver: `wake` pushes to the approver\'s device for a DID-signed decision; `bridge-relay` renders it through an enrolled bridge (e.g. a numbered card in the operator\'s messaging app) for a bridge-attested decision."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
