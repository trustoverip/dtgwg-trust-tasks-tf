// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/consent/revoke/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// `revoked` = the grant was deleted. `notFound` = no grant existed for the subject.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus revoked = ResponseStatus('revoked');
  static const ResponseStatus notFound = ResponseStatus('notFound');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[
    revoked,
    notFound
  ];
}

/// Consent Revoke — response payload
class Response {
  const Response({
    required this.status,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        status: ResponseStatus(json['status'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// `revoked` = the grant was deleted. `notFound` = no grant existed for the subject.
  final ResponseStatus status;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'status': status.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The interaction kind: a 1:1 direct message, a multi-party group, or a broadcast
/// channel.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Kind(String value) {
  static const Kind dm = Kind('dm');
  static const Kind group = Kind('group');
  static const Kind channel = Kind('channel');

  /// Every value this specification's schema permits.
  static const List<Kind> values = <Kind>[dm, group, channel];
}

/// The platform-agnostic identifier of WHAT is being consented to: one conversation,
/// for one agent.
class ConsentSubject {
  const ConsentSubject({
    required this.platform,
    required this.conversationRef,
    required this.kind,
    required this.agent,
  });

  /// Read this payload from a decoded JSON object.
  factory ConsentSubject.fromJson(Map<String, dynamic> json) => ConsentSubject(
        platform: json['platform'] as String,
        conversationRef: json['conversationRef'] as String,
        kind: Kind(json['kind'] as String),
        agent: json['agent'] as String,
      );

  /// Messaging-platform tag, e.g. "signal", "whatsapp", "slack".
  final String platform;

  /// The bridge's OPAQUE conversation handle (e.g. "sig-1a2b3c4d"). NEVER the raw
  /// platform address — the VTA never learns the phone number / chat id.
  final String conversationRef;
  final Kind kind;

  /// VID (DID) of the AI agent the conversation would reach.
  final String agent;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'platform': platform,
        'conversationRef': conversationRef,
        'kind': kind.value,
        'agent': agent,
      };
}

/// An operator revokes a standing consent grant.
class Payload {
  const Payload({
    required this.subject,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subject:
            ConsentSubject.fromJson(json['subject'] as Map<String, dynamic>),
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final ConsentSubject subject;

  /// Optional operator note recorded in the audit trail.
  final String? reason;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject.toJson(),
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/consent/revoke/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/consent/revoke/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/consent/revoke/1.0","title":"Consent Revoke — payload","description":"An operator revokes a standing consent grant.","type":"object","additionalProperties":false,"required":["subject"],"properties":{"subject":{"\$ref":"#/\$defs/ConsentSubject"},"reason":{"type":"string","maxLength":1024,"description":"Optional operator note recorded in the audit trail."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Consent Revoke — response payload","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["revoked","notFound"],"description":"`revoked` = the grant was deleted. `notFound` = no grant existed for the subject."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConsentSubject":{"type":"object","additionalProperties":false,"required":["platform","conversationRef","kind","agent"],"description":"The platform-agnostic identifier of WHAT is being consented to: one conversation, for one agent.","properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\", \\"whatsapp\\", \\"slack\\"."},"conversationRef":{"type":"string","minLength":1,"description":"The bridge\'s OPAQUE conversation handle (e.g. \\"sig-1a2b3c4d\\"). NEVER the raw platform address — the VTA never learns the phone number / chat id."},"kind":{"\$ref":"#/\$defs/Kind"},"agent":{"type":"string","minLength":1,"description":"VID (DID) of the AI agent the conversation would reach."}}},"Kind":{"type":"string","enum":["dm","group","channel"],"description":"The interaction kind: a 1:1 direct message, a multi-party group, or a broadcast channel."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Consent Revoke — response payload","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["revoked","notFound"],"description":"`revoked` = the grant was deleted. `notFound` = no grant existed for the subject."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConsentSubject":{"type":"object","additionalProperties":false,"required":["platform","conversationRef","kind","agent"],"description":"The platform-agnostic identifier of WHAT is being consented to: one conversation, for one agent.","properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\", \\"whatsapp\\", \\"slack\\"."},"conversationRef":{"type":"string","minLength":1,"description":"The bridge\'s OPAQUE conversation handle (e.g. \\"sig-1a2b3c4d\\"). NEVER the raw platform address — the VTA never learns the phone number / chat id."},"kind":{"\$ref":"#/\$defs/Kind"},"agent":{"type":"string","minLength":1,"description":"VID (DID) of the AI agent the conversation would reach."}}},"Kind":{"type":"string","enum":["dm","group","channel"],"description":"The interaction kind: a 1:1 direct message, a multi-party group, or a broadcast channel."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
