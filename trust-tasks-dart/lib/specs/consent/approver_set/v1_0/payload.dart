// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/consent/approver-set/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The binding was upserted.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus set = ResponseStatus('set');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[set];
}

/// Consent Set Approver — response payload
class Response {
  const Response({
    required this.status,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        status: ResponseStatus(json['status'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The binding was upserted.
  final ResponseStatus status;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'status': status.value,
        if (ext != null) 'ext': ext!,
      };
}

/// How a consent prompt reaches the approver: `wake` pushes to the approver's device
/// for a DID-signed decision; `bridge-relay` renders it through an enrolled bridge
/// (e.g. a numbered card in the operator's messaging app) for a bridge-attested
/// decision.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Route(String value) {
  static const Route wake = Route('wake');
  static const Route bridgeRelay = Route('bridge-relay');

  /// Every value this specification's schema permits.
  static const List<Route> values = <Route>[wake, bridgeRelay];
}

/// An admin binds the operator who approves inbound-messaging consent for a platform
/// within a context.
class Payload {
  const Payload({
    required this.platform,
    required this.context,
    required this.approver,
    this.route,
    this.routeHint,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        platform: json['platform'] as String,
        context: json['context'] as String,
        approver: json['approver'] as String,
        route: json['route'] == null ? null : Route(json['route'] as String),
        routeHint: json['routeHint'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Messaging-platform tag, e.g. "signal".
  final String platform;

  /// The VTA context path this binding applies to.
  final String context;

  /// VID of the operator authorized to decide consent.
  final String approver;

  /// How the prompt reaches the approver; defaults to bridge-relay.
  final Route? route;

  /// Optional routing detail (e.g. the operator's opaque conversationRef).
  final String? routeHint;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'platform': platform,
        'context': context,
        'approver': approver,
        if (route != null) 'route': route!.value,
        if (routeHint != null) 'routeHint': routeHint!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/consent/approver-set/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/consent/approver-set/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/consent/approver-set/1.0","title":"Consent Set Approver — payload","description":"An admin binds the operator who approves inbound-messaging consent for a platform within a context.","type":"object","additionalProperties":false,"required":["platform","context","approver"],"properties":{"platform":{"type":"string","minLength":1,"description":"Messaging-platform tag, e.g. \\"signal\\"."},"context":{"type":"string","minLength":1,"description":"The VTA context path this binding applies to."},"approver":{"type":"string","minLength":1,"description":"VID of the operator authorized to decide consent."},"route":{"\$ref":"#/\$defs/Route","description":"How the prompt reaches the approver; defaults to bridge-relay."},"routeHint":{"type":"string","description":"Optional routing detail (e.g. the operator\'s opaque conversationRef)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Consent Set Approver — response payload","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["set"],"description":"The binding was upserted."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Route":{"type":"string","enum":["wake","bridge-relay"],"description":"How a consent prompt reaches the approver: `wake` pushes to the approver\'s device for a DID-signed decision; `bridge-relay` renders it through an enrolled bridge (e.g. a numbered card in the operator\'s messaging app) for a bridge-attested decision."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Consent Set Approver — response payload","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["set"],"description":"The binding was upserted."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Route":{"type":"string","enum":["wake","bridge-relay"],"description":"How a consent prompt reaches the approver: `wake` pushes to the approver\'s device for a DID-signed decision; `bridge-relay` renders it through an enrolled bridge (e.g. a numbered card in the operator\'s messaging app) for a bridge-attested decision."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
