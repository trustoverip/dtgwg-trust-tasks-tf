// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/did/check-name/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// DidRecord
class DidRecord {
  const DidRecord({
    required this.mnemonic,
    required this.owner,
    required this.createdAt,
    required this.updatedAt,
    required this.versionCount,
    this.didId,
    this.didUrl,
    this.method,
    this.domain,
    this.disabled,
    this.totalResolves,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory DidRecord.fromJson(Map<String, dynamic> json) => DidRecord(
        mnemonic: json['mnemonic'] as String,
        owner: json['owner'] as String,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        versionCount: json['versionCount'] as int,
        didId: json['didId'] as String?,
        didUrl: json['didUrl'] as String?,
        method: json['method'] as String?,
        domain: json['domain'] as String?,
        disabled: json['disabled'] as bool?,
        totalResolves: json['totalResolves'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Local path under which the DID is hosted (e.g. `alice`, `tenant/staff/alice`,
  /// `.well-known`). Compared by exact string equality (SPEC.md §4.8); producers SHOULD
  /// emit canonical form.
  final String mnemonic;

  /// VID of the party that currently owns the record. Authorization to mutate the record
  /// is anchored on this field.
  final String owner;

  /// RFC3339 timestamp of initial reservation.
  final String createdAt;

  /// RFC3339 timestamp of the most recent record mutation.
  final String updatedAt;

  /// Number of log entries the host currently holds for the DID. `0` indicates a
  /// reservation with no published log yet.
  final int versionCount;

  /// Fully-qualified DID identifier resolved from the most recent log entry (e.g.
  /// `did:webvh:&lt;scid&gt;:host:path`). Absent when `versionCount === 0`.
  final String? didId;

  /// Resolvable URL of the DID's log document (e.g.
  /// `https://did.example.com/alice/did.jsonl`). Stable across the record's lifetime:
  /// present from the initial reservation (`versionCount === 0`), it tells the owner
  /// where to publish the signed log and where resolvers fetch it. Distinct from
  /// `didId`, which only exists once a log entry has been published.
  final String? didUrl;

  /// DID method this record was registered under (e.g. `webvh`, `web`). When omitted,
  /// consumers MAY treat the record as legacy; SHOULD default to `webvh` only if their
  /// host predates the multi-method era.
  final String? method;

  /// Hosting domain (hostname) under which the DID resolves. Matches the host segment of
  /// the embedded DID identifier.
  final String? domain;

  /// When `true`, the DID is administratively disabled — the host serves a deactivation
  /// marker but retains content for recovery within the host's retention policy.
  final bool? disabled;

  /// Lifetime resolve counter, when the host exposes per-DID statistics.
  final int? totalResolves;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'mnemonic': mnemonic,
        'owner': owner,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        'versionCount': versionCount,
        if (didId != null) 'didId': didId!,
        if (didUrl != null) 'didUrl': didUrl!,
        if (method != null) 'method': method!,
        if (domain != null) 'domain': domain!,
        if (disabled != null) 'disabled': disabled!,
        if (totalResolves != null) 'totalResolves': totalResolves!,
        if (ext != null) 'ext': ext!,
      };
}

/// DID Management Check Name — response payload
class Response {
  const Response({
    required this.available,
    required this.reserved,
    this.record,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        available: json['available'] as bool,
        reserved: json['reserved'] as bool,
        record: json['record'] == null
            ? null
            : DidRecord.fromJson(json['record'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// True iff the path is currently free to register.
  final bool available;

  /// True iff a reservation was committed in this call. Implies the request set
  /// `reserve: true` AND the path was available.
  final bool reserved;

  /// Present iff `reserved === true`. Carries the newly-committed DidRecord with
  /// `versionCount: 0`.
  final DidRecord? record;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'available': available,
        'reserved': reserved,
        if (record != null) 'record': record!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// DID Management Check Name — payload
class Payload {
  const Payload({
    this.path,
    this.reserve,
    this.domain,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        path: json['path'] as String?,
        reserve: json['reserve'] as bool?,
        domain: json['domain'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Local path to test. REQUIRED for an availability probe (`reserve: false`). OPTIONAL
  /// when `reserve: true`: omit it to ask the host to auto-assign a fresh,
  /// server-generated mnemonic for the reservation.
  final String? path;

  /// When true and the path is available — or, when `path` is omitted, always —
  /// atomically reserve a slot under the caller and return the resulting DidRecord. When
  /// `path` is omitted the host generates a fresh unused mnemonic (auto-assign).
  final bool? reserve;

  /// Optional explicit hosting domain for the reservation. Honored only when `reserve:
  /// true`.
  final String? domain;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (path != null) 'path': path!,
        if (reserve != null) 'reserve': reserve!,
        if (domain != null) 'domain': domain!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/did-management/did/check-name/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/did-management/did/check-name/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/did-management/did/check-name/0.1","title":"DID Management Check Name — payload","type":"object","additionalProperties":false,"properties":{"path":{"type":"string","minLength":1,"description":"Local path to test. REQUIRED for an availability probe (`reserve: false`). OPTIONAL when `reserve: true`: omit it to ask the host to auto-assign a fresh, server-generated mnemonic for the reservation."},"reserve":{"type":"boolean","default":false,"description":"When true and the path is available — or, when `path` is omitted, always — atomically reserve a slot under the caller and return the resulting DidRecord. When `path` is omitted the host generates a fresh unused mnemonic (auto-assign)."},"domain":{"type":"string","description":"Optional explicit hosting domain for the reservation. Honored only when `reserve: true`."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$comment":"`path` is intentionally not listed in `required`: it is REQUIRED for an availability probe but OPTIONAL (auto-assign) when `reserve: true`. JSON-Schema `if/then/else` would express this exactly, but the Rust codegen (typify) does not support conditional subschemas, so the rule is stated in spec.md §Conformance and enforced by the consumer, which MUST reject a path-less request unless `reserve: true`.","\$defs":{"Response":{"\$anchor":"response","title":"DID Management Check Name — response payload","type":"object","additionalProperties":false,"required":["available","reserved"],"properties":{"available":{"type":"boolean","description":"True iff the path is currently free to register."},"reserved":{"type":"boolean","description":"True iff a reservation was committed in this call. Implies the request set `reserve: true` AND the path was available."},"record":{"\$ref":"#/\$defs/DidRecord","description":"Present iff `reserved === true`. Carries the newly-committed DidRecord with `versionCount: 0`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DidRecord":{"title":"DidRecord","type":"object","additionalProperties":false,"required":["mnemonic","owner","createdAt","updatedAt","versionCount"],"properties":{"mnemonic":{"type":"string","description":"Local path under which the DID is hosted (e.g. `alice`, `tenant/staff/alice`, `.well-known`). Compared by exact string equality (SPEC.md §4.8); producers SHOULD emit canonical form."},"owner":{"type":"string","description":"VID of the party that currently owns the record. Authorization to mutate the record is anchored on this field."},"createdAt":{"type":"string","format":"date-time","description":"RFC3339 timestamp of initial reservation."},"updatedAt":{"type":"string","format":"date-time","description":"RFC3339 timestamp of the most recent record mutation."},"versionCount":{"type":"integer","minimum":0,"description":"Number of log entries the host currently holds for the DID. `0` indicates a reservation with no published log yet."},"didId":{"type":"string","description":"Fully-qualified DID identifier resolved from the most recent log entry (e.g. `did:webvh:<scid>:host:path`). Absent when `versionCount === 0`."},"didUrl":{"type":"string","format":"uri","description":"Resolvable URL of the DID\'s log document (e.g. `https://did.example.com/alice/did.jsonl`). Stable across the record\'s lifetime: present from the initial reservation (`versionCount === 0`), it tells the owner where to publish the signed log and where resolvers fetch it. Distinct from `didId`, which only exists once a log entry has been published."},"method":{"type":"string","description":"DID method this record was registered under (e.g. `webvh`, `web`). When omitted, consumers MAY treat the record as legacy; SHOULD default to `webvh` only if their host predates the multi-method era."},"domain":{"type":"string","description":"Hosting domain (hostname) under which the DID resolves. Matches the host segment of the embedded DID identifier."},"disabled":{"type":"boolean","description":"When `true`, the DID is administratively disabled — the host serves a deactivation marker but retains content for recovery within the host\'s retention policy."},"totalResolves":{"type":"integer","minimum":0,"description":"Lifetime resolve counter, when the host exposes per-DID statistics."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"DID Management Check Name — response payload","type":"object","additionalProperties":false,"required":["available","reserved"],"properties":{"available":{"type":"boolean","description":"True iff the path is currently free to register."},"reserved":{"type":"boolean","description":"True iff a reservation was committed in this call. Implies the request set `reserve: true` AND the path was available."},"record":{"\$ref":"#/\$defs/DidRecord","description":"Present iff `reserved === true`. Carries the newly-committed DidRecord with `versionCount: 0`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DidRecord":{"title":"DidRecord","type":"object","additionalProperties":false,"required":["mnemonic","owner","createdAt","updatedAt","versionCount"],"properties":{"mnemonic":{"type":"string","description":"Local path under which the DID is hosted (e.g. `alice`, `tenant/staff/alice`, `.well-known`). Compared by exact string equality (SPEC.md §4.8); producers SHOULD emit canonical form."},"owner":{"type":"string","description":"VID of the party that currently owns the record. Authorization to mutate the record is anchored on this field."},"createdAt":{"type":"string","format":"date-time","description":"RFC3339 timestamp of initial reservation."},"updatedAt":{"type":"string","format":"date-time","description":"RFC3339 timestamp of the most recent record mutation."},"versionCount":{"type":"integer","minimum":0,"description":"Number of log entries the host currently holds for the DID. `0` indicates a reservation with no published log yet."},"didId":{"type":"string","description":"Fully-qualified DID identifier resolved from the most recent log entry (e.g. `did:webvh:<scid>:host:path`). Absent when `versionCount === 0`."},"didUrl":{"type":"string","format":"uri","description":"Resolvable URL of the DID\'s log document (e.g. `https://did.example.com/alice/did.jsonl`). Stable across the record\'s lifetime: present from the initial reservation (`versionCount === 0`), it tells the owner where to publish the signed log and where resolvers fetch it. Distinct from `didId`, which only exists once a log entry has been published."},"method":{"type":"string","description":"DID method this record was registered under (e.g. `webvh`, `web`). When omitted, consumers MAY treat the record as legacy; SHOULD default to `webvh` only if their host predates the multi-method era."},"domain":{"type":"string","description":"Hosting domain (hostname) under which the DID resolves. Matches the host segment of the embedded DID identifier."},"disabled":{"type":"boolean","description":"When `true`, the DID is administratively disabled — the host serves a deactivation marker but retains content for recovery within the host\'s retention policy."},"totalResolves":{"type":"integer","minimum":0,"description":"Lifetime resolve counter, when the host exposes per-DID statistics."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
