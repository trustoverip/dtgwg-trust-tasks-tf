// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/did/problem-report/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// DID Management Problem Report — payload
class Payload {
  const Payload({
    required this.mnemonic,
    required this.code,
    required this.message,
    this.domain,
    this.ctx,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        mnemonic: json['mnemonic'] as String,
        code: json['code'] as String,
        message: json['message'] as String,
        domain: json['domain'] as String?,
        ctx: json['ctx'] as Map<String, dynamic>?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String mnemonic;
  final String code;
  final String message;
  final String? domain;

  /// Free-form structured context. Hosts SHOULD avoid leaking other tenants' data here.
  final Map<String, dynamic>? ctx;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'mnemonic': mnemonic,
        'code': code,
        'message': message,
        if (domain != null) 'domain': domain!,
        if (ctx != null) 'ctx': ctx!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/did-management/did/problem-report/0.1';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/did-management/did/problem-report/0.1","title":"DID Management Problem Report — payload","type":"object","additionalProperties":false,"required":["mnemonic","code","message"],"properties":{"mnemonic":{"type":"string","minLength":1},"code":{"type":"string","minLength":1},"message":{"type":"string","minLength":1,"maxLength":1024},"domain":{"type":"string"},"ctx":{"type":"object","additionalProperties":true,"description":"Free-form structured context. Hosts SHOULD avoid leaking other tenants\' data here."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);
