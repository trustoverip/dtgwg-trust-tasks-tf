// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/agent-name/check/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// DID Management Check Agent Name — response payload
class Response {
  const Response({
    required this.name,
    required this.domain,
    required this.available,
    required this.reserved,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        name: json['name'] as String,
        domain: json['domain'] as String,
        available: json['available'] as bool,
        reserved: json['reserved'] as bool,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The probed name's local part, echoed in canonical bare form (leading `@` stripped).
  final String name;

  /// The hosting domain the probe was evaluated against, after domain resolution.
  final String domain;

  /// Free to claim: neither reserved by the host nor already bound to a DID on this
  /// domain.
  final bool available;

  /// On the host's reserved list (`admin`, `support`, …) — unavailable but well-formed,
  /// distinct from a grammar error (which is rejected as invalid_name).
  final bool reserved;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'domain': domain,
        'available': available,
        'reserved': reserved,
        if (ext != null) 'ext': ext!,
      };
}

/// DID Management Check Agent Name — payload
class Payload {
  const Payload({
    required this.name,
    this.domain,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        name: json['name'] as String,
        domain: json['domain'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The agent name's local part to probe, without the leading `@` (the `alice` in
  /// `/@alice`). A leading `@` submitted by a lenient client is stripped by the consumer
  /// before evaluation.
  final String name;

  /// Optional explicit hosting domain to probe against. Availability is domain-scoped —
  /// the same name may be free on one domain and taken on another. When omitted,
  /// resolves via caller ACL default → system default. See category CONVENTIONS §1.
  final String? domain;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        if (domain != null) 'domain': domain!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/did-management/agent-name/check/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/did-management/agent-name/check/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/did-management/agent-name/check/0.1","title":"DID Management Check Agent Name — payload","type":"object","additionalProperties":false,"required":["name"],"properties":{"name":{"type":"string","minLength":1,"description":"The agent name\'s local part to probe, without the leading `@` (the `alice` in `/@alice`). A leading `@` submitted by a lenient client is stripped by the consumer before evaluation."},"domain":{"type":"string","description":"Optional explicit hosting domain to probe against. Availability is domain-scoped — the same name may be free on one domain and taken on another. When omitted, resolves via caller ACL default → system default. See category CONVENTIONS §1."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"DID Management Check Agent Name — response payload","type":"object","additionalProperties":false,"required":["name","domain","available","reserved"],"properties":{"name":{"type":"string","description":"The probed name\'s local part, echoed in canonical bare form (leading `@` stripped)."},"domain":{"type":"string","description":"The hosting domain the probe was evaluated against, after domain resolution."},"available":{"type":"boolean","description":"Free to claim: neither reserved by the host nor already bound to a DID on this domain."},"reserved":{"type":"boolean","description":"On the host\'s reserved list (`admin`, `support`, …) — unavailable but well-formed, distinct from a grammar error (which is rejected as invalid_name)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"DID Management Check Agent Name — response payload","type":"object","additionalProperties":false,"required":["name","domain","available","reserved"],"properties":{"name":{"type":"string","description":"The probed name\'s local part, echoed in canonical bare form (leading `@` stripped)."},"domain":{"type":"string","description":"The hosting domain the probe was evaluated against, after domain resolution."},"available":{"type":"boolean","description":"Free to claim: neither reserved by the host nor already bound to a DID on this domain."},"reserved":{"type":"boolean","description":"On the host\'s reserved list (`admin`, `support`, …) — unavailable but well-formed, distinct from a grammar error (which is rejected as invalid_name)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
