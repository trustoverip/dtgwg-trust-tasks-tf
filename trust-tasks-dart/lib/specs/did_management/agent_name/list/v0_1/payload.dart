// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/agent-name/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// AgentNameEntry
class AgentNameEntry {
  const AgentNameEntry({
    required this.name,
    required this.enabled,
    required this.createdAt,
  });

  /// Read this payload from a decoded JSON object.
  factory AgentNameEntry.fromJson(Map<String, dynamic> json) => AgentNameEntry(
        name: json['name'] as String,
        enabled: json['enabled'] as bool,
        createdAt: json['createdAt'] as String,
      );

  /// The name's local part, without the `@` — `alice` for `/@alice`. Bare rather than a
  /// full URL because a name is only meaningful within its domain, which is carried once
  /// on the response.
  final String name;

  /// Whether the name currently resolves. `false` means parked, not gone: the name still
  /// belongs to this DID and nobody else can claim it.
  final bool enabled;

  /// RFC3339 timestamp of when the name was first bound to this DID.
  final String createdAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'enabled': enabled,
        'createdAt': createdAt,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// DID Management List Agent Names — response payload
class Response {
  const Response({
    required this.mnemonic,
    this.domain,
    required this.agentNames,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        mnemonic: json['mnemonic'] as String,
        domain: json['domain'] as String?,
        agentNames: (json['agentNames'] as List<dynamic>)
            .map((e) => AgentNameEntry.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The slot the listing is for, echoed.
  final String mnemonic;

  /// The slot's hosting domain. Omitted (rather than empty) for an un-domained legacy
  /// slot.
  final String? domain;

  /// Every name bound to the slot, including parked entries. Always present — a slot
  /// with no names answers with an empty array, so a caller never has to distinguish 'no
  /// names' from 'field missing'.
  final List<AgentNameEntry> agentNames;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'mnemonic': mnemonic,
        if (domain != null) 'domain': domain!,
        'agentNames': agentNames.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// DID Management List Agent Names — payload
class Payload {
  const Payload({
    required this.mnemonic,
    this.domain,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        mnemonic: json['mnemonic'] as String,
        domain: json['domain'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The hosted DID slot whose agent-name registry is being read.
  final String mnemonic;

  /// Optional explicit hosting domain, for disambiguation per category CONVENTIONS §3.
  /// When present it MUST match the slot's recorded domain.
  final String? domain;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'mnemonic': mnemonic,
        if (domain != null) 'domain': domain!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/did-management/agent-name/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/did-management/agent-name/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/did-management/agent-name/list/0.1","title":"DID Management List Agent Names — payload","type":"object","additionalProperties":false,"required":["mnemonic"],"properties":{"mnemonic":{"type":"string","minLength":1,"description":"The hosted DID slot whose agent-name registry is being read."},"domain":{"type":"string","description":"Optional explicit hosting domain, for disambiguation per category CONVENTIONS §3. When present it MUST match the slot\'s recorded domain."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"AgentNameEntry":{"title":"AgentNameEntry","type":"object","additionalProperties":false,"required":["name","enabled","createdAt"],"properties":{"name":{"type":"string","description":"The name\'s local part, without the `@` — `alice` for `/@alice`. Bare rather than a full URL because a name is only meaningful within its domain, which is carried once on the response."},"enabled":{"type":"boolean","description":"Whether the name currently resolves. `false` means parked, not gone: the name still belongs to this DID and nobody else can claim it."},"createdAt":{"type":"string","format":"date-time","description":"RFC3339 timestamp of when the name was first bound to this DID."}}},"Response":{"\$anchor":"response","title":"DID Management List Agent Names — response payload","type":"object","additionalProperties":false,"required":["mnemonic","agentNames"],"properties":{"mnemonic":{"type":"string","description":"The slot the listing is for, echoed."},"domain":{"type":"string","description":"The slot\'s hosting domain. Omitted (rather than empty) for an un-domained legacy slot."},"agentNames":{"type":"array","items":{"\$ref":"#/\$defs/AgentNameEntry"},"description":"Every name bound to the slot, including parked entries. Always present — a slot with no names answers with an empty array, so a caller never has to distinguish \'no names\' from \'field missing\'."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"AgentNameEntry":{"title":"AgentNameEntry","type":"object","additionalProperties":false,"required":["name","enabled","createdAt"],"properties":{"name":{"type":"string","description":"The name\'s local part, without the `@` — `alice` for `/@alice`. Bare rather than a full URL because a name is only meaningful within its domain, which is carried once on the response."},"enabled":{"type":"boolean","description":"Whether the name currently resolves. `false` means parked, not gone: the name still belongs to this DID and nobody else can claim it."},"createdAt":{"type":"string","format":"date-time","description":"RFC3339 timestamp of when the name was first bound to this DID."}}},"Response":{"\$anchor":"response","title":"DID Management List Agent Names — response payload","type":"object","additionalProperties":false,"required":["mnemonic","agentNames"],"properties":{"mnemonic":{"type":"string","description":"The slot the listing is for, echoed."},"domain":{"type":"string","description":"The slot\'s hosting domain. Omitted (rather than empty) for an un-domained legacy slot."},"agentNames":{"type":"array","items":{"\$ref":"#/\$defs/AgentNameEntry"},"description":"Every name bound to the slot, including parked entries. Always present — a slot with no names answers with an empty array, so a caller never has to distinguish \'no names\' from \'field missing\'."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
