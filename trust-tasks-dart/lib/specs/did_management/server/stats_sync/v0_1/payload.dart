// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/server/stats-sync/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Server Stats Sync — response payload
class Response {
  const Response({
    required this.instanceId,
    required this.accepted,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        instanceId: json['instanceId'] as String,
        accepted: json['accepted'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String instanceId;
  final int accepted;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'instanceId': instanceId,
        'accepted': accepted,
        if (ext != null) 'ext': ext!,
      };
}

/// PayloadPerMnemonicItem, generated from its schema.
class PayloadPerMnemonicItem {
  const PayloadPerMnemonicItem({
    required this.mnemonic,
    required this.resolves,
    required this.updates,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadPerMnemonicItem.fromJson(Map<String, dynamic> json) =>
      PayloadPerMnemonicItem(
        mnemonic: json['mnemonic'] as String,
        resolves: json['resolves'] as int,
        updates: json['updates'] as int,
      );

  final String mnemonic;
  final int resolves;
  final int updates;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'mnemonic': mnemonic,
        'resolves': resolves,
        'updates': updates,
      };
}

/// PayloadBucketsItem, generated from its schema.
class PayloadBucketsItem {
  const PayloadBucketsItem({
    required this.mnemonic,
    required this.epoch,
    required this.r,
    required this.u,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadBucketsItem.fromJson(Map<String, dynamic> json) =>
      PayloadBucketsItem(
        mnemonic: json['mnemonic'] as String,
        epoch: json['epoch'] as int,
        r: json['r'] as int,
        u: json['u'] as int,
      );

  final String mnemonic;
  final int epoch;
  final int r;
  final int u;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'mnemonic': mnemonic,
        'epoch': epoch,
        'r': r,
        'u': u,
      };
}

/// Server Stats Sync — payload
class Payload {
  const Payload({
    required this.instanceId,
    required this.perMnemonic,
    this.buckets,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        instanceId: json['instanceId'] as String,
        perMnemonic: (json['perMnemonic'] as List<dynamic>)
            .map((e) =>
                PayloadPerMnemonicItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        buckets: json['buckets'] == null
            ? null
            : (json['buckets'] as List<dynamic>)
                .map((e) =>
                    PayloadBucketsItem.fromJson(e as Map<String, dynamic>))
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String instanceId;
  final List<PayloadPerMnemonicItem> perMnemonic;
  final List<PayloadBucketsItem>? buckets;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'instanceId': instanceId,
        'perMnemonic': perMnemonic.map((e) => e.toJson()).toList(),
        if (buckets != null)
          'buckets': buckets!.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/did-management/server/stats-sync/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/did-management/server/stats-sync/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/did-management/server/stats-sync/0.1","title":"Server Stats Sync — payload","type":"object","additionalProperties":false,"required":["instanceId","perMnemonic"],"properties":{"instanceId":{"type":"string","minLength":1},"perMnemonic":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["mnemonic","resolves","updates"],"properties":{"mnemonic":{"type":"string","minLength":1},"resolves":{"type":"integer","minimum":0},"updates":{"type":"integer","minimum":0}}}},"buckets":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["mnemonic","epoch","r","u"],"properties":{"mnemonic":{"type":"string","minLength":1},"epoch":{"type":"integer","minimum":0},"r":{"type":"integer","minimum":0},"u":{"type":"integer","minimum":0}}}},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Server Stats Sync — response payload","type":"object","additionalProperties":false,"required":["instanceId","accepted"],"properties":{"instanceId":{"type":"string"},"accepted":{"type":"integer","minimum":0},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Server Stats Sync — response payload","type":"object","additionalProperties":false,"required":["instanceId","accepted"],"properties":{"instanceId":{"type":"string"},"accepted":{"type":"integer","minimum":0},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
