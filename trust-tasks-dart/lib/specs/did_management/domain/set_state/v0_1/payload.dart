// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/did-management/domain/set-state/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only
/// mode for the host's configured grace period before purge becomes eligible.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DomainEntryStatus(String value) {
  static const DomainEntryStatus active = DomainEntryStatus('active');
  static const DomainEntryStatus disabled = DomainEntryStatus('disabled');

  /// Every value this specification's schema permits.
  static const List<DomainEntryStatus> values = <DomainEntryStatus>[
    active,
    disabled
  ];
}

/// DomainEntry
class DomainEntry {
  const DomainEntry({
    required this.name,
    this.label,
    required this.status,
    this.defaultDomain,
    required this.createdAt,
    this.disabledAt,
    this.purgeAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory DomainEntry.fromJson(Map<String, dynamic> json) => DomainEntry(
        name: json['name'] as String,
        label: json['label'] as String?,
        status: DomainEntryStatus(json['status'] as String),
        defaultDomain: json['defaultDomain'] as bool?,
        createdAt: json['createdAt'] as String,
        disabledAt: json['disabledAt'] as String?,
        purgeAt: json['purgeAt'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Hosting domain name (e.g. `did.example.com`). Compared case-insensitively;
  /// producers SHOULD emit lowercase canonical form.
  final String name;

  /// Optional human-readable label.
  final String? label;

  /// Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only
  /// mode for the host's configured grace period before purge becomes eligible.
  final DomainEntryStatus status;

  /// When `true`, this domain is the host's default — new DIDs created without an
  /// explicit domain are hosted here. Exactly one entry SHOULD carry `defaultDomain:
  /// true`.
  final bool? defaultDomain;
  final String createdAt;

  /// Present iff `status === "disabled"`.
  final String? disabledAt;

  /// Earliest RFC3339 timestamp at which the host's background sweep is allowed to purge
  /// content for a disabled domain. Operators with administrative authority MAY override
  /// the wait via `domain/purge`.
  final String? purgeAt;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        if (label != null) 'label': label!,
        'status': status.value,
        if (defaultDomain != null) 'defaultDomain': defaultDomain!,
        'createdAt': createdAt,
        if (disabledAt != null) 'disabledAt': disabledAt!,
        if (purgeAt != null) 'purgeAt': purgeAt!,
        if (ext != null) 'ext': ext!,
      };
}

/// Domain Set State — response payload
class Response {
  const Response({
    required this.entry,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        entry: DomainEntry.fromJson(json['entry'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final DomainEntry entry;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entry': entry.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Desired domain state. `active`: the domain is in active service and accepts new DID
/// registrations (re-activating a disabled domain cancels any pending purge).
/// `disabled`: existing DIDs remain readable for the host's grace period, but no new
/// DIDs may be hosted under it. Declarative — requesting the state the domain is
/// already in is an idempotent no-op.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadState(String value) {
  static const PayloadState active = PayloadState('active');
  static const PayloadState disabled = PayloadState('disabled');

  /// Every value this specification's schema permits.
  static const List<PayloadState> values = <PayloadState>[active, disabled];
}

/// Domain Set State — payload
class Payload {
  const Payload({
    required this.name,
    required this.state,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        name: json['name'] as String,
        state: PayloadState(json['state'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The hosting domain whose state is being set.
  final String name;

  /// Desired domain state. `active`: the domain is in active service and accepts new DID
  /// registrations (re-activating a disabled domain cancels any pending purge).
  /// `disabled`: existing DIDs remain readable for the host's grace period, but no new
  /// DIDs may be hosted under it. Declarative — requesting the state the domain is
  /// already in is an idempotent no-op.
  final PayloadState state;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'state': state.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/did-management/domain/set-state/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/did-management/domain/set-state/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/did-management/domain/set-state/0.1","title":"Domain Set State — payload","type":"object","additionalProperties":false,"required":["name","state"],"properties":{"name":{"type":"string","minLength":1,"description":"The hosting domain whose state is being set."},"state":{"type":"string","enum":["active","disabled"],"description":"Desired domain state. `active`: the domain is in active service and accepts new DID registrations (re-activating a disabled domain cancels any pending purge). `disabled`: existing DIDs remain readable for the host\'s grace period, but no new DIDs may be hosted under it. Declarative — requesting the state the domain is already in is an idempotent no-op."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Domain Set State — response payload","type":"object","additionalProperties":false,"required":["entry"],"properties":{"entry":{"\$ref":"#/\$defs/DomainEntry"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DomainEntry":{"title":"DomainEntry","type":"object","additionalProperties":false,"required":["name","status","createdAt"],"properties":{"name":{"type":"string","description":"Hosting domain name (e.g. `did.example.com`). Compared case-insensitively; producers SHOULD emit lowercase canonical form."},"label":{"type":"string","description":"Optional human-readable label."},"status":{"type":"string","enum":["active","disabled"],"description":"Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only mode for the host\'s configured grace period before purge becomes eligible."},"defaultDomain":{"type":"boolean","description":"When `true`, this domain is the host\'s default — new DIDs created without an explicit domain are hosted here. Exactly one entry SHOULD carry `defaultDomain: true`."},"createdAt":{"type":"string","format":"date-time"},"disabledAt":{"type":"string","format":"date-time","description":"Present iff `status === \\"disabled\\"`."},"purgeAt":{"type":"string","format":"date-time","description":"Earliest RFC3339 timestamp at which the host\'s background sweep is allowed to purge content for a disabled domain. Operators with administrative authority MAY override the wait via `domain/purge`."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Domain Set State — response payload","type":"object","additionalProperties":false,"required":["entry"],"properties":{"entry":{"\$ref":"#/\$defs/DomainEntry"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DomainEntry":{"title":"DomainEntry","type":"object","additionalProperties":false,"required":["name","status","createdAt"],"properties":{"name":{"type":"string","description":"Hosting domain name (e.g. `did.example.com`). Compared case-insensitively; producers SHOULD emit lowercase canonical form."},"label":{"type":"string","description":"Optional human-readable label."},"status":{"type":"string","enum":["active","disabled"],"description":"Domain lifecycle state. A `disabled` domain still serves existing DIDs in read-only mode for the host\'s configured grace period before purge becomes eligible."},"defaultDomain":{"type":"boolean","description":"When `true`, this domain is the host\'s default — new DIDs created without an explicit domain are hosted here. Exactly one entry SHOULD carry `defaultDomain: true`."},"createdAt":{"type":"string","format":"date-time"},"disabledAt":{"type":"string","format":"date-time","description":"Present iff `status === \\"disabled\\"`."},"purgeAt":{"type":"string","format":"date-time","description":"Earliest RFC3339 timestamp at which the host\'s background sweep is allowed to purge content for a disabled domain. Operators with administrative authority MAY override the wait via `domain/purge`."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
