// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/process-attestation/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Carried verbatim from the IANA Named Information Hash Algorithm registry, not
/// re-cased — SPEC §4.10 externally-owned values.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ArtifactAlgorithm(String value) {
  static const ArtifactAlgorithm sha256 = ArtifactAlgorithm('sha-256');
  static const ArtifactAlgorithm sha384 = ArtifactAlgorithm('sha-384');
  static const ArtifactAlgorithm sha512 = ArtifactAlgorithm('sha-512');
  static const ArtifactAlgorithm blake3 = ArtifactAlgorithm('blake3');

  /// Every value this specification's schema permits.
  static const List<ArtifactAlgorithm> values = <ArtifactAlgorithm>[
    sha256,
    sha384,
    sha512,
    blake3
  ];
}

/// Deterministic identification of the object whose production is in question.
class Artifact {
  const Artifact({
    required this.digest,
    required this.algorithm,
    this.byteLength,
  });

  /// Read this payload from a decoded JSON object.
  factory Artifact.fromJson(Map<String, dynamic> json) => Artifact(
        digest: json['digest'] as String,
        algorithm: ArtifactAlgorithm(json['algorithm'] as String),
        byteLength: json['byteLength'] as int?,
      );

  /// base64url, unpadded.
  final String digest;

  /// Carried verbatim from the IANA Named Information Hash Algorithm registry, not
  /// re-cased — SPEC §4.10 externally-owned values.
  final ArtifactAlgorithm algorithm;
  final int? byteLength;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'digest': digest,
        'algorithm': algorithm.value,
        if (byteLength != null) 'byteLength': byteLength!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Opaque at the task layer. The task standardizes the interaction, not the receipt.
class Assessment {
  const Assessment({
    required this.primitive,
    required this.determination,
    this.confidence,
  });

  /// Read this payload from a decoded JSON object.
  factory Assessment.fromJson(Map<String, dynamic> json) => Assessment(
        primitive: json['primitive'] as String,
        determination: json['determination'] as String,
        confidence: json['confidence'] as int?,
      );

  /// Specification the receipt is verified under. Names which vocabulary `determination`
  /// belongs to.
  final String primitive;

  /// Primitive-defined. Deliberately not normalized across primitives: a determination
  /// means only what the primitive that produced it defines it to mean.
  final String determination;

  /// The primitive's confidence in `determination`, as an integer percentage. An integer
  /// rather than a fraction because no other specification in this registry puts a
  /// floating-point value on the wire: a float's textual form is not stable across JSON
  /// implementations, which makes byte-exact re-serialization a language-dependent
  /// property.
  final int? confidence;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'primitive': primitive,
        'determination': determination,
        if (confidence != null) 'confidence': confidence!,
      };
}

/// Tagged union. Inline exists for Verifiers that cannot dereference a URI at
/// verification time.
typedef ReceiptReference = Object?;

/// DisclosedDocument, generated from its schema.
class DisclosedDocument {
  const DisclosedDocument({
    this.mediaType,
  });

  /// Read this payload from a decoded JSON object.
  factory DisclosedDocument.fromJson(Map<String, dynamic> json) =>
      DisclosedDocument(
        mediaType: json['mediaType'] as String?,
      );

  final String? mediaType;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (mediaType != null) 'mediaType': mediaType!,
      };
}

/// DisclosedTimeline, generated from its schema.
class DisclosedTimeline {
  const DisclosedTimeline({
    this.startedAt,
    this.endedAt,
    this.checkpointCount,
  });

  /// Read this payload from a decoded JSON object.
  factory DisclosedTimeline.fromJson(Map<String, dynamic> json) =>
      DisclosedTimeline(
        startedAt: json['startedAt'] as String?,
        endedAt: json['endedAt'] as String?,
        checkpointCount: json['checkpointCount'] as int?,
      );

  final String? startedAt;
  final String? endedAt;
  final int? checkpointCount;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (startedAt != null) 'startedAt': startedAt!,
        if (endedAt != null) 'endedAt': endedAt!,
        if (checkpointCount != null) 'checkpointCount': checkpointCount!,
      };
}

/// DisclosedIntegrityKeyCustody is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DisclosedIntegrityKeyCustody(String value) {
  static const DisclosedIntegrityKeyCustody software =
      DisclosedIntegrityKeyCustody('software');
  static const DisclosedIntegrityKeyCustody tpm =
      DisclosedIntegrityKeyCustody('tpm');
  static const DisclosedIntegrityKeyCustody secureEnclave =
      DisclosedIntegrityKeyCustody('secureEnclave');

  /// Every value this specification's schema permits.
  static const List<DisclosedIntegrityKeyCustody> values =
      <DisclosedIntegrityKeyCustody>[software, tpm, secureEnclave];
}

/// DisclosedIntegrity, generated from its schema.
class DisclosedIntegrity {
  const DisclosedIntegrity({
    this.chainValid,
    this.signatureValid,
    this.keyCustody,
  });

  /// Read this payload from a decoded JSON object.
  factory DisclosedIntegrity.fromJson(Map<String, dynamic> json) =>
      DisclosedIntegrity(
        chainValid: json['chainValid'] as bool?,
        signatureValid: json['signatureValid'] as bool?,
        keyCustody: json['keyCustody'] == null
            ? null
            : DisclosedIntegrityKeyCustody(json['keyCustody'] as String),
      );

  final bool? chainValid;
  final bool? signatureValid;
  final DisclosedIntegrityKeyCustody? keyCustody;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (chainValid != null) 'chainValid': chainValid!,
        if (signatureValid != null) 'signatureValid': signatureValid!,
        if (keyCustody != null) 'keyCustody': keyCustody!.value,
      };
}

/// DisclosedAnchorsItemKind is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DisclosedAnchorsItemKind(String value) {
  static const DisclosedAnchorsItemKind rfc3161 =
      DisclosedAnchorsItemKind('rfc3161');
  static const DisclosedAnchorsItemKind opentimestamps =
      DisclosedAnchorsItemKind('opentimestamps');
  static const DisclosedAnchorsItemKind roughtime =
      DisclosedAnchorsItemKind('roughtime');

  /// Every value this specification's schema permits.
  static const List<DisclosedAnchorsItemKind> values =
      <DisclosedAnchorsItemKind>[rfc3161, opentimestamps, roughtime];
}

/// DisclosedAnchorsItem, generated from its schema.
class DisclosedAnchorsItem {
  const DisclosedAnchorsItem({
    required this.kind,
    this.anchoredAt,
  });

  /// Read this payload from a decoded JSON object.
  factory DisclosedAnchorsItem.fromJson(Map<String, dynamic> json) =>
      DisclosedAnchorsItem(
        kind: DisclosedAnchorsItemKind(json['kind'] as String),
        anchoredAt: json['anchoredAt'] as String?,
      );

  final DisclosedAnchorsItemKind kind;
  final String? anchoredAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind.value,
        if (anchoredAt != null) 'anchoredAt': anchoredAt!,
      };
}

/// Only the categories the request asked for.
class Disclosed {
  const Disclosed({
    this.document,
    this.timeline,
    this.integrity,
    this.anchors,
  });

  /// Read this payload from a decoded JSON object.
  factory Disclosed.fromJson(Map<String, dynamic> json) => Disclosed(
        document: json['document'] == null
            ? null
            : DisclosedDocument.fromJson(
                json['document'] as Map<String, dynamic>),
        timeline: json['timeline'] == null
            ? null
            : DisclosedTimeline.fromJson(
                json['timeline'] as Map<String, dynamic>),
        integrity: json['integrity'] == null
            ? null
            : DisclosedIntegrity.fromJson(
                json['integrity'] as Map<String, dynamic>),
        anchors: json['anchors'] == null
            ? null
            : (json['anchors'] as List<dynamic>)
                .map((e) =>
                    DisclosedAnchorsItem.fromJson(e as Map<String, dynamic>))
                .toList(),
      );

  final DisclosedDocument? document;
  final DisclosedTimeline? timeline;
  final DisclosedIntegrity? integrity;
  final List<DisclosedAnchorsItem>? anchors;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (document != null) 'document': document!.toJson(),
        if (timeline != null) 'timeline': timeline!.toJson(),
        if (integrity != null) 'integrity': integrity!.toJson(),
        if (anchors != null)
          'anchors': anchors!.map((e) => e.toJson()).toList(),
      };
}

/// The Holder's answer. Failures use trust-task-error, not this shape. Both results
/// share one response type so the envelope carries no observable distinction between
/// them. `result` is the discriminator and stays inside the payload, so one envelope
/// type covers both branches.
typedef Response = Object?;

/// Derived aggregates only. An enum rather than a pattern so a primitive cannot widen
/// its own disclosure surface by inventing a name. The determination itself is not a
/// category: it is always in `assessment` when the result is `attested`. Raw
/// behavioral samples and fingerprints are out of scope for 0.1.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DisclosureCategory(String value) {
  static const DisclosureCategory document = DisclosureCategory('document');
  static const DisclosureCategory timeline = DisclosureCategory('timeline');
  static const DisclosureCategory integrity = DisclosureCategory('integrity');
  static const DisclosureCategory anchors = DisclosureCategory('anchors');

  /// Every value this specification's schema permits.
  static const List<DisclosureCategory> values = <DisclosureCategory>[
    document,
    timeline,
    integrity,
    anchors
  ];
}

/// What a Verifier asks of a Holder: identify an artifact by digest, supply a
/// freshness challenge, and name the disclosure categories wanted. The outer document
/// members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the
/// framework — SPEC §6.3.
class Payload {
  const Payload({
    required this.artifact,
    required this.challenge,
    this.disclosure,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        artifact: Artifact.fromJson(json['artifact'] as Map<String, dynamic>),
        challenge: json['challenge'] as String,
        disclosure: json['disclosure'] == null
            ? null
            : (json['disclosure'] as List<dynamic>)
                .map((e) => DisclosureCategory(e as String))
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Artifact artifact;

  /// Verifier-supplied freshness nonce, echoed verbatim in the response. The floor is 16
  /// characters because anti-replay is the only integrity property the request has.
  final String challenge;

  /// Categories the Verifier requests. A Holder MAY return fewer; it MUST NOT return
  /// more.
  final List<DisclosureCategory>? disclosure;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'artifact': artifact.toJson(),
        'challenge': challenge,
        if (disclosure != null)
          'disclosure': disclosure!.map((e) => e.value).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/process-attestation/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/process-attestation/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/process-attestation/0.1","title":"Process Attestation — payload","description":"What a Verifier asks of a Holder: identify an artifact by digest, supply a freshness challenge, and name the disclosure categories wanted. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.","type":"object","additionalProperties":false,"required":["artifact","challenge"],"properties":{"artifact":{"\$ref":"#/\$defs/Artifact"},"challenge":{"type":"string","minLength":16,"maxLength":256,"description":"Verifier-supplied freshness nonce, echoed verbatim in the response. The floor is 16 characters because anti-replay is the only integrity property the request has."},"disclosure":{"type":"array","uniqueItems":true,"items":{"\$ref":"#/\$defs/DisclosureCategory"},"description":"Categories the Verifier requests. A Holder MAY return fewer; it MUST NOT return more."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Process Attestation — response payload","description":"The Holder\'s answer. Failures use trust-task-error, not this shape. Both results share one response type so the envelope carries no observable distinction between them. `result` is the discriminator and stays inside the payload, so one envelope type covers both branches.","unevaluatedProperties":false,"oneOf":[{"type":"object","title":"Attested","additionalProperties":false,"required":["result","challenge","artifact","assessment","receiptReference"],"properties":{"result":{"const":"attested"},"challenge":{"type":"string","minLength":16,"maxLength":256,"description":"The request\'s challenge, echoed verbatim."},"artifact":{"\$ref":"#/\$defs/Artifact"},"ext":{"\$ref":"#/\$defs/Ext"},"assessment":{"\$ref":"#/\$defs/Assessment"},"receiptReference":{"\$ref":"#/\$defs/ReceiptReference"},"disclosed":{"\$ref":"#/\$defs/Disclosed"}}},{"type":"object","title":"Unavailable","additionalProperties":false,"required":["result","challenge","artifact"],"properties":{"result":{"const":"unavailable"},"challenge":{"type":"string","minLength":16,"maxLength":256,"description":"The request\'s challenge, echoed verbatim."},"artifact":{"\$ref":"#/\$defs/Artifact"},"ext":{"\$ref":"#/\$defs/Ext"}}}]},"Artifact":{"type":"object","additionalProperties":false,"required":["digest","algorithm"],"description":"Deterministic identification of the object whose production is in question.","properties":{"digest":{"type":"string","pattern":"^[A-Za-z0-9_-]{43,128}\$","description":"base64url, unpadded."},"algorithm":{"type":"string","enum":["sha-256","sha-384","sha-512","blake3"],"description":"Carried verbatim from the IANA Named Information Hash Algorithm registry, not re-cased — SPEC §4.10 externally-owned values."},"byteLength":{"type":"integer","minimum":0}}},"Assessment":{"type":"object","additionalProperties":false,"required":["primitive","determination"],"description":"Opaque at the task layer. The task standardizes the interaction, not the receipt.","properties":{"primitive":{"type":"string","format":"uri","description":"Specification the receipt is verified under. Names which vocabulary `determination` belongs to.","maxLength":2048},"determination":{"type":"string","minLength":1,"maxLength":128,"description":"Primitive-defined. Deliberately not normalized across primitives: a determination means only what the primitive that produced it defines it to mean."},"confidence":{"type":"integer","minimum":0,"maximum":100,"description":"The primitive\'s confidence in `determination`, as an integer percentage. An integer rather than a fraction because no other specification in this registry puts a floating-point value on the wire: a float\'s textual form is not stable across JSON implementations, which makes byte-exact re-serialization a language-dependent property."}}},"ReceiptReference":{"description":"Tagged union. Inline exists for Verifiers that cannot dereference a URI at verification time.","oneOf":[{"type":"object","additionalProperties":false,"required":["type","uri"],"properties":{"type":{"const":"uri"},"uri":{"type":"string","format":"uri","maxLength":2048},"mediaType":{"type":"string","maxLength":255}}},{"type":"object","additionalProperties":false,"required":["type","mediaType","data"],"properties":{"type":{"const":"inline"},"mediaType":{"type":"string","maxLength":255},"data":{"type":"string","pattern":"^[A-Za-z0-9_-]+\$","description":"base64url, unpadded. Bounded at 256 KiB of encoded text (~192 KiB of receipt): inline exists for the offline case, not as the general transport. A receipt larger than the bound MUST use the `uri` form.","maxLength":262144}}}]},"DisclosureCategory":{"type":"string","enum":["document","timeline","integrity","anchors"],"description":"Derived aggregates only. An enum rather than a pattern so a primitive cannot widen its own disclosure surface by inventing a name. The determination itself is not a category: it is always in `assessment` when the result is `attested`. Raw behavioral samples and fingerprints are out of scope for 0.1."},"Disclosed":{"type":"object","additionalProperties":false,"description":"Only the categories the request asked for.","properties":{"document":{"type":"object","additionalProperties":false,"properties":{"mediaType":{"type":"string","maxLength":255}}},"timeline":{"type":"object","additionalProperties":false,"properties":{"startedAt":{"type":"string","format":"date-time"},"endedAt":{"type":"string","format":"date-time"},"checkpointCount":{"type":"integer","minimum":0}}},"integrity":{"type":"object","additionalProperties":false,"properties":{"chainValid":{"type":"boolean"},"signatureValid":{"type":"boolean"},"keyCustody":{"type":"string","enum":["software","tpm","secureEnclave"]}}},"anchors":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["kind"],"properties":{"kind":{"type":"string","enum":["rfc3161","opentimestamps","roughtime"]},"anchoredAt":{"type":"string","format":"date-time"}}}}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Process Attestation — response payload","description":"The Holder\'s answer. Failures use trust-task-error, not this shape. Both results share one response type so the envelope carries no observable distinction between them. `result` is the discriminator and stays inside the payload, so one envelope type covers both branches.","unevaluatedProperties":false,"oneOf":[{"type":"object","title":"Attested","additionalProperties":false,"required":["result","challenge","artifact","assessment","receiptReference"],"properties":{"result":{"const":"attested"},"challenge":{"type":"string","minLength":16,"maxLength":256,"description":"The request\'s challenge, echoed verbatim."},"artifact":{"\$ref":"#/\$defs/Artifact"},"ext":{"\$ref":"#/\$defs/Ext"},"assessment":{"\$ref":"#/\$defs/Assessment"},"receiptReference":{"\$ref":"#/\$defs/ReceiptReference"},"disclosed":{"\$ref":"#/\$defs/Disclosed"}}},{"type":"object","title":"Unavailable","additionalProperties":false,"required":["result","challenge","artifact"],"properties":{"result":{"const":"unavailable"},"challenge":{"type":"string","minLength":16,"maxLength":256,"description":"The request\'s challenge, echoed verbatim."},"artifact":{"\$ref":"#/\$defs/Artifact"},"ext":{"\$ref":"#/\$defs/Ext"}}}]},"Artifact":{"type":"object","additionalProperties":false,"required":["digest","algorithm"],"description":"Deterministic identification of the object whose production is in question.","properties":{"digest":{"type":"string","pattern":"^[A-Za-z0-9_-]{43,128}\$","description":"base64url, unpadded."},"algorithm":{"type":"string","enum":["sha-256","sha-384","sha-512","blake3"],"description":"Carried verbatim from the IANA Named Information Hash Algorithm registry, not re-cased — SPEC §4.10 externally-owned values."},"byteLength":{"type":"integer","minimum":0}}},"Assessment":{"type":"object","additionalProperties":false,"required":["primitive","determination"],"description":"Opaque at the task layer. The task standardizes the interaction, not the receipt.","properties":{"primitive":{"type":"string","format":"uri","description":"Specification the receipt is verified under. Names which vocabulary `determination` belongs to.","maxLength":2048},"determination":{"type":"string","minLength":1,"maxLength":128,"description":"Primitive-defined. Deliberately not normalized across primitives: a determination means only what the primitive that produced it defines it to mean."},"confidence":{"type":"integer","minimum":0,"maximum":100,"description":"The primitive\'s confidence in `determination`, as an integer percentage. An integer rather than a fraction because no other specification in this registry puts a floating-point value on the wire: a float\'s textual form is not stable across JSON implementations, which makes byte-exact re-serialization a language-dependent property."}}},"ReceiptReference":{"description":"Tagged union. Inline exists for Verifiers that cannot dereference a URI at verification time.","oneOf":[{"type":"object","additionalProperties":false,"required":["type","uri"],"properties":{"type":{"const":"uri"},"uri":{"type":"string","format":"uri","maxLength":2048},"mediaType":{"type":"string","maxLength":255}}},{"type":"object","additionalProperties":false,"required":["type","mediaType","data"],"properties":{"type":{"const":"inline"},"mediaType":{"type":"string","maxLength":255},"data":{"type":"string","pattern":"^[A-Za-z0-9_-]+\$","description":"base64url, unpadded. Bounded at 256 KiB of encoded text (~192 KiB of receipt): inline exists for the offline case, not as the general transport. A receipt larger than the bound MUST use the `uri` form.","maxLength":262144}}}]},"DisclosureCategory":{"type":"string","enum":["document","timeline","integrity","anchors"],"description":"Derived aggregates only. An enum rather than a pattern so a primitive cannot widen its own disclosure surface by inventing a name. The determination itself is not a category: it is always in `assessment` when the result is `attested`. Raw behavioral samples and fingerprints are out of scope for 0.1."},"Disclosed":{"type":"object","additionalProperties":false,"description":"Only the categories the request asked for.","properties":{"document":{"type":"object","additionalProperties":false,"properties":{"mediaType":{"type":"string","maxLength":255}}},"timeline":{"type":"object","additionalProperties":false,"properties":{"startedAt":{"type":"string","format":"date-time"},"endedAt":{"type":"string","format":"date-time"},"checkpointCount":{"type":"integer","minimum":0}}},"integrity":{"type":"object","additionalProperties":false,"properties":{"chainValid":{"type":"boolean"},"signatureValid":{"type":"boolean"},"keyCustody":{"type":"string","enum":["software","tpm","secureEnclave"]}}},"anchors":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["kind"],"properties":{"kind":{"type":"string","enum":["rfc3161","opentimestamps","roughtime"]},"anchoredAt":{"type":"string","format":"date-time"}}}}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
