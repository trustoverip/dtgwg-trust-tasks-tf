// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/push/wake/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Outcome of the wake: `delivered` (push service accepted) or `tokenUnregistered`
/// (dead token; handle dropped — see push/wake:token_unregistered).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus delivered = ResponseStatus('delivered');
  static const ResponseStatus tokenUnregistered =
      ResponseStatus('tokenUnregistered');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[
    delivered,
    tokenUnregistered
  ];
}

/// Push Wake — response payload
class Response {
  const Response({
    required this.status,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        status: ResponseStatus(json['status'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Outcome of the wake: `delivered` (push service accepted) or `tokenUnregistered`
  /// (dead token; handle dropped — see push/wake:token_unregistered).
  final ResponseStatus status;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'status': status.value,
        if (ext != null) 'ext': ext!,
      };
}

/// OPTIONAL. A hint the consumer MAY map to platform priority/alert behavior.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadUrgency(String value) {
  static const PayloadUrgency interactive = PayloadUrgency('interactive');
  static const PayloadUrgency background = PayloadUrgency('background');

  /// Every value this specification's schema permits.
  static const List<PayloadUrgency> values = <PayloadUrgency>[
    interactive,
    background
  ];
}

/// A trigger (the device's mediator or its VTA) asks the push gateway to deliver a
/// contentless wake to a handle. Carries ONLY the push binding's contentless hint
/// fields — never Trust Task content. The gateway authorizes against the handle's
/// allowlist, then fires the doorbell. See the push wake-up binding
/// (https://trusttasks.org/binding/push/0.1).
class Payload {
  const Payload({
    required this.handle,
    required this.v,
    this.mediator,
    this.count,
    this.urgency,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        handle: json['handle'] as String,
        v: json['v'] as int,
        mediator: json['mediator'] as String?,
        count: json['count'] as int?,
        urgency: json['urgency'] == null
            ? null
            : PayloadUrgency(json['urgency'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The opaque gateway-issued handle to wake.
  final String handle;

  /// Push binding wire version.
  final int v;

  /// OPTIONAL. The mediator holding the queued messages, so a multi-mediator consumer
  /// knows which to drain. A hint — echoed into the contentless push.
  final String? mediator;

  /// OPTIONAL. Approximate count of queued messages. Advisory only.
  final int? count;

  /// OPTIONAL. A hint the consumer MAY map to platform priority/alert behavior.
  final PayloadUrgency? urgency;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'handle': handle,
        'v': v,
        if (mediator != null) 'mediator': mediator!,
        if (count != null) 'count': count!,
        if (urgency != null) 'urgency': urgency!.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/push/wake/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/push/wake/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/push/wake/0.2","title":"Push Wake — payload","description":"A trigger (the device\'s mediator or its VTA) asks the push gateway to deliver a contentless wake to a handle. Carries ONLY the push binding\'s contentless hint fields — never Trust Task content. The gateway authorizes against the handle\'s allowlist, then fires the doorbell. See the push wake-up binding (https://trusttasks.org/binding/push/0.1).","type":"object","additionalProperties":false,"required":["handle","v"],"properties":{"handle":{"type":"string","minLength":1,"description":"The opaque gateway-issued handle to wake."},"v":{"type":"integer","const":1,"description":"Push binding wire version."},"mediator":{"type":"string","description":"OPTIONAL. The mediator holding the queued messages, so a multi-mediator consumer knows which to drain. A hint — echoed into the contentless push."},"count":{"type":"integer","minimum":0,"description":"OPTIONAL. Approximate count of queued messages. Advisory only."},"urgency":{"type":"string","enum":["interactive","background"],"description":"OPTIONAL. A hint the consumer MAY map to platform priority/alert behavior."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Push Wake — response payload","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["delivered","tokenUnregistered"],"description":"Outcome of the wake: `delivered` (push service accepted) or `tokenUnregistered` (dead token; handle dropped — see push/wake:token_unregistered)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Push Wake — response payload","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["delivered","tokenUnregistered"],"description":"Outcome of the wake: `delivered` (push service accepted) or `tokenUnregistered` (dead token; handle dropped — see push/wake:token_unregistered)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
