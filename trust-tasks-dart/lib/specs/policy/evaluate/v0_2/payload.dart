// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/policy/evaluate/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// PolicyDecisionDecision is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PolicyDecisionDecision(String value) {
  static const PolicyDecisionDecision allow = PolicyDecisionDecision('allow');
  static const PolicyDecisionDecision deny = PolicyDecisionDecision('deny');
  static const PolicyDecisionDecision requireStepUp =
      PolicyDecisionDecision('requireStepUp');

  /// Every value this specification's schema permits.
  static const List<PolicyDecisionDecision> values = <PolicyDecisionDecision>[
    allow,
    deny,
    requireStepUp
  ];
}

/// When decision == "allow", whether the maintainer should proxy-login or
/// release-for-fill. Default: proxy.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PolicyDecisionMode(String value) {
  static const PolicyDecisionMode proxy = PolicyDecisionMode('proxy');
  static const PolicyDecisionMode fill = PolicyDecisionMode('fill');

  /// Every value this specification's schema permits.
  static const List<PolicyDecisionMode> values = <PolicyDecisionMode>[
    proxy,
    fill
  ];
}

/// PolicyDecisionStepUpMethod is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PolicyDecisionStepUpMethod(String value) {
  static const PolicyDecisionStepUpMethod webauthnUv =
      PolicyDecisionStepUpMethod('webauthnUv');
  static const PolicyDecisionStepUpMethod pushApproval =
      PolicyDecisionStepUpMethod('pushApproval');
  static const PolicyDecisionStepUpMethod totp =
      PolicyDecisionStepUpMethod('totp');

  /// Every value this specification's schema permits.
  static const List<PolicyDecisionStepUpMethod> values =
      <PolicyDecisionStepUpMethod>[webauthnUv, pushApproval, totp];
}

/// When decision == "require_step_up", which method to demand.
class PolicyDecisionStepUp {
  const PolicyDecisionStepUp({
    required this.method,
    this.ttlSeconds,
  });

  /// Read this payload from a decoded JSON object.
  factory PolicyDecisionStepUp.fromJson(Map<String, dynamic> json) =>
      PolicyDecisionStepUp(
        method: PolicyDecisionStepUpMethod(json['method'] as String),
        ttlSeconds: json['ttlSeconds'] as int?,
      );

  final PolicyDecisionStepUpMethod method;
  final int? ttlSeconds;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'method': method.value,
        if (ttlSeconds != null) 'ttlSeconds': ttlSeconds!,
      };
}

/// PolicyDecision
class PolicyDecision {
  const PolicyDecision({
    required this.decision,
    this.mode,
    this.stepUp,
    this.ttlSecondsCap,
    this.explanation,
  });

  /// Read this payload from a decoded JSON object.
  factory PolicyDecision.fromJson(Map<String, dynamic> json) => PolicyDecision(
        decision: PolicyDecisionDecision(json['decision'] as String),
        mode: json['mode'] == null
            ? null
            : PolicyDecisionMode(json['mode'] as String),
        stepUp: json['stepUp'] == null
            ? null
            : PolicyDecisionStepUp.fromJson(
                json['stepUp'] as Map<String, dynamic>),
        ttlSecondsCap: json['ttlSecondsCap'] as int?,
        explanation: json['explanation'] as String?,
      );

  final PolicyDecisionDecision decision;

  /// When decision == "allow", whether the maintainer should proxy-login or
  /// release-for-fill. Default: proxy.
  final PolicyDecisionMode? mode;

  /// When decision == "require_step_up", which method to demand.
  final PolicyDecisionStepUp? stepUp;

  /// When decision == "allow", maximum lifetime of the issued session blob / released
  /// secret.
  final int? ttlSecondsCap;

  /// Human-readable explanation for diagnostic display.
  final String? explanation;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'decision': decision.value,
        if (mode != null) 'mode': mode!.value,
        if (stepUp != null) 'stepUp': stepUp!.toJson(),
        if (ttlSecondsCap != null) 'ttlSecondsCap': ttlSecondsCap!,
        if (explanation != null) 'explanation': explanation!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Policy Evaluate — response payload
class Response {
  const Response({
    required this.decision,
    this.matchedPolicies,
    this.trace,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        decision:
            PolicyDecision.fromJson(json['decision'] as Map<String, dynamic>),
        matchedPolicies: json['matchedPolicies'] == null
            ? null
            : (json['matchedPolicies'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        trace: json['trace'] == null
            ? null
            : (json['trace'] as List<dynamic>).map((e) => e as String).toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final PolicyDecision decision;

  /// Ids (or `candidate` for the dry-run module) of the policies that returned a
  /// non-null decision, in evaluation order. The first is the winning policy.
  final List<String>? matchedPolicies;

  /// Rego evaluator trace lines when `includeTrace: true`. Maintainer-defined format;
  /// primarily for human debugging.
  final List<String>? trace;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'decision': decision.toJson(),
        if (matchedPolicies != null) 'matchedPolicies': matchedPolicies!,
        if (trace != null) 'trace': trace!,
        if (ext != null) 'ext': ext!,
      };
}

/// A single binding target for a vault entry. Tagged union over the discriminator
/// `kind`. A VaultEntry's `targets` array MAY mix any number of these.
typedef SiteTarget = Object?;

/// Discriminator: is this consumer a user-driven Companion or a headless Service?
typedef ConsumerKind = Object?;

/// PolicyInputRequestKind is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PolicyInputRequestKind(String value) {
  static const PolicyInputRequestKind proxyLogin =
      PolicyInputRequestKind('proxyLogin');
  static const PolicyInputRequestKind release =
      PolicyInputRequestKind('release');
  static const PolicyInputRequestKind stepUpResponse =
      PolicyInputRequestKind('stepUpResponse');

  /// Every value this specification's schema permits.
  static const List<PolicyInputRequestKind> values = <PolicyInputRequestKind>[
    proxyLogin,
    release,
    stepUpResponse
  ];
}

/// PolicyInputRequest, generated from its schema.
class PolicyInputRequest {
  const PolicyInputRequest({
    required this.kind,
  });

  /// Read this payload from a decoded JSON object.
  factory PolicyInputRequest.fromJson(Map<String, dynamic> json) =>
      PolicyInputRequest(
        kind: PolicyInputRequestKind(json['kind'] as String),
      );

  final PolicyInputRequestKind kind;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind.value,
      };
}

/// PolicyInputConsumerNetworkClass is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PolicyInputConsumerNetworkClass(String value) {
  static const PolicyInputConsumerNetworkClass unknown =
      PolicyInputConsumerNetworkClass('unknown');
  static const PolicyInputConsumerNetworkClass home =
      PolicyInputConsumerNetworkClass('home');
  static const PolicyInputConsumerNetworkClass corp =
      PolicyInputConsumerNetworkClass('corp');
  static const PolicyInputConsumerNetworkClass public =
      PolicyInputConsumerNetworkClass('public');
  static const PolicyInputConsumerNetworkClass vpn =
      PolicyInputConsumerNetworkClass('vpn');

  /// Every value this specification's schema permits.
  static const List<PolicyInputConsumerNetworkClass> values =
      <PolicyInputConsumerNetworkClass>[unknown, home, corp, public, vpn];
}

/// PolicyInputConsumer, generated from its schema.
class PolicyInputConsumer {
  const PolicyInputConsumer({
    required this.did,
    this.kind,
    this.deviceId,
    this.lastUserVerificationAt,
    this.networkClass,
  });

  /// Read this payload from a decoded JSON object.
  factory PolicyInputConsumer.fromJson(Map<String, dynamic> json) =>
      PolicyInputConsumer(
        did: json['did'] as String,
        kind: json['kind'],
        deviceId: json['deviceId'] as String?,
        lastUserVerificationAt: json['lastUserVerificationAt'] as String?,
        networkClass: json['networkClass'] == null
            ? null
            : PolicyInputConsumerNetworkClass(json['networkClass'] as String),
      );

  final String did;
  final ConsumerKind? kind;
  final String? deviceId;
  final String? lastUserVerificationAt;
  final PolicyInputConsumerNetworkClass? networkClass;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (kind != null) 'kind': kind!,
        if (deviceId != null) 'deviceId': deviceId!,
        if (lastUserVerificationAt != null)
          'lastUserVerificationAt': lastUserVerificationAt!,
        if (networkClass != null) 'networkClass': networkClass!.value,
      };
}

/// The structured input fed to a policy evaluator on every vault/proxy-login,
/// vault/release, and policy/evaluate call.
class PolicyInput {
  const PolicyInput({
    required this.request,
    required this.site,
    required this.contextId,
    required this.consumer,
  });

  /// Read this payload from a decoded JSON object.
  factory PolicyInput.fromJson(Map<String, dynamic> json) => PolicyInput(
        request: PolicyInputRequest.fromJson(
            json['request'] as Map<String, dynamic>),
        site: json['site'],
        contextId: json['contextId'] as String,
        consumer: PolicyInputConsumer.fromJson(
            json['consumer'] as Map<String, dynamic>),
      );

  final PolicyInputRequest request;
  final SiteTarget site;
  final String contextId;
  final PolicyInputConsumer consumer;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'request': request.toJson(),
        'site': site,
        'contextId': contextId,
        'consumer': consumer.toJson(),
      };
}

/// Dry-run a policy decision against a synthetic PolicyInput. Returns the policy
/// decision plus a trace of which policy modules matched and which rules fired. Used
/// by the policy-editor UI to verify changes before save and by admins to diagnose
/// unexpected deny/allow outcomes.
class Payload {
  const Payload({
    required this.input,
    this.candidateModule,
    this.candidatePriority,
    this.includeTrace,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        input: PolicyInput.fromJson(json['input'] as Map<String, dynamic>),
        candidateModule: json['candidateModule'] as String?,
        candidatePriority: json['candidatePriority'] as int?,
        includeTrace: json['includeTrace'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final PolicyInput input;

  /// Optional — when supplied, evaluate as if this Rego source were active (e.g. preview
  /// a pending upsert). The candidate is layered into the evaluator at the priority
  /// specified by `candidatePriority` (default 1000) for this call only.
  final String? candidateModule;
  final int? candidatePriority;
  final bool? includeTrace;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'input': input.toJson(),
        if (candidateModule != null) 'candidateModule': candidateModule!,
        if (candidatePriority != null) 'candidatePriority': candidatePriority!,
        if (includeTrace != null) 'includeTrace': includeTrace!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/policy/evaluate/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/policy/evaluate/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/policy/evaluate/0.2","title":"Policy Evaluate — payload","description":"Dry-run a policy decision against a synthetic PolicyInput. Returns the policy decision plus a trace of which policy modules matched and which rules fired. Used by the policy-editor UI to verify changes before save and by admins to diagnose unexpected deny/allow outcomes.","type":"object","additionalProperties":false,"required":["input"],"properties":{"input":{"\$ref":"#/\$defs/PolicyInput"},"candidateModule":{"type":"string","description":"Optional — when supplied, evaluate as if this Rego source were active (e.g. preview a pending upsert). The candidate is layered into the evaluator at the priority specified by `candidatePriority` (default 1000) for this call only."},"candidatePriority":{"type":"integer","minimum":0,"maximum":1000,"default":1000},"includeTrace":{"type":"boolean","default":false},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Policy Evaluate — response payload","type":"object","additionalProperties":false,"required":["decision"],"properties":{"decision":{"\$ref":"#/\$defs/PolicyDecision"},"matchedPolicies":{"type":"array","items":{"type":"string"},"description":"Ids (or `candidate` for the dry-run module) of the policies that returned a non-null decision, in evaluation order. The first is the winning policy."},"trace":{"type":"array","items":{"type":"string","maxLength":1024},"description":"Rego evaluator trace lines when `includeTrace: true`. Maintainer-defined format; primarily for human debugging."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"PolicyDecision":{"title":"PolicyDecision","type":"object","additionalProperties":false,"required":["decision"],"properties":{"decision":{"type":"string","enum":["allow","deny","requireStepUp"]},"mode":{"type":"string","enum":["proxy","fill"],"description":"When decision == \\"allow\\", whether the maintainer should proxy-login or release-for-fill. Default: proxy."},"stepUp":{"type":"object","additionalProperties":false,"required":["method"],"properties":{"method":{"type":"string","enum":["webauthnUv","pushApproval","totp"]},"ttlSeconds":{"type":"integer","minimum":1}},"description":"When decision == \\"require_step_up\\", which method to demand."},"ttlSecondsCap":{"type":"integer","minimum":1,"maximum":86400,"description":"When decision == \\"allow\\", maximum lifetime of the issued session blob / released secret."},"explanation":{"type":"string","maxLength":1024,"description":"Human-readable explanation for diagnostic display."}}},"PolicyInput":{"title":"PolicyInput","description":"The structured input fed to a policy evaluator on every vault/proxy-login, vault/release, and policy/evaluate call.","type":"object","additionalProperties":false,"required":["request","site","contextId","consumer"],"properties":{"request":{"type":"object","additionalProperties":false,"required":["kind"],"properties":{"kind":{"type":"string","enum":["proxyLogin","release","stepUpResponse"]}}},"site":{"\$ref":"#/\$defs/SiteTarget"},"contextId":{"type":"string","minLength":1},"consumer":{"type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string"},"kind":{"\$ref":"#/\$defs/ConsumerKind"},"deviceId":{"type":"string"},"lastUserVerificationAt":{"type":"string","format":"date-time"},"networkClass":{"type":"string","enum":["unknown","home","corp","public","vpn"]}}}}},"ConsumerKind":{"title":"ConsumerKind","description":"Discriminator: is this consumer a user-driven Companion or a headless Service?","oneOf":[{"title":"Companion","type":"object","additionalProperties":false,"required":["kind","formFactor"],"properties":{"kind":{"const":"companion"},"formFactor":{"type":"string","enum":["browser","mobile","desktop"]}}},{"title":"Service","type":"object","additionalProperties":false,"required":["kind","serviceKind"],"properties":{"kind":{"const":"service"},"serviceKind":{"type":"string","enum":["mediator","aiAgent","daemon"]}}}]},"SiteTarget":{"title":"SiteTarget","description":"A single binding target for a vault entry. Tagged union over the discriminator `kind`. A VaultEntry\'s `targets` array MAY mix any number of these.","oneOf":[{"title":"WebOrigin","type":"object","additionalProperties":false,"required":["kind","origin"],"properties":{"kind":{"const":"webOrigin"},"origin":{"type":"string","format":"uri","description":"Web origin per RFC 6454 (scheme + host + optional port), e.g. \\"https://github.com\\". Compared by exact string equality after canonicalisation (lowercase host, default port elided). Consumers wanting subdomain coverage SHOULD add multiple targets, not encode a wildcard."}}},{"title":"Did","type":"object","additionalProperties":false,"required":["kind","did"],"properties":{"kind":{"const":"did"},"did":{"type":"string","minLength":1,"description":"DID identifying the relying party (e.g. did:web:rp.example). The vault maintainer is responsible for any DID resolution required to act on this entry."}}},{"title":"IosApp","type":"object","additionalProperties":false,"required":["kind","bundleId"],"properties":{"kind":{"const":"iosApp"},"bundleId":{"type":"string","minLength":1,"pattern":"^[A-Za-z0-9.-]+\$","description":"iOS bundle identifier in reverse-DNS form (e.g. \\"com.github.stwalkerster.codehub\\"). Compared by exact string equality. Matches when an iOS Companion identifies the requesting app via its bundle id (typically via the OS Credential Manager integration)."},"teamId":{"type":"string","minLength":1,"pattern":"^[A-Z0-9]+\$","description":"Optional Apple Developer Team identifier (10-character alphanumeric). When supplied, the maintainer SHOULD also verify the team id of the requesting app before matching — defense in depth against bundle-id squatting on jailbroken devices."}}},{"title":"AndroidApp","type":"object","additionalProperties":false,"required":["kind","packageName","sha256CertFingerprints"],"properties":{"kind":{"const":"androidApp"},"packageName":{"type":"string","minLength":1,"pattern":"^[A-Za-z][A-Za-z0-9_]*(\\\\.[A-Za-z][A-Za-z0-9_]*)+\$","description":"Android package name in reverse-DNS form (e.g. \\"com.github.android\\")."},"sha256CertFingerprints":{"type":"array","minItems":1,"items":{"type":"string","pattern":"^[0-9A-F]{2}(:[0-9A-F]{2}){31}\$"},"uniqueItems":true,"description":"SHA-256 fingerprints of the app\'s signing certificates, in colon-separated hex (the format `apksigner` and the Play Console emit). At least one fingerprint MUST be present. The maintainer matches when ANY of the provided fingerprints matches the requesting app\'s signature — this supports apps signed by multiple keys (e.g. during certificate rotation via Play App Signing)."}}}]}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Policy Evaluate — response payload","type":"object","additionalProperties":false,"required":["decision"],"properties":{"decision":{"\$ref":"#/\$defs/PolicyDecision"},"matchedPolicies":{"type":"array","items":{"type":"string"},"description":"Ids (or `candidate` for the dry-run module) of the policies that returned a non-null decision, in evaluation order. The first is the winning policy."},"trace":{"type":"array","items":{"type":"string","maxLength":1024},"description":"Rego evaluator trace lines when `includeTrace: true`. Maintainer-defined format; primarily for human debugging."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"PolicyDecision":{"title":"PolicyDecision","type":"object","additionalProperties":false,"required":["decision"],"properties":{"decision":{"type":"string","enum":["allow","deny","requireStepUp"]},"mode":{"type":"string","enum":["proxy","fill"],"description":"When decision == \\"allow\\", whether the maintainer should proxy-login or release-for-fill. Default: proxy."},"stepUp":{"type":"object","additionalProperties":false,"required":["method"],"properties":{"method":{"type":"string","enum":["webauthnUv","pushApproval","totp"]},"ttlSeconds":{"type":"integer","minimum":1}},"description":"When decision == \\"require_step_up\\", which method to demand."},"ttlSecondsCap":{"type":"integer","minimum":1,"maximum":86400,"description":"When decision == \\"allow\\", maximum lifetime of the issued session blob / released secret."},"explanation":{"type":"string","maxLength":1024,"description":"Human-readable explanation for diagnostic display."}}},"PolicyInput":{"title":"PolicyInput","description":"The structured input fed to a policy evaluator on every vault/proxy-login, vault/release, and policy/evaluate call.","type":"object","additionalProperties":false,"required":["request","site","contextId","consumer"],"properties":{"request":{"type":"object","additionalProperties":false,"required":["kind"],"properties":{"kind":{"type":"string","enum":["proxyLogin","release","stepUpResponse"]}}},"site":{"\$ref":"#/\$defs/SiteTarget"},"contextId":{"type":"string","minLength":1},"consumer":{"type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string"},"kind":{"\$ref":"#/\$defs/ConsumerKind"},"deviceId":{"type":"string"},"lastUserVerificationAt":{"type":"string","format":"date-time"},"networkClass":{"type":"string","enum":["unknown","home","corp","public","vpn"]}}}}},"ConsumerKind":{"title":"ConsumerKind","description":"Discriminator: is this consumer a user-driven Companion or a headless Service?","oneOf":[{"title":"Companion","type":"object","additionalProperties":false,"required":["kind","formFactor"],"properties":{"kind":{"const":"companion"},"formFactor":{"type":"string","enum":["browser","mobile","desktop"]}}},{"title":"Service","type":"object","additionalProperties":false,"required":["kind","serviceKind"],"properties":{"kind":{"const":"service"},"serviceKind":{"type":"string","enum":["mediator","aiAgent","daemon"]}}}]},"SiteTarget":{"title":"SiteTarget","description":"A single binding target for a vault entry. Tagged union over the discriminator `kind`. A VaultEntry\'s `targets` array MAY mix any number of these.","oneOf":[{"title":"WebOrigin","type":"object","additionalProperties":false,"required":["kind","origin"],"properties":{"kind":{"const":"webOrigin"},"origin":{"type":"string","format":"uri","description":"Web origin per RFC 6454 (scheme + host + optional port), e.g. \\"https://github.com\\". Compared by exact string equality after canonicalisation (lowercase host, default port elided). Consumers wanting subdomain coverage SHOULD add multiple targets, not encode a wildcard."}}},{"title":"Did","type":"object","additionalProperties":false,"required":["kind","did"],"properties":{"kind":{"const":"did"},"did":{"type":"string","minLength":1,"description":"DID identifying the relying party (e.g. did:web:rp.example). The vault maintainer is responsible for any DID resolution required to act on this entry."}}},{"title":"IosApp","type":"object","additionalProperties":false,"required":["kind","bundleId"],"properties":{"kind":{"const":"iosApp"},"bundleId":{"type":"string","minLength":1,"pattern":"^[A-Za-z0-9.-]+\$","description":"iOS bundle identifier in reverse-DNS form (e.g. \\"com.github.stwalkerster.codehub\\"). Compared by exact string equality. Matches when an iOS Companion identifies the requesting app via its bundle id (typically via the OS Credential Manager integration)."},"teamId":{"type":"string","minLength":1,"pattern":"^[A-Z0-9]+\$","description":"Optional Apple Developer Team identifier (10-character alphanumeric). When supplied, the maintainer SHOULD also verify the team id of the requesting app before matching — defense in depth against bundle-id squatting on jailbroken devices."}}},{"title":"AndroidApp","type":"object","additionalProperties":false,"required":["kind","packageName","sha256CertFingerprints"],"properties":{"kind":{"const":"androidApp"},"packageName":{"type":"string","minLength":1,"pattern":"^[A-Za-z][A-Za-z0-9_]*(\\\\.[A-Za-z][A-Za-z0-9_]*)+\$","description":"Android package name in reverse-DNS form (e.g. \\"com.github.android\\")."},"sha256CertFingerprints":{"type":"array","minItems":1,"items":{"type":"string","pattern":"^[0-9A-F]{2}(:[0-9A-F]{2}){31}\$"},"uniqueItems":true,"description":"SHA-256 fingerprints of the app\'s signing certificates, in colon-separated hex (the format `apksigner` and the Play Console emit). At least one fingerprint MUST be present. The maintainer matches when ANY of the provided fingerprints matches the requesting app\'s signature — this supports apps signed by multiple keys (e.g. during certificate rotation via Play App Signing)."}}}]}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
