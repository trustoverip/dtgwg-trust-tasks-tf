// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/policy/activate/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Policy Activate — response payload
class Response {
  const Response({
    required this.activated,
    required this.purpose,
    this.previousPolicyId,
    this.contextId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        activated: json['activated'] as String,
        purpose: json['purpose'] as String,
        previousPolicyId: json['previousPolicyId'] as String?,
        contextId: json['contextId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The id now active for `purpose` — equal to the request `id`.
  final String activated;

  /// Echoed from the request.
  final String purpose;

  /// The policy id that was active for this (contextId, purpose) immediately before,
  /// deactivated by this call. `null` when no policy was active — the first activation
  /// for the slot. Recorded so an operator can roll back to the prior policy and so the
  /// audit trail links the displacement.
  final String? previousPolicyId;

  /// Echoed from the request when it was supplied.
  final String? contextId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'activated': activated,
        'purpose': purpose,
        if (previousPolicyId != null) 'previousPolicyId': previousPolicyId!,
        if (contextId != null) 'contextId': contextId!,
        if (ext != null) 'ext': ext!,
      };
}

/// Policy Activate — payload
class Payload {
  const Payload({
    required this.id,
    required this.purpose,
    this.contextId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        purpose: json['purpose'] as String,
        contextId: json['contextId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The id of the policy module to make active for `purpose`.
  final String id;

  /// The decision slot this policy becomes authoritative for. At most one policy is
  /// active per (contextId, purpose) at a time; activating this one deactivates whatever
  /// was active before. An opaque maintainer-scoped string — the framework does not
  /// enumerate its values, so each maintainer defines its own set (e.g. a community
  /// governance stage such as `join` or `removal`).
  final String purpose;

  /// Optional trust context the activation is scoped to, for a maintainer that
  /// partitions its active-policy map per context. Omitted = the maintainer's default
  /// (single-context) scope.
  final String? contextId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'purpose': purpose,
        if (contextId != null) 'contextId': contextId!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/policy/activate/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/policy/activate/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/policy/activate/0.1","title":"Policy Activate — payload","type":"object","additionalProperties":false,"required":["id","purpose"],"properties":{"id":{"type":"string","minLength":1,"description":"The id of the policy module to make active for `purpose`."},"purpose":{"type":"string","minLength":1,"maxLength":128,"description":"The decision slot this policy becomes authoritative for. At most one policy is active per (contextId, purpose) at a time; activating this one deactivates whatever was active before. An opaque maintainer-scoped string — the framework does not enumerate its values, so each maintainer defines its own set (e.g. a community governance stage such as `join` or `removal`)."},"contextId":{"type":"string","minLength":1,"description":"Optional trust context the activation is scoped to, for a maintainer that partitions its active-policy map per context. Omitted = the maintainer\'s default (single-context) scope."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Policy Activate — response payload","type":"object","additionalProperties":false,"required":["activated","purpose"],"properties":{"activated":{"type":"string","minLength":1,"description":"The id now active for `purpose` — equal to the request `id`."},"purpose":{"type":"string","minLength":1,"maxLength":128,"description":"Echoed from the request."},"previousPolicyId":{"type":["string","null"],"description":"The policy id that was active for this (contextId, purpose) immediately before, deactivated by this call. `null` when no policy was active — the first activation for the slot. Recorded so an operator can roll back to the prior policy and so the audit trail links the displacement."},"contextId":{"type":"string","minLength":1,"description":"Echoed from the request when it was supplied."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Policy Activate — response payload","type":"object","additionalProperties":false,"required":["activated","purpose"],"properties":{"activated":{"type":"string","minLength":1,"description":"The id now active for `purpose` — equal to the request `id`."},"purpose":{"type":"string","minLength":1,"maxLength":128,"description":"Echoed from the request."},"previousPolicyId":{"type":["string","null"],"description":"The policy id that was active for this (contextId, purpose) immediately before, deactivated by this call. `null` when no policy was active — the first activation for the slot. Recorded so an operator can roll back to the prior policy and so the audit trail links the displacement."},"contextId":{"type":"string","minLength":1,"description":"Echoed from the request when it was supplied."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
