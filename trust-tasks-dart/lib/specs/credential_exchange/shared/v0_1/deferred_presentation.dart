// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/credential-exchange/_shared/0.1/deferred-presentation.schema.json

/// One held credential a deferred query asked for, and the claims of it that would be
/// disclosed.
class RequestedCredential {
  const RequestedCredential({
    required this.credentialQueryId,
    required this.credentialId,
    required this.claims,
  });

  /// Read this payload from a decoded JSON object.
  factory RequestedCredential.fromJson(Map<String, dynamic> json) =>
      RequestedCredential(
        credentialQueryId: json['credentialQueryId'] as String,
        credentialId: json['credentialId'] as String,
        claims:
            (json['claims'] as List<dynamic>).map((e) => e as String).toList(),
      );

  /// The DCQL `credential_query_id` this held credential satisfied.
  final String credentialQueryId;

  /// The held credential that would satisfy it.
  final String credentialId;

  /// The claims the query asks to disclose from this credential. An empty array means
  /// the query matched the credential without naming claims — a consumer SHOULD treat
  /// that as a request for the whole credential and present it to the approver as such.
  final List<String> claims;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentialQueryId': credentialQueryId,
        'credentialId': credentialId,
        'claims': claims,
      };
}

/// One presentation request awaiting the holder's decision, as the approver sees it.
/// This is deliberately **not** the stored record. A consumer also retains the
/// original DCQL query so an approval can re-present byte-faithfully against the
/// verifier's original nonce; that is machinery, not a decision input, and is not
/// exposed here. What is exposed is exactly what an approver needs to answer "should I
/// disclose this": who is asking, why, and precisely which claims of which held
/// credentials would leave the wallet.
class DeferredPresentation {
  const DeferredPresentation({
    required this.id,
    required this.verifierDid,
    required this.requested,
    required this.purpose,
    required this.createdAt,
    required this.expiresAt,
  });

  /// Read this payload from a decoded JSON object.
  factory DeferredPresentation.fromJson(Map<String, dynamic> json) =>
      DeferredPresentation(
        id: json['id'] as String,
        verifierDid: json['verifierDid'] as String,
        requested: (json['requested'] as List<dynamic>)
            .map((e) => RequestedCredential.fromJson(e as Map<String, dynamic>))
            .toList(),
        purpose: json['purpose'] as String,
        createdAt: json['createdAt'] as String,
        expiresAt: json['expiresAt'] as String,
      );

  /// Approval handle for this deferral — the value `pending/approve` and `pending/deny`
  /// act on.
  final String id;

  /// The verifier that asked. An approved presentation binds to this audience, so
  /// approving is a decision about *this* party and not a standing permission.
  final String verifierDid;

  /// Every held credential the query would disclose, resolved against what the holder
  /// actually holds. This is the authorization surface: the approver is consenting to
  /// these claims leaving the wallet, not to the query in the abstract.
  final List<RequestedCredential> requested;

  /// The verifier's stated reason, carried through from the query. Purpose binding: an
  /// approver decides against a stated why, never a bare request.
  final String purpose;

  /// When the deferral was recorded.
  final String createdAt;

  /// After this the deferral is stale and approval MUST refuse — the verifier's nonce is
  /// no longer fresh, so any presentation minted against it would fail the verifier's
  /// own replay check.
  final String expiresAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'verifierDid': verifierDid,
        'requested': requested.map((e) => e.toJson()).toList(),
        'purpose': purpose,
        'createdAt': createdAt,
        'expiresAt': expiresAt,
      };
}
