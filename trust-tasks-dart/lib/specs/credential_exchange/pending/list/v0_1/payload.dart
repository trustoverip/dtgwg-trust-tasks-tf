// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/credential-exchange/pending/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// One held credential a deferred query asked for, and the claims of it that would be
/// disclosed.
class RequestedCredential {
  const RequestedCredential({
    required this.credentialQueryId,
    required this.credentialId,
    required this.claims,
  });

  /// Read this payload from a decoded JSON object.
  factory RequestedCredential.fromJson(Map<String, dynamic> json) =>
      RequestedCredential(
        credentialQueryId: json['credentialQueryId'] as String,
        credentialId: json['credentialId'] as String,
        claims:
            (json['claims'] as List<dynamic>).map((e) => e as String).toList(),
      );

  /// The DCQL `credential_query_id` this held credential satisfied.
  final String credentialQueryId;

  /// The held credential that would satisfy it.
  final String credentialId;

  /// The claims the query asks to disclose from this credential. An empty array means
  /// the query matched the credential without naming claims — a consumer SHOULD treat
  /// that as a request for the whole credential and present it to the approver as such.
  final List<String> claims;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentialQueryId': credentialQueryId,
        'credentialId': credentialId,
        'claims': claims,
      };
}

/// One presentation request awaiting the holder's decision, as the approver sees it.
/// This is deliberately **not** the stored record. A consumer also retains the
/// original DCQL query so an approval can re-present byte-faithfully against the
/// verifier's original nonce; that is machinery, not a decision input, and is not
/// exposed here. What is exposed is exactly what an approver needs to answer "should I
/// disclose this": who is asking, why, and precisely which claims of which held
/// credentials would leave the wallet.
class DeferredPresentation {
  const DeferredPresentation({
    required this.id,
    required this.verifierDid,
    required this.requested,
    required this.purpose,
    required this.createdAt,
    required this.expiresAt,
  });

  /// Read this payload from a decoded JSON object.
  factory DeferredPresentation.fromJson(Map<String, dynamic> json) =>
      DeferredPresentation(
        id: json['id'] as String,
        verifierDid: json['verifierDid'] as String,
        requested: (json['requested'] as List<dynamic>)
            .map((e) => RequestedCredential.fromJson(e as Map<String, dynamic>))
            .toList(),
        purpose: json['purpose'] as String,
        createdAt: json['createdAt'] as String,
        expiresAt: json['expiresAt'] as String,
      );

  /// Approval handle for this deferral — the value `pending/approve` and `pending/deny`
  /// act on.
  final String id;

  /// The verifier that asked. An approved presentation binds to this audience, so
  /// approving is a decision about *this* party and not a standing permission.
  final String verifierDid;

  /// Every held credential the query would disclose, resolved against what the holder
  /// actually holds. This is the authorization surface: the approver is consenting to
  /// these claims leaving the wallet, not to the query in the abstract.
  final List<RequestedCredential> requested;

  /// The verifier's stated reason, carried through from the query. Purpose binding: an
  /// approver decides against a stated why, never a bare request.
  final String purpose;

  /// When the deferral was recorded.
  final String createdAt;

  /// After this the deferral is stale and approval MUST refuse — the verifier's nonce is
  /// no longer fresh, so any presentation minted against it would fail the verifier's
  /// own replay check.
  final String expiresAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'verifierDid': verifierDid,
        'requested': requested.map((e) => e.toJson()).toList(),
        'purpose': purpose,
        'createdAt': createdAt,
        'expiresAt': expiresAt,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Credential Exchange Pending List — response payload
class Response {
  const Response({
    required this.pending,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        pending: (json['pending'] as List<dynamic>)
            .map(
                (e) => DeferredPresentation.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The actionable deferrals. A consumer MUST omit records that are already terminal or
  /// past their `expiresAt` — they cannot be approved, so listing them would offer the
  /// approver a decision that is guaranteed to fail. An empty array means nothing is
  /// awaiting a decision.
  final List<DeferredPresentation> pending;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'pending': pending.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// List the presentation requests this holder deferred for consent and that can still
/// be acted on. Takes no parameters: the caller's authenticated identity is the scope,
/// and a holder agent answers only for its own wallet.
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/credential-exchange/pending/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/credential-exchange/pending/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/credential-exchange/pending/list/0.1","title":"Credential Exchange Pending List — payload","description":"List the presentation requests this holder deferred for consent and that can still be acted on. Takes no parameters: the caller\'s authenticated identity is the scope, and a holder agent answers only for its own wallet.","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Credential Exchange Pending List — response payload","type":"object","additionalProperties":false,"required":["pending"],"properties":{"pending":{"type":"array","items":{"\$ref":"#/\$defs/DeferredPresentation"},"description":"The actionable deferrals. A consumer MUST omit records that are already terminal or past their `expiresAt` — they cannot be approved, so listing them would offer the approver a decision that is guaranteed to fail. An empty array means nothing is awaiting a decision."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DeferredPresentation":{"\$anchor":"deferredPresentation","title":"DeferredPresentation","description":"One presentation request awaiting the holder\'s decision, as the approver sees it.\\n\\nThis is deliberately **not** the stored record. A consumer also retains the original DCQL query so an approval can re-present byte-faithfully against the verifier\'s original nonce; that is machinery, not a decision input, and is not exposed here. What is exposed is exactly what an approver needs to answer \\"should I disclose this\\": who is asking, why, and precisely which claims of which held credentials would leave the wallet.","type":"object","additionalProperties":false,"required":["id","verifierDid","requested","purpose","createdAt","expiresAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Approval handle for this deferral — the value `pending/approve` and `pending/deny` act on."},"verifierDid":{"type":"string","minLength":1,"description":"The verifier that asked. An approved presentation binds to this audience, so approving is a decision about *this* party and not a standing permission."},"requested":{"type":"array","items":{"\$ref":"#/\$defs/RequestedCredential"},"description":"Every held credential the query would disclose, resolved against what the holder actually holds. This is the authorization surface: the approver is consenting to these claims leaving the wallet, not to the query in the abstract."},"purpose":{"type":"string","minLength":1,"maxLength":500,"description":"The verifier\'s stated reason, carried through from the query. Purpose binding: an approver decides against a stated why, never a bare request."},"createdAt":{"type":"string","format":"date-time","description":"When the deferral was recorded."},"expiresAt":{"type":"string","format":"date-time","description":"After this the deferral is stale and approval MUST refuse — the verifier\'s nonce is no longer fresh, so any presentation minted against it would fail the verifier\'s own replay check."}}},"RequestedCredential":{"\$anchor":"requestedCredential","title":"RequestedCredential","description":"One held credential a deferred query asked for, and the claims of it that would be disclosed.","type":"object","additionalProperties":false,"required":["credentialQueryId","credentialId","claims"],"properties":{"credentialQueryId":{"type":"string","minLength":1,"description":"The DCQL `credential_query_id` this held credential satisfied."},"credentialId":{"type":"string","minLength":1,"description":"The held credential that would satisfy it."},"claims":{"type":"array","items":{"type":"string","minLength":1},"description":"The claims the query asks to disclose from this credential. An empty array means the query matched the credential without naming claims — a consumer SHOULD treat that as a request for the whole credential and present it to the approver as such."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Credential Exchange Pending List — response payload","type":"object","additionalProperties":false,"required":["pending"],"properties":{"pending":{"type":"array","items":{"\$ref":"#/\$defs/DeferredPresentation"},"description":"The actionable deferrals. A consumer MUST omit records that are already terminal or past their `expiresAt` — they cannot be approved, so listing them would offer the approver a decision that is guaranteed to fail. An empty array means nothing is awaiting a decision."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DeferredPresentation":{"\$anchor":"deferredPresentation","title":"DeferredPresentation","description":"One presentation request awaiting the holder\'s decision, as the approver sees it.\\n\\nThis is deliberately **not** the stored record. A consumer also retains the original DCQL query so an approval can re-present byte-faithfully against the verifier\'s original nonce; that is machinery, not a decision input, and is not exposed here. What is exposed is exactly what an approver needs to answer \\"should I disclose this\\": who is asking, why, and precisely which claims of which held credentials would leave the wallet.","type":"object","additionalProperties":false,"required":["id","verifierDid","requested","purpose","createdAt","expiresAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Approval handle for this deferral — the value `pending/approve` and `pending/deny` act on."},"verifierDid":{"type":"string","minLength":1,"description":"The verifier that asked. An approved presentation binds to this audience, so approving is a decision about *this* party and not a standing permission."},"requested":{"type":"array","items":{"\$ref":"#/\$defs/RequestedCredential"},"description":"Every held credential the query would disclose, resolved against what the holder actually holds. This is the authorization surface: the approver is consenting to these claims leaving the wallet, not to the query in the abstract."},"purpose":{"type":"string","minLength":1,"maxLength":500,"description":"The verifier\'s stated reason, carried through from the query. Purpose binding: an approver decides against a stated why, never a bare request."},"createdAt":{"type":"string","format":"date-time","description":"When the deferral was recorded."},"expiresAt":{"type":"string","format":"date-time","description":"After this the deferral is stale and approval MUST refuse — the verifier\'s nonce is no longer fresh, so any presentation minted against it would fail the verifier\'s own replay check."}}},"RequestedCredential":{"\$anchor":"requestedCredential","title":"RequestedCredential","description":"One held credential a deferred query asked for, and the claims of it that would be disclosed.","type":"object","additionalProperties":false,"required":["credentialQueryId","credentialId","claims"],"properties":{"credentialQueryId":{"type":"string","minLength":1,"description":"The DCQL `credential_query_id` this held credential satisfied."},"credentialId":{"type":"string","minLength":1,"description":"The held credential that would satisfy it."},"claims":{"type":"array","items":{"type":"string","minLength":1},"description":"The claims the query asks to disclose from this credential. An empty array means the query matched the credential without naming claims — a consumer SHOULD treat that as a request for the whole credential and present it to the approver as such."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
