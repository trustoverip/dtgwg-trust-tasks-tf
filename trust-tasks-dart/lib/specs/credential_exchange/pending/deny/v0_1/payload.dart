// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/credential-exchange/pending/deny/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Terminal status. A single-valued enum rather than a bare string: a denial has
/// exactly one outcome, and leaving the field open would invite consumers to infer
/// meaning from free text.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus denied = ResponseStatus('denied');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[denied];
}

/// Credential Exchange Pending Deny — response payload
class Response {
  const Response({
    required this.id,
    required this.status,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        id: json['id'] as String,
        status: ResponseStatus(json['status'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The deferral that was refused.
  final String id;

  /// Terminal status. A single-valued enum rather than a bare string: a denial has
  /// exactly one outcome, and leaving the field open would invite consumers to infer
  /// meaning from free text.
  final ResponseStatus status;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'status': status.value,
        if (ext != null) 'ext': ext!,
      };
}

/// Refuse a deferred presentation request. Nothing is presented and the deferral
/// becomes terminal.
class Payload {
  const Payload({
    required this.id,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The deferral to refuse, as returned by credential-exchange/pending/list.
  final String id;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/credential-exchange/pending/deny/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/credential-exchange/pending/deny/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/credential-exchange/pending/deny/0.1","title":"Credential Exchange Pending Deny — payload","description":"Refuse a deferred presentation request. Nothing is presented and the deferral becomes terminal.","type":"object","additionalProperties":false,"required":["id"],"properties":{"id":{"type":"string","minLength":1,"description":"The deferral to refuse, as returned by credential-exchange/pending/list."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Credential Exchange Pending Deny — response payload","type":"object","additionalProperties":false,"required":["id","status"],"properties":{"id":{"type":"string","minLength":1,"description":"The deferral that was refused."},"status":{"type":"string","enum":["denied"],"description":"Terminal status. A single-valued enum rather than a bare string: a denial has exactly one outcome, and leaving the field open would invite consumers to infer meaning from free text."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Credential Exchange Pending Deny — response payload","type":"object","additionalProperties":false,"required":["id","status"],"properties":{"id":{"type":"string","minLength":1,"description":"The deferral that was refused."},"status":{"type":"string","enum":["denied"],"description":"Terminal status. A single-valued enum rather than a bare string: a denial has exactly one outcome, and leaving the field open would invite consumers to infer meaning from free text."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
