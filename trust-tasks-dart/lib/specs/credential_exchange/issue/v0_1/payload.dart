// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/credential-exchange/issue/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Issuer to holder: the issued credential. Exactly one of `credential_response`
/// (cleartext, known holder over an authenticated channel) or `sealed` (an armored
/// sealed-transfer bundle, for a secret-bearing credential or an unknown holder).
class Payload {
  const Payload({
    this.credentialResponse,
    this.sealed,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        credentialResponse:
            json['credential_response'] as Map<String, dynamic>?,
        sealed: json['sealed'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A cleartext OID4VCI Credential Response, carried verbatim. Format-agnostic: the
  /// `credential` within is a JSON string (SD-JWT-VC compact serialization) or a JSON
  /// object (a W3C Data-Integrity VC with its proof), and the holder infers the format
  /// from the value's shape rather than a separate discriminator. snake_case member
  /// names are OID4VCI's own.
  final Map<String, dynamic>? credentialResponse;

  /// An armored sealed-transfer bundle encrypted to the holder. Used when the credential
  /// is secret-bearing or the holder is not yet known (invite / air-gap). Only the
  /// holder can open it, and an out-of-band digest pins integrity — there is no
  /// trust-on-first-use.
  final String? sealed;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (credentialResponse != null)
          'credential_response': credentialResponse!,
        if (sealed != null) 'sealed': sealed!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/credential-exchange/issue/0.1';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/credential-exchange/issue/0.1","title":"Credential Exchange Issue — payload","description":"Issuer to holder: the issued credential. Exactly one of `credential_response` (cleartext, known holder over an authenticated channel) or `sealed` (an armored sealed-transfer bundle, for a secret-bearing credential or an unknown holder).","type":"object","additionalProperties":false,"oneOf":[{"required":["credential_response"],"not":{"required":["sealed"]}},{"required":["sealed"],"not":{"required":["credential_response"]}}],"properties":{"credential_response":{"type":"object","description":"A cleartext OID4VCI Credential Response, carried verbatim. Format-agnostic: the `credential` within is a JSON string (SD-JWT-VC compact serialization) or a JSON object (a W3C Data-Integrity VC with its proof), and the holder infers the format from the value\'s shape rather than a separate discriminator. snake_case member names are OID4VCI\'s own."},"sealed":{"type":"string","minLength":1,"description":"An armored sealed-transfer bundle encrypted to the holder. Used when the credential is secret-bearing or the holder is not yet known (invite / air-gap). Only the holder can open it, and an out-of-band digest pins integrity — there is no trust-on-first-use."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);
