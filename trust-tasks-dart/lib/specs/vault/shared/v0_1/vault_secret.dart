// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vault/_shared/0.1/vault-secret.schema.json

/// TotpSeedAlgorithm is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const TotpSeedAlgorithm(String value) {
  static const TotpSeedAlgorithm sha1 = TotpSeedAlgorithm('SHA1');
  static const TotpSeedAlgorithm sha256 = TotpSeedAlgorithm('SHA256');
  static const TotpSeedAlgorithm sha512 = TotpSeedAlgorithm('SHA512');

  /// Every value this specification's schema permits.
  static const List<TotpSeedAlgorithm> values = <TotpSeedAlgorithm>[
    sha1,
    sha256,
    sha512
  ];
}

/// TotpSeed
class TotpSeed {
  const TotpSeed({
    required this.secret,
    this.algorithm,
    this.digits,
    this.period,
  });

  /// Read this payload from a decoded JSON object.
  factory TotpSeed.fromJson(Map<String, dynamic> json) => TotpSeed(
        secret: json['secret'] as String,
        algorithm: json['algorithm'] == null
            ? null
            : TotpSeedAlgorithm(json['algorithm'] as String),
        digits: json['digits'] as int?,
        period: json['period'] as int?,
      );

  /// Base32 (RFC 4648) shared secret per RFC 6238.
  final String secret;
  final TotpSeedAlgorithm? algorithm;
  final int? digits;

  /// Time-step in seconds.
  final int? period;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'secret': secret,
        if (algorithm != null) 'algorithm': algorithm!.value,
        if (digits != null) 'digits': digits!,
        if (period != null) 'period': period!,
      };
}

/// Wire format for the request body. `json`: `Content-Type: application/json`, body is
/// `{"&lt;usernameField&gt;": ..., "&lt;passwordField&gt;": ..., ...extraFields}`.
/// `form-urlencoded`: `Content-Type: application/x-www-form-urlencoded`, body is the
/// same fields URL-encoded.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PasswordLoginConfigFormat(String value) {
  static const PasswordLoginConfigFormat json =
      PasswordLoginConfigFormat('json');
  static const PasswordLoginConfigFormat formUrlencoded =
      PasswordLoginConfigFormat('form-urlencoded');

  /// Every value this specification's schema permits.
  static const List<PasswordLoginConfigFormat> values =
      <PasswordLoginConfigFormat>[json, formUrlencoded];
}

/// How the maintainer should POST this entry's username + password to the third party
/// during a vault/proxy-login/0.1 call. Modeled to cover the two formats consumer
/// login APIs almost always use (JSON body, URL-encoded form body) plus the field-name
/// flexibility most sites need. Sites that demand a CSRF token fetched in a separate
/// round-trip, multi-step MFA prompts, or anti-bot challenges are out of scope here —
/// those need a richer driver (future spec).
class PasswordLoginConfig {
  const PasswordLoginConfig({
    required this.loginUrl,
    this.format,
    this.usernameField,
    this.passwordField,
    this.totpField,
    this.extraFields,
    this.successStatus,
  });

  /// Read this payload from a decoded JSON object.
  factory PasswordLoginConfig.fromJson(Map<String, dynamic> json) =>
      PasswordLoginConfig(
        loginUrl: json['loginUrl'] as String,
        format: json['format'] == null
            ? null
            : PasswordLoginConfigFormat(json['format'] as String),
        usernameField: json['usernameField'] as String?,
        passwordField: json['passwordField'] as String?,
        totpField: json['totpField'] as String?,
        extraFields: json['extraFields'] == null
            ? null
            : (json['extraFields'] as Map<String, dynamic>)
                .map((k, v) => MapEntry(k, v as String)),
        successStatus: json['successStatus'] == null
            ? null
            : (json['successStatus'] as List<dynamic>)
                .map((e) => e as int)
                .toList(),
      );

  /// Absolute URL the maintainer POSTs the credentials to. MUST be `https://` for any
  /// non-loopback target — maintainers MUST reject `http://` URLs outside the localhost
  /// loopback (defense against credential leakage on transit). The host of this URL also
  /// constrains which Set-Cookie domains the maintainer is willing to accept on the
  /// response (cookies with a domain unrelated to the loginUrl's host SHOULD be
  /// discarded — see `vault/_shared/0.1/session-blob#/$defs/CookieJarEntry.domain`).
  final String loginUrl;

  /// Wire format for the request body. `json`: `Content-Type: application/json`, body is
  /// `{"&lt;usernameField&gt;": ..., "&lt;passwordField&gt;": ..., ...extraFields}`.
  /// `form-urlencoded`: `Content-Type: application/x-www-form-urlencoded`, body is the
  /// same fields URL-encoded.
  final PasswordLoginConfigFormat? format;

  /// Field name carrying the username in the request body. Common alternatives: `email`,
  /// `login`, `userId`.
  final String? usernameField;

  /// Field name carrying the password in the request body.
  final String? passwordField;

  /// Optional field name carrying the TOTP code in the request body. When set AND the
  /// entry's `totp` is populated, the maintainer computes the current 6-digit code and
  /// includes it in the request alongside username + password. Omit for sites that
  /// prompt for TOTP in a separate round-trip (out of scope here).
  final String? totpField;

  /// Optional constant field/value pairs the maintainer MUST include in the request body
  /// alongside the credentials. Use for fixed selectors like `{"grantType": "password"}`
  /// or feature toggles the site expects. Values are constants — anything dynamic (CSRF
  /// tokens, request ids) is out of scope and needs a richer driver.
  final Map<String, String>? extraFields;

  /// HTTP status codes the maintainer treats as login success. Default `\[200, 204\]`. A
  /// response status outside this list MUST cause the maintainer to reject the
  /// proxy-login with `credentialRejected` (when 4xx) or `targetUnreachable` (when 5xx).
  final List<int>? successStatus;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'loginUrl': loginUrl,
        if (format != null) 'format': format!.value,
        if (usernameField != null) 'usernameField': usernameField!,
        if (passwordField != null) 'passwordField': passwordField!,
        if (totpField != null) 'totpField': totpField!,
        if (extraFields != null) 'extraFields': extraFields!,
        if (successStatus != null) 'successStatus': successStatus!,
      };
}

/// Hint for the consumer UI's rendering.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const CustomFieldKind(String value) {
  static const CustomFieldKind text = CustomFieldKind('text');
  static const CustomFieldKind url = CustomFieldKind('url');
  static const CustomFieldKind email = CustomFieldKind('email');
  static const CustomFieldKind phone = CustomFieldKind('phone');
  static const CustomFieldKind number = CustomFieldKind('number');
  static const CustomFieldKind date = CustomFieldKind('date');

  /// Every value this specification's schema permits.
  static const List<CustomFieldKind> values = <CustomFieldKind>[
    text,
    url,
    email,
    phone,
    number,
    date
  ];
}

/// CustomField
class CustomField {
  const CustomField({
    required this.name,
    required this.value,
    this.hidden,
    this.kind,
  });

  /// Read this payload from a decoded JSON object.
  factory CustomField.fromJson(Map<String, dynamic> json) => CustomField(
        name: json['name'] as String,
        value: json['value'] as String,
        hidden: json['hidden'] as bool?,
        kind: json['kind'] == null
            ? null
            : CustomFieldKind(json['kind'] as String),
      );

  /// Field label (also exposed in metadata view as `customFieldNames`).
  final String name;
  final String value;

  /// When true, consumers SHOULD render the value as masked (dotted) by default and
  /// require an explicit reveal action.
  final bool? hidden;

  /// Hint for the consumer UI's rendering.
  final CustomFieldKind? kind;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'value': value,
        if (hidden != null) 'hidden': hidden!,
        if (kind != null) 'kind': kind!.value,
      };
}

/// PasswordSecret
class PasswordSecret {
  const PasswordSecret({
    required this.kind,
    this.username,
    required this.password,
    this.totp,
    this.loginConfig,
    this.secureNotes,
    this.customFields,
  });

  /// Read this payload from a decoded JSON object.
  factory PasswordSecret.fromJson(Map<String, dynamic> json) => PasswordSecret(
        kind: json['kind'],
        username: json['username'] as String?,
        password: json['password'] as String,
        totp: json['totp'] == null
            ? null
            : TotpSeed.fromJson(json['totp'] as Map<String, dynamic>),
        loginConfig: json['loginConfig'] == null
            ? null
            : PasswordLoginConfig.fromJson(
                json['loginConfig'] as Map<String, dynamic>),
        secureNotes: json['secureNotes'] as String?,
        customFields: json['customFields'] == null
            ? null
            : (json['customFields'] as List<dynamic>)
                .map((e) => CustomField.fromJson(e as Map<String, dynamic>))
                .toList(),
      );

  final Object? kind;
  final String? username;
  final String password;
  final TotpSeed? totp;

  /// Optional driver config for `vault/proxy-login/0.1` against a Password-kind entry.
  /// When present, the maintainer performs an HTTP POST against the configured endpoint
  /// with the entry's `username` + `password` and returns the resulting Set-Cookie
  /// headers in the SessionBlob (`cookies\[\]`). When absent, the maintainer rejects
  /// proxy-login for this entry with `notProxyable` — the consumer falls back to
  /// vault/release for a browser-fill flow. Drivers other than HTTP POST (e.g. OAuth
  /// refresh, DIDComm) live on their own secret kinds; this struct is HTTP-POST-only.
  final PasswordLoginConfig? loginConfig;

  /// Sensitive notes (security questions, account recovery context). Distinct from
  /// `notes` on the metadata view — `notes` is non-sensitive, this is.
  final String? secureNotes;
  final List<CustomField>? customFields;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        if (username != null) 'username': username!,
        'password': password,
        if (totp != null) 'totp': totp!.toJson(),
        if (loginConfig != null) 'loginConfig': loginConfig!.toJson(),
        if (secureNotes != null) 'secureNotes': secureNotes!,
        if (customFields != null)
          'customFields': customFields!.map((e) => e.toJson()).toList(),
      };
}

/// COSE algorithm identifier.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PasskeySecretAlgorithm(String value) {
  static const PasskeySecretAlgorithm es256 = PasskeySecretAlgorithm('ES256');
  static const PasskeySecretAlgorithm edDsa = PasskeySecretAlgorithm('EdDSA');
  static const PasskeySecretAlgorithm rs256 = PasskeySecretAlgorithm('RS256');

  /// Every value this specification's schema permits.
  static const List<PasskeySecretAlgorithm> values = <PasskeySecretAlgorithm>[
    es256,
    edDsa,
    rs256
  ];
}

/// PasskeySecret
class PasskeySecret {
  const PasskeySecret({
    required this.kind,
    required this.credentialId,
    required this.privateKey,
    this.algorithm,
    required this.rpId,
    this.userHandle,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory PasskeySecret.fromJson(Map<String, dynamic> json) => PasskeySecret(
        kind: json['kind'],
        credentialId: json['credentialId'] as String,
        privateKey: json['privateKey'] as String,
        algorithm: json['algorithm'] == null
            ? null
            : PasskeySecretAlgorithm(json['algorithm'] as String),
        rpId: json['rpId'] as String,
        userHandle: json['userHandle'] as String?,
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;

  /// Base64url-encoded WebAuthn credential id.
  final String credentialId;

  /// Base64url-encoded private key bytes (algorithm implied by `algorithm`).
  final String privateKey;

  /// COSE algorithm identifier.
  final PasskeySecretAlgorithm? algorithm;

  /// WebAuthn rpId (a domain, e.g. "github.com").
  final String rpId;

  /// Base64url-encoded WebAuthn userHandle.
  final String? userHandle;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'credentialId': credentialId,
        'privateKey': privateKey,
        if (algorithm != null) 'algorithm': algorithm!.value,
        'rpId': rpId,
        if (userHandle != null) 'userHandle': userHandle!,
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// OAuthTokensSecret
class OAuthTokensSecret {
  const OAuthTokensSecret({
    required this.kind,
    required this.provider,
    required this.refreshToken,
    this.accessToken,
    this.accessTokenExpiresAt,
    this.scopes,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory OAuthTokensSecret.fromJson(Map<String, dynamic> json) =>
      OAuthTokensSecret(
        kind: json['kind'],
        provider: json['provider'] as String,
        refreshToken: json['refreshToken'] as String,
        accessToken: json['accessToken'] as String?,
        accessTokenExpiresAt: json['accessTokenExpiresAt'] as String?,
        scopes: json['scopes'] == null
            ? null
            : (json['scopes'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;

  /// Provider identifier ("google", "github", or a maintainer-defined string for
  /// less-common providers).
  final String provider;
  final String refreshToken;
  final String? accessToken;
  final String? accessTokenExpiresAt;
  final List<String>? scopes;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'provider': provider,
        'refreshToken': refreshToken,
        if (accessToken != null) 'accessToken': accessToken!,
        if (accessTokenExpiresAt != null)
          'accessTokenExpiresAt': accessTokenExpiresAt!,
        if (scopes != null) 'scopes': scopes!,
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// DidSelfIssuedSecret
class DidSelfIssuedSecret {
  const DidSelfIssuedSecret({
    required this.kind,
    required this.did,
    required this.signingKeyId,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory DidSelfIssuedSecret.fromJson(Map<String, dynamic> json) =>
      DidSelfIssuedSecret(
        kind: json['kind'],
        did: json['did'] as String,
        signingKeyId: json['signingKeyId'] as String,
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;
  final String did;

  /// Maintainer-internal identifier for the signing key (the key itself is held by the
  /// VTA; this is a pointer).
  final String signingKeyId;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'did': did,
        'signingKeyId': signingKeyId,
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// DidCommPeerSecret
class DidCommPeerSecret {
  const DidCommPeerSecret({
    required this.kind,
    required this.peerDid,
    required this.signingKeyId,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory DidCommPeerSecret.fromJson(Map<String, dynamic> json) =>
      DidCommPeerSecret(
        kind: json['kind'],
        peerDid: json['peerDid'] as String,
        signingKeyId: json['signingKeyId'] as String,
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;
  final String peerDid;
  final String signingKeyId;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'peerDid': peerDid,
        'signingKeyId': signingKeyId,
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// BearerTokenSecret
class BearerTokenSecret {
  const BearerTokenSecret({
    required this.kind,
    required this.token,
    this.headerName,
    this.headerPrefix,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory BearerTokenSecret.fromJson(Map<String, dynamic> json) =>
      BearerTokenSecret(
        kind: json['kind'],
        token: json['token'] as String,
        headerName: json['headerName'] as String?,
        headerPrefix: json['headerPrefix'] as String?,
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;
  final String token;

  /// Name of the HTTP header to carry the token in. Most APIs use "Authorization" with a
  /// "Bearer " prefix; some use "X-Api-Key" or similar.
  final String? headerName;

  /// String to prepend to the token when forming the header value.
  final String? headerPrefix;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'token': token,
        if (headerName != null) 'headerName': headerName!,
        if (headerPrefix != null) 'headerPrefix': headerPrefix!,
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// SshKeySecret
class SshKeySecret {
  const SshKeySecret({
    required this.kind,
    required this.privateKey,
    this.publicKey,
    this.comment,
    this.passphrase,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory SshKeySecret.fromJson(Map<String, dynamic> json) => SshKeySecret(
        kind: json['kind'],
        privateKey: json['privateKey'] as String,
        publicKey: json['publicKey'] as String?,
        comment: json['comment'] as String?,
        passphrase: json['passphrase'] as String?,
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;

  /// PEM-encoded private key (OpenSSH or PKCS#8 format).
  final String privateKey;
  final String? publicKey;
  final String? comment;

  /// Optional passphrase if the private key is itself encrypted.
  final String? passphrase;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'privateKey': privateKey,
        if (publicKey != null) 'publicKey': publicKey!,
        if (comment != null) 'comment': comment!,
        if (passphrase != null) 'passphrase': passphrase!,
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// CustomSecret
class CustomSecret {
  const CustomSecret({
    required this.kind,
    required this.fields,
    this.secureNotes,
  });

  /// Read this payload from a decoded JSON object.
  factory CustomSecret.fromJson(Map<String, dynamic> json) => CustomSecret(
        kind: json['kind'],
        fields: (json['fields'] as List<dynamic>)
            .map((e) => CustomField.fromJson(e as Map<String, dynamic>))
            .toList(),
        secureNotes: json['secureNotes'] as String?,
      );

  final Object? kind;
  final List<CustomField> fields;
  final String? secureNotes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind,
        'fields': fields.map((e) => e.toJson()).toList(),
        if (secureNotes != null) 'secureNotes': secureNotes!,
      };
}

/// Tagged union over `kind` (mirrors VaultEntry.secretKind).
typedef VaultSecret = Object?;
