// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vault/_shared/0.3/session-blob.schema.json

/// CookieJarEntrySameSite is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const CookieJarEntrySameSite(String value) {
  static const CookieJarEntrySameSite strict = CookieJarEntrySameSite('Strict');
  static const CookieJarEntrySameSite lax = CookieJarEntrySameSite('Lax');
  static const CookieJarEntrySameSite none = CookieJarEntrySameSite('None');

  /// Every value this specification's schema permits.
  static const List<CookieJarEntrySameSite> values = <CookieJarEntrySameSite>[
    strict,
    lax,
    none
  ];
}

/// CookieJarEntry
class CookieJarEntry {
  const CookieJarEntry({
    required this.name,
    required this.value,
    required this.domain,
    required this.path,
    this.expires,
    this.secure,
    this.httpOnly,
    this.sameSite,
  });

  /// Read this payload from a decoded JSON object.
  factory CookieJarEntry.fromJson(Map<String, dynamic> json) => CookieJarEntry(
        name: json['name'] as String,
        value: json['value'] as String,
        domain: json['domain'] as String,
        path: json['path'] as String,
        expires: json['expires'] as String?,
        secure: json['secure'] as bool?,
        httpOnly: json['httpOnly'] as bool?,
        sameSite: json['sameSite'] == null
            ? null
            : CookieJarEntrySameSite(json['sameSite'] as String),
      );

  final String name;
  final String value;
  final String domain;
  final String path;
  final String? expires;
  final bool? secure;
  final bool? httpOnly;
  final CookieJarEntrySameSite? sameSite;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'value': value,
        'domain': domain,
        'path': path,
        if (expires != null) 'expires': expires!,
        if (secure != null) 'secure': secure!,
        if (httpOnly != null) 'httpOnly': httpOnly!,
        if (sameSite != null) 'sameSite': sameSite!.value,
      };
}

/// RequestHeader
class RequestHeader {
  const RequestHeader({
    required this.name,
    required this.value,
  });

  /// Read this payload from a decoded JSON object.
  factory RequestHeader.fromJson(Map<String, dynamic> json) => RequestHeader(
        name: json['name'] as String,
        value: json['value'] as String,
      );

  final String name;
  final String value;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'value': value,
      };
}

/// StorageEntry
class StorageEntry {
  const StorageEntry({
    required this.key,
    required this.value,
  });

  /// Read this payload from a decoded JSON object.
  factory StorageEntry.fromJson(Map<String, dynamic> json) => StorageEntry(
        key: json['key'] as String,
        value: json['value'] as String,
      );

  final String key;
  final String value;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'key': key,
        'value': value,
      };
}

/// Tells the Companion when to call back for a refreshed session. `maintainerOnly` =
/// don't refresh on your own. `on401` = call when the third party returns 401.
/// `beforeExpiry` = pre-emptively refresh shortly before `expiresAt`.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const SessionBlobRefreshHint(String value) {
  static const SessionBlobRefreshHint maintainerOnly =
      SessionBlobRefreshHint('maintainerOnly');
  static const SessionBlobRefreshHint on401 = SessionBlobRefreshHint('on401');
  static const SessionBlobRefreshHint beforeExpiry =
      SessionBlobRefreshHint('beforeExpiry');

  /// Every value this specification's schema permits.
  static const List<SessionBlobRefreshHint> values = <SessionBlobRefreshHint>[
    maintainerOnly,
    on401,
    beforeExpiry
  ];
}

/// SessionBlob
class SessionBlob {
  const SessionBlob({
    required this.sessionId,
    required this.expiresAt,
    this.cookies,
    this.headers,
    this.localStorage,
    this.sessionStorage,
    this.bindOrigin,
    this.refreshHint,
  });

  /// Read this payload from a decoded JSON object.
  factory SessionBlob.fromJson(Map<String, dynamic> json) => SessionBlob(
        sessionId: json['sessionId'] as String,
        expiresAt: json['expiresAt'] as String,
        cookies: json['cookies'] == null
            ? null
            : (json['cookies'] as List<dynamic>)
                .map((e) => CookieJarEntry.fromJson(e as Map<String, dynamic>))
                .toList(),
        headers: json['headers'] == null
            ? null
            : (json['headers'] as List<dynamic>)
                .map((e) => RequestHeader.fromJson(e as Map<String, dynamic>))
                .toList(),
        localStorage: json['localStorage'] == null
            ? null
            : (json['localStorage'] as List<dynamic>)
                .map((e) => StorageEntry.fromJson(e as Map<String, dynamic>))
                .toList(),
        sessionStorage: json['sessionStorage'] == null
            ? null
            : (json['sessionStorage'] as List<dynamic>)
                .map((e) => StorageEntry.fromJson(e as Map<String, dynamic>))
                .toList(),
        bindOrigin: json['bindOrigin'] as String?,
        refreshHint: json['refreshHint'] == null
            ? null
            : SessionBlobRefreshHint(json['refreshHint'] as String),
      );

  /// Maintainer-assigned opaque id for this session. Used for revoke/refresh operations
  /// against the maintainer.
  final String sessionId;

  /// Time after which the session material MUST be discarded. The maintainer SHOULD set
  /// this conservatively — short TTL is the primary defence against a compromised
  /// Companion replaying the session beyond its useful lifetime.
  final String expiresAt;

  /// Cookies the Companion injects into the appropriate origin's cookie jar. Order is
  /// significant for sites that set multiple cookies with the same name on different
  /// paths.
  final List<CookieJarEntry>? cookies;

  /// HTTP request headers the Companion attaches to outbound requests to the bound
  /// origin until `expiresAt`. Typically holds `Authorization: Bearer …`.
  final List<RequestHeader>? headers;

  /// Optional localStorage entries to set on the origin. Used by SPAs that store session
  /// material there rather than in cookies.
  final List<StorageEntry>? localStorage;

  /// Optional sessionStorage entries to set on the origin.
  final List<StorageEntry>? sessionStorage;

  /// The web origin this session is for. Companions MUST refuse to inject the session
  /// into any other origin.
  final String? bindOrigin;

  /// Tells the Companion when to call back for a refreshed session. `maintainerOnly` =
  /// don't refresh on your own. `on401` = call when the third party returns 401.
  /// `beforeExpiry` = pre-emptively refresh shortly before `expiresAt`.
  final SessionBlobRefreshHint? refreshHint;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'sessionId': sessionId,
        'expiresAt': expiresAt,
        if (cookies != null)
          'cookies': cookies!.map((e) => e.toJson()).toList(),
        if (headers != null)
          'headers': headers!.map((e) => e.toJson()).toList(),
        if (localStorage != null)
          'localStorage': localStorage!.map((e) => e.toJson()).toList(),
        if (sessionStorage != null)
          'sessionStorage': sessionStorage!.map((e) => e.toJson()).toList(),
        if (bindOrigin != null) 'bindOrigin': bindOrigin!,
        if (refreshHint != null) 'refreshHint': refreshHint!.value,
      };
}
