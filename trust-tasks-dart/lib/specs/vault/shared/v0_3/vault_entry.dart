// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vault/_shared/0.3/vault-entry.schema.json

/// A single binding target for a vault entry. Tagged union over the discriminator
/// `kind`. A VaultEntry's `targets` array MAY mix any number of these.
typedef SiteTarget = Object?;

/// Discriminator for the secret type stored in the entry. Definitions: - `password` —
/// username + password (+ optional TOTP seed). - `passkey` — WebAuthn discoverable
/// credential (private key + rpId + userHandle). - `oauthTokens` — OAuth 2.0 refresh +
/// access token bundle for a specific provider. - `didSelfIssued` — Self-Issued OpenID
/// Provider v2 (SIOP) credential: the entry points at a DID + signing key already
/// managed by the VTA. - `didcommPeer` — DIDComm peer identity used to authenticate
/// against a DIDComm-speaking relying party. - `bearerToken` — opaque bearer token
/// carried in a maintainer-named header (covers API tokens, long-lived JWTs,
/// personal-access tokens). - `sshKey` — SSH private key + comment. - `custom` —
/// arbitrary structured fields; release-time consumer responsible for interpretation.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const SecretKind(String value) {
  static const SecretKind password = SecretKind('password');
  static const SecretKind passkey = SecretKind('passkey');
  static const SecretKind oauthTokens = SecretKind('oauthTokens');
  static const SecretKind didSelfIssued = SecretKind('didSelfIssued');
  static const SecretKind didcommPeer = SecretKind('didcommPeer');
  static const SecretKind bearerToken = SecretKind('bearerToken');
  static const SecretKind sshKey = SecretKind('sshKey');
  static const SecretKind custom = SecretKind('custom');

  /// Every value this specification's schema permits.
  static const List<SecretKind> values = <SecretKind>[
    password,
    passkey,
    oauthTokens,
    didSelfIssued,
    didcommPeer,
    bearerToken,
    sshKey,
    custom
  ];
}

/// A cryptographic digest as a multibase-encoded multihash — the encoding the W3C
/// Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one
/// `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
/// algorithm in-band, so the value is self-describing and the wire format survives an
/// algorithm change without a schema revision; multibase does the same for the base
/// encoding, so a verifier never infers base58 from base64url by context. A bare hex
/// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract
/// and is non-conforming here. This definition constrains the *encoding only*. What
/// the digest is computed over is stated by each referencing field, because it differs
/// legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS)
/// canonicalization, while a digest over an opaque artifact is taken over its bytes. A
/// field whose input is a JSON document and which does not name a canonicalization is
/// not reproducible. Restricted to the two multibase headers W3C Controlled
/// Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u`
/// (base64url-no-pad). CID permits others but states that "interoperability is not
/// guaranteed between implementations using such values", and a registry whose purpose
/// is interoperability should not mint digests a conforming verifier may be unable to
/// read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I
/// and l, and an earlier permissive pattern let three published examples carry digests
/// that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with
/// `did:key` and `did:webvh`.
typedef DigestMultibase = String;

/// AttachmentRef
class AttachmentRef {
  const AttachmentRef({
    required this.id,
    required this.name,
    required this.sizeBytes,
    required this.digestMultibase,
    this.contentType,
  });

  /// Read this payload from a decoded JSON object.
  factory AttachmentRef.fromJson(Map<String, dynamic> json) => AttachmentRef(
        id: json['id'] as String,
        name: json['name'] as String,
        sizeBytes: json['sizeBytes'] as int,
        digestMultibase: json['digestMultibase'] as String,
        contentType: json['contentType'] as String?,
      );

  /// Opaque maintainer-assigned id for this attachment; used to fetch the blob via a
  /// separate mechanism.
  final String id;

  /// User-supplied filename (e.g. "recovery-codes.txt").
  final String name;

  /// Size of the encrypted blob in bytes. Maintainers MAY enforce a maximum per
  /// attachment and per entry.
  final int sizeBytes;

  /// Digest over the encrypted blob bytes (post-encryption), so the consumer can verify
  /// integrity after fetch. Taken over the bytes as stored, not over a canonicalization:
  /// the blob is an opaque artifact, not a JSON document.
  final DigestMultibase digestMultibase;

  /// Optional MIME type hint for the consumer UI (e.g. "text/plain",
  /// "application/x-pem-file").
  final String? contentType;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
        'sizeBytes': sizeBytes,
        'digestMultibase': digestMultibase,
        if (contentType != null) 'contentType': contentType!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VaultEntry
class VaultEntry {
  const VaultEntry({
    required this.id,
    required this.contextId,
    required this.targets,
    required this.label,
    required this.secretKind,
    this.tags,
    this.notes,
    this.favicon,
    this.selectors,
    this.customFieldNames,
    this.attachments,
    this.expiresAt,
    this.breachedAt,
    this.passwordChangedAt,
    required this.createdAt,
    this.createdBy,
    required this.updatedAt,
    this.updatedBy,
    this.lastUsedAt,
    required this.version,
    this.principalDid,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory VaultEntry.fromJson(Map<String, dynamic> json) => VaultEntry(
        id: json['id'] as String,
        contextId: json['contextId'] as String,
        targets: (json['targets'] as List<dynamic>).map((e) => e).toList(),
        label: json['label'] as String,
        secretKind: SecretKind(json['secretKind'] as String),
        tags: json['tags'] == null
            ? null
            : (json['tags'] as List<dynamic>).map((e) => e as String).toList(),
        notes: json['notes'] as String?,
        favicon: json['favicon'] as String?,
        selectors: json['selectors'] == null
            ? null
            : (json['selectors'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        customFieldNames: json['customFieldNames'] == null
            ? null
            : (json['customFieldNames'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        attachments: json['attachments'] == null
            ? null
            : (json['attachments'] as List<dynamic>)
                .map((e) => AttachmentRef.fromJson(e as Map<String, dynamic>))
                .toList(),
        expiresAt: json['expiresAt'] as String?,
        breachedAt: json['breachedAt'] as String?,
        passwordChangedAt: json['passwordChangedAt'] as String?,
        createdAt: json['createdAt'] as String,
        createdBy: json['createdBy'] as String?,
        updatedAt: json['updatedAt'] as String,
        updatedBy: json['updatedBy'] as String?,
        lastUsedAt: json['lastUsedAt'] as String?,
        version: json['version'] as int,
        principalDid: json['principalDid'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque vault-maintainer-assigned identifier for the entry. ULID/UUID/base32 are
  /// common; the wire spec only requires non-empty string equality.
  final String id;

  /// Identifier of the trust context (persona) the entry belongs to. Opaque string
  /// interpreted by the vault maintainer; corresponds to a single ContextRecord on the
  /// VTA side.
  final String contextId;

  /// One or more binding targets — web origins, mobile app identifiers, and/or DIDs —
  /// that this credential applies to. A request from any matching target uses this
  /// entry. A typical entry for a service that exists as both a website and mobile apps
  /// will list a web origin, an iOS bundle id, and an Android package id; passkeys for
  /// that service typically list only the origin (because iOS Associated Domains and
  /// Android Asset Links bind apps to the domain at the OS level).
  final List<SiteTarget> targets;

  /// Human-readable display name (e.g. "Work GitHub", "Personal bank — checking").
  /// Bounded on the wire at 256 characters; a maintainer MAY enforce a shorter limit.
  final String label;

  /// Discriminator for the kind of secret this entry holds. The secret material itself
  /// is NEVER returned in metadata views; the kind is exposed so consumers can render an
  /// appropriate UI affordance and so policy decisions can route by kind.
  final SecretKind secretKind;

  /// User-defined tags for organisation and filtering (e.g. \["family", "finance"\]).
  /// Maintainers MAY enforce a maximum count; the wire spec does not.
  final List<String>? tags;

  /// Non-sensitive notes the user attached to the entry. Visible in metadata view
  /// (suitable for support contact, account number, expiry policy memos). SENSITIVE
  /// notes belong in the secret payload as a `secureNotes` field — those are only
  /// released by vault/release/0.1.
  final String? notes;

  /// Optional URI of an icon to display in the consumer UI. Maintainers MAY fetch and
  /// cache; consumers SHOULD treat as untrusted content and fetch via a sandboxed
  /// pipeline.
  final String? favicon;

  /// Opaque maintainer-defined selector strings fed to the policy engine when this entry
  /// is requested (e.g. "recent_uv_required", "network_class=corp", "step_up_push").
  /// Consumers MUST treat selectors as opaque; they exist for policy authoring on the
  /// maintainer side.
  final List<String>? selectors;

  /// Names of additional fields the user has attached (e.g. \["security-question-1",
  /// "account-number"\]). The VALUES live in the secret payload and are only delivered by
  /// vault/release/0.1. Exposing names in metadata lets the consumer render the right
  /// form layout before requesting release.
  final List<String>? customFieldNames;

  /// References to encrypted blobs associated with the entry (recovery codes, PEM files,
  /// screenshots of authenticator setup). The blobs themselves are fetched via a
  /// separate mechanism the maintainer documents; metadata view exposes only the
  /// descriptor.
  final List<AttachmentRef>? attachments;

  /// Optional time after which the credential is no longer expected to be valid (e.g. an
  /// OAuth refresh token's known expiry, a time-limited API token, an enterprise
  /// password rotation policy). Maintainers MAY surface this in the consumer UI as a
  /// warning.
  final String? expiresAt;

  /// Set by the maintainer (via HIBP integration or equivalent) when the password
  /// material associated with this entry is known to appear in a public breach.
  /// Consumers SHOULD surface this prominently. Cleared when the user rotates the
  /// password and the new password is not in any known breach.
  final String? breachedAt;

  /// Set whenever the password component of the secret payload is rotated. Maintainers
  /// MUST update this on every secret-material change for entries of kind `password` (or
  /// any kind that carries a password component). Used by consumers to surface
  /// rotation-overdue warnings.
  final String? passwordChangedAt;
  final String createdAt;

  /// VID of the consumer that originally created the entry.
  final String? createdBy;
  final String updatedAt;

  /// VID of the consumer that last modified the entry.
  final String? updatedBy;

  /// Most recent time the entry was used (either released or proxy-login performed).
  /// Maintainers MAY return this with reduced precision (e.g. hour-floored) when
  /// releasing to a less-trusted consumer.
  final String? lastUsedAt;

  /// Monotonic version counter incremented on every mutation. Used by consumers for
  /// optimistic-concurrency checks on vault/upsert and as the seq baseline for
  /// vault/sync.
  final int version;

  /// Optional cached DID the entry will act AS for DID-shaped flows — mirrors the `did`
  /// field of the entry's secret payload when `secretKind` carries one (`didSelfIssued`,
  /// `didcommPeer`). Absent for kinds that have no DID concept (`password`, `passkey`,
  /// `oauthTokens`, `bearerToken`, `sshKey`, `custom`). MAINTAINER-DERIVED, NOT
  /// CONSUMER-SUPPLIED: the maintainer MUST recompute this from the canonical secret at
  /// every upsert / secret rotation; a producer-supplied value on `vault/upsert/0.1`
  /// MUST be ignored (no error, but no honour). Read-only on the wire, present in
  /// metadata views so consumers can drive RP-side flows (e.g. fetch `/auth/challenge`
  /// keyed on the principal DID before requesting a proxy-login) without releasing the
  /// secret.
  final String? principalDid;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1. Reverse-DNS-namespaced;
  /// consumers MUST ignore unrecognized namespaces.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'contextId': contextId,
        'targets': targets,
        'label': label,
        'secretKind': secretKind.value,
        if (tags != null) 'tags': tags!,
        if (notes != null) 'notes': notes!,
        if (favicon != null) 'favicon': favicon!,
        if (selectors != null) 'selectors': selectors!,
        if (customFieldNames != null) 'customFieldNames': customFieldNames!,
        if (attachments != null)
          'attachments': attachments!.map((e) => e.toJson()).toList(),
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (breachedAt != null) 'breachedAt': breachedAt!,
        if (passwordChangedAt != null) 'passwordChangedAt': passwordChangedAt!,
        'createdAt': createdAt,
        if (createdBy != null) 'createdBy': createdBy!,
        'updatedAt': updatedAt,
        if (updatedBy != null) 'updatedBy': updatedBy!,
        if (lastUsedAt != null) 'lastUsedAt': lastUsedAt!,
        'version': version,
        if (principalDid != null) 'principalDid': principalDid!,
        if (ext != null) 'ext': ext!,
      };
}
