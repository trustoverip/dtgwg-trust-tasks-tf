// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vault/usage/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// UsageRecordKind is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const UsageRecordKind(String value) {
  static const UsageRecordKind proxyLogin = UsageRecordKind('proxyLogin');
  static const UsageRecordKind release = UsageRecordKind('release');

  /// Every value this specification's schema permits.
  static const List<UsageRecordKind> values = <UsageRecordKind>[
    proxyLogin,
    release
  ];
}

/// UsageRecordOutcome is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const UsageRecordOutcome(String value) {
  static const UsageRecordOutcome allowed = UsageRecordOutcome('allowed');
  static const UsageRecordOutcome denied = UsageRecordOutcome('denied');
  static const UsageRecordOutcome stepUpRequired =
      UsageRecordOutcome('stepUpRequired');
  static const UsageRecordOutcome stepUpSatisfied =
      UsageRecordOutcome('stepUpSatisfied');
  static const UsageRecordOutcome targetUnreachable =
      UsageRecordOutcome('targetUnreachable');
  static const UsageRecordOutcome credentialRejected =
      UsageRecordOutcome('credentialRejected');
  static const UsageRecordOutcome policyDeny = UsageRecordOutcome('policyDeny');

  /// Every value this specification's schema permits.
  static const List<UsageRecordOutcome> values = <UsageRecordOutcome>[
    allowed,
    denied,
    stepUpRequired,
    stepUpSatisfied,
    targetUnreachable,
    credentialRejected,
    policyDeny
  ];
}

/// UsageRecord
class UsageRecord {
  const UsageRecord({
    required this.id,
    required this.entryId,
    required this.contextId,
    required this.consumerDid,
    required this.kind,
    required this.outcome,
    required this.occurredAt,
    this.deviceId,
    this.sessionId,
    this.policyDecisionId,
    this.errorCode,
  });

  /// Read this payload from a decoded JSON object.
  factory UsageRecord.fromJson(Map<String, dynamic> json) => UsageRecord(
        id: json['id'] as String,
        entryId: json['entryId'] as String,
        contextId: json['contextId'] as String,
        consumerDid: json['consumerDid'] as String,
        kind: UsageRecordKind(json['kind'] as String),
        outcome: UsageRecordOutcome(json['outcome'] as String),
        occurredAt: json['occurredAt'] as String,
        deviceId: json['deviceId'] as String?,
        sessionId: json['sessionId'] as String?,
        policyDecisionId: json['policyDecisionId'] as String?,
        errorCode: json['errorCode'] as String?,
      );

  final String id;
  final String entryId;
  final String contextId;

  /// DID of the Companion/Service that initiated the use.
  final String consumerDid;
  final UsageRecordKind kind;
  final UsageRecordOutcome outcome;
  final String occurredAt;
  final String? deviceId;

  /// For successful proxy-logins, the maintainer-assigned session id; absent for
  /// releases and for failures.
  final String? sessionId;

  /// Pointer to the policy evaluation record for forensic detail.
  final String? policyDecisionId;

  /// Trust Task error code when outcome is a failure.
  final String? errorCode;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'entryId': entryId,
        'contextId': contextId,
        'consumerDid': consumerDid,
        'kind': kind.value,
        'outcome': outcome.value,
        'occurredAt': occurredAt,
        if (deviceId != null) 'deviceId': deviceId!,
        if (sessionId != null) 'sessionId': sessionId!,
        if (policyDecisionId != null) 'policyDecisionId': policyDecisionId!,
        if (errorCode != null) 'errorCode': errorCode!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Vault Usage — response payload
class Response {
  const Response({
    required this.uses,
    required this.truncated,
    this.cursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        uses: (json['uses'] as List<dynamic>)
            .map((e) => UsageRecord.fromJson(e as Map<String, dynamic>))
            .toList(),
        truncated: json['truncated'] as bool,
        cursor: json['cursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<UsageRecord> uses;
  final bool truncated;
  final String? cursor;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'uses': uses.map((e) => e.toJson()).toList(),
        'truncated': truncated,
        if (cursor != null) 'cursor': cursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// PayloadKindFilterItem is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadKindFilterItem(String value) {
  static const PayloadKindFilterItem proxyLogin =
      PayloadKindFilterItem('proxyLogin');
  static const PayloadKindFilterItem release = PayloadKindFilterItem('release');

  /// Every value this specification's schema permits.
  static const List<PayloadKindFilterItem> values = <PayloadKindFilterItem>[
    proxyLogin,
    release
  ];
}

/// Consumer queries the maintainer's audit log of recent vault uses (proxy-logins,
/// releases). Drives UIs like "recent activity" and "which AI agent has been using my
/// GitHub credential". Read-only.
class Payload {
  const Payload({
    this.entryId,
    this.contextId,
    this.byConsumer,
    this.since,
    this.until,
    this.kindFilter,
    this.pageSize,
    this.cursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        entryId: json['entryId'] as String?,
        contextId: json['contextId'] as String?,
        byConsumer: json['byConsumer'] as String?,
        since: json['since'] as String?,
        until: json['until'] as String?,
        kindFilter: json['kindFilter'] == null
            ? null
            : (json['kindFilter'] as List<dynamic>)
                .map((e) => PayloadKindFilterItem(e as String))
                .toList(),
        pageSize: json['pageSize'] as int?,
        cursor: json['cursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Optional filter — usage for this entry only.
  final String? entryId;

  /// Optional filter — usage within this trust context only.
  final String? contextId;

  /// Optional filter — usage by this consumer DID only. Useful for "what has AI agent X
  /// been doing on my behalf".
  final String? byConsumer;
  final String? since;
  final String? until;
  final List<PayloadKindFilterItem>? kindFilter;
  final int? pageSize;
  final String? cursor;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (entryId != null) 'entryId': entryId!,
        if (contextId != null) 'contextId': contextId!,
        if (byConsumer != null) 'byConsumer': byConsumer!,
        if (since != null) 'since': since!,
        if (until != null) 'until': until!,
        if (kindFilter != null)
          'kindFilter': kindFilter!.map((e) => e.value).toList(),
        if (pageSize != null) 'pageSize': pageSize!,
        if (cursor != null) 'cursor': cursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vault/usage/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vault/usage/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vault/usage/0.2","title":"Vault Usage — payload","description":"Consumer queries the maintainer\'s audit log of recent vault uses (proxy-logins, releases). Drives UIs like \\"recent activity\\" and \\"which AI agent has been using my GitHub credential\\". Read-only.","type":"object","additionalProperties":false,"properties":{"entryId":{"type":"string","minLength":1,"description":"Optional filter — usage for this entry only."},"contextId":{"type":"string","minLength":1,"description":"Optional filter — usage within this trust context only."},"byConsumer":{"type":"string","minLength":1,"description":"Optional filter — usage by this consumer DID only. Useful for \\"what has AI agent X been doing on my behalf\\"."},"since":{"type":"string","format":"date-time"},"until":{"type":"string","format":"date-time"},"kindFilter":{"type":"array","items":{"type":"string","enum":["proxyLogin","release"]},"uniqueItems":true},"pageSize":{"type":"integer","minimum":1,"maximum":1000},"cursor":{"type":"string"},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Vault Usage — response payload","type":"object","additionalProperties":false,"required":["uses","truncated"],"properties":{"uses":{"type":"array","items":{"\$ref":"#/\$defs/UsageRecord"}},"truncated":{"type":"boolean"},"cursor":{"type":"string"},"ext":{"\$ref":"#/\$defs/Ext"}}},"UsageRecord":{"title":"UsageRecord","type":"object","additionalProperties":false,"required":["id","entryId","contextId","consumerDid","kind","outcome","occurredAt"],"properties":{"id":{"type":"string","minLength":1},"entryId":{"type":"string","minLength":1},"contextId":{"type":"string","minLength":1},"consumerDid":{"type":"string","minLength":1,"description":"DID of the Companion/Service that initiated the use."},"kind":{"type":"string","enum":["proxyLogin","release"]},"outcome":{"type":"string","enum":["allowed","denied","stepUpRequired","stepUpSatisfied","targetUnreachable","credentialRejected","policyDeny"]},"occurredAt":{"type":"string","format":"date-time"},"deviceId":{"type":"string","minLength":1},"sessionId":{"type":"string","minLength":1,"description":"For successful proxy-logins, the maintainer-assigned session id; absent for releases and for failures."},"policyDecisionId":{"type":"string","description":"Pointer to the policy evaluation record for forensic detail."},"errorCode":{"type":"string","description":"Trust Task error code when outcome is a failure."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Vault Usage — response payload","type":"object","additionalProperties":false,"required":["uses","truncated"],"properties":{"uses":{"type":"array","items":{"\$ref":"#/\$defs/UsageRecord"}},"truncated":{"type":"boolean"},"cursor":{"type":"string"},"ext":{"\$ref":"#/\$defs/Ext"}}},"UsageRecord":{"title":"UsageRecord","type":"object","additionalProperties":false,"required":["id","entryId","contextId","consumerDid","kind","outcome","occurredAt"],"properties":{"id":{"type":"string","minLength":1},"entryId":{"type":"string","minLength":1},"contextId":{"type":"string","minLength":1},"consumerDid":{"type":"string","minLength":1,"description":"DID of the Companion/Service that initiated the use."},"kind":{"type":"string","enum":["proxyLogin","release"]},"outcome":{"type":"string","enum":["allowed","denied","stepUpRequired","stepUpSatisfied","targetUnreachable","credentialRejected","policyDeny"]},"occurredAt":{"type":"string","format":"date-time"},"deviceId":{"type":"string","minLength":1},"sessionId":{"type":"string","minLength":1,"description":"For successful proxy-logins, the maintainer-assigned session id; absent for releases and for failures."},"policyDecisionId":{"type":"string","description":"Pointer to the policy evaluation record for forensic detail."},"errorCode":{"type":"string","description":"Trust Task error code when outcome is a failure."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
