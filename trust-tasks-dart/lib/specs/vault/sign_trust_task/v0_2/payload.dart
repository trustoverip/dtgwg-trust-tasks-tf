// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vault/sign-trust-task/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Permissive shape for an unsigned Trust Task document — the framework-required
/// members MUST be present, and `proof` MUST NOT be. The maintainer does not validate
/// the inner `payload` against the embedded `type`'s schema; that validation is the
/// recipient's responsibility.
class UnsignedTrustTaskEnvelope {
  const UnsignedTrustTaskEnvelope({
    required this.id,
    this.threadId,
    required this.type,
    required this.issuer,
    required this.recipient,
    required this.issuedAt,
    this.expiresAt,
    required this.payload,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory UnsignedTrustTaskEnvelope.fromJson(Map<String, dynamic> json) =>
      UnsignedTrustTaskEnvelope(
        id: json['id'] as String,
        threadId: json['threadId'] as String?,
        type: json['type'] as String,
        issuer: json['issuer'] as String,
        recipient: json['recipient'] as String,
        issuedAt: json['issuedAt'] as String,
        expiresAt: json['expiresAt'] as String?,
        payload: json['payload'],
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Envelope identifier. Set by the producer of the inner task.
  final String id;

  /// Optional thread/correlation id, per framework §4.x.
  final String? threadId;

  /// Type URI of the inner Trust Task (e.g.
  /// `https://trusttasks.org/spec/acl/grant/0.1`).
  final String type;

  /// Issuer DID of the inner task. MUST equal the vault entry's principalDid — the
  /// maintainer rejects mismatches with `envelopeIssuerMismatch` rather than
  /// overwriting.
  final String issuer;

  /// Recipient DID — the relying party / audience the signed envelope will be delivered
  /// to.
  final String recipient;

  /// Producer's wall-clock when the inner task was constructed. Maintainer copies
  /// through; the proof's `created` is the maintainer's wall-clock at signing.
  final String issuedAt;

  /// Optional inner-task expiry. The maintainer rejects with `envelopeExpired` when this
  /// is in the past at sign time.
  final String? expiresAt;

  /// Inner task's payload object. Opaque to the maintainer — passed through unchanged
  /// into the signed envelope.
  final Object? payload;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (threadId != null) 'threadId': threadId!,
        'type': type,
        'issuer': issuer,
        'recipient': recipient,
        'issuedAt': issuedAt,
        if (expiresAt != null) 'expiresAt': expiresAt!,
        'payload': payload,
        if (ext != null) 'ext': ext!,
      };
}

/// Vault Sign Trust Task — response payload
class Response {
  const Response({
    required this.signedEnvelope,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        signedEnvelope: json['signedEnvelope'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The supplied `unsignedEnvelope` with a Data Integrity `proof` attached.
  /// `proof.verificationMethod` is `&lt;principalDid&gt;#&lt;signingKeyId&gt;`; `proof.proofPurpose`
  /// is `assertionMethod`; `proof.cryptosuite` is `eddsa-jcs-2022`. All other members of
  /// the envelope (`id`, `type`, `issuer`, `recipient`, `issuedAt`, `expiresAt`,
  /// `payload`, `ext`) are unchanged from the request.
  final Map<String, dynamic> signedEnvelope;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'signedEnvelope': signedEnvelope,
        if (ext != null) 'ext': ext!,
      };
}

/// StepUpProofKind is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const StepUpProofKind(String value) {
  static const StepUpProofKind webauthnUv = StepUpProofKind('webauthnUv');
  static const StepUpProofKind pushApproval = StepUpProofKind('pushApproval');
  static const StepUpProofKind totp = StepUpProofKind('totp');

  /// Every value this specification's schema permits.
  static const List<StepUpProofKind> values = <StepUpProofKind>[
    webauthnUv,
    pushApproval,
    totp
  ];
}

/// StepUpProof
class StepUpProof {
  const StepUpProof({
    required this.kind,
    required this.proof,
    required this.challengeId,
  });

  /// Read this payload from a decoded JSON object.
  factory StepUpProof.fromJson(Map<String, dynamic> json) => StepUpProof(
        kind: StepUpProofKind(json['kind'] as String),
        proof: json['proof'] as String,
        challengeId: json['challengeId'] as String,
      );

  final StepUpProofKind kind;

  /// Format depends on kind: WebAuthn assertion (base64url), DIDComm approval-response
  /// message id, or 6–8-digit TOTP code.
  final String proof;

  /// Maintainer-issued challenge id the proof responds to.
  final String challengeId;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind.value,
        'proof': proof,
        'challengeId': challengeId,
      };
}

/// Producer-supplied network classification. Advisory.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ConsumerContextNetworkClass(String value) {
  static const ConsumerContextNetworkClass unknown =
      ConsumerContextNetworkClass('unknown');
  static const ConsumerContextNetworkClass home =
      ConsumerContextNetworkClass('home');
  static const ConsumerContextNetworkClass corp =
      ConsumerContextNetworkClass('corp');
  static const ConsumerContextNetworkClass public =
      ConsumerContextNetworkClass('public');
  static const ConsumerContextNetworkClass vpn =
      ConsumerContextNetworkClass('vpn');

  /// Every value this specification's schema permits.
  static const List<ConsumerContextNetworkClass> values =
      <ConsumerContextNetworkClass>[unknown, home, corp, public, vpn];
}

/// ConsumerContext
class ConsumerContext {
  const ConsumerContext({
    this.deviceId,
    this.lastUserVerificationAt,
    this.networkClass,
  });

  /// Read this payload from a decoded JSON object.
  factory ConsumerContext.fromJson(Map<String, dynamic> json) =>
      ConsumerContext(
        deviceId: json['deviceId'] as String?,
        lastUserVerificationAt: json['lastUserVerificationAt'] as String?,
        networkClass: json['networkClass'] == null
            ? null
            : ConsumerContextNetworkClass(json['networkClass'] as String),
      );

  /// Device-binding id assigned at registration. The maintainer cross-checks this
  /// against the authenticated transport identity.
  final String? deviceId;

  /// Most recent local user-verification on the consumer device (WebAuthn UV, biometric
  /// unlock). The maintainer's policy may require this to be within N seconds.
  final String? lastUserVerificationAt;

  /// Producer-supplied network classification. Advisory.
  final ConsumerContextNetworkClass? networkClass;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (deviceId != null) 'deviceId': deviceId!,
        if (lastUserVerificationAt != null)
          'lastUserVerificationAt': lastUserVerificationAt!,
        if (networkClass != null) 'networkClass': networkClass!.value,
      };
}

/// Consumer asks the maintainer to attach a Data Integrity proof (eddsa-jcs-2022) to a
/// Trust Task envelope, signing as the principal DID of a `didSelfIssued` or
/// `didcommPeer` vault entry. The long-term signing key never leaves the maintainer.
/// This is the per-envelope signing complement to `vault/proxy-login/0.1`'s
/// session-credential minting: proxy-login mints a session at session-start;
/// sign-trust-task signs individual follow-up tasks during the session.
class Payload {
  const Payload({
    required this.entryId,
    required this.unsignedEnvelope,
    this.consumerContext,
    this.stepUpProof,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        entryId: json['entryId'] as String,
        unsignedEnvelope: UnsignedTrustTaskEnvelope.fromJson(
            json['unsignedEnvelope'] as Map<String, dynamic>),
        consumerContext: json['consumerContext'] == null
            ? null
            : ConsumerContext.fromJson(
                json['consumerContext'] as Map<String, dynamic>),
        stepUpProof: json['stepUpProof'] == null
            ? null
            : StepUpProof.fromJson(json['stepUpProof'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Identifier of the vault entry whose principal will sign. The maintainer rejects
  /// with `notSignable` when `entry.secretKind` is not `didSelfIssued` or `didcommPeer`
  /// (other kinds have no DID-based signing identity).
  final String entryId;

  /// The Trust Task document to sign. MUST satisfy the framework's structural
  /// requirements (id/type/issuer/recipient/issuedAt/payload). MUST NOT carry a `proof`.
  /// `issuer` MUST equal the referenced entry's principalDid — the maintainer refuses to
  /// silently rewrite `issuer` (see `envelopeIssuerMismatch`).
  final UnsignedTrustTaskEnvelope unsignedEnvelope;

  /// Caller's situational context — fed to the policy engine.
  final ConsumerContext? consumerContext;

  /// Step-up proof on retry after `stepUpRequired`.
  final StepUpProof? stepUpProof;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entryId': entryId,
        'unsignedEnvelope': unsignedEnvelope.toJson(),
        if (consumerContext != null)
          'consumerContext': consumerContext!.toJson(),
        if (stepUpProof != null) 'stepUpProof': stepUpProof!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vault/sign-trust-task/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vault/sign-trust-task/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vault/sign-trust-task/0.2","title":"Vault Sign Trust Task — payload","description":"Consumer asks the maintainer to attach a Data Integrity proof (eddsa-jcs-2022) to a Trust Task envelope, signing as the principal DID of a `didSelfIssued` or `didcommPeer` vault entry. The long-term signing key never leaves the maintainer. This is the per-envelope signing complement to `vault/proxy-login/0.1`\'s session-credential minting: proxy-login mints a session at session-start; sign-trust-task signs individual follow-up tasks during the session.","type":"object","additionalProperties":false,"required":["entryId","unsignedEnvelope"],"properties":{"entryId":{"type":"string","minLength":1,"description":"Identifier of the vault entry whose principal will sign. The maintainer rejects with `notSignable` when `entry.secretKind` is not `didSelfIssued` or `didcommPeer` (other kinds have no DID-based signing identity)."},"unsignedEnvelope":{"\$ref":"#/\$defs/UnsignedTrustTaskEnvelope","description":"The Trust Task document to sign. MUST satisfy the framework\'s structural requirements (id/type/issuer/recipient/issuedAt/payload). MUST NOT carry a `proof`. `issuer` MUST equal the referenced entry\'s principalDid — the maintainer refuses to silently rewrite `issuer` (see `envelopeIssuerMismatch`)."},"consumerContext":{"\$ref":"#/\$defs/ConsumerContext","description":"Caller\'s situational context — fed to the policy engine."},"stepUpProof":{"\$ref":"#/\$defs/StepUpProof","description":"Step-up proof on retry after `stepUpRequired`."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"UnsignedTrustTaskEnvelope":{"\$anchor":"unsigned-envelope","title":"Unsigned Trust Task envelope","description":"Permissive shape for an unsigned Trust Task document — the framework-required members MUST be present, and `proof` MUST NOT be. The maintainer does not validate the inner `payload` against the embedded `type`\'s schema; that validation is the recipient\'s responsibility.","type":"object","required":["id","type","issuer","recipient","issuedAt","payload"],"properties":{"id":{"type":"string","minLength":1,"description":"Envelope identifier. Set by the producer of the inner task."},"threadId":{"type":"string","minLength":1,"description":"Optional thread/correlation id, per framework §4.x."},"type":{"type":"string","format":"uri","minLength":1,"description":"Type URI of the inner Trust Task (e.g. `https://trusttasks.org/spec/acl/grant/0.1`)."},"issuer":{"type":"string","minLength":1,"description":"Issuer DID of the inner task. MUST equal the vault entry\'s principalDid — the maintainer rejects mismatches with `envelopeIssuerMismatch` rather than overwriting."},"recipient":{"type":"string","minLength":1,"description":"Recipient DID — the relying party / audience the signed envelope will be delivered to."},"issuedAt":{"type":"string","format":"date-time","description":"Producer\'s wall-clock when the inner task was constructed. Maintainer copies through; the proof\'s `created` is the maintainer\'s wall-clock at signing."},"expiresAt":{"type":"string","format":"date-time","description":"Optional inner-task expiry. The maintainer rejects with `envelopeExpired` when this is in the past at sign time."},"payload":{"description":"Inner task\'s payload object. Opaque to the maintainer — passed through unchanged into the signed envelope."},"ext":{"\$ref":"#/\$defs/Ext"}},"not":{"required":["proof"]}},"Response":{"\$anchor":"response","title":"Vault Sign Trust Task — response payload","type":"object","additionalProperties":false,"required":["signedEnvelope"],"properties":{"signedEnvelope":{"type":"object","description":"The supplied `unsignedEnvelope` with a Data Integrity `proof` attached. `proof.verificationMethod` is `<principalDid>#<signingKeyId>`; `proof.proofPurpose` is `assertionMethod`; `proof.cryptosuite` is `eddsa-jcs-2022`. All other members of the envelope (`id`, `type`, `issuer`, `recipient`, `issuedAt`, `expiresAt`, `payload`, `ext`) are unchanged from the request.","required":["id","type","issuer","recipient","issuedAt","payload","proof"]},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"StepUpProof":{"title":"StepUpProof","type":"object","additionalProperties":false,"required":["kind","proof","challengeId"],"properties":{"kind":{"type":"string","enum":["webauthnUv","pushApproval","totp"]},"proof":{"type":"string","description":"Format depends on kind: WebAuthn assertion (base64url), DIDComm approval-response message id, or 6–8-digit TOTP code."},"challengeId":{"type":"string","minLength":1,"description":"Maintainer-issued challenge id the proof responds to."}}},"ConsumerContext":{"title":"ConsumerContext","type":"object","additionalProperties":false,"properties":{"deviceId":{"type":"string","minLength":1,"description":"Device-binding id assigned at registration. The maintainer cross-checks this against the authenticated transport identity."},"lastUserVerificationAt":{"type":"string","format":"date-time","description":"Most recent local user-verification on the consumer device (WebAuthn UV, biometric unlock). The maintainer\'s policy may require this to be within N seconds."},"networkClass":{"type":"string","enum":["unknown","home","corp","public","vpn"],"description":"Producer-supplied network classification. Advisory."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"UnsignedTrustTaskEnvelope":{"\$anchor":"unsigned-envelope","title":"Unsigned Trust Task envelope","description":"Permissive shape for an unsigned Trust Task document — the framework-required members MUST be present, and `proof` MUST NOT be. The maintainer does not validate the inner `payload` against the embedded `type`\'s schema; that validation is the recipient\'s responsibility.","type":"object","required":["id","type","issuer","recipient","issuedAt","payload"],"properties":{"id":{"type":"string","minLength":1,"description":"Envelope identifier. Set by the producer of the inner task."},"threadId":{"type":"string","minLength":1,"description":"Optional thread/correlation id, per framework §4.x."},"type":{"type":"string","format":"uri","minLength":1,"description":"Type URI of the inner Trust Task (e.g. `https://trusttasks.org/spec/acl/grant/0.1`)."},"issuer":{"type":"string","minLength":1,"description":"Issuer DID of the inner task. MUST equal the vault entry\'s principalDid — the maintainer rejects mismatches with `envelopeIssuerMismatch` rather than overwriting."},"recipient":{"type":"string","minLength":1,"description":"Recipient DID — the relying party / audience the signed envelope will be delivered to."},"issuedAt":{"type":"string","format":"date-time","description":"Producer\'s wall-clock when the inner task was constructed. Maintainer copies through; the proof\'s `created` is the maintainer\'s wall-clock at signing."},"expiresAt":{"type":"string","format":"date-time","description":"Optional inner-task expiry. The maintainer rejects with `envelopeExpired` when this is in the past at sign time."},"payload":{"description":"Inner task\'s payload object. Opaque to the maintainer — passed through unchanged into the signed envelope."},"ext":{"\$ref":"#/\$defs/Ext"}},"not":{"required":["proof"]}},"Response":{"\$anchor":"response","title":"Vault Sign Trust Task — response payload","type":"object","additionalProperties":false,"required":["signedEnvelope"],"properties":{"signedEnvelope":{"type":"object","description":"The supplied `unsignedEnvelope` with a Data Integrity `proof` attached. `proof.verificationMethod` is `<principalDid>#<signingKeyId>`; `proof.proofPurpose` is `assertionMethod`; `proof.cryptosuite` is `eddsa-jcs-2022`. All other members of the envelope (`id`, `type`, `issuer`, `recipient`, `issuedAt`, `expiresAt`, `payload`, `ext`) are unchanged from the request.","required":["id","type","issuer","recipient","issuedAt","payload","proof"]},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"StepUpProof":{"title":"StepUpProof","type":"object","additionalProperties":false,"required":["kind","proof","challengeId"],"properties":{"kind":{"type":"string","enum":["webauthnUv","pushApproval","totp"]},"proof":{"type":"string","description":"Format depends on kind: WebAuthn assertion (base64url), DIDComm approval-response message id, or 6–8-digit TOTP code."},"challengeId":{"type":"string","minLength":1,"description":"Maintainer-issued challenge id the proof responds to."}}},"ConsumerContext":{"title":"ConsumerContext","type":"object","additionalProperties":false,"properties":{"deviceId":{"type":"string","minLength":1,"description":"Device-binding id assigned at registration. The maintainer cross-checks this against the authenticated transport identity."},"lastUserVerificationAt":{"type":"string","format":"date-time","description":"Most recent local user-verification on the consumer device (WebAuthn UV, biometric unlock). The maintainer\'s policy may require this to be within N seconds."},"networkClass":{"type":"string","enum":["unknown","home","corp","public","vpn"],"description":"Producer-supplied network classification. Advisory."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
