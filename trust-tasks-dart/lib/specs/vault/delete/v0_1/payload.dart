// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vault/delete/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Vault Delete — response payload
class Response {
  const Response({
    required this.id,
    required this.deletedAt,
    required this.graceUntil,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        id: json['id'] as String,
        deletedAt: json['deletedAt'] as String,
        graceUntil: json['graceUntil'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String id;
  final String deletedAt;

  /// Time after which the maintainer MAY garbage-collect the tombstone. Late-syncing
  /// consumers that connect before this time MUST receive the tombstone via vault/sync
  /// and wipe their cache; consumers that connect after this time will simply see the
  /// entry as absent.
  final String graceUntil;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'deletedAt': deletedAt,
        'graceUntil': graceUntil,
        if (ext != null) 'ext': ext!,
      };
}

/// Tombstone a vault entry. The maintainer SHOULD keep the tombstone for a configured
/// grace period so late-syncing consumers wipe the entry from their caches; after the
/// grace period expires the maintainer MAY garbage-collect.
class Payload {
  const Payload({
    required this.id,
    this.expectedVersion,
    this.reason,
    this.ext,
    this.force,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        expectedVersion: json['expectedVersion'] as int?,
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
        force: json['force'] as bool?,
      );

  final String id;

  /// Observed version for optimistic-concurrency check. Recommended to supply.
  final int? expectedVersion;

  /// Optional human-readable reason recorded in the audit log.
  final String? reason;
  final Ext? ext;

  /// Skip the grace window and hard-delete immediately: the secret bytes are zeroised
  /// and the entry is UNRECOVERABLE (equivalent to vault/purge). The default, false,
  /// performs a recoverable soft delete — the entry becomes a tombstone, restorable via
  /// vault/restore until the sweeper purges it at graceUntil. A consumer MUST NOT
  /// present a forced delete to a human as though it were recoverable.
  final bool? force;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (expectedVersion != null) 'expectedVersion': expectedVersion!,
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
        if (force != null) 'force': force!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vault/delete/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vault/delete/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vault/delete/0.1","title":"Vault Delete — payload","description":"Tombstone a vault entry. The maintainer SHOULD keep the tombstone for a configured grace period so late-syncing consumers wipe the entry from their caches; after the grace period expires the maintainer MAY garbage-collect.","type":"object","additionalProperties":false,"required":["id"],"properties":{"id":{"type":"string","minLength":1},"expectedVersion":{"type":"integer","minimum":0,"description":"Observed version for optimistic-concurrency check. Recommended to supply."},"reason":{"type":"string","maxLength":256,"description":"Optional human-readable reason recorded in the audit log."},"ext":{"\$ref":"#/\$defs/Ext"},"force":{"type":"boolean","default":false,"description":"Skip the grace window and hard-delete immediately: the secret bytes are zeroised and the entry is UNRECOVERABLE (equivalent to vault/purge). The default, false, performs a recoverable soft delete — the entry becomes a tombstone, restorable via vault/restore until the sweeper purges it at graceUntil. A consumer MUST NOT present a forced delete to a human as though it were recoverable."}},"\$defs":{"Response":{"\$anchor":"response","title":"Vault Delete — response payload","type":"object","additionalProperties":false,"required":["id","deletedAt","graceUntil"],"properties":{"id":{"type":"string","minLength":1},"deletedAt":{"type":"string","format":"date-time"},"graceUntil":{"type":"string","format":"date-time","description":"Time after which the maintainer MAY garbage-collect the tombstone. Late-syncing consumers that connect before this time MUST receive the tombstone via vault/sync and wipe their cache; consumers that connect after this time will simply see the entry as absent."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Vault Delete — response payload","type":"object","additionalProperties":false,"required":["id","deletedAt","graceUntil"],"properties":{"id":{"type":"string","minLength":1},"deletedAt":{"type":"string","format":"date-time"},"graceUntil":{"type":"string","format":"date-time","description":"Time after which the maintainer MAY garbage-collect the tombstone. Late-syncing consumers that connect before this time MUST receive the tombstone via vault/sync and wipe their cache; consumers that connect after this time will simply see the entry as absent."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
