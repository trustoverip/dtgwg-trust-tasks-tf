// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/contexts/update-did/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A context as the VTA holds it.
class ContextRecord {
  const ContextRecord({
    required this.id,
    required this.name,
    this.did,
    this.description,
    this.parent,
    required this.basePath,
    required this.createdAt,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory ContextRecord.fromJson(Map<String, dynamic> json) => ContextRecord(
        id: json['id'] as String,
        name: json['name'] as String,
        did: json['did'] as String?,
        description: json['description'] as String?,
        parent: json['parent'] as String?,
        basePath: json['basePath'] as String,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Context id. For a nested context this is the full path (`parent/leaf`), not the
  /// leaf alone — an ACL scope naming this context must use this value verbatim.
  final String id;

  /// Human-readable name. Operator-facing only; carries no authorization meaning, and
  /// two contexts may share a name.
  final String name;

  /// DID this context acts as, when one has been assigned. Absent means the context has
  /// no identity of its own yet — not that it has been denied one.
  final String? did;

  /// Free-form description.
  final String? description;

  /// Id of the context this one nests under. Absent for a top-level context. Authority
  /// granted at a parent reaches its children, so this member is load-bearing for anyone
  /// reasoning about scope.
  final String? parent;

  /// Resolved path from the root context, as the VTA derives it from the parent chain.
  /// Consumers MUST treat this as derived state: it is recomputed by the maintainer and
  /// is not independently settable.
  final String basePath;

  /// RFC 3339.
  final String createdAt;

  /// RFC 3339.
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
        if (did != null) 'did': did!,
        if (description != null) 'description': description!,
        if (parent != null) 'parent': parent!,
        'basePath': basePath,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// Success response to vta/contexts/update-did: the context as it now stands. Type
/// https://trusttasks.org/spec/vta/contexts/update-did/1.0#response.
typedef Response = ContextRecord;

/// VTA Contexts Update-DID — payload
class Payload {
  const Payload({
    required this.id,
    required this.did,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        did: json['did'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Context whose DID is being set.
  final String id;

  /// The DID this context acts as from now on. Anything that authenticated or signed as
  /// the previous DID keeps referring to it; this task does not migrate those
  /// references, and a consumer MUST NOT imply that it does.
  final String did;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'did': did,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/contexts/update-did/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/contexts/update-did/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/contexts/update-did/1.0","title":"VTA Contexts Update-DID — payload","type":"object","additionalProperties":false,"required":["id","did"],"properties":{"id":{"type":"string","minLength":1,"description":"Context whose DID is being set."},"did":{"type":"string","minLength":1,"description":"The DID this context acts as from now on. Anything that authenticated or signed as the previous DID keeps referring to it; this task does not migrate those references, and a consumer MUST NOT imply that it does."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Contexts Update-DID — response payload","description":"Success response to vta/contexts/update-did: the context as it now stands. Type https://trusttasks.org/spec/vta/contexts/update-did/1.0#response.","\$ref":"#/\$defs/ContextRecord"},"ContextRecord":{"title":"ContextRecord","description":"A context as the VTA holds it.","type":"object","additionalProperties":false,"required":["id","name","basePath","createdAt","updatedAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Context id. For a nested context this is the full path (`parent/leaf`), not the leaf alone — an ACL scope naming this context must use this value verbatim."},"name":{"type":"string","minLength":1,"maxLength":256,"description":"Human-readable name. Operator-facing only; carries no authorization meaning, and two contexts may share a name."},"did":{"type":"string","description":"DID this context acts as, when one has been assigned. Absent means the context has no identity of its own yet — not that it has been denied one."},"description":{"type":"string","maxLength":1024,"description":"Free-form description."},"parent":{"type":"string","description":"Id of the context this one nests under. Absent for a top-level context. Authority granted at a parent reaches its children, so this member is load-bearing for anyone reasoning about scope."},"basePath":{"type":"string","description":"Resolved path from the root context, as the VTA derives it from the parent chain. Consumers MUST treat this as derived state: it is recomputed by the maintainer and is not independently settable."},"createdAt":{"type":"string","format":"date-time","description":"RFC 3339."},"updatedAt":{"type":"string","format":"date-time","description":"RFC 3339."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Contexts Update-DID — response payload","description":"Success response to vta/contexts/update-did: the context as it now stands. Type https://trusttasks.org/spec/vta/contexts/update-did/1.0#response.","\$ref":"#/\$defs/ContextRecord"},"ContextRecord":{"title":"ContextRecord","description":"A context as the VTA holds it.","type":"object","additionalProperties":false,"required":["id","name","basePath","createdAt","updatedAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Context id. For a nested context this is the full path (`parent/leaf`), not the leaf alone — an ACL scope naming this context must use this value verbatim."},"name":{"type":"string","minLength":1,"maxLength":256,"description":"Human-readable name. Operator-facing only; carries no authorization meaning, and two contexts may share a name."},"did":{"type":"string","description":"DID this context acts as, when one has been assigned. Absent means the context has no identity of its own yet — not that it has been denied one."},"description":{"type":"string","maxLength":1024,"description":"Free-form description."},"parent":{"type":"string","description":"Id of the context this one nests under. Absent for a top-level context. Authority granted at a parent reaches its children, so this member is load-bearing for anyone reasoning about scope."},"basePath":{"type":"string","description":"Resolved path from the root context, as the VTA derives it from the parent chain. Consumers MUST treat this as derived state: it is recomputed by the maintainer and is not independently settable."},"createdAt":{"type":"string","format":"date-time","description":"RFC 3339."},"updatedAt":{"type":"string","format":"date-time","description":"RFC 3339."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
