// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/contexts/update/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A context as the VTA holds it.
class ContextRecord {
  const ContextRecord({
    required this.id,
    required this.name,
    this.did,
    this.description,
    this.parent,
    required this.basePath,
    required this.createdAt,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory ContextRecord.fromJson(Map<String, dynamic> json) => ContextRecord(
        id: json['id'] as String,
        name: json['name'] as String,
        did: json['did'] as String?,
        description: json['description'] as String?,
        parent: json['parent'] as String?,
        basePath: json['basePath'] as String,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Context id. For a nested context this is the full path (`parent/leaf`), not the
  /// leaf alone — an ACL scope naming this context must use this value verbatim.
  final String id;

  /// Human-readable name. Operator-facing only; carries no authorization meaning, and
  /// two contexts may share a name.
  final String name;

  /// DID this context acts as, when one has been assigned. Absent means the context has
  /// no identity of its own yet — not that it has been denied one.
  final String? did;

  /// Free-form description.
  final String? description;

  /// Id of the context this one nests under. Absent for a top-level context. Authority
  /// granted at a parent reaches its children, so this member is load-bearing for anyone
  /// reasoning about scope.
  final String? parent;

  /// Resolved path from the root context, as the VTA derives it from the parent chain.
  /// Consumers MUST treat this as derived state: it is recomputed by the maintainer and
  /// is not independently settable.
  final String basePath;

  /// RFC 3339.
  final String createdAt;

  /// RFC 3339.
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
        if (did != null) 'did': did!,
        if (description != null) 'description': description!,
        if (parent != null) 'parent': parent!,
        'basePath': basePath,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// Success response to vta/contexts/update: the context as it now stands. Type
/// https://trusttasks.org/spec/vta/contexts/update/1.0#response.
typedef Response = ContextRecord;

/// Per-context restrictions the VTA enforces. Every member is optional, and **absence
/// means unrestricted, not empty** — a policy that omits `presentableTypes` permits
/// every type, while one that sets it to `\[\]` permits none. The two are opposite
/// instructions and a consumer MUST NOT collapse them.
class ContextPolicy {
  const ContextPolicy({
    this.trustedVerifiers,
    this.presentableTypes,
    this.signableKeys,
    this.exportAllowed,
    this.quotas,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory ContextPolicy.fromJson(Map<String, dynamic> json) => ContextPolicy(
        trustedVerifiers: json['trustedVerifiers'] == null
            ? null
            : (json['trustedVerifiers'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        presentableTypes: json['presentableTypes'] == null
            ? null
            : (json['presentableTypes'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        signableKeys: json['signableKeys'] == null
            ? null
            : (json['signableKeys'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        exportAllowed: json['exportAllowed'] as bool?,
        quotas: json['quotas'] as Map<String, dynamic>?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// VIDs permitted to receive presentations from this context. Absent: any verifier.
  /// Present-but-empty: none.
  final List<String>? trustedVerifiers;

  /// Credential types that may be presented from this context. Absent: any type.
  /// Present-but-empty: none.
  final List<String>? presentableTypes;

  /// Key ids the signing oracle may be invoked on within this context. Absent: every key
  /// the context holds. Present-but-empty: no keys.
  final List<String>? signableKeys;

  /// Whether material in this context may leave it through an export. Defaults to true;
  /// set it to false for a context whose contents must not be extractable.
  final bool? exportAllowed;

  /// Resource limits for the context. Shape is maintainer-defined.
  final Map<String, dynamic>? quotas;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (trustedVerifiers != null) 'trustedVerifiers': trustedVerifiers!,
        if (presentableTypes != null) 'presentableTypes': presentableTypes!,
        if (signableKeys != null) 'signableKeys': signableKeys!,
        if (exportAllowed != null) 'exportAllowed': exportAllowed!,
        if (quotas != null) 'quotas': quotas!,
        if (ext != null) 'ext': ext!,
      };
}

/// Request payload for vta/contexts/update. Every member except `id` is optional and
/// omitting one leaves that field unchanged — this is a patch, not a replacement.
class Payload {
  const Payload({
    required this.id,
    this.name,
    this.did,
    this.description,
    this.contextPolicy,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        name: json['name'] as String?,
        did: json['did'] as String?,
        description: json['description'] as String?,
        contextPolicy: json['contextPolicy'] == null
            ? null
            : ContextPolicy.fromJson(
                json['contextPolicy'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Context to update. Not itself changeable: an id is what ACL scopes name, so
  /// renaming one would silently unhook every grant that referenced it.
  final String id;

  /// New human-readable name.
  final String? name;

  /// DID this context acts as. Prefer vta/contexts/update-did, which is the task named
  /// and audited for this change; setting it here is permitted for a maintainer that
  /// treats the two as one write.
  final String? did;

  /// New description.
  final String? description;

  /// Replacement policy for the context. Supplied whole: the members present in the new
  /// policy are the policy, and one omitted from it becomes unrestricted rather than
  /// retaining its previous value.
  final ContextPolicy? contextPolicy;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (name != null) 'name': name!,
        if (did != null) 'did': did!,
        if (description != null) 'description': description!,
        if (contextPolicy != null) 'contextPolicy': contextPolicy!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/contexts/update/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/contexts/update/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/contexts/update/1.0","title":"VTA Contexts Update — payload","description":"Request payload for vta/contexts/update. Every member except `id` is optional and omitting one leaves that field unchanged — this is a patch, not a replacement.","type":"object","additionalProperties":false,"required":["id"],"properties":{"id":{"type":"string","minLength":1,"description":"Context to update. Not itself changeable: an id is what ACL scopes name, so renaming one would silently unhook every grant that referenced it."},"name":{"type":"string","minLength":1,"maxLength":256,"description":"New human-readable name."},"did":{"type":"string","description":"DID this context acts as. Prefer vta/contexts/update-did, which is the task named and audited for this change; setting it here is permitted for a maintainer that treats the two as one write."},"description":{"type":"string","maxLength":1024,"description":"New description."},"contextPolicy":{"\$ref":"#/\$defs/ContextPolicy","description":"Replacement policy for the context. Supplied whole: the members present in the new policy are the policy, and one omitted from it becomes unrestricted rather than retaining its previous value."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Contexts Update — response payload","description":"Success response to vta/contexts/update: the context as it now stands. Type https://trusttasks.org/spec/vta/contexts/update/1.0#response.","\$ref":"#/\$defs/ContextRecord"},"ContextRecord":{"title":"ContextRecord","description":"A context as the VTA holds it.","type":"object","additionalProperties":false,"required":["id","name","basePath","createdAt","updatedAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Context id. For a nested context this is the full path (`parent/leaf`), not the leaf alone — an ACL scope naming this context must use this value verbatim."},"name":{"type":"string","minLength":1,"maxLength":256,"description":"Human-readable name. Operator-facing only; carries no authorization meaning, and two contexts may share a name."},"did":{"type":"string","description":"DID this context acts as, when one has been assigned. Absent means the context has no identity of its own yet — not that it has been denied one."},"description":{"type":"string","maxLength":1024,"description":"Free-form description."},"parent":{"type":"string","description":"Id of the context this one nests under. Absent for a top-level context. Authority granted at a parent reaches its children, so this member is load-bearing for anyone reasoning about scope."},"basePath":{"type":"string","description":"Resolved path from the root context, as the VTA derives it from the parent chain. Consumers MUST treat this as derived state: it is recomputed by the maintainer and is not independently settable."},"createdAt":{"type":"string","format":"date-time","description":"RFC 3339."},"updatedAt":{"type":"string","format":"date-time","description":"RFC 3339."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ContextPolicy":{"title":"ContextPolicy","description":"Per-context restrictions the VTA enforces. Every member is optional, and **absence means unrestricted, not empty** — a policy that omits `presentableTypes` permits every type, while one that sets it to `[]` permits none. The two are opposite instructions and a consumer MUST NOT collapse them.","type":"object","additionalProperties":false,"properties":{"trustedVerifiers":{"type":"array","items":{"type":"string"},"description":"VIDs permitted to receive presentations from this context. Absent: any verifier. Present-but-empty: none."},"presentableTypes":{"type":"array","items":{"type":"string"},"description":"Credential types that may be presented from this context. Absent: any type. Present-but-empty: none."},"signableKeys":{"type":"array","items":{"type":"string"},"description":"Key ids the signing oracle may be invoked on within this context. Absent: every key the context holds. Present-but-empty: no keys."},"exportAllowed":{"type":"boolean","default":true,"description":"Whether material in this context may leave it through an export. Defaults to true; set it to false for a context whose contents must not be extractable."},"quotas":{"type":"object","description":"Resource limits for the context. Shape is maintainer-defined.","additionalProperties":true},"ext":{"\$ref":"#/\$defs/Ext"}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Contexts Update — response payload","description":"Success response to vta/contexts/update: the context as it now stands. Type https://trusttasks.org/spec/vta/contexts/update/1.0#response.","\$ref":"#/\$defs/ContextRecord"},"ContextRecord":{"title":"ContextRecord","description":"A context as the VTA holds it.","type":"object","additionalProperties":false,"required":["id","name","basePath","createdAt","updatedAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Context id. For a nested context this is the full path (`parent/leaf`), not the leaf alone — an ACL scope naming this context must use this value verbatim."},"name":{"type":"string","minLength":1,"maxLength":256,"description":"Human-readable name. Operator-facing only; carries no authorization meaning, and two contexts may share a name."},"did":{"type":"string","description":"DID this context acts as, when one has been assigned. Absent means the context has no identity of its own yet — not that it has been denied one."},"description":{"type":"string","maxLength":1024,"description":"Free-form description."},"parent":{"type":"string","description":"Id of the context this one nests under. Absent for a top-level context. Authority granted at a parent reaches its children, so this member is load-bearing for anyone reasoning about scope."},"basePath":{"type":"string","description":"Resolved path from the root context, as the VTA derives it from the parent chain. Consumers MUST treat this as derived state: it is recomputed by the maintainer and is not independently settable."},"createdAt":{"type":"string","format":"date-time","description":"RFC 3339."},"updatedAt":{"type":"string","format":"date-time","description":"RFC 3339."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ContextPolicy":{"title":"ContextPolicy","description":"Per-context restrictions the VTA enforces. Every member is optional, and **absence means unrestricted, not empty** — a policy that omits `presentableTypes` permits every type, while one that sets it to `[]` permits none. The two are opposite instructions and a consumer MUST NOT collapse them.","type":"object","additionalProperties":false,"properties":{"trustedVerifiers":{"type":"array","items":{"type":"string"},"description":"VIDs permitted to receive presentations from this context. Absent: any verifier. Present-but-empty: none."},"presentableTypes":{"type":"array","items":{"type":"string"},"description":"Credential types that may be presented from this context. Absent: any type. Present-but-empty: none."},"signableKeys":{"type":"array","items":{"type":"string"},"description":"Key ids the signing oracle may be invoked on within this context. Absent: every key the context holds. Present-but-empty: no keys."},"exportAllowed":{"type":"boolean","default":true,"description":"Whether material in this context may leave it through an export. Defaults to true; set it to false for a context whose contents must not be extractable."},"quotas":{"type":"object","description":"Resource limits for the context. Shape is maintainer-defined.","additionalProperties":true},"ext":{"\$ref":"#/\$defs/Ext"}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
