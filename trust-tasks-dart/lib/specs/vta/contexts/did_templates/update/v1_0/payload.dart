// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/contexts/did-templates/update/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Authored template shape: a DID document with `{TOKEN}` placeholders plus the
/// variable contract the VTA's renderer enforces.
class DidTemplate {
  const DidTemplate({
    required this.schemaVersion,
    required this.name,
    required this.kind,
    this.description,
    this.methods,
    this.requiredVars,
    this.optionalVars,
    this.defaults,
    required this.document,
  });

  /// Read this payload from a decoded JSON object.
  factory DidTemplate.fromJson(Map<String, dynamic> json) => DidTemplate(
        schemaVersion: json['schemaVersion'] as int,
        name: json['name'] as String,
        kind: json['kind'] as String,
        description: json['description'] as String?,
        methods: json['methods'] == null
            ? null
            : (json['methods'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        requiredVars: json['requiredVars'] == null
            ? null
            : (json['requiredVars'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        optionalVars: json['optionalVars'] as Map<String, dynamic>?,
        defaults: json['defaults'] as Map<String, dynamic>?,
        document: json['document'] as Map<String, dynamic>,
      );

  /// Template schema version. Currently always 1.
  final int schemaVersion;

  /// Template id within its scope. Lowercase alphanumeric and hyphen only.
  final String name;

  /// Classification hint, e.g. `mediator`, `did-host-http`, `app`. Drives downstream
  /// provisioning behaviour.
  final String kind;

  /// Human-readable description of what the template provisions.
  final String? description;

  /// DID methods this template targets, e.g. \["webvh", "web"\] for a hosted DID or
  /// \["key"\] for a did:key.
  final List<String>? methods;

  /// Variables the caller MUST supply at render time. MUST NOT include reserved ambient
  /// names (DID, SIGNING_KEY_MB, KA_KEY_MB, VTA_DID, VTA_URL, CONTEXT_ID, CONTEXT_DID,
  /// NOW).
  final List<String>? requiredVars;

  /// Variables with default values, keyed by variable name.
  final Map<String, dynamic>? optionalVars;

  /// Hints for CLI / setup wizards (e.g. preRotationCount, portable,
  /// addMediatorService).
  final Map<String, dynamic>? defaults;

  /// The DID document body with `{TOKEN}` placeholders. `document.id` MUST contain the
  /// `{DID}` placeholder. Every `{TOKEN}` MUST be declared in requiredVars/optionalVars
  /// or be a reserved ambient name.
  final Map<String, dynamic> document;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'schemaVersion': schemaVersion,
        'name': name,
        'kind': kind,
        if (description != null) 'description': description!,
        if (methods != null) 'methods': methods!,
        if (requiredVars != null) 'requiredVars': requiredVars!,
        if (optionalVars != null) 'optionalVars': optionalVars!,
        if (defaults != null) 'defaults': defaults!,
        'document': document,
      };
}

/// Where a stored template lives. Tagged by `type`.
typedef Scope = Map<String, dynamic>;

/// The persisted DidTemplateRecord after replacement. The DidTemplate fields are
/// flattened at the top level alongside the resolved scope and provenance metadata.
/// Same shape returned by create/get.
class Response {
  const Response({
    required this.schemaVersion,
    required this.name,
    required this.kind,
    this.description,
    this.methods,
    this.requiredVars,
    this.optionalVars,
    this.defaults,
    required this.document,
    required this.scope,
    required this.createdAt,
    required this.updatedAt,
    required this.createdBy,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        schemaVersion: json['schemaVersion'] as int,
        name: json['name'] as String,
        kind: json['kind'] as String,
        description: json['description'] as String?,
        methods: json['methods'] == null
            ? null
            : (json['methods'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        requiredVars: json['requiredVars'] == null
            ? null
            : (json['requiredVars'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        optionalVars: json['optionalVars'] as Map<String, dynamic>?,
        defaults: json['defaults'] as Map<String, dynamic>?,
        document: json['document'] as Map<String, dynamic>,
        scope: json['scope'] as Map<String, dynamic>,
        createdAt: json['createdAt'] as int,
        updatedAt: json['updatedAt'] as int,
        createdBy: json['createdBy'] as String,
      );

  final int schemaVersion;
  final String name;
  final String kind;
  final String? description;
  final List<String>? methods;
  final List<String>? requiredVars;
  final Map<String, dynamic>? optionalVars;
  final Map<String, dynamic>? defaults;
  final Map<String, dynamic> document;

  /// Resolved scope of the stored template.
  final Scope scope;

  /// UTC unix-epoch seconds the template was first stored. Preserved across an update.
  final int createdAt;

  /// UTC unix-epoch seconds of the last write. Advanced by this update.
  final int updatedAt;

  /// DID of the admin who last wrote the template.
  final String createdBy;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'schemaVersion': schemaVersion,
        'name': name,
        'kind': kind,
        if (description != null) 'description': description!,
        if (methods != null) 'methods': methods!,
        if (requiredVars != null) 'requiredVars': requiredVars!,
        if (optionalVars != null) 'optionalVars': optionalVars!,
        if (defaults != null) 'defaults': defaults!,
        'document': document,
        'scope': scope,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        'createdBy': createdBy,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Replace an existing DID template scoped to a context on a VTA. Gated on the
/// context's admin (or super-admin). `name` is the resource id within the context
/// scope and MUST equal `template.name`. The VTA preserves the original
/// createdAt/createdBy and advances updatedAt. The success response is the persisted
/// DidTemplateRecord.
class Payload {
  const Payload({
    required this.contextId,
    required this.name,
    required this.template,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String,
        name: json['name'] as String,
        template:
            DidTemplate.fromJson(json['template'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The context the template is scoped to.
  final String contextId;

  /// Resource id of the template to replace; MUST equal `template.name`.
  final String name;

  /// Full replacement template document. Validated against the v1 template schema before
  /// persistence; an invalid shape is rejected with the framework's `malformedRequest`.
  final DidTemplate template;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'name': name,
        'template': template.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/contexts/did-templates/update/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/contexts/did-templates/update/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/contexts/did-templates/update/1.0","title":"VTA Context DID-Template Update — payload","description":"Replace an existing DID template scoped to a context on a VTA. Gated on the context\'s admin (or super-admin). `name` is the resource id within the context scope and MUST equal `template.name`. The VTA preserves the original createdAt/createdBy and advances updatedAt. The success response is the persisted DidTemplateRecord.","type":"object","additionalProperties":false,"required":["contextId","name","template"],"properties":{"contextId":{"type":"string","minLength":1,"description":"The context the template is scoped to."},"name":{"type":"string","minLength":1,"description":"Resource id of the template to replace; MUST equal `template.name`."},"template":{"\$ref":"#/\$defs/DidTemplate","description":"Full replacement template document. Validated against the v1 template schema before persistence; an invalid shape is rejected with the framework\'s `malformedRequest`."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"DidTemplate":{"title":"DidTemplate","description":"Authored template shape: a DID document with `{TOKEN}` placeholders plus the variable contract the VTA\'s renderer enforces.","type":"object","additionalProperties":false,"required":["schemaVersion","name","kind","document"],"properties":{"schemaVersion":{"type":"integer","const":1,"description":"Template schema version. Currently always 1."},"name":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z0-9-]+\$","description":"Template id within its scope. Lowercase alphanumeric and hyphen only."},"kind":{"type":"string","minLength":1,"description":"Classification hint, e.g. `mediator`, `did-host-http`, `app`. Drives downstream provisioning behaviour."},"description":{"type":["string","null"],"description":"Human-readable description of what the template provisions."},"methods":{"type":"array","items":{"type":"string"},"description":"DID methods this template targets, e.g. [\\"webvh\\", \\"web\\"] for a hosted DID or [\\"key\\"] for a did:key."},"requiredVars":{"type":"array","items":{"type":"string"},"description":"Variables the caller MUST supply at render time. MUST NOT include reserved ambient names (DID, SIGNING_KEY_MB, KA_KEY_MB, VTA_DID, VTA_URL, CONTEXT_ID, CONTEXT_DID, NOW)."},"optionalVars":{"type":"object","description":"Variables with default values, keyed by variable name.","additionalProperties":true},"defaults":{"type":"object","description":"Hints for CLI / setup wizards (e.g. preRotationCount, portable, addMediatorService).","additionalProperties":true},"document":{"type":"object","description":"The DID document body with `{TOKEN}` placeholders. `document.id` MUST contain the `{DID}` placeholder. Every `{TOKEN}` MUST be declared in requiredVars/optionalVars or be a reserved ambient name."}}},"Scope":{"title":"Scope","description":"Where a stored template lives. Tagged by `type`.","type":"object","oneOf":[{"title":"Builtin","type":"object","additionalProperties":false,"required":["type"],"properties":{"type":{"const":"builtin"}}},{"title":"Global","type":"object","additionalProperties":false,"required":["type"],"properties":{"type":{"const":"global"}}},{"title":"Context","type":"object","additionalProperties":false,"required":["type","contextId"],"properties":{"type":{"const":"context"},"contextId":{"type":"string","minLength":1}}}]},"Response":{"\$anchor":"response","title":"VTA Context DID-Template Update — response payload","description":"The persisted DidTemplateRecord after replacement. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata. Same shape returned by create/get.","type":"object","additionalProperties":false,"required":["schemaVersion","name","kind","document","scope","createdAt","updatedAt","createdBy"],"properties":{"schemaVersion":{"type":"integer","const":1},"name":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z0-9-]+\$"},"kind":{"type":"string","minLength":1},"description":{"type":["string","null"]},"methods":{"type":"array","items":{"type":"string"}},"requiredVars":{"type":"array","items":{"type":"string"}},"optionalVars":{"type":"object","additionalProperties":true},"defaults":{"type":"object","additionalProperties":true},"document":{"type":"object"},"scope":{"\$ref":"#/\$defs/Scope","description":"Resolved scope of the stored template."},"createdAt":{"type":"integer","description":"UTC unix-epoch seconds the template was first stored. Preserved across an update."},"updatedAt":{"type":"integer","description":"UTC unix-epoch seconds of the last write. Advanced by this update."},"createdBy":{"type":"string","description":"DID of the admin who last wrote the template."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"DidTemplate":{"title":"DidTemplate","description":"Authored template shape: a DID document with `{TOKEN}` placeholders plus the variable contract the VTA\'s renderer enforces.","type":"object","additionalProperties":false,"required":["schemaVersion","name","kind","document"],"properties":{"schemaVersion":{"type":"integer","const":1,"description":"Template schema version. Currently always 1."},"name":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z0-9-]+\$","description":"Template id within its scope. Lowercase alphanumeric and hyphen only."},"kind":{"type":"string","minLength":1,"description":"Classification hint, e.g. `mediator`, `did-host-http`, `app`. Drives downstream provisioning behaviour."},"description":{"type":["string","null"],"description":"Human-readable description of what the template provisions."},"methods":{"type":"array","items":{"type":"string"},"description":"DID methods this template targets, e.g. [\\"webvh\\", \\"web\\"] for a hosted DID or [\\"key\\"] for a did:key."},"requiredVars":{"type":"array","items":{"type":"string"},"description":"Variables the caller MUST supply at render time. MUST NOT include reserved ambient names (DID, SIGNING_KEY_MB, KA_KEY_MB, VTA_DID, VTA_URL, CONTEXT_ID, CONTEXT_DID, NOW)."},"optionalVars":{"type":"object","description":"Variables with default values, keyed by variable name.","additionalProperties":true},"defaults":{"type":"object","description":"Hints for CLI / setup wizards (e.g. preRotationCount, portable, addMediatorService).","additionalProperties":true},"document":{"type":"object","description":"The DID document body with `{TOKEN}` placeholders. `document.id` MUST contain the `{DID}` placeholder. Every `{TOKEN}` MUST be declared in requiredVars/optionalVars or be a reserved ambient name."}}},"Scope":{"title":"Scope","description":"Where a stored template lives. Tagged by `type`.","type":"object","oneOf":[{"title":"Builtin","type":"object","additionalProperties":false,"required":["type"],"properties":{"type":{"const":"builtin"}}},{"title":"Global","type":"object","additionalProperties":false,"required":["type"],"properties":{"type":{"const":"global"}}},{"title":"Context","type":"object","additionalProperties":false,"required":["type","contextId"],"properties":{"type":{"const":"context"},"contextId":{"type":"string","minLength":1}}}]},"Response":{"\$anchor":"response","title":"VTA Context DID-Template Update — response payload","description":"The persisted DidTemplateRecord after replacement. The DidTemplate fields are flattened at the top level alongside the resolved scope and provenance metadata. Same shape returned by create/get.","type":"object","additionalProperties":false,"required":["schemaVersion","name","kind","document","scope","createdAt","updatedAt","createdBy"],"properties":{"schemaVersion":{"type":"integer","const":1},"name":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z0-9-]+\$"},"kind":{"type":"string","minLength":1},"description":{"type":["string","null"]},"methods":{"type":"array","items":{"type":"string"}},"requiredVars":{"type":"array","items":{"type":"string"}},"optionalVars":{"type":"object","additionalProperties":true},"defaults":{"type":"object","additionalProperties":true},"document":{"type":"object"},"scope":{"\$ref":"#/\$defs/Scope","description":"Resolved scope of the stored template."},"createdAt":{"type":"integer","description":"UTC unix-epoch seconds the template was first stored. Preserved across an update."},"updatedAt":{"type":"integer","description":"UTC unix-epoch seconds of the last write. Advanced by this update."},"createdBy":{"type":"string","description":"DID of the admin who last wrote the template."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
