// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/contexts/secrets/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// What the key is for, and how to reconstruct it. `ed25519` signs, `x25519` is a
/// key-agreement key and signs nothing, `p256` is an ECDSA key for ES256. Stated as
/// well as encoded in `privateKeyMultibase` so a consumer can select a key by purpose
/// without decoding every entry.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const SecretEntryKeyType(String value) {
  static const SecretEntryKeyType ed25519 = SecretEntryKeyType('ed25519');
  static const SecretEntryKeyType x25519 = SecretEntryKeyType('x25519');
  static const SecretEntryKeyType p256 = SecretEntryKeyType('p256');

  /// Every value this specification's schema permits.
  static const List<SecretEntryKeyType> values = <SecretEntryKeyType>[
    ed25519,
    x25519,
    p256
  ];
}

/// One private key, named by the verification method it backs.
class SecretEntry {
  const SecretEntry({
    required this.keyId,
    required this.keyType,
    required this.privateKeyMultibase,
  });

  /// Read this payload from a decoded JSON object.
  factory SecretEntry.fromJson(Map<String, dynamic> json) => SecretEntry(
        keyId: json['keyId'] as String,
        keyType: SecretEntryKeyType(json['keyType'] as String),
        privateKeyMultibase: json['privateKeyMultibase'] as String,
      );

  /// The verification method this key backs, as an absolute DID URL under the response's
  /// `did` (`{did}#key-0`). It is the DID document — not the recipient's own storage —
  /// that decides this name, so a consumer installs the key under a `kid` that an
  /// inbound message's recipient field will actually match.
  final String keyId;

  /// What the key is for, and how to reconstruct it. `ed25519` signs, `x25519` is a
  /// key-agreement key and signs nothing, `p256` is an ECDSA key for ES256. Stated as
  /// well as encoded in `privateKeyMultibase` so a consumer can select a key by purpose
  /// without decoding every entry.
  final SecretEntryKeyType keyType;

  /// The private key, multibase-encoded (Base58BTC, `z` prefix) over its
  /// multicodec-prefixed bytes — `0x1300` Ed25519, `0x1302` X25519, `0x1306` P-256. The
  /// multicodec prefix, not this member's position in the array, is what identifies the
  /// key material.
  final String privateKeyMultibase;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'keyId': keyId,
        'keyType': keyType.value,
        'privateKeyMultibase': privateKeyMultibase,
      };
}

/// The context's DID and the private key material behind it, sufficient to operate as
/// that DID and nothing more. Failures use trust-task-error, not this shape. Type
/// https://trusttasks.org/spec/vta/contexts/secrets/1.0#response.
class Response {
  const Response({
    required this.did,
    required this.secrets,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        did: json['did'] as String,
        secrets: (json['secrets'] as List<dynamic>)
            .map((e) => SecretEntry.fromJson(e as Map<String, dynamic>))
            .toList(),
      );

  /// The DID the returned secrets belong to — the DID recorded on the named context.
  /// Every `keyId` below is a verification method of this DID, so a consumer can check
  /// the two agree before installing anything.
  final String did;

  /// One entry per verification method of `did` whose secret the recipient holds and is
  /// willing to release. MAY be empty: a context whose DID has no releasable key
  /// material is not an error, and a consumer that treats an empty array as a failure
  /// will misreport a context that is merely not provisioned yet.
  final List<SecretEntry> secrets;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'secrets': secrets.map((e) => e.toJson()).toList(),
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Names the trust context whose DID key material is being asked for. The outer
/// document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are
/// owned by the framework — SPEC §6.3.
class Payload {
  const Payload({
    required this.id,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Context id. For a nested context this is the full path (`parent/leaf`). Named `id`
  /// to match the rest of the `vta/contexts/*` family, where it always identifies the
  /// context being acted on.
  final String id;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/contexts/secrets/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/contexts/secrets/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/contexts/secrets/1.0","title":"VTA Context Secrets — payload","description":"Names the trust context whose DID key material is being asked for. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.","type":"object","additionalProperties":false,"required":["id"],"properties":{"id":{"type":"string","minLength":1,"description":"Context id. For a nested context this is the full path (`parent/leaf`). Named `id` to match the rest of the `vta/contexts/*` family, where it always identifies the context being acted on."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Context Secrets — response payload","type":"object","additionalProperties":false,"required":["did","secrets"],"description":"The context\'s DID and the private key material behind it, sufficient to operate as that DID and nothing more. Failures use trust-task-error, not this shape. Type https://trusttasks.org/spec/vta/contexts/secrets/1.0#response.","properties":{"did":{"type":"string","minLength":1,"description":"The DID the returned secrets belong to — the DID recorded on the named context. Every `keyId` below is a verification method of this DID, so a consumer can check the two agree before installing anything."},"secrets":{"type":"array","description":"One entry per verification method of `did` whose secret the recipient holds and is willing to release. MAY be empty: a context whose DID has no releasable key material is not an error, and a consumer that treats an empty array as a failure will misreport a context that is merely not provisioned yet.","items":{"\$ref":"#/\$defs/SecretEntry"}}}},"SecretEntry":{"title":"Secret entry","type":"object","additionalProperties":false,"required":["keyId","keyType","privateKeyMultibase"],"description":"One private key, named by the verification method it backs.","properties":{"keyId":{"type":"string","minLength":1,"description":"The verification method this key backs, as an absolute DID URL under the response\'s `did` (`{did}#key-0`). It is the DID document — not the recipient\'s own storage — that decides this name, so a consumer installs the key under a `kid` that an inbound message\'s recipient field will actually match."},"keyType":{"type":"string","enum":["ed25519","x25519","p256"],"description":"What the key is for, and how to reconstruct it. `ed25519` signs, `x25519` is a key-agreement key and signs nothing, `p256` is an ECDSA key for ES256. Stated as well as encoded in `privateKeyMultibase` so a consumer can select a key by purpose without decoding every entry."},"privateKeyMultibase":{"type":"string","minLength":1,"description":"The private key, multibase-encoded (Base58BTC, `z` prefix) over its multicodec-prefixed bytes — `0x1300` Ed25519, `0x1302` X25519, `0x1306` P-256. The multicodec prefix, not this member\'s position in the array, is what identifies the key material."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Context Secrets — response payload","type":"object","additionalProperties":false,"required":["did","secrets"],"description":"The context\'s DID and the private key material behind it, sufficient to operate as that DID and nothing more. Failures use trust-task-error, not this shape. Type https://trusttasks.org/spec/vta/contexts/secrets/1.0#response.","properties":{"did":{"type":"string","minLength":1,"description":"The DID the returned secrets belong to — the DID recorded on the named context. Every `keyId` below is a verification method of this DID, so a consumer can check the two agree before installing anything."},"secrets":{"type":"array","description":"One entry per verification method of `did` whose secret the recipient holds and is willing to release. MAY be empty: a context whose DID has no releasable key material is not an error, and a consumer that treats an empty array as a failure will misreport a context that is merely not provisioned yet.","items":{"\$ref":"#/\$defs/SecretEntry"}}}},"SecretEntry":{"title":"Secret entry","type":"object","additionalProperties":false,"required":["keyId","keyType","privateKeyMultibase"],"description":"One private key, named by the verification method it backs.","properties":{"keyId":{"type":"string","minLength":1,"description":"The verification method this key backs, as an absolute DID URL under the response\'s `did` (`{did}#key-0`). It is the DID document — not the recipient\'s own storage — that decides this name, so a consumer installs the key under a `kid` that an inbound message\'s recipient field will actually match."},"keyType":{"type":"string","enum":["ed25519","x25519","p256"],"description":"What the key is for, and how to reconstruct it. `ed25519` signs, `x25519` is a key-agreement key and signs nothing, `p256` is an ECDSA key for ES256. Stated as well as encoded in `privateKeyMultibase` so a consumer can select a key by purpose without decoding every entry."},"privateKeyMultibase":{"type":"string","minLength":1,"description":"The private key, multibase-encoded (Base58BTC, `z` prefix) over its multicodec-prefixed bytes — `0x1300` Ed25519, `0x1302` X25519, `0x1306` P-256. The multicodec prefix, not this member\'s position in the array, is what identifies the key material."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
