// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/passkey-vms/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// PasskeyVerificationMethod
class PasskeyVerificationMethod {
  const PasskeyVerificationMethod({
    required this.id,
    required this.type,
    required this.controller,
    required this.publicKeyMultibase,
    required this.webauthnCredentialId,
    this.webauthnTransports,
    this.label,
  });

  /// Read this payload from a decoded JSON object.
  factory PasskeyVerificationMethod.fromJson(Map<String, dynamic> json) =>
      PasskeyVerificationMethod(
        id: json['id'] as String,
        type: json['type'] as String,
        controller: json['controller'] as String,
        publicKeyMultibase: json['publicKeyMultibase'] as String,
        webauthnCredentialId: json['webauthnCredentialId'] as String,
        webauthnTransports: json['webauthnTransports'] == null
            ? null
            : (json['webauthnTransports'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        label: json['label'] as String?,
      );

  /// The verificationMethod id as it appears in the DID document:
  /// `&lt;did&gt;#passkey-&lt;base64url(sha256(credentialId))&gt;`. The fragment is content-derived
  /// so a verifier can locate this VM by recomputing `sha256(credential.id)`.
  final String id;

  /// Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID
  /// resolvers and verifiers need no WebAuthn-specific knowledge to consume it.
  final String type;

  /// The DID this verificationMethod is published on (the DID being augmented).
  final String controller;

  /// W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is
  /// the value a verifier validates a WebAuthn assertion against. Re-derived server-side
  /// from the attestation at enrolment — never trusted from the browser (see
  /// vta/passkey-vms/enroll-submit).
  final String publicKeyMultibase;

  /// The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute
  /// `sha256(credentialId)` and match the assertion to this VM's `id` fragment.
  final String webauthnCredentialId;

  /// Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`,
  /// `nfc`, `ble`). Advisory only — verifiers MUST NOT make trust decisions based on
  /// transport hints.
  final List<String>? webauthnTransports;

  /// Optional operator-supplied human-readable label (e.g. "MacBook Touch ID").
  /// Informational; not authoritative and not a security input.
  final String? label;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'type': type,
        'controller': controller,
        'publicKeyMultibase': publicKeyMultibase,
        'webauthnCredentialId': webauthnCredentialId,
        if (webauthnTransports != null)
          'webauthnTransports': webauthnTransports!,
        if (label != null) 'label': label!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The passkey verificationMethods currently on the DID.
class Response {
  const Response({
    required this.verificationMethods,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        verificationMethods: (json['verificationMethods'] as List<dynamic>)
            .map((e) =>
                PasskeyVerificationMethod.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Every passkey verificationMethod published on the DID. Empty when none are
  /// enrolled.
  final List<PasskeyVerificationMethod> verificationMethods;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'verificationMethods':
            verificationMethods.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// List every passkey verificationMethod currently published on a VTA-managed DID.
/// Admin-gated read. The returned entries are the same Multikey verificationMethods
/// that appear in the DID document.
class Payload {
  const Payload({
    required this.did,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID whose passkey verificationMethods to enumerate. The producer MUST hold the
  /// admin role on this DID's context.
  final String did;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/passkey-vms/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/passkey-vms/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/passkey-vms/list/0.1","title":"VTA Passkey-VM List — payload","description":"List every passkey verificationMethod currently published on a VTA-managed DID. Admin-gated read. The returned entries are the same Multikey verificationMethods that appear in the DID document.","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID whose passkey verificationMethods to enumerate. The producer MUST hold the admin role on this DID\'s context."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Passkey-VM List — response payload","description":"The passkey verificationMethods currently on the DID.","type":"object","additionalProperties":false,"required":["verificationMethods"],"properties":{"verificationMethods":{"type":"array","items":{"\$ref":"#/\$defs/PasskeyVerificationMethod"},"description":"Every passkey verificationMethod published on the DID. Empty when none are enrolled."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"PasskeyVerificationMethod":{"title":"PasskeyVerificationMethod","type":"object","additionalProperties":false,"required":["id","type","controller","publicKeyMultibase","webauthnCredentialId"],"properties":{"id":{"type":"string","minLength":1,"description":"The verificationMethod id as it appears in the DID document: `<did>#passkey-<base64url(sha256(credentialId))>`. The fragment is content-derived so a verifier can locate this VM by recomputing `sha256(credential.id)`."},"type":{"type":"string","const":"Multikey","description":"Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID resolvers and verifiers need no WebAuthn-specific knowledge to consume it."},"controller":{"type":"string","minLength":1,"description":"The DID this verificationMethod is published on (the DID being augmented)."},"publicKeyMultibase":{"type":"string","minLength":1,"description":"W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is the value a verifier validates a WebAuthn assertion against. Re-derived server-side from the attestation at enrolment — never trusted from the browser (see vta/passkey-vms/enroll-submit)."},"webauthnCredentialId":{"type":"string","minLength":1,"description":"The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute `sha256(credentialId)` and match the assertion to this VM\'s `id` fragment."},"webauthnTransports":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`, `nfc`, `ble`). Advisory only — verifiers MUST NOT make trust decisions based on transport hints."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"Optional operator-supplied human-readable label (e.g. \\"MacBook Touch ID\\"). Informational; not authoritative and not a security input."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Passkey-VM List — response payload","description":"The passkey verificationMethods currently on the DID.","type":"object","additionalProperties":false,"required":["verificationMethods"],"properties":{"verificationMethods":{"type":"array","items":{"\$ref":"#/\$defs/PasskeyVerificationMethod"},"description":"Every passkey verificationMethod published on the DID. Empty when none are enrolled."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"PasskeyVerificationMethod":{"title":"PasskeyVerificationMethod","type":"object","additionalProperties":false,"required":["id","type","controller","publicKeyMultibase","webauthnCredentialId"],"properties":{"id":{"type":"string","minLength":1,"description":"The verificationMethod id as it appears in the DID document: `<did>#passkey-<base64url(sha256(credentialId))>`. The fragment is content-derived so a verifier can locate this VM by recomputing `sha256(credential.id)`."},"type":{"type":"string","const":"Multikey","description":"Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID resolvers and verifiers need no WebAuthn-specific knowledge to consume it."},"controller":{"type":"string","minLength":1,"description":"The DID this verificationMethod is published on (the DID being augmented)."},"publicKeyMultibase":{"type":"string","minLength":1,"description":"W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is the value a verifier validates a WebAuthn assertion against. Re-derived server-side from the attestation at enrolment — never trusted from the browser (see vta/passkey-vms/enroll-submit)."},"webauthnCredentialId":{"type":"string","minLength":1,"description":"The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute `sha256(credentialId)` and match the assertion to this VM\'s `id` fragment."},"webauthnTransports":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`, `nfc`, `ble`). Advisory only — verifiers MUST NOT make trust decisions based on transport hints."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"Optional operator-supplied human-readable label (e.g. \\"MacBook Touch ID\\"). Informational; not authoritative and not a security input."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
