// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/passkey-vms/enroll-submit/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// PasskeyVerificationMethod
class PasskeyVerificationMethod {
  const PasskeyVerificationMethod({
    required this.id,
    required this.type,
    required this.controller,
    required this.publicKeyMultibase,
    required this.webauthnCredentialId,
    this.webauthnTransports,
    this.label,
  });

  /// Read this payload from a decoded JSON object.
  factory PasskeyVerificationMethod.fromJson(Map<String, dynamic> json) =>
      PasskeyVerificationMethod(
        id: json['id'] as String,
        type: json['type'] as String,
        controller: json['controller'] as String,
        publicKeyMultibase: json['publicKeyMultibase'] as String,
        webauthnCredentialId: json['webauthnCredentialId'] as String,
        webauthnTransports: json['webauthnTransports'] == null
            ? null
            : (json['webauthnTransports'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        label: json['label'] as String?,
      );

  /// The verificationMethod id as it appears in the DID document:
  /// `&lt;did&gt;#passkey-&lt;base64url(sha256(credentialId))&gt;`. The fragment is content-derived
  /// so a verifier can locate this VM by recomputing `sha256(credential.id)`.
  final String id;

  /// Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID
  /// resolvers and verifiers need no WebAuthn-specific knowledge to consume it.
  final String type;

  /// The DID this verificationMethod is published on (the DID being augmented).
  final String controller;

  /// W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is
  /// the value a verifier validates a WebAuthn assertion against. Re-derived server-side
  /// from the attestation at enrolment — never trusted from the browser (see
  /// vta/passkey-vms/enroll-submit).
  final String publicKeyMultibase;

  /// The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute
  /// `sha256(credentialId)` and match the assertion to this VM's `id` fragment.
  final String webauthnCredentialId;

  /// Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`,
  /// `nfc`, `ble`). Advisory only — verifiers MUST NOT make trust decisions based on
  /// transport hints.
  final List<String>? webauthnTransports;

  /// Optional operator-supplied human-readable label (e.g. "MacBook Touch ID").
  /// Informational; not authoritative and not a security input.
  final String? label;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'type': type,
        'controller': controller,
        'publicKeyMultibase': publicKeyMultibase,
        'webauthnCredentialId': webauthnCredentialId,
        if (webauthnTransports != null)
          'webauthnTransports': webauthnTransports!,
        if (label != null) 'label': label!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Successful enrolment. The verificationMethod has already been appended to the DID
/// document via a WebVH log entry at the returned version.
class Response {
  const Response({
    required this.verificationMethod,
    required this.webvhVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        verificationMethod: PasskeyVerificationMethod.fromJson(
            json['verificationMethod'] as Map<String, dynamic>),
        webvhVersion: json['webvhVersion'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The verificationMethod entry exactly as it now appears in the DID document.
  /// `publicKeyMultibase` here is the server-re-derived key, which is authoritative.
  final PasskeyVerificationMethod verificationMethod;

  /// The WebVH log-entry version that recorded the change (e.g. "3-Qm…").
  final String webvhVersion;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'verificationMethod': verificationMethod.toJson(),
        'webvhVersion': webvhVersion,
        if (ext != null) 'ext': ext!,
      };
}

/// Finalise passkey enrolment by submitting the WebAuthn registration result for a
/// ceremony opened by vta/passkey-vms/enroll-challenge. The VTA re-derives the
/// Multikey from attestationObject.authData and rejects on mismatch with the
/// browser-claimed publicKeyMultibase — the browser's value is NOT trusted as
/// authoritative. On success the VTA appends the verificationMethod to the DID
/// document via a WebVH log entry. All byte-valued fields are base64url-encoded (no
/// padding).
class Payload {
  const Payload({
    required this.did,
    required this.ceremonyId,
    required this.credentialId,
    required this.publicKeyMultibase,
    required this.coseAlgorithm,
    required this.attestationObject,
    required this.clientDataJson,
    required this.authenticatorData,
    this.transports,
    this.label,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        ceremonyId: json['ceremonyId'] as String,
        credentialId: json['credentialId'] as String,
        publicKeyMultibase: json['publicKeyMultibase'] as String,
        coseAlgorithm: json['coseAlgorithm'] as int,
        attestationObject: json['attestationObject'] as String,
        clientDataJson: json['clientDataJson'] as String,
        authenticatorData: json['authenticatorData'] as String,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        label: json['label'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID the new verificationMethod is to be added to. MUST match the DID bound to
  /// `ceremonyId` at challenge time — a mismatch is rejected as a cross-DID replay.
  final String did;

  /// The `ceremonyId` returned by vta/passkey-vms/enroll-challenge. Single-use; consumed
  /// by this submission.
  final String ceremonyId;

  /// WebAuthn `credential.id` (base64url, no padding). The published verificationMethod
  /// `id` fragment is derived as `passkey-&lt;base64url(sha256(credentialId))&gt;`.
  final String credentialId;

  /// Browser-computed W3C Multikey for the credential public key. ADVISORY: the VTA
  /// re-derives the Multikey from `attestationObject.authData` and rejects this
  /// submission if the values differ (anti-tamper gate). The re-derived key — not this
  /// one — is what gets published.
  final String publicKeyMultibase;

  /// COSE algorithm identifier of the credential key (e.g. -7 for ES256, -8 for EdDSA).
  /// Must be an algorithm the VTA can convert to a Multikey.
  final int coseAlgorithm;

  /// Raw WebAuthn `attestationObject` — base64url-encoded CBOR. The VTA parses
  /// `authData` from this to re-derive the authoritative public key.
  final String attestationObject;

  /// Raw WebAuthn `clientDataJSON` (base64url, no padding). Bound to the ceremony
  /// `challenge` during WebAuthn verification.
  final String clientDataJson;

  /// Raw WebAuthn `authenticatorData` (base64url, no padding).
  final String authenticatorData;

  /// Transport hints reported by the authenticator (e.g. `internal`, `hybrid`).
  /// Advisory; carried through to the published verificationMethod's
  /// `webauthnTransports`.
  final List<String>? transports;

  /// Optional operator-supplied label (e.g. "MacBook Touch ID"), carried through to the
  /// published verificationMethod.
  final String? label;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'ceremonyId': ceremonyId,
        'credentialId': credentialId,
        'publicKeyMultibase': publicKeyMultibase,
        'coseAlgorithm': coseAlgorithm,
        'attestationObject': attestationObject,
        'clientDataJson': clientDataJson,
        'authenticatorData': authenticatorData,
        if (transports != null) 'transports': transports!,
        if (label != null) 'label': label!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/passkey-vms/enroll-submit/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/passkey-vms/enroll-submit/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/passkey-vms/enroll-submit/0.1","title":"VTA Passkey-VM Enroll Submit — payload","description":"Finalise passkey enrolment by submitting the WebAuthn registration result for a ceremony opened by vta/passkey-vms/enroll-challenge. The VTA re-derives the Multikey from attestationObject.authData and rejects on mismatch with the browser-claimed publicKeyMultibase — the browser\'s value is NOT trusted as authoritative. On success the VTA appends the verificationMethod to the DID document via a WebVH log entry. All byte-valued fields are base64url-encoded (no padding).","type":"object","additionalProperties":false,"required":["did","ceremonyId","credentialId","publicKeyMultibase","coseAlgorithm","attestationObject","clientDataJson","authenticatorData"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID the new verificationMethod is to be added to. MUST match the DID bound to `ceremonyId` at challenge time — a mismatch is rejected as a cross-DID replay."},"ceremonyId":{"type":"string","minLength":1,"description":"The `ceremonyId` returned by vta/passkey-vms/enroll-challenge. Single-use; consumed by this submission."},"credentialId":{"type":"string","minLength":1,"description":"WebAuthn `credential.id` (base64url, no padding). The published verificationMethod `id` fragment is derived as `passkey-<base64url(sha256(credentialId))>`."},"publicKeyMultibase":{"type":"string","minLength":1,"description":"Browser-computed W3C Multikey for the credential public key. ADVISORY: the VTA re-derives the Multikey from `attestationObject.authData` and rejects this submission if the values differ (anti-tamper gate). The re-derived key — not this one — is what gets published."},"coseAlgorithm":{"type":"integer","description":"COSE algorithm identifier of the credential key (e.g. -7 for ES256, -8 for EdDSA). Must be an algorithm the VTA can convert to a Multikey."},"attestationObject":{"type":"string","minLength":1,"description":"Raw WebAuthn `attestationObject` — base64url-encoded CBOR. The VTA parses `authData` from this to re-derive the authoritative public key."},"clientDataJson":{"type":"string","minLength":1,"description":"Raw WebAuthn `clientDataJSON` (base64url, no padding). Bound to the ceremony `challenge` during WebAuthn verification."},"authenticatorData":{"type":"string","minLength":1,"description":"Raw WebAuthn `authenticatorData` (base64url, no padding)."},"transports":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"Transport hints reported by the authenticator (e.g. `internal`, `hybrid`). Advisory; carried through to the published verificationMethod\'s `webauthnTransports`."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"Optional operator-supplied label (e.g. \\"MacBook Touch ID\\"), carried through to the published verificationMethod."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Passkey-VM Enroll Submit — response payload","description":"Successful enrolment. The verificationMethod has already been appended to the DID document via a WebVH log entry at the returned version.","type":"object","additionalProperties":false,"required":["verificationMethod","webvhVersion"],"properties":{"verificationMethod":{"\$ref":"#/\$defs/PasskeyVerificationMethod","description":"The verificationMethod entry exactly as it now appears in the DID document. `publicKeyMultibase` here is the server-re-derived key, which is authoritative."},"webvhVersion":{"type":"string","minLength":1,"description":"The WebVH log-entry version that recorded the change (e.g. \\"3-Qm…\\")."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"PasskeyVerificationMethod":{"title":"PasskeyVerificationMethod","type":"object","additionalProperties":false,"required":["id","type","controller","publicKeyMultibase","webauthnCredentialId"],"properties":{"id":{"type":"string","minLength":1,"description":"The verificationMethod id as it appears in the DID document: `<did>#passkey-<base64url(sha256(credentialId))>`. The fragment is content-derived so a verifier can locate this VM by recomputing `sha256(credential.id)`."},"type":{"type":"string","const":"Multikey","description":"Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID resolvers and verifiers need no WebAuthn-specific knowledge to consume it."},"controller":{"type":"string","minLength":1,"description":"The DID this verificationMethod is published on (the DID being augmented)."},"publicKeyMultibase":{"type":"string","minLength":1,"description":"W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is the value a verifier validates a WebAuthn assertion against. Re-derived server-side from the attestation at enrolment — never trusted from the browser (see vta/passkey-vms/enroll-submit)."},"webauthnCredentialId":{"type":"string","minLength":1,"description":"The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute `sha256(credentialId)` and match the assertion to this VM\'s `id` fragment."},"webauthnTransports":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`, `nfc`, `ble`). Advisory only — verifiers MUST NOT make trust decisions based on transport hints."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"Optional operator-supplied human-readable label (e.g. \\"MacBook Touch ID\\"). Informational; not authoritative and not a security input."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Passkey-VM Enroll Submit — response payload","description":"Successful enrolment. The verificationMethod has already been appended to the DID document via a WebVH log entry at the returned version.","type":"object","additionalProperties":false,"required":["verificationMethod","webvhVersion"],"properties":{"verificationMethod":{"\$ref":"#/\$defs/PasskeyVerificationMethod","description":"The verificationMethod entry exactly as it now appears in the DID document. `publicKeyMultibase` here is the server-re-derived key, which is authoritative."},"webvhVersion":{"type":"string","minLength":1,"description":"The WebVH log-entry version that recorded the change (e.g. \\"3-Qm…\\")."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"PasskeyVerificationMethod":{"title":"PasskeyVerificationMethod","type":"object","additionalProperties":false,"required":["id","type","controller","publicKeyMultibase","webauthnCredentialId"],"properties":{"id":{"type":"string","minLength":1,"description":"The verificationMethod id as it appears in the DID document: `<did>#passkey-<base64url(sha256(credentialId))>`. The fragment is content-derived so a verifier can locate this VM by recomputing `sha256(credential.id)`."},"type":{"type":"string","const":"Multikey","description":"Always `Multikey`. The WebAuthn public key is published in W3C Multikey form so DID resolvers and verifiers need no WebAuthn-specific knowledge to consume it."},"controller":{"type":"string","minLength":1,"description":"The DID this verificationMethod is published on (the DID being augmented)."},"publicKeyMultibase":{"type":"string","minLength":1,"description":"W3C Multikey (multibase) encoding of the WebAuthn credential public key. This is the value a verifier validates a WebAuthn assertion against. Re-derived server-side from the attestation at enrolment — never trusted from the browser (see vta/passkey-vms/enroll-submit)."},"webauthnCredentialId":{"type":"string","minLength":1,"description":"The WebAuthn `credential.id` (base64url, no padding). Lets a verifier recompute `sha256(credentialId)` and match the assertion to this VM\'s `id` fragment."},"webauthnTransports":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"Transport hints reported by the authenticator (e.g. `internal`, `hybrid`, `usb`, `nfc`, `ble`). Advisory only — verifiers MUST NOT make trust decisions based on transport hints."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"Optional operator-supplied human-readable label (e.g. \\"MacBook Touch ID\\"). Informational; not authoritative and not a security input."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
