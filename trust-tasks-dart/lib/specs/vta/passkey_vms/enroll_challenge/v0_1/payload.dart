// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/passkey-vms/enroll-challenge/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The server-issued WebAuthn registration challenge. All byte-valued fields are
/// base64url-encoded (no padding); the browser passes the decoded bytes to
/// navigator.credentials.create.
class Response {
  const Response({
    required this.ceremonyId,
    required this.challenge,
    required this.rpId,
    required this.rpName,
    required this.userHandle,
    required this.userName,
    required this.userDisplayName,
    this.timeoutMs,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        ceremonyId: json['ceremonyId'] as String,
        challenge: json['challenge'] as String,
        rpId: json['rpId'] as String,
        rpName: json['rpName'] as String,
        userHandle: json['userHandle'] as String,
        userName: json['userName'] as String,
        userDisplayName: json['userDisplayName'] as String,
        timeoutMs: json['timeoutMs'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque server-side ceremony id. The producer MUST echo this back in the
  /// vta/passkey-vms/enroll-submit request. Binds the challenge, the target DID, and the
  /// eventual submission together.
  final String ceremonyId;

  /// WebAuthn challenge (base64url, no padding; at least 32 random bytes).
  final String challenge;

  /// WebAuthn Relying-Party identifier — a DNS name that matches the origin the browser
  /// is served from.
  final String rpId;

  /// Human-readable Relying-Party name.
  final String rpName;

  /// Stable WebAuthn user handle (base64url). Opaque to the client; the VTA derives a
  /// per-DID handle.
  final String userHandle;

  /// WebAuthn user name (e.g. the DID or the operator-supplied label).
  final String userName;

  /// WebAuthn user display name.
  final String userDisplayName;

  /// Suggested ceremony timeout in milliseconds. Advisory; the authenticator and browser
  /// apply their own limits.
  final int? timeoutMs;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'ceremonyId': ceremonyId,
        'challenge': challenge,
        'rpId': rpId,
        'rpName': rpName,
        'userHandle': userHandle,
        'userName': userName,
        'userDisplayName': userDisplayName,
        if (timeoutMs != null) 'timeoutMs': timeoutMs!,
        if (ext != null) 'ext': ext!,
      };
}

/// Request a fresh WebAuthn registration challenge for adding a passkey
/// verificationMethod to a VTA-managed DID. Step 1 of the two-step enrolment ceremony
/// (challenge → submit). The producer must hold the admin role on the target DID's
/// context.
class Payload {
  const Payload({
    required this.did,
    this.label,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        label: json['label'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID the new passkey verificationMethod will be added to. The producer MUST hold
  /// the admin role on this DID's context.
  final String did;

  /// Optional operator-supplied label for the new passkey (e.g. "MacBook Touch ID").
  /// Carried through to the WebAuthn user name and, if the ceremony completes, to the
  /// published verificationMethod.
  final String? label;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (label != null) 'label': label!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/passkey-vms/enroll-challenge/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/passkey-vms/enroll-challenge/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/passkey-vms/enroll-challenge/0.1","title":"VTA Passkey-VM Enroll Challenge — payload","description":"Request a fresh WebAuthn registration challenge for adding a passkey verificationMethod to a VTA-managed DID. Step 1 of the two-step enrolment ceremony (challenge → submit). The producer must hold the admin role on the target DID\'s context.","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID the new passkey verificationMethod will be added to. The producer MUST hold the admin role on this DID\'s context."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"Optional operator-supplied label for the new passkey (e.g. \\"MacBook Touch ID\\"). Carried through to the WebAuthn user name and, if the ceremony completes, to the published verificationMethod."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Passkey-VM Enroll Challenge — response payload","description":"The server-issued WebAuthn registration challenge. All byte-valued fields are base64url-encoded (no padding); the browser passes the decoded bytes to navigator.credentials.create.","type":"object","additionalProperties":false,"required":["ceremonyId","challenge","rpId","rpName","userHandle","userName","userDisplayName"],"properties":{"ceremonyId":{"type":"string","minLength":1,"description":"Opaque server-side ceremony id. The producer MUST echo this back in the vta/passkey-vms/enroll-submit request. Binds the challenge, the target DID, and the eventual submission together."},"challenge":{"type":"string","minLength":1,"description":"WebAuthn challenge (base64url, no padding; at least 32 random bytes)."},"rpId":{"type":"string","minLength":1,"description":"WebAuthn Relying-Party identifier — a DNS name that matches the origin the browser is served from."},"rpName":{"type":"string","minLength":1,"maxLength":64,"description":"Human-readable Relying-Party name."},"userHandle":{"type":"string","minLength":1,"description":"Stable WebAuthn user handle (base64url). Opaque to the client; the VTA derives a per-DID handle."},"userName":{"type":"string","minLength":1,"maxLength":64,"description":"WebAuthn user name (e.g. the DID or the operator-supplied label)."},"userDisplayName":{"type":"string","minLength":1,"maxLength":64,"description":"WebAuthn user display name."},"timeoutMs":{"type":"integer","minimum":0,"description":"Suggested ceremony timeout in milliseconds. Advisory; the authenticator and browser apply their own limits."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Passkey-VM Enroll Challenge — response payload","description":"The server-issued WebAuthn registration challenge. All byte-valued fields are base64url-encoded (no padding); the browser passes the decoded bytes to navigator.credentials.create.","type":"object","additionalProperties":false,"required":["ceremonyId","challenge","rpId","rpName","userHandle","userName","userDisplayName"],"properties":{"ceremonyId":{"type":"string","minLength":1,"description":"Opaque server-side ceremony id. The producer MUST echo this back in the vta/passkey-vms/enroll-submit request. Binds the challenge, the target DID, and the eventual submission together."},"challenge":{"type":"string","minLength":1,"description":"WebAuthn challenge (base64url, no padding; at least 32 random bytes)."},"rpId":{"type":"string","minLength":1,"description":"WebAuthn Relying-Party identifier — a DNS name that matches the origin the browser is served from."},"rpName":{"type":"string","minLength":1,"maxLength":64,"description":"Human-readable Relying-Party name."},"userHandle":{"type":"string","minLength":1,"description":"Stable WebAuthn user handle (base64url). Opaque to the client; the VTA derives a per-DID handle."},"userName":{"type":"string","minLength":1,"maxLength":64,"description":"WebAuthn user name (e.g. the DID or the operator-supplied label)."},"userDisplayName":{"type":"string","minLength":1,"maxLength":64,"description":"WebAuthn user display name."},"timeoutMs":{"type":"integer","minimum":0,"description":"Suggested ceremony timeout in milliseconds. Advisory; the authenticator and browser apply their own limits."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
