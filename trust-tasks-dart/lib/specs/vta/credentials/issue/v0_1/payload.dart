// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/credentials/issue/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Claims profile activated by credentialType "GovernancePolicyCredential": the
/// issuing authority attests the complete governing parameter document for a domain,
/// so that enforcement components load policy out of the credential and attestations
/// cite a policyHash whose issuance chain a verifier can check. When the request's
/// credentialType is GovernancePolicyCredential, payload.claims MUST validate against
/// this definition; the consumer refuses a mismatch with
/// vta/credentials/issue:profileViolation.
class GovernancePolicyClaims {
  const GovernancePolicyClaims({
    required this.domain,
    this.contextId,
    required this.policy,
    required this.policyHash,
    this.policyMediaType,
  });

  /// Read this payload from a decoded JSON object.
  factory GovernancePolicyClaims.fromJson(Map<String, dynamic> json) =>
      GovernancePolicyClaims(
        domain: json['domain'] as String,
        contextId: json['contextId'] as String?,
        policy: json['policy'] as Map<String, dynamic>,
        policyHash: json['policyHash'] as String,
        policyMediaType: json['policyMediaType'] as String?,
      );

  /// The domain (scope of enforcement) the policy governs — the identifier attestations
  /// bind to. Opaque to the framework; typically a DID or a deployment-scoped domain
  /// name.
  final String domain;

  /// Trust context the domain belongs to, when the issuing VTA partitions per context.
  final String? contextId;

  /// The complete governing parameter document (for an LLM gateway: model allowlist,
  /// budget caps, upstream pin, privacy tier, …). Opaque to the framework. The enforcing
  /// component MUST load its runtime policy from this object — citing the credential
  /// while enforcing locally-held configuration defeats the profile's purpose.
  final Map<String, dynamic> policy;

  /// Multibase-encoded multihash over the JCS (RFC 8785) canonicalization of `policy`.
  /// This is the value attestations cite; the consumer MUST verify it matches `policy`
  /// before minting and refuse a mismatch with profileViolation.
  final String policyHash;

  /// Media type of the `policy` document. Default application/json.
  final String? policyMediaType;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'domain': domain,
        if (contextId != null) 'contextId': contextId!,
        'policy': policy,
        'policyHash': policyHash,
        if (policyMediaType != null) 'policyMediaType': policyMediaType!,
      };
}

/// Stable identifier for an issued credential — the handle for revocation and audit.
/// Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be
/// parsed.
typedef CredentialId = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The success response to a vta/credentials/issue request. Carried in a Trust Task
/// document whose type is
/// https://trusttasks.org/spec/vta/credentials/issue/0.1#response. This is the
/// credentials/_shared IssuedCredential shape — same members, same required set — plus
/// `supersedes` and `ext`. It states them inline rather than `$ref`-ing the shared
/// definition because that definition sets `additionalProperties: false`, which under
/// `allOf` would reject the two extra members. The shared CredentialId is still
/// referenced. Keep the two in step by hand if either changes.
class Response {
  const Response({
    required this.credentialId,
    required this.expiresAt,
    required this.credential,
    this.supersedes,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        credentialId: json['credentialId'] as String,
        expiresAt: json['expiresAt'] as String,
        credential: json['credential'] as Map<String, dynamic>,
        supersedes: json['supersedes'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final CredentialId credentialId;

  /// When the credential's validUntil falls due.
  final String expiresAt;

  /// The issued Verifiable Credential (W3C VC Data Model 2.0), signed by the issuing
  /// context's key.
  final Map<String, dynamic> credential;

  /// credentialId of the previously-active credential this issuance revoked, for claims
  /// profiles with a single-active rule (GovernancePolicyCredential). Mirrors
  /// policy/activate's previousPolicyId: it makes the rotation auditable and reversible.
  /// Absent when nothing was displaced.
  final String? supersedes;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentialId': credentialId,
        'expiresAt': expiresAt,
        'credential': credential,
        if (supersedes != null) 'supersedes': supersedes!,
        if (ext != null) 'ext': ext!,
      };
}

/// VTA Credentials Issue — payload
class Payload {
  const Payload({
    required this.holder,
    required this.claims,
    this.credentialType,
    required this.validitySeconds,
    this.purpose,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        holder: json['holder'] as String,
        claims: json['claims'] as Map<String, dynamic>,
        credentialType: json['credentialType'] as String?,
        validitySeconds: json['validitySeconds'] as int,
        purpose: json['purpose'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// DID of the credential's subject/holder (becomes credentialSubject.id).
  final String holder;

  /// The scoped claims to attest — the share's scope. Opaque to the framework;
  /// non-empty.
  final Map<String, dynamic> claims;

  /// Additional credential type beyond VerifiableCredential (e.g.
  /// ScopedShareCredential). Some values name a claims profile defined by this
  /// specification (e.g. GovernancePolicyCredential), which constrains the shape of
  /// `claims`.
  final String? credentialType;

  /// Credential lifetime in seconds from issuance. The issuer MAY cap this.
  final int validitySeconds;

  /// Optional human-readable rationale, recorded for audit.
  final String? purpose;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'holder': holder,
        'claims': claims,
        if (credentialType != null) 'credentialType': credentialType!,
        'validitySeconds': validitySeconds,
        if (purpose != null) 'purpose': purpose!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/credentials/issue/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/credentials/issue/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/credentials/issue/0.1","title":"VTA Credentials Issue — payload","type":"object","additionalProperties":false,"required":["holder","claims","validitySeconds"],"properties":{"holder":{"type":"string","description":"DID of the credential\'s subject/holder (becomes credentialSubject.id).","pattern":"^did:"},"claims":{"type":"object","description":"The scoped claims to attest — the share\'s scope. Opaque to the framework; non-empty.","minProperties":1},"credentialType":{"type":"string","description":"Additional credential type beyond VerifiableCredential (e.g. ScopedShareCredential). Some values name a claims profile defined by this specification (e.g. GovernancePolicyCredential), which constrains the shape of `claims`."},"validitySeconds":{"type":"integer","minimum":1,"description":"Credential lifetime in seconds from issuance. The issuer MAY cap this."},"purpose":{"type":"string","maxLength":1024,"description":"Optional human-readable rationale, recorded for audit."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"GovernancePolicyClaims":{"\$anchor":"governancePolicyClaims","title":"GovernancePolicyClaims","description":"Claims profile activated by credentialType \\"GovernancePolicyCredential\\": the issuing authority attests the complete governing parameter document for a domain, so that enforcement components load policy out of the credential and attestations cite a policyHash whose issuance chain a verifier can check. When the request\'s credentialType is GovernancePolicyCredential, payload.claims MUST validate against this definition; the consumer refuses a mismatch with vta/credentials/issue:profileViolation.","type":"object","additionalProperties":false,"required":["domain","policy","policyHash"],"properties":{"domain":{"type":"string","minLength":1,"description":"The domain (scope of enforcement) the policy governs — the identifier attestations bind to. Opaque to the framework; typically a DID or a deployment-scoped domain name."},"contextId":{"type":"string","minLength":1,"description":"Trust context the domain belongs to, when the issuing VTA partitions per context."},"policy":{"type":"object","minProperties":1,"description":"The complete governing parameter document (for an LLM gateway: model allowlist, budget caps, upstream pin, privacy tier, …). Opaque to the framework. The enforcing component MUST load its runtime policy from this object — citing the credential while enforcing locally-held configuration defeats the profile\'s purpose."},"policyHash":{"type":"string","minLength":1,"description":"Multibase-encoded multihash over the JCS (RFC 8785) canonicalization of `policy`. This is the value attestations cite; the consumer MUST verify it matches `policy` before minting and refuse a mismatch with profileViolation."},"policyMediaType":{"type":"string","description":"Media type of the `policy` document. Default application/json."}}},"Response":{"\$anchor":"response","title":"VTA Credentials Issue — response payload","description":"The success response to a vta/credentials/issue request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/vta/credentials/issue/0.1#response.\\n\\nThis is the credentials/_shared IssuedCredential shape — same members, same required set — plus `supersedes` and `ext`. It states them inline rather than `\$ref`-ing the shared definition because that definition sets `additionalProperties: false`, which under `allOf` would reject the two extra members. The shared CredentialId is still referenced. Keep the two in step by hand if either changes.","type":"object","additionalProperties":false,"required":["credentialId","credential","expiresAt"],"properties":{"credentialId":{"\$ref":"#/\$defs/CredentialId"},"expiresAt":{"type":"string","format":"date-time","description":"When the credential\'s validUntil falls due."},"credential":{"type":"object","description":"The issued Verifiable Credential (W3C VC Data Model 2.0), signed by the issuing context\'s key."},"supersedes":{"type":"string","description":"credentialId of the previously-active credential this issuance revoked, for claims profiles with a single-active rule (GovernancePolicyCredential). Mirrors policy/activate\'s previousPolicyId: it makes the rotation auditable and reversible. Absent when nothing was displaced."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialId":{"\$anchor":"credentialId","title":"CredentialId","description":"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.","type":"string","minLength":1}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"GovernancePolicyClaims":{"\$anchor":"governancePolicyClaims","title":"GovernancePolicyClaims","description":"Claims profile activated by credentialType \\"GovernancePolicyCredential\\": the issuing authority attests the complete governing parameter document for a domain, so that enforcement components load policy out of the credential and attestations cite a policyHash whose issuance chain a verifier can check. When the request\'s credentialType is GovernancePolicyCredential, payload.claims MUST validate against this definition; the consumer refuses a mismatch with vta/credentials/issue:profileViolation.","type":"object","additionalProperties":false,"required":["domain","policy","policyHash"],"properties":{"domain":{"type":"string","minLength":1,"description":"The domain (scope of enforcement) the policy governs — the identifier attestations bind to. Opaque to the framework; typically a DID or a deployment-scoped domain name."},"contextId":{"type":"string","minLength":1,"description":"Trust context the domain belongs to, when the issuing VTA partitions per context."},"policy":{"type":"object","minProperties":1,"description":"The complete governing parameter document (for an LLM gateway: model allowlist, budget caps, upstream pin, privacy tier, …). Opaque to the framework. The enforcing component MUST load its runtime policy from this object — citing the credential while enforcing locally-held configuration defeats the profile\'s purpose."},"policyHash":{"type":"string","minLength":1,"description":"Multibase-encoded multihash over the JCS (RFC 8785) canonicalization of `policy`. This is the value attestations cite; the consumer MUST verify it matches `policy` before minting and refuse a mismatch with profileViolation."},"policyMediaType":{"type":"string","description":"Media type of the `policy` document. Default application/json."}}},"Response":{"\$anchor":"response","title":"VTA Credentials Issue — response payload","description":"The success response to a vta/credentials/issue request. Carried in a Trust Task document whose type is https://trusttasks.org/spec/vta/credentials/issue/0.1#response.\\n\\nThis is the credentials/_shared IssuedCredential shape — same members, same required set — plus `supersedes` and `ext`. It states them inline rather than `\$ref`-ing the shared definition because that definition sets `additionalProperties: false`, which under `allOf` would reject the two extra members. The shared CredentialId is still referenced. Keep the two in step by hand if either changes.","type":"object","additionalProperties":false,"required":["credentialId","credential","expiresAt"],"properties":{"credentialId":{"\$ref":"#/\$defs/CredentialId"},"expiresAt":{"type":"string","format":"date-time","description":"When the credential\'s validUntil falls due."},"credential":{"type":"object","description":"The issued Verifiable Credential (W3C VC Data Model 2.0), signed by the issuing context\'s key."},"supersedes":{"type":"string","description":"credentialId of the previously-active credential this issuance revoked, for claims profiles with a single-active rule (GovernancePolicyCredential). Mirrors policy/activate\'s previousPolicyId: it makes the rotation auditable and reversible. Absent when nothing was displaced."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialId":{"\$anchor":"credentialId","title":"CredentialId","description":"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.","type":"string","minLength":1}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
