// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/credentials/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Stable identifier for an issued credential — the handle for revocation and audit.
/// Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be
/// parsed.
typedef CredentialId = String;

/// Did, defined by this specification's schema.
typedef Did = String;

/// Derived at read time. `revoked` takes precedence over `expired`: a credential
/// revoked before its window closed is revoked, and reporting it as merely expired
/// would hide that somebody acted.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const IssuedCredentialStatus(String value) {
  static const IssuedCredentialStatus active = IssuedCredentialStatus('active');
  static const IssuedCredentialStatus expired =
      IssuedCredentialStatus('expired');
  static const IssuedCredentialStatus revoked =
      IssuedCredentialStatus('revoked');

  /// Every value this specification's schema permits.
  static const List<IssuedCredentialStatus> values = <IssuedCredentialStatus>[
    active,
    expired,
    revoked
  ];
}

/// A body-free projection of an issuance record. Carries no claim content by
/// construction — see the specification's rationale for why a list does not return
/// credentials.
class IssuedCredentialSummary {
  const IssuedCredentialSummary({
    required this.credentialId,
    required this.holder,
    this.credentialType,
    required this.issuedAt,
    required this.expiresAt,
    required this.status,
    this.revokedAt,
    this.revocationReason,
  });

  /// Read this payload from a decoded JSON object.
  factory IssuedCredentialSummary.fromJson(Map<String, dynamic> json) =>
      IssuedCredentialSummary(
        credentialId: json['credentialId'] as String,
        holder: json['holder'] as String,
        credentialType: json['credentialType'] as String?,
        issuedAt: json['issuedAt'] as String,
        expiresAt: json['expiresAt'] as String,
        status: IssuedCredentialStatus(json['status'] as String),
        revokedAt: json['revokedAt'] as String?,
        revocationReason: json['revocationReason'] as String?,
      );

  /// The id `vta/credentials/revoke` is keyed on.
  final CredentialId credentialId;

  /// Who the credential was issued to.
  final Did holder;

  /// The type tag beyond `VerifiableCredential`, when the credential carries one.
  final String? credentialType;
  final String issuedAt;
  final String expiresAt;
  final IssuedCredentialStatus status;

  /// Present iff `status` is `revoked`.
  final String? revokedAt;

  /// Free text recorded at revocation. Disclosed to every caller entitled to this list —
  /// see Security & Privacy.
  final String? revocationReason;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentialId': credentialId,
        'holder': holder,
        if (credentialType != null) 'credentialType': credentialType!,
        'issuedAt': issuedAt,
        'expiresAt': expiresAt,
        'status': status.value,
        if (revokedAt != null) 'revokedAt': revokedAt!,
        if (revocationReason != null) 'revocationReason': revocationReason!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTA Credentials List — response payload
class Response {
  const Response({
    required this.credentials,
    required this.truncated,
    this.cursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        credentials: (json['credentials'] as List<dynamic>)
            .map((e) =>
                IssuedCredentialSummary.fromJson(e as Map<String, dynamic>))
            .toList(),
        truncated: json['truncated'] as bool,
        cursor: json['cursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// One summary per matching credential. Empty when nothing matched — a successful
  /// answer, not an error.
  final List<IssuedCredentialSummary> credentials;

  /// The agent stopped early. A consumer must not read a truncated page as a complete
  /// account of what was issued.
  final bool truncated;

  /// Pass to the next request to continue. Absent when the page is the last.
  final String? cursor;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentials': credentials.map((e) => e.toJson()).toList(),
        'truncated': truncated,
        if (cursor != null) 'cursor': cursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// Enumerate the credentials an agent has issued, as metadata. Every filter is
/// optional and those present are AND-combined.
class Payload {
  const Payload({
    this.holder,
    this.credentialType,
    this.status,
    this.pageSize,
    this.cursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        holder: json['holder'] as String?,
        credentialType: json['credentialType'] as String?,
        status: json['status'] == null
            ? null
            : IssuedCredentialStatus(json['status'] as String),
        pageSize: json['pageSize'] as int?,
        cursor: json['cursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Only credentials issued to this DID.
  final Did? holder;

  /// Only credentials carrying this type tag beyond `VerifiableCredential`.
  final String? credentialType;

  /// Only credentials in this state. Derived by the agent at read time, never stored.
  final IssuedCredentialStatus? status;

  /// Maximum records to return. The agent MAY return fewer and caps this value.
  final int? pageSize;

  /// Continue a previous page. Opaque — consumers must not construct or parse one.
  final String? cursor;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (holder != null) 'holder': holder!,
        if (credentialType != null) 'credentialType': credentialType!,
        if (status != null) 'status': status!.value,
        if (pageSize != null) 'pageSize': pageSize!,
        if (cursor != null) 'cursor': cursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/credentials/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/credentials/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/credentials/list/0.1","title":"VTA Credentials — List","description":"Enumerate the credentials an agent has issued, as metadata. Every filter is optional and those present are AND-combined.","type":"object","additionalProperties":false,"properties":{"holder":{"\$ref":"#/\$defs/Did","description":"Only credentials issued to this DID."},"credentialType":{"type":"string","minLength":1,"maxLength":256,"description":"Only credentials carrying this type tag beyond `VerifiableCredential`."},"status":{"\$ref":"#/\$defs/IssuedCredentialStatus","description":"Only credentials in this state. Derived by the agent at read time, never stored."},"pageSize":{"type":"integer","minimum":1,"maximum":1000,"description":"Maximum records to return. The agent MAY return fewer and caps this value."},"cursor":{"type":"string","minLength":1,"maxLength":4096,"description":"Continue a previous page. Opaque — consumers must not construct or parse one."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Credentials List — response payload","type":"object","additionalProperties":false,"required":["credentials","truncated"],"properties":{"credentials":{"type":"array","items":{"\$ref":"#/\$defs/IssuedCredentialSummary"},"description":"One summary per matching credential. Empty when nothing matched — a successful answer, not an error."},"truncated":{"type":"boolean","description":"The agent stopped early. A consumer must not read a truncated page as a complete account of what was issued."},"cursor":{"type":"string","minLength":1,"maxLength":4096,"description":"Pass to the next request to continue. Absent when the page is the last."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"IssuedCredentialSummary":{"title":"IssuedCredentialSummary","description":"A body-free projection of an issuance record. Carries no claim content by construction — see the specification\'s rationale for why a list does not return credentials.","type":"object","additionalProperties":false,"required":["credentialId","holder","issuedAt","expiresAt","status"],"properties":{"credentialId":{"\$ref":"#/\$defs/CredentialId","description":"The id `vta/credentials/revoke` is keyed on."},"holder":{"\$ref":"#/\$defs/Did","description":"Who the credential was issued to."},"credentialType":{"type":"string","minLength":1,"maxLength":256,"description":"The type tag beyond `VerifiableCredential`, when the credential carries one."},"issuedAt":{"type":"string","format":"date-time"},"expiresAt":{"type":"string","format":"date-time"},"status":{"\$ref":"#/\$defs/IssuedCredentialStatus"},"revokedAt":{"type":"string","format":"date-time","description":"Present iff `status` is `revoked`."},"revocationReason":{"type":"string","maxLength":1024,"description":"Free text recorded at revocation. Disclosed to every caller entitled to this list — see Security & Privacy."}}},"IssuedCredentialStatus":{"type":"string","enum":["active","expired","revoked"],"description":"Derived at read time. `revoked` takes precedence over `expired`: a credential revoked before its window closed is revoked, and reporting it as merely expired would hide that somebody acted."},"Did":{"type":"string","pattern":"^did:[a-z0-9]+:.+\$","maxLength":2048},"CredentialId":{"\$anchor":"credentialId","title":"CredentialId","description":"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.","type":"string","minLength":1},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Credentials List — response payload","type":"object","additionalProperties":false,"required":["credentials","truncated"],"properties":{"credentials":{"type":"array","items":{"\$ref":"#/\$defs/IssuedCredentialSummary"},"description":"One summary per matching credential. Empty when nothing matched — a successful answer, not an error."},"truncated":{"type":"boolean","description":"The agent stopped early. A consumer must not read a truncated page as a complete account of what was issued."},"cursor":{"type":"string","minLength":1,"maxLength":4096,"description":"Pass to the next request to continue. Absent when the page is the last."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"IssuedCredentialSummary":{"title":"IssuedCredentialSummary","description":"A body-free projection of an issuance record. Carries no claim content by construction — see the specification\'s rationale for why a list does not return credentials.","type":"object","additionalProperties":false,"required":["credentialId","holder","issuedAt","expiresAt","status"],"properties":{"credentialId":{"\$ref":"#/\$defs/CredentialId","description":"The id `vta/credentials/revoke` is keyed on."},"holder":{"\$ref":"#/\$defs/Did","description":"Who the credential was issued to."},"credentialType":{"type":"string","minLength":1,"maxLength":256,"description":"The type tag beyond `VerifiableCredential`, when the credential carries one."},"issuedAt":{"type":"string","format":"date-time"},"expiresAt":{"type":"string","format":"date-time"},"status":{"\$ref":"#/\$defs/IssuedCredentialStatus"},"revokedAt":{"type":"string","format":"date-time","description":"Present iff `status` is `revoked`."},"revocationReason":{"type":"string","maxLength":1024,"description":"Free text recorded at revocation. Disclosed to every caller entitled to this list — see Security & Privacy."}}},"IssuedCredentialStatus":{"type":"string","enum":["active","expired","revoked"],"description":"Derived at read time. `revoked` takes precedence over `expired`: a credential revoked before its window closed is revoked, and reporting it as merely expired would hide that somebody acted."},"Did":{"type":"string","pattern":"^did:[a-z0-9]+:.+\$","maxLength":2048},"CredentialId":{"\$anchor":"credentialId","title":"CredentialId","description":"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.","type":"string","minLength":1},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
