// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/_shared/0.1/webvh-records.schema.json

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A did:webvh DID as the VTA holds it. The DID document itself is not here — it lives
/// in the log, which `vta/webvh/dids/get` returns on request.
class WebvhDidRecord {
  const WebvhDidRecord({
    required this.did,
    required this.serverId,
    required this.mnemonic,
    required this.scid,
    required this.contextId,
    required this.portable,
    required this.logEntryCount,
    this.preRotationCount,
    this.nextFragmentId,
    required this.createdAt,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory WebvhDidRecord.fromJson(Map<String, dynamic> json) => WebvhDidRecord(
        did: json['did'] as String,
        serverId: json['serverId'] as String,
        mnemonic: json['mnemonic'] as String,
        scid: json['scid'] as String,
        contextId: json['contextId'] as String,
        portable: json['portable'] as bool,
        logEntryCount: json['logEntryCount'] as int,
        preRotationCount: json['preRotationCount'] as int?,
        nextFragmentId: json['nextFragmentId'] as int?,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID, e.g. `did:webvh:&lt;scid&gt;:example.com:alice`.
  final String did;

  /// Id of the hosting server serving this DID's log. Empty for a serverless DID, whose
  /// location comes from the URL it was created with.
  final String serverId;

  /// The hosting server's handle for the record. Unrelated to a BIP-39 phrase; the
  /// collision of terms is historical.
  final String mnemonic;

  /// Self-certifying identifier: the hash committing to the log's first entry. It is
  /// what makes the DID's history tamper-evident, and it changes only when the log is
  /// re-created — never in place.
  final String scid;

  /// Context this DID belongs to. Deleting that context destroys this DID.
  final String contextId;

  /// Whether the DID may be moved to another domain without losing its identity. Fixed
  /// at creation: a DID created non-portable cannot be made portable later, because
  /// portability is a commitment recorded in the log's first entry.
  final bool portable;

  /// Number of entries in the log.
  final int logEntryCount;

  /// Pre-rotation keys committed by the most recent entry. `0` means pre-rotation is
  /// disabled — a compromise of the current key is then unrecoverable by rotation,
  /// because the successor was never committed in advance.
  final int? preRotationCount;

  /// Next `#key-{n}` fragment id to mint. Monotonic and never decremented, so a fragment
  /// is never reused for a different key.
  final int? nextFragmentId;
  final String createdAt;
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'serverId': serverId,
        'mnemonic': mnemonic,
        'scid': scid,
        'contextId': contextId,
        'portable': portable,
        'logEntryCount': logEntryCount,
        if (preRotationCount != null) 'preRotationCount': preRotationCount!,
        if (nextFragmentId != null) 'nextFragmentId': nextFragmentId!,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// A hosting server the VTA is registered with. Carries no credential: what the VTA
/// holds to authenticate against the server is never part of this record and never
/// leaves it.
class WebvhServerRecord {
  const WebvhServerRecord({
    required this.id,
    required this.did,
    this.label,
    required this.createdAt,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory WebvhServerRecord.fromJson(Map<String, dynamic> json) =>
      WebvhServerRecord(
        id: json['id'] as String,
        did: json['did'] as String,
        label: json['label'] as String?,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Local id for the server, chosen by the operator.
  final String id;

  /// The server's own DID, used to authenticate it.
  final String did;
  final String? label;
  final String createdAt;
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'did': did,
        if (label != null) 'label': label!,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// An agent name bound to a DID. A name is only ever authoritative in the DID→name
/// direction: the DID document's `alsoKnownAs` is what proves the binding, because
/// anyone controlling a domain can point a name at somebody else's DID.
class AgentNameEntry {
  const AgentNameEntry({
    required this.name,
    required this.enabled,
    this.createdAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory AgentNameEntry.fromJson(Map<String, dynamic> json) => AgentNameEntry(
        name: json['name'] as String,
        enabled: json['enabled'] as bool,
        createdAt: json['createdAt'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The name, without the host — the full form is `&lt;host&gt;/@&lt;name&gt;`.
  final String name;

  /// Whether the name currently resolves. A disabled name is still held — it is not
  /// available for anyone else to take.
  final bool enabled;

  /// Unix epoch seconds.
  final int? createdAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'enabled': enabled,
        if (createdAt != null) 'createdAt': createdAt!,
        if (ext != null) 'ext': ext!,
      };
}
