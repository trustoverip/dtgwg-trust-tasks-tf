// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/services/rollback/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// What the rollback did. `disabled` re-disabled the transport, `enabled`
/// re-advertised it at its prior settings, `updated` restored prior settings on an
/// entry that stayed advertised, and `noOp` means the previous state already matched —
/// nothing was written.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const RollbackResultKind(String value) {
  static const RollbackResultKind disabled = RollbackResultKind('disabled');
  static const RollbackResultKind enabled = RollbackResultKind('enabled');
  static const RollbackResultKind updated = RollbackResultKind('updated');
  static const RollbackResultKind noOp = RollbackResultKind('noOp');

  /// Every value this specification's schema permits.
  static const List<RollbackResultKind> values = <RollbackResultKind>[
    disabled,
    enabled,
    updated,
    noOp
  ];
}

/// The outcome of a rollback. Distinct from ServiceMutationResult because a rollback
/// can legitimately publish nothing: if the previous state already equals the current
/// one there is no change to write, and `kind: "noOp"` says so with
/// `logEntryVersionId` absent. Treating that as a failure would be wrong — the
/// requested state holds — and treating it as an ordinary success would report a log
/// entry that does not exist.
class RollbackResult {
  const RollbackResult({
    required this.kind,
    this.logEntryVersionId,
    this.effectiveAt,
    this.drainingMediator,
    this.drainUntil,
    this.vtaDid,
    this.serverless,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory RollbackResult.fromJson(Map<String, dynamic> json) => RollbackResult(
        kind: RollbackResultKind(json['kind'] as String),
        logEntryVersionId: json['logEntryVersionId'] as String?,
        effectiveAt: json['effectiveAt'] as String?,
        drainingMediator: json['drainingMediator'] as String?,
        drainUntil: json['drainUntil'] as String?,
        vtaDid: json['vtaDid'] as String?,
        serverless: json['serverless'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// What the rollback did. `disabled` re-disabled the transport, `enabled`
  /// re-advertised it at its prior settings, `updated` restored prior settings on an
  /// entry that stayed advertised, and `noOp` means the previous state already matched —
  /// nothing was written.
  final RollbackResultKind kind;

  /// Version id of the log entry the rollback wrote. **Absent when `kind` is `noOp`**,
  /// and present otherwise.
  final String? logEntryVersionId;

  /// RFC 3339 instant the rollback took effect. Absent for `noOp`.
  final String? effectiveAt;

  /// Present when rolling back DIDComm left a mediator draining.
  final String? drainingMediator;

  /// When that drain completes.
  final String? drainUntil;

  /// The agent's own DID.
  final String? vtaDid;

  /// True when the entry was written locally but not published — the operator must
  /// redeploy before any verifier sees it.
  final bool? serverless;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind.value,
        if (logEntryVersionId != null) 'logEntryVersionId': logEntryVersionId!,
        if (effectiveAt != null) 'effectiveAt': effectiveAt!,
        if (drainingMediator != null) 'drainingMediator': drainingMediator!,
        if (drainUntil != null) 'drainUntil': drainUntil!,
        if (vtaDid != null) 'vtaDid': vtaDid!,
        if (serverless != null) 'serverless': serverless!,
        if (ext != null) 'ext': ext!,
      };
}

/// Success response to vta/services/rollback. Type
/// https://trusttasks.org/spec/vta/services/rollback/1.0#response.
class Response {
  const Response({
    required this.result,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        result: RollbackResult.fromJson(json['result'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final RollbackResult result;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'result': result.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Which transport a task is acting on. This is the discriminator: it selects which
/// member of `config` is meaningful, and a payload naming one kind with another's
/// config is malformed rather than merely ignored.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ServiceKind(String value) {
  static const ServiceKind didcomm = ServiceKind('didcomm');
  static const ServiceKind rest = ServiceKind('rest');
  static const ServiceKind tsp = ServiceKind('tsp');
  static const ServiceKind webauthn = ServiceKind('webauthn');

  /// Every value this specification's schema permits.
  static const List<ServiceKind> values = <ServiceKind>[
    didcomm,
    rest,
    tsp,
    webauthn
  ];
}

/// Request payload for vta/services/rollback. Reverts a transport to its previous
/// advertised settings by writing a NEW log entry — the history is append-only, so a
/// rollback is a forward step that restores an earlier state, never an erasure of what
/// happened.
class Payload {
  const Payload({
    required this.service,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        service: ServiceKind(json['service'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final ServiceKind service;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'service': service.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/services/rollback/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/services/rollback/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/services/rollback/1.0","title":"VTA Services — Rollback — payload","description":"Request payload for vta/services/rollback. Reverts a transport to its previous advertised settings by writing a NEW log entry — the history is append-only, so a rollback is a forward step that restores an earlier state, never an erasure of what happened.","type":"object","additionalProperties":false,"required":["service"],"properties":{"service":{"\$ref":"#/\$defs/ServiceKind"},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Services — Rollback — response payload","description":"Success response to vta/services/rollback. Type https://trusttasks.org/spec/vta/services/rollback/1.0#response.","type":"object","additionalProperties":false,"required":["result"],"properties":{"result":{"\$ref":"#/\$defs/RollbackResult"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RollbackResult":{"title":"RollbackResult","description":"The outcome of a rollback. Distinct from ServiceMutationResult because a rollback can legitimately publish nothing: if the previous state already equals the current one there is no change to write, and `kind: \\"noOp\\"` says so with `logEntryVersionId` absent. Treating that as a failure would be wrong — the requested state holds — and treating it as an ordinary success would report a log entry that does not exist.","type":"object","additionalProperties":false,"required":["kind"],"properties":{"kind":{"type":"string","enum":["disabled","enabled","updated","noOp"],"description":"What the rollback did. `disabled` re-disabled the transport, `enabled` re-advertised it at its prior settings, `updated` restored prior settings on an entry that stayed advertised, and `noOp` means the previous state already matched — nothing was written."},"logEntryVersionId":{"type":"string","description":"Version id of the log entry the rollback wrote. **Absent when `kind` is `noOp`**, and present otherwise."},"effectiveAt":{"type":"string","format":"date-time","description":"RFC 3339 instant the rollback took effect. Absent for `noOp`."},"drainingMediator":{"type":"string","description":"Present when rolling back DIDComm left a mediator draining."},"drainUntil":{"type":"string","format":"date-time","description":"When that drain completes."},"vtaDid":{"type":"string","description":"The agent\'s own DID."},"serverless":{"type":"boolean","default":false,"description":"True when the entry was written locally but not published — the operator must redeploy before any verifier sees it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"ServiceKind":{"title":"ServiceKind","description":"Which transport a task is acting on. This is the discriminator: it selects which member of `config` is meaningful, and a payload naming one kind with another\'s config is malformed rather than merely ignored.","type":"string","enum":["didcomm","rest","tsp","webauthn"]}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Services — Rollback — response payload","description":"Success response to vta/services/rollback. Type https://trusttasks.org/spec/vta/services/rollback/1.0#response.","type":"object","additionalProperties":false,"required":["result"],"properties":{"result":{"\$ref":"#/\$defs/RollbackResult"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RollbackResult":{"title":"RollbackResult","description":"The outcome of a rollback. Distinct from ServiceMutationResult because a rollback can legitimately publish nothing: if the previous state already equals the current one there is no change to write, and `kind: \\"noOp\\"` says so with `logEntryVersionId` absent. Treating that as a failure would be wrong — the requested state holds — and treating it as an ordinary success would report a log entry that does not exist.","type":"object","additionalProperties":false,"required":["kind"],"properties":{"kind":{"type":"string","enum":["disabled","enabled","updated","noOp"],"description":"What the rollback did. `disabled` re-disabled the transport, `enabled` re-advertised it at its prior settings, `updated` restored prior settings on an entry that stayed advertised, and `noOp` means the previous state already matched — nothing was written."},"logEntryVersionId":{"type":"string","description":"Version id of the log entry the rollback wrote. **Absent when `kind` is `noOp`**, and present otherwise."},"effectiveAt":{"type":"string","format":"date-time","description":"RFC 3339 instant the rollback took effect. Absent for `noOp`."},"drainingMediator":{"type":"string","description":"Present when rolling back DIDComm left a mediator draining."},"drainUntil":{"type":"string","format":"date-time","description":"When that drain completes."},"vtaDid":{"type":"string","description":"The agent\'s own DID."},"serverless":{"type":"boolean","default":false,"description":"True when the entry was written locally but not published — the operator must redeploy before any verifier sees it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"ServiceKind":{"title":"ServiceKind","description":"Which transport a task is acting on. This is the discriminator: it selects which member of `config` is meaningful, and a payload naming one kind with another\'s config is malformed rather than merely ignored.","type":"string","enum":["didcomm","rest","tsp","webauthn"]}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
