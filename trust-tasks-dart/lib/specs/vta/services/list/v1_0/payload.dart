// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/services/list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Which transport a task is acting on. This is the discriminator: it selects which
/// member of `config` is meaningful, and a payload naming one kind with another's
/// config is malformed rather than merely ignored.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ServiceKind(String value) {
  static const ServiceKind didcomm = ServiceKind('didcomm');
  static const ServiceKind rest = ServiceKind('rest');
  static const ServiceKind tsp = ServiceKind('tsp');
  static const ServiceKind webauthn = ServiceKind('webauthn');

  /// Every value this specification's schema permits.
  static const List<ServiceKind> values = <ServiceKind>[
    didcomm,
    rest,
    tsp,
    webauthn
  ];
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// What the agent currently advertises for one service. `enabled` is the only member
/// every kind carries; the rest are kind-specific and absent when they do not apply.
/// Note that `enabled: false` and an absent entry are different: the first says the
/// agent knows about this transport and is not advertising it, the second says it has
/// never been configured.
class ServiceState {
  const ServiceState({
    required this.kind,
    required this.enabled,
    this.mediatorDid,
    this.url,
    this.drainsUntil,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory ServiceState.fromJson(Map<String, dynamic> json) => ServiceState(
        kind: ServiceKind(json['kind'] as String),
        enabled: json['enabled'] as bool,
        mediatorDid: json['mediatorDid'] as String?,
        url: json['url'] as String?,
        drainsUntil: json['drainsUntil'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final ServiceKind kind;

  /// Whether the agent currently advertises this transport in its DID document.
  final bool enabled;

  /// The mediator this transport routes through. Present for `didcomm` and `tsp`.
  final String? mediatorDid;

  /// The advertised endpoint. Present for `rest` and `webauthn`.
  final String? url;

  /// RFC 3339 instant at which a scheduled drain completes. Present only while a
  /// `didcomm` mediator is draining — see `vta/services/drain/list`.
  final String? drainsUntil;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'kind': kind.value,
        'enabled': enabled,
        if (mediatorDid != null) 'mediatorDid': mediatorDid!,
        if (url != null) 'url': url!,
        if (drainsUntil != null) 'drainsUntil': drainsUntil!,
        if (ext != null) 'ext': ext!,
      };
}

/// Success response to vta/services/list. Type
/// https://trusttasks.org/spec/vta/services/list/1.0#response.
class Response {
  const Response({
    required this.services,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        services: (json['services'] as List<dynamic>)
            .map((e) => ServiceState.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Every transport the agent knows about, advertised or not. A kind absent from this
  /// array has never been configured — distinct from one present with `enabled: false`.
  final List<ServiceState> services;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'services': services.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Request payload for vta/services/list. Takes no filters: the answer is the agent's
/// whole advertised surface, which is public in its DID document anyway.
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/services/list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/services/list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/services/list/1.0","title":"VTA Services — List — payload","description":"Request payload for vta/services/list. Takes no filters: the answer is the agent\'s whole advertised surface, which is public in its DID document anyway.","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA Services — List — response payload","description":"Success response to vta/services/list. Type https://trusttasks.org/spec/vta/services/list/1.0#response.","type":"object","additionalProperties":false,"required":["services"],"properties":{"services":{"type":"array","description":"Every transport the agent knows about, advertised or not. A kind absent from this array has never been configured — distinct from one present with `enabled: false`.","items":{"\$ref":"#/\$defs/ServiceState"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ServiceState":{"title":"ServiceState","description":"What the agent currently advertises for one service. `enabled` is the only member every kind carries; the rest are kind-specific and absent when they do not apply. Note that `enabled: false` and an absent entry are different: the first says the agent knows about this transport and is not advertising it, the second says it has never been configured.","type":"object","additionalProperties":false,"required":["kind","enabled"],"properties":{"kind":{"\$ref":"#/\$defs/ServiceKind"},"enabled":{"type":"boolean","description":"Whether the agent currently advertises this transport in its DID document."},"mediatorDid":{"type":"string","description":"The mediator this transport routes through. Present for `didcomm` and `tsp`."},"url":{"type":"string","description":"The advertised endpoint. Present for `rest` and `webauthn`."},"drainsUntil":{"type":"string","format":"date-time","description":"RFC 3339 instant at which a scheduled drain completes. Present only while a `didcomm` mediator is draining — see `vta/services/drain/list`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"ServiceKind":{"title":"ServiceKind","description":"Which transport a task is acting on. This is the discriminator: it selects which member of `config` is meaningful, and a payload naming one kind with another\'s config is malformed rather than merely ignored.","type":"string","enum":["didcomm","rest","tsp","webauthn"]}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA Services — List — response payload","description":"Success response to vta/services/list. Type https://trusttasks.org/spec/vta/services/list/1.0#response.","type":"object","additionalProperties":false,"required":["services"],"properties":{"services":{"type":"array","description":"Every transport the agent knows about, advertised or not. A kind absent from this array has never been configured — distinct from one present with `enabled: false`.","items":{"\$ref":"#/\$defs/ServiceState"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ServiceState":{"title":"ServiceState","description":"What the agent currently advertises for one service. `enabled` is the only member every kind carries; the rest are kind-specific and absent when they do not apply. Note that `enabled: false` and an absent entry are different: the first says the agent knows about this transport and is not advertising it, the second says it has never been configured.","type":"object","additionalProperties":false,"required":["kind","enabled"],"properties":{"kind":{"\$ref":"#/\$defs/ServiceKind"},"enabled":{"type":"boolean","description":"Whether the agent currently advertises this transport in its DID document."},"mediatorDid":{"type":"string","description":"The mediator this transport routes through. Present for `didcomm` and `tsp`."},"url":{"type":"string","description":"The advertised endpoint. Present for `rest` and `webauthn`."},"drainsUntil":{"type":"string","format":"date-time","description":"RFC 3339 instant at which a scheduled drain completes. Present only while a `didcomm` mediator is draining — see `vta/services/drain/list`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"ServiceKind":{"title":"ServiceKind","description":"Which transport a task is acting on. This is the discriminator: it selects which member of `config` is meaningful, and a payload naming one kind with another\'s config is malformed rather than merely ignored.","type":"string","enum":["didcomm","rest","tsp","webauthn"]}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
