// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/did-templates/delete/2.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Confirmation that the named template was removed from the selected scope.
class Response {
  const Response({
    required this.name,
    required this.deleted,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        name: json['name'] as String,
        deleted: json['deleted'] as bool,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The deleted template's name, echoed for audit pipelines.
  final String name;

  /// Always true on success.
  final bool deleted;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'deleted': deleted,
        if (ext != null) 'ext': ext!,
      };
}

/// Remove a DID template from a VTA by name. Omit `contextId` to remove from the
/// global scope (super-admin gated); set it to remove a template scoped to that
/// context (gated on the context's admin, or a super-admin). The success response
/// echoes the deleted name for audit pipelines.
class Payload {
  const Payload({
    this.contextId,
    required this.name,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String?,
        name: json['name'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Scope selector. Absent: the global scope, gated on a super-administrator. Present:
  /// the context the template is scoped to, gated on that context's administrator (or a
  /// super-administrator).
  final String? contextId;

  /// Resource id — the name of the template to remove within the selected scope.
  final String name;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (contextId != null) 'contextId': contextId!,
        'name': name,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/did-templates/delete/2.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/did-templates/delete/2.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/did-templates/delete/2.0","title":"VTA DID-Template Delete — payload","description":"Remove a DID template from a VTA by name. Omit `contextId` to remove from the global scope (super-admin gated); set it to remove a template scoped to that context (gated on the context\'s admin, or a super-admin). The success response echoes the deleted name for audit pipelines.","type":"object","additionalProperties":false,"required":["name"],"properties":{"contextId":{"type":"string","minLength":1,"description":"Scope selector. Absent: the global scope, gated on a super-administrator. Present: the context the template is scoped to, gated on that context\'s administrator (or a super-administrator)."},"name":{"type":"string","minLength":1,"description":"Resource id — the name of the template to remove within the selected scope."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA DID-Template Delete — response payload","description":"Confirmation that the named template was removed from the selected scope.","type":"object","additionalProperties":false,"required":["name","deleted"],"properties":{"name":{"type":"string","description":"The deleted template\'s name, echoed for audit pipelines."},"deleted":{"type":"boolean","description":"Always true on success."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA DID-Template Delete — response payload","description":"Confirmation that the named template was removed from the selected scope.","type":"object","additionalProperties":false,"required":["name","deleted"],"properties":{"name":{"type":"string","description":"The deleted template\'s name, echoed for audit pipelines."},"deleted":{"type":"boolean","description":"Always true on success."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
