// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/dids/realign-keys/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A record renamed onto the identifier its key is published under
class RealignedKey {
  const RealignedKey({
    required this.from,
    required this.to,
    required this.publicKey,
  });

  /// Read this payload from a decoded JSON object.
  factory RealignedKey.fromJson(Map<String, dynamic> json) => RealignedKey(
        from: json['from'] as String,
        to: json['to'] as String,
        publicKey: json['publicKey'] as String,
      );

  /// The identifier the record had.
  final String from;

  /// The verification-method identifier it takes.
  final String to;

  /// Multibase public half of the key, which is what identified the record. Reported so
  /// the producer can check the rename against the published document rather than
  /// trusting this report.
  final String publicKey;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'from': from,
        'to': to,
        'publicKey': publicKey,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to vta/webvh/dids/realign-keys. Type
/// https://trusttasks.org/spec/vta/webvh/dids/realign-keys/1.0#response.
class Response {
  const Response({
    required this.did,
    required this.moved,
    required this.alreadyAligned,
    required this.unmatched,
    required this.nextFragmentId,
    required this.dryRun,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        did: json['did'] as String,
        moved: (json['moved'] as List<dynamic>)
            .map((e) => RealignedKey.fromJson(e as Map<String, dynamic>))
            .toList(),
        alreadyAligned: (json['alreadyAligned'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
        unmatched: (json['unmatched'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
        nextFragmentId: json['nextFragmentId'] as int,
        dryRun: json['dryRun'] as bool,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String did;

  /// The renames performed, or in a dry run the ones that would be.
  final List<RealignedKey> moved;

  /// Verification-method identifiers whose record was already correct.
  final List<String> alreadyAligned;

  /// Verification-method identifiers the recipient holds no key for. Not a failure — a
  /// DID may publish a method whose private half lives elsewhere — but the difference
  /// between this being empty and not is the difference between the DID being consistent
  /// and merely unchanged.
  final List<String> unmatched;

  /// Where the recipient will allocate the next identifier it mints for this DID. Past
  /// everything the document already publishes, so a later rotation cannot reissue a
  /// published identifier under new key material.
  final int nextFragmentId;

  /// True when nothing was written. Echoed so a report is self-describing: a plan and a
  /// receipt are otherwise the same shape.
  final bool dryRun;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'moved': moved.map((e) => e.toJson()).toList(),
        'alreadyAligned': alreadyAligned,
        'unmatched': unmatched,
        'nextFragmentId': nextFragmentId,
        'dryRun': dryRun,
        if (ext != null) 'ext': ext!,
      };
}

/// Rename a did:webvh's key records so each is addressed by the verification-method
/// identifier its published document declares. The payload names no identifier but the
/// DID: every target is derived by the recipient from that DID's own document.
class Payload {
  const Payload({
    required this.did,
    this.dryRun,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        dryRun: json['dryRun'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID whose key records are to be realigned. The only identifier the producer
  /// chooses.
  final String did;

  /// Compute and report the realignment without performing it. Absent reads as false.
  final bool? dryRun;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (dryRun != null) 'dryRun': dryRun!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/webvh/dids/realign-keys/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/webvh/dids/realign-keys/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/webvh/dids/realign-keys/1.0","title":"VTA WebVH DIDs Realign-Keys — payload","description":"Rename a did:webvh\'s key records so each is addressed by the verification-method identifier its published document declares. The payload names no identifier but the DID: every target is derived by the recipient from that DID\'s own document.","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID whose key records are to be realigned. The only identifier the producer chooses."},"dryRun":{"type":"boolean","description":"Compute and report the realignment without performing it. Absent reads as false."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH DIDs Realign-Keys — response payload","description":"Success response to vta/webvh/dids/realign-keys. Type https://trusttasks.org/spec/vta/webvh/dids/realign-keys/1.0#response.","type":"object","additionalProperties":false,"required":["did","moved","alreadyAligned","unmatched","nextFragmentId","dryRun"],"properties":{"did":{"type":"string"},"moved":{"type":"array","description":"The renames performed, or in a dry run the ones that would be.","items":{"\$ref":"#/\$defs/RealignedKey"}},"alreadyAligned":{"type":"array","description":"Verification-method identifiers whose record was already correct.","items":{"type":"string"}},"unmatched":{"type":"array","description":"Verification-method identifiers the recipient holds no key for. Not a failure — a DID may publish a method whose private half lives elsewhere — but the difference between this being empty and not is the difference between the DID being consistent and merely unchanged.","items":{"type":"string"}},"nextFragmentId":{"type":"integer","minimum":0,"description":"Where the recipient will allocate the next identifier it mints for this DID. Past everything the document already publishes, so a later rotation cannot reissue a published identifier under new key material."},"dryRun":{"type":"boolean","description":"True when nothing was written. Echoed so a report is self-describing: a plan and a receipt are otherwise the same shape."},"ext":{"\$ref":"#/\$defs/Ext"}}},"RealignedKey":{"title":"A record renamed onto the identifier its key is published under","type":"object","additionalProperties":false,"required":["from","to","publicKey"],"properties":{"from":{"type":"string","minLength":1,"description":"The identifier the record had."},"to":{"type":"string","minLength":1,"description":"The verification-method identifier it takes."},"publicKey":{"type":"string","minLength":1,"description":"Multibase public half of the key, which is what identified the record. Reported so the producer can check the rename against the published document rather than trusting this report."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH DIDs Realign-Keys — response payload","description":"Success response to vta/webvh/dids/realign-keys. Type https://trusttasks.org/spec/vta/webvh/dids/realign-keys/1.0#response.","type":"object","additionalProperties":false,"required":["did","moved","alreadyAligned","unmatched","nextFragmentId","dryRun"],"properties":{"did":{"type":"string"},"moved":{"type":"array","description":"The renames performed, or in a dry run the ones that would be.","items":{"\$ref":"#/\$defs/RealignedKey"}},"alreadyAligned":{"type":"array","description":"Verification-method identifiers whose record was already correct.","items":{"type":"string"}},"unmatched":{"type":"array","description":"Verification-method identifiers the recipient holds no key for. Not a failure — a DID may publish a method whose private half lives elsewhere — but the difference between this being empty and not is the difference between the DID being consistent and merely unchanged.","items":{"type":"string"}},"nextFragmentId":{"type":"integer","minimum":0,"description":"Where the recipient will allocate the next identifier it mints for this DID. Past everything the document already publishes, so a later rotation cannot reissue a published identifier under new key material."},"dryRun":{"type":"boolean","description":"True when nothing was written. Echoed so a report is self-describing: a plan and a receipt are otherwise the same shape."},"ext":{"\$ref":"#/\$defs/Ext"}}},"RealignedKey":{"title":"A record renamed onto the identifier its key is published under","type":"object","additionalProperties":false,"required":["from","to","publicKey"],"properties":{"from":{"type":"string","minLength":1,"description":"The identifier the record had."},"to":{"type":"string","minLength":1,"description":"The verification-method identifier it takes."},"publicKey":{"type":"string","minLength":1,"description":"Multibase public half of the key, which is what identified the record. Reported so the producer can check the rename against the published document rather than trusting this report."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
