// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/dids/update/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The published entry. camelCase per the framework convention — note the counts are
/// the state AFTER the update, including the rotation the caller did not ask for.
class Response {
  const Response({
    required this.did,
    required this.newVersionId,
    this.newScid,
    this.newLogEntry,
    this.updateKeysCount,
    this.preRotationKeyCount,
    this.serverless,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        did: json['did'] as String,
        newVersionId: json['newVersionId'] as String,
        newScid: json['newScid'] as String?,
        newLogEntry: json['newLogEntry'] as String?,
        updateKeysCount: json['updateKeysCount'] as int?,
        preRotationKeyCount: json['preRotationKeyCount'] as int?,
        serverless: json['serverless'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String did;

  /// versionId of the entry just appended. A caller intending a further edit SHOULD pass
  /// this back as the next request's `expectedVersionId`.
  final String newVersionId;
  final String? newScid;

  /// The appended log entry, as JSON text.
  final String? newLogEntry;

  /// Update keys authorized AFTER this entry. Where `document` was supplied these are
  /// new keys — the previous ones no longer authorize anything.
  final int? updateKeysCount;

  /// Pre-rotation commitments published by this entry.
  final int? preRotationKeyCount;

  /// True when the agent holds the log itself and no hosting server was published to —
  /// the operator must fetch and redeploy `did.jsonl`.
  final bool? serverless;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'newVersionId': newVersionId,
        if (newScid != null) 'newScid': newScid!,
        if (newLogEntry != null) 'newLogEntry': newLogEntry!,
        if (updateKeysCount != null) 'updateKeysCount': updateKeysCount!,
        if (preRotationKeyCount != null)
          'preRotationKeyCount': preRotationKeyCount!,
        if (serverless != null) 'serverless': serverless!,
        if (ext != null) 'ext': ext!,
      };
}

/// Ask a Verifiable Trust Agent to publish a new entry in a did:webvh log it holds the
/// update key for. The agent decides whether to do so; the caller proposes.
class Payload {
  const Payload({
    required this.did,
    this.document,
    this.preRotationCount,
    this.witnesses,
    this.watchers,
    this.ttl,
    this.label,
    this.expectedVersionId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        document: json['document'] as Map<String, dynamic>?,
        preRotationCount: json['preRotationCount'] as int?,
        witnesses: json['witnesses'],
        watchers: json['watchers'] == null
            ? null
            : (json['watchers'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        ttl: json['ttl'] as int?,
        label: json['label'] as String?,
        expectedVersionId: json['expectedVersionId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The did:webvh being updated. The agent MUST verify it speaks for this subject.
  final String did;

  /// The new DID document. Omit to leave it unchanged. Supplying this ROTATES the DID's
  /// update key and refreshes its pre-rotation commitments, as a parallel consequence of
  /// the change — a consequence not visible anywhere in this payload, because it lives
  /// in the executing handler's semantics. A consent surface MUST therefore render
  /// effects the executor computed, and MUST NOT derive them from this document.
  final Map<String, dynamic>? document;

  /// Number of pre-rotation commitments to publish. Omit to keep the current count; `0`
  /// disables pre-rotation going forward.
  final int? preRotationCount;

  /// New witness configuration. Omit to keep the current one.
  final Object? witnesses;

  /// New watcher URLs. Omit to keep the current set; an empty array removes them.
  final List<String>? watchers;

  /// New TTL in seconds. Omit to keep the current value.
  final int? ttl;

  /// Operator-facing audit label.
  final String? label;

  /// Optimistic-concurrency precondition: the versionId the caller based this edit on.
  /// The agent MUST refuse the update if the DID's latest entry no longer matches.
  /// Without it a `get -&gt; edit -&gt; save` cycle silently overwrites a concurrent edit with
  /// a chain that is structurally valid, verifies perfectly, and is based on a stale
  /// read. Where a human approves the update the window is minutes wide, so this is a
  /// routine race rather than an exotic one. OPTIONAL because a scripted caller with no
  /// concurrent writers has nothing to protect against; not optional for anything a
  /// person looked at.
  final String? expectedVersionId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (document != null) 'document': document!,
        if (preRotationCount != null) 'preRotationCount': preRotationCount!,
        if (witnesses != null) 'witnesses': witnesses!,
        if (watchers != null) 'watchers': watchers!,
        if (ttl != null) 'ttl': ttl!,
        if (label != null) 'label': label!,
        if (expectedVersionId != null) 'expectedVersionId': expectedVersionId!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/webvh/dids/update/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/webvh/dids/update/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/webvh/dids/update/1.0","title":"WebVH DID Update — payload","description":"Ask a Verifiable Trust Agent to publish a new entry in a did:webvh log it holds the update key for. The agent decides whether to do so; the caller proposes.","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"The did:webvh being updated. The agent MUST verify it speaks for this subject."},"document":{"type":"object","description":"The new DID document. Omit to leave it unchanged. Supplying this ROTATES the DID\'s update key and refreshes its pre-rotation commitments, as a parallel consequence of the change — a consequence not visible anywhere in this payload, because it lives in the executing handler\'s semantics. A consent surface MUST therefore render effects the executor computed, and MUST NOT derive them from this document."},"preRotationCount":{"type":"integer","minimum":0,"description":"Number of pre-rotation commitments to publish. Omit to keep the current count; `0` disables pre-rotation going forward."},"witnesses":{"description":"New witness configuration. Omit to keep the current one."},"watchers":{"type":"array","items":{"type":"string"},"description":"New watcher URLs. Omit to keep the current set; an empty array removes them."},"ttl":{"type":"integer","minimum":0,"description":"New TTL in seconds. Omit to keep the current value."},"label":{"type":"string","maxLength":256,"description":"Operator-facing audit label."},"expectedVersionId":{"type":"string","minLength":1,"description":"Optimistic-concurrency precondition: the versionId the caller based this edit on. The agent MUST refuse the update if the DID\'s latest entry no longer matches. Without it a `get -> edit -> save` cycle silently overwrites a concurrent edit with a chain that is structurally valid, verifies perfectly, and is based on a stale read. Where a human approves the update the window is minutes wide, so this is a routine race rather than an exotic one. OPTIONAL because a scripted caller with no concurrent writers has nothing to protect against; not optional for anything a person looked at."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"WebVH DID Update — response payload","description":"The published entry. camelCase per the framework convention — note the counts are the state AFTER the update, including the rotation the caller did not ask for.","type":"object","additionalProperties":false,"required":["did","newVersionId"],"properties":{"did":{"type":"string"},"newVersionId":{"type":"string","description":"versionId of the entry just appended. A caller intending a further edit SHOULD pass this back as the next request\'s `expectedVersionId`."},"newScid":{"type":"string"},"newLogEntry":{"type":"string","description":"The appended log entry, as JSON text."},"updateKeysCount":{"type":"integer","minimum":0,"description":"Update keys authorized AFTER this entry. Where `document` was supplied these are new keys — the previous ones no longer authorize anything."},"preRotationKeyCount":{"type":"integer","minimum":0,"description":"Pre-rotation commitments published by this entry."},"serverless":{"type":"boolean","description":"True when the agent holds the log itself and no hosting server was published to — the operator must fetch and redeploy `did.jsonl`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"WebVH DID Update — response payload","description":"The published entry. camelCase per the framework convention — note the counts are the state AFTER the update, including the rotation the caller did not ask for.","type":"object","additionalProperties":false,"required":["did","newVersionId"],"properties":{"did":{"type":"string"},"newVersionId":{"type":"string","description":"versionId of the entry just appended. A caller intending a further edit SHOULD pass this back as the next request\'s `expectedVersionId`."},"newScid":{"type":"string"},"newLogEntry":{"type":"string","description":"The appended log entry, as JSON text."},"updateKeysCount":{"type":"integer","minimum":0,"description":"Update keys authorized AFTER this entry. Where `document` was supplied these are new keys — the previous ones no longer authorize anything."},"preRotationKeyCount":{"type":"integer","minimum":0,"description":"Pre-rotation commitments published by this entry."},"serverless":{"type":"boolean","description":"True when the agent holds the log itself and no hosting server was published to — the operator must fetch and redeploy `did.jsonl`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
