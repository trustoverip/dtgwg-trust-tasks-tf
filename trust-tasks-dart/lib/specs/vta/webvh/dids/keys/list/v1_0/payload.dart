// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/dids/keys/list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Which view of the DID's key roles the caller was given (CONVENTIONS.md §5).
/// `public` — only what the published document and log already reveal to anyone who
/// resolves the DID. `custody` — additionally the VTA's own custody state: pending
/// keys, the custodian record behind each key, overlap deadlines, who initiated and
/// approved each change, and why. The response always states which it is, so a caller
/// never mistakes an absent member for a statement that the thing does not exist.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const KeyRoleProjection(String value) {
  static const KeyRoleProjection public = KeyRoleProjection('public');
  static const KeyRoleProjection custody = KeyRoleProjection('custody');

  /// Every value this specification's schema permits.
  static const List<KeyRoleProjection> values = <KeyRoleProjection>[
    public,
    custody
  ];
}

/// The named key role a key occupies in the DID (VTI-KEY-070 onward). `attestation` —
/// the only keys in `assertionMethod`; sign the DID's attestation artefacts
/// (membership and role credentials, endorsements, status lists, every other
/// credential the node issues), through the VTA's signing service and nowhere else.
/// Generated inside the VTA, never derived, never exportable, never in a backup.
/// `operational` — the only keys in `authentication`; sign the node's own traffic
/// (Trust Task requests and responses, DIDComm and TSP message signatures,
/// invitations, notices, audit checkpoints) with proof purpose `authentication`.
/// `messaging` — the only keys in `keyAgreement`; sign nothing. `update` — the keys
/// that authorize appending to the DID's log (a did:webvh `updateKeys` entry) and the
/// pre-rotation commitments that authorize the next ones; never a verification method,
/// never in `keyRoles`. Generated inside the VTA, never derived, never exportable,
/// never in a backup. No role permits `capabilityInvocation` or `capabilityDelegation`
/// (VTI-KEY-075); both are reserved for a future role. The mapping is normative and is
/// set out in CONVENTIONS.md §1. The set is an extensible registry and growing it is a
/// MINOR change: a consumer that receives a role it does not implement MUST refuse the
/// document rather than map the key onto a role it does know.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const KeyRole(String value) {
  static const KeyRole attestation = KeyRole('attestation');
  static const KeyRole operational = KeyRole('operational');
  static const KeyRole messaging = KeyRole('messaging');
  static const KeyRole update = KeyRole('update');

  /// Every value this specification's schema permits.
  static const List<KeyRole> values = <KeyRole>[
    attestation,
    operational,
    messaging,
    update
  ];
}

/// A DID Core verification relationship.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DidVerificationRelationship(String value) {
  static const DidVerificationRelationship authentication =
      DidVerificationRelationship('authentication');
  static const DidVerificationRelationship assertionMethod =
      DidVerificationRelationship('assertionMethod');
  static const DidVerificationRelationship keyAgreement =
      DidVerificationRelationship('keyAgreement');
  static const DidVerificationRelationship capabilityInvocation =
      DidVerificationRelationship('capabilityInvocation');
  static const DidVerificationRelationship capabilityDelegation =
      DidVerificationRelationship('capabilityDelegation');

  /// Every value this specification's schema permits.
  static const List<DidVerificationRelationship> values =
      <DidVerificationRelationship>[
    authentication,
    assertionMethod,
    keyAgreement,
    capabilityInvocation,
    capabilityDelegation
  ];
}

/// Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA),
/// `x25519` performs key agreement and never signs, `p256` signs (ES256), and
/// `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS
/// 204. The set is expected to grow as algorithms are standardised, and growing it is
/// a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a
/// value relaxes a constraint rather than narrowing one, and the generated libraries
/// mark this enumeration non-exhaustive so that a consumer absorbs a new value rather
/// than failing to compile. Two ML-DSA parameter sets are carried because two
/// specifications require different ones — W3C Quantum-Resistant Cryptosuites defines
/// Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1
/// mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key
/// and the two are not redundant. A consumer that does not implement a value it
/// receives MUST refuse the document rather than substitute one it does support.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const KeyType(String value) {
  static const KeyType ed25519 = KeyType('ed25519');
  static const KeyType x25519 = KeyType('x25519');
  static const KeyType p256 = KeyType('p256');
  static const KeyType mldsa44 = KeyType('mldsa44');
  static const KeyType mldsa65 = KeyType('mldsa65');

  /// Every value this specification's schema permits.
  static const List<KeyType> values = <KeyType>[
    ed25519,
    x25519,
    p256,
    mldsa44,
    mldsa65
  ];
}

/// Where a key is in its role's lifecycle. `pending` — planned by a preview or a
/// change awaiting approval; not published; custody projection only. `staged` —
/// published and bound to its role, but not yet used: a planned rotation publishes the
/// successor and waits until the entry's cache horizon — publication plus the longer
/// of the document's TTL and the verifier cache cap (CONVENTIONS.md §11.1) — before
/// the VTA first uses it, so that no verifier holding a cached document sees a
/// signature by a key it has never seen (VTI-KEY-122). Becomes `active` at the
/// rotation's `activatesAt`. `active` — the key the VTA uses for new signatures (or,
/// for `messaging`, advertises for new sessions). `retiring` — still published so that
/// what it signed and sessions keyed to it keep working, but never used again: from
/// the successor's first use the VTA MUST NOT sign with it, while a retiring
/// `messaging` key stays usable for decryption, and senders may still encrypt to it,
/// until it is retired (CONVENTIONS.md §11.3). `retired` — removed by planned
/// rotation; what it signed while published remains valid, judged against the DID
/// version current at issuance (CONVENTIONS.md §7). A retired `attestation` or
/// `messaging` key is destroyed at retirement. `revoked` — removed from every
/// relationship and from `keyRoles` in one entry, without overlap, because it is or
/// may be compromised; what it signed from `compromisedSince` onward establishes
/// nothing. `retired` and `revoked` are terminal.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const RoleKeyState(String value) {
  static const RoleKeyState pending = RoleKeyState('pending');
  static const RoleKeyState staged = RoleKeyState('staged');
  static const RoleKeyState active = RoleKeyState('active');
  static const RoleKeyState retiring = RoleKeyState('retiring');
  static const RoleKeyState retired = RoleKeyState('retired');
  static const RoleKeyState revoked = RoleKeyState('revoked');

  /// Every value this specification's schema permits.
  static const List<RoleKeyState> values = <RoleKeyState>[
    pending,
    staged,
    active,
    retiring,
    retired,
    revoked
  ];
}

/// Where the private key came from. `derived` means the maintainer generated it from a
/// seed it holds and can reproduce it from `derivationPath`; `imported` means it
/// arrived from outside and exists only as stored material; `internal` means the
/// maintainer generated it from a CSPRNG and it is reproducible from nothing at all.
/// The distinction is operationally load-bearing: a `derived` key survives a seed
/// restore, an `imported` one is lost unless it was backed up separately, and an
/// `internal` one cannot be recovered by any means once the maintainer's storage is
/// gone. This member is also the only way a consumer can confirm that a `keys/create`
/// request for an `internal` key was honoured rather than silently downgraded to a
/// derived one — see that specification's `internal` member.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const KeyOrigin(String value) {
  static const KeyOrigin derived = KeyOrigin('derived');
  static const KeyOrigin imported = KeyOrigin('imported');
  static const KeyOrigin internal = KeyOrigin('internal');

  /// Every value this specification's schema permits.
  static const List<KeyOrigin> values = <KeyOrigin>[
    derived,
    imported,
    internal
  ];
}

/// How the VTA holds a key. Present only in the custody projection. Carries no key
/// material, and a conforming VTA MUST NOT place any in `ext`.
class RoleKeyCustody {
  const RoleKeyCustody({
    required this.keyId,
    this.origin,
    required this.exportable,
    required this.neverExportable,
    this.hardwareBacked,
    required this.inBackups,
    this.destroyed,
  });

  /// Read this payload from a decoded JSON object.
  factory RoleKeyCustody.fromJson(Map<String, dynamic> json) => RoleKeyCustody(
        keyId: json['keyId'] as String,
        origin:
            json['origin'] == null ? null : KeyOrigin(json['origin'] as String),
        exportable: json['exportable'] as bool,
        neverExportable: json['neverExportable'] as bool,
        hardwareBacked: json['hardwareBacked'] as bool?,
        inBackups: json['inBackups'] as bool,
        destroyed: json['destroyed'] as bool?,
      );

  /// The custodian record identifier (`keys/*`'s `keyId`) behind this key.
  final String keyId;
  final KeyOrigin? origin;

  /// Whether the custodian would release the private half through `keys/export-secret`.
  /// Always `false` for `attestation` and `update` keys (CONVENTIONS.md §4). Stated
  /// explicitly — never absent — because this is the member an operator reads to confirm
  /// a role key cannot leave the VTA, and `keys/*`'s absent-means-exportable reading is
  /// the wrong default to leave them to infer.
  final bool exportable;

  /// True when no authority can make the key exportable — the role forbids it, or the
  /// material exists only inside a module that cannot release it. `true` implies
  /// `exportable: false`.
  final bool neverExportable;

  /// True when the private half is held by a hardware or enclave module that performs
  /// the operation without releasing it. Absent means the VTA does not say, not that it
  /// is false.
  final bool? hardwareBacked;

  /// Whether the key, or material it can be re-derived from, is included in the VTA's
  /// backups or any state transfer. Always `false` for `attestation` and `update` keys
  /// (VTI-KEY-112): they are generated, not derived, so there is no seed that recovers
  /// them, and their loss is recovered by rotation, never by restore.
  final bool inBackups;

  /// True once the private half has been destroyed — a retired `attestation` key
  /// (VTI-KEY-125). The record survives so the key's past signatures stay attributable.
  final bool? destroyed;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'keyId': keyId,
        if (origin != null) 'origin': origin!.value,
        'exportable': exportable,
        'neverExportable': neverExportable,
        if (hardwareBacked != null) 'hardwareBacked': hardwareBacked!,
        'inBackups': inBackups,
        if (destroyed != null) 'destroyed': destroyed!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// One key in one role. A key is in exactly one role for its whole life; a change of
/// role is a retirement from one and an addition to another, under a new
/// verification-method identifier.
class RoleKey {
  const RoleKey({
    this.verificationMethod,
    required this.role,
    this.relationships,
    required this.keyType,
    required this.publicKeyMultibase,
    required this.state,
    this.publishedInVersionId,
    this.removedInVersionId,
    this.retireAfter,
    this.compromisedSince,
    this.rotationId,
    this.label,
    this.custody,
    this.ext,
    this.activatesAt,
  });

  /// Read this payload from a decoded JSON object.
  factory RoleKey.fromJson(Map<String, dynamic> json) => RoleKey(
        verificationMethod: json['verificationMethod'] as String?,
        role: KeyRole(json['role'] as String),
        relationships: json['relationships'] == null
            ? null
            : (json['relationships'] as List<dynamic>)
                .map((e) => DidVerificationRelationship(e as String))
                .toList(),
        keyType: KeyType(json['keyType'] as String),
        publicKeyMultibase: json['publicKeyMultibase'] as String,
        state: RoleKeyState(json['state'] as String),
        publishedInVersionId: json['publishedInVersionId'] as String?,
        removedInVersionId: json['removedInVersionId'] as String?,
        retireAfter: json['retireAfter'] as String?,
        compromisedSince: json['compromisedSince'] as String?,
        rotationId: json['rotationId'] as String?,
        label: json['label'] as String?,
        custody: json['custody'] == null
            ? null
            : RoleKeyCustody.fromJson(json['custody'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
        activatesAt: json['activatesAt'] as String?,
      );

  /// The full verification-method identifier (`did#fragment`) the document publishes the
  /// key under, and lists in `keyRoles`. Absent only for `update` keys, which are
  /// published as log parameters and never as verification methods (VTI-KEY-093). A VTA
  /// minting a new method SHOULD derive its fragment from the key (VTI-KEY-150).
  final String? verificationMethod;
  final KeyRole role;

  /// The relationships the key is (or, while `pending`, will be) listed in. Always
  /// exactly its role's single relationship (CONVENTIONS.md §1) — carried so a caller
  /// can check it against the document rather than trusting it. Empty for `update`,
  /// `retired` and `revoked` keys.
  final List<DidVerificationRelationship>? relationships;
  final KeyType keyType;

  /// The public half, multibase-encoded, exactly as published.
  final String publicKeyMultibase;
  final RoleKeyState state;

  /// versionId of the log entry that first published the key. Absent while `pending`.
  final String? publishedInVersionId;

  /// versionId of the log entry that retired or revoked the key.
  final String? removedInVersionId;

  /// For a `retiring` key: the end of its overlap window. Custody projection only.
  final String? retireAfter;

  /// For a `revoked` key: the compromise time (VTI-KEY-121) — the earliest instant from
  /// which the key is treated as compromised. Where the operator cannot establish it,
  /// the time the key was first published. Published, because it is what a verifier
  /// needs.
  final String? compromisedSince;

  /// The rotation this key was introduced or retired by, when it was.
  final String? rotationId;

  /// Operator-facing label. Custody projection only.
  final String? label;
  final RoleKeyCustody? custody;
  final Ext? ext;

  /// For a `staged` key: when the VTA will begin using it — no earlier than the
  /// publishing entry's cache horizon (CONVENTIONS.md §11.1).
  final String? activatesAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (verificationMethod != null)
          'verificationMethod': verificationMethod!,
        'role': role.value,
        if (relationships != null)
          'relationships': relationships!.map((e) => e.value).toList(),
        'keyType': keyType.value,
        'publicKeyMultibase': publicKeyMultibase,
        'state': state.value,
        if (publishedInVersionId != null)
          'publishedInVersionId': publishedInVersionId!,
        if (removedInVersionId != null)
          'removedInVersionId': removedInVersionId!,
        if (retireAfter != null) 'retireAfter': retireAfter!,
        if (compromisedSince != null) 'compromisedSince': compromisedSince!,
        if (rotationId != null) 'rotationId': rotationId!,
        if (label != null) 'label': label!,
        if (custody != null) 'custody': custody!.toJson(),
        if (ext != null) 'ext': ext!,
        if (activatesAt != null) 'activatesAt': activatesAt!,
      };
}

/// RoleSummary
class RoleSummary {
  const RoleSummary({
    required this.role,
    required this.relationships,
    required this.keys,
    this.preRotationCommitments,
  });

  /// Read this payload from a decoded JSON object.
  factory RoleSummary.fromJson(Map<String, dynamic> json) => RoleSummary(
        role: KeyRole(json['role'] as String),
        relationships: (json['relationships'] as List<dynamic>)
            .map((e) => DidVerificationRelationship(e as String))
            .toList(),
        keys: (json['keys'] as List<dynamic>)
            .map((e) => RoleKey.fromJson(e as Map<String, dynamic>))
            .toList(),
        preRotationCommitments: json['preRotationCommitments'] as int?,
      );

  final KeyRole role;

  /// The relationship this role owns (CONVENTIONS.md §1). Empty for `update`.
  final List<DidVerificationRelationship> relationships;

  /// The role's keys, in the order the document publishes them. Terminal-state keys are
  /// included only when the request asked for them.
  final List<RoleKey> keys;

  /// `update` only: successors the latest entry commits to. `0` means a compromise of
  /// the current update key cannot be recovered from by rotation, and the DID would have
  /// to be deactivated (VTI-KEY-124).
  final int? preRotationCommitments;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'role': role.value,
        'relationships': relationships.map((e) => e.value).toList(),
        'keys': keys.map((e) => e.toJson()).toList(),
        if (preRotationCommitments != null)
          'preRotationCommitments': preRotationCommitments!,
      };
}

/// `planned` — a successor was staged, activated, and the predecessor retired on the
/// operator's schedule. `compromise` — a key was revoked and, where the role would
/// otherwise be empty, replaced in the same log entry. `addition` — a key was added to
/// a role without anything leaving it (for example a post-quantum key alongside a
/// classical one).
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const RotationKind(String value) {
  static const RotationKind planned = RotationKind('planned');
  static const RotationKind compromise = RotationKind('compromise');
  static const RotationKind addition = RotationKind('addition');

  /// Every value this specification's schema permits.
  static const List<RotationKind> values = <RotationKind>[
    planned,
    compromise,
    addition
  ];
}

/// `pendingApproval` — awaiting the approvals the VTA's policy requires; nothing is
/// published. `staged` — successor published, not yet used; predecessor still active.
/// `overlapping` — successor active, predecessor `retiring`. `completed` — every
/// predecessor retired (or, for `compromise` and `addition`, the entry is published
/// and any overlap it began has ended). `aborted` — the successor was retired instead
/// of the predecessor, which stayed or returned to `active`. `expired` — approvals
/// were not gathered before the preview expired; nothing was published.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const RotationState(String value) {
  static const RotationState pendingApproval = RotationState('pendingApproval');
  static const RotationState staged = RotationState('staged');
  static const RotationState overlapping = RotationState('overlapping');
  static const RotationState completed = RotationState('completed');
  static const RotationState aborted = RotationState('aborted');
  static const RotationState expired = RotationState('expired');

  /// Every value this specification's schema permits.
  static const List<RotationState> values = <RotationState>[
    pendingApproval,
    staged,
    overlapping,
    completed,
    aborted,
    expired
  ];
}

/// Approval progress of a change the VTA's policy gates on more than one approval
/// (CONVENTIONS.md §3.3).
class KeyRoleApprovalState {
  const KeyRoleApprovalState({
    required this.required,
    required this.received,
    this.approvers,
    required this.expiresAt,
  });

  /// Read this payload from a decoded JSON object.
  factory KeyRoleApprovalState.fromJson(Map<String, dynamic> json) =>
      KeyRoleApprovalState(
        required: json['required'] as int,
        received: json['received'] as int,
        approvers: json['approvers'] == null
            ? null
            : (json['approvers'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        expiresAt: json['expiresAt'] as String,
      );

  /// Distinct approvals the VTA's policy requires.
  final int required;

  /// Distinct approvals recorded so far, including the initiator's own.
  final int received;

  /// VIDs whose approvals were recorded. Custody projection only; omitted from a
  /// response to anyone who is not themselves an eligible approver of this change.
  final List<String>? approvers;

  /// When the preview, and every approval bound to it, stops being usable.
  final String expiresAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'required': required,
        'received': received,
        if (approvers != null) 'approvers': approvers!,
        'expiresAt': expiresAt,
      };
}

/// One change to a role's key set, from preview to completion. The rotation history of
/// a DID is the list of these; the log is the authority for what was published, and
/// this record adds what the log cannot say — which change was which, and (custody
/// projection only) who asked, who approved, and why.
class RotationRecord {
  const RotationRecord({
    required this.rotationId,
    required this.role,
    required this.kind,
    required this.state,
    this.predecessors,
    this.successors,
    this.startedInVersionId,
    this.endedInVersionId,
    this.startedAt,
    this.overlapUntil,
    this.autoRetire,
    this.endedAt,
    this.initiatedBy,
    this.approvals,
    this.reason,
    this.ext,
    this.activatesAt,
    this.cacheHorizonAt,
  });

  /// Read this payload from a decoded JSON object.
  factory RotationRecord.fromJson(Map<String, dynamic> json) => RotationRecord(
        rotationId: json['rotationId'] as String,
        role: KeyRole(json['role'] as String),
        kind: RotationKind(json['kind'] as String),
        state: RotationState(json['state'] as String),
        predecessors: json['predecessors'] == null
            ? null
            : (json['predecessors'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        successors: json['successors'] == null
            ? null
            : (json['successors'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        startedInVersionId: json['startedInVersionId'] as String?,
        endedInVersionId: json['endedInVersionId'] as String?,
        startedAt: json['startedAt'] as String?,
        overlapUntil: json['overlapUntil'] as String?,
        autoRetire: json['autoRetire'] as bool?,
        endedAt: json['endedAt'] as String?,
        initiatedBy: json['initiatedBy'] as String?,
        approvals: json['approvals'] == null
            ? null
            : KeyRoleApprovalState.fromJson(
                json['approvals'] as Map<String, dynamic>),
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
        activatesAt: json['activatesAt'] as String?,
        cacheHorizonAt: json['cacheHorizonAt'] as String?,
      );

  final String rotationId;
  final KeyRole role;
  final RotationKind kind;
  final RotationState state;

  /// Verification-method identifiers (or, for `update`, public keys) leaving the role.
  final List<String>? predecessors;

  /// Verification-method identifiers (or, for `update`, public keys) entering the role.
  final List<String>? successors;
  final String? startedInVersionId;
  final String? endedInVersionId;
  final String? startedAt;

  /// Custody projection only. End of the overlap window.
  final String? overlapUntil;

  /// Custody projection only. Whether the VTA retires the predecessor itself at
  /// `overlapUntil`.
  final bool? autoRetire;
  final String? endedAt;

  /// Custody projection only. VID of the administrator whose signed request started the
  /// change.
  final String? initiatedBy;

  /// Custody projection only.
  final KeyRoleApprovalState? approvals;

  /// Custody projection only. The operator's stated reason. Never published.
  final String? reason;
  final Ext? ext;

  /// When the successor becomes `active` and the predecessor `retiring`; never before
  /// `cacheHorizonAt`.
  final String? activatesAt;

  /// Planned rotations: the publishing entry's cache horizon — publication plus the
  /// longer of the document's TTL and the verifier cache cap (CONVENTIONS.md §11.1). The
  /// switch, and any retirement of the predecessor, happen no earlier. Public: it
  /// follows from the log and the published cap.
  final String? cacheHorizonAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'rotationId': rotationId,
        'role': role.value,
        'kind': kind.value,
        'state': state.value,
        if (predecessors != null) 'predecessors': predecessors!,
        if (successors != null) 'successors': successors!,
        if (startedInVersionId != null)
          'startedInVersionId': startedInVersionId!,
        if (endedInVersionId != null) 'endedInVersionId': endedInVersionId!,
        if (startedAt != null) 'startedAt': startedAt!,
        if (overlapUntil != null) 'overlapUntil': overlapUntil!,
        if (autoRetire != null) 'autoRetire': autoRetire!,
        if (endedAt != null) 'endedAt': endedAt!,
        if (initiatedBy != null) 'initiatedBy': initiatedBy!,
        if (approvals != null) 'approvals': approvals!.toJson(),
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
        if (activatesAt != null) 'activatesAt': activatesAt!,
        if (cacheHorizonAt != null) 'cacheHorizonAt': cacheHorizonAt!,
      };
}

/// Success response to vta/webvh/dids/keys/list. Type
/// https://trusttasks.org/spec/vta/webvh/dids/keys/list/1.0#response.
class Response {
  const Response({
    required this.did,
    required this.versionId,
    required this.projection,
    required this.roles,
    this.rotations,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        did: json['did'] as String,
        versionId: json['versionId'] as String,
        projection: KeyRoleProjection(json['projection'] as String),
        roles: (json['roles'] as List<dynamic>)
            .map((e) => RoleSummary.fromJson(e as Map<String, dynamic>))
            .toList(),
        rotations: json['rotations'] == null
            ? null
            : (json['rotations'] as List<dynamic>)
                .map((e) => RotationRecord.fromJson(e as Map<String, dynamic>))
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String did;

  /// The log entry the answer describes. Pass it as `expectedVersionId` to a change made
  /// on the strength of this answer.
  final String versionId;
  final KeyRoleProjection projection;

  /// One entry per role asked for, including roles with no keys — an empty role is an
  /// answer, and omitting it would read as 'not asked'.
  final List<RoleSummary> roles;

  /// Present exactly when `includeHistory` was true. Newest first. In the custody
  /// projection this includes changes in `pendingApproval`; in the public projection it
  /// includes only changes that published an entry.
  final List<RotationRecord>? rotations;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'versionId': versionId,
        'projection': projection.value,
        'roles': roles.map((e) => e.toJson()).toList(),
        if (rotations != null)
          'rotations': rotations!.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// List a did:webvh's keys by role, with their state, and optionally the DID's
/// rotation history and any changes awaiting approval.
class Payload {
  const Payload({
    required this.did,
    this.roles,
    this.includeEnded,
    this.includeHistory,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        roles: json['roles'] == null
            ? null
            : (json['roles'] as List<dynamic>)
                .map((e) => KeyRole(e as String))
                .toList(),
        includeEnded: json['includeEnded'] as bool?,
        includeHistory: json['includeHistory'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID whose key roles are listed.
  final String did;

  /// Return only these roles. Absent returns every role. Present-but-empty is refused by
  /// the schema rather than read as 'every role' — emptiness is never a wildcard.
  final List<KeyRole>? roles;

  /// Include `retired` and `revoked` keys. Absent reads as false. `revoked` keys still
  /// published in `verificationMethod` are always included, because the document shows
  /// them to anyone who resolves it.
  final bool? includeEnded;

  /// Include the DID's rotation history. Absent reads as false.
  final bool? includeHistory;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (roles != null) 'roles': roles!.map((e) => e.value).toList(),
        if (includeEnded != null) 'includeEnded': includeEnded!,
        if (includeHistory != null) 'includeHistory': includeHistory!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/webvh/dids/keys/list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/webvh/dids/keys/list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/webvh/dids/keys/list/1.0","title":"VTA WebVH DIDs Keys List — payload","description":"List a did:webvh\'s keys by role, with their state, and optionally the DID\'s rotation history and any changes awaiting approval.","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID whose key roles are listed."},"roles":{"type":"array","items":{"\$ref":"#/\$defs/KeyRole"},"uniqueItems":true,"minItems":1,"description":"Return only these roles. Absent returns every role. Present-but-empty is refused by the schema rather than read as \'every role\' — emptiness is never a wildcard."},"includeEnded":{"type":"boolean","description":"Include `retired` and `revoked` keys. Absent reads as false. `revoked` keys still published in `verificationMethod` are always included, because the document shows them to anyone who resolves it."},"includeHistory":{"type":"boolean","description":"Include the DID\'s rotation history. Absent reads as false."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH DIDs Keys List — response payload","description":"Success response to vta/webvh/dids/keys/list. Type https://trusttasks.org/spec/vta/webvh/dids/keys/list/1.0#response.","type":"object","additionalProperties":false,"required":["did","versionId","projection","roles"],"properties":{"did":{"type":"string"},"versionId":{"type":"string","description":"The log entry the answer describes. Pass it as `expectedVersionId` to a change made on the strength of this answer."},"projection":{"\$ref":"#/\$defs/KeyRoleProjection"},"roles":{"type":"array","items":{"\$ref":"#/\$defs/RoleSummary"},"description":"One entry per role asked for, including roles with no keys — an empty role is an answer, and omitting it would read as \'not asked\'."},"rotations":{"type":"array","items":{"\$ref":"#/\$defs/RotationRecord"},"description":"Present exactly when `includeHistory` was true. Newest first. In the custody projection this includes changes in `pendingApproval`; in the public projection it includes only changes that published an entry."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RotationRecord":{"title":"RotationRecord","type":"object","additionalProperties":false,"required":["rotationId","role","kind","state"],"description":"One change to a role\'s key set, from preview to completion. The rotation history of a DID is the list of these; the log is the authority for what was published, and this record adds what the log cannot say — which change was which, and (custody projection only) who asked, who approved, and why.","properties":{"rotationId":{"type":"string","minLength":1},"role":{"\$ref":"#/\$defs/KeyRole"},"kind":{"\$ref":"#/\$defs/RotationKind"},"state":{"\$ref":"#/\$defs/RotationState"},"predecessors":{"type":"array","items":{"type":"string"},"description":"Verification-method identifiers (or, for `update`, public keys) leaving the role."},"successors":{"type":"array","items":{"type":"string"},"description":"Verification-method identifiers (or, for `update`, public keys) entering the role."},"startedInVersionId":{"type":"string"},"endedInVersionId":{"type":"string"},"startedAt":{"type":"string","format":"date-time"},"overlapUntil":{"type":"string","format":"date-time","description":"Custody projection only. End of the overlap window."},"autoRetire":{"type":"boolean","description":"Custody projection only. Whether the VTA retires the predecessor itself at `overlapUntil`."},"endedAt":{"type":"string","format":"date-time"},"initiatedBy":{"type":"string","description":"Custody projection only. VID of the administrator whose signed request started the change."},"approvals":{"\$ref":"#/\$defs/KeyRoleApprovalState","description":"Custody projection only."},"reason":{"type":"string","maxLength":1024,"description":"Custody projection only. The operator\'s stated reason. Never published."},"ext":{"\$ref":"#/\$defs/Ext"},"activatesAt":{"type":"string","format":"date-time","description":"When the successor becomes `active` and the predecessor `retiring`; never before `cacheHorizonAt`."},"cacheHorizonAt":{"type":"string","format":"date-time","description":"Planned rotations: the publishing entry\'s cache horizon — publication plus the longer of the document\'s TTL and the verifier cache cap (CONVENTIONS.md §11.1). The switch, and any retirement of the predecessor, happen no earlier. Public: it follows from the log and the published cap."}}},"KeyRoleApprovalState":{"title":"KeyRoleApprovalState","type":"object","additionalProperties":false,"required":["required","received","expiresAt"],"description":"Approval progress of a change the VTA\'s policy gates on more than one approval (CONVENTIONS.md §3.3).","properties":{"required":{"type":"integer","minimum":1,"description":"Distinct approvals the VTA\'s policy requires."},"received":{"type":"integer","minimum":0,"description":"Distinct approvals recorded so far, including the initiator\'s own."},"approvers":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"VIDs whose approvals were recorded. Custody projection only; omitted from a response to anyone who is not themselves an eligible approver of this change."},"expiresAt":{"type":"string","format":"date-time","description":"When the preview, and every approval bound to it, stops being usable."}}},"RotationState":{"title":"RotationState","type":"string","enum":["pendingApproval","staged","overlapping","completed","aborted","expired"],"description":"`pendingApproval` — awaiting the approvals the VTA\'s policy requires; nothing is published. `staged` — successor published, not yet used; predecessor still active. `overlapping` — successor active, predecessor `retiring`. `completed` — every predecessor retired (or, for `compromise` and `addition`, the entry is published and any overlap it began has ended). `aborted` — the successor was retired instead of the predecessor, which stayed or returned to `active`. `expired` — approvals were not gathered before the preview expired; nothing was published."},"RotationKind":{"title":"RotationKind","type":"string","enum":["planned","compromise","addition"],"description":"`planned` — a successor was staged, activated, and the predecessor retired on the operator\'s schedule. `compromise` — a key was revoked and, where the role would otherwise be empty, replaced in the same log entry. `addition` — a key was added to a role without anything leaving it (for example a post-quantum key alongside a classical one)."},"KeyRole":{"title":"KeyRole","type":"string","enum":["attestation","operational","messaging","update"],"description":"The named key role a key occupies in the DID (VTI-KEY-070 onward). `attestation` — the only keys in `assertionMethod`; sign the DID\'s attestation artefacts (membership and role credentials, endorsements, status lists, every other credential the node issues), through the VTA\'s signing service and nowhere else. Generated inside the VTA, never derived, never exportable, never in a backup. `operational` — the only keys in `authentication`; sign the node\'s own traffic (Trust Task requests and responses, DIDComm and TSP message signatures, invitations, notices, audit checkpoints) with proof purpose `authentication`. `messaging` — the only keys in `keyAgreement`; sign nothing. `update` — the keys that authorize appending to the DID\'s log (a did:webvh `updateKeys` entry) and the pre-rotation commitments that authorize the next ones; never a verification method, never in `keyRoles`. Generated inside the VTA, never derived, never exportable, never in a backup. No role permits `capabilityInvocation` or `capabilityDelegation` (VTI-KEY-075); both are reserved for a future role. The mapping is normative and is set out in CONVENTIONS.md §1. The set is an extensible registry and growing it is a MINOR change: a consumer that receives a role it does not implement MUST refuse the document rather than map the key onto a role it does know."},"RoleSummary":{"title":"RoleSummary","type":"object","additionalProperties":false,"required":["role","relationships","keys"],"properties":{"role":{"\$ref":"#/\$defs/KeyRole"},"relationships":{"type":"array","items":{"\$ref":"#/\$defs/DidVerificationRelationship"},"uniqueItems":true,"description":"The relationship this role owns (CONVENTIONS.md §1). Empty for `update`."},"keys":{"type":"array","items":{"\$ref":"#/\$defs/RoleKey"},"description":"The role\'s keys, in the order the document publishes them. Terminal-state keys are included only when the request asked for them."},"preRotationCommitments":{"type":"integer","minimum":0,"description":"`update` only: successors the latest entry commits to. `0` means a compromise of the current update key cannot be recovered from by rotation, and the DID would have to be deactivated (VTI-KEY-124)."}}},"RoleKey":{"title":"RoleKey","type":"object","additionalProperties":false,"required":["role","keyType","publicKeyMultibase","state"],"description":"One key in one role. A key is in exactly one role for its whole life; a change of role is a retirement from one and an addition to another, under a new verification-method identifier.","properties":{"verificationMethod":{"type":"string","minLength":1,"description":"The full verification-method identifier (`did#fragment`) the document publishes the key under, and lists in `keyRoles`. Absent only for `update` keys, which are published as log parameters and never as verification methods (VTI-KEY-093). A VTA minting a new method SHOULD derive its fragment from the key (VTI-KEY-150)."},"role":{"\$ref":"#/\$defs/KeyRole"},"relationships":{"type":"array","items":{"\$ref":"#/\$defs/DidVerificationRelationship"},"uniqueItems":true,"description":"The relationships the key is (or, while `pending`, will be) listed in. Always exactly its role\'s single relationship (CONVENTIONS.md §1) — carried so a caller can check it against the document rather than trusting it. Empty for `update`, `retired` and `revoked` keys."},"keyType":{"\$ref":"#/\$defs/KeyType"},"publicKeyMultibase":{"type":"string","minLength":1,"description":"The public half, multibase-encoded, exactly as published."},"state":{"\$ref":"#/\$defs/RoleKeyState"},"publishedInVersionId":{"type":"string","description":"versionId of the log entry that first published the key. Absent while `pending`."},"removedInVersionId":{"type":"string","description":"versionId of the log entry that retired or revoked the key."},"retireAfter":{"type":"string","format":"date-time","description":"For a `retiring` key: the end of its overlap window. Custody projection only."},"compromisedSince":{"type":"string","format":"date-time","description":"For a `revoked` key: the compromise time (VTI-KEY-121) — the earliest instant from which the key is treated as compromised. Where the operator cannot establish it, the time the key was first published. Published, because it is what a verifier needs."},"rotationId":{"type":"string","description":"The rotation this key was introduced or retired by, when it was."},"label":{"type":"string","maxLength":256,"description":"Operator-facing label. Custody projection only."},"custody":{"\$ref":"#/\$defs/RoleKeyCustody"},"ext":{"\$ref":"#/\$defs/Ext"},"activatesAt":{"type":"string","format":"date-time","description":"For a `staged` key: when the VTA will begin using it — no earlier than the publishing entry\'s cache horizon (CONVENTIONS.md §11.1)."}}},"RoleKeyCustody":{"title":"RoleKeyCustody","type":"object","additionalProperties":false,"required":["keyId","exportable","neverExportable","inBackups"],"description":"How the VTA holds a key. Present only in the custody projection. Carries no key material, and a conforming VTA MUST NOT place any in `ext`.","properties":{"keyId":{"type":"string","minLength":1,"description":"The custodian record identifier (`keys/*`\'s `keyId`) behind this key."},"origin":{"\$ref":"#/\$defs/KeyOrigin"},"exportable":{"type":"boolean","description":"Whether the custodian would release the private half through `keys/export-secret`. Always `false` for `attestation` and `update` keys (CONVENTIONS.md §4). Stated explicitly — never absent — because this is the member an operator reads to confirm a role key cannot leave the VTA, and `keys/*`\'s absent-means-exportable reading is the wrong default to leave them to infer."},"neverExportable":{"type":"boolean","description":"True when no authority can make the key exportable — the role forbids it, or the material exists only inside a module that cannot release it. `true` implies `exportable: false`."},"hardwareBacked":{"type":"boolean","description":"True when the private half is held by a hardware or enclave module that performs the operation without releasing it. Absent means the VTA does not say, not that it is false."},"inBackups":{"type":"boolean","description":"Whether the key, or material it can be re-derived from, is included in the VTA\'s backups or any state transfer. Always `false` for `attestation` and `update` keys (VTI-KEY-112): they are generated, not derived, so there is no seed that recovers them, and their loss is recovered by rotation, never by restore."},"destroyed":{"type":"boolean","description":"True once the private half has been destroyed — a retired `attestation` key (VTI-KEY-125). The record survives so the key\'s past signatures stay attributable."}}},"KeyOrigin":{"title":"KeyOrigin","type":"string","enum":["derived","imported","internal"],"description":"Where the private key came from. `derived` means the maintainer generated it from a seed it holds and can reproduce it from `derivationPath`; `imported` means it arrived from outside and exists only as stored material; `internal` means the maintainer generated it from a CSPRNG and it is reproducible from nothing at all. The distinction is operationally load-bearing: a `derived` key survives a seed restore, an `imported` one is lost unless it was backed up separately, and an `internal` one cannot be recovered by any means once the maintainer\'s storage is gone. This member is also the only way a consumer can confirm that a `keys/create` request for an `internal` key was honoured rather than silently downgraded to a derived one — see that specification\'s `internal` member.","default":"derived"},"RoleKeyState":{"title":"RoleKeyState","type":"string","enum":["pending","staged","active","retiring","retired","revoked"],"description":"Where a key is in its role\'s lifecycle. `pending` — planned by a preview or a change awaiting approval; not published; custody projection only. `staged` — published and bound to its role, but not yet used: a planned rotation publishes the successor and waits until the entry\'s cache horizon — publication plus the longer of the document\'s TTL and the verifier cache cap (CONVENTIONS.md §11.1) — before the VTA first uses it, so that no verifier holding a cached document sees a signature by a key it has never seen (VTI-KEY-122). Becomes `active` at the rotation\'s `activatesAt`. `active` — the key the VTA uses for new signatures (or, for `messaging`, advertises for new sessions). `retiring` — still published so that what it signed and sessions keyed to it keep working, but never used again: from the successor\'s first use the VTA MUST NOT sign with it, while a retiring `messaging` key stays usable for decryption, and senders may still encrypt to it, until it is retired (CONVENTIONS.md §11.3). `retired` — removed by planned rotation; what it signed while published remains valid, judged against the DID version current at issuance (CONVENTIONS.md §7). A retired `attestation` or `messaging` key is destroyed at retirement. `revoked` — removed from every relationship and from `keyRoles` in one entry, without overlap, because it is or may be compromised; what it signed from `compromisedSince` onward establishes nothing. `retired` and `revoked` are terminal."},"DidVerificationRelationship":{"title":"DidVerificationRelationship","type":"string","enum":["authentication","assertionMethod","keyAgreement","capabilityInvocation","capabilityDelegation"],"description":"A DID Core verification relationship."},"KeyType":{"title":"KeyType","type":"string","enum":["ed25519","x25519","p256","mldsa44","mldsa65"],"description":"Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA), `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is expected to grow as algorithms are standardised, and growing it is a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a constraint rather than narrowing one, and the generated libraries mark this enumeration non-exhaustive so that a consumer absorbs a new value rather than failing to compile. Two ML-DSA parameter sets are carried because two specifications require different ones — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key and the two are not redundant. A consumer that does not implement a value it receives MUST refuse the document rather than substitute one it does support."},"KeyRoleProjection":{"title":"KeyRoleProjection","type":"string","enum":["public","custody"],"description":"Which view of the DID\'s key roles the caller was given (CONVENTIONS.md §5). `public` — only what the published document and log already reveal to anyone who resolves the DID. `custody` — additionally the VTA\'s own custody state: pending keys, the custodian record behind each key, overlap deadlines, who initiated and approved each change, and why. The response always states which it is, so a caller never mistakes an absent member for a statement that the thing does not exist."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH DIDs Keys List — response payload","description":"Success response to vta/webvh/dids/keys/list. Type https://trusttasks.org/spec/vta/webvh/dids/keys/list/1.0#response.","type":"object","additionalProperties":false,"required":["did","versionId","projection","roles"],"properties":{"did":{"type":"string"},"versionId":{"type":"string","description":"The log entry the answer describes. Pass it as `expectedVersionId` to a change made on the strength of this answer."},"projection":{"\$ref":"#/\$defs/KeyRoleProjection"},"roles":{"type":"array","items":{"\$ref":"#/\$defs/RoleSummary"},"description":"One entry per role asked for, including roles with no keys — an empty role is an answer, and omitting it would read as \'not asked\'."},"rotations":{"type":"array","items":{"\$ref":"#/\$defs/RotationRecord"},"description":"Present exactly when `includeHistory` was true. Newest first. In the custody projection this includes changes in `pendingApproval`; in the public projection it includes only changes that published an entry."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RotationRecord":{"title":"RotationRecord","type":"object","additionalProperties":false,"required":["rotationId","role","kind","state"],"description":"One change to a role\'s key set, from preview to completion. The rotation history of a DID is the list of these; the log is the authority for what was published, and this record adds what the log cannot say — which change was which, and (custody projection only) who asked, who approved, and why.","properties":{"rotationId":{"type":"string","minLength":1},"role":{"\$ref":"#/\$defs/KeyRole"},"kind":{"\$ref":"#/\$defs/RotationKind"},"state":{"\$ref":"#/\$defs/RotationState"},"predecessors":{"type":"array","items":{"type":"string"},"description":"Verification-method identifiers (or, for `update`, public keys) leaving the role."},"successors":{"type":"array","items":{"type":"string"},"description":"Verification-method identifiers (or, for `update`, public keys) entering the role."},"startedInVersionId":{"type":"string"},"endedInVersionId":{"type":"string"},"startedAt":{"type":"string","format":"date-time"},"overlapUntil":{"type":"string","format":"date-time","description":"Custody projection only. End of the overlap window."},"autoRetire":{"type":"boolean","description":"Custody projection only. Whether the VTA retires the predecessor itself at `overlapUntil`."},"endedAt":{"type":"string","format":"date-time"},"initiatedBy":{"type":"string","description":"Custody projection only. VID of the administrator whose signed request started the change."},"approvals":{"\$ref":"#/\$defs/KeyRoleApprovalState","description":"Custody projection only."},"reason":{"type":"string","maxLength":1024,"description":"Custody projection only. The operator\'s stated reason. Never published."},"ext":{"\$ref":"#/\$defs/Ext"},"activatesAt":{"type":"string","format":"date-time","description":"When the successor becomes `active` and the predecessor `retiring`; never before `cacheHorizonAt`."},"cacheHorizonAt":{"type":"string","format":"date-time","description":"Planned rotations: the publishing entry\'s cache horizon — publication plus the longer of the document\'s TTL and the verifier cache cap (CONVENTIONS.md §11.1). The switch, and any retirement of the predecessor, happen no earlier. Public: it follows from the log and the published cap."}}},"KeyRoleApprovalState":{"title":"KeyRoleApprovalState","type":"object","additionalProperties":false,"required":["required","received","expiresAt"],"description":"Approval progress of a change the VTA\'s policy gates on more than one approval (CONVENTIONS.md §3.3).","properties":{"required":{"type":"integer","minimum":1,"description":"Distinct approvals the VTA\'s policy requires."},"received":{"type":"integer","minimum":0,"description":"Distinct approvals recorded so far, including the initiator\'s own."},"approvers":{"type":"array","items":{"type":"string"},"uniqueItems":true,"description":"VIDs whose approvals were recorded. Custody projection only; omitted from a response to anyone who is not themselves an eligible approver of this change."},"expiresAt":{"type":"string","format":"date-time","description":"When the preview, and every approval bound to it, stops being usable."}}},"RotationState":{"title":"RotationState","type":"string","enum":["pendingApproval","staged","overlapping","completed","aborted","expired"],"description":"`pendingApproval` — awaiting the approvals the VTA\'s policy requires; nothing is published. `staged` — successor published, not yet used; predecessor still active. `overlapping` — successor active, predecessor `retiring`. `completed` — every predecessor retired (or, for `compromise` and `addition`, the entry is published and any overlap it began has ended). `aborted` — the successor was retired instead of the predecessor, which stayed or returned to `active`. `expired` — approvals were not gathered before the preview expired; nothing was published."},"RotationKind":{"title":"RotationKind","type":"string","enum":["planned","compromise","addition"],"description":"`planned` — a successor was staged, activated, and the predecessor retired on the operator\'s schedule. `compromise` — a key was revoked and, where the role would otherwise be empty, replaced in the same log entry. `addition` — a key was added to a role without anything leaving it (for example a post-quantum key alongside a classical one)."},"KeyRole":{"title":"KeyRole","type":"string","enum":["attestation","operational","messaging","update"],"description":"The named key role a key occupies in the DID (VTI-KEY-070 onward). `attestation` — the only keys in `assertionMethod`; sign the DID\'s attestation artefacts (membership and role credentials, endorsements, status lists, every other credential the node issues), through the VTA\'s signing service and nowhere else. Generated inside the VTA, never derived, never exportable, never in a backup. `operational` — the only keys in `authentication`; sign the node\'s own traffic (Trust Task requests and responses, DIDComm and TSP message signatures, invitations, notices, audit checkpoints) with proof purpose `authentication`. `messaging` — the only keys in `keyAgreement`; sign nothing. `update` — the keys that authorize appending to the DID\'s log (a did:webvh `updateKeys` entry) and the pre-rotation commitments that authorize the next ones; never a verification method, never in `keyRoles`. Generated inside the VTA, never derived, never exportable, never in a backup. No role permits `capabilityInvocation` or `capabilityDelegation` (VTI-KEY-075); both are reserved for a future role. The mapping is normative and is set out in CONVENTIONS.md §1. The set is an extensible registry and growing it is a MINOR change: a consumer that receives a role it does not implement MUST refuse the document rather than map the key onto a role it does know."},"RoleSummary":{"title":"RoleSummary","type":"object","additionalProperties":false,"required":["role","relationships","keys"],"properties":{"role":{"\$ref":"#/\$defs/KeyRole"},"relationships":{"type":"array","items":{"\$ref":"#/\$defs/DidVerificationRelationship"},"uniqueItems":true,"description":"The relationship this role owns (CONVENTIONS.md §1). Empty for `update`."},"keys":{"type":"array","items":{"\$ref":"#/\$defs/RoleKey"},"description":"The role\'s keys, in the order the document publishes them. Terminal-state keys are included only when the request asked for them."},"preRotationCommitments":{"type":"integer","minimum":0,"description":"`update` only: successors the latest entry commits to. `0` means a compromise of the current update key cannot be recovered from by rotation, and the DID would have to be deactivated (VTI-KEY-124)."}}},"RoleKey":{"title":"RoleKey","type":"object","additionalProperties":false,"required":["role","keyType","publicKeyMultibase","state"],"description":"One key in one role. A key is in exactly one role for its whole life; a change of role is a retirement from one and an addition to another, under a new verification-method identifier.","properties":{"verificationMethod":{"type":"string","minLength":1,"description":"The full verification-method identifier (`did#fragment`) the document publishes the key under, and lists in `keyRoles`. Absent only for `update` keys, which are published as log parameters and never as verification methods (VTI-KEY-093). A VTA minting a new method SHOULD derive its fragment from the key (VTI-KEY-150)."},"role":{"\$ref":"#/\$defs/KeyRole"},"relationships":{"type":"array","items":{"\$ref":"#/\$defs/DidVerificationRelationship"},"uniqueItems":true,"description":"The relationships the key is (or, while `pending`, will be) listed in. Always exactly its role\'s single relationship (CONVENTIONS.md §1) — carried so a caller can check it against the document rather than trusting it. Empty for `update`, `retired` and `revoked` keys."},"keyType":{"\$ref":"#/\$defs/KeyType"},"publicKeyMultibase":{"type":"string","minLength":1,"description":"The public half, multibase-encoded, exactly as published."},"state":{"\$ref":"#/\$defs/RoleKeyState"},"publishedInVersionId":{"type":"string","description":"versionId of the log entry that first published the key. Absent while `pending`."},"removedInVersionId":{"type":"string","description":"versionId of the log entry that retired or revoked the key."},"retireAfter":{"type":"string","format":"date-time","description":"For a `retiring` key: the end of its overlap window. Custody projection only."},"compromisedSince":{"type":"string","format":"date-time","description":"For a `revoked` key: the compromise time (VTI-KEY-121) — the earliest instant from which the key is treated as compromised. Where the operator cannot establish it, the time the key was first published. Published, because it is what a verifier needs."},"rotationId":{"type":"string","description":"The rotation this key was introduced or retired by, when it was."},"label":{"type":"string","maxLength":256,"description":"Operator-facing label. Custody projection only."},"custody":{"\$ref":"#/\$defs/RoleKeyCustody"},"ext":{"\$ref":"#/\$defs/Ext"},"activatesAt":{"type":"string","format":"date-time","description":"For a `staged` key: when the VTA will begin using it — no earlier than the publishing entry\'s cache horizon (CONVENTIONS.md §11.1)."}}},"RoleKeyCustody":{"title":"RoleKeyCustody","type":"object","additionalProperties":false,"required":["keyId","exportable","neverExportable","inBackups"],"description":"How the VTA holds a key. Present only in the custody projection. Carries no key material, and a conforming VTA MUST NOT place any in `ext`.","properties":{"keyId":{"type":"string","minLength":1,"description":"The custodian record identifier (`keys/*`\'s `keyId`) behind this key."},"origin":{"\$ref":"#/\$defs/KeyOrigin"},"exportable":{"type":"boolean","description":"Whether the custodian would release the private half through `keys/export-secret`. Always `false` for `attestation` and `update` keys (CONVENTIONS.md §4). Stated explicitly — never absent — because this is the member an operator reads to confirm a role key cannot leave the VTA, and `keys/*`\'s absent-means-exportable reading is the wrong default to leave them to infer."},"neverExportable":{"type":"boolean","description":"True when no authority can make the key exportable — the role forbids it, or the material exists only inside a module that cannot release it. `true` implies `exportable: false`."},"hardwareBacked":{"type":"boolean","description":"True when the private half is held by a hardware or enclave module that performs the operation without releasing it. Absent means the VTA does not say, not that it is false."},"inBackups":{"type":"boolean","description":"Whether the key, or material it can be re-derived from, is included in the VTA\'s backups or any state transfer. Always `false` for `attestation` and `update` keys (VTI-KEY-112): they are generated, not derived, so there is no seed that recovers them, and their loss is recovered by rotation, never by restore."},"destroyed":{"type":"boolean","description":"True once the private half has been destroyed — a retired `attestation` key (VTI-KEY-125). The record survives so the key\'s past signatures stay attributable."}}},"KeyOrigin":{"title":"KeyOrigin","type":"string","enum":["derived","imported","internal"],"description":"Where the private key came from. `derived` means the maintainer generated it from a seed it holds and can reproduce it from `derivationPath`; `imported` means it arrived from outside and exists only as stored material; `internal` means the maintainer generated it from a CSPRNG and it is reproducible from nothing at all. The distinction is operationally load-bearing: a `derived` key survives a seed restore, an `imported` one is lost unless it was backed up separately, and an `internal` one cannot be recovered by any means once the maintainer\'s storage is gone. This member is also the only way a consumer can confirm that a `keys/create` request for an `internal` key was honoured rather than silently downgraded to a derived one — see that specification\'s `internal` member.","default":"derived"},"RoleKeyState":{"title":"RoleKeyState","type":"string","enum":["pending","staged","active","retiring","retired","revoked"],"description":"Where a key is in its role\'s lifecycle. `pending` — planned by a preview or a change awaiting approval; not published; custody projection only. `staged` — published and bound to its role, but not yet used: a planned rotation publishes the successor and waits until the entry\'s cache horizon — publication plus the longer of the document\'s TTL and the verifier cache cap (CONVENTIONS.md §11.1) — before the VTA first uses it, so that no verifier holding a cached document sees a signature by a key it has never seen (VTI-KEY-122). Becomes `active` at the rotation\'s `activatesAt`. `active` — the key the VTA uses for new signatures (or, for `messaging`, advertises for new sessions). `retiring` — still published so that what it signed and sessions keyed to it keep working, but never used again: from the successor\'s first use the VTA MUST NOT sign with it, while a retiring `messaging` key stays usable for decryption, and senders may still encrypt to it, until it is retired (CONVENTIONS.md §11.3). `retired` — removed by planned rotation; what it signed while published remains valid, judged against the DID version current at issuance (CONVENTIONS.md §7). A retired `attestation` or `messaging` key is destroyed at retirement. `revoked` — removed from every relationship and from `keyRoles` in one entry, without overlap, because it is or may be compromised; what it signed from `compromisedSince` onward establishes nothing. `retired` and `revoked` are terminal."},"DidVerificationRelationship":{"title":"DidVerificationRelationship","type":"string","enum":["authentication","assertionMethod","keyAgreement","capabilityInvocation","capabilityDelegation"],"description":"A DID Core verification relationship."},"KeyType":{"title":"KeyType","type":"string","enum":["ed25519","x25519","p256","mldsa44","mldsa65"],"description":"Cryptographic algorithm the key material belongs to. `ed25519` signs (EdDSA), `x25519` performs key agreement and never signs, `p256` signs (ES256), and `mldsa44` and `mldsa65` sign with the post-quantum ML-DSA scheme of US NIST FIPS 204. The set is expected to grow as algorithms are standardised, and growing it is a MINOR change under SPEC.md §5.2: `keyType` selects no schema branch, so adding a value relaxes a constraint rather than narrowing one, and the generated libraries mark this enumeration non-exhaustive so that a consumer absorbs a new value rather than failing to compile. Two ML-DSA parameter sets are carried because two specifications require different ones — W3C Quantum-Resistant Cryptosuites defines Data Integrity suites only for ML-DSA-44, while Trust Spanning Protocol Rev 3 §8.1 mandates ML-DSA-65 — so the parameter set is chosen by whatever consumes the key and the two are not redundant. A consumer that does not implement a value it receives MUST refuse the document rather than substitute one it does support."},"KeyRoleProjection":{"title":"KeyRoleProjection","type":"string","enum":["public","custody"],"description":"Which view of the DID\'s key roles the caller was given (CONVENTIONS.md §5). `public` — only what the published document and log already reveal to anyone who resolves the DID. `custody` — additionally the VTA\'s own custody state: pending keys, the custodian record behind each key, overlap deadlines, who initiated and approved each change, and why. The response always states which it is, so a caller never mistakes an absent member for a statement that the thing does not exist."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
