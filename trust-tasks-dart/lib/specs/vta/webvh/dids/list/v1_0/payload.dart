// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/dids/list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A did:webvh DID as the VTA holds it. The DID document itself is not here — it lives
/// in the log, which `vta/webvh/dids/get` returns on request.
class WebvhDidRecord {
  const WebvhDidRecord({
    required this.did,
    required this.serverId,
    required this.mnemonic,
    required this.scid,
    required this.contextId,
    required this.portable,
    required this.logEntryCount,
    this.preRotationCount,
    this.nextFragmentId,
    required this.createdAt,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory WebvhDidRecord.fromJson(Map<String, dynamic> json) => WebvhDidRecord(
        did: json['did'] as String,
        serverId: json['serverId'] as String,
        mnemonic: json['mnemonic'] as String,
        scid: json['scid'] as String,
        contextId: json['contextId'] as String,
        portable: json['portable'] as bool,
        logEntryCount: json['logEntryCount'] as int,
        preRotationCount: json['preRotationCount'] as int?,
        nextFragmentId: json['nextFragmentId'] as int?,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID, e.g. `did:webvh:&lt;scid&gt;:example.com:alice`.
  final String did;

  /// Id of the hosting server serving this DID's log. Empty for a serverless DID, whose
  /// location comes from the URL it was created with.
  final String serverId;

  /// The hosting server's handle for the record. Unrelated to a BIP-39 phrase; the
  /// collision of terms is historical.
  final String mnemonic;

  /// Self-certifying identifier: the hash committing to the log's first entry. It is
  /// what makes the DID's history tamper-evident, and it changes only when the log is
  /// re-created — never in place.
  final String scid;

  /// Context this DID belongs to. Deleting that context destroys this DID.
  final String contextId;

  /// Whether the DID may be moved to another domain without losing its identity. Fixed
  /// at creation: a DID created non-portable cannot be made portable later, because
  /// portability is a commitment recorded in the log's first entry.
  final bool portable;

  /// Number of entries in the log.
  final int logEntryCount;

  /// Pre-rotation keys committed by the most recent entry. `0` means pre-rotation is
  /// disabled — a compromise of the current key is then unrecoverable by rotation,
  /// because the successor was never committed in advance.
  final int? preRotationCount;

  /// Next `#key-{n}` fragment id to mint. Monotonic and never decremented, so a fragment
  /// is never reused for a different key.
  final int? nextFragmentId;
  final String createdAt;
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'serverId': serverId,
        'mnemonic': mnemonic,
        'scid': scid,
        'contextId': contextId,
        'portable': portable,
        'logEntryCount': logEntryCount,
        if (preRotationCount != null) 'preRotationCount': preRotationCount!,
        if (nextFragmentId != null) 'nextFragmentId': nextFragmentId!,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// Success response to vta/webvh/dids/list. Type
/// https://trusttasks.org/spec/vta/webvh/dids/list/1.0#response.
class Response {
  const Response({
    required this.dids,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        dids: (json['dids'] as List<dynamic>)
            .map((e) => WebvhDidRecord.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Matching DIDs the caller may see. An empty array is a successful answer and does
  /// not imply the VTA holds none.
  final List<WebvhDidRecord> dids;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'dids': dids.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// VTA WebVH DIDs List — payload
class Payload {
  const Payload({
    this.contextId,
    this.serverId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String?,
        serverId: json['serverId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Only DIDs in this context.
  final String? contextId;

  /// Only DIDs published through this hosting server.
  final String? serverId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (contextId != null) 'contextId': contextId!,
        if (serverId != null) 'serverId': serverId!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vta/webvh/dids/list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/webvh/dids/list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/webvh/dids/list/1.0","title":"VTA WebVH DIDs List — payload","type":"object","additionalProperties":false,"properties":{"contextId":{"type":"string","description":"Only DIDs in this context."},"serverId":{"type":"string","description":"Only DIDs published through this hosting server."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH DIDs List — response payload","description":"Success response to vta/webvh/dids/list. Type https://trusttasks.org/spec/vta/webvh/dids/list/1.0#response.","type":"object","additionalProperties":false,"required":["dids"],"properties":{"dids":{"type":"array","description":"Matching DIDs the caller may see. An empty array is a successful answer and does not imply the VTA holds none.","items":{"\$ref":"#/\$defs/WebvhDidRecord"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"WebvhDidRecord":{"title":"WebvhDidRecord","description":"A did:webvh DID as the VTA holds it. The DID document itself is not here — it lives in the log, which `vta/webvh/dids/get` returns on request.","type":"object","additionalProperties":false,"required":["did","serverId","mnemonic","scid","contextId","portable","logEntryCount","createdAt","updatedAt"],"properties":{"did":{"type":"string","description":"The DID, e.g. `did:webvh:<scid>:example.com:alice`."},"serverId":{"type":"string","description":"Id of the hosting server serving this DID\'s log. Empty for a serverless DID, whose location comes from the URL it was created with."},"mnemonic":{"type":"string","description":"The hosting server\'s handle for the record. Unrelated to a BIP-39 phrase; the collision of terms is historical."},"scid":{"type":"string","description":"Self-certifying identifier: the hash committing to the log\'s first entry. It is what makes the DID\'s history tamper-evident, and it changes only when the log is re-created — never in place."},"contextId":{"type":"string","description":"Context this DID belongs to. Deleting that context destroys this DID."},"portable":{"type":"boolean","description":"Whether the DID may be moved to another domain without losing its identity. Fixed at creation: a DID created non-portable cannot be made portable later, because portability is a commitment recorded in the log\'s first entry."},"logEntryCount":{"type":"integer","minimum":0,"description":"Number of entries in the log."},"preRotationCount":{"type":"integer","minimum":0,"default":0,"description":"Pre-rotation keys committed by the most recent entry. `0` means pre-rotation is disabled — a compromise of the current key is then unrecoverable by rotation, because the successor was never committed in advance."},"nextFragmentId":{"type":"integer","minimum":1,"default":1,"description":"Next `#key-{n}` fragment id to mint. Monotonic and never decremented, so a fragment is never reused for a different key."},"createdAt":{"type":"string","format":"date-time"},"updatedAt":{"type":"string","format":"date-time"},"ext":{"\$ref":"#/\$defs/Ext"}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH DIDs List — response payload","description":"Success response to vta/webvh/dids/list. Type https://trusttasks.org/spec/vta/webvh/dids/list/1.0#response.","type":"object","additionalProperties":false,"required":["dids"],"properties":{"dids":{"type":"array","description":"Matching DIDs the caller may see. An empty array is a successful answer and does not imply the VTA holds none.","items":{"\$ref":"#/\$defs/WebvhDidRecord"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"WebvhDidRecord":{"title":"WebvhDidRecord","description":"A did:webvh DID as the VTA holds it. The DID document itself is not here — it lives in the log, which `vta/webvh/dids/get` returns on request.","type":"object","additionalProperties":false,"required":["did","serverId","mnemonic","scid","contextId","portable","logEntryCount","createdAt","updatedAt"],"properties":{"did":{"type":"string","description":"The DID, e.g. `did:webvh:<scid>:example.com:alice`."},"serverId":{"type":"string","description":"Id of the hosting server serving this DID\'s log. Empty for a serverless DID, whose location comes from the URL it was created with."},"mnemonic":{"type":"string","description":"The hosting server\'s handle for the record. Unrelated to a BIP-39 phrase; the collision of terms is historical."},"scid":{"type":"string","description":"Self-certifying identifier: the hash committing to the log\'s first entry. It is what makes the DID\'s history tamper-evident, and it changes only when the log is re-created — never in place."},"contextId":{"type":"string","description":"Context this DID belongs to. Deleting that context destroys this DID."},"portable":{"type":"boolean","description":"Whether the DID may be moved to another domain without losing its identity. Fixed at creation: a DID created non-portable cannot be made portable later, because portability is a commitment recorded in the log\'s first entry."},"logEntryCount":{"type":"integer","minimum":0,"description":"Number of entries in the log."},"preRotationCount":{"type":"integer","minimum":0,"default":0,"description":"Pre-rotation keys committed by the most recent entry. `0` means pre-rotation is disabled — a compromise of the current key is then unrecoverable by rotation, because the successor was never committed in advance."},"nextFragmentId":{"type":"integer","minimum":1,"default":1,"description":"Next `#key-{n}` fragment id to mint. Monotonic and never decremented, so a fragment is never reused for a different key."},"createdAt":{"type":"string","format":"date-time"},"updatedAt":{"type":"string","format":"date-time"},"ext":{"\$ref":"#/\$defs/Ext"}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
