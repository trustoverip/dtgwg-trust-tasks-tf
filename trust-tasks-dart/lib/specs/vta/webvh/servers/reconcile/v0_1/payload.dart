// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/servers/reconcile/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A DID the hosting server serves and the agent has no record of
class HostOnlyDid {
  const HostOnlyDid({
    required this.slotId,
    this.did,
    this.domain,
    this.disabled,
  });

  /// Read this payload from a decoded JSON object.
  factory HostOnlyDid.fromJson(Map<String, dynamic> json) => HostOnlyDid(
        slotId: json['slotId'] as String,
        did: json['did'] as String?,
        domain: json['domain'] as String?,
        disabled: json['disabled'] as bool?,
      );

  /// The hosting server's identifier for the slot — what the server's own management API
  /// addresses it by. This is the comparison key, not `did`: a slot that was reserved
  /// but never published to has no DID at all, and is as unreconciled as one that was.
  final String slotId;

  /// The DID served at that slot, where the slot has been published to. Absent for a
  /// reserved-but-unpublished slot.
  final String? did;

  /// Hosting domain the slot belongs to, as the server reported it. Absent where the
  /// server reports none.
  final String? domain;

  /// Whether the hosting server currently has the slot disabled. A disabled slot is
  /// still unreconciled — the agent holds no key for it either way.
  final bool? disabled;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'slotId': slotId,
        if (did != null) 'did': did!,
        if (domain != null) 'domain': domain!,
        if (disabled != null) 'disabled': disabled!,
      };
}

/// A DID the agent records as hosted on this server, which the server does not have
class AgentOnlyDid {
  const AgentOnlyDid({
    required this.did,
    required this.slotId,
    this.contextId,
  });

  /// Read this payload from a decoded JSON object.
  factory AgentOnlyDid.fromJson(Map<String, dynamic> json) => AgentOnlyDid(
        did: json['did'] as String,
        slotId: json['slotId'] as String,
        contextId: json['contextId'] as String?,
      );

  /// The DID the agent holds a record for.
  final String did;

  /// The slot the agent believes the DID occupies on that server.
  final String slotId;

  /// Agent-local grouping the DID belongs to, where the agent has one. Reported so an
  /// operator can tell which part of their deployment the divergence is in.
  final String? contextId;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'slotId': slotId,
        if (contextId != null) 'contextId': contextId!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The comparison. Carried in a Trust Task document whose type is
/// https://trusttasks.org/spec/vta/webvh/servers/reconcile/0.1#response.
class Response {
  const Response({
    required this.serverId,
    required this.hostOnly,
    required this.agentOnly,
    required this.inBoth,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        serverId: json['serverId'] as String,
        hostOnly: (json['hostOnly'] as List<dynamic>)
            .map((e) => HostOnlyDid.fromJson(e as Map<String, dynamic>))
            .toList(),
        agentOnly: (json['agentOnly'] as List<dynamic>)
            .map((e) => AgentOnlyDid.fromJson(e as Map<String, dynamic>))
            .toList(),
        inBoth: json['inBoth'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The server compared against, echoed so a stored report is self-describing.
  final String serverId;

  /// Slots the hosting server serves that the agent has no record of. The agent holds no
  /// update key for these, so no further version of them can be signed by it.
  final List<HostOnlyDid> hostOnly;

  /// DIDs the agent records as hosted on this server, which the server does not have.
  final List<AgentOnlyDid> agentOnly;

  /// How many the two agree on. Required, and required even when both arrays are empty:
  /// it is what distinguishes 'compared them, all matched' from 'compared nothing'. A
  /// consumer that rendered only the divergences would show an identical screen for a
  /// clean estate and for a listing that silently returned no rows.
  final int inBoth;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'serverId': serverId,
        'hostOnly': hostOnly.map((e) => e.toJson()).toList(),
        'agentOnly': agentOnly.map((e) => e.toJson()).toList(),
        'inBoth': inBoth,
        if (ext != null) 'ext': ext!,
      };
}

/// Ask an agent to compare the DIDs a hosting server holds for it against the DIDs it
/// has records for, and report where the two disagree. Neither party can answer this
/// alone: the producer holds no credentials for the hosting server, and the server has
/// no view of the agent's records.
class Payload {
  const Payload({
    required this.serverId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        serverId: json['serverId'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Identifier of the registered hosting server to compare against. Required for the
  /// same reason as on `vta/webvh/servers/domains/0.1`: an agent may hold registrations
  /// with several servers, and a comparison that merged them would place every DID in a
  /// report without saying which host it was — or was not — on.
  final String serverId;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'serverId': serverId,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/webvh/servers/reconcile/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/webvh/servers/reconcile/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/webvh/servers/reconcile/0.1","title":"WebVH Servers — Reconcile — payload","description":"Ask an agent to compare the DIDs a hosting server holds for it against the DIDs it has records for, and report where the two disagree. Neither party can answer this alone: the producer holds no credentials for the hosting server, and the server has no view of the agent\'s records.","type":"object","additionalProperties":false,"required":["serverId"],"properties":{"serverId":{"type":"string","minLength":1,"description":"Identifier of the registered hosting server to compare against. Required for the same reason as on `vta/webvh/servers/domains/0.1`: an agent may hold registrations with several servers, and a comparison that merged them would place every DID in a report without saying which host it was — or was not — on."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"HostOnlyDid":{"title":"A DID the hosting server serves and the agent has no record of","type":"object","additionalProperties":false,"required":["slotId"],"properties":{"slotId":{"type":"string","minLength":1,"description":"The hosting server\'s identifier for the slot — what the server\'s own management API addresses it by. This is the comparison key, not `did`: a slot that was reserved but never published to has no DID at all, and is as unreconciled as one that was."},"did":{"type":"string","minLength":1,"description":"The DID served at that slot, where the slot has been published to. Absent for a reserved-but-unpublished slot."},"domain":{"type":"string","minLength":1,"description":"Hosting domain the slot belongs to, as the server reported it. Absent where the server reports none."},"disabled":{"type":"boolean","default":false,"description":"Whether the hosting server currently has the slot disabled. A disabled slot is still unreconciled — the agent holds no key for it either way."}}},"AgentOnlyDid":{"title":"A DID the agent records as hosted on this server, which the server does not have","type":"object","additionalProperties":false,"required":["did","slotId"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID the agent holds a record for."},"slotId":{"type":"string","minLength":1,"description":"The slot the agent believes the DID occupies on that server."},"contextId":{"type":"string","minLength":1,"description":"Agent-local grouping the DID belongs to, where the agent has one. Reported so an operator can tell which part of their deployment the divergence is in."}}},"Response":{"\$anchor":"response","title":"WebVH Servers — Reconcile — response payload","description":"The comparison. Carried in a Trust Task document whose type is https://trusttasks.org/spec/vta/webvh/servers/reconcile/0.1#response.","type":"object","additionalProperties":false,"required":["serverId","hostOnly","agentOnly","inBoth"],"properties":{"serverId":{"type":"string","minLength":1,"description":"The server compared against, echoed so a stored report is self-describing."},"hostOnly":{"type":"array","description":"Slots the hosting server serves that the agent has no record of. The agent holds no update key for these, so no further version of them can be signed by it.","items":{"\$ref":"#/\$defs/HostOnlyDid"}},"agentOnly":{"type":"array","description":"DIDs the agent records as hosted on this server, which the server does not have.","items":{"\$ref":"#/\$defs/AgentOnlyDid"}},"inBoth":{"type":"integer","minimum":0,"description":"How many the two agree on. Required, and required even when both arrays are empty: it is what distinguishes \'compared them, all matched\' from \'compared nothing\'. A consumer that rendered only the divergences would show an identical screen for a clean estate and for a listing that silently returned no rows."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"HostOnlyDid":{"title":"A DID the hosting server serves and the agent has no record of","type":"object","additionalProperties":false,"required":["slotId"],"properties":{"slotId":{"type":"string","minLength":1,"description":"The hosting server\'s identifier for the slot — what the server\'s own management API addresses it by. This is the comparison key, not `did`: a slot that was reserved but never published to has no DID at all, and is as unreconciled as one that was."},"did":{"type":"string","minLength":1,"description":"The DID served at that slot, where the slot has been published to. Absent for a reserved-but-unpublished slot."},"domain":{"type":"string","minLength":1,"description":"Hosting domain the slot belongs to, as the server reported it. Absent where the server reports none."},"disabled":{"type":"boolean","default":false,"description":"Whether the hosting server currently has the slot disabled. A disabled slot is still unreconciled — the agent holds no key for it either way."}}},"AgentOnlyDid":{"title":"A DID the agent records as hosted on this server, which the server does not have","type":"object","additionalProperties":false,"required":["did","slotId"],"properties":{"did":{"type":"string","minLength":1,"description":"The DID the agent holds a record for."},"slotId":{"type":"string","minLength":1,"description":"The slot the agent believes the DID occupies on that server."},"contextId":{"type":"string","minLength":1,"description":"Agent-local grouping the DID belongs to, where the agent has one. Reported so an operator can tell which part of their deployment the divergence is in."}}},"Response":{"\$anchor":"response","title":"WebVH Servers — Reconcile — response payload","description":"The comparison. Carried in a Trust Task document whose type is https://trusttasks.org/spec/vta/webvh/servers/reconcile/0.1#response.","type":"object","additionalProperties":false,"required":["serverId","hostOnly","agentOnly","inBoth"],"properties":{"serverId":{"type":"string","minLength":1,"description":"The server compared against, echoed so a stored report is self-describing."},"hostOnly":{"type":"array","description":"Slots the hosting server serves that the agent has no record of. The agent holds no update key for these, so no further version of them can be signed by it.","items":{"\$ref":"#/\$defs/HostOnlyDid"}},"agentOnly":{"type":"array","description":"DIDs the agent records as hosted on this server, which the server does not have.","items":{"\$ref":"#/\$defs/AgentOnlyDid"}},"inBoth":{"type":"integer","minimum":0,"description":"How many the two agree on. Required, and required even when both arrays are empty: it is what distinguishes \'compared them, all matched\' from \'compared nothing\'. A consumer that rendered only the divergences would show an identical screen for a clean estate and for a listing that silently returned no rows."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
