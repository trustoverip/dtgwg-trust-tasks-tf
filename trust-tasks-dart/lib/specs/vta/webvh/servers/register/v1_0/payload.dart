// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vta/webvh/servers/register/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A hosting server the VTA is registered with. Carries no credential: what the VTA
/// holds to authenticate against the server is never part of this record and never
/// leaves it.
class WebvhServerRecord {
  const WebvhServerRecord({
    required this.id,
    required this.did,
    this.label,
    required this.createdAt,
    required this.updatedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory WebvhServerRecord.fromJson(Map<String, dynamic> json) =>
      WebvhServerRecord(
        id: json['id'] as String,
        did: json['did'] as String,
        label: json['label'] as String?,
        createdAt: json['createdAt'] as String,
        updatedAt: json['updatedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Local id for the server, chosen by the operator.
  final String id;

  /// The server's own DID, used to authenticate it.
  final String did;
  final String? label;
  final String createdAt;
  final String updatedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'did': did,
        if (label != null) 'label': label!,
        'createdAt': createdAt,
        'updatedAt': updatedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// Success response to vta/webvh/servers/register: the registration the VTA now holds.
/// Type https://trusttasks.org/spec/vta/webvh/servers/register/1.0#response.
typedef Response = WebvhServerRecord;

/// VTA WebVH Servers Register — payload
class Payload {
  const Payload({
    required this.id,
    this.did,
    this.label,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        did: json['did'] as String?,
        label: json['label'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Local id for the server. Referenced by every DID published through it.
  final String id;

  /// The server's DID, used to authenticate it. Absent where the VTA resolves it during
  /// registration.
  final String? did;
  final String? label;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (did != null) 'did': did!,
        if (label != null) 'label': label!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vta/webvh/servers/register/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vta/webvh/servers/register/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vta/webvh/servers/register/1.0","title":"VTA WebVH Servers Register — payload","type":"object","additionalProperties":false,"required":["id"],"properties":{"id":{"type":"string","minLength":1,"description":"Local id for the server. Referenced by every DID published through it."},"did":{"type":"string","description":"The server\'s DID, used to authenticate it. Absent where the VTA resolves it during registration."},"label":{"type":"string","maxLength":256},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH Servers Register — response payload","description":"Success response to vta/webvh/servers/register: the registration the VTA now holds. Type https://trusttasks.org/spec/vta/webvh/servers/register/1.0#response.","\$ref":"#/\$defs/WebvhServerRecord"},"WebvhServerRecord":{"title":"WebvhServerRecord","description":"A hosting server the VTA is registered with. Carries no credential: what the VTA holds to authenticate against the server is never part of this record and never leaves it.","type":"object","additionalProperties":false,"required":["id","did","createdAt","updatedAt"],"properties":{"id":{"type":"string","description":"Local id for the server, chosen by the operator."},"did":{"type":"string","description":"The server\'s own DID, used to authenticate it."},"label":{"type":"string","maxLength":256},"createdAt":{"type":"string","format":"date-time"},"updatedAt":{"type":"string","format":"date-time"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTA WebVH Servers Register — response payload","description":"Success response to vta/webvh/servers/register: the registration the VTA now holds. Type https://trusttasks.org/spec/vta/webvh/servers/register/1.0#response.","\$ref":"#/\$defs/WebvhServerRecord"},"WebvhServerRecord":{"title":"WebvhServerRecord","description":"A hosting server the VTA is registered with. Carries no credential: what the VTA holds to authenticate against the server is never part of this record and never leaves it.","type":"object","additionalProperties":false,"required":["id","did","createdAt","updatedAt"],"properties":{"id":{"type":"string","description":"Local id for the server, chosen by the operator."},"did":{"type":"string","description":"The server\'s own DID, used to authenticate it."},"label":{"type":"string","maxLength":256},"createdAt":{"type":"string","format":"date-time"},"updatedAt":{"type":"string","format":"date-time"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
