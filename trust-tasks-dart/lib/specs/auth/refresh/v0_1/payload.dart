// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/refresh/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A logical authentication context bound to a subject. Producers and consumers
/// exchange Session-shaped data in challenge issuance, authentication responses, and
/// introspection (whoami).
class Session {
  const Session({
    required this.id,
    required this.subject,
    required this.issuedAt,
    required this.expiresAt,
    this.amr,
    this.acr,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Session.fromJson(Map<String, dynamic> json) => Session(
        id: json['id'] as String,
        subject: json['subject'] as String,
        issuedAt: json['issuedAt'] as String,
        expiresAt: json['expiresAt'] as String,
        amr: json['amr'] == null
            ? null
            : (json['amr'] as List<dynamic>).map((e) => e as String).toList(),
        acr: json['acr'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque, server-chosen session identifier. Stable for the lifetime of the session.
  /// Consumers MUST treat the value as opaque; no structure is implied.
  final String id;

  /// The authenticated party's VID (typically a DID URL).
  final String subject;

  /// ISO-8601 timestamp when the session was created.
  final String issuedAt;

  /// ISO-8601 timestamp when the session ceases to be valid. Producers SHOULD refresh
  /// before this time; consumers MUST reject after.
  final String expiresAt;

  /// Authentication Methods References per \[RFC 8176\]. Typical values: "did"
  /// (challenge-response), "passkey" (WebAuthn), "vta" (verifiable-trust agent
  /// approval). Multi-factor sessions list every method used.
  final List<String>? amr;

  /// Authentication Context Class Reference per \[OIDC Core §2\]. Profiles define their
  /// own values; the recommended set is "aal1" (single-factor DID auth), "aal2" (a
  /// second possession-or-biometric factor confirmed), and "aal3" (hardware-bound second
  /// factor).
  final String? acr;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'subject': subject,
        'issuedAt': issuedAt,
        'expiresAt': expiresAt,
        if (amr != null) 'amr': amr!,
        if (acr != null) 'acr': acr!,
        if (ext != null) 'ext': ext!,
      };
}

/// An access token (typically short-lived JWT) paired with an optional refresh token
/// (typically long-lived opaque string). The shapes follow OAuth 2.0 (RFC 6749 §5.1)
/// conventions but are not coupled to any particular OAuth profile.
class TokenBundle {
  const TokenBundle({
    required this.accessToken,
    this.refreshToken,
    required this.tokenType,
    required this.expiresIn,
    this.refreshExpiresIn,
    this.scope,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory TokenBundle.fromJson(Map<String, dynamic> json) => TokenBundle(
        accessToken: json['accessToken'] as String,
        refreshToken: json['refreshToken'] as String?,
        tokenType: json['tokenType'] as String,
        expiresIn: json['expiresIn'] as int,
        refreshExpiresIn: json['refreshExpiresIn'] as int?,
        scope: json['scope'] == null
            ? null
            : (json['scope'] as List<dynamic>).map((e) => e as String).toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Bearer-style access token. Consumers presenting this token to downstream services
  /// prove the holder of the original session. Format is consumer-defined — JWT is
  /// common, but opaque strings are also valid.
  final String accessToken;

  /// Long-lived token redeemable via auth/refresh for a new access token. MAY be absent
  /// when the issuer does not support refresh.
  final String? refreshToken;

  /// Token presentation scheme. Almost always "Bearer"; reserved for future schemes.
  final String tokenType;

  /// Seconds from issuance until the access token expires.
  final int expiresIn;

  /// Seconds from issuance until the refresh token expires, when one was issued.
  final int? refreshExpiresIn;

  /// Capability tags effective on this token. Format is consumer-defined; the framework
  /// imposes no syntax.
  final List<String>? scope;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'accessToken': accessToken,
        if (refreshToken != null) 'refreshToken': refreshToken!,
        'tokenType': tokenType,
        'expiresIn': expiresIn,
        if (refreshExpiresIn != null) 'refreshExpiresIn': refreshExpiresIn!,
        if (scope != null) 'scope': scope!,
        if (ext != null) 'ext': ext!,
      };
}

/// Issued by the auth service when the presented refresh token is valid. Carried in a
/// Trust Task document whose type is
/// https://trusttasks.org/spec/auth/refresh/0.1#response.
class Response {
  const Response({
    this.session,
    required this.tokens,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        session: json['session'] == null
            ? null
            : Session.fromJson(json['session'] as Map<String, dynamic>),
        tokens: TokenBundle.fromJson(json['tokens'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Optional session snapshot. Consumers SHOULD include it so the producer can refresh
  /// client-side state (acr changes after step-up, scope changes after policy edits)
  /// without a separate whoami call.
  final Session? session;

  /// New access token. If the consumer's policy rotates refresh tokens on each use, a
  /// new refreshToken is included; otherwise the producer continues using the
  /// previously-issued one.
  final TokenBundle tokens;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (session != null) 'session': session!.toJson(),
        'tokens': tokens.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Exchange a refresh token for a new access token (and optionally a new refresh
/// token).
class Payload {
  const Payload({
    required this.refreshToken,
    this.scope,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        refreshToken: json['refreshToken'] as String,
        scope: json['scope'] == null
            ? null
            : (json['scope'] as List<dynamic>).map((e) => e as String).toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The refresh token previously issued in a TokenBundle. Consumers verify it
  /// server-side; producers treat it as opaque.
  final String refreshToken;

  /// Optional capability tags requested on the new access token. MAY be narrower than
  /// the original session's scope; consumers MUST NOT widen scope on refresh.
  final List<String>? scope;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'refreshToken': refreshToken,
        if (scope != null) 'scope': scope!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/auth/refresh/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/auth/refresh/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/auth/refresh/0.1","title":"Auth — Refresh","description":"Exchange a refresh token for a new access token (and optionally a new refresh token).","type":"object","additionalProperties":false,"required":["refreshToken"],"properties":{"refreshToken":{"type":"string","minLength":1,"description":"The refresh token previously issued in a TokenBundle. Consumers verify it server-side; producers treat it as opaque."},"scope":{"type":"array","items":{"type":"string","minLength":1},"description":"Optional capability tags requested on the new access token. MAY be narrower than the original session\'s scope; consumers MUST NOT widen scope on refresh."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Auth Refresh — response payload","description":"Issued by the auth service when the presented refresh token is valid. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/refresh/0.1#response.","type":"object","additionalProperties":false,"required":["tokens"],"properties":{"session":{"\$ref":"#/\$defs/Session","description":"Optional session snapshot. Consumers SHOULD include it so the producer can refresh client-side state (acr changes after step-up, scope changes after policy edits) without a separate whoami call."},"tokens":{"\$ref":"#/\$defs/TokenBundle","description":"New access token. If the consumer\'s policy rotates refresh tokens on each use, a new refreshToken is included; otherwise the producer continues using the previously-issued one."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"TokenBundle":{"\$anchor":"tokenBundle","title":"TokenBundle","description":"An access token (typically short-lived JWT) paired with an optional refresh token (typically long-lived opaque string). The shapes follow OAuth 2.0 (RFC 6749 §5.1) conventions but are not coupled to any particular OAuth profile.","type":"object","additionalProperties":false,"required":["accessToken","tokenType","expiresIn"],"properties":{"accessToken":{"type":"string","minLength":1,"description":"Bearer-style access token. Consumers presenting this token to downstream services prove the holder of the original session. Format is consumer-defined — JWT is common, but opaque strings are also valid."},"refreshToken":{"type":"string","minLength":1,"description":"Long-lived token redeemable via auth/refresh for a new access token. MAY be absent when the issuer does not support refresh."},"tokenType":{"type":"string","minLength":1,"description":"Token presentation scheme. Almost always \\"Bearer\\"; reserved for future schemes."},"expiresIn":{"type":"integer","minimum":1,"description":"Seconds from issuance until the access token expires."},"refreshExpiresIn":{"type":"integer","minimum":1,"description":"Seconds from issuance until the refresh token expires, when one was issued."},"scope":{"type":"array","items":{"type":"string","minLength":1},"description":"Capability tags effective on this token. Format is consumer-defined; the framework imposes no syntax."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Session":{"\$anchor":"session","title":"Session","description":"A logical authentication context bound to a subject. Producers and consumers exchange Session-shaped data in challenge issuance, authentication responses, and introspection (whoami).","type":"object","additionalProperties":false,"required":["id","subject","issuedAt","expiresAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Opaque, server-chosen session identifier. Stable for the lifetime of the session. Consumers MUST treat the value as opaque; no structure is implied."},"subject":{"type":"string","minLength":1,"description":"The authenticated party\'s VID (typically a DID URL)."},"issuedAt":{"type":"string","format":"date-time","description":"ISO-8601 timestamp when the session was created."},"expiresAt":{"type":"string","format":"date-time","description":"ISO-8601 timestamp when the session ceases to be valid. Producers SHOULD refresh before this time; consumers MUST reject after."},"amr":{"type":"array","minItems":1,"items":{"type":"string","minLength":1},"description":"Authentication Methods References per [RFC 8176]. Typical values: \\"did\\" (challenge-response), \\"passkey\\" (WebAuthn), \\"vta\\" (verifiable-trust agent approval). Multi-factor sessions list every method used."},"acr":{"type":"string","description":"Authentication Context Class Reference per [OIDC Core §2]. Profiles define their own values; the recommended set is \\"aal1\\" (single-factor DID auth), \\"aal2\\" (a second possession-or-biometric factor confirmed), and \\"aal3\\" (hardware-bound second factor)."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Auth Refresh — response payload","description":"Issued by the auth service when the presented refresh token is valid. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/refresh/0.1#response.","type":"object","additionalProperties":false,"required":["tokens"],"properties":{"session":{"\$ref":"#/\$defs/Session","description":"Optional session snapshot. Consumers SHOULD include it so the producer can refresh client-side state (acr changes after step-up, scope changes after policy edits) without a separate whoami call."},"tokens":{"\$ref":"#/\$defs/TokenBundle","description":"New access token. If the consumer\'s policy rotates refresh tokens on each use, a new refreshToken is included; otherwise the producer continues using the previously-issued one."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"TokenBundle":{"\$anchor":"tokenBundle","title":"TokenBundle","description":"An access token (typically short-lived JWT) paired with an optional refresh token (typically long-lived opaque string). The shapes follow OAuth 2.0 (RFC 6749 §5.1) conventions but are not coupled to any particular OAuth profile.","type":"object","additionalProperties":false,"required":["accessToken","tokenType","expiresIn"],"properties":{"accessToken":{"type":"string","minLength":1,"description":"Bearer-style access token. Consumers presenting this token to downstream services prove the holder of the original session. Format is consumer-defined — JWT is common, but opaque strings are also valid."},"refreshToken":{"type":"string","minLength":1,"description":"Long-lived token redeemable via auth/refresh for a new access token. MAY be absent when the issuer does not support refresh."},"tokenType":{"type":"string","minLength":1,"description":"Token presentation scheme. Almost always \\"Bearer\\"; reserved for future schemes."},"expiresIn":{"type":"integer","minimum":1,"description":"Seconds from issuance until the access token expires."},"refreshExpiresIn":{"type":"integer","minimum":1,"description":"Seconds from issuance until the refresh token expires, when one was issued."},"scope":{"type":"array","items":{"type":"string","minLength":1},"description":"Capability tags effective on this token. Format is consumer-defined; the framework imposes no syntax."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Session":{"\$anchor":"session","title":"Session","description":"A logical authentication context bound to a subject. Producers and consumers exchange Session-shaped data in challenge issuance, authentication responses, and introspection (whoami).","type":"object","additionalProperties":false,"required":["id","subject","issuedAt","expiresAt"],"properties":{"id":{"type":"string","minLength":1,"description":"Opaque, server-chosen session identifier. Stable for the lifetime of the session. Consumers MUST treat the value as opaque; no structure is implied."},"subject":{"type":"string","minLength":1,"description":"The authenticated party\'s VID (typically a DID URL)."},"issuedAt":{"type":"string","format":"date-time","description":"ISO-8601 timestamp when the session was created."},"expiresAt":{"type":"string","format":"date-time","description":"ISO-8601 timestamp when the session ceases to be valid. Producers SHOULD refresh before this time; consumers MUST reject after."},"amr":{"type":"array","minItems":1,"items":{"type":"string","minLength":1},"description":"Authentication Methods References per [RFC 8176]. Typical values: \\"did\\" (challenge-response), \\"passkey\\" (WebAuthn), \\"vta\\" (verifiable-trust agent approval). Multi-factor sessions list every method used."},"acr":{"type":"string","description":"Authentication Context Class Reference per [OIDC Core §2]. Profiles define their own values; the recommended set is \\"aal1\\" (single-factor DID auth), \\"aal2\\" (a second possession-or-biometric factor confirmed), and \\"aal3\\" (hardware-bound second factor)."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
