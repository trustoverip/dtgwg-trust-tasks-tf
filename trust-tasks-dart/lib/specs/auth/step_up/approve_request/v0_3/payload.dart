// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/step-up/approve-request/0.3/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// `accepted` means the approver received the request and will return an
/// approve-response (typically via DIDComm). `refused` means it will not — `reason`
/// MUST be set.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus accepted = ResponseStatus('accepted');
  static const ResponseStatus refused = ResponseStatus('refused');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[
    accepted,
    refused
  ];
}

/// Synchronous ack from the approver acknowledging it received the request and will
/// (or will not) deliver an approve-response. Carried in a Trust Task document whose
/// type is https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response.
class Response {
  const Response({
    required this.status,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        status: ResponseStatus(json['status'] as String),
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// `accepted` means the approver received the request and will return an
  /// approve-response (typically via DIDComm). `refused` means it will not — `reason`
  /// MUST be set.
  final ResponseStatus status;

  /// Required when status is `refused`.
  final String? reason;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'status': status.value,
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
      };
}

/// PublicKeyCredentialDescriptor
class CredentialDescriptor {
  const CredentialDescriptor({
    required this.type,
    required this.id,
    this.transports,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialDescriptor.fromJson(Map<String, dynamic> json) =>
      CredentialDescriptor(
        type: json['type'],
        id: json['id'] as String,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>).map((e) => e).toList(),
      );

  final Object? type;

  /// base64url-encoded credential id.
  final String id;
  final List<Object?>? transports;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'id': id,
        if (transports != null) 'transports': transports!,
      };
}

/// Server-issued options for `navigator.credentials.get({ publicKey: ... })`.
class CredentialRequestOptions {
  const CredentialRequestOptions({
    this.extensions,
    required this.challenge,
    this.timeout,
    this.rpId,
    this.allowCredentials,
    this.userVerification,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialRequestOptions.fromJson(Map<String, dynamic> json) =>
      CredentialRequestOptions(
        extensions: json['extensions'] as Map<String, dynamic>?,
        challenge: json['challenge'] as String,
        timeout: json['timeout'] as int?,
        rpId: json['rpId'] as String?,
        allowCredentials: json['allowCredentials'] == null
            ? null
            : (json['allowCredentials'] as List<dynamic>)
                .map((e) =>
                    CredentialDescriptor.fromJson(e as Map<String, dynamic>))
                .toList(),
        userVerification: json['userVerification'],
      );

  /// Client extension inputs, per the WebAuthn Level 2
  /// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
  /// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it
  /// while closing the object with `additionalProperties: false` made the two claims
  /// contradict each other: a server emitting standard WebAuthn options could not
  /// conform, and the widely-used server libraries emit this member by default.
  /// Structure is deliberately unconstrained. The set of extensions is open and
  /// registered outside this framework, so enumerating them here would date the schema
  /// against a registry it does not own — and a closed list would reproduce the original
  /// defect one revision later.
  final Map<String, dynamic>? extensions;

  /// base64url-encoded one-time nonce.
  final String challenge;
  final int? timeout;
  final String? rpId;
  final List<CredentialDescriptor>? allowCredentials;
  final Object? userVerification;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (extensions != null) 'extensions': extensions!,
        'challenge': challenge,
        if (timeout != null) 'timeout': timeout!,
        if (rpId != null) 'rpId': rpId!,
        if (allowCredentials != null)
          'allowCredentials': allowCredentials!.map((e) => e.toJson()).toList(),
        if (userVerification != null) 'userVerification': userVerification!,
      };
}

/// PayloadAcceptableEvidenceItem is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadAcceptableEvidenceItem(String value) {
  static const PayloadAcceptableEvidenceItem didSigned =
      PayloadAcceptableEvidenceItem('didSigned');
  static const PayloadAcceptableEvidenceItem webauthn =
      PayloadAcceptableEvidenceItem('webauthn');

  /// Every value this specification's schema permits.
  static const List<PayloadAcceptableEvidenceItem> values =
      <PayloadAcceptableEvidenceItem>[didSigned, webauthn];
}

/// A relying party asks an approver (typically a wallet or a VTA) to ratify an AAL
/// elevation for a subject's session, or — bound to one operation — to approve that
/// one act without elevating any session.
class Payload {
  const Payload({
    required this.subject,
    this.sessionId,
    this.boundTo,
    required this.challenge,
    required this.reason,
    this.targetAcr,
    this.acceptableEvidence,
    this.webauthn,
    this.ttl,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subject: json['subject'] as String,
        sessionId: json['sessionId'] as String?,
        boundTo: json['boundTo'] as String?,
        challenge: json['challenge'] as String,
        reason: json['reason'] as String,
        targetAcr: json['targetAcr'] as String?,
        acceptableEvidence: json['acceptableEvidence'] == null
            ? null
            : (json['acceptableEvidence'] as List<dynamic>)
                .map((e) => PayloadAcceptableEvidenceItem(e as String))
                .toList(),
        webauthn: json['webauthn'] == null
            ? null
            : CredentialRequestOptions.fromJson(
                json['webauthn'] as Map<String, dynamic>),
        ttl: json['ttl'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The VID whose session is being elevated, or — for a step-up bound to one operation
  /// — the VID whose act that operation is. The approver MUST verify this is a VID it
  /// can speak for.
  final String subject;

  /// The session the relying party wants elevated. Opaque to the approver. Absent only
  /// when the step-up is bound to one operation that arrived without a session (a signed
  /// Trust Task document, for instance); see `boundTo`.
  final String? sessionId;

  /// Present when the step-up authorizes exactly one operation rather than raising a
  /// session's assurance level. An opaque relying-party identifier for that operation —
  /// for an operation that arrived as a signed document, a digest of its type and
  /// payload salted with `challenge`. Echoed by a `recorded` approve-response
  /// acknowledgement. Not a capability: the binding is the relying party's own state.
  final String? boundTo;

  /// base64url-encoded nonce the approver will include in the approve-response
  /// signature. ≥128 bits entropy.
  final String challenge;

  /// Human-readable explanation of WHY the relying party is asking (e.g. "confirm
  /// transfer of 1000 USD to bob.example"). Surfaced to the user by the approver for
  /// consent. SHOULD be specific enough that a user can refuse intelligently.
  final String reason;

  /// The acr the relying party expects on the elevated session. Approvers MAY refuse if
  /// they cannot deliver this level.
  final String? targetAcr;

  /// Which approve-response evidence kinds the relying party will accept (see
  /// auth/step-up/approve-response `evidence`). When omitted, the approver MAY use any
  /// kind it supports. An approver that cannot satisfy any listed kind SHOULD refuse
  /// with `methodUnsupported`.
  final List<PayloadAcceptableEvidenceItem>? acceptableEvidence;

  /// Optional WebAuthn `PublicKeyCredentialRequestOptions` the approver passes to the
  /// platform passkey API when producing `webauthn` evidence. When present, its
  /// `challenge` MUST equal `payload.challenge` so the resulting assertion binds the
  /// same nonce the relying party bound server-side. `rpId`/`allowCredentials` identify
  /// which credential the approver should assert with.
  final CredentialRequestOptions? webauthn;

  /// Seconds within which the relying party expects the approve-response. Approvers
  /// SHOULD treat as advisory — the relying party's own expiry policy is authoritative.
  final int? ttl;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        if (sessionId != null) 'sessionId': sessionId!,
        if (boundTo != null) 'boundTo': boundTo!,
        'challenge': challenge,
        'reason': reason,
        if (targetAcr != null) 'targetAcr': targetAcr!,
        if (acceptableEvidence != null)
          'acceptableEvidence':
              acceptableEvidence!.map((e) => e.value).toList(),
        if (webauthn != null) 'webauthn': webauthn!.toJson(),
        if (ttl != null) 'ttl': ttl!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/auth/step-up/approve-request/0.3';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/auth/step-up/approve-request/0.3","title":"Auth — Step-up Approve Request","description":"A relying party asks an approver (typically a wallet or a VTA) to ratify an AAL elevation for a subject\'s session, or — bound to one operation — to approve that one act without elevating any session.","type":"object","additionalProperties":false,"required":["subject","challenge","reason"],"properties":{"subject":{"type":"string","minLength":1,"description":"The VID whose session is being elevated, or — for a step-up bound to one operation — the VID whose act that operation is. The approver MUST verify this is a VID it can speak for."},"sessionId":{"type":"string","minLength":1,"description":"The session the relying party wants elevated. Opaque to the approver. Absent only when the step-up is bound to one operation that arrived without a session (a signed Trust Task document, for instance); see `boundTo`."},"boundTo":{"type":"string","minLength":1,"maxLength":256,"description":"Present when the step-up authorizes exactly one operation rather than raising a session\'s assurance level. An opaque relying-party identifier for that operation — for an operation that arrived as a signed document, a digest of its type and payload salted with `challenge`. Echoed by a `recorded` approve-response acknowledgement. Not a capability: the binding is the relying party\'s own state."},"challenge":{"type":"string","minLength":16,"description":"base64url-encoded nonce the approver will include in the approve-response signature. ≥128 bits entropy."},"reason":{"type":"string","minLength":1,"maxLength":500,"description":"Human-readable explanation of WHY the relying party is asking (e.g. \\"confirm transfer of 1000 USD to bob.example\\"). Surfaced to the user by the approver for consent. SHOULD be specific enough that a user can refuse intelligently."},"targetAcr":{"type":"string","description":"The acr the relying party expects on the elevated session. Approvers MAY refuse if they cannot deliver this level."},"acceptableEvidence":{"type":"array","minItems":1,"uniqueItems":true,"items":{"type":"string","enum":["didSigned","webauthn"]},"description":"Which approve-response evidence kinds the relying party will accept (see auth/step-up/approve-response `evidence`). When omitted, the approver MAY use any kind it supports. An approver that cannot satisfy any listed kind SHOULD refuse with `methodUnsupported`."},"webauthn":{"\$ref":"#/\$defs/CredentialRequestOptions","description":"Optional WebAuthn `PublicKeyCredentialRequestOptions` the approver passes to the platform passkey API when producing `webauthn` evidence. When present, its `challenge` MUST equal `payload.challenge` so the resulting assertion binds the same nonce the relying party bound server-side. `rpId`/`allowCredentials` identify which credential the approver should assert with."},"ttl":{"type":"integer","minimum":1,"description":"Seconds within which the relying party expects the approve-response. Approvers SHOULD treat as advisory — the relying party\'s own expiry policy is authoritative."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Auth Step-up Approve Request — response payload","description":"Synchronous ack from the approver acknowledging it received the request and will (or will not) deliver an approve-response. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response.","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["accepted","refused"],"description":"`accepted` means the approver received the request and will return an approve-response (typically via DIDComm). `refused` means it will not — `reason` MUST be set."},"reason":{"type":"string","maxLength":1024,"description":"Required when status is `refused`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialRequestOptions":{"\$anchor":"credentialRequestOptions","title":"PublicKeyCredentialRequestOptions","description":"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.","type":"object","additionalProperties":false,"required":["challenge"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"timeout":{"type":"integer","minimum":1},"rpId":{"type":"string","minLength":1},"allowCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"userVerification":{"enum":["discouraged","preferred","required"]}}},"CredentialDescriptor":{"\$anchor":"credentialDescriptor","title":"PublicKeyCredentialDescriptor","type":"object","additionalProperties":false,"required":["type","id"],"properties":{"type":{"const":"public-key"},"id":{"type":"string","description":"base64url-encoded credential id."},"transports":{"type":"array","items":{"enum":["usb","nfc","ble","internal","hybrid"]}}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Auth Step-up Approve Request — response payload","description":"Synchronous ack from the approver acknowledging it received the request and will (or will not) deliver an approve-response. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/step-up/approve-request/0.3#response.","type":"object","additionalProperties":false,"required":["status"],"properties":{"status":{"type":"string","enum":["accepted","refused"],"description":"`accepted` means the approver received the request and will return an approve-response (typically via DIDComm). `refused` means it will not — `reason` MUST be set."},"reason":{"type":"string","maxLength":1024,"description":"Required when status is `refused`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialRequestOptions":{"\$anchor":"credentialRequestOptions","title":"PublicKeyCredentialRequestOptions","description":"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.","type":"object","additionalProperties":false,"required":["challenge"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"timeout":{"type":"integer","minimum":1},"rpId":{"type":"string","minLength":1},"allowCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"userVerification":{"enum":["discouraged","preferred","required"]}}},"CredentialDescriptor":{"\$anchor":"credentialDescriptor","title":"PublicKeyCredentialDescriptor","type":"object","additionalProperties":false,"required":["type","id"],"properties":{"type":{"const":"public-key"},"id":{"type":"string","description":"base64url-encoded credential id."},"transports":{"type":"array","items":{"enum":["usb","nfc","ble","internal","hybrid"]}}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
