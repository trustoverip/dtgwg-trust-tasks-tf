// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/_shared/0.1/webauthn.schema.json

/// PublicKeyCredentialDescriptor
class CredentialDescriptor {
  const CredentialDescriptor({
    required this.type,
    required this.id,
    this.transports,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialDescriptor.fromJson(Map<String, dynamic> json) =>
      CredentialDescriptor(
        type: json['type'],
        id: json['id'] as String,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>).map((e) => e).toList(),
      );

  final Object? type;

  /// base64url-encoded credential id.
  final String id;
  final List<Object?>? transports;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'id': id,
        if (transports != null) 'transports': transports!,
      };
}

/// CredentialCreationOptionsRp, generated from its schema.
class CredentialCreationOptionsRp {
  const CredentialCreationOptionsRp({
    required this.id,
    required this.name,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsRp.fromJson(Map<String, dynamic> json) =>
      CredentialCreationOptionsRp(
        id: json['id'] as String,
        name: json['name'] as String,
      );

  final String id;
  final String name;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
      };
}

/// CredentialCreationOptionsUser, generated from its schema.
class CredentialCreationOptionsUser {
  const CredentialCreationOptionsUser({
    required this.id,
    required this.name,
    required this.displayName,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsUser.fromJson(Map<String, dynamic> json) =>
      CredentialCreationOptionsUser(
        id: json['id'] as String,
        name: json['name'] as String,
        displayName: json['displayName'] as String,
      );

  /// base64url-encoded user handle. SHOULD be a stable opaque identifier — never reuse a
  /// human-readable username here.
  final String id;
  final String name;
  final String displayName;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
        'displayName': displayName,
      };
}

/// CredentialCreationOptionsPubKeyCredParamsItem, generated from its schema.
class CredentialCreationOptionsPubKeyCredParamsItem {
  const CredentialCreationOptionsPubKeyCredParamsItem({
    required this.type,
    required this.alg,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsPubKeyCredParamsItem.fromJson(
          Map<String, dynamic> json) =>
      CredentialCreationOptionsPubKeyCredParamsItem(
        type: json['type'],
        alg: json['alg'] as int,
      );

  final Object? type;

  /// COSE algorithm identifier (e.g. -8 Ed25519, -7 ES256, -257 RS256).
  final int alg;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'alg': alg,
      };
}

/// CredentialCreationOptionsAuthenticatorSelection, generated from its schema.
class CredentialCreationOptionsAuthenticatorSelection {
  const CredentialCreationOptionsAuthenticatorSelection({
    this.authenticatorAttachment,
    this.requireResidentKey,
    this.residentKey,
    this.userVerification,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsAuthenticatorSelection.fromJson(
          Map<String, dynamic> json) =>
      CredentialCreationOptionsAuthenticatorSelection(
        authenticatorAttachment: json['authenticatorAttachment'],
        requireResidentKey: json['requireResidentKey'] as bool?,
        residentKey: json['residentKey'],
        userVerification: json['userVerification'],
      );

  final Object? authenticatorAttachment;
  final bool? requireResidentKey;
  final Object? residentKey;
  final Object? userVerification;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (authenticatorAttachment != null)
          'authenticatorAttachment': authenticatorAttachment!,
        if (requireResidentKey != null)
          'requireResidentKey': requireResidentKey!,
        if (residentKey != null) 'residentKey': residentKey!,
        if (userVerification != null) 'userVerification': userVerification!,
      };
}

/// Server-issued options for `navigator.credentials.create({ publicKey: ... })`.
/// Mirrors the WebAuthn Level 2 `PublicKeyCredentialCreationOptions` dictionary;
/// binary fields are base64url-encoded strings (rather than ArrayBuffers) so the value
/// is JSON-safe over the wire.
class CredentialCreationOptions {
  const CredentialCreationOptions({
    this.extensions,
    required this.challenge,
    required this.rp,
    required this.user,
    required this.pubKeyCredParams,
    this.timeout,
    this.excludeCredentials,
    this.authenticatorSelection,
    this.attestation,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptions.fromJson(Map<String, dynamic> json) =>
      CredentialCreationOptions(
        extensions: json['extensions'] as Map<String, dynamic>?,
        challenge: json['challenge'] as String,
        rp: CredentialCreationOptionsRp.fromJson(
            json['rp'] as Map<String, dynamic>),
        user: CredentialCreationOptionsUser.fromJson(
            json['user'] as Map<String, dynamic>),
        pubKeyCredParams: (json['pubKeyCredParams'] as List<dynamic>)
            .map((e) => CredentialCreationOptionsPubKeyCredParamsItem.fromJson(
                e as Map<String, dynamic>))
            .toList(),
        timeout: json['timeout'] as int?,
        excludeCredentials: json['excludeCredentials'] == null
            ? null
            : (json['excludeCredentials'] as List<dynamic>)
                .map((e) =>
                    CredentialDescriptor.fromJson(e as Map<String, dynamic>))
                .toList(),
        authenticatorSelection: json['authenticatorSelection'] == null
            ? null
            : CredentialCreationOptionsAuthenticatorSelection.fromJson(
                json['authenticatorSelection'] as Map<String, dynamic>),
        attestation: json['attestation'],
      );

  /// Client extension inputs, per the WebAuthn Level 2
  /// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
  /// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it
  /// while closing the object with `additionalProperties: false` made the two claims
  /// contradict each other: a server emitting standard WebAuthn options could not
  /// conform, and the widely-used server libraries emit this member by default.
  /// Structure is deliberately unconstrained. The set of extensions is open and
  /// registered outside this framework, so enumerating them here would date the schema
  /// against a registry it does not own — and a closed list would reproduce the original
  /// defect one revision later.
  final Map<String, dynamic>? extensions;

  /// base64url-encoded one-time nonce.
  final String challenge;
  final CredentialCreationOptionsRp rp;
  final CredentialCreationOptionsUser user;
  final List<CredentialCreationOptionsPubKeyCredParamsItem> pubKeyCredParams;
  final int? timeout;
  final List<CredentialDescriptor>? excludeCredentials;
  final CredentialCreationOptionsAuthenticatorSelection? authenticatorSelection;
  final Object? attestation;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (extensions != null) 'extensions': extensions!,
        'challenge': challenge,
        'rp': rp.toJson(),
        'user': user.toJson(),
        'pubKeyCredParams': pubKeyCredParams.map((e) => e.toJson()).toList(),
        if (timeout != null) 'timeout': timeout!,
        if (excludeCredentials != null)
          'excludeCredentials':
              excludeCredentials!.map((e) => e.toJson()).toList(),
        if (authenticatorSelection != null)
          'authenticatorSelection': authenticatorSelection!.toJson(),
        if (attestation != null) 'attestation': attestation!,
      };
}

/// Server-issued options for `navigator.credentials.get({ publicKey: ... })`.
class CredentialRequestOptions {
  const CredentialRequestOptions({
    this.extensions,
    required this.challenge,
    this.timeout,
    this.rpId,
    this.allowCredentials,
    this.userVerification,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialRequestOptions.fromJson(Map<String, dynamic> json) =>
      CredentialRequestOptions(
        extensions: json['extensions'] as Map<String, dynamic>?,
        challenge: json['challenge'] as String,
        timeout: json['timeout'] as int?,
        rpId: json['rpId'] as String?,
        allowCredentials: json['allowCredentials'] == null
            ? null
            : (json['allowCredentials'] as List<dynamic>)
                .map((e) =>
                    CredentialDescriptor.fromJson(e as Map<String, dynamic>))
                .toList(),
        userVerification: json['userVerification'],
      );

  /// Client extension inputs, per the WebAuthn Level 2
  /// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
  /// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it
  /// while closing the object with `additionalProperties: false` made the two claims
  /// contradict each other: a server emitting standard WebAuthn options could not
  /// conform, and the widely-used server libraries emit this member by default.
  /// Structure is deliberately unconstrained. The set of extensions is open and
  /// registered outside this framework, so enumerating them here would date the schema
  /// against a registry it does not own — and a closed list would reproduce the original
  /// defect one revision later.
  final Map<String, dynamic>? extensions;

  /// base64url-encoded one-time nonce.
  final String challenge;
  final int? timeout;
  final String? rpId;
  final List<CredentialDescriptor>? allowCredentials;
  final Object? userVerification;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (extensions != null) 'extensions': extensions!,
        'challenge': challenge,
        if (timeout != null) 'timeout': timeout!,
        if (rpId != null) 'rpId': rpId!,
        if (allowCredentials != null)
          'allowCredentials': allowCredentials!.map((e) => e.toJson()).toList(),
        if (userVerification != null) 'userVerification': userVerification!,
      };
}

/// A passkey already bound to a subject, as listed by `auth/passkey/list` and targeted
/// by `auth/passkey/revoke/*`. This is the auth service's own management view of a
/// credential — not a WebAuthn dictionary — so its members are camelCase per SPEC.md
/// §4.10. The exception is `transports`, whose values are the externally-owned
/// WebAuthn transport tokens and are carried verbatim.
class RegisteredCredential {
  const RegisteredCredential({
    required this.credentialId,
    this.deviceLabel,
    this.transports,
    required this.registeredAt,
    this.lastUsedAt,
  });

  /// Read this payload from a decoded JSON object.
  factory RegisteredCredential.fromJson(Map<String, dynamic> json) =>
      RegisteredCredential(
        credentialId: json['credentialId'] as String,
        deviceLabel: json['deviceLabel'] as String?,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        registeredAt: json['registeredAt'] as String,
        lastUsedAt: json['lastUsedAt'] as String?,
      );

  /// Durable identifier for this credential, as issued by `auth/passkey/enroll/finish`:
  /// the base64url-encoded WebAuthn credential id. Opaque to the producer — it MUST be
  /// echoed verbatim when revoking and MUST NOT be parsed.
  final String credentialId;

  /// Operator-facing label captured at enrollment (e.g. "Alice's MacBook Pro"). Absent
  /// when the subject enrolled without one. Consumers MUST NOT synthesize a label,
  /// because an invented one is indistinguishable from a chosen one to somebody deciding
  /// which credential to revoke.
  final String? deviceLabel;

  /// WebAuthn transport hints reported by the authenticator at enrollment (`usb`, `nfc`,
  /// `ble`, `internal`, `hybrid`, …). Externally owned and carried verbatim.
  /// Deliberately not an enum — unlike `CredentialDescriptor.transports`, which the
  /// consumer itself emits, these values were reported by a third-party authenticator,
  /// the WebAuthn transport registry grows independently of this specification, and an
  /// unrecognized token here is a display concern rather than a validation failure.
  final List<String>? transports;

  /// When the credential was bound to the subject.
  final String registeredAt;

  /// When this credential last completed an assertion. Absent if it never has, or if the
  /// consumer does not track usage. Those two cases are deliberately NOT distinguished:
  /// a nullable variant cannot survive the round-trip through generated bindings, which
  /// map absent and null onto the same optional, so a distinction stated here would not
  /// be one any conforming implementation could rely on. Consumers that need to
  /// advertise "usage is not tracked" SHOULD say so under `ext`.
  final String? lastUsedAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentialId': credentialId,
        if (deviceLabel != null) 'deviceLabel': deviceLabel!,
        if (transports != null) 'transports': transports!,
        'registeredAt': registeredAt,
        if (lastUsedAt != null) 'lastUsedAt': lastUsedAt!,
      };
}

/// AttestationResponseResponse, generated from its schema.
class AttestationResponseResponse {
  const AttestationResponseResponse({
    required this.clientDataJson,
    required this.attestationObject,
    this.transports,
  });

  /// Read this payload from a decoded JSON object.
  factory AttestationResponseResponse.fromJson(Map<String, dynamic> json) =>
      AttestationResponseResponse(
        clientDataJson: json['clientDataJSON'] as String,
        attestationObject: json['attestationObject'] as String,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
      );

  final String clientDataJson;
  final String attestationObject;
  final List<String>? transports;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'clientDataJSON': clientDataJson,
        'attestationObject': attestationObject,
        if (transports != null) 'transports': transports!,
      };
}

/// The credential the client returns from `navigator.credentials.create`. Binary
/// fields are base64url-encoded.
class AttestationResponse {
  const AttestationResponse({
    required this.id,
    required this.rawId,
    required this.type,
    required this.response,
    this.authenticatorAttachment,
    this.clientExtensionResults,
  });

  /// Read this payload from a decoded JSON object.
  factory AttestationResponse.fromJson(Map<String, dynamic> json) =>
      AttestationResponse(
        id: json['id'] as String,
        rawId: json['rawId'] as String,
        type: json['type'],
        response: AttestationResponseResponse.fromJson(
            json['response'] as Map<String, dynamic>),
        authenticatorAttachment: json['authenticatorAttachment'],
        clientExtensionResults:
            json['clientExtensionResults'] as Map<String, dynamic>?,
      );

  final String id;
  final String rawId;
  final Object? type;
  final AttestationResponseResponse response;
  final Object? authenticatorAttachment;
  final Map<String, dynamic>? clientExtensionResults;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'rawId': rawId,
        'type': type,
        'response': response.toJson(),
        if (authenticatorAttachment != null)
          'authenticatorAttachment': authenticatorAttachment!,
        if (clientExtensionResults != null)
          'clientExtensionResults': clientExtensionResults!,
      };
}

/// AssertionResponseResponse, generated from its schema.
class AssertionResponseResponse {
  const AssertionResponseResponse({
    required this.clientDataJson,
    required this.authenticatorData,
    required this.signature,
    this.userHandle,
  });

  /// Read this payload from a decoded JSON object.
  factory AssertionResponseResponse.fromJson(Map<String, dynamic> json) =>
      AssertionResponseResponse(
        clientDataJson: json['clientDataJSON'] as String,
        authenticatorData: json['authenticatorData'] as String,
        signature: json['signature'] as String,
        userHandle: json['userHandle'] as String?,
      );

  final String clientDataJson;
  final String authenticatorData;
  final String signature;
  final String? userHandle;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'clientDataJSON': clientDataJson,
        'authenticatorData': authenticatorData,
        'signature': signature,
        if (userHandle != null) 'userHandle': userHandle!,
      };
}

/// The credential the client returns from `navigator.credentials.get`. Binary fields
/// are base64url-encoded.
class AssertionResponse {
  const AssertionResponse({
    required this.id,
    required this.rawId,
    required this.type,
    required this.response,
    this.authenticatorAttachment,
    this.clientExtensionResults,
  });

  /// Read this payload from a decoded JSON object.
  factory AssertionResponse.fromJson(Map<String, dynamic> json) =>
      AssertionResponse(
        id: json['id'] as String,
        rawId: json['rawId'] as String,
        type: json['type'],
        response: AssertionResponseResponse.fromJson(
            json['response'] as Map<String, dynamic>),
        authenticatorAttachment: json['authenticatorAttachment'],
        clientExtensionResults:
            json['clientExtensionResults'] as Map<String, dynamic>?,
      );

  final String id;
  final String rawId;
  final Object? type;
  final AssertionResponseResponse response;
  final Object? authenticatorAttachment;
  final Map<String, dynamic>? clientExtensionResults;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'rawId': rawId,
        'type': type,
        'response': response.toJson(),
        if (authenticatorAttachment != null)
          'authenticatorAttachment': authenticatorAttachment!,
        if (clientExtensionResults != null)
          'clientExtensionResults': clientExtensionResults!,
      };
}
