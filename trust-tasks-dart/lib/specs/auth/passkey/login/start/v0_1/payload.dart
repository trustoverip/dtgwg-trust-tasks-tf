// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/passkey/login/start/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// PublicKeyCredentialDescriptor
class CredentialDescriptor {
  const CredentialDescriptor({
    required this.type,
    required this.id,
    this.transports,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialDescriptor.fromJson(Map<String, dynamic> json) =>
      CredentialDescriptor(
        type: json['type'],
        id: json['id'] as String,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>).map((e) => e).toList(),
      );

  final Object? type;

  /// base64url-encoded credential id.
  final String id;
  final List<Object?>? transports;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'id': id,
        if (transports != null) 'transports': transports!,
      };
}

/// Server-issued options for `navigator.credentials.get({ publicKey: ... })`.
class CredentialRequestOptions {
  const CredentialRequestOptions({
    this.extensions,
    required this.challenge,
    this.timeout,
    this.rpId,
    this.allowCredentials,
    this.userVerification,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialRequestOptions.fromJson(Map<String, dynamic> json) =>
      CredentialRequestOptions(
        extensions: json['extensions'] as Map<String, dynamic>?,
        challenge: json['challenge'] as String,
        timeout: json['timeout'] as int?,
        rpId: json['rpId'] as String?,
        allowCredentials: json['allowCredentials'] == null
            ? null
            : (json['allowCredentials'] as List<dynamic>)
                .map((e) =>
                    CredentialDescriptor.fromJson(e as Map<String, dynamic>))
                .toList(),
        userVerification: json['userVerification'],
      );

  /// Client extension inputs, per the WebAuthn Level 2
  /// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
  /// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it
  /// while closing the object with `additionalProperties: false` made the two claims
  /// contradict each other: a server emitting standard WebAuthn options could not
  /// conform, and the widely-used server libraries emit this member by default.
  /// Structure is deliberately unconstrained. The set of extensions is open and
  /// registered outside this framework, so enumerating them here would date the schema
  /// against a registry it does not own — and a closed list would reproduce the original
  /// defect one revision later.
  final Map<String, dynamic>? extensions;

  /// base64url-encoded one-time nonce.
  final String challenge;
  final int? timeout;
  final String? rpId;
  final List<CredentialDescriptor>? allowCredentials;
  final Object? userVerification;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (extensions != null) 'extensions': extensions!,
        'challenge': challenge,
        if (timeout != null) 'timeout': timeout!,
        if (rpId != null) 'rpId': rpId!,
        if (allowCredentials != null)
          'allowCredentials': allowCredentials!.map((e) => e.toJson()).toList(),
        if (userVerification != null) 'userVerification': userVerification!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Server-issued WebAuthn request options. Carried in a Trust Task document whose type
/// is https://trusttasks.org/spec/auth/passkey/login/start/0.1#response.
class Response {
  const Response({
    required this.authId,
    required this.options,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        authId: json['authId'] as String,
        options: CredentialRequestOptions.fromJson(
            json['options'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque server handle correlating this start with the matching finish.
  final String authId;

  /// PublicKeyCredentialRequestOptions for navigator.credentials.get.
  final CredentialRequestOptions options;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'authId': authId,
        'options': options.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Producer-declared intent. `login` issues a new session; `step-up` elevates an
/// existing session's `acr`. The consumer's behaviour on the matching finish differs
/// accordingly.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadPurpose(String value) {
  static const PayloadPurpose login = PayloadPurpose('login');
  static const PayloadPurpose stepUp = PayloadPurpose('step-up');

  /// Every value this specification's schema permits.
  static const List<PayloadPurpose> values = <PayloadPurpose>[login, stepUp];
}

/// Ask the auth service to begin a WebAuthn authentication ceremony. The response
/// carries PublicKeyCredentialRequestOptions for `navigator.credentials.get`.
class Payload {
  const Payload({
    this.subject,
    this.purpose,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subject: json['subject'] as String?,
        purpose: json['purpose'] == null
            ? null
            : PayloadPurpose(json['purpose'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The VID the producer intends to authenticate as. Optional — omit for usernameless /
  /// discoverable-credential flows where any registered passkey may answer.
  final String? subject;

  /// Producer-declared intent. `login` issues a new session; `step-up` elevates an
  /// existing session's `acr`. The consumer's behaviour on the matching finish differs
  /// accordingly.
  final PayloadPurpose? purpose;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (subject != null) 'subject': subject!,
        if (purpose != null) 'purpose': purpose!.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/auth/passkey/login/start/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/auth/passkey/login/start/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/auth/passkey/login/start/0.1","title":"Auth — Passkey Login (start)","description":"Ask the auth service to begin a WebAuthn authentication ceremony. The response carries PublicKeyCredentialRequestOptions for `navigator.credentials.get`.","type":"object","additionalProperties":false,"properties":{"subject":{"type":"string","minLength":1,"description":"The VID the producer intends to authenticate as. Optional — omit for usernameless / discoverable-credential flows where any registered passkey may answer."},"purpose":{"type":"string","enum":["login","step-up"],"description":"Producer-declared intent. `login` issues a new session; `step-up` elevates an existing session\'s `acr`. The consumer\'s behaviour on the matching finish differs accordingly."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Auth Passkey Login Start — response payload","description":"Server-issued WebAuthn request options. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/login/start/0.1#response.","type":"object","additionalProperties":false,"required":["authId","options"],"properties":{"authId":{"type":"string","minLength":1,"description":"Opaque server handle correlating this start with the matching finish."},"options":{"\$ref":"#/\$defs/CredentialRequestOptions","description":"PublicKeyCredentialRequestOptions for navigator.credentials.get."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialRequestOptions":{"\$anchor":"credentialRequestOptions","title":"PublicKeyCredentialRequestOptions","description":"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.","type":"object","additionalProperties":false,"required":["challenge"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"timeout":{"type":"integer","minimum":1},"rpId":{"type":"string","minLength":1},"allowCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"userVerification":{"enum":["discouraged","preferred","required"]}}},"CredentialDescriptor":{"\$anchor":"credentialDescriptor","title":"PublicKeyCredentialDescriptor","type":"object","additionalProperties":false,"required":["type","id"],"properties":{"type":{"const":"public-key"},"id":{"type":"string","description":"base64url-encoded credential id."},"transports":{"type":"array","items":{"enum":["usb","nfc","ble","internal","hybrid"]}}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Auth Passkey Login Start — response payload","description":"Server-issued WebAuthn request options. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/login/start/0.1#response.","type":"object","additionalProperties":false,"required":["authId","options"],"properties":{"authId":{"type":"string","minLength":1,"description":"Opaque server handle correlating this start with the matching finish."},"options":{"\$ref":"#/\$defs/CredentialRequestOptions","description":"PublicKeyCredentialRequestOptions for navigator.credentials.get."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialRequestOptions":{"\$anchor":"credentialRequestOptions","title":"PublicKeyCredentialRequestOptions","description":"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.","type":"object","additionalProperties":false,"required":["challenge"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"timeout":{"type":"integer","minimum":1},"rpId":{"type":"string","minLength":1},"allowCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"userVerification":{"enum":["discouraged","preferred","required"]}}},"CredentialDescriptor":{"\$anchor":"credentialDescriptor","title":"PublicKeyCredentialDescriptor","type":"object","additionalProperties":false,"required":["type","id"],"properties":{"type":{"const":"public-key"},"id":{"type":"string","description":"base64url-encoded credential id."},"transports":{"type":"array","items":{"enum":["usb","nfc","ble","internal","hybrid"]}}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
