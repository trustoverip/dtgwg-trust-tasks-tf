// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/passkey/enroll/redeem/start/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// PublicKeyCredentialDescriptor
class CredentialDescriptor {
  const CredentialDescriptor({
    required this.type,
    required this.id,
    this.transports,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialDescriptor.fromJson(Map<String, dynamic> json) =>
      CredentialDescriptor(
        type: json['type'],
        id: json['id'] as String,
        transports: json['transports'] == null
            ? null
            : (json['transports'] as List<dynamic>).map((e) => e).toList(),
      );

  final Object? type;

  /// base64url-encoded credential id.
  final String id;
  final List<Object?>? transports;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'id': id,
        if (transports != null) 'transports': transports!,
      };
}

/// CredentialCreationOptionsRp, generated from its schema.
class CredentialCreationOptionsRp {
  const CredentialCreationOptionsRp({
    required this.id,
    required this.name,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsRp.fromJson(Map<String, dynamic> json) =>
      CredentialCreationOptionsRp(
        id: json['id'] as String,
        name: json['name'] as String,
      );

  final String id;
  final String name;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
      };
}

/// CredentialCreationOptionsUser, generated from its schema.
class CredentialCreationOptionsUser {
  const CredentialCreationOptionsUser({
    required this.id,
    required this.name,
    required this.displayName,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsUser.fromJson(Map<String, dynamic> json) =>
      CredentialCreationOptionsUser(
        id: json['id'] as String,
        name: json['name'] as String,
        displayName: json['displayName'] as String,
      );

  /// base64url-encoded user handle. SHOULD be a stable opaque identifier — never reuse a
  /// human-readable username here.
  final String id;
  final String name;
  final String displayName;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'name': name,
        'displayName': displayName,
      };
}

/// CredentialCreationOptionsPubKeyCredParamsItem, generated from its schema.
class CredentialCreationOptionsPubKeyCredParamsItem {
  const CredentialCreationOptionsPubKeyCredParamsItem({
    required this.type,
    required this.alg,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsPubKeyCredParamsItem.fromJson(
          Map<String, dynamic> json) =>
      CredentialCreationOptionsPubKeyCredParamsItem(
        type: json['type'],
        alg: json['alg'] as int,
      );

  final Object? type;

  /// COSE algorithm identifier (e.g. -8 Ed25519, -7 ES256, -257 RS256).
  final int alg;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'alg': alg,
      };
}

/// CredentialCreationOptionsAuthenticatorSelection, generated from its schema.
class CredentialCreationOptionsAuthenticatorSelection {
  const CredentialCreationOptionsAuthenticatorSelection({
    this.authenticatorAttachment,
    this.requireResidentKey,
    this.residentKey,
    this.userVerification,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptionsAuthenticatorSelection.fromJson(
          Map<String, dynamic> json) =>
      CredentialCreationOptionsAuthenticatorSelection(
        authenticatorAttachment: json['authenticatorAttachment'],
        requireResidentKey: json['requireResidentKey'] as bool?,
        residentKey: json['residentKey'],
        userVerification: json['userVerification'],
      );

  final Object? authenticatorAttachment;
  final bool? requireResidentKey;
  final Object? residentKey;
  final Object? userVerification;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (authenticatorAttachment != null)
          'authenticatorAttachment': authenticatorAttachment!,
        if (requireResidentKey != null)
          'requireResidentKey': requireResidentKey!,
        if (residentKey != null) 'residentKey': residentKey!,
        if (userVerification != null) 'userVerification': userVerification!,
      };
}

/// Server-issued options for `navigator.credentials.create({ publicKey: ... })`.
/// Mirrors the WebAuthn Level 2 `PublicKeyCredentialCreationOptions` dictionary;
/// binary fields are base64url-encoded strings (rather than ArrayBuffers) so the value
/// is JSON-safe over the wire.
class CredentialCreationOptions {
  const CredentialCreationOptions({
    this.extensions,
    required this.challenge,
    required this.rp,
    required this.user,
    required this.pubKeyCredParams,
    this.timeout,
    this.excludeCredentials,
    this.authenticatorSelection,
    this.attestation,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialCreationOptions.fromJson(Map<String, dynamic> json) =>
      CredentialCreationOptions(
        extensions: json['extensions'] as Map<String, dynamic>?,
        challenge: json['challenge'] as String,
        rp: CredentialCreationOptionsRp.fromJson(
            json['rp'] as Map<String, dynamic>),
        user: CredentialCreationOptionsUser.fromJson(
            json['user'] as Map<String, dynamic>),
        pubKeyCredParams: (json['pubKeyCredParams'] as List<dynamic>)
            .map((e) => CredentialCreationOptionsPubKeyCredParamsItem.fromJson(
                e as Map<String, dynamic>))
            .toList(),
        timeout: json['timeout'] as int?,
        excludeCredentials: json['excludeCredentials'] == null
            ? null
            : (json['excludeCredentials'] as List<dynamic>)
                .map((e) =>
                    CredentialDescriptor.fromJson(e as Map<String, dynamic>))
                .toList(),
        authenticatorSelection: json['authenticatorSelection'] == null
            ? null
            : CredentialCreationOptionsAuthenticatorSelection.fromJson(
                json['authenticatorSelection'] as Map<String, dynamic>),
        attestation: json['attestation'],
      );

  /// Client extension inputs, per the WebAuthn Level 2
  /// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
  /// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it
  /// while closing the object with `additionalProperties: false` made the two claims
  /// contradict each other: a server emitting standard WebAuthn options could not
  /// conform, and the widely-used server libraries emit this member by default.
  /// Structure is deliberately unconstrained. The set of extensions is open and
  /// registered outside this framework, so enumerating them here would date the schema
  /// against a registry it does not own — and a closed list would reproduce the original
  /// defect one revision later.
  final Map<String, dynamic>? extensions;

  /// base64url-encoded one-time nonce.
  final String challenge;
  final CredentialCreationOptionsRp rp;
  final CredentialCreationOptionsUser user;
  final List<CredentialCreationOptionsPubKeyCredParamsItem> pubKeyCredParams;
  final int? timeout;
  final List<CredentialDescriptor>? excludeCredentials;
  final CredentialCreationOptionsAuthenticatorSelection? authenticatorSelection;
  final Object? attestation;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (extensions != null) 'extensions': extensions!,
        'challenge': challenge,
        'rp': rp.toJson(),
        'user': user.toJson(),
        'pubKeyCredParams': pubKeyCredParams.map((e) => e.toJson()).toList(),
        if (timeout != null) 'timeout': timeout!,
        if (excludeCredentials != null)
          'excludeCredentials':
              excludeCredentials!.map((e) => e.toJson()).toList(),
        if (authenticatorSelection != null)
          'authenticatorSelection': authenticatorSelection!.toJson(),
        if (attestation != null) 'attestation': attestation!,
      };
}

/// Server-issued options for `navigator.credentials.get({ publicKey: ... })`.
class CredentialRequestOptions {
  const CredentialRequestOptions({
    this.extensions,
    required this.challenge,
    this.timeout,
    this.rpId,
    this.allowCredentials,
    this.userVerification,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialRequestOptions.fromJson(Map<String, dynamic> json) =>
      CredentialRequestOptions(
        extensions: json['extensions'] as Map<String, dynamic>?,
        challenge: json['challenge'] as String,
        timeout: json['timeout'] as int?,
        rpId: json['rpId'] as String?,
        allowCredentials: json['allowCredentials'] == null
            ? null
            : (json['allowCredentials'] as List<dynamic>)
                .map((e) =>
                    CredentialDescriptor.fromJson(e as Map<String, dynamic>))
                .toList(),
        userVerification: json['userVerification'],
      );

  /// Client extension inputs, per the WebAuthn Level 2
  /// `AuthenticationExtensionsClientInputs` dictionary. This component states that it
  /// mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it
  /// while closing the object with `additionalProperties: false` made the two claims
  /// contradict each other: a server emitting standard WebAuthn options could not
  /// conform, and the widely-used server libraries emit this member by default.
  /// Structure is deliberately unconstrained. The set of extensions is open and
  /// registered outside this framework, so enumerating them here would date the schema
  /// against a registry it does not own — and a closed list would reproduce the original
  /// defect one revision later.
  final Map<String, dynamic>? extensions;

  /// base64url-encoded one-time nonce.
  final String challenge;
  final int? timeout;
  final String? rpId;
  final List<CredentialDescriptor>? allowCredentials;
  final Object? userVerification;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (extensions != null) 'extensions': extensions!,
        'challenge': challenge,
        if (timeout != null) 'timeout': timeout!,
        if (rpId != null) 'rpId': rpId!,
        if (allowCredentials != null)
          'allowCredentials': allowCredentials!.map((e) => e.toJson()).toList(),
        if (userVerification != null) 'userVerification': userVerification!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// What the credential will authenticate, from the invite.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponsePurpose(String value) {
  static const ResponsePurpose session = ResponsePurpose('session');
  static const ResponsePurpose stepUp = ResponsePurpose('stepUp');

  /// Every value this specification's schema permits.
  static const List<ResponsePurpose> values = <ResponsePurpose>[
    session,
    stepUp
  ];
}

/// The registration ceremony the invite authorises.
class Response {
  const Response({
    required this.enrollmentId,
    required this.subject,
    required this.purpose,
    this.deviceLabel,
    required this.options,
    this.uvOptions,
    required this.expiresAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        enrollmentId: json['enrollmentId'] as String,
        subject: json['subject'] as String,
        purpose: ResponsePurpose(json['purpose'] as String),
        deviceLabel: json['deviceLabel'] as String?,
        options: CredentialCreationOptions.fromJson(
            json['options'] as Map<String, dynamic>),
        uvOptions: json['uvOptions'] == null
            ? null
            : CredentialRequestOptions.fromJson(
                json['uvOptions'] as Map<String, dynamic>),
        expiresAt: json['expiresAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Opaque handle correlating this start with the matching
  /// auth/passkey/enroll/redeem/finish. Echoed verbatim.
  final String enrollmentId;

  /// The VID the invite binds the credential to, so the invitee can see whose passkey
  /// they are enrolling before they create it.
  final String subject;

  /// What the credential will authenticate, from the invite.
  final ResponsePurpose purpose;

  /// The inviter's suggested label, if any. The invitee MAY override it at finish.
  final String? deviceLabel;

  /// PublicKeyCredentialCreationOptions for navigator.credentials.create.
  final CredentialCreationOptions options;

  /// PublicKeyCredentialRequestOptions over the subject's existing credentials of the
  /// same purpose, present exactly when the subject already has one. The invitee MUST
  /// run both ceremonies and return both results to finish.
  final CredentialRequestOptions? uvOptions;

  /// When this ceremony lapses (not the invite).
  final String expiresAt;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'enrollmentId': enrollmentId,
        'subject': subject,
        'purpose': purpose.value,
        if (deviceLabel != null) 'deviceLabel': deviceLabel!,
        'options': options.toJson(),
        if (uvOptions != null) 'uvOptions': uvOptions!.toJson(),
        'expiresAt': expiresAt,
        if (ext != null) 'ext': ext!,
      };
}

/// The invitee presents an auth/passkey/enroll/invite token and its claim code, and
/// the auth service begins the WebAuthn registration ceremony the invite authorises.
class Payload {
  const Payload({
    required this.token,
    required this.claimCode,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        token: json['token'] as String,
        claimCode: json['claimCode'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The invite token, as carried in the invite URL.
  final String token;

  /// The claim code the administrator delivered separately.
  final String claimCode;

  /// Ecosystem-defined extension members per SPEC.md §4.5.1.
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'token': token,
        'claimCode': claimCode,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/auth/passkey/enroll/redeem/start/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/auth/passkey/enroll/redeem/start/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/auth/passkey/enroll/redeem/start/0.1","title":"Auth — Passkey Enroll (redeem, start)","description":"The invitee presents an auth/passkey/enroll/invite token and its claim code, and the auth service begins the WebAuthn registration ceremony the invite authorises.","type":"object","additionalProperties":false,"required":["token","claimCode"],"properties":{"token":{"type":"string","minLength":16,"maxLength":512,"description":"The invite token, as carried in the invite URL."},"claimCode":{"type":"string","minLength":1,"maxLength":64,"description":"The claim code the administrator delivered separately."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}},"\$defs":{"Response":{"\$anchor":"response","title":"Auth Passkey Enroll Redeem Start — response payload","description":"The registration ceremony the invite authorises.","type":"object","additionalProperties":false,"required":["enrollmentId","subject","purpose","options","expiresAt"],"properties":{"enrollmentId":{"type":"string","minLength":1,"description":"Opaque handle correlating this start with the matching auth/passkey/enroll/redeem/finish. Echoed verbatim."},"subject":{"type":"string","minLength":1,"description":"The VID the invite binds the credential to, so the invitee can see whose passkey they are enrolling before they create it."},"purpose":{"type":"string","enum":["session","stepUp"],"description":"What the credential will authenticate, from the invite."},"deviceLabel":{"type":"string","maxLength":256,"description":"The inviter\'s suggested label, if any. The invitee MAY override it at finish."},"options":{"\$ref":"#/\$defs/CredentialCreationOptions","description":"PublicKeyCredentialCreationOptions for navigator.credentials.create."},"uvOptions":{"\$ref":"#/\$defs/CredentialRequestOptions","description":"PublicKeyCredentialRequestOptions over the subject\'s existing credentials of the same purpose, present exactly when the subject already has one. The invitee MUST run both ceremonies and return both results to finish."},"expiresAt":{"type":"string","format":"date-time","description":"When this ceremony lapses (not the invite)."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialRequestOptions":{"\$anchor":"credentialRequestOptions","title":"PublicKeyCredentialRequestOptions","description":"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.","type":"object","additionalProperties":false,"required":["challenge"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"timeout":{"type":"integer","minimum":1},"rpId":{"type":"string","minLength":1},"allowCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"userVerification":{"enum":["discouraged","preferred","required"]}}},"CredentialDescriptor":{"\$anchor":"credentialDescriptor","title":"PublicKeyCredentialDescriptor","type":"object","additionalProperties":false,"required":["type","id"],"properties":{"type":{"const":"public-key"},"id":{"type":"string","description":"base64url-encoded credential id."},"transports":{"type":"array","items":{"enum":["usb","nfc","ble","internal","hybrid"]}}}},"CredentialCreationOptions":{"\$anchor":"credentialCreationOptions","title":"PublicKeyCredentialCreationOptions","description":"Server-issued options for `navigator.credentials.create({ publicKey: ... })`. Mirrors the WebAuthn Level 2 `PublicKeyCredentialCreationOptions` dictionary; binary fields are base64url-encoded strings (rather than ArrayBuffers) so the value is JSON-safe over the wire.","type":"object","additionalProperties":false,"required":["challenge","rp","user","pubKeyCredParams"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"rp":{"type":"object","additionalProperties":false,"required":["id","name"],"properties":{"id":{"type":"string","minLength":1},"name":{"type":"string","minLength":1,"maxLength":64}}},"user":{"type":"object","additionalProperties":false,"required":["id","name","displayName"],"properties":{"id":{"type":"string","description":"base64url-encoded user handle. SHOULD be a stable opaque identifier — never reuse a human-readable username here."},"name":{"type":"string","minLength":1,"maxLength":64},"displayName":{"type":"string","maxLength":64}}},"pubKeyCredParams":{"type":"array","minItems":1,"items":{"type":"object","additionalProperties":false,"required":["type","alg"],"properties":{"type":{"const":"public-key"},"alg":{"type":"integer","description":"COSE algorithm identifier (e.g. -8 Ed25519, -7 ES256, -257 RS256)."}}}},"timeout":{"type":"integer","minimum":1},"excludeCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"authenticatorSelection":{"type":"object","additionalProperties":false,"properties":{"authenticatorAttachment":{"enum":["platform","cross-platform"]},"requireResidentKey":{"type":"boolean"},"residentKey":{"enum":["discouraged","preferred","required"]},"userVerification":{"enum":["discouraged","preferred","required"]}}},"attestation":{"enum":["none","indirect","direct","enterprise"]}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Auth Passkey Enroll Redeem Start — response payload","description":"The registration ceremony the invite authorises.","type":"object","additionalProperties":false,"required":["enrollmentId","subject","purpose","options","expiresAt"],"properties":{"enrollmentId":{"type":"string","minLength":1,"description":"Opaque handle correlating this start with the matching auth/passkey/enroll/redeem/finish. Echoed verbatim."},"subject":{"type":"string","minLength":1,"description":"The VID the invite binds the credential to, so the invitee can see whose passkey they are enrolling before they create it."},"purpose":{"type":"string","enum":["session","stepUp"],"description":"What the credential will authenticate, from the invite."},"deviceLabel":{"type":"string","maxLength":256,"description":"The inviter\'s suggested label, if any. The invitee MAY override it at finish."},"options":{"\$ref":"#/\$defs/CredentialCreationOptions","description":"PublicKeyCredentialCreationOptions for navigator.credentials.create."},"uvOptions":{"\$ref":"#/\$defs/CredentialRequestOptions","description":"PublicKeyCredentialRequestOptions over the subject\'s existing credentials of the same purpose, present exactly when the subject already has one. The invitee MUST run both ceremonies and return both results to finish."},"expiresAt":{"type":"string","format":"date-time","description":"When this ceremony lapses (not the invite)."},"ext":{"\$ref":"#/\$defs/Ext","description":"Ecosystem-defined extension members per SPEC.md §4.5.1."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CredentialRequestOptions":{"\$anchor":"credentialRequestOptions","title":"PublicKeyCredentialRequestOptions","description":"Server-issued options for `navigator.credentials.get({ publicKey: ... })`.","type":"object","additionalProperties":false,"required":["challenge"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"timeout":{"type":"integer","minimum":1},"rpId":{"type":"string","minLength":1},"allowCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"userVerification":{"enum":["discouraged","preferred","required"]}}},"CredentialDescriptor":{"\$anchor":"credentialDescriptor","title":"PublicKeyCredentialDescriptor","type":"object","additionalProperties":false,"required":["type","id"],"properties":{"type":{"const":"public-key"},"id":{"type":"string","description":"base64url-encoded credential id."},"transports":{"type":"array","items":{"enum":["usb","nfc","ble","internal","hybrid"]}}}},"CredentialCreationOptions":{"\$anchor":"credentialCreationOptions","title":"PublicKeyCredentialCreationOptions","description":"Server-issued options for `navigator.credentials.create({ publicKey: ... })`. Mirrors the WebAuthn Level 2 `PublicKeyCredentialCreationOptions` dictionary; binary fields are base64url-encoded strings (rather than ArrayBuffers) so the value is JSON-safe over the wire.","type":"object","additionalProperties":false,"required":["challenge","rp","user","pubKeyCredParams"],"properties":{"extensions":{"type":"object","description":"Client extension inputs, per the WebAuthn Level 2 `AuthenticationExtensionsClientInputs` dictionary.\\n\\nThis component states that it mirrors the W3C dictionary, and that dictionary defines `extensions`. Omitting it while closing the object with `additionalProperties: false` made the two claims contradict each other: a server emitting standard WebAuthn options could not conform, and the widely-used server libraries emit this member by default.\\n\\nStructure is deliberately unconstrained. The set of extensions is open and registered outside this framework, so enumerating them here would date the schema against a registry it does not own — and a closed list would reproduce the original defect one revision later."},"challenge":{"type":"string","description":"base64url-encoded one-time nonce."},"rp":{"type":"object","additionalProperties":false,"required":["id","name"],"properties":{"id":{"type":"string","minLength":1},"name":{"type":"string","minLength":1,"maxLength":64}}},"user":{"type":"object","additionalProperties":false,"required":["id","name","displayName"],"properties":{"id":{"type":"string","description":"base64url-encoded user handle. SHOULD be a stable opaque identifier — never reuse a human-readable username here."},"name":{"type":"string","minLength":1,"maxLength":64},"displayName":{"type":"string","maxLength":64}}},"pubKeyCredParams":{"type":"array","minItems":1,"items":{"type":"object","additionalProperties":false,"required":["type","alg"],"properties":{"type":{"const":"public-key"},"alg":{"type":"integer","description":"COSE algorithm identifier (e.g. -8 Ed25519, -7 ES256, -257 RS256)."}}}},"timeout":{"type":"integer","minimum":1},"excludeCredentials":{"type":"array","items":{"\$ref":"#/\$defs/CredentialDescriptor"}},"authenticatorSelection":{"type":"object","additionalProperties":false,"properties":{"authenticatorAttachment":{"enum":["platform","cross-platform"]},"requireResidentKey":{"type":"boolean"},"residentKey":{"enum":["discouraged","preferred","required"]},"userVerification":{"enum":["discouraged","preferred","required"]}}},"attestation":{"enum":["none","indirect","direct","enterprise"]}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
