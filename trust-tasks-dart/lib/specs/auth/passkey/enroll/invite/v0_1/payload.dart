// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/auth/passkey/enroll/invite/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseInvite, generated from its schema.
class ResponseInvite {
  const ResponseInvite({
    required this.token,
    required this.url,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseInvite.fromJson(Map<String, dynamic> json) => ResponseInvite(
        token: json['token'] as String,
        url: json['url'] as String,
      );

  /// Opaque single-use invite token. The invitee presents it to the consumer during
  /// enrollment. ≥128 bits entropy.
  final String token;

  /// Operator-shareable URL that, when opened in a WebAuthn-capable browser, drives the
  /// enrollment ceremony. Contains the token as a query parameter.
  final String url;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'token': token,
        'url': url,
      };
}

/// The issued invite. Carried in a Trust Task document whose type is
/// https://trusttasks.org/spec/auth/passkey/enroll/invite/0.1#response.
class Response {
  const Response({
    required this.invite,
    required this.subject,
    required this.expiresAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        invite: ResponseInvite.fromJson(json['invite'] as Map<String, dynamic>),
        subject: json['subject'] as String,
        expiresAt: json['expiresAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final ResponseInvite invite;

  /// Echo of the invitee's VID.
  final String subject;
  final String expiresAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'invite': invite.toJson(),
        'subject': subject,
        'expiresAt': expiresAt,
        if (ext != null) 'ext': ext!,
      };
}

/// An administrator issues a single-use invite link an unenrolled subject can redeem
/// to bind their first passkey, without first holding an authenticated session.
class Payload {
  const Payload({
    required this.subject,
    this.role,
    this.scopes,
    this.deviceLabel,
    this.ttl,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subject: json['subject'] as String,
        role: json['role'] as String?,
        scopes: json['scopes'] == null
            ? null
            : (json['scopes'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        deviceLabel: json['deviceLabel'] as String?,
        ttl: json['ttl'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The VID the invitee will enroll. The admin asserts this binding; the redemption
  /// ceremony verifies the invitee actually controls a passkey but does NOT
  /// independently verify the subject — trust flows from the admin.
  final String subject;

  /// Role the invitee receives on successful enrollment. Consumer-defined vocabulary; if
  /// absent the consumer applies its default role.
  final String? role;

  /// Optional scopes to attach on enrollment.
  final List<String>? scopes;

  /// Suggested label for the credential. Used as a default; the invitee MAY override
  /// during redemption.
  final String? deviceLabel;

  /// Seconds the invite remains valid. Consumers SHOULD pick 1 h – 7 d; longer windows
  /// widen the attack surface.
  final int? ttl;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        if (role != null) 'role': role!,
        if (scopes != null) 'scopes': scopes!,
        if (deviceLabel != null) 'deviceLabel': deviceLabel!,
        if (ttl != null) 'ttl': ttl!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/auth/passkey/enroll/invite/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/auth/passkey/enroll/invite/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/auth/passkey/enroll/invite/0.1","title":"Auth — Passkey Enroll (invite)","description":"An administrator issues a single-use invite link an unenrolled subject can redeem to bind their first passkey, without first holding an authenticated session.","type":"object","additionalProperties":false,"required":["subject"],"properties":{"subject":{"type":"string","minLength":1,"description":"The VID the invitee will enroll. The admin asserts this binding; the redemption ceremony verifies the invitee actually controls a passkey but does NOT independently verify the subject — trust flows from the admin."},"role":{"type":"string","description":"Role the invitee receives on successful enrollment. Consumer-defined vocabulary; if absent the consumer applies its default role."},"scopes":{"type":"array","items":{"type":"string","minLength":1},"description":"Optional scopes to attach on enrollment."},"deviceLabel":{"type":"string","maxLength":256,"description":"Suggested label for the credential. Used as a default; the invitee MAY override during redemption."},"ttl":{"type":"integer","minimum":1,"description":"Seconds the invite remains valid. Consumers SHOULD pick 1 h – 7 d; longer windows widen the attack surface."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Auth Passkey Enroll Invite — response payload","description":"The issued invite. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/enroll/invite/0.1#response.","type":"object","additionalProperties":false,"required":["invite","subject","expiresAt"],"properties":{"invite":{"type":"object","additionalProperties":false,"required":["token","url"],"properties":{"token":{"type":"string","minLength":16,"description":"Opaque single-use invite token. The invitee presents it to the consumer during enrollment. ≥128 bits entropy."},"url":{"type":"string","format":"uri","description":"Operator-shareable URL that, when opened in a WebAuthn-capable browser, drives the enrollment ceremony. Contains the token as a query parameter."}}},"subject":{"type":"string","description":"Echo of the invitee\'s VID."},"expiresAt":{"type":"string","format":"date-time"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Auth Passkey Enroll Invite — response payload","description":"The issued invite. Carried in a Trust Task document whose type is https://trusttasks.org/spec/auth/passkey/enroll/invite/0.1#response.","type":"object","additionalProperties":false,"required":["invite","subject","expiresAt"],"properties":{"invite":{"type":"object","additionalProperties":false,"required":["token","url"],"properties":{"token":{"type":"string","minLength":16,"description":"Opaque single-use invite token. The invitee presents it to the consumer during enrollment. ≥128 bits entropy."},"url":{"type":"string","format":"uri","description":"Operator-shareable URL that, when opened in a WebAuthn-capable browser, drives the enrollment ceremony. Contains the token as a query parameter."}}},"subject":{"type":"string","description":"Echo of the invitee\'s VID."},"expiresAt":{"type":"string","format":"date-time"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
