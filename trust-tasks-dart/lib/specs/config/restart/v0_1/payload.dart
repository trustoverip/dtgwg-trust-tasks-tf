// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/config/restart/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Config Restart — response payload
class Response {
  const Response({
    required this.supervisor,
    required this.drainTimeoutSeconds,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        supervisor: json['supervisor'] as String,
        drainTimeoutSeconds: json['drainTimeoutSeconds'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The process supervisor that will bring the maintainer back up, detected by the
  /// maintainer. An opaque maintainer-defined label — the framework does not enumerate
  /// it (e.g. `systemd`, `kubernetes`, or an explicit operator opt-in). A maintainer
  /// that detects no supervisor MUST refuse (see errorCodes) rather than exit
  /// unsupervised.
  final String supervisor;

  /// How long the maintainer will drain in-flight work before exiting, so a caller knows
  /// the expected downtime window.
  final int drainTimeoutSeconds;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'supervisor': supervisor,
        'drainTimeoutSeconds': drainTimeoutSeconds,
        if (ext != null) 'ext': ext!,
      };
}

/// Request a graceful restart so a process supervisor brings the maintainer back up
/// with restart-gated configuration applied. Takes no parameters.
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/config/restart/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/config/restart/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/config/restart/0.1","title":"Config Restart — payload","description":"Request a graceful restart so a process supervisor brings the maintainer back up with restart-gated configuration applied. Takes no parameters.","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Config Restart — response payload","type":"object","additionalProperties":false,"required":["supervisor","drainTimeoutSeconds"],"properties":{"supervisor":{"type":"string","minLength":1,"description":"The process supervisor that will bring the maintainer back up, detected by the maintainer. An opaque maintainer-defined label — the framework does not enumerate it (e.g. `systemd`, `kubernetes`, or an explicit operator opt-in). A maintainer that detects no supervisor MUST refuse (see errorCodes) rather than exit unsupervised."},"drainTimeoutSeconds":{"type":"integer","minimum":0,"description":"How long the maintainer will drain in-flight work before exiting, so a caller knows the expected downtime window."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Config Restart — response payload","type":"object","additionalProperties":false,"required":["supervisor","drainTimeoutSeconds"],"properties":{"supervisor":{"type":"string","minLength":1,"description":"The process supervisor that will bring the maintainer back up, detected by the maintainer. An opaque maintainer-defined label — the framework does not enumerate it (e.g. `systemd`, `kubernetes`, or an explicit operator opt-in). A maintainer that detects no supervisor MUST refuse (see errorCodes) rather than exit unsupervised."},"drainTimeoutSeconds":{"type":"integer","minimum":0,"description":"How long the maintainer will drain in-flight work before exiting, so a caller knows the expected downtime window."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
