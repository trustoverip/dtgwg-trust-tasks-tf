// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/config/show/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// One runtime configuration key as the maintainer currently sees it, after resolving
/// whatever layered overlay it uses.
class ConfigField {
  const ConfigField({
    required this.key,
    required this.value,
    required this.source,
    required this.requiresRestart,
  });

  /// Read this payload from a decoded JSON object.
  factory ConfigField.fromJson(Map<String, dynamic> json) => ConfigField(
        key: json['key'] as String,
        value: json['value'],
        source: json['source'] as String,
        requiresRestart: json['requiresRestart'] as bool,
      );

  /// The configuration key, e.g. `server.port` or `log.level`.
  final String key;

  /// The effective value. Any JSON scalar the key holds (string, number, boolean, or
  /// null). A maintainer MUST redact the value of a secret-bearing key (see the spec)
  /// rather than return it here.
  final Object? value;

  /// Which layer supplied the effective value. An opaque maintainer-defined label — the
  /// framework does not enumerate it. A maintainer with a layered overlay reports the
  /// winning layer (e.g. `env`, `db`, `toml`, `default`); one without layers reports its
  /// single source.
  final String source;

  /// True when a change to this key takes effect only after a restart, rather than on
  /// the next read.
  final bool requiresRestart;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'key': key,
        'value': value,
        'source': source,
        'requiresRestart': requiresRestart,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Config Show — response payload
class Response {
  const Response({
    required this.fields,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        fields: (json['fields'] as List<dynamic>)
            .map((e) => ConfigField.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The effective value of each requested (or every) configuration key.
  final List<ConfigField> fields;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'fields': fields.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Config Show — payload
class Payload {
  const Payload({
    this.keys,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        keys: json['keys'] == null
            ? null
            : (json['keys'] as List<dynamic>).map((e) => e as String).toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Return only these keys. Omitted = return every registered key.
  final List<String>? keys;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (keys != null) 'keys': keys!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/config/show/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/config/show/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/config/show/0.1","title":"Config Show — payload","type":"object","additionalProperties":false,"properties":{"keys":{"type":"array","items":{"type":"string","minLength":1},"uniqueItems":true,"minItems":1,"description":"Return only these keys. Omitted = return every registered key."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Config Show — response payload","type":"object","additionalProperties":false,"required":["fields"],"properties":{"fields":{"type":"array","description":"The effective value of each requested (or every) configuration key.","items":{"\$ref":"#/\$defs/ConfigField"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConfigField":{"\$anchor":"configField","title":"ConfigField","description":"One runtime configuration key as the maintainer currently sees it, after resolving whatever layered overlay it uses.","type":"object","additionalProperties":false,"required":["key","value","source","requiresRestart"],"properties":{"key":{"type":"string","minLength":1,"description":"The configuration key, e.g. `server.port` or `log.level`."},"value":{"description":"The effective value. Any JSON scalar the key holds (string, number, boolean, or null). A maintainer MUST redact the value of a secret-bearing key (see the spec) rather than return it here."},"source":{"type":"string","minLength":1,"description":"Which layer supplied the effective value. An opaque maintainer-defined label — the framework does not enumerate it. A maintainer with a layered overlay reports the winning layer (e.g. `env`, `db`, `toml`, `default`); one without layers reports its single source."},"requiresRestart":{"type":"boolean","description":"True when a change to this key takes effect only after a restart, rather than on the next read."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Config Show — response payload","type":"object","additionalProperties":false,"required":["fields"],"properties":{"fields":{"type":"array","description":"The effective value of each requested (or every) configuration key.","items":{"\$ref":"#/\$defs/ConfigField"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConfigField":{"\$anchor":"configField","title":"ConfigField","description":"One runtime configuration key as the maintainer currently sees it, after resolving whatever layered overlay it uses.","type":"object","additionalProperties":false,"required":["key","value","source","requiresRestart"],"properties":{"key":{"type":"string","minLength":1,"description":"The configuration key, e.g. `server.port` or `log.level`."},"value":{"description":"The effective value. Any JSON scalar the key holds (string, number, boolean, or null). A maintainer MUST redact the value of a secret-bearing key (see the spec) rather than return it here."},"source":{"type":"string","minLength":1,"description":"Which layer supplied the effective value. An opaque maintainer-defined label — the framework does not enumerate it. A maintainer with a layered overlay reports the winning layer (e.g. `env`, `db`, `toml`, `default`); one without layers reports its single source."},"requiresRestart":{"type":"boolean","description":"True when a change to this key takes effect only after a restart, rather than on the next read."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
