// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/config/patch/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A key a patch declined to apply, with the reason.
class RejectedKey {
  const RejectedKey({
    required this.key,
    required this.reason,
  });

  /// Read this payload from a decoded JSON object.
  factory RejectedKey.fromJson(Map<String, dynamic> json) => RejectedKey(
        key: json['key'] as String,
        reason: json['reason'] as String,
      );

  final String key;

  /// Why the key was rejected — unknown key, wrong type, out-of-range, allowlist
  /// mismatch, etc.
  final String reason;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'key': key,
        'reason': reason,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Config Patch — response payload
class Response {
  const Response({
    required this.applied,
    required this.pendingRestart,
    required this.rejected,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        applied:
            (json['applied'] as List<dynamic>).map((e) => e as String).toList(),
        pendingRestart: (json['pendingRestart'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
        rejected: (json['rejected'] as List<dynamic>)
            .map((e) => RejectedKey.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Keys whose new value is now in effect.
  final List<String> applied;

  /// Keys whose new value was stored but takes effect only after a restart (their
  /// ConfigField has requiresRestart: true).
  final List<String> pendingRestart;

  /// Keys that were not applied, each with a reason. Empty when every override
  /// succeeded.
  final List<RejectedKey> rejected;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'applied': applied,
        'pendingRestart': pendingRestart,
        'rejected': rejected.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Config Patch — payload
class Payload {
  const Payload({
    required this.overrides,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        overrides: json['overrides'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A key -&gt; value map of overrides to write. Keys are configuration keys; values are
  /// the desired new values. Wrapped in an object (rather than being the top-level
  /// payload) so the payload keeps a fixed, additionalProperties:false envelope. Each
  /// entry is validated independently — an invalid or unknown key is reported under
  /// `rejected` and does not block the rest.
  final Map<String, dynamic> overrides;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'overrides': overrides,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/config/patch/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/config/patch/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/config/patch/0.1","title":"Config Patch — payload","type":"object","additionalProperties":false,"required":["overrides"],"properties":{"overrides":{"type":"object","minProperties":1,"additionalProperties":true,"description":"A key -> value map of overrides to write. Keys are configuration keys; values are the desired new values. Wrapped in an object (rather than being the top-level payload) so the payload keeps a fixed, additionalProperties:false envelope. Each entry is validated independently — an invalid or unknown key is reported under `rejected` and does not block the rest."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Config Patch — response payload","type":"object","additionalProperties":false,"required":["applied","pendingRestart","rejected"],"properties":{"applied":{"type":"array","items":{"type":"string","minLength":1},"uniqueItems":true,"description":"Keys whose new value is now in effect."},"pendingRestart":{"type":"array","items":{"type":"string","minLength":1},"uniqueItems":true,"description":"Keys whose new value was stored but takes effect only after a restart (their ConfigField has requiresRestart: true)."},"rejected":{"type":"array","description":"Keys that were not applied, each with a reason. Empty when every override succeeded.","items":{"\$ref":"#/\$defs/RejectedKey"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RejectedKey":{"\$anchor":"rejectedKey","title":"RejectedKey","description":"A key a patch declined to apply, with the reason.","type":"object","additionalProperties":false,"required":["key","reason"],"properties":{"key":{"type":"string","minLength":1},"reason":{"type":"string","minLength":1,"maxLength":1024,"description":"Why the key was rejected — unknown key, wrong type, out-of-range, allowlist mismatch, etc."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Config Patch — response payload","type":"object","additionalProperties":false,"required":["applied","pendingRestart","rejected"],"properties":{"applied":{"type":"array","items":{"type":"string","minLength":1},"uniqueItems":true,"description":"Keys whose new value is now in effect."},"pendingRestart":{"type":"array","items":{"type":"string","minLength":1},"uniqueItems":true,"description":"Keys whose new value was stored but takes effect only after a restart (their ConfigField has requiresRestart: true)."},"rejected":{"type":"array","description":"Keys that were not applied, each with a reason. Empty when every override succeeded.","items":{"\$ref":"#/\$defs/RejectedKey"}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RejectedKey":{"\$anchor":"rejectedKey","title":"RejectedKey","description":"A key a patch declined to apply, with the reason.","type":"object","additionalProperties":false,"required":["key","reason"],"properties":{"key":{"type":"string","minLength":1},"reason":{"type":"string","minLength":1,"maxLength":1024,"description":"Why the key was rejected — unknown key, wrong type, out-of-range, allowlist mismatch, etc."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
