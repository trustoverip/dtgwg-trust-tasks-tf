// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/git-trust/grant/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Git Trust Grant — response payload
class Response {
  const Response({
    required this.subject,
    required this.resource,
    required this.granted,
    this.message,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        subject: json['subject'] as String,
        resource: json['resource'] as String,
        granted: json['granted'] as bool,
        message: json['message'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String subject;
  final String resource;

  /// True once the grant tuple is durably recorded.
  final bool granted;

  /// Additional human-readable detail.
  final String? message;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        'resource': resource,
        'granted': granted,
        if (message != null) 'message': message!,
        if (ext != null) 'ext': ext!,
      };
}

/// A community operator grants a member's DID the authority to sign git commits for a
/// resource (an org or org/repo). The host records the grant as a TRQP authorization
/// tuple {entity: subject, authority: the community's authority DID, action:
/// git.commit.sign, resource} in the community's trust registry, where CI verifiers
/// (e.g. did-git-sign verify-trust) query it anonymously.
class Payload {
  const Payload({
    required this.subject,
    required this.resource,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subject: json['subject'] as String,
        resource: json['resource'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// DID of the member being granted commit-signing trust.
  final String subject;

  /// The resource the grant covers: an org (`&lt;org&gt;`) for an org-wide grant or a
  /// repository slug (`&lt;org&gt;/&lt;repo&gt;`).
  final String resource;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        'resource': resource,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/git-trust/grant/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/git-trust/grant/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/git-trust/grant/0.1","title":"Git Trust Grant — payload","description":"A community operator grants a member\'s DID the authority to sign git commits for a resource (an org or org/repo). The host records the grant as a TRQP authorization tuple {entity: subject, authority: the community\'s authority DID, action: git.commit.sign, resource} in the community\'s trust registry, where CI verifiers (e.g. did-git-sign verify-trust) query it anonymously.","type":"object","additionalProperties":false,"required":["subject","resource"],"properties":{"subject":{"type":"string","pattern":"^did:","description":"DID of the member being granted commit-signing trust."},"resource":{"type":"string","minLength":1,"description":"The resource the grant covers: an org (`<org>`) for an org-wide grant or a repository slug (`<org>/<repo>`)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Git Trust Grant — response payload","type":"object","additionalProperties":false,"required":["subject","resource","granted"],"properties":{"subject":{"type":"string"},"resource":{"type":"string"},"granted":{"type":"boolean","description":"True once the grant tuple is durably recorded."},"message":{"type":"string","maxLength":1024,"description":"Additional human-readable detail."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Git Trust Grant — response payload","type":"object","additionalProperties":false,"required":["subject","resource","granted"],"properties":{"subject":{"type":"string"},"resource":{"type":"string"},"granted":{"type":"boolean","description":"True once the grant tuple is durably recorded."},"message":{"type":"string","maxLength":1024,"description":"Additional human-readable detail."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
