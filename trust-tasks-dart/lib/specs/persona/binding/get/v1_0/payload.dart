// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/binding/get/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to persona/binding/get. Type
/// https://trusttasks.org/spec/persona/binding/get/1.0#response. This task is
/// context-callable, which is why the response is deliberately thin: an application in
/// the context learns THAT a profile is bound and what the holder calls it, and never
/// what it contains. Contents reach an application only through the disclosure path,
/// where the holder sees a preview first.
class Response {
  const Response({
    required this.contextId,
    required this.personaDid,
    required this.bound,
    this.profileId,
    this.label,
    this.profileName,
    this.claimCount,
    this.boundAt,
    this.until,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        contextId: json['contextId'] as String,
        personaDid: json['personaDid'] as String,
        bound: json['bound'] as bool,
        profileId: json['profileId'] as String?,
        label: json['label'] as String?,
        profileName: json['profileName'] as String?,
        claimCount: json['claimCount'] as int?,
        boundAt: json['boundAt'] as String?,
        until: json['until'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String contextId;
  final String personaDid;

  /// Whether a profile is bound. False is a normal state, not an error — a persona need
  /// not have a profile.
  final bool bound;

  /// Present when bound. An identifier only; a context-scoped caller cannot resolve it,
  /// because profile reads are holder-authorized.
  final Ulid? profileId;

  /// The name the holder chose for this context to call the face (persona/binding/set
  /// `label`). Absent when they chose none.
  final String? label;

  /// The holder's OWN name for the bound face. A maintainer MUST omit it unless the
  /// caller is holder-authorized: it is the holder's filing, not something they told
  /// this context, and it can say far more than the face shows. A context-scoped caller
  /// reads `label`.
  final String? profileName;

  /// How many claims the materialised projection holds. Lets an application render "12
  /// details available" without obtaining any of them.
  final int? claimCount;
  final String? boundAt;

  /// When this binding ends on its own. Absent when it lasts until changed.
  /// Context-visible: the context learns when the face stops being worn here, which is
  /// the thing the holder set it to tell.
  final String? until;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'personaDid': personaDid,
        'bound': bound,
        if (profileId != null) 'profileId': profileId!,
        if (label != null) 'label': label!,
        if (profileName != null) 'profileName': profileName!,
        if (claimCount != null) 'claimCount': claimCount!,
        if (boundAt != null) 'boundAt': boundAt!,
        if (until != null) 'until': until!,
        if (ext != null) 'ext': ext!,
      };
}

/// Read the binding for one persona in a context: whether a profile is bound, and its
/// label — never its contents.
class Payload {
  const Payload({
    required this.contextId,
    required this.personaDid,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String,
        personaDid: json['personaDid'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String contextId;
  final String personaDid;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'personaDid': personaDid,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/persona/binding/get/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/binding/get/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/binding/get/1.0","title":"Persona Binding Get — payload","description":"Read the binding for one persona in a context: whether a profile is bound, and its label — never its contents.","type":"object","additionalProperties":false,"required":["contextId","personaDid"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1,"maxLength":2048},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Binding Get — response payload","description":"Success response to persona/binding/get. Type https://trusttasks.org/spec/persona/binding/get/1.0#response. This task is context-callable, which is why the response is deliberately thin: an application in the context learns THAT a profile is bound and what the holder calls it, and never what it contains. Contents reach an application only through the disclosure path, where the holder sees a preview first.","type":"object","additionalProperties":false,"required":["contextId","personaDid","bound"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1},"bound":{"type":"boolean","description":"Whether a profile is bound. False is a normal state, not an error — a persona need not have a profile."},"profileId":{"\$ref":"#/\$defs/Ulid","description":"Present when bound. An identifier only; a context-scoped caller cannot resolve it, because profile reads are holder-authorized."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"The name the holder chose for this context to call the face (persona/binding/set `label`). Absent when they chose none."},"profileName":{"type":"string","maxLength":128,"description":"The holder\'s OWN name for the bound face. A maintainer MUST omit it unless the caller is holder-authorized: it is the holder\'s filing, not something they told this context, and it can say far more than the face shows. A context-scoped caller reads `label`."},"claimCount":{"type":"integer","minimum":0,"description":"How many claims the materialised projection holds. Lets an application render \\"12 details available\\" without obtaining any of them."},"boundAt":{"type":"string","format":"date-time"},"until":{"type":"string","format":"date-time","description":"When this binding ends on its own. Absent when it lasts until changed. Context-visible: the context learns when the face stops being worn here, which is the thing the holder set it to tell."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Binding Get — response payload","description":"Success response to persona/binding/get. Type https://trusttasks.org/spec/persona/binding/get/1.0#response. This task is context-callable, which is why the response is deliberately thin: an application in the context learns THAT a profile is bound and what the holder calls it, and never what it contains. Contents reach an application only through the disclosure path, where the holder sees a preview first.","type":"object","additionalProperties":false,"required":["contextId","personaDid","bound"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1},"bound":{"type":"boolean","description":"Whether a profile is bound. False is a normal state, not an error — a persona need not have a profile."},"profileId":{"\$ref":"#/\$defs/Ulid","description":"Present when bound. An identifier only; a context-scoped caller cannot resolve it, because profile reads are holder-authorized."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"The name the holder chose for this context to call the face (persona/binding/set `label`). Absent when they chose none."},"profileName":{"type":"string","maxLength":128,"description":"The holder\'s OWN name for the bound face. A maintainer MUST omit it unless the caller is holder-authorized: it is the holder\'s filing, not something they told this context, and it can say far more than the face shows. A context-scoped caller reads `label`."},"claimCount":{"type":"integer","minimum":0,"description":"How many claims the materialised projection holds. Lets an application render \\"12 details available\\" without obtaining any of them."},"boundAt":{"type":"string","format":"date-time"},"until":{"type":"string","format":"date-time","description":"When this binding ends on its own. Absent when it lasts until changed. Context-visible: the context learns when the face stops being worn here, which is the thing the holder set it to tell."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
