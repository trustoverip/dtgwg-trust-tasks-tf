// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/facet/delete/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to persona/facet/delete. Type
/// https://trusttasks.org/spec/persona/facet/delete/1.0#response.
class Response {
  const Response({
    required this.existed,
    this.releasedFaces,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        existed: json['existed'] as bool,
        releasedFaces: json['releasedFaces'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// False when there was nothing to delete. A successful no-op rather than an error: a
  /// producer retrying after a lost response must be able to reach the state it wanted
  /// without having to tell a second delete apart from a first.
  final bool existed;

  /// How many profiles no longer belong to any facet as a result. Nothing was deleted
  /// and nothing changed about them — this is the count a consumer needs to say what the
  /// screen will look like afterwards, which is the honest end of a sentence that would
  /// otherwise read only 'deleted'.
  final int? releasedFaces;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'existed': existed,
        if (releasedFaces != null) 'releasedFaces': releasedFaces!,
        if (ext != null) 'ext': ext!,
      };
}

/// Optimistic-concurrency precondition. A positive value requires the record's current
/// `version` to equal it exactly; zero means create-only and applies only when no live
/// record exists at the address.
typedef ExpectedVersion = int;

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// Delete one facet. The faces and attributes that belonged to it are untouched — a
/// facet is a way of arranging what the holder keeps, never a container for it.
class Payload {
  const Payload({
    required this.facetId,
    this.expectedVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        facetId: json['facetId'] as String,
        expectedVersion: json['expectedVersion'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ulid facetId;

  /// Optional precondition. Supply the version a prior read returned to refuse a delete
  /// against a facet that has changed since it was looked at.
  final ExpectedVersion? expectedVersion;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'facetId': facetId,
        if (expectedVersion != null) 'expectedVersion': expectedVersion!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/persona/facet/delete/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/facet/delete/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/facet/delete/1.0","title":"Persona — Facet Delete — payload","description":"Delete one facet. The faces and attributes that belonged to it are untouched — a facet is a way of arranging what the holder keeps, never a container for it.","type":"object","additionalProperties":false,"required":["facetId"],"properties":{"facetId":{"\$ref":"#/\$defs/Ulid"},"expectedVersion":{"\$ref":"#/\$defs/ExpectedVersion","description":"Optional precondition. Supply the version a prior read returned to refuse a delete against a facet that has changed since it was looked at."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Facet Delete — response payload","description":"Success response to persona/facet/delete. Type https://trusttasks.org/spec/persona/facet/delete/1.0#response.","type":"object","additionalProperties":false,"required":["existed"],"properties":{"existed":{"type":"boolean","description":"False when there was nothing to delete. A successful no-op rather than an error: a producer retrying after a lost response must be able to reach the state it wanted without having to tell a second delete apart from a first."},"releasedFaces":{"type":"integer","minimum":0,"description":"How many profiles no longer belong to any facet as a result. Nothing was deleted and nothing changed about them — this is the count a consumer needs to say what the screen will look like afterwards, which is the honest end of a sentence that would otherwise read only \'deleted\'."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Facet Delete — response payload","description":"Success response to persona/facet/delete. Type https://trusttasks.org/spec/persona/facet/delete/1.0#response.","type":"object","additionalProperties":false,"required":["existed"],"properties":{"existed":{"type":"boolean","description":"False when there was nothing to delete. A successful no-op rather than an error: a producer retrying after a lost response must be able to reach the state it wanted without having to tell a second delete apart from a first."},"releasedFaces":{"type":"integer","minimum":0,"description":"How many profiles no longer belong to any facet as a result. Nothing was deleted and nothing changed about them — this is the count a consumer needs to say what the screen will look like afterwards, which is the honest end of a sentence that would otherwise read only \'deleted\'."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
