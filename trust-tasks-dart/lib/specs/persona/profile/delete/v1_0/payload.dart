// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/profile/delete/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// How many distinct parties this face has disclosed to, across how many contexts,
/// from the holder's disclosure history. Counts, not identifiers;
/// persona/disclosure/history names them. Returned on the delete because the delete
/// does not un-tell anyone: the history, and what those parties hold, remain.
class ResponseDisclosedTo {
  const ResponseDisclosedTo({
    required this.partyCount,
    required this.contextCount,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseDisclosedTo.fromJson(Map<String, dynamic> json) =>
      ResponseDisclosedTo(
        partyCount: json['partyCount'] as int,
        contextCount: json['contextCount'] as int,
      );

  final int partyCount;
  final int contextCount;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'partyCount': partyCount,
        'contextCount': contextCount,
      };
}

/// Success response to persona/profile/delete. Type
/// https://trusttasks.org/spec/persona/profile/delete/1.0#response. Deleting an absent
/// profile is a success carrying existed: false, which is what makes a repeated delete
/// converge.
class Response {
  const Response({
    required this.profileId,
    required this.existed,
    this.disclosedTo,
    this.unboundPersonas,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        profileId: json['profileId'] as String,
        existed: json['existed'] as bool,
        disclosedTo: json['disclosedTo'] == null
            ? null
            : ResponseDisclosedTo.fromJson(
                json['disclosedTo'] as Map<String, dynamic>),
        unboundPersonas: json['unboundPersonas'] == null
            ? null
            : (json['unboundPersonas'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ulid profileId;

  /// False when no live profile was present. A repeat delete returns false and
  /// deliberately takes no new version, so watchers never observe a change that did not
  /// happen.
  final bool existed;

  /// How many distinct parties this face has disclosed to, across how many contexts,
  /// from the holder's disclosure history. Counts, not identifiers;
  /// persona/disclosure/history names them. Returned on the delete because the delete
  /// does not un-tell anyone: the history, and what those parties hold, remain.
  final ResponseDisclosedTo? disclosedTo;

  /// Persona DIDs whose bindings were cleared. Present only when `unbind` was true and
  /// something was cleared — a holder is owed the list of which personas their one
  /// action left presenting nothing.
  final List<String>? unboundPersonas;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'profileId': profileId,
        'existed': existed,
        if (disclosedTo != null) 'disclosedTo': disclosedTo!.toJson(),
        if (unboundPersonas != null) 'unboundPersonas': unboundPersonas!,
        if (ext != null) 'ext': ext!,
      };
}

/// Optimistic-concurrency precondition. A positive value requires the record's current
/// `version` to equal it exactly; zero means create-only and applies only when no live
/// record exists at the address.
typedef ExpectedVersion = int;

/// Remove one profile. Refused by default while a persona is still bound to it,
/// because a bound persona whose profile vanished would present nothing without the
/// holder having said so.
class Payload {
  const Payload({
    required this.profileId,
    this.unbind,
    this.expectedVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        profileId: json['profileId'] as String,
        unbind: json['unbind'] as bool?,
        expectedVersion: json['expectedVersion'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ulid profileId;

  /// When false (the default) a bound profile is refused and the error names the bound
  /// personas. When true the profile is removed and every binding to it is cleared,
  /// leaving those personas with no profile — which is a legal state, and one the holder
  /// is told about rather than discovering. Never the default: a persona that silently
  /// stopped presenting anything is a failure a holder finds out about from the other
  /// side.
  final bool? unbind;
  final ExpectedVersion? expectedVersion;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'profileId': profileId,
        if (unbind != null) 'unbind': unbind!,
        if (expectedVersion != null) 'expectedVersion': expectedVersion!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/persona/profile/delete/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/profile/delete/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/profile/delete/1.0","title":"Persona Profile Delete — payload","description":"Remove one profile. Refused by default while a persona is still bound to it, because a bound persona whose profile vanished would present nothing without the holder having said so.","type":"object","additionalProperties":false,"required":["profileId"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"unbind":{"type":"boolean","default":false,"description":"When false (the default) a bound profile is refused and the error names the bound personas. When true the profile is removed and every binding to it is cleared, leaving those personas with no profile — which is a legal state, and one the holder is told about rather than discovering. Never the default: a persona that silently stopped presenting anything is a failure a holder finds out about from the other side."},"expectedVersion":{"\$ref":"#/\$defs/ExpectedVersion"},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Profile Delete — response payload","description":"Success response to persona/profile/delete. Type https://trusttasks.org/spec/persona/profile/delete/1.0#response. Deleting an absent profile is a success carrying existed: false, which is what makes a repeated delete converge.","type":"object","additionalProperties":false,"required":["profileId","existed"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"existed":{"type":"boolean","description":"False when no live profile was present. A repeat delete returns false and deliberately takes no new version, so watchers never observe a change that did not happen."},"disclosedTo":{"type":"object","additionalProperties":false,"required":["partyCount","contextCount"],"description":"How many distinct parties this face has disclosed to, across how many contexts, from the holder\'s disclosure history. Counts, not identifiers; persona/disclosure/history names them. Returned on the delete because the delete does not un-tell anyone: the history, and what those parties hold, remain.","properties":{"partyCount":{"type":"integer","minimum":0},"contextCount":{"type":"integer","minimum":0}}},"unboundPersonas":{"type":"array","maxItems":256,"items":{"type":"string","minLength":1},"description":"Persona DIDs whose bindings were cleared. Present only when `unbind` was true and something was cleared — a holder is owed the list of which personas their one action left presenting nothing."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Profile Delete — response payload","description":"Success response to persona/profile/delete. Type https://trusttasks.org/spec/persona/profile/delete/1.0#response. Deleting an absent profile is a success carrying existed: false, which is what makes a repeated delete converge.","type":"object","additionalProperties":false,"required":["profileId","existed"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"existed":{"type":"boolean","description":"False when no live profile was present. A repeat delete returns false and deliberately takes no new version, so watchers never observe a change that did not happen."},"disclosedTo":{"type":"object","additionalProperties":false,"required":["partyCount","contextCount"],"description":"How many distinct parties this face has disclosed to, across how many contexts, from the holder\'s disclosure history. Counts, not identifiers; persona/disclosure/history names them. Returned on the delete because the delete does not un-tell anyone: the history, and what those parties hold, remain.","properties":{"partyCount":{"type":"integer","minimum":0},"contextCount":{"type":"integer","minimum":0}}},"unboundPersonas":{"type":"array","maxItems":256,"items":{"type":"string","minLength":1},"description":"Persona DIDs whose bindings were cleared. Present only when `unbind` was true and something was cleared — a holder is owed the list of which personas their one action left presenting nothing."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
