// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/profile/retire/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// A value of the store's monotonic write counter. Server-assigned; a producer never
/// chooses one.
typedef Version = int;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseUnboundItem, generated from its schema.
class ResponseUnboundItem {
  const ResponseUnboundItem({
    required this.contextId,
    required this.personaDid,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseUnboundItem.fromJson(Map<String, dynamic> json) =>
      ResponseUnboundItem(
        contextId: json['contextId'] as String,
        personaDid: json['personaDid'] as String,
      );

  final String contextId;
  final String personaDid;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'personaDid': personaDid,
      };
}

/// Success response to persona/profile/retire. Type
/// https://trusttasks.org/spec/persona/profile/retire/1.0#response. Retiring a face
/// already retired is a success that changes nothing and takes no new version, so a
/// repeated retire converges.
class Response {
  const Response({
    required this.profileId,
    required this.version,
    required this.retiredAt,
    this.unbound,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        profileId: json['profileId'] as String,
        version: json['version'] as int,
        retiredAt: json['retiredAt'] as String,
        unbound: json['unbound'] == null
            ? null
            : (json['unbound'] as List<dynamic>)
                .map((e) =>
                    ResponseUnboundItem.fromJson(e as Map<String, dynamic>))
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ulid profileId;
  final Version version;
  final String retiredAt;

  /// Where the face was worn and no longer is. Absent when it was worn nowhere.
  final List<ResponseUnboundItem>? unbound;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'profileId': profileId,
        'version': version,
        'retiredAt': retiredAt,
        if (unbound != null)
          'unbound': unbound!.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Optimistic-concurrency precondition. A positive value requires the record's current
/// `version` to equal it exactly; zero means create-only and applies only when no live
/// record exists at the address.
typedef ExpectedVersion = int;

/// Stop being a face everywhere, and keep it. The face is taken off every persona
/// wearing it, left out of pickers and default listings, and cannot be worn until
/// reinstated; its values and its disclosure history are kept. The reversible middle
/// between 'not here' (a null persona/binding/set) and 'gone'
/// (persona/profile/delete).
class Payload {
  const Payload({
    required this.profileId,
    this.contextId,
    this.expectedVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        profileId: json['profileId'] as String,
        contextId: json['contextId'] as String?,
        expectedVersion: json['expectedVersion'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A pool face, or a context-local one.
  final Ulid profileId;

  /// The context a context-local face lives in. Omit for a pool face.
  final String? contextId;
  final ExpectedVersion? expectedVersion;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'profileId': profileId,
        if (contextId != null) 'contextId': contextId!,
        if (expectedVersion != null) 'expectedVersion': expectedVersion!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/persona/profile/retire/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/profile/retire/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/profile/retire/1.0","title":"Persona Profile Retire — payload","description":"Stop being a face everywhere, and keep it. The face is taken off every persona wearing it, left out of pickers and default listings, and cannot be worn until reinstated; its values and its disclosure history are kept. The reversible middle between \'not here\' (a null persona/binding/set) and \'gone\' (persona/profile/delete).","type":"object","additionalProperties":false,"required":["profileId"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid","description":"A pool face, or a context-local one."},"contextId":{"type":"string","minLength":1,"description":"The context a context-local face lives in. Omit for a pool face."},"expectedVersion":{"\$ref":"#/\$defs/ExpectedVersion"},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Profile Retire — response payload","description":"Success response to persona/profile/retire. Type https://trusttasks.org/spec/persona/profile/retire/1.0#response. Retiring a face already retired is a success that changes nothing and takes no new version, so a repeated retire converges.","type":"object","additionalProperties":false,"required":["profileId","version","retiredAt"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"version":{"\$ref":"#/\$defs/Version"},"retiredAt":{"type":"string","format":"date-time"},"unbound":{"type":"array","maxItems":256,"description":"Where the face was worn and no longer is. Absent when it was worn nowhere.","items":{"type":"object","additionalProperties":false,"required":["contextId","personaDid"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Version":{"title":"Version","description":"A value of the store\'s monotonic write counter. Server-assigned; a producer never chooses one.","type":"integer","minimum":1},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Profile Retire — response payload","description":"Success response to persona/profile/retire. Type https://trusttasks.org/spec/persona/profile/retire/1.0#response. Retiring a face already retired is a success that changes nothing and takes no new version, so a repeated retire converges.","type":"object","additionalProperties":false,"required":["profileId","version","retiredAt"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"version":{"\$ref":"#/\$defs/Version"},"retiredAt":{"type":"string","format":"date-time"},"unbound":{"type":"array","maxItems":256,"description":"Where the face was worn and no longer is. Absent when it was worn nowhere.","items":{"type":"object","additionalProperties":false,"required":["contextId","personaDid"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Version":{"title":"Version","description":"A value of the store\'s monotonic write counter. Server-assigned; a producer never chooses one.","type":"integer","minimum":1},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
