// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/renderers/list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseRenderersItem, generated from its schema.
class ResponseRenderersItem {
  const ResponseRenderersItem({
    required this.id,
    this.description,
    this.canonical,
    required this.drops,
    required this.canCarryPredicates,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseRenderersItem.fromJson(Map<String, dynamic> json) =>
      ResponseRenderersItem(
        id: json['id'] as String,
        description: json['description'] as String?,
        canonical: json['canonical'] as bool?,
        drops:
            (json['drops'] as List<dynamic>).map((e) => e as String).toList(),
        canCarryPredicates: json['canCarryPredicates'] as bool,
      );

  final String id;
  final String? description;

  /// True for the maintainer's lossless native form, used when a producer names no
  /// renderer. Exactly one renderer is canonical.
  final bool? canonical;

  /// What this format discards — commonly provenance, because most general-purpose
  /// contact formats have nowhere to put it. Declared here so that lossiness is
  /// negotiated rather than discovered by a holder afterwards.
  final List<String> drops;

  /// Whether the format can represent a claim proven rather than shown. Most cannot:
  /// there is no field in a general contact format for 'over the threshold'. A
  /// disclosure containing a predicate through a renderer that cannot carry one fails at
  /// negotiation rather than silently dropping the claim.
  final bool canCarryPredicates;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        if (description != null) 'description': description!,
        if (canonical != null) 'canonical': canonical!,
        'drops': drops,
        'canCarryPredicates': canCarryPredicates,
      };
}

/// Success response to persona/renderers/list. Type
/// https://trusttasks.org/spec/persona/renderers/list/1.0#response.
class Response {
  const Response({
    required this.renderers,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        renderers: (json['renderers'] as List<dynamic>)
            .map((e) =>
                ResponseRenderersItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<ResponseRenderersItem> renderers;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'renderers': renderers.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// Enumerate the output formats this agent can present in, and what each one discards.
/// A client cannot negotiate a format it cannot discover.
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/persona/renderers/list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/renderers/list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/renderers/list/1.0","title":"Persona Renderers List — payload","description":"Enumerate the output formats this agent can present in, and what each one discards. A client cannot negotiate a format it cannot discover.","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Renderers List — response payload","description":"Success response to persona/renderers/list. Type https://trusttasks.org/spec/persona/renderers/list/1.0#response.","type":"object","additionalProperties":false,"required":["renderers"],"properties":{"renderers":{"type":"array","maxItems":32,"items":{"type":"object","additionalProperties":false,"required":["id","drops","canCarryPredicates"],"properties":{"id":{"type":"string","maxLength":64},"description":{"type":"string","maxLength":512},"canonical":{"type":"boolean","description":"True for the maintainer\'s lossless native form, used when a producer names no renderer. Exactly one renderer is canonical."},"drops":{"type":"array","maxItems":32,"items":{"type":"string","maxLength":64},"description":"What this format discards — commonly provenance, because most general-purpose contact formats have nowhere to put it. Declared here so that lossiness is negotiated rather than discovered by a holder afterwards."},"canCarryPredicates":{"type":"boolean","description":"Whether the format can represent a claim proven rather than shown. Most cannot: there is no field in a general contact format for \'over the threshold\'. A disclosure containing a predicate through a renderer that cannot carry one fails at negotiation rather than silently dropping the claim."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Renderers List — response payload","description":"Success response to persona/renderers/list. Type https://trusttasks.org/spec/persona/renderers/list/1.0#response.","type":"object","additionalProperties":false,"required":["renderers"],"properties":{"renderers":{"type":"array","maxItems":32,"items":{"type":"object","additionalProperties":false,"required":["id","drops","canCarryPredicates"],"properties":{"id":{"type":"string","maxLength":64},"description":{"type":"string","maxLength":512},"canonical":{"type":"boolean","description":"True for the maintainer\'s lossless native form, used when a producer names no renderer. Exactly one renderer is canonical."},"drops":{"type":"array","maxItems":32,"items":{"type":"string","maxLength":64},"description":"What this format discards — commonly provenance, because most general-purpose contact formats have nowhere to put it. Declared here so that lossiness is negotiated rather than discovered by a holder afterwards."},"canCarryPredicates":{"type":"boolean","description":"Whether the format can represent a claim proven rather than shown. Most cannot: there is no field in a general contact format for \'over the threshold\'. A disclosure containing a predicate through a renderer that cannot carry one fails at negotiation rather than silently dropping the claim."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
