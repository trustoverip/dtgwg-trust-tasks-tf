// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/attribute/delete/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to persona/attribute/delete. Type
/// https://trusttasks.org/spec/persona/attribute/delete/1.0#response. Deleting an
/// attribute that is already absent is a success carrying `existed: false`, which is
/// what makes a repeated delete converge.
class Response {
  const Response({
    required this.attributeId,
    required this.existed,
    this.removedFromProfiles,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        attributeId: json['attributeId'] as String,
        existed: json['existed'] as bool,
        removedFromProfiles: json['removedFromProfiles'] == null
            ? null
            : (json['removedFromProfiles'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ulid attributeId;

  /// False when no live attribute was present. A repeat delete returns false and
  /// deliberately does NOT take a new counter value — had it done so, every consumer
  /// watching the store would see a change that did not happen, and delete could not be
  /// safely retried.
  final bool existed;

  /// Profiles whose entries were removed by a cascading delete. Present only when
  /// `cascade` was true and something was removed; a holder is owed the list of what
  /// their one action changed.
  final List<Ulid>? removedFromProfiles;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'attributeId': attributeId,
        'existed': existed,
        if (removedFromProfiles != null)
          'removedFromProfiles': removedFromProfiles!,
        if (ext != null) 'ext': ext!,
      };
}

/// Optimistic-concurrency precondition. A positive value requires the record's current
/// `version` to equal it exactly; zero means create-only and applies only when no live
/// record exists at the address.
typedef ExpectedVersion = int;

/// Remove one attribute from the holder's pool. Refused by default while any profile
/// still references it, because a silent removal would change what those profiles
/// present without the holder having said so.
class Payload {
  const Payload({
    required this.attributeId,
    this.cascade,
    this.expectedVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        attributeId: json['attributeId'] as String,
        cascade: json['cascade'] as bool?,
        expectedVersion: json['expectedVersion'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ulid attributeId;

  /// When false (the default) a referenced attribute is refused, and the error names the
  /// referring profiles so the holder can decide. When true the attribute is removed and
  /// every referring profile entry is removed with it. Cascade is the destructive option
  /// and is therefore never the default: deleting one fact and silently changing what
  /// three profiles disclose is exactly the surprise this family exists to prevent.
  final bool? cascade;

  /// Optional precondition, so a delete cannot race a concurrent edit the holder has not
  /// seen.
  final ExpectedVersion? expectedVersion;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'attributeId': attributeId,
        if (cascade != null) 'cascade': cascade!,
        if (expectedVersion != null) 'expectedVersion': expectedVersion!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/persona/attribute/delete/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/attribute/delete/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/attribute/delete/1.0","title":"Persona Attribute Delete — payload","description":"Remove one attribute from the holder\'s pool. Refused by default while any profile still references it, because a silent removal would change what those profiles present without the holder having said so.","type":"object","additionalProperties":false,"required":["attributeId"],"properties":{"attributeId":{"\$ref":"#/\$defs/Ulid"},"cascade":{"type":"boolean","default":false,"description":"When false (the default) a referenced attribute is refused, and the error names the referring profiles so the holder can decide. When true the attribute is removed and every referring profile entry is removed with it. Cascade is the destructive option and is therefore never the default: deleting one fact and silently changing what three profiles disclose is exactly the surprise this family exists to prevent."},"expectedVersion":{"\$ref":"#/\$defs/ExpectedVersion","description":"Optional precondition, so a delete cannot race a concurrent edit the holder has not seen."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Attribute Delete — response payload","description":"Success response to persona/attribute/delete. Type https://trusttasks.org/spec/persona/attribute/delete/1.0#response. Deleting an attribute that is already absent is a success carrying `existed: false`, which is what makes a repeated delete converge.","type":"object","additionalProperties":false,"required":["attributeId","existed"],"properties":{"attributeId":{"\$ref":"#/\$defs/Ulid"},"existed":{"type":"boolean","description":"False when no live attribute was present. A repeat delete returns false and deliberately does NOT take a new counter value — had it done so, every consumer watching the store would see a change that did not happen, and delete could not be safely retried."},"removedFromProfiles":{"type":"array","maxItems":256,"items":{"\$ref":"#/\$defs/Ulid"},"description":"Profiles whose entries were removed by a cascading delete. Present only when `cascade` was true and something was removed; a holder is owed the list of what their one action changed."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Attribute Delete — response payload","description":"Success response to persona/attribute/delete. Type https://trusttasks.org/spec/persona/attribute/delete/1.0#response. Deleting an attribute that is already absent is a success carrying `existed: false`, which is what makes a repeated delete converge.","type":"object","additionalProperties":false,"required":["attributeId","existed"],"properties":{"attributeId":{"\$ref":"#/\$defs/Ulid"},"existed":{"type":"boolean","description":"False when no live attribute was present. A repeat delete returns false and deliberately does NOT take a new counter value — had it done so, every consumer watching the store would see a change that did not happen, and delete could not be safely retried."},"removedFromProfiles":{"type":"array","maxItems":256,"items":{"\$ref":"#/\$defs/Ulid"},"description":"Profiles whose entries were removed by a cascading delete. Present only when `cascade` was true and something was removed; a holder is owed the list of what their one action changed."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
