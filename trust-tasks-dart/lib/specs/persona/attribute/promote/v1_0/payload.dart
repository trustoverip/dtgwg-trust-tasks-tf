// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/attribute/promote/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// A value of the store's monotonic write counter. Server-assigned; a producer never
/// chooses one.
typedef Version = int;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponsePromotedItem, generated from its schema.
class ResponsePromotedItem {
  const ResponsePromotedItem({
    required this.entry,
    required this.attributeId,
    required this.created,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponsePromotedItem.fromJson(Map<String, dynamic> json) =>
      ResponsePromotedItem(
        entry: json['entry'] as int,
        attributeId: json['attributeId'] as String,
        created: json['created'] as bool,
      );

  final int entry;
  final Ulid attributeId;
  final bool created;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entry': entry,
        'attributeId': attributeId,
        'created': created,
      };
}

/// Success response to persona/attribute/promote. Type
/// https://trusttasks.org/spec/persona/attribute/promote/1.0#response.
class Response {
  const Response({
    required this.profileId,
    required this.version,
    required this.promoted,
    this.reboundPersonaDids,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        profileId: json['profileId'] as String,
        version: json['version'] as int,
        promoted: (json['promoted'] as List<dynamic>)
            .map(
                (e) => ResponsePromotedItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        reboundPersonaDids: json['reboundPersonaDids'] == null
            ? null
            : (json['reboundPersonaDids'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Unchanged: the face keeps its id. It is now read through persona/profile/get.
  final Ulid profileId;

  /// The face's version in the pool.
  final Version version;

  /// For each promoted position, in request order, the pool attribute the entry now
  /// references and whether this promote created it. `created: false` means the holder
  /// already kept exactly this value, and the face now shares it with whatever else
  /// presents it.
  final List<ResponsePromotedItem> promoted;

  /// Personas in `contextId` that wore the local face and now wear it from the pool.
  /// What they present is unchanged. Absent when none did.
  final List<String>? reboundPersonaDids;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'profileId': profileId,
        'version': version,
        'promoted': promoted.map((e) => e.toJson()).toList(),
        if (reboundPersonaDids != null)
          'reboundPersonaDids': reboundPersonaDids!,
        if (ext != null) 'ext': ext!,
      };
}

/// Make values a context-local face carries reusable: each named entry's value becomes
/// a pool attribute, and the face — which can no longer live inside one context once
/// it references the pool — moves above contexts, keeping its id and staying worn
/// wherever it was. One-way.
class Payload {
  const Payload({
    required this.contextId,
    required this.profileId,
    required this.entries,
    required this.expectedVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String,
        profileId: json['profileId'] as String,
        entries:
            (json['entries'] as List<dynamic>).map((e) => e as int).toList(),
        expectedVersion: json['expectedVersion'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The context the local face lives in.
  final String contextId;

  /// The context-local face.
  final Ulid profileId;

  /// Zero-based positions, in the face's `entries`, of the values to promote. Positions
  /// are meaningful only against the version the producer read, which is why
  /// `expectedVersion` is required.
  final List<int> entries;

  /// The local face's version as the producer read it. Required: a position into a face
  /// edited since would promote a different value than the holder chose.
  final Version expectedVersion;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'profileId': profileId,
        'entries': entries,
        'expectedVersion': expectedVersion,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/persona/attribute/promote/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/attribute/promote/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/attribute/promote/1.0","title":"Persona Attribute Promote — payload","description":"Make values a context-local face carries reusable: each named entry\'s value becomes a pool attribute, and the face — which can no longer live inside one context once it references the pool — moves above contexts, keeping its id and staying worn wherever it was. One-way.","type":"object","additionalProperties":false,"required":["contextId","profileId","entries","expectedVersion"],"properties":{"contextId":{"type":"string","minLength":1,"description":"The context the local face lives in."},"profileId":{"\$ref":"#/\$defs/Ulid","description":"The context-local face."},"entries":{"type":"array","minItems":1,"maxItems":64,"uniqueItems":true,"items":{"type":"integer","minimum":0},"description":"Zero-based positions, in the face\'s `entries`, of the values to promote. Positions are meaningful only against the version the producer read, which is why `expectedVersion` is required."},"expectedVersion":{"\$ref":"#/\$defs/Version","description":"The local face\'s version as the producer read it. Required: a position into a face edited since would promote a different value than the holder chose."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Attribute Promote — response payload","description":"Success response to persona/attribute/promote. Type https://trusttasks.org/spec/persona/attribute/promote/1.0#response.","type":"object","additionalProperties":false,"required":["profileId","version","promoted"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid","description":"Unchanged: the face keeps its id. It is now read through persona/profile/get."},"version":{"\$ref":"#/\$defs/Version","description":"The face\'s version in the pool."},"promoted":{"type":"array","minItems":1,"maxItems":64,"description":"For each promoted position, in request order, the pool attribute the entry now references and whether this promote created it. `created: false` means the holder already kept exactly this value, and the face now shares it with whatever else presents it.","items":{"type":"object","additionalProperties":false,"required":["entry","attributeId","created"],"properties":{"entry":{"type":"integer","minimum":0},"attributeId":{"\$ref":"#/\$defs/Ulid"},"created":{"type":"boolean"}}}},"reboundPersonaDids":{"type":"array","maxItems":256,"items":{"type":"string","minLength":1},"description":"Personas in `contextId` that wore the local face and now wear it from the pool. What they present is unchanged. Absent when none did."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"Version":{"title":"Version","description":"A value of the store\'s monotonic write counter. Server-assigned; a producer never chooses one.","type":"integer","minimum":1}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Attribute Promote — response payload","description":"Success response to persona/attribute/promote. Type https://trusttasks.org/spec/persona/attribute/promote/1.0#response.","type":"object","additionalProperties":false,"required":["profileId","version","promoted"],"properties":{"profileId":{"\$ref":"#/\$defs/Ulid","description":"Unchanged: the face keeps its id. It is now read through persona/profile/get."},"version":{"\$ref":"#/\$defs/Version","description":"The face\'s version in the pool."},"promoted":{"type":"array","minItems":1,"maxItems":64,"description":"For each promoted position, in request order, the pool attribute the entry now references and whether this promote created it. `created: false` means the holder already kept exactly this value, and the face now shares it with whatever else presents it.","items":{"type":"object","additionalProperties":false,"required":["entry","attributeId","created"],"properties":{"entry":{"type":"integer","minimum":0},"attributeId":{"\$ref":"#/\$defs/Ulid"},"created":{"type":"boolean"}}}},"reboundPersonaDids":{"type":"array","maxItems":256,"items":{"type":"string","minLength":1},"description":"Personas in `contextId` that wore the local face and now wear it from the pool. What they present is unchanged. Absent when none did."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"Version":{"title":"Version","description":"A value of the store\'s monotonic write counter. Server-assigned; a producer never chooses one.","type":"integer","minimum":1}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
