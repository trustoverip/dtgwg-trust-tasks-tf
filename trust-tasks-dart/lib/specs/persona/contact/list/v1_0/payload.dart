// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/contact/list/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseContactsItem, generated from its schema.
class ResponseContactsItem {
  const ResponseContactsItem({
    required this.contactId,
    required this.subjectDid,
    required this.knownByPersona,
    required this.rev,
    this.displayName,
    this.claimCount,
    required this.receivedAt,
    this.hasUnreviewedChange,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseContactsItem.fromJson(Map<String, dynamic> json) =>
      ResponseContactsItem(
        contactId: json['contactId'] as String,
        subjectDid: json['subjectDid'] as String,
        knownByPersona: json['knownByPersona'] as String,
        rev: json['rev'] as int,
        displayName: json['displayName'] as String?,
        claimCount: json['claimCount'] as int?,
        receivedAt: json['receivedAt'] as String,
        hasUnreviewedChange: json['hasUnreviewedChange'] as bool?,
      );

  final Ulid contactId;
  final String subjectDid;
  final String knownByPersona;
  final int rev;

  /// A rendering hint drawn from the contact's own disclosed display-name claim, so a
  /// list is legible. Absent when they disclosed none; a producer MUST NOT substitute
  /// the DID silently as though it were a name.
  final String? displayName;
  final int? claimCount;
  final String receivedAt;

  /// True when the current revision superseded another and the holder has not yet
  /// acknowledged the difference. This is what lets a producer badge a contact whose
  /// payment address moved — the whole point of keeping revisions.
  final bool? hasUnreviewedChange;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contactId': contactId,
        'subjectDid': subjectDid,
        'knownByPersona': knownByPersona,
        'rev': rev,
        if (displayName != null) 'displayName': displayName!,
        if (claimCount != null) 'claimCount': claimCount!,
        'receivedAt': receivedAt,
        if (hasUnreviewedChange != null)
          'hasUnreviewedChange': hasUnreviewedChange!,
      };
}

/// Success response to persona/contact/list. Type
/// https://trusttasks.org/spec/persona/contact/list/1.0#response. Summaries carry no
/// claim values: a list is for finding a contact, and finding one does not require
/// disclosing every contact's details.
class Response {
  const Response({
    required this.contacts,
    this.nextCursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        contacts: (json['contacts'] as List<dynamic>)
            .map(
                (e) => ResponseContactsItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<ResponseContactsItem> contacts;
  final String? nextCursor;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contacts': contacts.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// Enumerate contacts in a context, optionally narrowed to those known by one persona.
/// Summaries only; documents are fetched one at a time.
class Payload {
  const Payload({
    required this.contextId,
    this.knownByPersona,
    this.changedSince,
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String,
        knownByPersona: json['knownByPersona'] as String?,
        changedSince: json['changedSince'] as String?,
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String contextId;

  /// Narrow to contacts known by this persona. Omitting it lists the context's contacts
  /// across every persona in it, which a producer SHOULD offer deliberately rather than
  /// by default — the unfiltered view is the one that shows the holder's personas side
  /// by side.
  final String? knownByPersona;

  /// Return only contacts whose current revision was received after this instant. The
  /// cheap way to answer "what changed while I was away", which is the question the
  /// revision history exists to make answerable.
  final String? changedSince;
  final String? cursor;
  final int? limit;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        if (knownByPersona != null) 'knownByPersona': knownByPersona!,
        if (changedSince != null) 'changedSince': changedSince!,
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/persona/contact/list/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/contact/list/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/contact/list/1.0","title":"Persona Contact List — payload","description":"Enumerate contacts in a context, optionally narrowed to those known by one persona. Summaries only; documents are fetched one at a time.","type":"object","additionalProperties":false,"required":["contextId"],"properties":{"contextId":{"type":"string","minLength":1},"knownByPersona":{"type":"string","maxLength":2048,"description":"Narrow to contacts known by this persona. Omitting it lists the context\'s contacts across every persona in it, which a producer SHOULD offer deliberately rather than by default — the unfiltered view is the one that shows the holder\'s personas side by side."},"changedSince":{"type":"string","format":"date-time","description":"Return only contacts whose current revision was received after this instant. The cheap way to answer \\"what changed while I was away\\", which is the question the revision history exists to make answerable."},"cursor":{"type":"string","maxLength":4096},"limit":{"type":"integer","minimum":1,"maximum":500,"default":100},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Contact List — response payload","description":"Success response to persona/contact/list. Type https://trusttasks.org/spec/persona/contact/list/1.0#response. Summaries carry no claim values: a list is for finding a contact, and finding one does not require disclosing every contact\'s details.","type":"object","additionalProperties":false,"required":["contacts"],"properties":{"contacts":{"type":"array","maxItems":500,"items":{"type":"object","additionalProperties":false,"required":["contactId","subjectDid","knownByPersona","rev","receivedAt"],"properties":{"contactId":{"\$ref":"#/\$defs/Ulid"},"subjectDid":{"type":"string","minLength":1},"knownByPersona":{"type":"string","minLength":1},"rev":{"type":"integer","minimum":1},"displayName":{"type":"string","maxLength":256,"description":"A rendering hint drawn from the contact\'s own disclosed display-name claim, so a list is legible. Absent when they disclosed none; a producer MUST NOT substitute the DID silently as though it were a name."},"claimCount":{"type":"integer","minimum":0},"receivedAt":{"type":"string","format":"date-time"},"hasUnreviewedChange":{"type":"boolean","description":"True when the current revision superseded another and the holder has not yet acknowledged the difference. This is what lets a producer badge a contact whose payment address moved — the whole point of keeping revisions."}}}},"nextCursor":{"type":"string","maxLength":4096},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Contact List — response payload","description":"Success response to persona/contact/list. Type https://trusttasks.org/spec/persona/contact/list/1.0#response. Summaries carry no claim values: a list is for finding a contact, and finding one does not require disclosing every contact\'s details.","type":"object","additionalProperties":false,"required":["contacts"],"properties":{"contacts":{"type":"array","maxItems":500,"items":{"type":"object","additionalProperties":false,"required":["contactId","subjectDid","knownByPersona","rev","receivedAt"],"properties":{"contactId":{"\$ref":"#/\$defs/Ulid"},"subjectDid":{"type":"string","minLength":1},"knownByPersona":{"type":"string","minLength":1},"rev":{"type":"integer","minimum":1},"displayName":{"type":"string","maxLength":256,"description":"A rendering hint drawn from the contact\'s own disclosed display-name claim, so a list is legible. Absent when they disclosed none; a producer MUST NOT substitute the DID silently as though it were a name."},"claimCount":{"type":"integer","minimum":0},"receivedAt":{"type":"string","format":"date-time"},"hasUnreviewedChange":{"type":"boolean","description":"True when the current revision superseded another and the holder has not yet acknowledged the difference. This is what lets a producer badge a contact whose payment address moved — the whole point of keeping revisions."}}}},"nextCursor":{"type":"string","maxLength":4096},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
