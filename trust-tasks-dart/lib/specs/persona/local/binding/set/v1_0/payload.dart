// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/local/binding/set/1.0/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// A value of the store's monotonic write counter. Server-assigned; a producer never
/// chooses one.
typedef Version = int;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Success response to persona/local/binding/set. Type
/// https://trusttasks.org/spec/persona/local/binding/set/1.0#response.
class Response {
  const Response({
    required this.contextId,
    required this.personaDid,
    this.profileId,
    required this.version,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        contextId: json['contextId'] as String,
        personaDid: json['personaDid'] as String,
        profileId: json['profileId'],
        version: json['version'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String contextId;
  final String personaDid;
  final Object? profileId;
  final Version version;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'personaDid': personaDid,
        if (profileId != null) 'profileId': profileId!,
        'version': version,
        if (ext != null) 'ext': ext!,
      };
}

/// Optimistic-concurrency precondition. A positive value requires the record's current
/// `version` to equal it exactly; zero means create-only and applies only when no live
/// record exists at the address.
typedef ExpectedVersion = int;

/// Bind a context-local profile to a persona in the same context. Safely
/// context-callable — unlike persona/binding/set — precisely because both objects it
/// names live below the boundary and neither reaches the holder's pool.
class Payload {
  const Payload({
    required this.contextId,
    required this.personaDid,
    this.profileId,
    this.label,
    this.expectedVersion,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        contextId: json['contextId'] as String,
        personaDid: json['personaDid'] as String,
        profileId: json['profileId'],
        label: json['label'] as String?,
        expectedVersion: json['expectedVersion'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String contextId;
  final String personaDid;

  /// A LOCAL profile, or null to clear. A maintainer MUST refuse an identifier that
  /// names a pool profile: honouring it would let a context-scoped caller bind the
  /// holder's composition, which is the one escalation the whole boundary exists to
  /// prevent.
  final Object? profileId;

  /// What this context may call the face the persona wears here, chosen by the holder
  /// for this context. Returned by persona/binding/get and persona/binding/list in place
  /// of the holder's own name for the face, which is theirs and may say far more than
  /// they would tell a context ('the divorce'). Omit to give the context no name at all.
  final String? label;
  final ExpectedVersion? expectedVersion;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'contextId': contextId,
        'personaDid': personaDid,
        if (profileId != null) 'profileId': profileId!,
        if (label != null) 'label': label!,
        if (expectedVersion != null) 'expectedVersion': expectedVersion!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/persona/local/binding/set/1.0';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/local/binding/set/1.0#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/local/binding/set/1.0","title":"Persona Local Binding Set — payload","description":"Bind a context-local profile to a persona in the same context. Safely context-callable — unlike persona/binding/set — precisely because both objects it names live below the boundary and neither reaches the holder\'s pool.","type":"object","additionalProperties":false,"required":["contextId","personaDid"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1,"maxLength":2048},"profileId":{"oneOf":[{"\$ref":"#/\$defs/Ulid"},{"type":"null"}],"description":"A LOCAL profile, or null to clear. A maintainer MUST refuse an identifier that names a pool profile: honouring it would let a context-scoped caller bind the holder\'s composition, which is the one escalation the whole boundary exists to prevent."},"label":{"type":"string","minLength":1,"maxLength":128,"description":"What this context may call the face the persona wears here, chosen by the holder for this context. Returned by persona/binding/get and persona/binding/list in place of the holder\'s own name for the face, which is theirs and may say far more than they would tell a context (\'the divorce\'). Omit to give the context no name at all."},"expectedVersion":{"\$ref":"#/\$defs/ExpectedVersion"},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Local Binding Set — response payload","description":"Success response to persona/local/binding/set. Type https://trusttasks.org/spec/persona/local/binding/set/1.0#response.","type":"object","additionalProperties":false,"required":["contextId","personaDid","version"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1},"profileId":{"oneOf":[{"\$ref":"#/\$defs/Ulid"},{"type":"null"}]},"version":{"\$ref":"#/\$defs/Version"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Version":{"title":"Version","description":"A value of the store\'s monotonic write counter. Server-assigned; a producer never chooses one.","type":"integer","minimum":1},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Local Binding Set — response payload","description":"Success response to persona/local/binding/set. Type https://trusttasks.org/spec/persona/local/binding/set/1.0#response.","type":"object","additionalProperties":false,"required":["contextId","personaDid","version"],"properties":{"contextId":{"type":"string","minLength":1},"personaDid":{"type":"string","minLength":1},"profileId":{"oneOf":[{"\$ref":"#/\$defs/Ulid"},{"type":"null"}]},"version":{"\$ref":"#/\$defs/Version"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Version":{"title":"Version","description":"A value of the store\'s monotonic write counter. Server-assigned; a producer never chooses one.","type":"integer","minimum":1},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ExpectedVersion":{"title":"ExpectedVersion","description":"Optimistic-concurrency precondition. A positive value requires the record\'s current `version` to equal it exactly; zero means create-only and applies only when no live record exists at the address.","type":"integer","minimum":0}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
