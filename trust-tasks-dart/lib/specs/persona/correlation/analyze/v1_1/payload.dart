// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/persona/correlation/analyze/1.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`.
/// Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered
/// scan of the store is also creation-ordered and a `list` needs no secondary sort.
/// Server-assigned on create; a producer MAY supply one to make a create idempotent,
/// and a maintainer MUST reject a supplied value that already exists rather than
/// silently overwriting.
typedef Ulid = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseFindingsItemSeverity is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseFindingsItemSeverity(String value) {
  static const ResponseFindingsItemSeverity low =
      ResponseFindingsItemSeverity('low');
  static const ResponseFindingsItemSeverity high =
      ResponseFindingsItemSeverity('high');

  /// Every value this specification's schema permits.
  static const List<ResponseFindingsItemSeverity> values =
      <ResponseFindingsItemSeverity>[low, high];
}

/// ResponseFindingsItemSharedWithItem, generated from its schema.
class ResponseFindingsItemSharedWithItem {
  const ResponseFindingsItemSharedWithItem({
    this.profileId,
    this.contextId,
    this.personaDid,
    this.disclosedTo,
    this.worldId,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseFindingsItemSharedWithItem.fromJson(
          Map<String, dynamic> json) =>
      ResponseFindingsItemSharedWithItem(
        profileId: json['profileId'] as String?,
        contextId: json['contextId'] as String?,
        personaDid: json['personaDid'] as String?,
        disclosedTo: json['disclosedTo'] == null
            ? null
            : (json['disclosedTo'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        worldId: json['worldId'] as String?,
      );

  final Ulid? profileId;
  final String? contextId;
  final String? personaDid;
  final List<String>? disclosedTo;

  /// The world the profile at this location belongs to. Absent where it belongs to none,
  /// which is a real and common state rather than a gap — most profiles are unarranged
  /// until someone arranges them, and a consumer MUST NOT read absence as a world of its
  /// own.
  final Ulid? worldId;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (profileId != null) 'profileId': profileId!,
        if (contextId != null) 'contextId': contextId!,
        if (personaDid != null) 'personaDid': personaDid!,
        if (disclosedTo != null) 'disclosedTo': disclosedTo!,
        if (worldId != null) 'worldId': worldId!,
      };
}

/// ResponseFindingsItemRemediesItem is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseFindingsItemRemediesItem(String value) {
  static const ResponseFindingsItemRemediesItem useDifferentValue =
      ResponseFindingsItemRemediesItem('useDifferentValue');
  static const ResponseFindingsItemRemediesItem reissueCredentialToThisDid =
      ResponseFindingsItemRemediesItem('reissueCredentialToThisDid');
  static const ResponseFindingsItemRemediesItem correlateDeliberately =
      ResponseFindingsItemRemediesItem('correlateDeliberately');
  static const ResponseFindingsItemRemediesItem proceedAndRecord =
      ResponseFindingsItemRemediesItem('proceedAndRecord');

  /// Every value this specification's schema permits.
  static const List<ResponseFindingsItemRemediesItem> values =
      <ResponseFindingsItemRemediesItem>[
    useDifferentValue,
    reissueCredentialToThisDid,
    correlateDeliberately,
    proceedAndRecord
  ];
}

/// ResponseFindingsItem, generated from its schema.
class ResponseFindingsItem {
  const ResponseFindingsItem({
    this.attributeId,
    required this.severity,
    this.sharedWith,
    required this.why,
    required this.remedies,
    this.crossesWorlds,
    this.worldIds,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseFindingsItem.fromJson(Map<String, dynamic> json) =>
      ResponseFindingsItem(
        attributeId: json['attributeId'] as String?,
        severity: ResponseFindingsItemSeverity(json['severity'] as String),
        sharedWith: json['sharedWith'] == null
            ? null
            : (json['sharedWith'] as List<dynamic>)
                .map((e) => ResponseFindingsItemSharedWithItem.fromJson(
                    e as Map<String, dynamic>))
                .toList(),
        why: json['why'] as String,
        remedies: (json['remedies'] as List<dynamic>)
            .map((e) => ResponseFindingsItemRemediesItem(e as String))
            .toList(),
        crossesWorlds: json['crossesWorlds'] as bool?,
        worldIds: json['worldIds'] == null
            ? null
            : (json['worldIds'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
      );

  final Ulid? attributeId;
  final ResponseFindingsItemSeverity severity;

  /// Where else this value appears, and where it has actually gone. Identifiers ARE
  /// returned here, unlike the counts the write tasks give back — this task is
  /// holder-authorized and its whole purpose is to let the holder act, which they cannot
  /// do on a number.
  final List<ResponseFindingsItemSharedWithItem>? sharedWith;

  /// Plain-language cause. A severity with no explanation is a warning a holder learns
  /// to dismiss.
  final String why;

  /// What the holder can actually do. Naming reissueCredentialToThisDid matters more
  /// than it looks: without it, a holder told 'this links your personas' has no action
  /// but to abandon the attribute, and the honest fix — a credential re-issued against
  /// the persona actually using it — stays invisible unless the analysis names it.
  final List<ResponseFindingsItemRemediesItem> remedies;

  /// Whether this linkage spans two or more of the holder's worlds — parts of their life
  /// they have said belong apart. **A second axis, not a restatement of `severity`.**
  /// `severity` says how strongly a disclosure would link the holder, which is a fact
  /// about provenance and proof rung and is true whatever the holder intended; this says
  /// whether the holder would mind. A value shared between two profiles in the SAME
  /// world is linkage the holder arranged on purpose — a work email in every work
  /// profile — and a consumer that alarmed on it teaches people to dismiss the alarm. A
  /// value shared ACROSS worlds is the finding worth raising. True only when two or more
  /// DISTINCT worlds appear among `sharedWith`: a profile belonging to no world is
  /// unarranged, not a second world, and contributes nothing here. Absent when the
  /// maintainer does not implement worlds.
  final bool? crossesWorlds;

  /// The distinct worlds this linkage touches, so a consumer can name them — "Work and
  /// Home share your mobile number" is a sentence a holder can act on, where "a value is
  /// shared" is not. Identifiers rather than names, on the same reasoning as every other
  /// member here: the caller already holds the world records and a name repeated on the
  /// wire is a second copy to keep correct.
  final List<Ulid>? worldIds;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (attributeId != null) 'attributeId': attributeId!,
        'severity': severity.value,
        if (sharedWith != null)
          'sharedWith': sharedWith!.map((e) => e.toJson()).toList(),
        'why': why,
        'remedies': remedies.map((e) => e.value).toList(),
        if (crossesWorlds != null) 'crossesWorlds': crossesWorlds!,
        if (worldIds != null) 'worldIds': worldIds!,
      };
}

/// Success response to persona/correlation/analyze. Type
/// https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.
class Response {
  const Response({
    required this.findings,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        findings: (json['findings'] as List<dynamic>)
            .map(
                (e) => ResponseFindingsItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<ResponseFindingsItem> findings;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'findings': findings.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// The JSON shape of `value`, declared so that a consumer can render and compare
/// without guessing. The maintainer validates that `value` agrees with this member and
/// does nothing further: it does NOT validate a phone number against a phone-number
/// grammar. That is a producer's affordance, and a store that grows opinions about the
/// contents of its records eventually blocks its consumer's release.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ValueType(String value) {
  static const ValueType string = ValueType('string');
  static const ValueType number = ValueType('number');
  static const ValueType boolean = ValueType('boolean');
  static const ValueType date = ValueType('date');
  static const ValueType object = ValueType('object');

  /// Every value this specification's schema permits.
  static const List<ValueType> values = <ValueType>[
    string,
    number,
    boolean,
    date,
    object
  ];
}

/// The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`,
/// `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a
/// consumer with no knowledge of the specific token can still group by its prefix. The
/// token is the maintainer's own; no external vocabulary is primary. External
/// vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied
/// at PRESENTATION by a renderer, not at rest, so that a query written in any of them
/// can be matched without the store having to live inside any one of them. The `x:`
/// prefix is an open extension namespace and is not decoration. The closest prior art
/// — Windows CardSpace's self-issued card — supported exactly fifteen predefined claim
/// types with no extensibility, and that is the specific way it failed the requirement
/// a holder actually has. An `x:` attribute stores, composes, binds and discloses
/// exactly like a known one; it renders generically and matches only an explicit
/// query.
typedef ClaimType = String;

/// Analyse a value the holder is CONSIDERING but has not written. This is what lets a
/// builder warn before the mistake rather than after it, which is the difference
/// between a guard and a report. A maintainer MUST NOT store a candidate.
class PayloadCandidate {
  const PayloadCandidate({
    required this.type,
    required this.valueType,
    required this.value,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadCandidate.fromJson(Map<String, dynamic> json) =>
      PayloadCandidate(
        type: json['type'] as String,
        valueType: ValueType(json['valueType'] as String),
        value: json['value'],
      );

  final ClaimType type;
  final ValueType valueType;
  final Object? value;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'valueType': valueType.value,
        'value': value,
      };
}

/// Report where the holder's identities link to one another, and what can be done
/// about each link. The findings task behind the counts the write tasks return.
class Payload {
  const Payload({
    this.attributeId,
    this.profileId,
    this.candidate,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        attributeId: json['attributeId'] as String?,
        profileId: json['profileId'] as String?,
        candidate: json['candidate'] == null
            ? null
            : PayloadCandidate.fromJson(
                json['candidate'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Analyse one attribute. Omit to analyse the whole pool.
  final Ulid? attributeId;

  /// Analyse one composition — what would this profile link if bound.
  final Ulid? profileId;

  /// Analyse a value the holder is CONSIDERING but has not written. This is what lets a
  /// builder warn before the mistake rather than after it, which is the difference
  /// between a guard and a report. A maintainer MUST NOT store a candidate.
  final PayloadCandidate? candidate;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (attributeId != null) 'attributeId': attributeId!,
        if (profileId != null) 'profileId': profileId!,
        if (candidate != null) 'candidate': candidate!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/persona/correlation/analyze/1.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/persona/correlation/analyze/1.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/persona/correlation/analyze/1.1","title":"Persona Correlation Analyze — payload","description":"Report where the holder\'s identities link to one another, and what can be done about each link. The findings task behind the counts the write tasks return.","type":"object","additionalProperties":false,"properties":{"attributeId":{"\$ref":"#/\$defs/Ulid","description":"Analyse one attribute. Omit to analyse the whole pool."},"profileId":{"\$ref":"#/\$defs/Ulid","description":"Analyse one composition — what would this profile link if bound."},"candidate":{"type":"object","additionalProperties":false,"required":["type","valueType","value"],"description":"Analyse a value the holder is CONSIDERING but has not written. This is what lets a builder warn before the mistake rather than after it, which is the difference between a guard and a report. A maintainer MUST NOT store a candidate.","properties":{"type":{"\$ref":"#/\$defs/ClaimType"},"valueType":{"\$ref":"#/\$defs/ValueType"},"value":{}}},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Persona Correlation Analyze — response payload","description":"Success response to persona/correlation/analyze. Type https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.","type":"object","additionalProperties":false,"required":["findings"],"properties":{"findings":{"type":"array","maxItems":256,"items":{"type":"object","additionalProperties":false,"required":["severity","why","remedies"],"properties":{"attributeId":{"\$ref":"#/\$defs/Ulid"},"severity":{"type":"string","enum":["low","high"]},"sharedWith":{"type":"array","maxItems":128,"description":"Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.","items":{"type":"object","additionalProperties":false,"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"contextId":{"type":"string"},"personaDid":{"type":"string"},"disclosedTo":{"type":"array","maxItems":64,"items":{"type":"string"}},"worldId":{"\$ref":"#/\$defs/Ulid","description":"The world the profile at this location belongs to. Absent where it belongs to none, which is a real and common state rather than a gap — most profiles are unarranged until someone arranges them, and a consumer MUST NOT read absence as a world of its own."}}}},"why":{"type":"string","maxLength":1024,"description":"Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss."},"remedies":{"type":"array","maxItems":8,"items":{"type":"string","enum":["useDifferentValue","reissueCredentialToThisDid","correlateDeliberately","proceedAndRecord"]},"description":"What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told \'this links your personas\' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it."},"crossesWorlds":{"type":"boolean","description":"Whether this linkage spans two or more of the holder\'s worlds — parts of their life they have said belong apart. **A second axis, not a restatement of `severity`.** `severity` says how strongly a disclosure would link the holder, which is a fact about provenance and proof rung and is true whatever the holder intended; this says whether the holder would mind. A value shared between two profiles in the SAME world is linkage the holder arranged on purpose — a work email in every work profile — and a consumer that alarmed on it teaches people to dismiss the alarm. A value shared ACROSS worlds is the finding worth raising. True only when two or more DISTINCT worlds appear among `sharedWith`: a profile belonging to no world is unarranged, not a second world, and contributes nothing here. Absent when the maintainer does not implement worlds."},"worldIds":{"type":"array","maxItems":64,"uniqueItems":true,"items":{"\$ref":"#/\$defs/Ulid"},"description":"The distinct worlds this linkage touches, so a consumer can name them — \\"Work and Home share your mobile number\\" is a sentence a holder can act on, where \\"a value is shared\\" is not. Identifiers rather than names, on the same reasoning as every other member here: the caller already holds the world records and a name repeated on the wire is a second copy to keep correct."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ValueType":{"title":"ValueType","description":"The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer\'s affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer\'s release.","type":"string","enum":["string","number","boolean","date","object"]},"ClaimType":{"title":"ClaimType","description":"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer\'s own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace\'s self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.","type":"string","minLength":1,"maxLength":128,"pattern":"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*\$"}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Persona Correlation Analyze — response payload","description":"Success response to persona/correlation/analyze. Type https://trusttasks.org/spec/persona/correlation/analyze/1.0#response.","type":"object","additionalProperties":false,"required":["findings"],"properties":{"findings":{"type":"array","maxItems":256,"items":{"type":"object","additionalProperties":false,"required":["severity","why","remedies"],"properties":{"attributeId":{"\$ref":"#/\$defs/Ulid"},"severity":{"type":"string","enum":["low","high"]},"sharedWith":{"type":"array","maxItems":128,"description":"Where else this value appears, and where it has actually gone. Identifiers ARE returned here, unlike the counts the write tasks give back — this task is holder-authorized and its whole purpose is to let the holder act, which they cannot do on a number.","items":{"type":"object","additionalProperties":false,"properties":{"profileId":{"\$ref":"#/\$defs/Ulid"},"contextId":{"type":"string"},"personaDid":{"type":"string"},"disclosedTo":{"type":"array","maxItems":64,"items":{"type":"string"}},"worldId":{"\$ref":"#/\$defs/Ulid","description":"The world the profile at this location belongs to. Absent where it belongs to none, which is a real and common state rather than a gap — most profiles are unarranged until someone arranges them, and a consumer MUST NOT read absence as a world of its own."}}}},"why":{"type":"string","maxLength":1024,"description":"Plain-language cause. A severity with no explanation is a warning a holder learns to dismiss."},"remedies":{"type":"array","maxItems":8,"items":{"type":"string","enum":["useDifferentValue","reissueCredentialToThisDid","correlateDeliberately","proceedAndRecord"]},"description":"What the holder can actually do. Naming reissueCredentialToThisDid matters more than it looks: without it, a holder told \'this links your personas\' has no action but to abandon the attribute, and the honest fix — a credential re-issued against the persona actually using it — stays invisible unless the analysis names it."},"crossesWorlds":{"type":"boolean","description":"Whether this linkage spans two or more of the holder\'s worlds — parts of their life they have said belong apart. **A second axis, not a restatement of `severity`.** `severity` says how strongly a disclosure would link the holder, which is a fact about provenance and proof rung and is true whatever the holder intended; this says whether the holder would mind. A value shared between two profiles in the SAME world is linkage the holder arranged on purpose — a work email in every work profile — and a consumer that alarmed on it teaches people to dismiss the alarm. A value shared ACROSS worlds is the finding worth raising. True only when two or more DISTINCT worlds appear among `sharedWith`: a profile belonging to no world is unarranged, not a second world, and contributes nothing here. Absent when the maintainer does not implement worlds."},"worldIds":{"type":"array","maxItems":64,"uniqueItems":true,"items":{"\$ref":"#/\$defs/Ulid"},"description":"The distinct worlds this linkage touches, so a consumer can name them — \\"Work and Home share your mobile number\\" is a sentence a holder can act on, where \\"a value is shared\\" is not. Identifiers rather than names, on the same reasoning as every other member here: the caller already holds the world records and a name repeated on the wire is a second copy to keep correct."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Ulid":{"title":"Ulid","description":"A ULID in Crockford base32, uppercase. Used for `attributeId` and `profileId`. Chosen over a UUID because the leading 48 bits are a timestamp, so a key-ordered scan of the store is also creation-ordered and a `list` needs no secondary sort. Server-assigned on create; a producer MAY supply one to make a create idempotent, and a maintainer MUST reject a supplied value that already exists rather than silently overwriting.","type":"string","pattern":"^[0-9A-HJKMNP-TV-Z]{26}\$"},"ValueType":{"title":"ValueType","description":"The JSON shape of `value`, declared so that a consumer can render and compare without guessing. The maintainer validates that `value` agrees with this member and does nothing further: it does NOT validate a phone number against a phone-number grammar. That is a producer\'s affordance, and a store that grows opinions about the contents of its records eventually blocks its consumer\'s release.","type":"string","enum":["string","number","boolean","date","object"]},"ClaimType":{"title":"ClaimType","description":"The vocabulary token naming what a value IS — `name.legal`, `phone.mobile`, `address.postal`, `person.birthDate`. Dotted, most-general segment first, so that a consumer with no knowledge of the specific token can still group by its prefix.\\n\\nThe token is the maintainer\'s own; no external vocabulary is primary. External vocabularies (vCard/jCard, OIDC standard claims, schema.org) are mappings applied at PRESENTATION by a renderer, not at rest, so that a query written in any of them can be matched without the store having to live inside any one of them.\\n\\nThe `x:` prefix is an open extension namespace and is not decoration. The closest prior art — Windows CardSpace\'s self-issued card — supported exactly fifteen predefined claim types with no extensibility, and that is the specific way it failed the requirement a holder actually has. An `x:` attribute stores, composes, binds and discloses exactly like a known one; it renders generically and matches only an explicit query.","type":"string","minLength":1,"maxLength":128,"pattern":"^(x:)?[a-z][a-zA-Z0-9]*(\\\\.[a-z][a-zA-Z0-9]*)*\$"}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
