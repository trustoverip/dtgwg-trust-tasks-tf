// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/registry/recognition/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Optional TRQP query context. `time` requests evaluation as of a given instant;
/// `locator` is an authority-defined hint for locating records.
class QueryContext {
  const QueryContext({
    this.time,
    this.locator,
  });

  /// Read this payload from a decoded JSON object.
  factory QueryContext.fromJson(Map<String, dynamic> json) => QueryContext(
        time: json['time'] as String?,
        locator: json['locator'] as String?,
      );

  /// Evaluate the query as of this RFC3339 (Z offset) instant. Blank/absent uses current
  /// server time.
  final String? time;

  /// Optional hint for systems that need extra information to locate the records in
  /// question (authorization queries).
  final String? locator;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (time != null) 'time': time!,
        if (locator != null) 'locator': locator!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Registry Recognition — response payload
class Response {
  const Response({
    required this.entityId,
    required this.authorityId,
    required this.action,
    required this.resource,
    required this.recognized,
    required this.timeEvaluated,
    this.timeRequested,
    this.message,
    this.context,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        entityId: json['entity_id'] as String,
        authorityId: json['authority_id'] as String,
        action: json['action'] as String,
        resource: json['resource'] as String,
        recognized: json['recognized'] as bool,
        timeEvaluated: json['time_evaluated'] as String,
        timeRequested: json['time_requested'] as String?,
        message: json['message'] as String?,
        context: json['context'] == null
            ? null
            : QueryContext.fromJson(json['context'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String entityId;
  final String authorityId;
  final String action;
  final String resource;

  /// True if the action+resource has been recognised, false otherwise.
  final bool recognized;

  /// Server time the query was evaluated at, per the endpoint clock.
  final String timeEvaluated;

  /// The requested server time, echoed from the request `context.time` when supplied.
  final String? timeRequested;

  /// Additional human-readable detail about the recognition response.
  final String? message;
  final QueryContext? context;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entity_id': entityId,
        'authority_id': authorityId,
        'action': action,
        'resource': resource,
        'recognized': recognized,
        'time_evaluated': timeEvaluated,
        if (timeRequested != null) 'time_requested': timeRequested!,
        if (message != null) 'message': message!,
        if (context != null) 'context': context!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// TRQP v2.0 recognition query: ask a trust registry whether an entity is recognised
/// by an authority for a given action+resource. Field names are verbatim from the TRQP
/// recognition request/response schemas so the same payload serves the plain HTTP TRQP
/// binding and the Trust Task binding.
class Payload {
  const Payload({
    required this.entityId,
    required this.authorityId,
    required this.action,
    required this.resource,
    this.context,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        entityId: json['entity_id'] as String,
        authorityId: json['authority_id'] as String,
        action: json['action'] as String,
        resource: json['resource'] as String,
        context: json['context'] == null
            ? null
            : QueryContext.fromJson(json['context'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Identifier of the entity being tested for recognition.
  final String entityId;

  /// Identifier of the authority being queried to recognise the entity.
  final String authorityId;

  /// The action the query is checking recognition for.
  final String action;

  /// The resource the query is checking recognition for.
  final String resource;
  final QueryContext? context;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entity_id': entityId,
        'authority_id': authorityId,
        'action': action,
        'resource': resource,
        if (context != null) 'context': context!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/registry/recognition/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/registry/recognition/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/registry/recognition/0.1","title":"Registry Recognition — payload","description":"TRQP v2.0 recognition query: ask a trust registry whether an entity is recognised by an authority for a given action+resource. Field names are verbatim from the TRQP recognition request/response schemas so the same payload serves the plain HTTP TRQP binding and the Trust Task binding.","type":"object","additionalProperties":false,"required":["entity_id","authority_id","action","resource"],"properties":{"entity_id":{"type":"string","description":"Identifier of the entity being tested for recognition."},"authority_id":{"type":"string","description":"Identifier of the authority being queried to recognise the entity."},"action":{"type":"string","description":"The action the query is checking recognition for."},"resource":{"type":"string","description":"The resource the query is checking recognition for."},"context":{"\$ref":"#/\$defs/QueryContext"},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Registry Recognition — response payload","type":"object","additionalProperties":false,"required":["entity_id","authority_id","action","resource","recognized","time_evaluated"],"properties":{"entity_id":{"type":"string"},"authority_id":{"type":"string"},"action":{"type":"string"},"resource":{"type":"string"},"recognized":{"type":"boolean","description":"True if the action+resource has been recognised, false otherwise."},"time_evaluated":{"type":"string","format":"date-time","description":"Server time the query was evaluated at, per the endpoint clock."},"time_requested":{"type":"string","format":"date-time","description":"The requested server time, echoed from the request `context.time` when supplied."},"message":{"type":"string","maxLength":1024,"description":"Additional human-readable detail about the recognition response."},"context":{"\$ref":"#/\$defs/QueryContext"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"QueryContext":{"title":"QueryContext","type":"object","description":"Optional TRQP query context. `time` requests evaluation as of a given instant; `locator` is an authority-defined hint for locating records.","properties":{"time":{"type":"string","format":"date-time","description":"Evaluate the query as of this RFC3339 (Z offset) instant. Blank/absent uses current server time."},"locator":{"type":"string","description":"Optional hint for systems that need extra information to locate the records in question (authorization queries)."}},"additionalProperties":{"type":"string"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Registry Recognition — response payload","type":"object","additionalProperties":false,"required":["entity_id","authority_id","action","resource","recognized","time_evaluated"],"properties":{"entity_id":{"type":"string"},"authority_id":{"type":"string"},"action":{"type":"string"},"resource":{"type":"string"},"recognized":{"type":"boolean","description":"True if the action+resource has been recognised, false otherwise."},"time_evaluated":{"type":"string","format":"date-time","description":"Server time the query was evaluated at, per the endpoint clock."},"time_requested":{"type":"string","format":"date-time","description":"The requested server time, echoed from the request `context.time` when supplied."},"message":{"type":"string","maxLength":1024,"description":"Additional human-readable detail about the recognition response."},"context":{"\$ref":"#/\$defs/QueryContext"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"QueryContext":{"title":"QueryContext","type":"object","description":"Optional TRQP query context. `time` requests evaluation as of a given instant; `locator` is an authority-defined hint for locating records.","properties":{"time":{"type":"string","format":"date-time","description":"Evaluate the query as of this RFC3339 (Z offset) instant. Blank/absent uses current server time."},"locator":{"type":"string","description":"Optional hint for systems that need extra information to locate the records in question (authorization queries)."}},"additionalProperties":{"type":"string"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
