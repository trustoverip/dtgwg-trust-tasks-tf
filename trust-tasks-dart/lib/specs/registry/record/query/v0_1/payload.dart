// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/registry/record/query/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Whether the record asserts an authorization or a recognition relationship.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const RecordType(String value) {
  static const RecordType authorization = RecordType('authorization');
  static const RecordType recognition = RecordType('recognition');

  /// Every value this specification's schema permits.
  static const List<RecordType> values = <RecordType>[
    authorization,
    recognition
  ];
}

/// TrustRecord
class TrustRecord {
  const TrustRecord({
    required this.entityId,
    required this.authorityId,
    required this.action,
    required this.resource,
    this.recognized,
    this.authorized,
    this.context,
    required this.recordType,
  });

  /// Read this payload from a decoded JSON object.
  factory TrustRecord.fromJson(Map<String, dynamic> json) => TrustRecord(
        entityId: json['entity_id'] as String,
        authorityId: json['authority_id'] as String,
        action: json['action'] as String,
        resource: json['resource'] as String,
        recognized: json['recognized'] as bool?,
        authorized: json['authorized'] as bool?,
        context: json['context'] as Map<String, dynamic>?,
        recordType: RecordType(json['record_type'] as String),
      );

  final String entityId;
  final String authorityId;
  final String action;
  final String resource;

  /// Present on recognition records: whether the action+resource is recognised.
  final bool? recognized;

  /// Present on authorization records: whether the action+resource authorization is
  /// confirmed.
  final bool? authorized;

  /// Opaque governance context attached to the record.
  final Map<String, dynamic>? context;
  final RecordType recordType;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'entity_id': entityId,
        'authority_id': authorityId,
        'action': action,
        'resource': resource,
        if (recognized != null) 'recognized': recognized!,
        if (authorized != null) 'authorized': authorized!,
        if (context != null) 'context': context!,
        'record_type': recordType.value,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Registry Record Query — response payload
class Response {
  const Response({
    required this.records,
    this.nextCursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        records: (json['records'] as List<dynamic>)
            .map((e) => TrustRecord.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The matching trust records. Exactly one entry on a fully keyed fetch; zero or more
  /// per page on an enumeration.
  final List<TrustRecord> records;

  /// Continuation token for the next page, or null/absent when this is the last.
  final String? nextCursor;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'records': records.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// An administrator queries a trust registry's stored records by an optional four-part
/// key. All four parts supplied: an exact fetch of one record (notFound on a miss).
/// Fewer parts: a filtered, cursor-paginated enumeration — fixing the pagination gap
/// the superseded registry/record/list conceded. Key field names are verbatim TRQP
/// (snake_case), matching the registry/* family.
class Payload {
  const Payload({
    this.entityId,
    this.authorityId,
    this.action,
    this.resource,
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        entityId: json['entity_id'] as String?,
        authorityId: json['authority_id'] as String?,
        action: json['action'] as String?,
        resource: json['resource'] as String?,
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Filter to records for this entity.
  final String? entityId;

  /// Filter to records asserted by this authority.
  final String? authorityId;

  /// Filter to records for this action.
  final String? action;

  /// Filter to records for this resource.
  final String? resource;

  /// Opaque continuation token from a previous page's `nextCursor`.
  final String? cursor;

  /// Page size; the registry clamps to 1..=200 (default 50). Ignored on a fully keyed
  /// exact fetch.
  final int? limit;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (entityId != null) 'entity_id': entityId!,
        if (authorityId != null) 'authority_id': authorityId!,
        if (action != null) 'action': action!,
        if (resource != null) 'resource': resource!,
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/registry/record/query/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/registry/record/query/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/registry/record/query/0.1","title":"Registry Record Query — payload","description":"An administrator queries a trust registry\'s stored records by an optional four-part key. All four parts supplied: an exact fetch of one record (notFound on a miss). Fewer parts: a filtered, cursor-paginated enumeration — fixing the pagination gap the superseded registry/record/list conceded. Key field names are verbatim TRQP (snake_case), matching the registry/* family.","type":"object","additionalProperties":false,"properties":{"entity_id":{"type":"string","description":"Filter to records for this entity."},"authority_id":{"type":"string","description":"Filter to records asserted by this authority."},"action":{"type":"string","description":"Filter to records for this action."},"resource":{"type":"string","description":"Filter to records for this resource."},"cursor":{"type":"string","description":"Opaque continuation token from a previous page\'s `nextCursor`."},"limit":{"type":"integer","minimum":1,"maximum":200,"description":"Page size; the registry clamps to 1..=200 (default 50). Ignored on a fully keyed exact fetch."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Registry Record Query — response payload","type":"object","additionalProperties":false,"required":["records"],"properties":{"records":{"type":"array","description":"The matching trust records. Exactly one entry on a fully keyed fetch; zero or more per page on an enumeration.","items":{"\$ref":"#/\$defs/TrustRecord"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null/absent when this is the last."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"TrustRecord":{"title":"TrustRecord","type":"object","additionalProperties":false,"required":["entity_id","authority_id","action","resource","record_type"],"properties":{"entity_id":{"type":"string"},"authority_id":{"type":"string"},"action":{"type":"string"},"resource":{"type":"string"},"recognized":{"type":"boolean","description":"Present on recognition records: whether the action+resource is recognised."},"authorized":{"type":"boolean","description":"Present on authorization records: whether the action+resource authorization is confirmed."},"context":{"type":"object","description":"Opaque governance context attached to the record.","additionalProperties":true},"record_type":{"\$ref":"#/\$defs/RecordType"}}},"RecordType":{"title":"RecordType","type":"string","enum":["authorization","recognition"],"description":"Whether the record asserts an authorization or a recognition relationship."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Registry Record Query — response payload","type":"object","additionalProperties":false,"required":["records"],"properties":{"records":{"type":"array","description":"The matching trust records. Exactly one entry on a fully keyed fetch; zero or more per page on an enumeration.","items":{"\$ref":"#/\$defs/TrustRecord"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null/absent when this is the last."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"TrustRecord":{"title":"TrustRecord","type":"object","additionalProperties":false,"required":["entity_id","authority_id","action","resource","record_type"],"properties":{"entity_id":{"type":"string"},"authority_id":{"type":"string"},"action":{"type":"string"},"resource":{"type":"string"},"recognized":{"type":"boolean","description":"Present on recognition records: whether the action+resource is recognised."},"authorized":{"type":"boolean","description":"Present on authorization records: whether the action+resource authorization is confirmed."},"context":{"type":"object","description":"Opaque governance context attached to the record.","additionalProperties":true},"record_type":{"\$ref":"#/\$defs/RecordType"}}},"RecordType":{"title":"RecordType","type":"string","enum":["authorization","recognition"],"description":"Whether the record asserts an authorization or a recognition relationship."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
