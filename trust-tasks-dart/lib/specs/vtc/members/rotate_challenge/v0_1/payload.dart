// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/members/rotate-challenge/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Members Rotate-Challenge — response payload
class Response {
  const Response({
    required this.rotationId,
    required this.expiresAt,
    required this.signingPayloadHex,
    required this.canonicalTemplate,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        rotationId: json['rotationId'] as String,
        expiresAt: json['expiresAt'] as String,
        signingPayloadHex: json['signingPayloadHex'] as String,
        canonicalTemplate: json['canonicalTemplate'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A UUID identifying this rotation ceremony.
  final String rotationId;
  final String expiresAt;

  /// Hex bytes the member signs with both the old and new keys to prove control of each.
  final String signingPayloadHex;

  /// The canonical rotation template the signatures cover (opaque here).
  final Map<String, dynamic> canonicalTemplate;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'rotationId': rotationId,
        'expiresAt': expiresAt,
        'signingPayloadHex': signingPayloadHex,
        'canonicalTemplate': canonicalTemplate,
        if (ext != null) 'ext': ext!,
      };
}

/// Self-asserted motive, bound to the authenticated session that opens the ceremony
/// and recorded on the audit envelope. NOT covered by the rotation signatures —
/// intent, not evidence. Omitted = unspecified.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadReason(String value) {
  static const PayloadReason routine = PayloadReason('routine');
  static const PayloadReason compromise = PayloadReason('compromise');
  static const PayloadReason deviceLoss = PayloadReason('deviceLoss');
  static const PayloadReason migration = PayloadReason('migration');
  static const PayloadReason unspecified = PayloadReason('unspecified');

  /// Every value this specification's schema permits.
  static const List<PayloadReason> values = <PayloadReason>[
    routine,
    compromise,
    deviceLoss,
    migration,
    unspecified
  ];
}

/// VTC Members Rotate-Challenge — payload
class Payload {
  const Payload({
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        reason: json['reason'] == null
            ? null
            : PayloadReason(json['reason'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Self-asserted motive, bound to the authenticated session that opens the ceremony
  /// and recorded on the audit envelope. NOT covered by the rotation signatures —
  /// intent, not evidence. Omitted = unspecified.
  final PayloadReason? reason;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (reason != null) 'reason': reason!.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/members/rotate-challenge/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/members/rotate-challenge/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/members/rotate-challenge/0.1","title":"VTC Members Rotate-Challenge — payload","type":"object","additionalProperties":false,"properties":{"reason":{"type":"string","enum":["routine","compromise","deviceLoss","migration","unspecified"],"description":"Self-asserted motive, bound to the authenticated session that opens the ceremony and recorded on the audit envelope. NOT covered by the rotation signatures — intent, not evidence. Omitted = unspecified."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Rotate-Challenge — response payload","type":"object","additionalProperties":false,"required":["rotationId","expiresAt","signingPayloadHex","canonicalTemplate"],"properties":{"rotationId":{"type":"string","description":"A UUID identifying this rotation ceremony."},"expiresAt":{"type":"string","format":"date-time"},"signingPayloadHex":{"type":"string","pattern":"^[0-9a-f]+\$","description":"Hex bytes the member signs with both the old and new keys to prove control of each."},"canonicalTemplate":{"type":"object","description":"The canonical rotation template the signatures cover (opaque here)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Rotate-Challenge — response payload","type":"object","additionalProperties":false,"required":["rotationId","expiresAt","signingPayloadHex","canonicalTemplate"],"properties":{"rotationId":{"type":"string","description":"A UUID identifying this rotation ceremony."},"expiresAt":{"type":"string","format":"date-time"},"signingPayloadHex":{"type":"string","pattern":"^[0-9a-f]+\$","description":"Hex bytes the member signs with both the old and new keys to prove control of each."},"canonicalTemplate":{"type":"object","description":"The canonical rotation template the signatures cover (opaque here)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
