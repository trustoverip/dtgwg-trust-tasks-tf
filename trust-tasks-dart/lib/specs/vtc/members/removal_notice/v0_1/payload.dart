// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/members/removal-notice/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Which removal happened. `adminRemoved` is a policy-governed removal by an
/// administrator; `purged` is a forceful super-administrator deletion that skips the
/// removal policy. The two differ in what recourse a member has, which is why they are
/// distinguishable rather than a single `removed`.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadCode(String value) {
  static const PayloadCode adminRemoved = PayloadCode('adminRemoved');
  static const PayloadCode purged = PayloadCode('purged');

  /// Every value this specification's schema permits.
  static const List<PayloadCode> values = <PayloadCode>[adminRemoved, purged];
}

/// How the member's published record was handled. Always concrete — a community
/// resolves any `policydefault` preference before sending, since a notice naming an
/// unresolved default tells the member nothing about their own record.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadDisposition(String value) {
  static const PayloadDisposition purge = PayloadDisposition('purge');
  static const PayloadDisposition tombstone = PayloadDisposition('tombstone');
  static const PayloadDisposition historical = PayloadDisposition('historical');

  /// Every value this specification's schema permits.
  static const List<PayloadDisposition> values = <PayloadDisposition>[
    purge,
    tombstone,
    historical
  ];
}

/// A community tells a member it removed them, on whose authority, when, and why.
/// Unsolicited: unlike vtc/members/self-remove-receipt this answers no request from
/// the member, because the member did not ask.
class Payload {
  const Payload({
    required this.did,
    required this.code,
    required this.disposition,
    this.reason,
    required this.decidedAt,
    required this.decidedBy,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        code: PayloadCode(json['code'] as String),
        disposition: PayloadDisposition(json['disposition'] as String),
        reason: json['reason'] as String?,
        decidedAt: json['decidedAt'] as String,
        decidedBy: json['decidedBy'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The removed member's DID — the notice's recipient. Carried in the payload as well
  /// as the envelope so that a forwarded notice, detached from its transport, still
  /// names its subject.
  final String did;

  /// Which removal happened. `adminRemoved` is a policy-governed removal by an
  /// administrator; `purged` is a forceful super-administrator deletion that skips the
  /// removal policy. The two differ in what recourse a member has, which is why they are
  /// distinguishable rather than a single `removed`.
  final PayloadCode code;

  /// How the member's published record was handled. Always concrete — a community
  /// resolves any `policydefault` preference before sending, since a notice naming an
  /// unresolved default tells the member nothing about their own record.
  final PayloadDisposition disposition;

  /// The operator's stated reason, verbatim. OPTIONAL because a community may remove
  /// without giving one — but omitting it and sending an empty string are different
  /// claims, and a community that has a reason SHOULD send it. This is the member's only
  /// account of why.
  final String? reason;

  /// When the removal took effect. Distinct from the notice's own `issuedAt`, which may
  /// be much later if the member was offline — and it is the decision, not the delivery,
  /// that a member or a third party needs to place in time.
  final String decidedAt;

  /// DID of the administrator who decided. Names the deciding authority rather than the
  /// community, because 'the community removed me' is not answerable and 'this
  /// administrator removed me' is. The community itself is the document's `issuer`.
  final String decidedBy;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'code': code.value,
        'disposition': disposition.value,
        if (reason != null) 'reason': reason!,
        'decidedAt': decidedAt,
        'decidedBy': decidedBy,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/members/removal-notice/0.1';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/members/removal-notice/0.1","title":"VTC Members Removal Notice — payload","description":"A community tells a member it removed them, on whose authority, when, and why. Unsolicited: unlike vtc/members/self-remove-receipt this answers no request from the member, because the member did not ask.","type":"object","additionalProperties":false,"required":["did","code","disposition","decidedAt","decidedBy"],"properties":{"did":{"type":"string","minLength":1,"description":"The removed member\'s DID — the notice\'s recipient. Carried in the payload as well as the envelope so that a forwarded notice, detached from its transport, still names its subject."},"code":{"type":"string","enum":["adminRemoved","purged"],"description":"Which removal happened. `adminRemoved` is a policy-governed removal by an administrator; `purged` is a forceful super-administrator deletion that skips the removal policy. The two differ in what recourse a member has, which is why they are distinguishable rather than a single `removed`."},"disposition":{"type":"string","enum":["purge","tombstone","historical"],"description":"How the member\'s published record was handled. Always concrete — a community resolves any `policydefault` preference before sending, since a notice naming an unresolved default tells the member nothing about their own record."},"reason":{"type":"string","maxLength":1024,"description":"The operator\'s stated reason, verbatim. OPTIONAL because a community may remove without giving one — but omitting it and sending an empty string are different claims, and a community that has a reason SHOULD send it. This is the member\'s only account of why."},"decidedAt":{"type":"string","format":"date-time","description":"When the removal took effect. Distinct from the notice\'s own `issuedAt`, which may be much later if the member was offline — and it is the decision, not the delivery, that a member or a third party needs to place in time."},"decidedBy":{"type":"string","minLength":1,"description":"DID of the administrator who decided. Names the deciding authority rather than the community, because \'the community removed me\' is not answerable and \'this administrator removed me\' is. The community itself is the document\'s `issuer`."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);
