// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/members/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// How the member's record is handled on departure.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const MemberResponseDeparturePreference(String value) {
  static const MemberResponseDeparturePreference purge =
      MemberResponseDeparturePreference('purge');
  static const MemberResponseDeparturePreference tombstone =
      MemberResponseDeparturePreference('tombstone');
  static const MemberResponseDeparturePreference historical =
      MemberResponseDeparturePreference('historical');
  static const MemberResponseDeparturePreference policydefault =
      MemberResponseDeparturePreference('policydefault');

  /// Every value this specification's schema permits.
  static const List<MemberResponseDeparturePreference> values =
      <MemberResponseDeparturePreference>[
    purge,
    tombstone,
    historical,
    policydefault
  ];
}

/// One community member as the maintainer sees it: the membership record joined with
/// its ACL role.
class MemberResponse {
  const MemberResponse({
    required this.did,
    required this.role,
    this.label,
    required this.joinedAt,
    required this.publishConsent,
    required this.departurePreference,
    this.statusListIndex,
    this.currentVmcId,
    this.currentRoleVecId,
    this.personhood,
    this.personhoodAssertedAt,
    this.joinedViaInvitation,
    this.memberVmcId,
    this.memberVmcReceivedAt,
    required this.extensions,
  });

  /// Read this payload from a decoded JSON object.
  factory MemberResponse.fromJson(Map<String, dynamic> json) => MemberResponse(
        did: json['did'] as String,
        role: json['role'] as String,
        label: json['label'] as String?,
        joinedAt: json['joinedAt'] as String,
        publishConsent: json['publishConsent'] as bool,
        departurePreference: MemberResponseDeparturePreference(
            json['departurePreference'] as String),
        statusListIndex: json['statusListIndex'] as int?,
        currentVmcId: json['currentVmcId'] as String?,
        currentRoleVecId: json['currentRoleVecId'] as String?,
        personhood: json['personhood'] as bool?,
        personhoodAssertedAt: json['personhoodAssertedAt'] as String?,
        joinedViaInvitation: json['joinedViaInvitation'] as bool?,
        memberVmcId: json['memberVmcId'] as String?,
        memberVmcReceivedAt: json['memberVmcReceivedAt'] as String?,
        extensions: json['extensions'] as Map<String, dynamic>,
      );

  /// The member's DID.
  final String did;

  /// The member's role in wire form (e.g. `admin`, `moderator`, `custom:editor`).
  final String role;

  /// Optional human-readable label.
  final String? label;
  final String joinedAt;

  /// Whether the member consented to being published in the community directory.
  final bool publishConsent;

  /// How the member's record is handled on departure.
  final MemberResponseDeparturePreference departurePreference;

  /// The member's slot on the community's membership status list, when allocated.
  final int? statusListIndex;

  /// Id of the member's current Verifiable Membership Credential, if issued.
  final String? currentVmcId;

  /// Id of the member's current role Verifiable Endorsement Credential, if issued.
  final String? currentRoleVecId;

  /// Whether the community has asserted that this member is a distinct real person.
  /// Read-only here: it is set and cleared by the personhood verbs, and cleared by a
  /// renewal-policy downgrade. Load-bearing rather than informational — a community that
  /// recognises members of another community may gate on it, so a consumer that cannot
  /// read it cannot make that decision.
  final bool? personhood;

  /// When personhood was most recently asserted. Absent where it never has been.
  /// Operator-facing: it belongs on admin-gated reads, and a member's own view of
  /// themselves need carry only the flag.
  final String? personhoodAssertedAt;

  /// Whether this member joined by presenting an invitation credential rather than by an
  /// admin's decision on an open request. The two routes to membership carry different
  /// evidence, and an operator reviewing a roster can otherwise not tell them apart.
  final bool? joinedViaInvitation;

  /// `id` of the member-issued reciprocal membership credential — the member half of the
  /// pair — once the member has sent it. Absent until then, which is the useful signal:
  /// it distinguishes a membership the community has asserted from one the member has
  /// acknowledged. The credential body is not echoed here.
  final String? memberVmcId;

  /// When that reciprocal credential was received. Paired with `memberVmcId`.
  final String? memberVmcReceivedAt;

  /// Opaque community-defined extension bag. Keys are maintainer-defined; the maintainer
  /// caps its size (16 KiB in the reference implementation).
  final Map<String, dynamic> extensions;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'role': role,
        if (label != null) 'label': label!,
        'joinedAt': joinedAt,
        'publishConsent': publishConsent,
        'departurePreference': departurePreference.value,
        if (statusListIndex != null) 'statusListIndex': statusListIndex!,
        if (currentVmcId != null) 'currentVmcId': currentVmcId!,
        if (currentRoleVecId != null) 'currentRoleVecId': currentRoleVecId!,
        if (personhood != null) 'personhood': personhood!,
        if (personhoodAssertedAt != null)
          'personhoodAssertedAt': personhoodAssertedAt!,
        if (joinedViaInvitation != null)
          'joinedViaInvitation': joinedViaInvitation!,
        if (memberVmcId != null) 'memberVmcId': memberVmcId!,
        if (memberVmcReceivedAt != null)
          'memberVmcReceivedAt': memberVmcReceivedAt!,
        'extensions': extensions,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Members List — response payload
class Response {
  const Response({
    required this.items,
    this.nextCursor,
    this.totalEstimate,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        items: (json['items'] as List<dynamic>)
            .map((e) => MemberResponse.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        totalEstimate: json['totalEstimate'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<MemberResponse> items;

  /// Continuation token for the next page, or null when this is the last.
  final String? nextCursor;

  /// Approximate total matching members, when the maintainer can cheaply estimate it.
  final int? totalEstimate;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'items': items.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (totalEstimate != null) 'totalEstimate': totalEstimate!,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Members List — payload
class Payload {
  const Payload({
    this.role,
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        role: json['role'] as String?,
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Filter to members with this role (wire form, e.g. `admin`, `custom:editor`).
  final String? role;

  /// Opaque continuation token from a previous page's `nextCursor`.
  final String? cursor;

  /// Page size; the maintainer clamps to 1..=200 (default 50).
  final int? limit;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (role != null) 'role': role!,
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/members/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/members/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/members/list/0.1","title":"VTC Members List — payload","type":"object","additionalProperties":false,"properties":{"role":{"type":"string","minLength":1,"description":"Filter to members with this role (wire form, e.g. `admin`, `custom:editor`)."},"cursor":{"type":"string","description":"Opaque continuation token from a previous page\'s `nextCursor`."},"limit":{"type":"integer","minimum":1,"maximum":200,"description":"Page size; the maintainer clamps to 1..=200 (default 50)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Members List — response payload","type":"object","additionalProperties":false,"required":["items"],"properties":{"items":{"type":"array","items":{"\$ref":"#/\$defs/MemberResponse"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null when this is the last."},"totalEstimate":{"type":["integer","null"],"description":"Approximate total matching members, when the maintainer can cheaply estimate it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"MemberResponse":{"\$anchor":"memberResponse","title":"MemberResponse","description":"One community member as the maintainer sees it: the membership record joined with its ACL role.","type":"object","additionalProperties":false,"required":["did","role","joinedAt","publishConsent","departurePreference","extensions"],"properties":{"did":{"type":"string","minLength":1,"description":"The member\'s DID."},"role":{"type":"string","minLength":1,"description":"The member\'s role in wire form (e.g. `admin`, `moderator`, `custom:editor`)."},"label":{"type":["string","null"],"maxLength":256,"description":"Optional human-readable label."},"joinedAt":{"type":"string","format":"date-time"},"publishConsent":{"type":"boolean","description":"Whether the member consented to being published in the community directory."},"departurePreference":{"type":"string","enum":["purge","tombstone","historical","policydefault"],"description":"How the member\'s record is handled on departure."},"statusListIndex":{"type":["integer","null"],"description":"The member\'s slot on the community\'s membership status list, when allocated."},"currentVmcId":{"type":["string","null"],"description":"Id of the member\'s current Verifiable Membership Credential, if issued."},"currentRoleVecId":{"type":["string","null"],"description":"Id of the member\'s current role Verifiable Endorsement Credential, if issued."},"personhood":{"type":"boolean","description":"Whether the community has asserted that this member is a distinct real person. Read-only here: it is set and cleared by the personhood verbs, and cleared by a renewal-policy downgrade. Load-bearing rather than informational — a community that recognises members of another community may gate on it, so a consumer that cannot read it cannot make that decision."},"personhoodAssertedAt":{"type":["string","null"],"format":"date-time","description":"When personhood was most recently asserted. Absent where it never has been. Operator-facing: it belongs on admin-gated reads, and a member\'s own view of themselves need carry only the flag."},"joinedViaInvitation":{"type":"boolean","description":"Whether this member joined by presenting an invitation credential rather than by an admin\'s decision on an open request. The two routes to membership carry different evidence, and an operator reviewing a roster can otherwise not tell them apart."},"memberVmcId":{"type":["string","null"],"description":"`id` of the member-issued reciprocal membership credential — the member half of the pair — once the member has sent it. Absent until then, which is the useful signal: it distinguishes a membership the community has asserted from one the member has acknowledged. The credential body is not echoed here."},"memberVmcReceivedAt":{"type":["string","null"],"format":"date-time","description":"When that reciprocal credential was received. Paired with `memberVmcId`."},"extensions":{"type":"object","description":"Opaque community-defined extension bag. Keys are maintainer-defined; the maintainer caps its size (16 KiB in the reference implementation)."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Members List — response payload","type":"object","additionalProperties":false,"required":["items"],"properties":{"items":{"type":"array","items":{"\$ref":"#/\$defs/MemberResponse"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null when this is the last."},"totalEstimate":{"type":["integer","null"],"description":"Approximate total matching members, when the maintainer can cheaply estimate it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"MemberResponse":{"\$anchor":"memberResponse","title":"MemberResponse","description":"One community member as the maintainer sees it: the membership record joined with its ACL role.","type":"object","additionalProperties":false,"required":["did","role","joinedAt","publishConsent","departurePreference","extensions"],"properties":{"did":{"type":"string","minLength":1,"description":"The member\'s DID."},"role":{"type":"string","minLength":1,"description":"The member\'s role in wire form (e.g. `admin`, `moderator`, `custom:editor`)."},"label":{"type":["string","null"],"maxLength":256,"description":"Optional human-readable label."},"joinedAt":{"type":"string","format":"date-time"},"publishConsent":{"type":"boolean","description":"Whether the member consented to being published in the community directory."},"departurePreference":{"type":"string","enum":["purge","tombstone","historical","policydefault"],"description":"How the member\'s record is handled on departure."},"statusListIndex":{"type":["integer","null"],"description":"The member\'s slot on the community\'s membership status list, when allocated."},"currentVmcId":{"type":["string","null"],"description":"Id of the member\'s current Verifiable Membership Credential, if issued."},"currentRoleVecId":{"type":["string","null"],"description":"Id of the member\'s current role Verifiable Endorsement Credential, if issued."},"personhood":{"type":"boolean","description":"Whether the community has asserted that this member is a distinct real person. Read-only here: it is set and cleared by the personhood verbs, and cleared by a renewal-policy downgrade. Load-bearing rather than informational — a community that recognises members of another community may gate on it, so a consumer that cannot read it cannot make that decision."},"personhoodAssertedAt":{"type":["string","null"],"format":"date-time","description":"When personhood was most recently asserted. Absent where it never has been. Operator-facing: it belongs on admin-gated reads, and a member\'s own view of themselves need carry only the flag."},"joinedViaInvitation":{"type":"boolean","description":"Whether this member joined by presenting an invitation credential rather than by an admin\'s decision on an open request. The two routes to membership carry different evidence, and an operator reviewing a roster can otherwise not tell them apart."},"memberVmcId":{"type":["string","null"],"description":"`id` of the member-issued reciprocal membership credential — the member half of the pair — once the member has sent it. Absent until then, which is the useful signal: it distinguishes a membership the community has asserted from one the member has acknowledged. The credential body is not echoed here."},"memberVmcReceivedAt":{"type":["string","null"],"format":"date-time","description":"When that reciprocal credential was received. Paired with `memberVmcId`."},"extensions":{"type":"object","description":"Opaque community-defined extension bag. Keys are maintainer-defined; the maintainer caps its size (16 KiB in the reference implementation)."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
