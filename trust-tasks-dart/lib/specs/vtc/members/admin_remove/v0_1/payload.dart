// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/members/admin-remove/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseDisposition is a closed set of string values defined by this
/// specification's schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseDisposition(String value) {
  static const ResponseDisposition purge = ResponseDisposition('purge');
  static const ResponseDisposition tombstone = ResponseDisposition('tombstone');
  static const ResponseDisposition historical =
      ResponseDisposition('historical');

  /// Every value this specification's schema permits.
  static const List<ResponseDisposition> values = <ResponseDisposition>[
    purge,
    tombstone,
    historical
  ];
}

/// VTC Members Admin-Remove — response payload
class Response {
  const Response({
    required this.did,
    required this.disposition,
    required this.removed,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        did: json['did'] as String,
        disposition: ResponseDisposition(json['disposition'] as String),
        removed: json['removed'] as bool,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String did;
  final ResponseDisposition disposition;
  final bool removed;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'disposition': disposition.value,
        'removed': removed,
        if (ext != null) 'ext': ext!,
      };
}

/// PayloadDisposition is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadDisposition(String value) {
  static const PayloadDisposition purge = PayloadDisposition('purge');
  static const PayloadDisposition tombstone = PayloadDisposition('tombstone');
  static const PayloadDisposition historical = PayloadDisposition('historical');
  static const PayloadDisposition policydefault =
      PayloadDisposition('policydefault');

  /// Every value this specification's schema permits.
  static const List<PayloadDisposition> values = <PayloadDisposition>[
    purge,
    tombstone,
    historical,
    policydefault
  ];
}

/// VTC Members Admin-Remove — payload
class Payload {
  const Payload({
    required this.did,
    this.disposition,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        disposition: json['disposition'] == null
            ? null
            : PayloadDisposition(json['disposition'] as String),
        reason: json['reason'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The member to remove.
  final String did;
  final PayloadDisposition? disposition;

  /// Operator reason, recorded in the audit trail.
  final String? reason;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        if (disposition != null) 'disposition': disposition!.value,
        if (reason != null) 'reason': reason!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/members/admin-remove/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/members/admin-remove/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/members/admin-remove/0.1","title":"VTC Members Admin-Remove — payload","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"The member to remove."},"disposition":{"type":"string","enum":["purge","tombstone","historical","policydefault"]},"reason":{"type":"string","maxLength":1024,"description":"Operator reason, recorded in the audit trail."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Admin-Remove — response payload","type":"object","additionalProperties":false,"required":["did","disposition","removed"],"properties":{"did":{"type":"string","minLength":1},"disposition":{"type":"string","enum":["purge","tombstone","historical"]},"removed":{"type":"boolean"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Admin-Remove — response payload","type":"object","additionalProperties":false,"required":["did","disposition","removed"],"properties":{"did":{"type":"string","minLength":1},"disposition":{"type":"string","enum":["purge","tombstone","historical"]},"removed":{"type":"boolean"},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
