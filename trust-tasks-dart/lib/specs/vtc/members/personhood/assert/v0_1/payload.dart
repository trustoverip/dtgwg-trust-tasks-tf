// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/members/personhood/assert/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Members Personhood-Assert — response payload
class Response {
  const Response({
    required this.did,
    required this.personhood,
    required this.vmc,
    required this.roleVec,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        did: json['did'] as String,
        personhood: json['personhood'] as bool,
        vmc: json['vmc'] as Map<String, dynamic>,
        roleVec: json['roleVec'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String did;

  /// Always true on success — the flag flipped or stayed asserted.
  final bool personhood;

  /// Re-issued membership credential carrying the asserted personhood (opaque here).
  final Map<String, dynamic> vmc;

  /// Re-issued role credential (opaque here).
  final Map<String, dynamic> roleVec;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'personhood': personhood,
        'vmc': vmc,
        'roleVec': roleVec,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Members Personhood-Assert — payload
class Payload {
  const Payload({
    required this.did,
    required this.presentation,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        did: json['did'] as String,
        presentation: json['presentation'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The member asserting personhood; MUST equal the presentation's holder.
  final String did;

  /// A W3C Verifiable Presentation (opaque here). Its holder MUST match did, its
  /// proof.challenge MUST be the paired challengeId, and it MUST carry at least one
  /// credential satisfying the community's active personhood policy.
  final Map<String, dynamic> presentation;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'presentation': presentation,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/members/personhood/assert/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/members/personhood/assert/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/members/personhood/assert/0.1","title":"VTC Members Personhood-Assert — payload","type":"object","additionalProperties":false,"required":["did","presentation"],"properties":{"did":{"type":"string","minLength":1,"description":"The member asserting personhood; MUST equal the presentation\'s holder."},"presentation":{"type":"object","description":"A W3C Verifiable Presentation (opaque here). Its holder MUST match did, its proof.challenge MUST be the paired challengeId, and it MUST carry at least one credential satisfying the community\'s active personhood policy."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Personhood-Assert — response payload","type":"object","additionalProperties":false,"required":["did","personhood","vmc","roleVec"],"properties":{"did":{"type":"string","minLength":1},"personhood":{"type":"boolean","const":true,"description":"Always true on success — the flag flipped or stayed asserted."},"vmc":{"type":"object","description":"Re-issued membership credential carrying the asserted personhood (opaque here)."},"roleVec":{"type":"object","description":"Re-issued role credential (opaque here)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Personhood-Assert — response payload","type":"object","additionalProperties":false,"required":["did","personhood","vmc","roleVec"],"properties":{"did":{"type":"string","minLength":1},"personhood":{"type":"boolean","const":true,"description":"Always true on success — the flag flipped or stayed asserted."},"vmc":{"type":"object","description":"Re-issued membership credential carrying the asserted personhood (opaque here)."},"roleVec":{"type":"object","description":"Re-issued role credential (opaque here)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
