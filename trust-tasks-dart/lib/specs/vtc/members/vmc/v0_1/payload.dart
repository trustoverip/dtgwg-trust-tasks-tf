// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/members/vmc/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Terminal state of the delivery.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseStatus(String value) {
  static const ResponseStatus stored = ResponseStatus('stored');

  /// Every value this specification's schema permits.
  static const List<ResponseStatus> values = <ResponseStatus>[stored];
}

/// VTC Members Deliver VMC — receipt payload
class Response {
  const Response({
    required this.memberDid,
    required this.vmcId,
    required this.status,
    this.requestId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        memberDid: json['memberDid'] as String,
        vmcId: json['vmcId'] as String,
        status: ResponseStatus(json['status'] as String),
        requestId: json['requestId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The delivering member, as proven by the transport.
  final String memberDid;

  /// Identifier of the stored credential.
  final String vmcId;

  /// Terminal state of the delivery.
  final ResponseStatus status;

  /// Echoed when the delivery also closed a join request (the request payload carried
  /// `requestId`).
  final String? requestId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'memberDid': memberDid,
        'vmcId': vmcId,
        'status': status.value,
        if (requestId != null) 'requestId': requestId!,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Members Deliver VMC — payload
class Payload {
  const Payload({
    required this.vc,
    this.requestId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        vc: json['vc'] as Map<String, dynamic>,
        requestId: json['requestId'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The member-issued MembershipCredential (W3C VC). Opaque here; credentialSubject.id
  /// must be the community DID.
  final Map<String, dynamic> vc;

  /// Optional: an approved join request this delivery also closes. When present and
  /// naming an approved request whose applicant is the delivering member, the community
  /// records the delivered credential as the reciprocal half of the join and marks the
  /// request's reciprocation complete.
  final String? requestId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'vc': vc,
        if (requestId != null) 'requestId': requestId!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/members/vmc/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/members/vmc/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/members/vmc/0.1","title":"VTC Members Deliver VMC — payload","type":"object","additionalProperties":false,"required":["vc"],"properties":{"vc":{"type":"object","description":"The member-issued MembershipCredential (W3C VC). Opaque here; credentialSubject.id must be the community DID."},"requestId":{"type":"string","minLength":1,"description":"Optional: an approved join request this delivery also closes. When present and naming an approved request whose applicant is the delivering member, the community records the delivered credential as the reciprocal half of the join and marks the request\'s reciprocation complete."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Deliver VMC — receipt payload","type":"object","additionalProperties":false,"required":["memberDid","vmcId","status"],"properties":{"memberDid":{"type":"string","minLength":1,"description":"The delivering member, as proven by the transport."},"vmcId":{"type":"string","minLength":1,"description":"Identifier of the stored credential."},"status":{"type":"string","enum":["stored"],"description":"Terminal state of the delivery."},"requestId":{"type":"string","minLength":1,"description":"Echoed when the delivery also closed a join request (the request payload carried `requestId`)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Members Deliver VMC — receipt payload","type":"object","additionalProperties":false,"required":["memberDid","vmcId","status"],"properties":{"memberDid":{"type":"string","minLength":1,"description":"The delivering member, as proven by the transport."},"vmcId":{"type":"string","minLength":1,"description":"Identifier of the stored credential."},"status":{"type":"string","enum":["stored"],"description":"Terminal state of the delivery."},"requestId":{"type":"string","minLength":1,"description":"Echoed when the delivery also closed a join request (the request payload carried `requestId`)."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
