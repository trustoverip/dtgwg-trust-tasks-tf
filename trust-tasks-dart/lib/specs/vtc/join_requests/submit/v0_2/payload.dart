// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/join-requests/submit/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// What the policy decided. `allow` — admitted. `deny` — refused, terminally for this
/// submission. `refer` — parked for a human or quorum decision; the applicant is
/// neither in nor out. `requestMore` — the policy cannot decide yet and names what
/// further evidence it needs. The four are not reducible to a pending/decided pair.
/// `refer` and `requestMore` are both 'not decided', but they place the next action
/// with different parties: `refer` waits on the community, `requestMore` waits on the
/// applicant. A consumer that cannot tell them apart cannot tell a user whether to
/// wait or to act.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const VerdictEffect(String value) {
  static const VerdictEffect allow = VerdictEffect('allow');
  static const VerdictEffect deny = VerdictEffect('deny');
  static const VerdictEffect refer = VerdictEffect('refer');
  static const VerdictEffect requestMore = VerdictEffect('requestMore');

  /// Every value this specification's schema permits.
  static const List<VerdictEffect> values = <VerdictEffect>[
    allow,
    deny,
    refer,
    requestMore
  ];
}

/// The effect-dependent detail of a verdict. Every member is optional at the schema
/// level and which ones are meaningful depends on `effect`: `role` / `obligations` /
/// `bundleRef` on `allow`, `code` / `reason` on `deny`, `queue` / `reason` on `refer`,
/// `needs` / `presentationDefinition` on `requestMore`. The dependency is stated here
/// rather than enforced by `if`/`then` per effect, so that the shape stays a single
/// flat object a generated type can carry without a discriminated union per family — a
/// deliberate trade of schema strictness for implementability, and the reason a
/// consumer MUST branch on `effect` rather than on which members happen to be present.
class VerdictWith {
  const VerdictWith({
    this.role,
    this.obligations,
    this.bundleRef,
    this.code,
    this.reason,
    this.queue,
    this.needs,
    this.presentationDefinition,
  });

  /// Read this payload from a decoded JSON object.
  factory VerdictWith.fromJson(Map<String, dynamic> json) => VerdictWith(
        role: json['role'] as String?,
        obligations: json['obligations'] as Map<String, dynamic>?,
        bundleRef: json['bundleRef'] as Map<String, dynamic>?,
        code: json['code'] as String?,
        reason: json['reason'] as String?,
        queue: json['queue'] as String?,
        needs: json['needs'] == null
            ? null
            : (json['needs'] as List<dynamic>).map((e) => e as String).toList(),
        presentationDefinition:
            json['presentationDefinition'] as Map<String, dynamic>?,
      );

  /// The granted local role. `allow` only.
  final String? role;

  /// Conditions attached to the grant. `allow` only.
  final Map<String, dynamic>? obligations;

  /// Pointer to a sealed credential bundle, added by the community where issuance
  /// occurred rather than emitted by the policy. `allow` only.
  final Map<String, dynamic>? bundleRef;

  /// Stable refusal code, safe to branch on. `deny` only.
  final String? code;

  /// Elaboration in prose, when the decider gave one. `deny` and `refer`.
  final String? reason;

  /// Which review queue the decision was parked in, so an applicant can be told who now
  /// holds it. `refer` only.
  final String? queue;

  /// What further evidence is required, named so the applicant can act without a support
  /// conversation. `requestMore` only.
  final List<String>? needs;

  /// A machine-readable statement of the same request, so a wallet can satisfy it
  /// without a human reading `needs`. `requestMore` only.
  final Map<String, dynamic>? presentationDefinition;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (role != null) 'role': role!,
        if (obligations != null) 'obligations': obligations!,
        if (bundleRef != null) 'bundleRef': bundleRef!,
        if (code != null) 'code': code!,
        if (reason != null) 'reason': reason!,
        if (queue != null) 'queue': queue!,
        if (needs != null) 'needs': needs!,
        if (presentationDefinition != null)
          'presentationDefinition': presentationDefinition!,
      };
}

/// A ceremony decision: the effect, plus its effect-dependent detail.
class Verdict {
  const Verdict({
    required this.effect,
    required this.withValue,
  });

  /// Read this payload from a decoded JSON object.
  factory Verdict.fromJson(Map<String, dynamic> json) => Verdict(
        effect: VerdictEffect(json['effect'] as String),
        withValue: VerdictWith.fromJson(json['with'] as Map<String, dynamic>),
      );

  final VerdictEffect effect;
  final VerdictWith withValue;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'effect': effect.value,
        'with': withValue.toJson(),
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Join-Requests Submit — response payload
class Response {
  const Response({
    required this.requestId,
    required this.verdict,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        requestId: json['requestId'] as String,
        verdict: Verdict.fromJson(json['verdict'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Id of the created join request (a UUID).
  final String requestId;

  /// What the community decided about this submission. `0.1` returned `status:
  /// "pending"` — a constant, which could express only one of the four outcomes a
  /// submission actually has. A policy that admits outright, refuses outright, or asks
  /// for more evidence had to be reported as 'pending' or not at all.
  final Verdict verdict;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'requestId': requestId,
        'verdict': verdict.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Join-Requests Submit — payload
class Payload {
  const Payload({
    required this.vp,
    this.registryConsent,
    this.extensions,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        vp: json['vp'] as Map<String, dynamic>,
        registryConsent: json['registryConsent'] as bool?,
        extensions: json['extensions'] as Map<String, dynamic>?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The applicant's W3C Verifiable Presentation (opaque here), satisfying the
  /// community's join policy. The applicant DID is the document proof's signer — not a
  /// payload field.
  final Map<String, dynamic> vp;

  /// Whether the applicant consents to trust-registry publication.
  final bool? registryConsent;

  /// Opaque applicant-supplied extension bag.
  final Map<String, dynamic>? extensions;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'vp': vp,
        if (registryConsent != null) 'registryConsent': registryConsent!,
        if (extensions != null) 'extensions': extensions!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/join-requests/submit/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/join-requests/submit/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/join-requests/submit/0.2","title":"VTC Join-Requests Submit — payload","type":"object","additionalProperties":false,"required":["vp"],"properties":{"vp":{"type":"object","description":"The applicant\'s W3C Verifiable Presentation (opaque here), satisfying the community\'s join policy. The applicant DID is the document proof\'s signer — not a payload field."},"registryConsent":{"type":"boolean","description":"Whether the applicant consents to trust-registry publication."},"extensions":{"type":"object","description":"Opaque applicant-supplied extension bag."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Join-Requests Submit — response payload","type":"object","additionalProperties":false,"required":["requestId","verdict"],"properties":{"requestId":{"type":"string","minLength":1,"description":"Id of the created join request (a UUID)."},"verdict":{"\$ref":"#/\$defs/Verdict","description":"What the community decided about this submission.\\n\\n`0.1` returned `status: \\"pending\\"` — a constant, which could express only one of the four outcomes a submission actually has. A policy that admits outright, refuses outright, or asks for more evidence had to be reported as \'pending\' or not at all."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Verdict":{"\$anchor":"verdict","title":"Verdict","type":"object","additionalProperties":false,"required":["effect","with"],"description":"A ceremony decision: the effect, plus its effect-dependent detail.","properties":{"effect":{"\$ref":"#/\$defs/VerdictEffect"},"with":{"\$ref":"#/\$defs/VerdictWith"}}},"VerdictWith":{"\$anchor":"verdictWith","title":"VerdictWith","type":"object","additionalProperties":false,"description":"The effect-dependent detail of a verdict.\\n\\nEvery member is optional at the schema level and which ones are meaningful depends on `effect`: `role` / `obligations` / `bundleRef` on `allow`, `code` / `reason` on `deny`, `queue` / `reason` on `refer`, `needs` / `presentationDefinition` on `requestMore`. The dependency is stated here rather than enforced by `if`/`then` per effect, so that the shape stays a single flat object a generated type can carry without a discriminated union per family — a deliberate trade of schema strictness for implementability, and the reason a consumer MUST branch on `effect` rather than on which members happen to be present.","properties":{"role":{"type":"string","minLength":1,"description":"The granted local role. `allow` only."},"obligations":{"type":"object","description":"Conditions attached to the grant. `allow` only."},"bundleRef":{"type":"object","description":"Pointer to a sealed credential bundle, added by the community where issuance occurred rather than emitted by the policy. `allow` only."},"code":{"type":"string","minLength":1,"description":"Stable refusal code, safe to branch on. `deny` only."},"reason":{"type":["string","null"],"maxLength":1024,"description":"Elaboration in prose, when the decider gave one. `deny` and `refer`."},"queue":{"type":"string","minLength":1,"description":"Which review queue the decision was parked in, so an applicant can be told who now holds it. `refer` only."},"needs":{"type":"array","items":{"type":"string","minLength":1},"description":"What further evidence is required, named so the applicant can act without a support conversation. `requestMore` only."},"presentationDefinition":{"type":"object","description":"A machine-readable statement of the same request, so a wallet can satisfy it without a human reading `needs`. `requestMore` only."}}},"VerdictEffect":{"\$anchor":"verdictEffect","title":"VerdictEffect","type":"string","enum":["allow","deny","refer","requestMore"],"description":"What the policy decided.\\n\\n`allow` — admitted. `deny` — refused, terminally for this submission. `refer` — parked for a human or quorum decision; the applicant is neither in nor out. `requestMore` — the policy cannot decide yet and names what further evidence it needs.\\n\\nThe four are not reducible to a pending/decided pair. `refer` and `requestMore` are both \'not decided\', but they place the next action with different parties: `refer` waits on the community, `requestMore` waits on the applicant. A consumer that cannot tell them apart cannot tell a user whether to wait or to act."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Join-Requests Submit — response payload","type":"object","additionalProperties":false,"required":["requestId","verdict"],"properties":{"requestId":{"type":"string","minLength":1,"description":"Id of the created join request (a UUID)."},"verdict":{"\$ref":"#/\$defs/Verdict","description":"What the community decided about this submission.\\n\\n`0.1` returned `status: \\"pending\\"` — a constant, which could express only one of the four outcomes a submission actually has. A policy that admits outright, refuses outright, or asks for more evidence had to be reported as \'pending\' or not at all."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Verdict":{"\$anchor":"verdict","title":"Verdict","type":"object","additionalProperties":false,"required":["effect","with"],"description":"A ceremony decision: the effect, plus its effect-dependent detail.","properties":{"effect":{"\$ref":"#/\$defs/VerdictEffect"},"with":{"\$ref":"#/\$defs/VerdictWith"}}},"VerdictWith":{"\$anchor":"verdictWith","title":"VerdictWith","type":"object","additionalProperties":false,"description":"The effect-dependent detail of a verdict.\\n\\nEvery member is optional at the schema level and which ones are meaningful depends on `effect`: `role` / `obligations` / `bundleRef` on `allow`, `code` / `reason` on `deny`, `queue` / `reason` on `refer`, `needs` / `presentationDefinition` on `requestMore`. The dependency is stated here rather than enforced by `if`/`then` per effect, so that the shape stays a single flat object a generated type can carry without a discriminated union per family — a deliberate trade of schema strictness for implementability, and the reason a consumer MUST branch on `effect` rather than on which members happen to be present.","properties":{"role":{"type":"string","minLength":1,"description":"The granted local role. `allow` only."},"obligations":{"type":"object","description":"Conditions attached to the grant. `allow` only."},"bundleRef":{"type":"object","description":"Pointer to a sealed credential bundle, added by the community where issuance occurred rather than emitted by the policy. `allow` only."},"code":{"type":"string","minLength":1,"description":"Stable refusal code, safe to branch on. `deny` only."},"reason":{"type":["string","null"],"maxLength":1024,"description":"Elaboration in prose, when the decider gave one. `deny` and `refer`."},"queue":{"type":"string","minLength":1,"description":"Which review queue the decision was parked in, so an applicant can be told who now holds it. `refer` only."},"needs":{"type":"array","items":{"type":"string","minLength":1},"description":"What further evidence is required, named so the applicant can act without a support conversation. `requestMore` only."},"presentationDefinition":{"type":"object","description":"A machine-readable statement of the same request, so a wallet can satisfy it without a human reading `needs`. `requestMore` only."}}},"VerdictEffect":{"\$anchor":"verdictEffect","title":"VerdictEffect","type":"string","enum":["allow","deny","refer","requestMore"],"description":"What the policy decided.\\n\\n`allow` — admitted. `deny` — refused, terminally for this submission. `refer` — parked for a human or quorum decision; the applicant is neither in nor out. `requestMore` — the policy cannot decide yet and names what further evidence it needs.\\n\\nThe four are not reducible to a pending/decided pair. `refer` and `requestMore` are both \'not decided\', but they place the next action with different parties: `refer` waits on the community, `requestMore` waits on the applicant. A consumer that cannot tell them apart cannot tell a user whether to wait or to act."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
