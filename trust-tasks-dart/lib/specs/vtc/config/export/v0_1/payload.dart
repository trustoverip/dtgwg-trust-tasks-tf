// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/config/export/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A community's published position on personhood. DTG Credentials §Personhood
/// Credentials places PHC status here rather than in the credential — "PHC status is
/// determined by governance and trust registries, not by credential structure" — so a
/// verifier deciding whether a VMC carries personhood weight reads this, not the
/// credential's type array. §Governance Considerations item 1 makes the
/// acceptable-IDVP list the community's to define and publish. Every member is a
/// declaration, not an enforcement: what a community actually accepts is decided by
/// its own policy. Absent means the community has not published a position, which is
/// not the same as asserting the negative.
class PersonhoodGovernance {
  const PersonhoodGovernance({
    this.realHuman,
    this.singleMembership,
    this.acceptedIdvps,
    this.governanceFrameworkUrl,
  });

  /// Read this payload from a decoded JSON object.
  factory PersonhoodGovernance.fromJson(Map<String, dynamic> json) =>
      PersonhoodGovernance(
        realHuman: json['realHuman'] as bool?,
        singleMembership: json['singleMembership'] as bool?,
        acceptedIdvps: json['acceptedIdvps'] == null
            ? null
            : (json['acceptedIdvps'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        governanceFrameworkUrl: json['governanceFrameworkUrl'] as String?,
      );

  /// Governance requires that members are real humans. Defaults to `false` when absent:
  /// a community that has not considered the question asserts nothing, which is the only
  /// safe default for a claim a verifier may rely on.
  final bool? realHuman;

  /// Governance requires that each person holds at most one membership in **this**
  /// community. Per-community by definition — the glossary says "exactly one membership
  /// in that VTC" — so a single community can satisfy it without any network above it.
  final bool? singleMembership;

  /// DIDs of the identity-verification providers whose credentials this community
  /// accepts as personhood evidence. A community that vets its own members in person
  /// lists its own community DID here — it is acting as its own IDVP, which §IDVC
  /// permits. An empty list means no list has been published, not that everything is
  /// accepted.
  final List<String>? acceptedIdvps;

  /// Where the governance framework these assertions refer to can be read.
  final String? governanceFrameworkUrl;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (realHuman != null) 'realHuman': realHuman!,
        if (singleMembership != null) 'singleMembership': singleMembership!,
        if (acceptedIdvps != null) 'acceptedIdvps': acceptedIdvps!,
        if (governanceFrameworkUrl != null)
          'governanceFrameworkUrl': governanceFrameworkUrl!,
      };
}

/// The community's declared relationship-identifier default, as defined on
/// `CommunityProfile` in the `vtc/_shared/0.1/community` schema. Carried in a config
/// export so a restore reproduces the declaration rather than silently reverting it to
/// the implementation default.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const CommunityProfileSnapshotRelationshipIdentifierDefault(
    String value) {
  static const CommunityProfileSnapshotRelationshipIdentifierDefault
      attributed =
      CommunityProfileSnapshotRelationshipIdentifierDefault('attributed');
  static const CommunityProfileSnapshotRelationshipIdentifierDefault pairwise =
      CommunityProfileSnapshotRelationshipIdentifierDefault('pairwise');

  /// Every value this specification's schema permits.
  static const List<CommunityProfileSnapshotRelationshipIdentifierDefault>
      values = <CommunityProfileSnapshotRelationshipIdentifierDefault>[
    attributed,
    pairwise
  ];
}

/// A community profile as a portable export carries it — the mutable profile members
/// plus the immutable identity they belong to. Distinct from `vtc/_shared/community`'s
/// `CommunityProfile`, which is the update-facing view and deliberately omits
/// `communityDid` so that a patch cannot re-point a community's identity. Here the DID
/// is REQUIRED and is the whole point: it is what lets an importing community refuse a
/// document taken from a different one. `registryStatus` is absent because
/// trust-registry reachability is a property of a running maintainer, not of exported
/// state.
class CommunityProfileSnapshot {
  const CommunityProfileSnapshot({
    required this.communityDid,
    required this.name,
    this.description,
    this.logoUrl,
    this.publicUrl,
    this.contactEmail,
    required this.language,
    this.relationshipIdentifierDefault,
    this.personhood,
    this.extensions,
    this.createdAt,
  });

  /// Read this payload from a decoded JSON object.
  factory CommunityProfileSnapshot.fromJson(Map<String, dynamic> json) =>
      CommunityProfileSnapshot(
        communityDid: json['communityDid'] as String,
        name: json['name'] as String,
        description: json['description'] as String?,
        logoUrl: json['logoUrl'] as String?,
        publicUrl: json['publicUrl'] as String?,
        contactEmail: json['contactEmail'] as String?,
        language: json['language'] as String,
        relationshipIdentifierDefault:
            json['relationshipIdentifierDefault'] == null
                ? null
                : CommunityProfileSnapshotRelationshipIdentifierDefault(
                    json['relationshipIdentifierDefault'] as String),
        personhood: json['personhood'] == null
            ? null
            : PersonhoodGovernance.fromJson(
                json['personhood'] as Map<String, dynamic>),
        extensions: json['extensions'] as Map<String, dynamic>?,
        createdAt: json['createdAt'] as String?,
      );

  /// DID of the community this document was taken from. Immutable, set at install.
  final String communityDid;
  final String name;
  final String? description;
  final String? logoUrl;
  final String? publicUrl;
  final String? contactEmail;

  /// BCP 47 language tag.
  final String language;

  /// The community's declared relationship-identifier default, as defined on
  /// `CommunityProfile` in the `vtc/_shared/0.1/community` schema. Carried in a config
  /// export so a restore reproduces the declaration rather than silently reverting it to
  /// the implementation default.
  final CommunityProfileSnapshotRelationshipIdentifierDefault?
      relationshipIdentifierDefault;

  /// The community's published personhood position, carried so an export restores it.
  /// Governance state, not runtime state — unlike `registryStatus`, which is
  /// deliberately absent here because reachability belongs to a running maintainer
  /// rather than to exported state.
  final PersonhoodGovernance? personhood;

  /// Opaque community-defined extension bag.
  final Map<String, dynamic>? extensions;

  /// When the community was created. Provenance only — an import never writes it.
  final String? createdAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'communityDid': communityDid,
        'name': name,
        if (description != null) 'description': description!,
        if (logoUrl != null) 'logoUrl': logoUrl!,
        if (publicUrl != null) 'publicUrl': publicUrl!,
        if (contactEmail != null) 'contactEmail': contactEmail!,
        'language': language,
        if (relationshipIdentifierDefault != null)
          'relationshipIdentifierDefault': relationshipIdentifierDefault!.value,
        if (personhood != null) 'personhood': personhood!.toJson(),
        if (extensions != null) 'extensions': extensions!,
        if (createdAt != null) 'createdAt': createdAt!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A community's portable configuration: its profile plus the configuration overrides
/// a maintainer stores for itself. Deliberately excludes per-host layers (environment
/// variables, on-disk config files) — those describe where a maintainer runs, not what
/// the community is, and carrying them would make an import overwrite the target
/// host's own deployment settings. This document is designed to survive a round-trip
/// through a file. An operator exports it, keeps it, and feeds it back later —
/// possibly to a different maintainer, possibly across a version boundary — so it is
/// self-describing rather than relying on the Type URI of the envelope that happened
/// to carry it.
class ConfigExportDocument {
  const ConfigExportDocument({
    required this.schemaVersion,
    required this.exportedAt,
    this.communityProfile,
    required this.configOverrides,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory ConfigExportDocument.fromJson(Map<String, dynamic> json) =>
      ConfigExportDocument(
        schemaVersion: json['schemaVersion'] as int,
        exportedAt: json['exportedAt'] as String,
        communityProfile: json['communityProfile'] == null
            ? null
            : CommunityProfileSnapshot.fromJson(
                json['communityProfile'] as Map<String, dynamic>),
        configOverrides: json['configOverrides'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Version of this document's own shape. Not redundant with the Type URI version: once
  /// the document is written to a file it is bare JSON with no envelope, and this is the
  /// only thing a later reader has to check it against. A consumer MUST reject a version
  /// it does not implement rather than guess.
  final int schemaVersion;

  /// When the export was taken. Provenance for the operator; a consumer does not act on
  /// it.
  final String exportedAt;

  /// The community's profile at export time. Absent when the community has no profile
  /// yet — a maintainer exported before bootstrap.
  final CommunityProfileSnapshot? communityProfile;

  /// Stored configuration overrides as `key → value`, keyed by the maintainer's own
  /// configuration registry (the same keys `config/show` and `config/patch` use). An
  /// empty object is valid and means no overrides are set.
  final Map<String, dynamic> configOverrides;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'schemaVersion': schemaVersion,
        'exportedAt': exportedAt,
        if (communityProfile != null)
          'communityProfile': communityProfile!.toJson(),
        'configOverrides': configOverrides,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Config Export — response payload
class Response {
  const Response({
    required this.document,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        document: ConfigExportDocument.fromJson(
            json['document'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The portable configuration document, ready to feed back to vtc/config/import.
  final ConfigExportDocument document;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'document': document.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// Export a community's portable configuration — its profile and its stored
/// configuration overrides — as one document. Takes no parameters: a partial export is
/// not portable, so there is nothing to select.
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/config/export/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/config/export/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/config/export/0.1","title":"VTC Config Export — payload","description":"Export a community\'s portable configuration — its profile and its stored configuration overrides — as one document. Takes no parameters: a partial export is not portable, so there is nothing to select.","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Config Export — response payload","type":"object","additionalProperties":false,"required":["document"],"properties":{"document":{"\$ref":"#/\$defs/ConfigExportDocument","description":"The portable configuration document, ready to feed back to vtc/config/import."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConfigExportDocument":{"\$anchor":"configExportDocument","title":"ConfigExportDocument","description":"A community\'s portable configuration: its profile plus the configuration overrides a maintainer stores for itself. Deliberately excludes per-host layers (environment variables, on-disk config files) — those describe where a maintainer runs, not what the community is, and carrying them would make an import overwrite the target host\'s own deployment settings.\\n\\nThis document is designed to survive a round-trip through a file. An operator exports it, keeps it, and feeds it back later — possibly to a different maintainer, possibly across a version boundary — so it is self-describing rather than relying on the Type URI of the envelope that happened to carry it.","type":"object","additionalProperties":false,"required":["schemaVersion","exportedAt","configOverrides"],"properties":{"schemaVersion":{"type":"integer","minimum":1,"description":"Version of this document\'s own shape. Not redundant with the Type URI version: once the document is written to a file it is bare JSON with no envelope, and this is the only thing a later reader has to check it against. A consumer MUST reject a version it does not implement rather than guess."},"exportedAt":{"type":"string","format":"date-time","description":"When the export was taken. Provenance for the operator; a consumer does not act on it."},"communityProfile":{"\$ref":"#/\$defs/CommunityProfileSnapshot","description":"The community\'s profile at export time. Absent when the community has no profile yet — a maintainer exported before bootstrap."},"configOverrides":{"type":"object","additionalProperties":true,"description":"Stored configuration overrides as `key → value`, keyed by the maintainer\'s own configuration registry (the same keys `config/show` and `config/patch` use). An empty object is valid and means no overrides are set."},"ext":{"\$ref":"#/\$defs/Ext"}}},"CommunityProfileSnapshot":{"\$anchor":"communityProfileSnapshot","title":"CommunityProfileSnapshot","description":"A community profile as a portable export carries it — the mutable profile members plus the immutable identity they belong to.\\n\\nDistinct from `vtc/_shared/community`\'s `CommunityProfile`, which is the update-facing view and deliberately omits `communityDid` so that a patch cannot re-point a community\'s identity. Here the DID is REQUIRED and is the whole point: it is what lets an importing community refuse a document taken from a different one. `registryStatus` is absent because trust-registry reachability is a property of a running maintainer, not of exported state.","type":"object","additionalProperties":false,"required":["communityDid","name","language"],"properties":{"communityDid":{"type":"string","minLength":1,"description":"DID of the community this document was taken from. Immutable, set at install."},"name":{"type":"string","minLength":1,"maxLength":256},"description":{"type":"string","maxLength":1024},"logoUrl":{"type":["string","null"]},"publicUrl":{"type":["string","null"]},"contactEmail":{"type":["string","null"]},"language":{"type":"string","minLength":1,"description":"BCP 47 language tag."},"relationshipIdentifierDefault":{"type":"string","enum":["attributed","pairwise"],"description":"The community\'s declared relationship-identifier default, as defined on `CommunityProfile` in the `vtc/_shared/0.1/community` schema. Carried in a config export so a restore reproduces the declaration rather than silently reverting it to the implementation default."},"personhood":{"\$ref":"#/\$defs/PersonhoodGovernance","description":"The community\'s published personhood position, carried so an export restores it. Governance state, not runtime state — unlike `registryStatus`, which is deliberately absent here because reachability belongs to a running maintainer rather than to exported state."},"extensions":{"type":"object","description":"Opaque community-defined extension bag."},"createdAt":{"type":"string","format":"date-time","description":"When the community was created. Provenance only — an import never writes it."}}},"PersonhoodGovernance":{"\$anchor":"personhoodGovernance","title":"PersonhoodGovernance","type":"object","additionalProperties":false,"description":"A community\'s published position on personhood.\\n\\nDTG Credentials §Personhood Credentials places PHC status here rather than in the credential — \\"PHC status is determined by governance and trust registries, not by credential structure\\" — so a verifier deciding whether a VMC carries personhood weight reads this, not the credential\'s type array. §Governance Considerations item 1 makes the acceptable-IDVP list the community\'s to define and publish.\\n\\nEvery member is a declaration, not an enforcement: what a community actually accepts is decided by its own policy. Absent means the community has not published a position, which is not the same as asserting the negative.","properties":{"realHuman":{"type":"boolean","description":"Governance requires that members are real humans. Defaults to `false` when absent: a community that has not considered the question asserts nothing, which is the only safe default for a claim a verifier may rely on."},"singleMembership":{"type":"boolean","description":"Governance requires that each person holds at most one membership in **this** community. Per-community by definition — the glossary says \\"exactly one membership in that VTC\\" — so a single community can satisfy it without any network above it."},"acceptedIdvps":{"type":"array","items":{"type":"string","minLength":1},"description":"DIDs of the identity-verification providers whose credentials this community accepts as personhood evidence. A community that vets its own members in person lists its own community DID here — it is acting as its own IDVP, which §IDVC permits. An empty list means no list has been published, not that everything is accepted."},"governanceFrameworkUrl":{"type":["string","null"],"description":"Where the governance framework these assertions refer to can be read."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Config Export — response payload","type":"object","additionalProperties":false,"required":["document"],"properties":{"document":{"\$ref":"#/\$defs/ConfigExportDocument","description":"The portable configuration document, ready to feed back to vtc/config/import."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ConfigExportDocument":{"\$anchor":"configExportDocument","title":"ConfigExportDocument","description":"A community\'s portable configuration: its profile plus the configuration overrides a maintainer stores for itself. Deliberately excludes per-host layers (environment variables, on-disk config files) — those describe where a maintainer runs, not what the community is, and carrying them would make an import overwrite the target host\'s own deployment settings.\\n\\nThis document is designed to survive a round-trip through a file. An operator exports it, keeps it, and feeds it back later — possibly to a different maintainer, possibly across a version boundary — so it is self-describing rather than relying on the Type URI of the envelope that happened to carry it.","type":"object","additionalProperties":false,"required":["schemaVersion","exportedAt","configOverrides"],"properties":{"schemaVersion":{"type":"integer","minimum":1,"description":"Version of this document\'s own shape. Not redundant with the Type URI version: once the document is written to a file it is bare JSON with no envelope, and this is the only thing a later reader has to check it against. A consumer MUST reject a version it does not implement rather than guess."},"exportedAt":{"type":"string","format":"date-time","description":"When the export was taken. Provenance for the operator; a consumer does not act on it."},"communityProfile":{"\$ref":"#/\$defs/CommunityProfileSnapshot","description":"The community\'s profile at export time. Absent when the community has no profile yet — a maintainer exported before bootstrap."},"configOverrides":{"type":"object","additionalProperties":true,"description":"Stored configuration overrides as `key → value`, keyed by the maintainer\'s own configuration registry (the same keys `config/show` and `config/patch` use). An empty object is valid and means no overrides are set."},"ext":{"\$ref":"#/\$defs/Ext"}}},"CommunityProfileSnapshot":{"\$anchor":"communityProfileSnapshot","title":"CommunityProfileSnapshot","description":"A community profile as a portable export carries it — the mutable profile members plus the immutable identity they belong to.\\n\\nDistinct from `vtc/_shared/community`\'s `CommunityProfile`, which is the update-facing view and deliberately omits `communityDid` so that a patch cannot re-point a community\'s identity. Here the DID is REQUIRED and is the whole point: it is what lets an importing community refuse a document taken from a different one. `registryStatus` is absent because trust-registry reachability is a property of a running maintainer, not of exported state.","type":"object","additionalProperties":false,"required":["communityDid","name","language"],"properties":{"communityDid":{"type":"string","minLength":1,"description":"DID of the community this document was taken from. Immutable, set at install."},"name":{"type":"string","minLength":1,"maxLength":256},"description":{"type":"string","maxLength":1024},"logoUrl":{"type":["string","null"]},"publicUrl":{"type":["string","null"]},"contactEmail":{"type":["string","null"]},"language":{"type":"string","minLength":1,"description":"BCP 47 language tag."},"relationshipIdentifierDefault":{"type":"string","enum":["attributed","pairwise"],"description":"The community\'s declared relationship-identifier default, as defined on `CommunityProfile` in the `vtc/_shared/0.1/community` schema. Carried in a config export so a restore reproduces the declaration rather than silently reverting it to the implementation default."},"personhood":{"\$ref":"#/\$defs/PersonhoodGovernance","description":"The community\'s published personhood position, carried so an export restores it. Governance state, not runtime state — unlike `registryStatus`, which is deliberately absent here because reachability belongs to a running maintainer rather than to exported state."},"extensions":{"type":"object","description":"Opaque community-defined extension bag."},"createdAt":{"type":"string","format":"date-time","description":"When the community was created. Provenance only — an import never writes it."}}},"PersonhoodGovernance":{"\$anchor":"personhoodGovernance","title":"PersonhoodGovernance","type":"object","additionalProperties":false,"description":"A community\'s published position on personhood.\\n\\nDTG Credentials §Personhood Credentials places PHC status here rather than in the credential — \\"PHC status is determined by governance and trust registries, not by credential structure\\" — so a verifier deciding whether a VMC carries personhood weight reads this, not the credential\'s type array. §Governance Considerations item 1 makes the acceptable-IDVP list the community\'s to define and publish.\\n\\nEvery member is a declaration, not an enforcement: what a community actually accepts is decided by its own policy. Absent means the community has not published a position, which is not the same as asserting the negative.","properties":{"realHuman":{"type":"boolean","description":"Governance requires that members are real humans. Defaults to `false` when absent: a community that has not considered the question asserts nothing, which is the only safe default for a claim a verifier may rely on."},"singleMembership":{"type":"boolean","description":"Governance requires that each person holds at most one membership in **this** community. Per-community by definition — the glossary says \\"exactly one membership in that VTC\\" — so a single community can satisfy it without any network above it."},"acceptedIdvps":{"type":"array","items":{"type":"string","minLength":1},"description":"DIDs of the identity-verification providers whose credentials this community accepts as personhood evidence. A community that vets its own members in person lists its own community DID here — it is acting as its own IDVP, which §IDVC permits. An empty list means no list has been published, not that everything is accepted."},"governanceFrameworkUrl":{"type":["string","null"],"description":"Where the governance framework these assertions refer to can be read."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
