// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/endorsements/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Stable identifier for an issued credential — the handle for revocation and audit.
/// Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be
/// parsed.
typedef CredentialId = String;

/// A pointer to an issued credential, without the credential itself. The counterpart
/// to IssuedCredential, for the far more common case of *reading about* a credential
/// rather than being handed one. A listing that embedded the signed credential in
/// every row would grow with the size of the credentials rather than the number of
/// them — a page of fifty is megabytes — and a reader that only needs to know a
/// credential exists, when it lapses, and how to revoke it does not need the bytes.
/// The holder can always fetch the credential itself by `credentialId`, and a verifier
/// can check revocation from the row's status-list slot without either. Reach for
/// IssuedCredential only at the moment of minting, where the caller has no other way
/// to receive what was just made for them.
class CredentialReference {
  const CredentialReference({
    required this.credentialId,
    this.issuedAt,
    this.expiresAt,
  });

  /// Read this payload from a decoded JSON object.
  factory CredentialReference.fromJson(Map<String, dynamic> json) =>
      CredentialReference(
        credentialId: json['credentialId'] as String,
        issuedAt: json['issuedAt'] as String?,
        expiresAt: json['expiresAt'] as String?,
      );

  final CredentialId credentialId;

  /// When the credential was minted.
  final String? issuedAt;

  /// When it lapses, or null when it does not.
  final String? expiresAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'credentialId': credentialId,
        if (issuedAt != null) 'issuedAt': issuedAt!,
        if (expiresAt != null) 'expiresAt': expiresAt!,
      };
}

/// Endorsement
class Endorsement {
  const Endorsement({
    required this.endorsementId,
    required this.typeUri,
    required this.subjectDid,
    this.claim,
    required this.issued,
    required this.statusListIndex,
    this.revokedAt,
  });

  /// Read this payload from a decoded JSON object.
  factory Endorsement.fromJson(Map<String, dynamic> json) => Endorsement(
        endorsementId: json['endorsementId'] as String,
        typeUri: json['typeUri'] as String,
        subjectDid: json['subjectDid'] as String,
        claim: json['claim'] as Map<String, dynamic>?,
        issued: CredentialReference.fromJson(
            json['issued'] as Map<String, dynamic>),
        statusListIndex: json['statusListIndex'] as int,
        revokedAt: json['revokedAt'] as String?,
      );

  /// Community-scoped identifier for this endorsement row.
  final String endorsementId;

  /// The registered endorsement type this VEC asserts; see vtc/endorsement-types/*.
  final String typeUri;

  /// DID of the endorsement's subject (becomes credentialSubject.id).
  final String subjectDid;

  /// The attested claim body, validated against the endorsement type's claimSchema when
  /// it declares one.
  final Map<String, dynamic>? claim;

  /// A pointer to the issued VEC — its identifier and lifetime, not its bytes.
  /// `endorsements/issue` additionally returns the credential itself, because that is
  /// the one call whose caller has no other way to receive it.
  final CredentialReference issued;

  /// The endorsement's slot on the community's shared Revocation status list. Published,
  /// so a foreign verifier can check revocation without contacting this community.
  final int statusListIndex;

  /// When the endorsement was revoked, or null while live.
  final String? revokedAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'endorsementId': endorsementId,
        'typeUri': typeUri,
        'subjectDid': subjectDid,
        if (claim != null) 'claim': claim!,
        'issued': issued.toJson(),
        'statusListIndex': statusListIndex,
        if (revokedAt != null) 'revokedAt': revokedAt!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Endorsements List — response payload
class Response {
  const Response({
    required this.items,
    this.nextCursor,
    this.totalEstimate,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        items: (json['items'] as List<dynamic>)
            .map((e) => Endorsement.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        totalEstimate: json['totalEstimate'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<Endorsement> items;

  /// Continuation token for the next page, or null when this is the last.
  final String? nextCursor;

  /// Approximate total matching endorsements, when the maintainer can cheaply estimate
  /// it.
  final int? totalEstimate;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'items': items.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (totalEstimate != null) 'totalEstimate': totalEstimate!,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Endorsements List — payload
class Payload {
  const Payload({
    this.typeUri,
    this.subjectDid,
    this.includeRevoked,
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        typeUri: json['typeUri'] as String?,
        subjectDid: json['subjectDid'] as String?,
        includeRevoked: json['includeRevoked'] as bool?,
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Filter to endorsements of this registered type.
  final String? typeUri;

  /// Filter to endorsements about this subject.
  final String? subjectDid;

  /// Include revoked rows. Defaults to true — both live and revoked rows surface;
  /// consumers filter on revokedAt.
  final bool? includeRevoked;

  /// Opaque continuation token from a previous page's nextCursor.
  final String? cursor;

  /// Page size; the maintainer clamps to 1..=200 (default 50).
  final int? limit;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (typeUri != null) 'typeUri': typeUri!,
        if (subjectDid != null) 'subjectDid': subjectDid!,
        if (includeRevoked != null) 'includeRevoked': includeRevoked!,
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/endorsements/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/endorsements/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/endorsements/list/0.1","title":"VTC Endorsements List — payload","type":"object","additionalProperties":false,"properties":{"typeUri":{"type":"string","minLength":1,"maxLength":512,"description":"Filter to endorsements of this registered type."},"subjectDid":{"type":"string","pattern":"^did:","description":"Filter to endorsements about this subject."},"includeRevoked":{"type":"boolean","description":"Include revoked rows. Defaults to true — both live and revoked rows surface; consumers filter on revokedAt."},"cursor":{"type":"string","description":"Opaque continuation token from a previous page\'s nextCursor."},"limit":{"type":"integer","minimum":1,"maximum":200,"description":"Page size; the maintainer clamps to 1..=200 (default 50)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Endorsements List — response payload","type":"object","additionalProperties":false,"required":["items"],"properties":{"items":{"type":"array","items":{"\$ref":"#/\$defs/Endorsement"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null when this is the last."},"totalEstimate":{"type":["integer","null"],"description":"Approximate total matching endorsements, when the maintainer can cheaply estimate it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Endorsement":{"\$anchor":"endorsement","title":"Endorsement","type":"object","additionalProperties":false,"required":["endorsementId","typeUri","subjectDid","issued","statusListIndex"],"properties":{"endorsementId":{"type":"string","minLength":1,"description":"Community-scoped identifier for this endorsement row."},"typeUri":{"type":"string","minLength":1,"maxLength":512,"description":"The registered endorsement type this VEC asserts; see vtc/endorsement-types/*."},"subjectDid":{"type":"string","pattern":"^did:","description":"DID of the endorsement\'s subject (becomes credentialSubject.id)."},"claim":{"type":"object","description":"The attested claim body, validated against the endorsement type\'s claimSchema when it declares one."},"issued":{"\$ref":"#/\$defs/CredentialReference","description":"A pointer to the issued VEC — its identifier and lifetime, not its bytes. `endorsements/issue` additionally returns the credential itself, because that is the one call whose caller has no other way to receive it."},"statusListIndex":{"type":"integer","minimum":0,"description":"The endorsement\'s slot on the community\'s shared Revocation status list. Published, so a foreign verifier can check revocation without contacting this community."},"revokedAt":{"type":["string","null"],"format":"date-time","description":"When the endorsement was revoked, or null while live."}}},"CredentialReference":{"\$anchor":"credentialReference","title":"CredentialReference","type":"object","additionalProperties":false,"description":"A pointer to an issued credential, without the credential itself.\\n\\nThe counterpart to IssuedCredential, for the far more common case of *reading about* a credential rather than being handed one. A listing that embedded the signed credential in every row would grow with the size of the credentials rather than the number of them — a page of fifty is megabytes — and a reader that only needs to know a credential exists, when it lapses, and how to revoke it does not need the bytes.\\n\\nThe holder can always fetch the credential itself by `credentialId`, and a verifier can check revocation from the row\'s status-list slot without either. Reach for IssuedCredential only at the moment of minting, where the caller has no other way to receive what was just made for them.","required":["credentialId"],"properties":{"credentialId":{"\$ref":"#/\$defs/CredentialId"},"issuedAt":{"type":"string","format":"date-time","description":"When the credential was minted."},"expiresAt":{"type":["string","null"],"format":"date-time","description":"When it lapses, or null when it does not."}}},"CredentialId":{"\$anchor":"credentialId","title":"CredentialId","description":"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.","type":"string","minLength":1}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Endorsements List — response payload","type":"object","additionalProperties":false,"required":["items"],"properties":{"items":{"type":"array","items":{"\$ref":"#/\$defs/Endorsement"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null when this is the last."},"totalEstimate":{"type":["integer","null"],"description":"Approximate total matching endorsements, when the maintainer can cheaply estimate it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Endorsement":{"\$anchor":"endorsement","title":"Endorsement","type":"object","additionalProperties":false,"required":["endorsementId","typeUri","subjectDid","issued","statusListIndex"],"properties":{"endorsementId":{"type":"string","minLength":1,"description":"Community-scoped identifier for this endorsement row."},"typeUri":{"type":"string","minLength":1,"maxLength":512,"description":"The registered endorsement type this VEC asserts; see vtc/endorsement-types/*."},"subjectDid":{"type":"string","pattern":"^did:","description":"DID of the endorsement\'s subject (becomes credentialSubject.id)."},"claim":{"type":"object","description":"The attested claim body, validated against the endorsement type\'s claimSchema when it declares one."},"issued":{"\$ref":"#/\$defs/CredentialReference","description":"A pointer to the issued VEC — its identifier and lifetime, not its bytes. `endorsements/issue` additionally returns the credential itself, because that is the one call whose caller has no other way to receive it."},"statusListIndex":{"type":"integer","minimum":0,"description":"The endorsement\'s slot on the community\'s shared Revocation status list. Published, so a foreign verifier can check revocation without contacting this community."},"revokedAt":{"type":["string","null"],"format":"date-time","description":"When the endorsement was revoked, or null while live."}}},"CredentialReference":{"\$anchor":"credentialReference","title":"CredentialReference","type":"object","additionalProperties":false,"description":"A pointer to an issued credential, without the credential itself.\\n\\nThe counterpart to IssuedCredential, for the far more common case of *reading about* a credential rather than being handed one. A listing that embedded the signed credential in every row would grow with the size of the credentials rather than the number of them — a page of fifty is megabytes — and a reader that only needs to know a credential exists, when it lapses, and how to revoke it does not need the bytes.\\n\\nThe holder can always fetch the credential itself by `credentialId`, and a verifier can check revocation from the row\'s status-list slot without either. Reach for IssuedCredential only at the moment of minting, where the caller has no other way to receive what was just made for them.","required":["credentialId"],"properties":{"credentialId":{"\$ref":"#/\$defs/CredentialId"},"issuedAt":{"type":"string","format":"date-time","description":"When the credential was minted."},"expiresAt":{"type":["string","null"],"format":"date-time","description":"When it lapses, or null when it does not."}}},"CredentialId":{"\$anchor":"credentialId","title":"CredentialId","description":"Stable identifier for an issued credential — the handle for revocation and audit. Opaque to the holder: it MUST be echoed verbatim when revoking and MUST NOT be parsed.","type":"string","minLength":1}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
