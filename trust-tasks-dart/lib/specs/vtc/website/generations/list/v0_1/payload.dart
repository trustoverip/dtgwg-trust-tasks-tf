// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/website/generations/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseGenerationsItem, generated from its schema.
class ResponseGenerationsItem {
  const ResponseGenerationsItem({
    required this.generation,
    required this.current,
    this.deployedAt,
    this.sizeBytes,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseGenerationsItem.fromJson(Map<String, dynamic> json) =>
      ResponseGenerationsItem(
        generation: json['generation'] as String,
        current: json['current'] as bool,
        deployedAt: json['deployedAt'] as String?,
        sizeBytes: json['sizeBytes'] as int?,
      );

  /// Generation label (e.g. gen-5).
  final String generation;

  /// True for the generation current resolves to.
  final bool current;

  /// When this generation was deployed. A rollback target is chosen by *when* far more
  /// often than by number.
  final String? deployedAt;

  /// Total size of the generation on disk. Distinguishes a real deployment from a
  /// truncated or empty one before an operator rolls onto it.
  final int? sizeBytes;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'generation': generation,
        'current': current,
        if (deployedAt != null) 'deployedAt': deployedAt!,
        if (sizeBytes != null) 'sizeBytes': sizeBytes!,
      };
}

/// VTC Website Generations List — response payload
class Response {
  const Response({
    required this.generations,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        generations: (json['generations'] as List<dynamic>)
            .map((e) =>
                ResponseGenerationsItem.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<ResponseGenerationsItem> generations;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'generations': generations.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Website Generations List — payload
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/website/generations/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/website/generations/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/website/generations/list/0.1","title":"VTC Website Generations List — payload","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Website Generations List — response payload","type":"object","additionalProperties":false,"required":["generations"],"properties":{"generations":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["generation","current"],"properties":{"generation":{"type":"string","minLength":1,"description":"Generation label (e.g. gen-5)."},"current":{"type":"boolean","description":"True for the generation current resolves to."},"deployedAt":{"type":"string","format":"date-time","description":"When this generation was deployed. A rollback target is chosen by *when* far more often than by number."},"sizeBytes":{"type":"integer","minimum":0,"description":"Total size of the generation on disk. Distinguishes a real deployment from a truncated or empty one before an operator rolls onto it."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Website Generations List — response payload","type":"object","additionalProperties":false,"required":["generations"],"properties":{"generations":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["generation","current"],"properties":{"generation":{"type":"string","minLength":1,"description":"Generation label (e.g. gen-5)."},"current":{"type":"boolean","description":"True for the generation current resolves to."},"deployedAt":{"type":"string","format":"date-time","description":"When this generation was deployed. A rollback target is chosen by *when* far more often than by number."},"sizeBytes":{"type":"integer","minimum":0,"description":"Total size of the generation on disk. Distinguishes a real deployment from a truncated or empty one before an operator rolls onto it."}}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
