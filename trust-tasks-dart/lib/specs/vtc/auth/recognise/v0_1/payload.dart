// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/auth/recognise/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// ResponseData, generated from its schema.
class ResponseData {
  const ResponseData({
    required this.accessToken,
    required this.accessExpiresAt,
    required this.foreignIssuerDid,
    required this.mappedRole,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseData.fromJson(Map<String, dynamic> json) => ResponseData(
        accessToken: json['accessToken'] as String,
        accessExpiresAt: json['accessExpiresAt'] as int,
        foreignIssuerDid: json['foreignIssuerDid'] as String,
        mappedRole: json['mappedRole'] as String,
      );

  final String accessToken;

  /// Unix-seconds expiry, clamped to min(local default, earliest of the vec/vmc
  /// validUntil).
  final int accessExpiresAt;
  final String foreignIssuerDid;

  /// The LOCAL role from cross_community_roles policy — not the foreign role.
  final String mappedRole;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'accessToken': accessToken,
        'accessExpiresAt': accessExpiresAt,
        'foreignIssuerDid': foreignIssuerDid,
        'mappedRole': mappedRole,
      };
}

/// VTC Auth Recognise — response payload
class Response {
  const Response({
    required this.sessionId,
    required this.data,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        sessionId: json['sessionId'] as String,
        data: ResponseData.fromJson(json['data'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A cross-community session id (xc-&lt;uuid&gt;), distinguishing it from local-member
  /// sessions.
  final String sessionId;
  final ResponseData data;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'sessionId': sessionId,
        'data': data.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Auth Recognise — payload
class Payload {
  const Payload({
    required this.vec,
    required this.vmc,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        vec: json['vec'] as Map<String, dynamic>,
        vmc: json['vmc'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A foreign-issued Verifiable Endorsement Credential carrying the role grant, with
  /// its own eddsa-jcs-2022 proof (opaque here).
  final Map<String, dynamic> vec;

  /// A foreign-issued Verifiable Membership Credential proving membership, optionally
  /// with credentialStatus for live revocation (opaque here).
  final Map<String, dynamic> vmc;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'vec': vec,
        'vmc': vmc,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/auth/recognise/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/auth/recognise/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/auth/recognise/0.1","title":"VTC Auth Recognise — payload","type":"object","additionalProperties":false,"required":["vec","vmc"],"properties":{"vec":{"type":"object","description":"A foreign-issued Verifiable Endorsement Credential carrying the role grant, with its own eddsa-jcs-2022 proof (opaque here)."},"vmc":{"type":"object","description":"A foreign-issued Verifiable Membership Credential proving membership, optionally with credentialStatus for live revocation (opaque here)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Auth Recognise — response payload","type":"object","additionalProperties":false,"required":["sessionId","data"],"properties":{"sessionId":{"type":"string","pattern":"^xc-","description":"A cross-community session id (xc-<uuid>), distinguishing it from local-member sessions."},"data":{"type":"object","additionalProperties":false,"required":["accessToken","accessExpiresAt","foreignIssuerDid","mappedRole"],"properties":{"accessToken":{"type":"string"},"accessExpiresAt":{"type":"integer","minimum":0,"description":"Unix-seconds expiry, clamped to min(local default, earliest of the vec/vmc validUntil)."},"foreignIssuerDid":{"type":"string"},"mappedRole":{"type":"string","description":"The LOCAL role from cross_community_roles policy — not the foreign role."}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Auth Recognise — response payload","type":"object","additionalProperties":false,"required":["sessionId","data"],"properties":{"sessionId":{"type":"string","pattern":"^xc-","description":"A cross-community session id (xc-<uuid>), distinguishing it from local-member sessions."},"data":{"type":"object","additionalProperties":false,"required":["accessToken","accessExpiresAt","foreignIssuerDid","mappedRole"],"properties":{"accessToken":{"type":"string"},"accessExpiresAt":{"type":"integer","minimum":0,"description":"Unix-seconds expiry, clamped to min(local default, earliest of the vec/vmc validUntil)."},"foreignIssuerDid":{"type":"string"},"mappedRole":{"type":"string","description":"The LOCAL role from cross_community_roles policy — not the foreign role."}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
