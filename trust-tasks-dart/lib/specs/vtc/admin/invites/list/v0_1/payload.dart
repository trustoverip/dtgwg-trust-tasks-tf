// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/admin/invites/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// `issued` is outstanding and revocable; `consumed` and `expired` are terminal.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const InviteStatus(String value) {
  static const InviteStatus issued = InviteStatus('issued');
  static const InviteStatus consumed = InviteStatus('consumed');
  static const InviteStatus expired = InviteStatus('expired');

  /// Every value this specification's schema permits.
  static const List<InviteStatus> values = <InviteStatus>[
    issued,
    consumed,
    expired
  ];
}

/// One outstanding or historical admin invite. Consumed rows are retained as audit
/// history and cannot be revoked.
class InviteSummary {
  const InviteSummary({
    required this.jti,
    required this.status,
    this.targetDid,
    this.expiresAt,
    this.consumedAt,
  });

  /// Read this payload from a decoded JSON object.
  factory InviteSummary.fromJson(Map<String, dynamic> json) => InviteSummary(
        jti: json['jti'] as String,
        status: InviteStatus(json['status'] as String),
        targetDid: json['targetDid'] as String?,
        expiresAt: json['expiresAt'] as String?,
        consumedAt: json['consumedAt'] as String?,
      );

  /// Unique invite identifier; the revoke target.
  final String jti;
  final InviteStatus status;

  /// The admin DID this invite enrols a passkey for.
  final String? targetDid;

  /// When an `issued` invite lapses.
  final String? expiresAt;

  /// When the invite was redeemed, if it was.
  final String? consumedAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jti': jti,
        'status': status.value,
        if (targetDid != null) 'targetDid': targetDid!,
        if (expiresAt != null) 'expiresAt': expiresAt!,
        if (consumedAt != null) 'consumedAt': consumedAt!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Admin Invites List — response payload
class Response {
  const Response({
    required this.invites,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        invites: (json['invites'] as List<dynamic>)
            .map((e) => InviteSummary.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Every invite the community holds, outstanding and terminal.
  final List<InviteSummary> invites;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'invites': invites.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Admin Invites List — payload
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/admin/invites/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/admin/invites/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/admin/invites/list/0.1","title":"VTC Admin Invites List — payload","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Admin Invites List — response payload","type":"object","additionalProperties":false,"required":["invites"],"properties":{"invites":{"type":"array","items":{"\$ref":"#/\$defs/InviteSummary"},"description":"Every invite the community holds, outstanding and terminal."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"InviteSummary":{"\$anchor":"inviteSummary","title":"InviteSummary","description":"One outstanding or historical admin invite. Consumed rows are retained as audit history and cannot be revoked.","type":"object","additionalProperties":false,"required":["jti","status"],"properties":{"jti":{"type":"string","minLength":1,"description":"Unique invite identifier; the revoke target."},"status":{"\$ref":"#/\$defs/InviteStatus"},"targetDid":{"type":"string","minLength":1,"description":"The admin DID this invite enrols a passkey for."},"expiresAt":{"type":"string","format":"date-time","description":"When an `issued` invite lapses."},"consumedAt":{"type":"string","format":"date-time","description":"When the invite was redeemed, if it was."}}},"InviteStatus":{"\$anchor":"inviteStatus","title":"InviteStatus","type":"string","enum":["issued","consumed","expired"],"description":"`issued` is outstanding and revocable; `consumed` and `expired` are terminal."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Admin Invites List — response payload","type":"object","additionalProperties":false,"required":["invites"],"properties":{"invites":{"type":"array","items":{"\$ref":"#/\$defs/InviteSummary"},"description":"Every invite the community holds, outstanding and terminal."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"InviteSummary":{"\$anchor":"inviteSummary","title":"InviteSummary","description":"One outstanding or historical admin invite. Consumed rows are retained as audit history and cannot be revoked.","type":"object","additionalProperties":false,"required":["jti","status"],"properties":{"jti":{"type":"string","minLength":1,"description":"Unique invite identifier; the revoke target."},"status":{"\$ref":"#/\$defs/InviteStatus"},"targetDid":{"type":"string","minLength":1,"description":"The admin DID this invite enrols a passkey for."},"expiresAt":{"type":"string","format":"date-time","description":"When an `issued` invite lapses."},"consumedAt":{"type":"string","format":"date-time","description":"When the invite was redeemed, if it was."}}},"InviteStatus":{"\$anchor":"inviteStatus","title":"InviteStatus","type":"string","enum":["issued","consumed","expired"],"description":"`issued` is outstanding and revocable; `consumed` and `expired` are terminal."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
