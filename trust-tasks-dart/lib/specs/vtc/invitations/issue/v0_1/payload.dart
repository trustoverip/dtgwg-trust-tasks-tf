// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/invitations/issue/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Invitations Issue — response payload
class Response {
  const Response({
    required this.subjectDid,
    this.validUntil,
    required this.vic,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        subjectDid: json['subjectDid'] as String,
        validUntil: json['validUntil'] as String?,
        vic: json['vic'] as Map<String, dynamic>,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The invited DID, echoed.
  final String subjectDid;

  /// Expiry, or null when the invitation does not lapse.
  final String? validUntil;

  /// The signed Invitation Credential (W3C VC). Opaque here; returned once.
  final Map<String, dynamic> vic;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subjectDid': subjectDid,
        if (validUntil != null) 'validUntil': validUntil!,
        'vic': vic,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Invitations Issue — payload
class Payload {
  const Payload({
    required this.subjectDid,
    this.validityDays,
    this.role,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        subjectDid: json['subjectDid'] as String,
        validityDays: json['validityDays'] as int?,
        role: json['role'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// DID the invitation admits.
  final String subjectDid;

  /// Credential lifetime in days. Omit for the community default.
  final int? validityDays;

  /// Role granted on redemption. Must be one this community defines.
  final String? role;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subjectDid': subjectDid,
        if (validityDays != null) 'validityDays': validityDays!,
        if (role != null) 'role': role!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/vtc/invitations/issue/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/invitations/issue/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/invitations/issue/0.1","title":"VTC Invitations Issue — payload","type":"object","additionalProperties":false,"required":["subjectDid"],"properties":{"subjectDid":{"type":"string","minLength":1,"description":"DID the invitation admits."},"validityDays":{"type":"integer","minimum":1,"description":"Credential lifetime in days. Omit for the community default."},"role":{"type":"string","minLength":1,"description":"Role granted on redemption. Must be one this community defines."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Invitations Issue — response payload","type":"object","additionalProperties":false,"required":["subjectDid","vic"],"properties":{"subjectDid":{"type":"string","minLength":1,"description":"The invited DID, echoed."},"validUntil":{"type":["string","null"],"format":"date-time","description":"Expiry, or null when the invitation does not lapse."},"vic":{"type":"object","description":"The signed Invitation Credential (W3C VC). Opaque here; returned once."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Invitations Issue — response payload","type":"object","additionalProperties":false,"required":["subjectDid","vic"],"properties":{"subjectDid":{"type":"string","minLength":1,"description":"The invited DID, echoed."},"validUntil":{"type":["string","null"],"format":"date-time","description":"Expiry, or null when the invitation does not lapse."},"vic":{"type":"object","description":"The signed Invitation Credential (W3C VC). Opaque here; returned once."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
