// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/invitations/deliver/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The channel used, echoed.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseChannel(String value) {
  static const ResponseChannel message = ResponseChannel('message');
  static const ResponseChannel offer = ResponseChannel('offer');

  /// Every value this specification's schema permits.
  static const List<ResponseChannel> values = <ResponseChannel>[message, offer];
}

/// VTC Invitations Deliver — response payload
class Response {
  const Response({
    required this.id,
    required this.channel,
    this.offer,
    required this.expiresAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        id: json['id'] as String,
        channel: ResponseChannel(json['channel'] as String),
        offer: json['offer'] as Map<String, dynamic>?,
        expiresAt: json['expiresAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The invitation's identifier, echoed.
  final String id;

  /// The channel used, echoed.
  final ResponseChannel channel;

  /// Present exactly when `channel` is `offer`: an OID4VCI Credential Offer object,
  /// carried verbatim, whose pre-authorized code redeems only for the invited DID. Its
  /// members are OID4VCI's and are deliberately not re-specified here.
  final Map<String, dynamic>? offer;

  /// When this delivery's offer lapses. Never later than the invitation's own expiry.
  final String expiresAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'channel': channel.value,
        if (offer != null) 'offer': offer!,
        'expiresAt': expiresAt,
        if (ext != null) 'ext': ext!,
      };
}

/// `message`: the community sends a `credential-exchange/offer` to the invited DID
/// over a transport that DID advertises. `offer`: the community returns the offer, for
/// the inviter to hand over out of band — typically as a QR code.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadChannel(String value) {
  static const PayloadChannel message = PayloadChannel('message');
  static const PayloadChannel offer = PayloadChannel('offer');

  /// Every value this specification's schema permits.
  static const List<PayloadChannel> values = <PayloadChannel>[message, offer];
}

/// Deliver an issued invitation to the DID it admits: push an offer to that DID, or
/// return the offer for a QR code. The outer document members (id, type, issuer,
/// recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.
class Payload {
  const Payload({
    required this.id,
    required this.channel,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        id: json['id'] as String,
        channel: PayloadChannel(json['channel'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Identifier of the invitation to deliver, as `vtc/invitations/list` reports it.
  final String id;

  /// `message`: the community sends a `credential-exchange/offer` to the invited DID
  /// over a transport that DID advertises. `offer`: the community returns the offer, for
  /// the inviter to hand over out of band — typically as a QR code.
  final PayloadChannel channel;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'channel': channel.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/invitations/deliver/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/invitations/deliver/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/invitations/deliver/0.1","title":"VTC Invitations Deliver — payload","description":"Deliver an issued invitation to the DID it admits: push an offer to that DID, or return the offer for a QR code. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.","type":"object","additionalProperties":false,"required":["id","channel"],"properties":{"id":{"type":"string","minLength":1,"maxLength":256,"description":"Identifier of the invitation to deliver, as `vtc/invitations/list` reports it."},"channel":{"type":"string","enum":["message","offer"],"description":"`message`: the community sends a `credential-exchange/offer` to the invited DID over a transport that DID advertises. `offer`: the community returns the offer, for the inviter to hand over out of band — typically as a QR code."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Invitations Deliver — response payload","type":"object","additionalProperties":false,"required":["id","channel","expiresAt"],"properties":{"id":{"type":"string","minLength":1,"maxLength":256,"description":"The invitation\'s identifier, echoed."},"channel":{"type":"string","enum":["message","offer"],"description":"The channel used, echoed."},"offer":{"type":"object","description":"Present exactly when `channel` is `offer`: an OID4VCI Credential Offer object, carried verbatim, whose pre-authorized code redeems only for the invited DID. Its members are OID4VCI\'s and are deliberately not re-specified here."},"expiresAt":{"type":"string","format":"date-time","description":"When this delivery\'s offer lapses. Never later than the invitation\'s own expiry."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Invitations Deliver — response payload","type":"object","additionalProperties":false,"required":["id","channel","expiresAt"],"properties":{"id":{"type":"string","minLength":1,"maxLength":256,"description":"The invitation\'s identifier, echoed."},"channel":{"type":"string","enum":["message","offer"],"description":"The channel used, echoed."},"offer":{"type":"object","description":"Present exactly when `channel` is `offer`: an OID4VCI Credential Offer object, carried verbatim, whose pre-authorized code redeems only for the invited DID. Its members are OID4VCI\'s and are deliberately not re-specified here."},"expiresAt":{"type":"string","format":"date-time","description":"When this delivery\'s offer lapses. Never later than the invitation\'s own expiry."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
