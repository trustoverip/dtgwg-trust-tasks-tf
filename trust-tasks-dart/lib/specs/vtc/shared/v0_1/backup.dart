// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/_shared/0.1/backup.schema.json

/// KdfParams
class KdfParams {
  const KdfParams({
    required this.algorithm,
    required this.salt,
    required this.mCost,
    required this.tCost,
    required this.pCost,
  });

  /// Read this payload from a decoded JSON object.
  factory KdfParams.fromJson(Map<String, dynamic> json) => KdfParams(
        algorithm: json['algorithm'] as String,
        salt: json['salt'] as String,
        mCost: json['mCost'] as int,
        tCost: json['tCost'] as int,
        pCost: json['pCost'] as int,
      );

  /// KDF identifier, e.g. `argon2id`.
  final String algorithm;

  /// base64url, 32 bytes.
  final String salt;

  /// Argon2id memory cost (KiB).
  final int mCost;

  /// Argon2id time cost (iterations).
  final int tCost;

  /// Argon2id parallelism.
  final int pCost;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'algorithm': algorithm,
        'salt': salt,
        'mCost': mCost,
        'tCost': tCost,
        'pCost': pCost,
      };
}

/// EncryptionParams
class EncryptionParams {
  const EncryptionParams({
    required this.algorithm,
    required this.nonce,
  });

  /// Read this payload from a decoded JSON object.
  factory EncryptionParams.fromJson(Map<String, dynamic> json) =>
      EncryptionParams(
        algorithm: json['algorithm'] as String,
        nonce: json['nonce'] as String,
      );

  /// AEAD identifier, e.g. `aes-256-gcm`.
  final String algorithm;

  /// base64url, 12 bytes.
  final String nonce;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'algorithm': algorithm,
        'nonce': nonce,
      };
}

/// Unencrypted metadata plus the encrypted payload. The KDF and cipher parameters
/// travel in the clear so a reader can decrypt without knowing this specification's
/// defaults.
class BackupEnvelope {
  const BackupEnvelope({
    required this.version,
    required this.format,
    required this.createdAt,
    this.sourceDid,
    required this.sourceVersion,
    required this.kdf,
    required this.encryption,
    required this.includesAudit,
    required this.ciphertext,
  });

  /// Read this payload from a decoded JSON object.
  factory BackupEnvelope.fromJson(Map<String, dynamic> json) => BackupEnvelope(
        version: json['version'] as int,
        format: json['format'] as String,
        createdAt: json['createdAt'] as String,
        sourceDid: json['sourceDid'] as String?,
        sourceVersion: json['sourceVersion'] as String,
        kdf: KdfParams.fromJson(json['kdf'] as Map<String, dynamic>),
        encryption: EncryptionParams.fromJson(
            json['encryption'] as Map<String, dynamic>),
        includesAudit: json['includesAudit'] as bool,
        ciphertext: json['ciphertext'] as String,
      );

  /// Envelope schema version.
  final int version;

  /// Envelope format tag, e.g. `vtc-backup-v1`.
  final String format;

  /// When the export was taken.
  final String createdAt;

  /// DID of the community exported from, when it has one. An import cross-checks this
  /// against the running community.
  final String? sourceDid;

  /// Version of the software that produced the envelope.
  final String sourceVersion;
  final KdfParams kdf;
  final EncryptionParams encryption;

  /// Whether the audit log and its signed checkpoints are inside `ciphertext`.
  final bool includesAudit;

  /// base64url(AEAD(JSON(payload))).
  final String ciphertext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'version': version,
        'format': format,
        'createdAt': createdAt,
        if (sourceDid != null) 'sourceDid': sourceDid!,
        'sourceVersion': sourceVersion,
        'kdf': kdf.toJson(),
        'encryption': encryption.toJson(),
        'includesAudit': includesAudit,
        'ciphertext': ciphertext,
      };
}
