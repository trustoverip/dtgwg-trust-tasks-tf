// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/_shared/0.1/member.schema.json

/// How the member's record is handled on departure.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const MemberResponseDeparturePreference(String value) {
  static const MemberResponseDeparturePreference purge =
      MemberResponseDeparturePreference('purge');
  static const MemberResponseDeparturePreference tombstone =
      MemberResponseDeparturePreference('tombstone');
  static const MemberResponseDeparturePreference historical =
      MemberResponseDeparturePreference('historical');
  static const MemberResponseDeparturePreference policydefault =
      MemberResponseDeparturePreference('policydefault');

  /// Every value this specification's schema permits.
  static const List<MemberResponseDeparturePreference> values =
      <MemberResponseDeparturePreference>[
    purge,
    tombstone,
    historical,
    policydefault
  ];
}

/// One community member as the maintainer sees it: the membership record joined with
/// its ACL role.
class MemberResponse {
  const MemberResponse({
    required this.did,
    required this.role,
    this.label,
    required this.joinedAt,
    required this.publishConsent,
    required this.departurePreference,
    this.statusListIndex,
    this.currentVmcId,
    this.currentRoleVecId,
    this.personhood,
    this.personhoodAssertedAt,
    this.joinedViaInvitation,
    this.memberVmcId,
    this.memberVmcReceivedAt,
    required this.extensions,
  });

  /// Read this payload from a decoded JSON object.
  factory MemberResponse.fromJson(Map<String, dynamic> json) => MemberResponse(
        did: json['did'] as String,
        role: json['role'] as String,
        label: json['label'] as String?,
        joinedAt: json['joinedAt'] as String,
        publishConsent: json['publishConsent'] as bool,
        departurePreference: MemberResponseDeparturePreference(
            json['departurePreference'] as String),
        statusListIndex: json['statusListIndex'] as int?,
        currentVmcId: json['currentVmcId'] as String?,
        currentRoleVecId: json['currentRoleVecId'] as String?,
        personhood: json['personhood'] as bool?,
        personhoodAssertedAt: json['personhoodAssertedAt'] as String?,
        joinedViaInvitation: json['joinedViaInvitation'] as bool?,
        memberVmcId: json['memberVmcId'] as String?,
        memberVmcReceivedAt: json['memberVmcReceivedAt'] as String?,
        extensions: json['extensions'] as Map<String, dynamic>,
      );

  /// The member's DID.
  final String did;

  /// The member's role in wire form (e.g. `admin`, `moderator`, `custom:editor`).
  final String role;

  /// Optional human-readable label.
  final String? label;
  final String joinedAt;

  /// Whether the member consented to being published in the community directory.
  final bool publishConsent;

  /// How the member's record is handled on departure.
  final MemberResponseDeparturePreference departurePreference;

  /// The member's slot on the community's membership status list, when allocated.
  final int? statusListIndex;

  /// Id of the member's current Verifiable Membership Credential, if issued.
  final String? currentVmcId;

  /// Id of the member's current role Verifiable Endorsement Credential, if issued.
  final String? currentRoleVecId;

  /// Whether the community has asserted that this member is a distinct real person.
  /// Read-only here: it is set and cleared by the personhood verbs, and cleared by a
  /// renewal-policy downgrade. Load-bearing rather than informational — a community that
  /// recognises members of another community may gate on it, so a consumer that cannot
  /// read it cannot make that decision.
  final bool? personhood;

  /// When personhood was most recently asserted. Absent where it never has been.
  /// Operator-facing: it belongs on admin-gated reads, and a member's own view of
  /// themselves need carry only the flag.
  final String? personhoodAssertedAt;

  /// Whether this member joined by presenting an invitation credential rather than by an
  /// admin's decision on an open request. The two routes to membership carry different
  /// evidence, and an operator reviewing a roster can otherwise not tell them apart.
  final bool? joinedViaInvitation;

  /// `id` of the member-issued reciprocal membership credential — the member half of the
  /// pair — once the member has sent it. Absent until then, which is the useful signal:
  /// it distinguishes a membership the community has asserted from one the member has
  /// acknowledged. The credential body is not echoed here.
  final String? memberVmcId;

  /// When that reciprocal credential was received. Paired with `memberVmcId`.
  final String? memberVmcReceivedAt;

  /// Opaque community-defined extension bag. Keys are maintainer-defined; the maintainer
  /// caps its size (16 KiB in the reference implementation).
  final Map<String, dynamic> extensions;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
        'role': role,
        if (label != null) 'label': label!,
        'joinedAt': joinedAt,
        'publishConsent': publishConsent,
        'departurePreference': departurePreference.value,
        if (statusListIndex != null) 'statusListIndex': statusListIndex!,
        if (currentVmcId != null) 'currentVmcId': currentVmcId!,
        if (currentRoleVecId != null) 'currentRoleVecId': currentRoleVecId!,
        if (personhood != null) 'personhood': personhood!,
        if (personhoodAssertedAt != null)
          'personhoodAssertedAt': personhoodAssertedAt!,
        if (joinedViaInvitation != null)
          'joinedViaInvitation': joinedViaInvitation!,
        if (memberVmcId != null) 'memberVmcId': memberVmcId!,
        if (memberVmcReceivedAt != null)
          'memberVmcReceivedAt': memberVmcReceivedAt!,
        'extensions': extensions,
      };
}
