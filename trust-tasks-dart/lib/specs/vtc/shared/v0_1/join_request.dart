// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/_shared/0.1/join-request.schema.json

/// JoinRequestStatus is a closed set of string values defined by this specification's
/// schema.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const JoinRequestStatus(String value) {
  static const JoinRequestStatus pending = JoinRequestStatus('pending');
  static const JoinRequestStatus approved = JoinRequestStatus('approved');
  static const JoinRequestStatus rejected = JoinRequestStatus('rejected');
  static const JoinRequestStatus withdrawn = JoinRequestStatus('withdrawn');
  static const JoinRequestStatus deferred = JoinRequestStatus('deferred');

  /// Every value this specification's schema permits.
  static const List<JoinRequestStatus> values = <JoinRequestStatus>[
    pending,
    approved,
    rejected,
    withdrawn,
    deferred
  ];
}

/// Why this request was refused, in terms meant for the applicant rather than the
/// operator. `null` unless the request was rejected. Distinct from `policyDecision`,
/// which records the community's internal verdict: both rejection paths — a policy
/// auto-deny at submit and an admin's later refusal — write this one, so a client
/// reads a single shape instead of reconciling two.
class JoinRequestDecision {
  const JoinRequestDecision({
    required this.code,
    this.reason,
    required this.decidedAt,
  });

  /// Read this payload from a decoded JSON object.
  factory JoinRequestDecision.fromJson(Map<String, dynamic> json) =>
      JoinRequestDecision(
        code: json['code'] as String,
        reason: json['reason'] as String?,
        decidedAt: json['decidedAt'] as String,
      );

  /// Stable refusal code, safe to branch on.
  final String code;

  /// Elaboration in prose, when the decider gave one.
  final String? reason;

  /// When the decision was taken — not when the poll answering it was produced. On an
  /// admin refusal the two diverge by however long the applicant takes to ask.
  final String decidedAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'code': code,
        if (reason != null) 'reason': reason!,
        'decidedAt': decidedAt,
      };
}

/// One application to join a Verifiable Trust Community.
class JoinRequest {
  const JoinRequest({
    required this.id,
    required this.applicantDid,
    required this.vp,
    this.vpClaims,
    required this.submittedAt,
    required this.status,
    this.policyDecision,
    this.decision,
    this.registryConsent,
    this.extensions,
  });

  /// Read this payload from a decoded JSON object.
  factory JoinRequest.fromJson(Map<String, dynamic> json) => JoinRequest(
        id: json['id'] as String,
        applicantDid: json['applicantDid'] as String,
        vp: json['vp'] as Map<String, dynamic>,
        vpClaims: json['vpClaims'] as Map<String, dynamic>?,
        submittedAt: json['submittedAt'] as String,
        status: JoinRequestStatus(json['status'] as String),
        policyDecision: json['policyDecision'] as Map<String, dynamic>?,
        decision: json['decision'] == null
            ? null
            : JoinRequestDecision.fromJson(
                json['decision'] as Map<String, dynamic>),
        registryConsent: json['registryConsent'] as bool?,
        extensions: json['extensions'] as Map<String, dynamic>?,
      );

  /// Stable id of this join request (a UUID).
  final String id;

  /// DID of the applicant.
  final String applicantDid;

  /// The W3C Verifiable Presentation the applicant submitted (opaque here).
  final Map<String, dynamic> vp;

  /// Canonical projection of `vp`, extracted when the request was submitted and used as
  /// the input the community's join policy reads. Carried on the row so an approval does
  /// not have to re-extract it, and opaque here: its members are whatever the
  /// community's policy asks of an applicant. `null` on a request recorded before a
  /// community began extracting one.
  final Map<String, dynamic>? vpClaims;
  final String submittedAt;
  final JoinRequestStatus status;

  /// The community policy verdict recorded for this request (opaque here); absent while
  /// pending.
  final Map<String, dynamic>? policyDecision;

  /// Why this request was refused, in terms meant for the applicant rather than the
  /// operator. `null` unless the request was rejected. Distinct from `policyDecision`,
  /// which records the community's internal verdict: both rejection paths — a policy
  /// auto-deny at submit and an admin's later refusal — write this one, so a client
  /// reads a single shape instead of reconciling two.
  final JoinRequestDecision? decision;

  /// Whether the applicant consented to trust-registry publication.
  final bool? registryConsent;

  /// Opaque community-defined extension bag.
  final Map<String, dynamic>? extensions;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'applicantDid': applicantDid,
        'vp': vp,
        if (vpClaims != null) 'vpClaims': vpClaims!,
        'submittedAt': submittedAt,
        'status': status.value,
        if (policyDecision != null) 'policyDecision': policyDecision!,
        if (decision != null) 'decision': decision!.toJson(),
        if (registryConsent != null) 'registryConsent': registryConsent!,
        if (extensions != null) 'extensions': extensions!,
      };
}
