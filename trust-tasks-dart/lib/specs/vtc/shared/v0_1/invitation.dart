// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/_shared/0.1/invitation.schema.json

/// Registry view of one issued Invitation Credential. Carries no credential material —
/// the VIC itself is returned only once, by `issue`.
class InvitationSummary {
  const InvitationSummary({
    required this.id,
    required this.subjectDid,
    this.role,
    required this.issuedBy,
    required this.issuedAt,
    this.validUntil,
    this.revokedAt,
  });

  /// Read this payload from a decoded JSON object.
  factory InvitationSummary.fromJson(Map<String, dynamic> json) =>
      InvitationSummary(
        id: json['id'] as String,
        subjectDid: json['subjectDid'] as String,
        role: json['role'] as String?,
        issuedBy: json['issuedBy'] as String,
        issuedAt: json['issuedAt'] as String,
        validUntil: json['validUntil'] as String?,
        revokedAt: json['revokedAt'] as String?,
      );

  /// VIC identifier; the revoke target.
  final String id;

  /// DID the invitation admits.
  final String subjectDid;

  /// Role granted on redemption, when the invitation names one.
  final String? role;

  /// DID of the operator who issued it.
  final String issuedBy;

  /// When it was issued.
  final String issuedAt;

  /// Expiry, when the invitation is time-bounded.
  final String? validUntil;

  /// When it was revoked; absent while live.
  final String? revokedAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'subjectDid': subjectDid,
        if (role != null) 'role': role!,
        'issuedBy': issuedBy,
        'issuedAt': issuedAt,
        if (validUntil != null) 'validUntil': validUntil!,
        if (revokedAt != null) 'revokedAt': revokedAt!,
      };
}
