// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/registry/sync-jobs/list/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// `failed` is terminal: the reconciler has abandoned the job and will not retry it
/// without an operator. `pending` and `inFlight` are moving on their own.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const State(String value) {
  static const State pending = State('pending');
  static const State inFlight = State('inFlight');
  static const State failed = State('failed');

  /// Every value this specification's schema permits.
  static const List<State> values = <State>[pending, inFlight, failed];
}

/// Which mutation the job carries to the registry.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Kind(String value) {
  static const Kind publishMember = Kind('publishMember');
  static const Kind updateMember = Kind('updateMember');
  static const Kind deleteMember = Kind('deleteMember');
  static const Kind markDeparted = Kind('markDeparted');

  /// Every value this specification's schema permits.
  static const List<Kind> values = <Kind>[
    publishMember,
    updateMember,
    deleteMember,
    markDeparted
  ];
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// Job, generated from its schema.
class Job {
  const Job({
    required this.jobId,
    required this.kind,
    required this.memberDid,
    required this.state,
    required this.attempts,
    this.lastError,
    required this.createdAt,
    this.lastAttemptedAt,
    this.nextAttemptAt,
    this.purgeDueAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Job.fromJson(Map<String, dynamic> json) => Job(
        jobId: json['jobId'] as String,
        kind: Kind(json['kind'] as String),
        memberDid: json['memberDid'] as String,
        state: State(json['state'] as String),
        attempts: json['attempts'] as int,
        lastError: json['lastError'] as String?,
        createdAt: json['createdAt'] as String,
        lastAttemptedAt: json['lastAttemptedAt'] as String?,
        nextAttemptAt: json['nextAttemptAt'] as String?,
        purgeDueAt: json['purgeDueAt'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Identifier of the queued job. The handle `vtc/registry/sync-jobs/retry` and
  /// `.../discard` take.
  final String jobId;
  final Kind kind;

  /// The member whose registry record the job carries. In the clear, because an operator
  /// holding a hash cannot tell who stopped publishing.
  final String memberDid;
  final State state;

  /// Attempts made so far. `1` on a failed job means the registry answered and refused;
  /// a value at the maintainer's retry ceiling means it never answered.
  final int attempts;

  /// The registry's last answer, verbatim. Diagnostic prose for an operator; a consumer
  /// MUST NOT parse it.
  final String? lastError;

  /// When the job was enqueued.
  final String createdAt;

  /// When it was last dispatched.
  final String? lastAttemptedAt;

  /// When it is next due. Null on a failed job: nothing is scheduled.
  final String? nextAttemptAt;

  /// When the maintainer's retention sweep will delete this row. A deadline, not a fix —
  /// the member stays unpublished.
  final String? purgeDueAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jobId': jobId,
        'kind': kind.value,
        'memberDid': memberDid,
        'state': state.value,
        'attempts': attempts,
        if (lastError != null) 'lastError': lastError!,
        'createdAt': createdAt,
        if (lastAttemptedAt != null) 'lastAttemptedAt': lastAttemptedAt!,
        if (nextAttemptAt != null) 'nextAttemptAt': nextAttemptAt!,
        if (purgeDueAt != null) 'purgeDueAt': purgeDueAt!,
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Registry Sync Jobs List — response payload
class Response {
  const Response({
    required this.items,
    this.nextCursor,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        items: (json['items'] as List<dynamic>)
            .map((e) => Job.fromJson(e as Map<String, dynamic>))
            .toList(),
        nextCursor: json['nextCursor'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final List<Job> items;

  /// Continuation token for the next page, or null when this is the last.
  final String? nextCursor;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'items': items.map((e) => e.toJson()).toList(),
        if (nextCursor != null) 'nextCursor': nextCursor!,
        if (ext != null) 'ext': ext!,
      };
}

/// Enumerate the community's trust-registry reconciliation queue. Each job is one
/// pending or abandoned attempt to publish a membership change to the registry.
class Payload {
  const Payload({
    this.state,
    this.cursor,
    this.limit,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        state: json['state'] == null ? null : State(json['state'] as String),
        cursor: json['cursor'] as String?,
        limit: json['limit'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Filter to jobs in this state. Omit for every state.
  final State? state;

  /// Opaque continuation token from a previous page's `nextCursor`.
  final String? cursor;

  /// Page size; the maintainer clamps to 1..=200 (default 50).
  final int? limit;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (state != null) 'state': state!.value,
        if (cursor != null) 'cursor': cursor!,
        if (limit != null) 'limit': limit!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/registry/sync-jobs/list/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/registry/sync-jobs/list/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/registry/sync-jobs/list/0.1","title":"VTC Registry Sync Jobs List — payload","description":"Enumerate the community\'s trust-registry reconciliation queue. Each job is one pending or abandoned attempt to publish a membership change to the registry.","type":"object","additionalProperties":false,"properties":{"state":{"\$ref":"#/\$defs/State","description":"Filter to jobs in this state. Omit for every state."},"cursor":{"type":"string","description":"Opaque continuation token from a previous page\'s `nextCursor`."},"limit":{"type":"integer","minimum":1,"maximum":200,"description":"Page size; the maintainer clamps to 1..=200 (default 50)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"State":{"type":"string","enum":["pending","inFlight","failed"],"description":"`failed` is terminal: the reconciler has abandoned the job and will not retry it without an operator. `pending` and `inFlight` are moving on their own."},"Kind":{"type":"string","enum":["publishMember","updateMember","deleteMember","markDeparted"],"description":"Which mutation the job carries to the registry."},"Job":{"type":"object","additionalProperties":false,"required":["jobId","kind","memberDid","state","attempts","createdAt"],"properties":{"jobId":{"type":"string","minLength":1,"description":"Identifier of the queued job. The handle `vtc/registry/sync-jobs/retry` and `.../discard` take."},"kind":{"\$ref":"#/\$defs/Kind"},"memberDid":{"type":"string","minLength":1,"description":"The member whose registry record the job carries. In the clear, because an operator holding a hash cannot tell who stopped publishing."},"state":{"\$ref":"#/\$defs/State"},"attempts":{"type":"integer","minimum":0,"description":"Attempts made so far. `1` on a failed job means the registry answered and refused; a value at the maintainer\'s retry ceiling means it never answered."},"lastError":{"type":["string","null"],"maxLength":1024,"description":"The registry\'s last answer, verbatim. Diagnostic prose for an operator; a consumer MUST NOT parse it."},"createdAt":{"type":"string","format":"date-time","description":"When the job was enqueued."},"lastAttemptedAt":{"type":["string","null"],"format":"date-time","description":"When it was last dispatched."},"nextAttemptAt":{"type":["string","null"],"format":"date-time","description":"When it is next due. Null on a failed job: nothing is scheduled."},"purgeDueAt":{"type":["string","null"],"format":"date-time","description":"When the maintainer\'s retention sweep will delete this row. A deadline, not a fix — the member stays unpublished."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Response":{"\$anchor":"response","title":"VTC Registry Sync Jobs List — response payload","type":"object","additionalProperties":false,"required":["items"],"properties":{"items":{"type":"array","items":{"\$ref":"#/\$defs/Job"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null when this is the last."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"State":{"type":"string","enum":["pending","inFlight","failed"],"description":"`failed` is terminal: the reconciler has abandoned the job and will not retry it without an operator. `pending` and `inFlight` are moving on their own."},"Kind":{"type":"string","enum":["publishMember","updateMember","deleteMember","markDeparted"],"description":"Which mutation the job carries to the registry."},"Job":{"type":"object","additionalProperties":false,"required":["jobId","kind","memberDid","state","attempts","createdAt"],"properties":{"jobId":{"type":"string","minLength":1,"description":"Identifier of the queued job. The handle `vtc/registry/sync-jobs/retry` and `.../discard` take."},"kind":{"\$ref":"#/\$defs/Kind"},"memberDid":{"type":"string","minLength":1,"description":"The member whose registry record the job carries. In the clear, because an operator holding a hash cannot tell who stopped publishing."},"state":{"\$ref":"#/\$defs/State"},"attempts":{"type":"integer","minimum":0,"description":"Attempts made so far. `1` on a failed job means the registry answered and refused; a value at the maintainer\'s retry ceiling means it never answered."},"lastError":{"type":["string","null"],"maxLength":1024,"description":"The registry\'s last answer, verbatim. Diagnostic prose for an operator; a consumer MUST NOT parse it."},"createdAt":{"type":"string","format":"date-time","description":"When the job was enqueued."},"lastAttemptedAt":{"type":["string","null"],"format":"date-time","description":"When it was last dispatched."},"nextAttemptAt":{"type":["string","null"],"format":"date-time","description":"When it is next due. Null on a failed job: nothing is scheduled."},"purgeDueAt":{"type":["string","null"],"format":"date-time","description":"When the maintainer\'s retention sweep will delete this row. A deadline, not a fix — the member stays unpublished."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Response":{"\$anchor":"response","title":"VTC Registry Sync Jobs List — response payload","type":"object","additionalProperties":false,"required":["items"],"properties":{"items":{"type":"array","items":{"\$ref":"#/\$defs/Job"}},"nextCursor":{"type":["string","null"],"description":"Continuation token for the next page, or null when this is the last."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
