// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/relationships/publish/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A cryptographic digest as a multibase-encoded multihash — the encoding the W3C
/// Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one
/// `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
/// algorithm in-band, so the value is self-describing and the wire format survives an
/// algorithm change without a schema revision; multibase does the same for the base
/// encoding, so a verifier never infers base58 from base64url by context. A bare hex
/// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract
/// and is non-conforming here. This definition constrains the *encoding only*. What
/// the digest is computed over is stated by each referencing field, because it differs
/// legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS)
/// canonicalization, while a digest over an opaque artifact is taken over its bytes. A
/// field whose input is a JSON document and which does not name a canonicalization is
/// not reproducible. Restricted to the two multibase headers W3C Controlled
/// Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u`
/// (base64url-no-pad). CID permits others but states that "interoperability is not
/// guaranteed between implementations using such values", and a registry whose purpose
/// is interoperability should not mint digests a conforming verifier may be unable to
/// read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I
/// and l, and an earlier permissive pattern let three published examples carry digests
/// that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with
/// `did:key` and `did:webvh`.
typedef DigestMultibase = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Relationships Publish — response payload
class Response {
  const Response({
    required this.id,
    required this.issuerDid,
    required this.subjectDid,
    required this.vrcDigestMultibase,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        id: json['id'] as String,
        issuerDid: json['issuerDid'] as String,
        subjectDid: json['subjectDid'] as String,
        vrcDigestMultibase: json['vrcDigestMultibase'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Id assigned to the stored relationship (a UUID).
  final String id;
  final String issuerDid;
  final String subjectDid;

  /// Digest over the RFC 8785 canonicalization of the stored VRC, for out-of-band
  /// integrity checks. The same value vtc/relationships/request reports on issuance and
  /// vtc/relationships/list reports per entry, so a member can tie the three together
  /// without re-hashing.
  final DigestMultibase vrcDigestMultibase;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'issuerDid': issuerDid,
        'subjectDid': subjectDid,
        'vrcDigestMultibase': vrcDigestMultibase,
        if (ext != null) 'ext': ext!,
      };
}

/// Proof that the caller controls the key behind the VRC's `issuer`, when that is not
/// the party issuing this document. A member publishes an edge under a relationship
/// DID — an identifier scoped to one counterparty, which names nobody. The community
/// still has to know a member published it, and the document's own `proof` says only
/// that the member sent it, not that they control the credential's issuing key.
/// Without this, any member handed a VRC could publish another party's edge. Omit it
/// when the VRC's `issuer` is this document's issuer: the document's own proof already
/// establishes control.
class PayloadPop {
  const PayloadPop({
    required this.type,
    required this.documentId,
    required this.vrcDigestMultibase,
    required this.proof,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadPop.fromJson(Map<String, dynamic> json) => PayloadPop(
        type: json['type'],
        documentId: json['documentId'] as String,
        vrcDigestMultibase: json['vrcDigestMultibase'] as String,
        proof: json['proof'] as Map<String, dynamic>,
      );

  /// Guards against a signature the issuer made over some other object being replayed
  /// here as authorization to publish.
  final Object? type;

  /// The `id` of the Trust Task document carrying this authorization. Binds the
  /// authorization to one document, so a captured one cannot be replayed in another —
  /// including by a different member. Every document carries a unique `id` (SPEC §4.3),
  /// which is why the binding needs no transport-specific notion of a session.
  final String documentId;

  /// Digest over the RFC 8785 canonicalization of the VRC this authorizes. Binds the
  /// authorization to one credential, so it cannot be moved to another.
  final DigestMultibase vrcDigestMultibase;

  /// Data-integrity proof over this object, by a verification method the VRC's `issuer`
  /// controls. This is the proof of possession; the members above only bound what it
  /// authorizes.
  final Map<String, dynamic> proof;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type,
        'documentId': documentId,
        'vrcDigestMultibase': vrcDigestMultibase,
        'proof': proof,
      };
}

/// VTC Relationships Publish — payload
class Payload {
  const Payload({
    required this.vrc,
    this.pop,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        vrc: json['vrc'] as Map<String, dynamic>,
        pop: json['pop'] == null
            ? null
            : PayloadPop.fromJson(json['pop'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// A signed W3C Verifiable Relationship Credential (opaque here). Its issuer MUST
  /// equal the document proof signer; credentialSubject.id names the subject member; it
  /// carries its own DataIntegrityProof.
  final Map<String, dynamic> vrc;

  /// Proof that the caller controls the key behind the VRC's `issuer`, when that is not
  /// the party issuing this document. A member publishes an edge under a relationship
  /// DID — an identifier scoped to one counterparty, which names nobody. The community
  /// still has to know a member published it, and the document's own `proof` says only
  /// that the member sent it, not that they control the credential's issuing key.
  /// Without this, any member handed a VRC could publish another party's edge. Omit it
  /// when the VRC's `issuer` is this document's issuer: the document's own proof already
  /// establishes control.
  final PayloadPop? pop;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'vrc': vrc,
        if (pop != null) 'pop': pop!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/relationships/publish/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/relationships/publish/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/relationships/publish/0.2","title":"VTC Relationships Publish — payload","type":"object","additionalProperties":false,"required":["vrc"],"properties":{"vrc":{"type":"object","description":"A signed W3C Verifiable Relationship Credential (opaque here). Its issuer MUST equal the document proof signer; credentialSubject.id names the subject member; it carries its own DataIntegrityProof."},"pop":{"type":"object","description":"Proof that the caller controls the key behind the VRC\'s `issuer`, when that is not the party issuing this document.\\n\\nA member publishes an edge under a relationship DID — an identifier scoped to one counterparty, which names nobody. The community still has to know a member published it, and the document\'s own `proof` says only that the member sent it, not that they control the credential\'s issuing key. Without this, any member handed a VRC could publish another party\'s edge.\\n\\nOmit it when the VRC\'s `issuer` is this document\'s issuer: the document\'s own proof already establishes control.","properties":{"type":{"const":"VrcPublishAuthorization","description":"Guards against a signature the issuer made over some other object being replayed here as authorization to publish."},"documentId":{"type":"string","description":"The `id` of the Trust Task document carrying this authorization. Binds the authorization to one document, so a captured one cannot be replayed in another — including by a different member. Every document carries a unique `id` (SPEC §4.3), which is why the binding needs no transport-specific notion of a session."},"vrcDigestMultibase":{"\$ref":"#/\$defs/DigestMultibase","description":"Digest over the RFC 8785 canonicalization of the VRC this authorizes. Binds the authorization to one credential, so it cannot be moved to another."},"proof":{"type":"object","description":"Data-integrity proof over this object, by a verification method the VRC\'s `issuer` controls. This is the proof of possession; the members above only bound what it authorizes."}},"required":["type","documentId","vrcDigestMultibase","proof"],"additionalProperties":false},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Relationships Publish — response payload","type":"object","additionalProperties":false,"required":["id","issuerDid","subjectDid","vrcDigestMultibase"],"properties":{"id":{"type":"string","minLength":1,"description":"Id assigned to the stored relationship (a UUID)."},"issuerDid":{"type":"string","minLength":1},"subjectDid":{"type":"string","minLength":1},"vrcDigestMultibase":{"\$ref":"#/\$defs/DigestMultibase","description":"Digest over the RFC 8785 canonicalization of the stored VRC, for out-of-band integrity checks. The same value vtc/relationships/request reports on issuance and vtc/relationships/list reports per entry, so a member can tie the three together without re-hashing."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Relationships Publish — response payload","type":"object","additionalProperties":false,"required":["id","issuerDid","subjectDid","vrcDigestMultibase"],"properties":{"id":{"type":"string","minLength":1,"description":"Id assigned to the stored relationship (a UUID)."},"issuerDid":{"type":"string","minLength":1},"subjectDid":{"type":"string","minLength":1},"vrcDigestMultibase":{"\$ref":"#/\$defs/DigestMultibase","description":"Digest over the RFC 8785 canonicalization of the stored VRC, for out-of-band integrity checks. The same value vtc/relationships/request reports on issuance and vtc/relationships/list reports per entry, so a member can tie the three together without re-hashing."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
