// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/relationships/graph/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// GraphNode
class GraphNode {
  const GraphNode({
    required this.did,
  });

  /// Read this payload from a decoded JSON object.
  factory GraphNode.fromJson(Map<String, dynamic> json) => GraphNode(
        did: json['did'] as String,
      );

  /// A DID participating in at least one live relationship.
  final String did;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'did': did,
      };
}

/// One published relationship credential: a **directed half** of an edge, asserted by
/// `issuerDid` about `subjectDid`. Body-free on purpose — the graph shows the shape of
/// the trust network, not credential contents. `id` is the row identifier a revoke
/// takes.
class GraphHalf {
  const GraphHalf({
    required this.id,
    required this.issuerDid,
    required this.subjectDid,
    required this.createdAt,
    this.personaDid,
  });

  /// Read this payload from a decoded JSON object.
  factory GraphHalf.fromJson(Map<String, dynamic> json) => GraphHalf(
        id: json['id'] as String,
        issuerDid: json['issuerDid'] as String,
        subjectDid: json['subjectDid'] as String,
        createdAt: json['createdAt'] as String,
        personaDid: json['personaDid'] as String?,
      );

  final String id;

  /// The asserting party.
  final String issuerDid;

  /// The party asserted about.
  final String subjectDid;
  final String createdAt;

  /// The persona this issuer has asserted on this half, when they have. The one place
  /// deliberate correlation becomes visible: two pairwise halves carrying the same
  /// `personaDid` are the same party, said so by that party. A consumer that cannot read
  /// it cannot honour a correlation its subject chose to publish.
  final String? personaDid;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'id': id,
        'issuerDid': issuerDid,
        'subjectDid': subjectDid,
        'createdAt': createdAt,
        if (personaDid != null) 'personaDid': personaDid!,
      };
}

/// One edge between a **pair** of identifiers, carrying every half published between
/// them. `0.1` called each credential an edge, which made a DTG edge inexpressible:
/// the two directed halves between the same pair are one relationship, and a consumer
/// given a flat list had to re-derive that pairing — sorting DIDs, grouping, and
/// deciding for itself what `complete` means. Two implementations doing that
/// independently will disagree at the margins, which is exactly the reasoning a schema
/// exists to settle once.
class GraphEdge {
  const GraphEdge({
    required this.endpoints,
    required this.halves,
    required this.complete,
  });

  /// Read this payload from a decoded JSON object.
  factory GraphEdge.fromJson(Map<String, dynamic> json) => GraphEdge(
        endpoints: (json['endpoints'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
        halves: (json['halves'] as List<dynamic>)
            .map((e) => GraphHalf.fromJson(e as Map<String, dynamic>))
            .toList(),
        complete: json['complete'] as bool,
      );

  /// The two endpoints, DID-sorted. Sorting gives the pair one identity whichever half
  /// was published first. Both entries are equal only for a self-issued credential.
  final List<String> endpoints;

  /// Every credential published between the endpoints, oldest first. One for a
  /// half-edge, two for the ordinary reciprocated edge, more where a party has
  /// re-issued.
  final List<GraphHalf> halves;

  /// True when both endpoints have asserted about each other. The distinction a flat
  /// list cannot make: an edge asserted by one party is a claim, and an edge asserted by
  /// both is a relationship.
  final bool complete;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'endpoints': endpoints,
        'halves': halves.map((e) => e.toJson()).toList(),
        'complete': complete,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Relationships Graph — response payload
class Response {
  const Response({
    required this.nodes,
    required this.edges,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        nodes: (json['nodes'] as List<dynamic>)
            .map((e) => GraphNode.fromJson(e as Map<String, dynamic>))
            .toList(),
        edges: (json['edges'] as List<dynamic>)
            .map((e) => GraphEdge.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// One per distinct DID appearing in a live edge. No isolated nodes.
  final List<GraphNode> nodes;

  /// One per **pair** of identifiers with at least one live credential between them, not
  /// one per credential.
  final List<GraphEdge> edges;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'nodes': nodes.map((e) => e.toJson()).toList(),
        'edges': edges.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// VTC Relationships Graph — payload
class Payload {
  const Payload({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/relationships/graph/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/relationships/graph/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/relationships/graph/0.2","title":"VTC Relationships Graph — payload","type":"object","additionalProperties":false,"properties":{"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Relationships Graph — response payload","type":"object","additionalProperties":false,"required":["nodes","edges"],"properties":{"nodes":{"type":"array","items":{"\$ref":"#/\$defs/GraphNode"},"description":"One per distinct DID appearing in a live edge. No isolated nodes."},"edges":{"type":"array","items":{"\$ref":"#/\$defs/GraphEdge"},"description":"One per **pair** of identifiers with at least one live credential between them, not one per credential."},"ext":{"\$ref":"#/\$defs/Ext"}}},"GraphNode":{"\$anchor":"graphNode","title":"GraphNode","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"A DID participating in at least one live relationship."}}},"GraphEdge":{"\$anchor":"graphEdge","title":"GraphEdge","type":"object","additionalProperties":false,"required":["endpoints","halves","complete"],"properties":{"endpoints":{"type":"array","items":{"type":"string","pattern":"^did:"},"minItems":2,"maxItems":2,"description":"The two endpoints, DID-sorted. Sorting gives the pair one identity whichever half was published first. Both entries are equal only for a self-issued credential."},"halves":{"type":"array","items":{"\$ref":"#/\$defs/GraphHalf"},"minItems":1,"description":"Every credential published between the endpoints, oldest first. One for a half-edge, two for the ordinary reciprocated edge, more where a party has re-issued."},"complete":{"type":"boolean","description":"True when both endpoints have asserted about each other. The distinction a flat list cannot make: an edge asserted by one party is a claim, and an edge asserted by both is a relationship."}},"description":"One edge between a **pair** of identifiers, carrying every half published between them.\\n\\n`0.1` called each credential an edge, which made a DTG edge inexpressible: the two directed halves between the same pair are one relationship, and a consumer given a flat list had to re-derive that pairing — sorting DIDs, grouping, and deciding for itself what `complete` means. Two implementations doing that independently will disagree at the margins, which is exactly the reasoning a schema exists to settle once."},"GraphHalf":{"\$anchor":"graphHalf","title":"GraphHalf","type":"object","additionalProperties":false,"description":"One published relationship credential: a **directed half** of an edge, asserted by `issuerDid` about `subjectDid`.\\n\\nBody-free on purpose — the graph shows the shape of the trust network, not credential contents. `id` is the row identifier a revoke takes.","required":["id","issuerDid","subjectDid","createdAt"],"properties":{"id":{"type":"string","minLength":1},"issuerDid":{"type":"string","pattern":"^did:","description":"The asserting party."},"subjectDid":{"type":"string","pattern":"^did:","description":"The party asserted about."},"createdAt":{"type":"string","format":"date-time"},"personaDid":{"type":["string","null"],"pattern":"^did:|^\$","description":"The persona this issuer has asserted on this half, when they have.\\n\\nThe one place deliberate correlation becomes visible: two pairwise halves carrying the same `personaDid` are the same party, said so by that party. A consumer that cannot read it cannot honour a correlation its subject chose to publish."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Relationships Graph — response payload","type":"object","additionalProperties":false,"required":["nodes","edges"],"properties":{"nodes":{"type":"array","items":{"\$ref":"#/\$defs/GraphNode"},"description":"One per distinct DID appearing in a live edge. No isolated nodes."},"edges":{"type":"array","items":{"\$ref":"#/\$defs/GraphEdge"},"description":"One per **pair** of identifiers with at least one live credential between them, not one per credential."},"ext":{"\$ref":"#/\$defs/Ext"}}},"GraphNode":{"\$anchor":"graphNode","title":"GraphNode","type":"object","additionalProperties":false,"required":["did"],"properties":{"did":{"type":"string","minLength":1,"description":"A DID participating in at least one live relationship."}}},"GraphEdge":{"\$anchor":"graphEdge","title":"GraphEdge","type":"object","additionalProperties":false,"required":["endpoints","halves","complete"],"properties":{"endpoints":{"type":"array","items":{"type":"string","pattern":"^did:"},"minItems":2,"maxItems":2,"description":"The two endpoints, DID-sorted. Sorting gives the pair one identity whichever half was published first. Both entries are equal only for a self-issued credential."},"halves":{"type":"array","items":{"\$ref":"#/\$defs/GraphHalf"},"minItems":1,"description":"Every credential published between the endpoints, oldest first. One for a half-edge, two for the ordinary reciprocated edge, more where a party has re-issued."},"complete":{"type":"boolean","description":"True when both endpoints have asserted about each other. The distinction a flat list cannot make: an edge asserted by one party is a claim, and an edge asserted by both is a relationship."}},"description":"One edge between a **pair** of identifiers, carrying every half published between them.\\n\\n`0.1` called each credential an edge, which made a DTG edge inexpressible: the two directed halves between the same pair are one relationship, and a consumer given a flat list had to re-derive that pairing — sorting DIDs, grouping, and deciding for itself what `complete` means. Two implementations doing that independently will disagree at the margins, which is exactly the reasoning a schema exists to settle once."},"GraphHalf":{"\$anchor":"graphHalf","title":"GraphHalf","type":"object","additionalProperties":false,"description":"One published relationship credential: a **directed half** of an edge, asserted by `issuerDid` about `subjectDid`.\\n\\nBody-free on purpose — the graph shows the shape of the trust network, not credential contents. `id` is the row identifier a revoke takes.","required":["id","issuerDid","subjectDid","createdAt"],"properties":{"id":{"type":"string","minLength":1},"issuerDid":{"type":"string","pattern":"^did:","description":"The asserting party."},"subjectDid":{"type":"string","pattern":"^did:","description":"The party asserted about."},"createdAt":{"type":"string","format":"date-time"},"personaDid":{"type":["string","null"],"pattern":"^did:|^\$","description":"The persona this issuer has asserted on this half, when they have.\\n\\nThe one place deliberate correlation becomes visible: two pairwise halves carrying the same `personaDid` are the same party, said so by that party. A consumer that cannot read it cannot honour a correlation its subject chose to publish."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
