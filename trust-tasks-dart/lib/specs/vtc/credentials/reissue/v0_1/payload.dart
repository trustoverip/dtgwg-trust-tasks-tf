// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/credentials/reissue/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// ReissueProgress
class Progress {
  const Progress({
    required this.total,
    required this.done,
    required this.failed,
    this.undeliverable,
  });

  /// Read this payload from a decoded JSON object.
  factory Progress.fromJson(Map<String, dynamic> json) => Progress(
        total: json['total'] as int,
        done: json['done'] as int,
        failed: json['failed'] as int,
        undeliverable: json['undeliverable'] as int?,
      );

  final int total;
  final int done;
  final int failed;

  /// Artefacts re-issued but not yet delivered to their holder — the holder is offline
  /// or unreachable. They are held for the holder's next renewal or contact.
  final int? undeliverable;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'total': total,
        'done': done,
        'failed': failed,
        if (undeliverable != null) 'undeliverable': undeliverable!,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// `planned` — a dry run. `running` — status lists are being re-signed.
/// `statusListsDone` — every status list is re-signed; artefacts are being re-issued
/// (or, for `statusListsOnly`, the job is done). `completed` — everything in scope is
/// re-signed and re-issued. `failed` — the job stopped; `artefacts.failed` and the
/// audit trail say where.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const ResponseState(String value) {
  static const ResponseState planned = ResponseState('planned');
  static const ResponseState running = ResponseState('running');
  static const ResponseState statusListsDone = ResponseState('statusListsDone');
  static const ResponseState completed = ResponseState('completed');
  static const ResponseState failed = ResponseState('failed');

  /// Every value this specification's schema permits.
  static const List<ResponseState> values = <ResponseState>[
    planned,
    running,
    statusListsDone,
    completed,
    failed
  ];
}

/// Success response. Type
/// https://trusttasks.org/spec/vtc/credentials/reissue/0.1#response. Counts only —
/// never a list of members or credentials.
class Response {
  const Response({
    required this.jobId,
    required this.state,
    required this.dryRun,
    required this.statusLists,
    required this.artefacts,
    this.signedWith,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        jobId: json['jobId'] as String,
        state: ResponseState(json['state'] as String),
        dryRun: json['dryRun'] as bool,
        statusLists:
            Progress.fromJson(json['statusLists'] as Map<String, dynamic>),
        artefacts: Progress.fromJson(json['artefacts'] as Map<String, dynamic>),
        signedWith: json['signedWith'] == null
            ? null
            : (json['signedWith'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The job this cause started. The same cause always names the same job.
  final String jobId;

  /// `planned` — a dry run. `running` — status lists are being re-signed.
  /// `statusListsDone` — every status list is re-signed; artefacts are being re-issued
  /// (or, for `statusListsOnly`, the job is done). `completed` — everything in scope is
  /// re-signed and re-issued. `failed` — the job stopped; `artefacts.failed` and the
  /// audit trail say where.
  final ResponseState state;
  final bool dryRun;
  final Progress statusLists;
  final Progress artefacts;

  /// The attestation keys (verification methods) the new signatures are made with.
  final List<String>? signedWith;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jobId': jobId,
        'state': state.value,
        'dryRun': dryRun,
        'statusLists': statusLists.toJson(),
        'artefacts': artefacts.toJson(),
        if (signedWith != null) 'signedWith': signedWith!,
        if (ext != null) 'ext': ext!,
      };
}

/// The revocation that requires re-issuance. The VTC MUST verify it against its own
/// DID's key roles (vta/webvh/dids/keys/list) rather than trust it.
class PayloadCause {
  const PayloadCause({
    required this.verificationMethod,
    this.rotationId,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadCause.fromJson(Map<String, dynamic> json) => PayloadCause(
        verificationMethod: json['verificationMethod'] as String,
        rotationId: json['rotationId'] as String?,
      );

  /// The attestation key of the community's DID that was revoked for compromise.
  final String verificationMethod;

  /// The VTA's rotation record (`kind: compromise`) for the revocation, when known.
  final String? rotationId;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'verificationMethod': verificationMethod,
        if (rotationId != null) 'rotationId': rotationId!,
      };
}

/// `all` (the default) re-signs status lists first and then re-issues artefacts.
/// `statusListsOnly` re-signs status lists and stops — for an operator who must
/// restore status checking within minutes and schedule the rest.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadScope(String value) {
  static const PayloadScope all = PayloadScope('all');
  static const PayloadScope statusListsOnly = PayloadScope('statusListsOnly');

  /// Every value this specification's schema permits.
  static const List<PayloadScope> values = <PayloadScope>[all, statusListsOnly];
}

/// Ask a VTC to re-sign its status lists and re-issue the attestation artefacts still
/// in force that an attestation key revoked for compromise signed, under the
/// community's current attestation key. Re-sending the same request returns the
/// progress of the job it started.
class Payload {
  const Payload({
    required this.cause,
    this.scope,
    this.dryRun,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        cause: PayloadCause.fromJson(json['cause'] as Map<String, dynamic>),
        scope: json['scope'] == null
            ? null
            : PayloadScope(json['scope'] as String),
        dryRun: json['dryRun'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The revocation that requires re-issuance. The VTC MUST verify it against its own
  /// DID's key roles (vta/webvh/dids/keys/list) rather than trust it.
  final PayloadCause cause;

  /// `all` (the default) re-signs status lists first and then re-issues artefacts.
  /// `statusListsOnly` re-signs status lists and stops — for an operator who must
  /// restore status checking within minutes and schedule the rest.
  final PayloadScope? scope;

  /// Report what would be re-signed and re-issued, without doing it. Absent reads as
  /// false.
  final bool? dryRun;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'cause': cause.toJson(),
        if (scope != null) 'scope': scope!.value,
        if (dryRun != null) 'dryRun': dryRun!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/credentials/reissue/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/credentials/reissue/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/credentials/reissue/0.1","title":"VTC Credentials Reissue — payload","description":"Ask a VTC to re-sign its status lists and re-issue the attestation artefacts still in force that an attestation key revoked for compromise signed, under the community\'s current attestation key. Re-sending the same request returns the progress of the job it started.","type":"object","additionalProperties":false,"required":["cause"],"properties":{"cause":{"type":"object","additionalProperties":false,"required":["verificationMethod"],"description":"The revocation that requires re-issuance. The VTC MUST verify it against its own DID\'s key roles (vta/webvh/dids/keys/list) rather than trust it.","properties":{"verificationMethod":{"type":"string","minLength":1,"description":"The attestation key of the community\'s DID that was revoked for compromise."},"rotationId":{"type":"string","minLength":1,"description":"The VTA\'s rotation record (`kind: compromise`) for the revocation, when known."}}},"scope":{"type":"string","enum":["all","statusListsOnly"],"description":"`all` (the default) re-signs status lists first and then re-issues artefacts. `statusListsOnly` re-signs status lists and stops — for an operator who must restore status checking within minutes and schedule the rest."},"dryRun":{"type":"boolean","description":"Report what would be re-signed and re-issued, without doing it. Absent reads as false."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Credentials Reissue — response payload","description":"Success response. Type https://trusttasks.org/spec/vtc/credentials/reissue/0.1#response. Counts only — never a list of members or credentials.","type":"object","additionalProperties":false,"required":["jobId","state","dryRun","statusLists","artefacts"],"properties":{"jobId":{"type":"string","minLength":1,"description":"The job this cause started. The same cause always names the same job."},"state":{"type":"string","enum":["planned","running","statusListsDone","completed","failed"],"description":"`planned` — a dry run. `running` — status lists are being re-signed. `statusListsDone` — every status list is re-signed; artefacts are being re-issued (or, for `statusListsOnly`, the job is done). `completed` — everything in scope is re-signed and re-issued. `failed` — the job stopped; `artefacts.failed` and the audit trail say where."},"dryRun":{"type":"boolean"},"statusLists":{"\$ref":"#/\$defs/Progress"},"artefacts":{"\$ref":"#/\$defs/Progress"},"signedWith":{"type":"array","items":{"type":"string"},"description":"The attestation keys (verification methods) the new signatures are made with."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Progress":{"title":"ReissueProgress","type":"object","additionalProperties":false,"required":["total","done","failed"],"properties":{"total":{"type":"integer","minimum":0},"done":{"type":"integer","minimum":0},"failed":{"type":"integer","minimum":0},"undeliverable":{"type":"integer","minimum":0,"description":"Artefacts re-issued but not yet delivered to their holder — the holder is offline or unreachable. They are held for the holder\'s next renewal or contact."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Credentials Reissue — response payload","description":"Success response. Type https://trusttasks.org/spec/vtc/credentials/reissue/0.1#response. Counts only — never a list of members or credentials.","type":"object","additionalProperties":false,"required":["jobId","state","dryRun","statusLists","artefacts"],"properties":{"jobId":{"type":"string","minLength":1,"description":"The job this cause started. The same cause always names the same job."},"state":{"type":"string","enum":["planned","running","statusListsDone","completed","failed"],"description":"`planned` — a dry run. `running` — status lists are being re-signed. `statusListsDone` — every status list is re-signed; artefacts are being re-issued (or, for `statusListsOnly`, the job is done). `completed` — everything in scope is re-signed and re-issued. `failed` — the job stopped; `artefacts.failed` and the audit trail say where."},"dryRun":{"type":"boolean"},"statusLists":{"\$ref":"#/\$defs/Progress"},"artefacts":{"\$ref":"#/\$defs/Progress"},"signedWith":{"type":"array","items":{"type":"string"},"description":"The attestation keys (verification methods) the new signatures are made with."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Progress":{"title":"ReissueProgress","type":"object","additionalProperties":false,"required":["total","done","failed"],"properties":{"total":{"type":"integer","minimum":0},"done":{"type":"integer","minimum":0},"failed":{"type":"integer","minimum":0},"undeliverable":{"type":"integer","minimum":0,"description":"Artefacts re-issued but not yet delivered to their holder — the holder is offline or unreachable. They are held for the holder\'s next renewal or contact."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
