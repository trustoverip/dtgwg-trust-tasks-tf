// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/vetting/revoke-statement/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The community recorded the notice.
class Response {
  const Response({
    required this.recordedAt,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        recordedAt: json['recordedAt'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// When the community recorded the notice. A repeated notice for the same statement
  /// returns the time the first was recorded: revocation converges, and a vetter
  /// retrying cannot move it.
  final String recordedAt;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'recordedAt': recordedAt,
        if (ext != null) 'ext': ext!,
      };
}

/// A cryptographic digest as a multibase-encoded multihash — the encoding the W3C
/// Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one
/// `did:webvh` uses for its SCID and entry hashes. Multihash carries the hash
/// algorithm in-band, so the value is self-describing and the wire format survives an
/// algorithm change without a schema revision; multibase does the same for the base
/// encoding, so a verifier never infers base58 from base64url by context. A bare hex
/// string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract
/// and is non-conforming here. This definition constrains the *encoding only*. What
/// the digest is computed over is stated by each referencing field, because it differs
/// legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS)
/// canonicalization, while a digest over an opaque artifact is taken over its bytes. A
/// field whose input is a JSON document and which does not name a canonicalization is
/// not reproducible. Restricted to the two multibase headers W3C Controlled
/// Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u`
/// (base64url-no-pad). CID permits others but states that "interoperability is not
/// guaranteed between implementations using such values", and a registry whose purpose
/// is interoperability should not mint digests a conforming verifier may be unable to
/// read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I
/// and l, and an earlier permissive pattern let three published examples carry digests
/// that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with
/// `did:key` and `did:webvh`.
typedef DigestMultibase = String;

/// OPTIONAL. `mistake` — the vetter attested in error. `newInformation` — the vetter
/// learned something that changes its view. `keyCompromise` — the key that signed the
/// statement may be in someone else's hands. `other` — none of these.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadReason(String value) {
  static const PayloadReason mistake = PayloadReason('mistake');
  static const PayloadReason newInformation = PayloadReason('newInformation');
  static const PayloadReason keyCompromise = PayloadReason('keyCompromise');
  static const PayloadReason other = PayloadReason('other');

  /// Every value this specification's schema permits.
  static const List<PayloadReason> values = <PayloadReason>[
    mistake,
    newInformation,
    keyCompromise,
    other
  ];
}

/// A vetter tells the community it withdraws a Vetting Statement it issued. The
/// community records the notice against the statement's id and digest, and from then
/// on that statement does not count. The response is the time the community recorded
/// the notice; repeating a notice returns the original time.
class Payload {
  const Payload({
    required this.statementId,
    required this.statementDigestMultibase,
    this.reason,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        statementId: json['statementId'] as String,
        statementDigestMultibase: json['statementDigestMultibase'] as String,
        reason: json['reason'] == null
            ? null
            : PayloadReason(json['reason'] as String),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The withdrawn statement's `id`, a URI (e.g. `urn:uuid:…`).
  final String statementId;

  /// Digest over the RFC 8785 canonicalization of the withdrawn statement exactly as
  /// issued, `proof` included (SHA-256 RECOMMENDED). Pins the notice to one credential,
  /// so it cannot be aimed at a different statement that reuses the id.
  final DigestMultibase statementDigestMultibase;

  /// OPTIONAL. `mistake` — the vetter attested in error. `newInformation` — the vetter
  /// learned something that changes its view. `keyCompromise` — the key that signed the
  /// statement may be in someone else's hands. `other` — none of these.
  final PayloadReason? reason;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'statementId': statementId,
        'statementDigestMultibase': statementDigestMultibase,
        if (reason != null) 'reason': reason!.value,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/vetting/revoke-statement/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/vetting/revoke-statement/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/vetting/revoke-statement/0.1","title":"VTC Vetting — Revoke Statement — payload","description":"A vetter tells the community it withdraws a Vetting Statement it issued. The community records the notice against the statement\'s id and digest, and from then on that statement does not count. The response is the time the community recorded the notice; repeating a notice returns the original time.","type":"object","additionalProperties":false,"required":["statementId","statementDigestMultibase"],"properties":{"statementId":{"type":"string","minLength":1,"maxLength":512,"pattern":"^[a-zA-Z][a-zA-Z0-9+.-]*:\\\\S+\$","description":"The withdrawn statement\'s `id`, a URI (e.g. `urn:uuid:…`)."},"statementDigestMultibase":{"\$ref":"#/\$defs/DigestMultibase","description":"Digest over the RFC 8785 canonicalization of the withdrawn statement exactly as issued, `proof` included (SHA-256 RECOMMENDED). Pins the notice to one credential, so it cannot be aimed at a different statement that reuses the id."},"reason":{"type":"string","enum":["mistake","newInformation","keyCompromise","other"],"description":"OPTIONAL. `mistake` — the vetter attested in error. `newInformation` — the vetter learned something that changes its view. `keyCompromise` — the key that signed the statement may be in someone else\'s hands. `other` — none of these."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting — Revoke Statement — response payload","description":"The community recorded the notice.","type":"object","additionalProperties":false,"required":["recordedAt"],"properties":{"recordedAt":{"type":"string","format":"date-time","description":"When the community recorded the notice. A repeated notice for the same statement returns the time the first was recorded: revocation converges, and a vetter retrying cannot move it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting — Revoke Statement — response payload","description":"The community recorded the notice.","type":"object","additionalProperties":false,"required":["recordedAt"],"properties":{"recordedAt":{"type":"string","format":"date-time","description":"When the community recorded the notice. A repeated notice for the same statement returns the time the first was recorded: revocation converges, and a vetter retrying cannot move it."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"DigestMultibase":{"title":"DigestMultibase","description":"A cryptographic digest as a multibase-encoded multihash — the encoding the W3C Verifiable Credentials Data Model 2.0 defines for `digestMultibase`, and the one `did:webvh` uses for its SCID and entry hashes.\\n\\nMultihash carries the hash algorithm in-band, so the value is self-describing and the wire format survives an algorithm change without a schema revision; multibase does the same for the base encoding, so a verifier never infers base58 from base64url by context. A bare hex string or a `sha-256:`-style prefix hard-codes one algorithm into the wire contract and is non-conforming here.\\n\\nThis definition constrains the *encoding only*. What the digest is computed over is stated by each referencing field, because it differs legitimately: a digest over a JSON document is taken over its RFC 8785 (JCS) canonicalization, while a digest over an opaque artifact is taken over its bytes. A field whose input is a JSON document and which does not name a canonicalization is not reproducible.\\n\\nRestricted to the two multibase headers W3C Controlled Identifiers 1.0 §2.4 normatively requires — `z` (base58btc) and `u` (base64url-no-pad). CID permits others but states that \\"interoperability is not guaranteed between implementations using such values\\", and a registry whose purpose is interoperability should not mint digests a conforming verifier may be unable to read. The alphabets are enforced rather than assumed: base58btc excludes 0, O, I and l, and an earlier permissive pattern let three published examples carry digests that were not valid base58 at all. base58btc is RECOMMENDED, for consistency with `did:key` and `did:webvh`.","type":"string","minLength":16,"pattern":"^(z[1-9A-HJ-NP-Za-km-z]+|u[A-Za-z0-9_-]+)\$","examples":["zQmbWqxBEKC3P8tqsKc98xmWNzrzDtRLMiMPL8wBuTGsMnR"]}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
