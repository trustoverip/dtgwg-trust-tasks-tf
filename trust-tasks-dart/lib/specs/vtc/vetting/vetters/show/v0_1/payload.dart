// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/vetting/vetters/show/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The state of a vetter's grant in this community. The three refusing values are
/// deliberately distinct: `revoked` says the community withdrew a grant it had made,
/// `expired` says one ran out, and `none` says there is nothing to withdraw. An
/// applicant reads those differently, and so does a vetter checking their own
/// standing.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const GrantStatus(String value) {
  static const GrantStatus live = GrantStatus('live');
  static const GrantStatus revoked = GrantStatus('revoked');
  static const GrantStatus expired = GrantStatus('expired');
  static const GrantStatus none = GrantStatus('none');

  /// Every value this specification's schema permits.
  static const List<GrantStatus> values = <GrantStatus>[
    live,
    revoked,
    expired,
    none
  ];
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// One vetter's grant status. Carries no profile: what a vetter published for a
/// directory is answered by vtc/vetting/vetters/list, and this task answers a caller
/// who already knows which vetter they mean.
class Response {
  const Response({
    required this.vetterDid,
    required this.status,
    this.grantId,
    this.validUntil,
    this.revokedAt,
    this.listed,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        vetterDid: json['vetterDid'] as String,
        status: GrantStatus(json['status'] as String),
        grantId: json['grantId'] as String?,
        validUntil: json['validUntil'] as String?,
        revokedAt: json['revokedAt'] as String?,
        listed: json['listed'] as bool?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID asked about, echoed so a response is self-contained.
  final String vetterDid;
  final GrantStatus status;

  /// The identifier of the grant this status is about. Present for `live`, `revoked` and
  /// `expired`; absent for `none`, which is the absence of a grant and so has nothing to
  /// identify.
  final String? grantId;

  /// The grant's `validUntil`. Present for `live` and `expired` — for `expired` it is
  /// when it ran out, which tells a caller whether it lapsed long ago or yesterday.
  /// Absent for `none`, and for `revoked`, where the revocation and not the expiry is
  /// what ended it.
  final String? validUntil;

  /// When the grant was revoked. Present only for `revoked`. The reason is deliberately
  /// not carried: it is the community's internal record, and an applicant needs to know
  /// the grant does not hold, not why.
  final String? revokedAt;

  /// Whether this vetter has a profile stored with `listed: true`, and so would appear
  /// in vtc/vetting/vetters/list. Present only for `live`. It is what separates the two
  /// reasons a live vetter is missing from a listing: unlisted by choice, rather than
  /// not a vetter.
  final bool? listed;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'vetterDid': vetterDid,
        'status': status.value,
        if (grantId != null) 'grantId': grantId!,
        if (validUntil != null) 'validUntil': validUntil!,
        if (revokedAt != null) 'revokedAt': revokedAt!,
        if (listed != null) 'listed': listed!,
        if (ext != null) 'ext': ext!,
      };
}

/// An authenticated applicant or member asks a community about one vetter, by DID. The
/// answer is the state of that vetter's grant — `live`, `revoked`, `expired` or `none`
/// — which a list of vetters cannot express, because an absent vetter and a revoked
/// one look identical in it. The outer document members (id, type, issuer, recipient,
/// issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.
class Payload {
  const Payload({
    required this.vetterDid,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        vetterDid: json['vetterDid'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The DID of the vetter being asked about — the subject of the vetter role
  /// credential. The caller usually holds it from an earlier listing, or from a vetting
  /// exchange that has since gone quiet.
  final String vetterDid;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'vetterDid': vetterDid,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/show/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/show/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/vetting/vetters/show/0.1","title":"VTC Vetting — Show Vetter — payload","description":"An authenticated applicant or member asks a community about one vetter, by DID. The answer is the state of that vetter\'s grant — `live`, `revoked`, `expired` or `none` — which a list of vetters cannot express, because an absent vetter and a revoked one look identical in it. The outer document members (id, type, issuer, recipient, issuedAt, expiresAt, proof) are owned by the framework — SPEC §6.3.","type":"object","additionalProperties":false,"required":["vetterDid"],"properties":{"vetterDid":{"type":"string","pattern":"^did:","minLength":1,"maxLength":2048,"description":"The DID of the vetter being asked about — the subject of the vetter role credential. The caller usually holds it from an earlier listing, or from a vetting exchange that has since gone quiet."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"GrantStatus":{"title":"GrantStatus","description":"The state of a vetter\'s grant in this community. The three refusing values are deliberately distinct: `revoked` says the community withdrew a grant it had made, `expired` says one ran out, and `none` says there is nothing to withdraw. An applicant reads those differently, and so does a vetter checking their own standing.","type":"string","enum":["live","revoked","expired","none"]},"Response":{"\$anchor":"response","title":"VTC Vetting — Show Vetter — response payload","description":"One vetter\'s grant status. Carries no profile: what a vetter published for a directory is answered by vtc/vetting/vetters/list, and this task answers a caller who already knows which vetter they mean.","type":"object","additionalProperties":false,"required":["vetterDid","status"],"properties":{"vetterDid":{"type":"string","pattern":"^did:","description":"The DID asked about, echoed so a response is self-contained."},"status":{"\$ref":"#/\$defs/GrantStatus"},"grantId":{"type":"string","minLength":1,"maxLength":512,"description":"The identifier of the grant this status is about. Present for `live`, `revoked` and `expired`; absent for `none`, which is the absence of a grant and so has nothing to identify."},"validUntil":{"type":"string","format":"date-time","description":"The grant\'s `validUntil`. Present for `live` and `expired` — for `expired` it is when it ran out, which tells a caller whether it lapsed long ago or yesterday. Absent for `none`, and for `revoked`, where the revocation and not the expiry is what ended it."},"revokedAt":{"type":"string","format":"date-time","description":"When the grant was revoked. Present only for `revoked`. The reason is deliberately not carried: it is the community\'s internal record, and an applicant needs to know the grant does not hold, not why."},"listed":{"type":"boolean","description":"Whether this vetter has a profile stored with `listed: true`, and so would appear in vtc/vetting/vetters/list. Present only for `live`. It is what separates the two reasons a live vetter is missing from a listing: unlisted by choice, rather than not a vetter."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"GrantStatus":{"title":"GrantStatus","description":"The state of a vetter\'s grant in this community. The three refusing values are deliberately distinct: `revoked` says the community withdrew a grant it had made, `expired` says one ran out, and `none` says there is nothing to withdraw. An applicant reads those differently, and so does a vetter checking their own standing.","type":"string","enum":["live","revoked","expired","none"]},"Response":{"\$anchor":"response","title":"VTC Vetting — Show Vetter — response payload","description":"One vetter\'s grant status. Carries no profile: what a vetter published for a directory is answered by vtc/vetting/vetters/list, and this task answers a caller who already knows which vetter they mean.","type":"object","additionalProperties":false,"required":["vetterDid","status"],"properties":{"vetterDid":{"type":"string","pattern":"^did:","description":"The DID asked about, echoed so a response is self-contained."},"status":{"\$ref":"#/\$defs/GrantStatus"},"grantId":{"type":"string","minLength":1,"maxLength":512,"description":"The identifier of the grant this status is about. Present for `live`, `revoked` and `expired`; absent for `none`, which is the absence of a grant and so has nothing to identify."},"validUntil":{"type":"string","format":"date-time","description":"The grant\'s `validUntil`. Present for `live` and `expired` — for `expired` it is when it ran out, which tells a caller whether it lapsed long ago or yesterday. Absent for `none`, and for `revoked`, where the revocation and not the expiry is what ended it."},"revokedAt":{"type":"string","format":"date-time","description":"When the grant was revoked. Present only for `revoked`. The reason is deliberately not carried: it is the community\'s internal record, and an applicant needs to know the grant does not hold, not why."},"listed":{"type":"boolean","description":"Whether this vetter has a profile stored with `listed: true`, and so would appear in vtc/vetting/vetters/list. Present only for `live`. It is what separates the two reasons a live vetter is missing from a listing: unlisted by choice, rather than not a vetter."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: false,
  payloadSchema: responsePayloadSchemaJson,
);
