// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/vetting/vetters/profile/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The community stored the profile.
class Response {
  const Response({
    required this.listed,
    required this.updatedAt,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        listed: json['listed'] as bool,
        updatedAt: json['updatedAt'] as String,
      );

  /// The stored profile's `listed` value.
  final bool listed;

  /// When the community stored this profile. vtc/vetting/vetters/list returns the same
  /// value.
  final String updatedAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'listed': listed,
        'updatedAt': updatedAt,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// A calendar date, `YYYY-MM-DD` (RFC 3339 full-date), with no time or zone. Compared
/// as a UTC date.
typedef CalendarDate = String;

/// An ISO 3166-1 alpha-2 country code, upper case, e.g. `DE`.
typedef CountryCode = String;

/// A region or city name as the vetter writes it. Compared case-insensitively and
/// otherwise exactly.
typedef PlaceName = String;

/// Where a vetter can meet people, as coarse as the vetter chooses: a country,
/// optionally a region, optionally a city. Never a street address.
class VetterLocation {
  const VetterLocation({
    required this.country,
    this.region,
    this.city,
  });

  /// Read this payload from a decoded JSON object.
  factory VetterLocation.fromJson(Map<String, dynamic> json) => VetterLocation(
        country: json['country'] as String,
        region: json['region'] as String?,
        city: json['city'] as String?,
      );

  final CountryCode country;
  final PlaceName? region;
  final PlaceName? city;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'country': country,
        if (region != null) 'region': region!,
        if (city != null) 'city': city!,
      };
}

/// An event the vetter will attend and vet at. `endDate` is on or after `startDate`
/// and no more than 31 days after it; JSON Schema cannot compare two members, so the
/// community checks both and refuses a violation with `malformedRequest`.
class VetterEvent {
  const VetterEvent({
    required this.name,
    required this.startDate,
    required this.endDate,
    this.location,
    this.url,
  });

  /// Read this payload from a decoded JSON object.
  factory VetterEvent.fromJson(Map<String, dynamic> json) => VetterEvent(
        name: json['name'] as String,
        startDate: json['startDate'] as String,
        endDate: json['endDate'] as String,
        location: json['location'] == null
            ? null
            : VetterLocation.fromJson(json['location'] as Map<String, dynamic>),
        url: json['url'] as String?,
      );

  /// The event's name, e.g. `Linux Plumbers Conference 2026`.
  final String name;
  final CalendarDate startDate;
  final CalendarDate endDate;
  final VetterLocation? location;

  /// OPTIONAL. The event's own https page.
  final String? url;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'name': name,
        'startDate': startDate,
        'endDate': endDate,
        if (location != null) 'location': location!.toJson(),
        if (url != null) 'url': url!,
      };
}

/// Vetter-authored free text on how to obtain a ticket from them, e.g. `Find me at the
/// OpenVTC booth`. A hint, not an address a request can be sent to: vetting/request
/// still needs a ticket or an introduction the vetter accepts.
typedef VetterContactHint = String;

/// Vetter-authored free text on when they are available to vet. Attributed to the
/// vetter.
typedef VetterAvailability = String;

/// A class of documentation, named in lowerCamelCase. Open rather than enumerated,
/// because what documentation a vetter accepts is each vetter's own choice. Well-known
/// values: `passport`, `nationalId`, `driverLicence`, and `none` — the vetter will
/// attest without a document, which is the `priorAcquaintance` case. Only the class
/// ever travels — never a document number, an image, an issuing authority or an expiry
/// date. `none` states a policy (what a vetter accepts); a record of what was relied
/// on expresses 'no document' as an empty list instead.
typedef VettingDocumentation = String;

/// What documentation the vetter relies on, in the same tokens as vetting/request's
/// `acceptsDocumentation` — `passport`, `nationalId`, `driverLicence`, `none`, or
/// another lowerCamelCase class. The vetter's own choice; empty says nothing either
/// way.
typedef VetterAcceptsDocumentation = List<VettingDocumentation>;

/// How the vetter established that the person they checked is the person controlling
/// the applicant's DID. `inPerson` — both people were physically together. `video` — a
/// live, two-way video call. `priorAcquaintance` — the vetter has known or worked with
/// this person over a period, and attests from that knowledge rather than from a
/// document. A method is a description of what happened, not an assurance level: which
/// methods count, and how many of each, is community policy.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const VettingMethod(String value) {
  static const VettingMethod inPerson = VettingMethod('inPerson');
  static const VettingMethod video = VettingMethod('video');
  static const VettingMethod priorAcquaintance =
      VettingMethod('priorAcquaintance');

  /// Every value this specification's schema permits.
  static const List<VettingMethod> values = <VettingMethod>[
    inPerson,
    video,
    priorAcquaintance
  ];
}

/// The methods the vetter offers, from `inPerson`, `video`, `priorAcquaintance`.
typedef VetterMethods = List<VettingMethod>;

/// A BCP 47 language tag, e.g. `en`, `de-AT`. Compared case-insensitively.
typedef LanguageTag = String;

/// The name the vetter chooses to be listed under. Self-asserted and unverified: it is
/// not the name on any document, and a client MUST attribute it to the vetter.
typedef VetterDisplayName = String;

/// A vetter publishes, replaces or unlists their vetter profile with their community:
/// the languages, coarse location, methods, documentation and events an applicant
/// looking for a vetter can filter on. The payload is the whole profile and replaces
/// whatever the community held. `listed: false` keeps the profile but removes it from
/// vtc/vetting/vetters/list.
class Payload {
  const Payload({
    required this.listed,
    this.displayName,
    required this.languages,
    this.location,
    required this.methods,
    required this.acceptsDocumentation,
    this.availability,
    this.contactHint,
    required this.events,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        listed: json['listed'] as bool,
        displayName: json['displayName'] as String?,
        languages: (json['languages'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
        location: json['location'] == null
            ? null
            : VetterLocation.fromJson(json['location'] as Map<String, dynamic>),
        methods: (json['methods'] as List<dynamic>)
            .map((e) => VettingMethod(e as String))
            .toList(),
        acceptsDocumentation: (json['acceptsDocumentation'] as List<dynamic>)
            .map((e) => e as String)
            .toList(),
        availability: json['availability'] as String?,
        contactHint: json['contactHint'] as String?,
        events: (json['events'] as List<dynamic>)
            .map((e) => VetterEvent.fromJson(e as Map<String, dynamic>))
            .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// Whether the profile appears in vtc/vetting/vetters/list. `false` keeps it stored
  /// and hidden.
  final bool listed;
  final VetterDisplayName? displayName;

  /// BCP 47 tags of the languages the vetter can hold a session in, most preferred
  /// first. May be empty.
  final List<LanguageTag> languages;
  final VetterLocation? location;
  final VetterMethods methods;
  final VetterAcceptsDocumentation acceptsDocumentation;
  final VetterAvailability? availability;
  final VetterContactHint? contactHint;

  /// Events the vetter will attend and vet at. May be empty. Events that have ended stay
  /// stored but are not listed.
  final List<VetterEvent> events;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'listed': listed,
        if (displayName != null) 'displayName': displayName!,
        'languages': languages,
        if (location != null) 'location': location!.toJson(),
        'methods': methods.map((e) => e.value).toList(),
        'acceptsDocumentation': acceptsDocumentation,
        if (availability != null) 'availability': availability!,
        if (contactHint != null) 'contactHint': contactHint!,
        'events': events.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/profile/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/profile/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/vetting/vetters/profile/0.1","title":"VTC Vetting — Vetters Profile — payload","description":"A vetter publishes, replaces or unlists their vetter profile with their community: the languages, coarse location, methods, documentation and events an applicant looking for a vetter can filter on. The payload is the whole profile and replaces whatever the community held. `listed: false` keeps the profile but removes it from vtc/vetting/vetters/list.","type":"object","additionalProperties":false,"required":["listed","languages","methods","acceptsDocumentation","events"],"properties":{"listed":{"type":"boolean","description":"Whether the profile appears in vtc/vetting/vetters/list. `false` keeps it stored and hidden."},"displayName":{"\$ref":"#/\$defs/VetterDisplayName"},"languages":{"type":"array","maxItems":16,"uniqueItems":true,"items":{"\$ref":"#/\$defs/LanguageTag"},"description":"BCP 47 tags of the languages the vetter can hold a session in, most preferred first. May be empty."},"location":{"\$ref":"#/\$defs/VetterLocation"},"methods":{"\$ref":"#/\$defs/VetterMethods"},"acceptsDocumentation":{"\$ref":"#/\$defs/VetterAcceptsDocumentation"},"availability":{"\$ref":"#/\$defs/VetterAvailability"},"contactHint":{"\$ref":"#/\$defs/VetterContactHint"},"events":{"type":"array","maxItems":32,"items":{"\$ref":"#/\$defs/VetterEvent"},"description":"Events the vetter will attend and vet at. May be empty. Events that have ended stay stored but are not listed."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting — Vetters Profile — response payload","description":"The community stored the profile.","type":"object","additionalProperties":false,"required":["listed","updatedAt"],"properties":{"listed":{"type":"boolean","description":"The stored profile\'s `listed` value."},"updatedAt":{"type":"string","format":"date-time","description":"When the community stored this profile. vtc/vetting/vetters/list returns the same value."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"VetterEvent":{"title":"VetterEvent","type":"object","additionalProperties":false,"required":["name","startDate","endDate"],"properties":{"name":{"type":"string","minLength":1,"maxLength":200,"description":"The event\'s name, e.g. `Linux Plumbers Conference 2026`."},"startDate":{"\$ref":"#/\$defs/CalendarDate"},"endDate":{"\$ref":"#/\$defs/CalendarDate"},"location":{"\$ref":"#/\$defs/VetterLocation"},"url":{"type":"string","format":"uri","pattern":"^https://","maxLength":2048,"description":"OPTIONAL. The event\'s own https page."}},"description":"An event the vetter will attend and vet at. `endDate` is on or after `startDate` and no more than 31 days after it; JSON Schema cannot compare two members, so the community checks both and refuses a violation with `malformedRequest`."},"VetterLocation":{"title":"VetterLocation","type":"object","additionalProperties":false,"required":["country"],"properties":{"country":{"\$ref":"#/\$defs/CountryCode"},"region":{"\$ref":"#/\$defs/PlaceName"},"city":{"\$ref":"#/\$defs/PlaceName"}},"description":"Where a vetter can meet people, as coarse as the vetter chooses: a country, optionally a region, optionally a city. Never a street address."},"PlaceName":{"title":"PlaceName","type":"string","minLength":1,"maxLength":128,"description":"A region or city name as the vetter writes it. Compared case-insensitively and otherwise exactly."},"CountryCode":{"title":"CountryCode","type":"string","pattern":"^[A-Z]{2}\$","description":"An ISO 3166-1 alpha-2 country code, upper case, e.g. `DE`."},"CalendarDate":{"title":"CalendarDate","type":"string","format":"date","pattern":"^[0-9]{4}-[0-9]{2}-[0-9]{2}\$","description":"A calendar date, `YYYY-MM-DD` (RFC 3339 full-date), with no time or zone. Compared as a UTC date."},"VetterContactHint":{"title":"VetterContactHint","type":"string","minLength":1,"maxLength":300,"description":"Vetter-authored free text on how to obtain a ticket from them, e.g. `Find me at the OpenVTC booth`. A hint, not an address a request can be sent to: vetting/request still needs a ticket or an introduction the vetter accepts."},"VetterAvailability":{"title":"VetterAvailability","type":"string","minLength":1,"maxLength":500,"description":"Vetter-authored free text on when they are available to vet. Attributed to the vetter."},"VetterAcceptsDocumentation":{"title":"VetterAcceptsDocumentation","type":"array","maxItems":16,"uniqueItems":true,"items":{"\$ref":"#/\$defs/VettingDocumentation"},"description":"What documentation the vetter relies on, in the same tokens as vetting/request\'s `acceptsDocumentation` — `passport`, `nationalId`, `driverLicence`, `none`, or another lowerCamelCase class. The vetter\'s own choice; empty says nothing either way."},"VettingDocumentation":{"title":"VettingDocumentation","type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z][a-zA-Z0-9]*\$","description":"A class of documentation, named in lowerCamelCase. Open rather than enumerated, because what documentation a vetter accepts is each vetter\'s own choice. Well-known values: `passport`, `nationalId`, `driverLicence`, and `none` — the vetter will attest without a document, which is the `priorAcquaintance` case. Only the class ever travels — never a document number, an image, an issuing authority or an expiry date. `none` states a policy (what a vetter accepts); a record of what was relied on expresses \'no document\' as an empty list instead."},"VetterMethods":{"title":"VetterMethods","type":"array","minItems":1,"maxItems":3,"uniqueItems":true,"items":{"\$ref":"#/\$defs/VettingMethod"},"description":"The methods the vetter offers, from `inPerson`, `video`, `priorAcquaintance`."},"VettingMethod":{"title":"VettingMethod","type":"string","enum":["inPerson","video","priorAcquaintance"],"description":"How the vetter established that the person they checked is the person controlling the applicant\'s DID. `inPerson` — both people were physically together. `video` — a live, two-way video call. `priorAcquaintance` — the vetter has known or worked with this person over a period, and attests from that knowledge rather than from a document. A method is a description of what happened, not an assurance level: which methods count, and how many of each, is community policy."},"LanguageTag":{"title":"LanguageTag","type":"string","minLength":2,"maxLength":35,"pattern":"^[A-Za-z]{2,3}(-[A-Za-z0-9]{1,8})*\$","description":"A BCP 47 language tag, e.g. `en`, `de-AT`. Compared case-insensitively."},"VetterDisplayName":{"title":"VetterDisplayName","type":"string","minLength":1,"maxLength":128,"description":"The name the vetter chooses to be listed under. Self-asserted and unverified: it is not the name on any document, and a client MUST attribute it to the vetter."}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting — Vetters Profile — response payload","description":"The community stored the profile.","type":"object","additionalProperties":false,"required":["listed","updatedAt"],"properties":{"listed":{"type":"boolean","description":"The stored profile\'s `listed` value."},"updatedAt":{"type":"string","format":"date-time","description":"When the community stored this profile. vtc/vetting/vetters/list returns the same value."}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"VetterEvent":{"title":"VetterEvent","type":"object","additionalProperties":false,"required":["name","startDate","endDate"],"properties":{"name":{"type":"string","minLength":1,"maxLength":200,"description":"The event\'s name, e.g. `Linux Plumbers Conference 2026`."},"startDate":{"\$ref":"#/\$defs/CalendarDate"},"endDate":{"\$ref":"#/\$defs/CalendarDate"},"location":{"\$ref":"#/\$defs/VetterLocation"},"url":{"type":"string","format":"uri","pattern":"^https://","maxLength":2048,"description":"OPTIONAL. The event\'s own https page."}},"description":"An event the vetter will attend and vet at. `endDate` is on or after `startDate` and no more than 31 days after it; JSON Schema cannot compare two members, so the community checks both and refuses a violation with `malformedRequest`."},"VetterLocation":{"title":"VetterLocation","type":"object","additionalProperties":false,"required":["country"],"properties":{"country":{"\$ref":"#/\$defs/CountryCode"},"region":{"\$ref":"#/\$defs/PlaceName"},"city":{"\$ref":"#/\$defs/PlaceName"}},"description":"Where a vetter can meet people, as coarse as the vetter chooses: a country, optionally a region, optionally a city. Never a street address."},"PlaceName":{"title":"PlaceName","type":"string","minLength":1,"maxLength":128,"description":"A region or city name as the vetter writes it. Compared case-insensitively and otherwise exactly."},"CountryCode":{"title":"CountryCode","type":"string","pattern":"^[A-Z]{2}\$","description":"An ISO 3166-1 alpha-2 country code, upper case, e.g. `DE`."},"CalendarDate":{"title":"CalendarDate","type":"string","format":"date","pattern":"^[0-9]{4}-[0-9]{2}-[0-9]{2}\$","description":"A calendar date, `YYYY-MM-DD` (RFC 3339 full-date), with no time or zone. Compared as a UTC date."},"VetterContactHint":{"title":"VetterContactHint","type":"string","minLength":1,"maxLength":300,"description":"Vetter-authored free text on how to obtain a ticket from them, e.g. `Find me at the OpenVTC booth`. A hint, not an address a request can be sent to: vetting/request still needs a ticket or an introduction the vetter accepts."},"VetterAvailability":{"title":"VetterAvailability","type":"string","minLength":1,"maxLength":500,"description":"Vetter-authored free text on when they are available to vet. Attributed to the vetter."},"VetterAcceptsDocumentation":{"title":"VetterAcceptsDocumentation","type":"array","maxItems":16,"uniqueItems":true,"items":{"\$ref":"#/\$defs/VettingDocumentation"},"description":"What documentation the vetter relies on, in the same tokens as vetting/request\'s `acceptsDocumentation` — `passport`, `nationalId`, `driverLicence`, `none`, or another lowerCamelCase class. The vetter\'s own choice; empty says nothing either way."},"VettingDocumentation":{"title":"VettingDocumentation","type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z][a-zA-Z0-9]*\$","description":"A class of documentation, named in lowerCamelCase. Open rather than enumerated, because what documentation a vetter accepts is each vetter\'s own choice. Well-known values: `passport`, `nationalId`, `driverLicence`, and `none` — the vetter will attest without a document, which is the `priorAcquaintance` case. Only the class ever travels — never a document number, an image, an issuing authority or an expiry date. `none` states a policy (what a vetter accepts); a record of what was relied on expresses \'no document\' as an empty list instead."},"VetterMethods":{"title":"VetterMethods","type":"array","minItems":1,"maxItems":3,"uniqueItems":true,"items":{"\$ref":"#/\$defs/VettingMethod"},"description":"The methods the vetter offers, from `inPerson`, `video`, `priorAcquaintance`."},"VettingMethod":{"title":"VettingMethod","type":"string","enum":["inPerson","video","priorAcquaintance"],"description":"How the vetter established that the person they checked is the person controlling the applicant\'s DID. `inPerson` — both people were physically together. `video` — a live, two-way video call. `priorAcquaintance` — the vetter has known or worked with this person over a period, and attests from that knowledge rather than from a document. A method is a description of what happened, not an assurance level: which methods count, and how many of each, is community policy."},"LanguageTag":{"title":"LanguageTag","type":"string","minLength":2,"maxLength":35,"pattern":"^[A-Za-z]{2,3}(-[A-Za-z0-9]{1,8})*\$","description":"A BCP 47 language tag, e.g. `en`, `de-AT`. Compared case-insensitively."},"VetterDisplayName":{"title":"VetterDisplayName","type":"string","minLength":1,"maxLength":128,"description":"The name the vetter chooses to be listed under. Self-asserted and unverified: it is not the name on any document, and a client MUST attribute it to the vetter."}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: false,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
