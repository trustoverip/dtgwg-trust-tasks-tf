// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/vetting/vetters/grant/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The grant — newly issued, or the member's existing live grant.
class Response {
  const Response({
    required this.endorsementId,
    required this.credentialId,
    required this.validFrom,
    required this.validUntil,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        endorsementId: json['endorsementId'] as String,
        credentialId: json['credentialId'] as String,
        validFrom: json['validFrom'] as String,
        validUntil: json['validUntil'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The community's endorsement record for this grant — the identifier
  /// vtc/endorsements/revoke takes.
  final String endorsementId;

  /// The `id` of the role credential, a URI.
  final String credentialId;

  /// The role credential's `validFrom`.
  final String validFrom;

  /// The role credential's `validUntil`.
  final String validUntil;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'endorsementId': endorsementId,
        'credentialId': credentialId,
        'validFrom': validFrom,
        'validUntil': validUntil,
        if (ext != null) 'ext': ext!,
      };
}

/// A community administrator makes a member a vetter. The community issues the member
/// a revocable `CommunityRole` endorsement credential for the `vetter` role, delivers
/// it over credential-exchange/issue, and returns the identifiers and validity of the
/// grant. A member who already holds a live grant gets that grant back unchanged.
class Payload {
  const Payload({
    required this.memberDid,
    this.validitySeconds,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        memberDid: json['memberDid'] as String,
        validitySeconds: json['validitySeconds'] as int?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The member DID to grant the vetter role to — the subject of the role credential,
  /// and the DID the vetter signs statements with.
  final String memberDid;

  /// OPTIONAL. How long the role credential is valid, from issuance: at least one day,
  /// at most two years. Absent: 31536000 (365 days). Ignored when the member already
  /// holds a live grant.
  final int? validitySeconds;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'memberDid': memberDid,
        if (validitySeconds != null) 'validitySeconds': validitySeconds!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/grant/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/grant/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/vetting/vetters/grant/0.1","title":"VTC Vetting — Vetters Grant — payload","description":"A community administrator makes a member a vetter. The community issues the member a revocable `CommunityRole` endorsement credential for the `vetter` role, delivers it over credential-exchange/issue, and returns the identifiers and validity of the grant. A member who already holds a live grant gets that grant back unchanged.","type":"object","additionalProperties":false,"required":["memberDid"],"properties":{"memberDid":{"type":"string","pattern":"^did:","description":"The member DID to grant the vetter role to — the subject of the role credential, and the DID the vetter signs statements with."},"validitySeconds":{"type":"integer","minimum":86400,"maximum":63072000,"description":"OPTIONAL. How long the role credential is valid, from issuance: at least one day, at most two years. Absent: 31536000 (365 days). Ignored when the member already holds a live grant."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting — Vetters Grant — response payload","description":"The grant — newly issued, or the member\'s existing live grant.","type":"object","additionalProperties":false,"required":["endorsementId","credentialId","validFrom","validUntil"],"properties":{"endorsementId":{"type":"string","minLength":1,"maxLength":128,"description":"The community\'s endorsement record for this grant — the identifier vtc/endorsements/revoke takes."},"credentialId":{"type":"string","minLength":1,"maxLength":512,"pattern":"^[a-zA-Z][a-zA-Z0-9+.-]*:\\\\S+\$","description":"The `id` of the role credential, a URI."},"validFrom":{"type":"string","format":"date-time","description":"The role credential\'s `validFrom`."},"validUntil":{"type":"string","format":"date-time","description":"The role credential\'s `validUntil`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting — Vetters Grant — response payload","description":"The grant — newly issued, or the member\'s existing live grant.","type":"object","additionalProperties":false,"required":["endorsementId","credentialId","validFrom","validUntil"],"properties":{"endorsementId":{"type":"string","minLength":1,"maxLength":128,"description":"The community\'s endorsement record for this grant — the identifier vtc/endorsements/revoke takes."},"credentialId":{"type":"string","minLength":1,"maxLength":512,"pattern":"^[a-zA-Z][a-zA-Z0-9+.-]*:\\\\S+\$","description":"The `id` of the role credential, a URI."},"validFrom":{"type":"string","format":"date-time","description":"The role credential\'s `validFrom`."},"validUntil":{"type":"string","format":"date-time","description":"The role credential\'s `validUntil`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
