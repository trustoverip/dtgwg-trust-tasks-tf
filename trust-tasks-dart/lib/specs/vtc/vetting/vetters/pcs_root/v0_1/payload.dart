// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/vetting/vetters/pcs-root/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The blind pre-credential. The vetter unblinds it into a credential the community
/// has never seen.
class Response {
  const Response({
    required this.label,
    required this.preCredential,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        label: json['label'] as String,
        preCredential: json['preCredential'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The label signed under — the one asked for.
  final String label;

  /// The pre-credential, multibase base58btc. Useless to anyone but the holder of the
  /// request's blinding state.
  final String preCredential;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'label': label,
        'preCredential': preCredential,
        if (ext != null) 'ext': ext!,
      };
}

/// The blinded root request, as the suite serialises it. Opaque to the community
/// beyond verifying it; its members are fixed by the suite named in the criterion's
/// parameters, not by this schema.
class PayloadRequest {
  const PayloadRequest({
    this.encoding,
    this.t0,
    required this.proof,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadRequest.fromJson(Map<String, dynamic> json) => PayloadRequest(
        encoding: json['encoding'] as String?,
        t0: json['t0'] as String?,
        proof: json['proof'] as String,
      );

  /// The issuance encoding the suite carries.
  final String? encoding;

  /// The request's tag under the root context.
  final String? t0;

  /// The proof that the commitment is well formed and the requester holds its secret.
  final String proof;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (encoding != null) 'encoding': encoding!,
        if (t0 != null) 't0': t0!,
        'proof': proof,
      };
}

/// A vetter asks the community to blind-sign its class credential for a vetting
/// period. What the community signs is a commitment it cannot open, so the credential
/// it issues is one it cannot later recognise — which is what lets the vetter attest
/// without being named.
class Payload {
  const Payload({
    required this.label,
    required this.id,
    required this.request,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        label: json['label'] as String,
        id: json['id'] as String,
        request:
            PayloadRequest.fromJson(json['request'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The class label asked for, `vetter/&lt;YYYY-MM&gt;`. A community issues under its current
  /// label only; a stale client is refused rather than silently served under another.
  final String label;

  /// The vetter's PCS identifier, multibase base58btc. Recorded once and required again
  /// at every later label: a member holding two identifiers could be counted twice in
  /// one proof.
  final String id;

  /// The blinded root request, as the suite serialises it. Opaque to the community
  /// beyond verifying it; its members are fixed by the suite named in the criterion's
  /// parameters, not by this schema.
  final PayloadRequest request;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'label': label,
        'id': id,
        'request': request.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/pcs-root/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/vetting/vetters/pcs-root/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/vetting/vetters/pcs-root/0.1","title":"VTC — Vetting PCS Root Credential — payload","description":"A vetter asks the community to blind-sign its class credential for a vetting period. What the community signs is a commitment it cannot open, so the credential it issues is one it cannot later recognise — which is what lets the vetter attest without being named.","type":"object","additionalProperties":false,"required":["label","id","request"],"properties":{"label":{"type":"string","pattern":"^vetter/[0-9]{4}-[0-9]{2}\$","description":"The class label asked for, `vetter/<YYYY-MM>`. A community issues under its current label only; a stale client is refused rather than silently served under another."},"id":{"type":"string","pattern":"^z[1-9A-HJ-NP-Za-km-z]+\$","description":"The vetter\'s PCS identifier, multibase base58btc. Recorded once and required again at every later label: a member holding two identifiers could be counted twice in one proof."},"request":{"type":"object","description":"The blinded root request, as the suite serialises it. Opaque to the community beyond verifying it; its members are fixed by the suite named in the criterion\'s parameters, not by this schema.","properties":{"encoding":{"type":"string","pattern":"^z[1-9A-HJ-NP-Za-km-z]+\$","description":"The issuance encoding the suite carries."},"t0":{"type":"string","pattern":"^z[1-9A-HJ-NP-Za-km-z]+\$","description":"The request\'s tag under the root context."},"proof":{"type":"string","pattern":"^z[1-9A-HJ-NP-Za-km-z]+\$","description":"The proof that the commitment is well formed and the requester holds its secret."}},"required":["proof"]},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting PCS Root Credential — response payload","type":"object","additionalProperties":false,"required":["label","preCredential"],"description":"The blind pre-credential. The vetter unblinds it into a credential the community has never seen.","properties":{"label":{"type":"string","pattern":"^vetter/[0-9]{4}-[0-9]{2}\$","description":"The label signed under — the one asked for."},"preCredential":{"type":"string","pattern":"^z[1-9A-HJ-NP-Za-km-z]+\$","description":"The pre-credential, multibase base58btc. Useless to anyone but the holder of the request\'s blinding state."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Vetting PCS Root Credential — response payload","type":"object","additionalProperties":false,"required":["label","preCredential"],"description":"The blind pre-credential. The vetter unblinds it into a credential the community has never seen.","properties":{"label":{"type":"string","pattern":"^vetter/[0-9]{4}-[0-9]{2}\$","description":"The label signed under — the one asked for."},"preCredential":{"type":"string","pattern":"^z[1-9A-HJ-NP-Za-km-z]+\$","description":"The pre-credential, multibase base58btc. Useless to anyone but the holder of the request\'s blinding state."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
