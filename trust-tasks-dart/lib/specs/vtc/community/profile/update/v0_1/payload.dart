// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/vtc/community/profile/update/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// A community's published position on personhood. DTG Credentials §Personhood
/// Credentials places PHC status here rather than in the credential — "PHC status is
/// determined by governance and trust registries, not by credential structure" — so a
/// verifier deciding whether a VMC carries personhood weight reads this, not the
/// credential's type array. §Governance Considerations item 1 makes the
/// acceptable-IDVP list the community's to define and publish. Every member is a
/// declaration, not an enforcement: what a community actually accepts is decided by
/// its own policy. Absent means the community has not published a position, which is
/// not the same as asserting the negative.
class PersonhoodGovernance {
  const PersonhoodGovernance({
    this.realHuman,
    this.singleMembership,
    this.acceptedIdvps,
    this.governanceFrameworkUrl,
  });

  /// Read this payload from a decoded JSON object.
  factory PersonhoodGovernance.fromJson(Map<String, dynamic> json) =>
      PersonhoodGovernance(
        realHuman: json['realHuman'] as bool?,
        singleMembership: json['singleMembership'] as bool?,
        acceptedIdvps: json['acceptedIdvps'] == null
            ? null
            : (json['acceptedIdvps'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        governanceFrameworkUrl: json['governanceFrameworkUrl'] as String?,
      );

  /// Governance requires that members are real humans. Defaults to `false` when absent:
  /// a community that has not considered the question asserts nothing, which is the only
  /// safe default for a claim a verifier may rely on.
  final bool? realHuman;

  /// Governance requires that each person holds at most one membership in **this**
  /// community. Per-community by definition — the glossary says "exactly one membership
  /// in that VTC" — so a single community can satisfy it without any network above it.
  final bool? singleMembership;

  /// DIDs of the identity-verification providers whose credentials this community
  /// accepts as personhood evidence. A community that vets its own members in person
  /// lists its own community DID here — it is acting as its own IDVP, which §IDVC
  /// permits. An empty list means no list has been published, not that everything is
  /// accepted.
  final List<String>? acceptedIdvps;

  /// Where the governance framework these assertions refer to can be read.
  final String? governanceFrameworkUrl;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (realHuman != null) 'realHuman': realHuman!,
        if (singleMembership != null) 'singleMembership': singleMembership!,
        if (acceptedIdvps != null) 'acceptedIdvps': acceptedIdvps!,
        if (governanceFrameworkUrl != null)
          'governanceFrameworkUrl': governanceFrameworkUrl!,
      };
}

/// Which identifier form the community expects members to issue relationship
/// credentials under. `attributed` means the member's membership DID, so an edge names
/// them; `pairwise` means a relationship DID unique to each counterparty. A
/// declaration, not an enforcement: the member still chooses per relationship, and a
/// community that wants to require one form does so in its own policy. Absent means
/// the community has not declared one; implementations default to `pairwise`, matching
/// the DTG Credentials recommendation.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const CommunityProfileViewRelationshipIdentifierDefault(
    String value) {
  static const CommunityProfileViewRelationshipIdentifierDefault attributed =
      CommunityProfileViewRelationshipIdentifierDefault('attributed');
  static const CommunityProfileViewRelationshipIdentifierDefault pairwise =
      CommunityProfileViewRelationshipIdentifierDefault('pairwise');

  /// Every value this specification's schema permits.
  static const List<CommunityProfileViewRelationshipIdentifierDefault> values =
      <CommunityProfileViewRelationshipIdentifierDefault>[attributed, pairwise];
}

/// Trust-registry reachability. Populated on reads; not settable.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const CommunityProfileViewRegistryStatus(String value) {
  static const CommunityProfileViewRegistryStatus active =
      CommunityProfileViewRegistryStatus('active');
  static const CommunityProfileViewRegistryStatus degraded =
      CommunityProfileViewRegistryStatus('degraded');

  /// Every value this specification's schema permits.
  static const List<CommunityProfileViewRegistryStatus> values =
      <CommunityProfileViewRegistryStatus>[active, degraded];
}

/// A community profile **as read**: every member of CommunityProfile, plus the
/// immutable identity a reader needs and an update must never set. CommunityProfile is
/// the update-facing view and omits `communityDid` on purpose, so that a patch cannot
/// re-point a community's identity. That is right for a payload and wrong for a
/// response: a client holding a profile has no other way to learn which DID to verify
/// that community's credentials against, and `community/profile/show` returning the
/// update-facing view left the one member a consumer most needs undefined. So reads
/// get this and updates keep CommunityProfile. The split is the point — the
/// immutability of `communityDid` is enforced by it being absent from the payload, not
/// by prose asking implementers not to honour it.
class CommunityProfileView {
  const CommunityProfileView({
    required this.communityDid,
    this.createdAt,
    required this.name,
    this.description,
    this.logoUrl,
    this.publicUrl,
    this.contactEmail,
    required this.language,
    this.relationshipIdentifierDefault,
    this.extensions,
    this.registryStatus,
    this.personhood,
  });

  /// Read this payload from a decoded JSON object.
  factory CommunityProfileView.fromJson(Map<String, dynamic> json) =>
      CommunityProfileView(
        communityDid: json['communityDid'] as String,
        createdAt: json['createdAt'] as String?,
        name: json['name'] as String,
        description: json['description'] as String?,
        logoUrl: json['logoUrl'] as String?,
        publicUrl: json['publicUrl'] as String?,
        contactEmail: json['contactEmail'] as String?,
        language: json['language'] as String,
        relationshipIdentifierDefault:
            json['relationshipIdentifierDefault'] == null
                ? null
                : CommunityProfileViewRelationshipIdentifierDefault(
                    json['relationshipIdentifierDefault'] as String),
        extensions: json['extensions'] as Map<String, dynamic>?,
        registryStatus: json['registryStatus'] == null
            ? null
            : CommunityProfileViewRegistryStatus(
                json['registryStatus'] as String),
        personhood: json['personhood'] == null
            ? null
            : PersonhoodGovernance.fromJson(
                json['personhood'] as Map<String, dynamic>),
      );

  /// The community's own DID — the identifier every credential it issues names as
  /// `issuer`, and the one a member's credential binds them to. Immutable: set when the
  /// community is created and not settable through an update.
  final String communityDid;

  /// When the community was created. Immutable, like `communityDid`.
  final String? createdAt;
  final String name;
  final String? description;
  final String? logoUrl;
  final String? publicUrl;
  final String? contactEmail;
  final String language;

  /// Which identifier form the community expects members to issue relationship
  /// credentials under. `attributed` means the member's membership DID, so an edge names
  /// them; `pairwise` means a relationship DID unique to each counterparty. A
  /// declaration, not an enforcement: the member still chooses per relationship, and a
  /// community that wants to require one form does so in its own policy. Absent means
  /// the community has not declared one; implementations default to `pairwise`, matching
  /// the DTG Credentials recommendation.
  final CommunityProfileViewRelationshipIdentifierDefault?
      relationshipIdentifierDefault;

  /// Opaque community-defined extension bag.
  final Map<String, dynamic>? extensions;

  /// Trust-registry reachability. Populated on reads; not settable.
  final CommunityProfileViewRegistryStatus? registryStatus;

  /// What this community's governance asserts about the personhood of its members.
  final PersonhoodGovernance? personhood;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'communityDid': communityDid,
        if (createdAt != null) 'createdAt': createdAt!,
        'name': name,
        if (description != null) 'description': description!,
        if (logoUrl != null) 'logoUrl': logoUrl!,
        if (publicUrl != null) 'publicUrl': publicUrl!,
        if (contactEmail != null) 'contactEmail': contactEmail!,
        'language': language,
        if (relationshipIdentifierDefault != null)
          'relationshipIdentifierDefault': relationshipIdentifierDefault!.value,
        if (extensions != null) 'extensions': extensions!,
        if (registryStatus != null) 'registryStatus': registryStatus!.value,
        if (personhood != null) 'personhood': personhood!.toJson(),
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// VTC Community Profile Update — response payload
class Response {
  const Response({
    required this.profile,
    this.fieldsChanged,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        profile: CommunityProfileView.fromJson(
            json['profile'] as Map<String, dynamic>),
        fieldsChanged: json['fieldsChanged'] == null
            ? null
            : (json['fieldsChanged'] as List<dynamic>)
                .map((e) => e as String)
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The community's profile as read, including the immutable `communityDid` an update
  /// cannot set.
  final CommunityProfileView profile;

  /// Names of the profile members this update actually changed, in the wire spelling.
  /// Empty when the submitted values all matched what was already stored. Lets a caller
  /// distinguish a no-op from an applied change without diffing the returned profile
  /// against the one it sent.
  final List<String>? fieldsChanged;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'profile': profile.toJson(),
        if (fieldsChanged != null) 'fieldsChanged': fieldsChanged!,
        if (ext != null) 'ext': ext!,
      };
}

/// Set the community's declared relationship-identifier default. See
/// `CommunityProfile.relationshipIdentifierDefault` in the `vtc/_shared/0.1/community`
/// schema. Omit to leave it unchanged.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadRelationshipIdentifierDefault(String value) {
  static const PayloadRelationshipIdentifierDefault attributed =
      PayloadRelationshipIdentifierDefault('attributed');
  static const PayloadRelationshipIdentifierDefault pairwise =
      PayloadRelationshipIdentifierDefault('pairwise');

  /// Every value this specification's schema permits.
  static const List<PayloadRelationshipIdentifierDefault> values =
      <PayloadRelationshipIdentifierDefault>[attributed, pairwise];
}

/// VTC Community Profile Update — payload
class Payload {
  const Payload({
    this.name,
    this.description,
    this.logoUrl,
    this.publicUrl,
    this.contactEmail,
    this.language,
    this.relationshipIdentifierDefault,
    this.personhood,
    this.extensions,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        name: json['name'] as String?,
        description: json['description'] as String?,
        logoUrl: json['logoUrl'] as String?,
        publicUrl: json['publicUrl'] as String?,
        contactEmail: json['contactEmail'] as String?,
        language: json['language'] as String?,
        relationshipIdentifierDefault:
            json['relationshipIdentifierDefault'] == null
                ? null
                : PayloadRelationshipIdentifierDefault(
                    json['relationshipIdentifierDefault'] as String),
        personhood: json['personhood'] == null
            ? null
            : PersonhoodGovernance.fromJson(
                json['personhood'] as Map<String, dynamic>),
        extensions: json['extensions'] as Map<String, dynamic>?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String? name;
  final String? description;
  final String? logoUrl;
  final String? publicUrl;
  final String? contactEmail;
  final String? language;

  /// Set the community's declared relationship-identifier default. See
  /// `CommunityProfile.relationshipIdentifierDefault` in the `vtc/_shared/0.1/community`
  /// schema. Omit to leave it unchanged.
  final PayloadRelationshipIdentifierDefault? relationshipIdentifierDefault;

  /// Set the community's published personhood position. See
  /// `CommunityProfile.personhood` in the `vtc/_shared/0.1/community` schema. Replaces
  /// the whole object — a partial update would leave a verifier unable to tell an unset
  /// member from a cleared one. Omit to leave it unchanged.
  final PersonhoodGovernance? personhood;

  /// Opaque community-defined extension bag (maintainer-capped).
  final Map<String, dynamic>? extensions;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (name != null) 'name': name!,
        if (description != null) 'description': description!,
        if (logoUrl != null) 'logoUrl': logoUrl!,
        if (publicUrl != null) 'publicUrl': publicUrl!,
        if (contactEmail != null) 'contactEmail': contactEmail!,
        if (language != null) 'language': language!,
        if (relationshipIdentifierDefault != null)
          'relationshipIdentifierDefault': relationshipIdentifierDefault!.value,
        if (personhood != null) 'personhood': personhood!.toJson(),
        if (extensions != null) 'extensions': extensions!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/vtc/community/profile/update/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/vtc/community/profile/update/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/vtc/community/profile/update/0.1","title":"VTC Community Profile Update — payload","type":"object","additionalProperties":false,"properties":{"name":{"type":"string","minLength":1,"maxLength":256},"description":{"type":"string","maxLength":1024},"logoUrl":{"type":["string","null"]},"publicUrl":{"type":["string","null"]},"contactEmail":{"type":["string","null"]},"language":{"type":"string","minLength":1},"relationshipIdentifierDefault":{"type":"string","enum":["attributed","pairwise"],"description":"Set the community\'s declared relationship-identifier default. See `CommunityProfile.relationshipIdentifierDefault` in the `vtc/_shared/0.1/community` schema. Omit to leave it unchanged."},"personhood":{"\$ref":"#/\$defs/PersonhoodGovernance","description":"Set the community\'s published personhood position. See `CommunityProfile.personhood` in the `vtc/_shared/0.1/community` schema. Replaces the whole object — a partial update would leave a verifier unable to tell an unset member from a cleared one. Omit to leave it unchanged."},"extensions":{"type":"object","description":"Opaque community-defined extension bag (maintainer-capped)."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"VTC Community Profile Update — response payload","type":"object","additionalProperties":false,"required":["profile"],"properties":{"profile":{"\$ref":"#/\$defs/CommunityProfileView","description":"The community\'s profile as read, including the immutable `communityDid` an update cannot set."},"fieldsChanged":{"type":"array","items":{"type":"string"},"description":"Names of the profile members this update actually changed, in the wire spelling. Empty when the submitted values all matched what was already stored. Lets a caller distinguish a no-op from an applied change without diffing the returned profile against the one it sent."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CommunityProfileView":{"\$anchor":"communityProfileView","title":"CommunityProfileView","type":"object","additionalProperties":false,"description":"A community profile **as read**: every member of CommunityProfile, plus the immutable identity a reader needs and an update must never set.\\n\\nCommunityProfile is the update-facing view and omits `communityDid` on purpose, so that a patch cannot re-point a community\'s identity. That is right for a payload and wrong for a response: a client holding a profile has no other way to learn which DID to verify that community\'s credentials against, and `community/profile/show` returning the update-facing view left the one member a consumer most needs undefined.\\n\\nSo reads get this and updates keep CommunityProfile. The split is the point — the immutability of `communityDid` is enforced by it being absent from the payload, not by prose asking implementers not to honour it.","required":["communityDid","name","language"],"properties":{"communityDid":{"type":"string","minLength":1,"description":"The community\'s own DID — the identifier every credential it issues names as `issuer`, and the one a member\'s credential binds them to. Immutable: set when the community is created and not settable through an update."},"createdAt":{"type":"string","format":"date-time","description":"When the community was created. Immutable, like `communityDid`."},"name":{"type":"string","minLength":1,"maxLength":256},"description":{"type":"string","maxLength":1024},"logoUrl":{"type":["string","null"]},"publicUrl":{"type":["string","null"]},"contactEmail":{"type":["string","null"]},"language":{"type":"string","minLength":1},"relationshipIdentifierDefault":{"type":"string","enum":["attributed","pairwise"],"description":"Which identifier form the community expects members to issue relationship credentials under. `attributed` means the member\'s membership DID, so an edge names them; `pairwise` means a relationship DID unique to each counterparty. A declaration, not an enforcement: the member still chooses per relationship, and a community that wants to require one form does so in its own policy. Absent means the community has not declared one; implementations default to `pairwise`, matching the DTG Credentials recommendation."},"extensions":{"type":"object","description":"Opaque community-defined extension bag."},"registryStatus":{"type":"string","enum":["active","degraded"],"description":"Trust-registry reachability. Populated on reads; not settable."},"personhood":{"\$ref":"#/\$defs/PersonhoodGovernance","description":"What this community\'s governance asserts about the personhood of its members."}}},"PersonhoodGovernance":{"\$anchor":"personhoodGovernance","title":"PersonhoodGovernance","type":"object","additionalProperties":false,"description":"A community\'s published position on personhood.\\n\\nDTG Credentials §Personhood Credentials places PHC status here rather than in the credential — \\"PHC status is determined by governance and trust registries, not by credential structure\\" — so a verifier deciding whether a VMC carries personhood weight reads this, not the credential\'s type array. §Governance Considerations item 1 makes the acceptable-IDVP list the community\'s to define and publish.\\n\\nEvery member is a declaration, not an enforcement: what a community actually accepts is decided by its own policy. Absent means the community has not published a position, which is not the same as asserting the negative.","properties":{"realHuman":{"type":"boolean","description":"Governance requires that members are real humans. Defaults to `false` when absent: a community that has not considered the question asserts nothing, which is the only safe default for a claim a verifier may rely on."},"singleMembership":{"type":"boolean","description":"Governance requires that each person holds at most one membership in **this** community. Per-community by definition — the glossary says \\"exactly one membership in that VTC\\" — so a single community can satisfy it without any network above it."},"acceptedIdvps":{"type":"array","items":{"type":"string","minLength":1},"description":"DIDs of the identity-verification providers whose credentials this community accepts as personhood evidence. A community that vets its own members in person lists its own community DID here — it is acting as its own IDVP, which §IDVC permits. An empty list means no list has been published, not that everything is accepted."},"governanceFrameworkUrl":{"type":["string","null"],"description":"Where the governance framework these assertions refer to can be read."}}}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"VTC Community Profile Update — response payload","type":"object","additionalProperties":false,"required":["profile"],"properties":{"profile":{"\$ref":"#/\$defs/CommunityProfileView","description":"The community\'s profile as read, including the immutable `communityDid` an update cannot set."},"fieldsChanged":{"type":"array","items":{"type":"string"},"description":"Names of the profile members this update actually changed, in the wire spelling. Empty when the submitted values all matched what was already stored. Lets a caller distinguish a no-op from an applied change without diffing the returned profile against the one it sent."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"CommunityProfileView":{"\$anchor":"communityProfileView","title":"CommunityProfileView","type":"object","additionalProperties":false,"description":"A community profile **as read**: every member of CommunityProfile, plus the immutable identity a reader needs and an update must never set.\\n\\nCommunityProfile is the update-facing view and omits `communityDid` on purpose, so that a patch cannot re-point a community\'s identity. That is right for a payload and wrong for a response: a client holding a profile has no other way to learn which DID to verify that community\'s credentials against, and `community/profile/show` returning the update-facing view left the one member a consumer most needs undefined.\\n\\nSo reads get this and updates keep CommunityProfile. The split is the point — the immutability of `communityDid` is enforced by it being absent from the payload, not by prose asking implementers not to honour it.","required":["communityDid","name","language"],"properties":{"communityDid":{"type":"string","minLength":1,"description":"The community\'s own DID — the identifier every credential it issues names as `issuer`, and the one a member\'s credential binds them to. Immutable: set when the community is created and not settable through an update."},"createdAt":{"type":"string","format":"date-time","description":"When the community was created. Immutable, like `communityDid`."},"name":{"type":"string","minLength":1,"maxLength":256},"description":{"type":"string","maxLength":1024},"logoUrl":{"type":["string","null"]},"publicUrl":{"type":["string","null"]},"contactEmail":{"type":["string","null"]},"language":{"type":"string","minLength":1},"relationshipIdentifierDefault":{"type":"string","enum":["attributed","pairwise"],"description":"Which identifier form the community expects members to issue relationship credentials under. `attributed` means the member\'s membership DID, so an edge names them; `pairwise` means a relationship DID unique to each counterparty. A declaration, not an enforcement: the member still chooses per relationship, and a community that wants to require one form does so in its own policy. Absent means the community has not declared one; implementations default to `pairwise`, matching the DTG Credentials recommendation."},"extensions":{"type":"object","description":"Opaque community-defined extension bag."},"registryStatus":{"type":"string","enum":["active","degraded"],"description":"Trust-registry reachability. Populated on reads; not settable."},"personhood":{"\$ref":"#/\$defs/PersonhoodGovernance","description":"What this community\'s governance asserts about the personhood of its members."}}},"PersonhoodGovernance":{"\$anchor":"personhoodGovernance","title":"PersonhoodGovernance","type":"object","additionalProperties":false,"description":"A community\'s published position on personhood.\\n\\nDTG Credentials §Personhood Credentials places PHC status here rather than in the credential — \\"PHC status is determined by governance and trust registries, not by credential structure\\" — so a verifier deciding whether a VMC carries personhood weight reads this, not the credential\'s type array. §Governance Considerations item 1 makes the acceptable-IDVP list the community\'s to define and publish.\\n\\nEvery member is a declaration, not an enforcement: what a community actually accepts is decided by its own policy. Absent means the community has not published a position, which is not the same as asserting the negative.","properties":{"realHuman":{"type":"boolean","description":"Governance requires that members are real humans. Defaults to `false` when absent: a community that has not considered the question asserts nothing, which is the only safe default for a claim a verifier may rely on."},"singleMembership":{"type":"boolean","description":"Governance requires that each person holds at most one membership in **this** community. Per-community by definition — the glossary says \\"exactly one membership in that VTC\\" — so a single community can satisfy it without any network above it."},"acceptedIdvps":{"type":"array","items":{"type":"string","minLength":1},"description":"DIDs of the identity-verification providers whose credentials this community accepts as personhood evidence. A community that vets its own members in person lists its own community DID here — it is acting as its own IDVP, which §IDVC permits. An empty list means no list has been published, not that everything is accepted."},"governanceFrameworkUrl":{"type":["string","null"],"description":"Where the governance framework these assertions refer to can be read."}}}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
