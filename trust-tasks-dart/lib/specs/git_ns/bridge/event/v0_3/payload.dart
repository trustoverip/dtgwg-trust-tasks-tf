// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/bridge/event/0.3/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The VTC's identifier for a job, unique across the VTC's jobs.
typedef JobId = String;

/// A role in the forge's own vocabulary (`maintain`, `write`).
typedef ForgeRole = String;

/// An identifier the forge itself assigns — a repository id, a user or organisation id
/// — carried as a string so a forge whose ids are not numbers needs no new version.
/// GitHub and Forgejo ids are decimal integers written as strings (`"812736451"`).
/// Unlike a name, it survives renames and transfers, which is why rights and bindings
/// are keyed by it.
typedef ForgeId = String;

/// A forge-qualified resource naming exactly one repository:
/// `&lt;forge-host&gt;/&lt;owner&gt;/&lt;repo&gt;`, lowercase.
typedef RepoResource = String;

/// The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host,
/// `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No
/// scheme, no port, no path. The host is a segment of every resource, so a right never
/// crosses forges.
typedef ForgeHost = String;

/// A person's account on one forge. `id` is authoritative; `login` is for display
/// only, because logins can be renamed and re-registered.
class ForgeAccount {
  const ForgeAccount({
    required this.forge,
    required this.id,
    required this.login,
  });

  /// Read this payload from a decoded JSON object.
  factory ForgeAccount.fromJson(Map<String, dynamic> json) => ForgeAccount(
        forge: json['forge'] as String,
        id: json['id'] as String,
        login: json['login'] as String,
      );

  final ForgeHost forge;
  final ForgeId id;

  /// The account's current login, as the forge reported it when last seen. Display only.
  final String login;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'forge': forge,
        'id': id,
        'login': login,
      };
}

/// A level of the bridge's forge-neutral role ladder, lowest first: the vocabulary
/// role drift's `observed` and `expected` are reported in. `none` is no direct role on
/// the repository.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const MappedRole(String value) {
  static const MappedRole none = MappedRole('none');
  static const MappedRole read = MappedRole('read');
  static const MappedRole triage = MappedRole('triage');
  static const MappedRole write = MappedRole('write');
  static const MappedRole maintain = MappedRole('maintain');
  static const MappedRole admin = MappedRole('admin');

  /// Every value this specification's schema permits.
  static const List<MappedRole> values = <MappedRole>[
    none,
    read,
    triage,
    write,
    maintain,
    admin
  ];
}

/// The forge role each repository right is given, as the forge applies it: after the
/// forge's own ladder has rounded it down. `own` is the role `git.repo.own` projects
/// to, `maintain` that of `git.repo.maintain`, `commit` that of `git.commit.sign`
/// (`none` is fork-based contribution). There is no member for `git.ns.admin` or
/// `git.repo.create`, which project to no forge role whatever a bridge is configured
/// with. Ordered: `own` is at least `maintain`, which is at least `commit`, and
/// `commit` is at most `write`.
class RoleMap {
  const RoleMap({
    required this.own,
    required this.maintain,
    required this.commit,
  });

  /// Read this payload from a decoded JSON object.
  factory RoleMap.fromJson(Map<String, dynamic> json) => RoleMap(
        own: MappedRole(json['own'] as String),
        maintain: MappedRole(json['maintain'] as String),
        commit: MappedRole(json['commit'] as String),
      );

  final MappedRole own;
  final MappedRole maintain;
  final MappedRole commit;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'own': own.value,
        'maintain': maintain.value,
        'commit': commit.value,
      };
}

/// One forge-neutral event, discriminated by `type`. The specification's event table
/// says what each type means and what the VTC does with it.
typedef ForgeEvent = Map<String, dynamic>;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The VTC has recorded the event. Nothing else is returned.
class Response {
  const Response({
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        if (ext != null) 'ext': ext!,
      };
}

/// `roleAdded` — someone holds a forge role the projection does not give them.
/// `roleRemoved` — a projected role is missing. `roleChanged` — a projected role is
/// present at another level. `requiredCheckMissing` — the verify-trust check is no
/// longer required. `protectionWeakened` — branch protection or a ruleset is weaker
/// than the projection in another way (force-push allowed, bypass actors added).
/// `bootstrapMissing` — a bootstrap file or variable is gone.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const DriftItemType(String value) {
  static const DriftItemType roleAdded = DriftItemType('roleAdded');
  static const DriftItemType roleRemoved = DriftItemType('roleRemoved');
  static const DriftItemType roleChanged = DriftItemType('roleChanged');
  static const DriftItemType requiredCheckMissing =
      DriftItemType('requiredCheckMissing');
  static const DriftItemType protectionWeakened =
      DriftItemType('protectionWeakened');
  static const DriftItemType bootstrapMissing =
      DriftItemType('bootstrapMissing');

  /// Every value this specification's schema permits.
  static const List<DriftItemType> values = <DriftItemType>[
    roleAdded,
    roleRemoved,
    roleChanged,
    requiredCheckMissing,
    protectionWeakened,
    bootstrapMissing
  ];
}

/// One difference between the forge's observed state and the VTC's projection of a
/// repository.
class DriftItem {
  const DriftItem({
    required this.type,
    required this.resource,
    this.account,
    this.observed,
    this.expected,
  });

  /// Read this payload from a decoded JSON object.
  factory DriftItem.fromJson(Map<String, dynamic> json) => DriftItem(
        type: DriftItemType(json['type'] as String),
        resource: json['resource'] as String,
        account: json['account'] == null
            ? null
            : ForgeAccount.fromJson(json['account'] as Map<String, dynamic>),
        observed: json['observed'] as String?,
        expected: json['expected'] as String?,
      );

  /// `roleAdded` — someone holds a forge role the projection does not give them.
  /// `roleRemoved` — a projected role is missing. `roleChanged` — a projected role is
  /// present at another level. `requiredCheckMissing` — the verify-trust check is no
  /// longer required. `protectionWeakened` — branch protection or a ruleset is weaker
  /// than the projection in another way (force-push allowed, bypass actors added).
  /// `bootstrapMissing` — a bootstrap file or variable is gone.
  final DriftItemType type;
  final RepoResource resource;

  /// Whose role differs, for the three role types.
  final ForgeAccount? account;

  /// What the forge shows, in the forge's own vocabulary (a role name such as
  /// `maintain`, a setting name). Absent when nothing is there.
  final String? observed;

  /// What the projection calls for, in the forge's own vocabulary. Absent when the
  /// projection calls for nothing.
  final String? expected;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'type': type.value,
        'resource': resource,
        if (account != null) 'account': account!.toJson(),
        if (observed != null) 'observed': observed!,
        if (expected != null) 'expected': expected!,
      };
}

/// The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for
/// the life of the binding; never reused for another binding.
typedef NamespaceId = String;

/// The bridge tells the VTC something happened on the forge — translated from the
/// forge's webhook or found by an inspection sweep — together with any drift it now
/// sees; or reports the role map it projects with.
class Payload {
  const Payload({
    required this.namespace,
    required this.event,
    this.drift,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        namespace: json['namespace'] as String,
        event: json['event'] as Map<String, dynamic>,
        drift: json['drift'] == null
            ? null
            : (json['drift'] as List<dynamic>)
                .map((e) => DriftItem.fromJson(e as Map<String, dynamic>))
                .toList(),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The namespace the event happened in.
  final NamespaceId namespace;
  final ForgeEvent event;

  /// The complete outstanding drift for each repository the event concerns, replacing
  /// what the VTC held for those repositories. Absent: the event changes no repository's
  /// drift.
  final List<DriftItem>? drift;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'namespace': namespace,
        'event': event,
        if (drift != null) 'drift': drift!.map((e) => e.toJson()).toList(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/git-ns/bridge/event/0.3';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/git-ns/bridge/event/0.3#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/git-ns/bridge/event/0.3","title":"Git Namespaces — Bridge Event — payload","description":"The bridge tells the VTC something happened on the forge — translated from the forge\'s webhook or found by an inspection sweep — together with any drift it now sees; or reports the role map it projects with.","type":"object","additionalProperties":false,"required":["namespace","event"],"properties":{"namespace":{"\$ref":"#/\$defs/NamespaceId","description":"The namespace the event happened in."},"event":{"\$ref":"#/\$defs/ForgeEvent"},"drift":{"type":"array","items":{"\$ref":"#/\$defs/DriftItem"},"description":"The complete outstanding drift for each repository the event concerns, replacing what the VTC held for those repositories. Absent: the event changes no repository\'s drift."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"JobId":{"type":"string","minLength":1,"maxLength":128,"description":"The VTC\'s identifier for a job, unique across the VTC\'s jobs."},"ForgeRole":{"type":"string","minLength":1,"maxLength":64,"description":"A role in the forge\'s own vocabulary (`maintain`, `write`)."},"ForgeEvent":{"title":"ForgeEvent","description":"One forge-neutral event, discriminated by `type`. The specification\'s event table says what each type means and what the VTC does with it.","type":"object","oneOf":[{"title":"RepoRenamed","type":"object","additionalProperties":false,"required":["type","forgeId","from","to"],"properties":{"type":{"const":"repoRenamed"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"from":{"\$ref":"#/\$defs/RepoResource"},"to":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RepoTransferred","type":"object","additionalProperties":false,"required":["type","forgeId","from","to"],"properties":{"type":{"const":"repoTransferred"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"from":{"\$ref":"#/\$defs/RepoResource"},"to":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RepoCreatedUnmanaged","type":"object","additionalProperties":false,"required":["type","forgeId","resource"],"properties":{"type":{"const":"repoCreatedUnmanaged"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RepoDeleted","type":"object","additionalProperties":false,"required":["type","forgeId","resource"],"properties":{"type":{"const":"repoDeleted"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RoleChanged","type":"object","additionalProperties":false,"required":["type","forgeId","resource","account"],"properties":{"type":{"const":"roleChanged"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"},"account":{"\$ref":"#/\$defs/ForgeAccount"},"role":{"\$ref":"#/\$defs/ForgeRole","description":"The role the account now holds. Absent: the account holds no role now."}}},{"title":"ProtectionChanged","type":"object","additionalProperties":false,"required":["type","forgeId","resource","requiredCheck"],"properties":{"type":{"const":"protectionChanged"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"},"requiredCheck":{"type":"boolean","description":"Whether the forge still refuses merges without a passing verify-trust check, with no bypass."}}},{"title":"InstallationRemoved","type":"object","additionalProperties":false,"required":["type"],"properties":{"type":{"const":"installationRemoved"}}},{"title":"AccountLinked","type":"object","additionalProperties":false,"required":["type","jobId","account"],"properties":{"type":{"const":"accountLinked"},"jobId":{"\$ref":"#/\$defs/JobId","description":"The beginAccountLink job."},"account":{"\$ref":"#/\$defs/ForgeAccount"}}},{"title":"BindCompleted","type":"object","additionalProperties":false,"required":["type","jobId","ownerId","kind"],"properties":{"type":{"const":"bindCompleted"},"jobId":{"\$ref":"#/\$defs/JobId","description":"The beginBind job."},"ownerId":{"\$ref":"#/\$defs/ForgeId","description":"The forge\'s id for the owner, which survives renames."},"kind":{"type":"string","enum":["organization","user"]}}},{"title":"RoleMapReported","type":"object","additionalProperties":false,"required":["type","roleMap"],"properties":{"type":{"const":"roleMapReported"},"roleMap":{"\$ref":"#/\$defs/RoleMap"},"repos":{"type":"array","maxItems":1000,"description":"Repositories whose own map differs from `roleMap`, each at most once. Absent or empty: every repository uses `roleMap`.","items":{"type":"object","additionalProperties":false,"required":["resource","roleMap"],"properties":{"resource":{"\$ref":"#/\$defs/RepoResource"},"roleMap":{"\$ref":"#/\$defs/RoleMap"}}}},"stale":{"type":"array","maxItems":10000,"uniqueItems":true,"description":"Repositories whose forge roles the bridge last projected under a different map from the one it now applies to them. Absent or empty: none.","items":{"\$ref":"#/\$defs/RepoResource"}}},"description":"`roleMap` is the map the bridge projects every repository in the namespace with, except those listed in `repos`."}]},"Response":{"\$anchor":"response","title":"Git Namespaces — Bridge Event — response payload","description":"The VTC has recorded the event. Nothing else is returned.","type":"object","additionalProperties":false,"required":[],"properties":{"ext":{"\$ref":"#/\$defs/Ext"}}},"MappedRole":{"title":"MappedRole","type":"string","enum":["none","read","triage","write","maintain","admin"],"description":"A level of the bridge\'s forge-neutral role ladder, lowest first: the vocabulary role drift\'s `observed` and `expected` are reported in. `none` is no direct role on the repository."},"RoleMap":{"title":"RoleMap","description":"The forge role each repository right is given, as the forge applies it: after the forge\'s own ladder has rounded it down. `own` is the role `git.repo.own` projects to, `maintain` that of `git.repo.maintain`, `commit` that of `git.commit.sign` (`none` is fork-based contribution). There is no member for `git.ns.admin` or `git.repo.create`, which project to no forge role whatever a bridge is configured with. Ordered: `own` is at least `maintain`, which is at least `commit`, and `commit` is at most `write`.","type":"object","additionalProperties":false,"required":["own","maintain","commit"],"properties":{"own":{"\$ref":"#/\$defs/MappedRole"},"maintain":{"\$ref":"#/\$defs/MappedRole"},"commit":{"\$ref":"#/\$defs/MappedRole"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RepoResource":{"title":"RepoResource","description":"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.","type":"string","maxLength":455,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}\$"},"ForgeId":{"title":"ForgeId","description":"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\"812736451\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.","type":"string","minLength":1,"maxLength":64},"ForgeAccount":{"title":"ForgeAccount","description":"A person\'s account on one forge. `id` is authoritative; `login` is for display only, because logins can be renamed and re-registered.","type":"object","additionalProperties":false,"required":["forge","id","login"],"properties":{"forge":{"\$ref":"#/\$defs/ForgeHost"},"id":{"\$ref":"#/\$defs/ForgeId"},"login":{"type":"string","minLength":1,"maxLength":100,"description":"The account\'s current login, as the forge reported it when last seen. Display only."}}},"ForgeHost":{"title":"ForgeHost","description":"The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No scheme, no port, no path. The host is a segment of every resource, so a right never crosses forges.","type":"string","minLength":3,"maxLength":253,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+\$"},"DriftItem":{"title":"DriftItem","description":"One difference between the forge\'s observed state and the VTC\'s projection of a repository.","type":"object","additionalProperties":false,"required":["type","resource"],"properties":{"type":{"type":"string","enum":["roleAdded","roleRemoved","roleChanged","requiredCheckMissing","protectionWeakened","bootstrapMissing"],"description":"`roleAdded` — someone holds a forge role the projection does not give them. `roleRemoved` — a projected role is missing. `roleChanged` — a projected role is present at another level. `requiredCheckMissing` — the verify-trust check is no longer required. `protectionWeakened` — branch protection or a ruleset is weaker than the projection in another way (force-push allowed, bypass actors added). `bootstrapMissing` — a bootstrap file or variable is gone."},"resource":{"\$ref":"#/\$defs/RepoResource"},"account":{"\$ref":"#/\$defs/ForgeAccount","description":"Whose role differs, for the three role types."},"observed":{"type":"string","maxLength":256,"description":"What the forge shows, in the forge\'s own vocabulary (a role name such as `maintain`, a setting name). Absent when nothing is there."},"expected":{"type":"string","maxLength":256,"description":"What the projection calls for, in the forge\'s own vocabulary. Absent when the projection calls for nothing."}}},"NamespaceId":{"title":"NamespaceId","description":"The VTC\'s opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.","type":"string","minLength":1,"maxLength":128}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"JobId":{"type":"string","minLength":1,"maxLength":128,"description":"The VTC\'s identifier for a job, unique across the VTC\'s jobs."},"ForgeRole":{"type":"string","minLength":1,"maxLength":64,"description":"A role in the forge\'s own vocabulary (`maintain`, `write`)."},"ForgeEvent":{"title":"ForgeEvent","description":"One forge-neutral event, discriminated by `type`. The specification\'s event table says what each type means and what the VTC does with it.","type":"object","oneOf":[{"title":"RepoRenamed","type":"object","additionalProperties":false,"required":["type","forgeId","from","to"],"properties":{"type":{"const":"repoRenamed"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"from":{"\$ref":"#/\$defs/RepoResource"},"to":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RepoTransferred","type":"object","additionalProperties":false,"required":["type","forgeId","from","to"],"properties":{"type":{"const":"repoTransferred"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"from":{"\$ref":"#/\$defs/RepoResource"},"to":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RepoCreatedUnmanaged","type":"object","additionalProperties":false,"required":["type","forgeId","resource"],"properties":{"type":{"const":"repoCreatedUnmanaged"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RepoDeleted","type":"object","additionalProperties":false,"required":["type","forgeId","resource"],"properties":{"type":{"const":"repoDeleted"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"}}},{"title":"RoleChanged","type":"object","additionalProperties":false,"required":["type","forgeId","resource","account"],"properties":{"type":{"const":"roleChanged"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"},"account":{"\$ref":"#/\$defs/ForgeAccount"},"role":{"\$ref":"#/\$defs/ForgeRole","description":"The role the account now holds. Absent: the account holds no role now."}}},{"title":"ProtectionChanged","type":"object","additionalProperties":false,"required":["type","forgeId","resource","requiredCheck"],"properties":{"type":{"const":"protectionChanged"},"forgeId":{"\$ref":"#/\$defs/ForgeId"},"resource":{"\$ref":"#/\$defs/RepoResource"},"requiredCheck":{"type":"boolean","description":"Whether the forge still refuses merges without a passing verify-trust check, with no bypass."}}},{"title":"InstallationRemoved","type":"object","additionalProperties":false,"required":["type"],"properties":{"type":{"const":"installationRemoved"}}},{"title":"AccountLinked","type":"object","additionalProperties":false,"required":["type","jobId","account"],"properties":{"type":{"const":"accountLinked"},"jobId":{"\$ref":"#/\$defs/JobId","description":"The beginAccountLink job."},"account":{"\$ref":"#/\$defs/ForgeAccount"}}},{"title":"BindCompleted","type":"object","additionalProperties":false,"required":["type","jobId","ownerId","kind"],"properties":{"type":{"const":"bindCompleted"},"jobId":{"\$ref":"#/\$defs/JobId","description":"The beginBind job."},"ownerId":{"\$ref":"#/\$defs/ForgeId","description":"The forge\'s id for the owner, which survives renames."},"kind":{"type":"string","enum":["organization","user"]}}},{"title":"RoleMapReported","type":"object","additionalProperties":false,"required":["type","roleMap"],"properties":{"type":{"const":"roleMapReported"},"roleMap":{"\$ref":"#/\$defs/RoleMap"},"repos":{"type":"array","maxItems":1000,"description":"Repositories whose own map differs from `roleMap`, each at most once. Absent or empty: every repository uses `roleMap`.","items":{"type":"object","additionalProperties":false,"required":["resource","roleMap"],"properties":{"resource":{"\$ref":"#/\$defs/RepoResource"},"roleMap":{"\$ref":"#/\$defs/RoleMap"}}}},"stale":{"type":"array","maxItems":10000,"uniqueItems":true,"description":"Repositories whose forge roles the bridge last projected under a different map from the one it now applies to them. Absent or empty: none.","items":{"\$ref":"#/\$defs/RepoResource"}}},"description":"`roleMap` is the map the bridge projects every repository in the namespace with, except those listed in `repos`."}]},"Response":{"\$anchor":"response","title":"Git Namespaces — Bridge Event — response payload","description":"The VTC has recorded the event. Nothing else is returned.","type":"object","additionalProperties":false,"required":[],"properties":{"ext":{"\$ref":"#/\$defs/Ext"}}},"MappedRole":{"title":"MappedRole","type":"string","enum":["none","read","triage","write","maintain","admin"],"description":"A level of the bridge\'s forge-neutral role ladder, lowest first: the vocabulary role drift\'s `observed` and `expected` are reported in. `none` is no direct role on the repository."},"RoleMap":{"title":"RoleMap","description":"The forge role each repository right is given, as the forge applies it: after the forge\'s own ladder has rounded it down. `own` is the role `git.repo.own` projects to, `maintain` that of `git.repo.maintain`, `commit` that of `git.commit.sign` (`none` is fork-based contribution). There is no member for `git.ns.admin` or `git.repo.create`, which project to no forge role whatever a bridge is configured with. Ordered: `own` is at least `maintain`, which is at least `commit`, and `commit` is at most `write`.","type":"object","additionalProperties":false,"required":["own","maintain","commit"],"properties":{"own":{"\$ref":"#/\$defs/MappedRole"},"maintain":{"\$ref":"#/\$defs/MappedRole"},"commit":{"\$ref":"#/\$defs/MappedRole"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"RepoResource":{"title":"RepoResource","description":"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.","type":"string","maxLength":455,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}\$"},"ForgeId":{"title":"ForgeId","description":"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\"812736451\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.","type":"string","minLength":1,"maxLength":64},"ForgeAccount":{"title":"ForgeAccount","description":"A person\'s account on one forge. `id` is authoritative; `login` is for display only, because logins can be renamed and re-registered.","type":"object","additionalProperties":false,"required":["forge","id","login"],"properties":{"forge":{"\$ref":"#/\$defs/ForgeHost"},"id":{"\$ref":"#/\$defs/ForgeId"},"login":{"type":"string","minLength":1,"maxLength":100,"description":"The account\'s current login, as the forge reported it when last seen. Display only."}}},"ForgeHost":{"title":"ForgeHost","description":"The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No scheme, no port, no path. The host is a segment of every resource, so a right never crosses forges.","type":"string","minLength":3,"maxLength":253,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+\$"},"DriftItem":{"title":"DriftItem","description":"One difference between the forge\'s observed state and the VTC\'s projection of a repository.","type":"object","additionalProperties":false,"required":["type","resource"],"properties":{"type":{"type":"string","enum":["roleAdded","roleRemoved","roleChanged","requiredCheckMissing","protectionWeakened","bootstrapMissing"],"description":"`roleAdded` — someone holds a forge role the projection does not give them. `roleRemoved` — a projected role is missing. `roleChanged` — a projected role is present at another level. `requiredCheckMissing` — the verify-trust check is no longer required. `protectionWeakened` — branch protection or a ruleset is weaker than the projection in another way (force-push allowed, bypass actors added). `bootstrapMissing` — a bootstrap file or variable is gone."},"resource":{"\$ref":"#/\$defs/RepoResource"},"account":{"\$ref":"#/\$defs/ForgeAccount","description":"Whose role differs, for the three role types."},"observed":{"type":"string","maxLength":256,"description":"What the forge shows, in the forge\'s own vocabulary (a role name such as `maintain`, a setting name). Absent when nothing is there."},"expected":{"type":"string","maxLength":256,"description":"What the projection calls for, in the forge\'s own vocabulary. Absent when the projection calls for nothing."}}},"NamespaceId":{"title":"NamespaceId","description":"The VTC\'s opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.","type":"string","minLength":1,"maxLength":128}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
