// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/bridge/job/0.2/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The VTC's identifier for a job, unique across the VTC's jobs.
typedef JobId = String;

/// One step of a forge adapter's plan. Forge-neutral names: `create`, `workflow`,
/// `keyring`, `variables`, `requiredCheck`, `roles`, `archive`. An adapter MAY add its
/// own (`mergeStyle`, `runner`), which a VTC displays but need not understand.
typedef StepName = String;

/// A DID, compared by exact string equality.
typedef Did = String;

/// The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host,
/// `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No
/// scheme, no port, no path. The host is a segment of every resource, so a right never
/// crosses forges.
typedef ForgeHost = String;

/// An identifier the forge itself assigns — a repository id, a user or organisation id
/// — carried as a string so a forge whose ids are not numbers needs no new version.
/// GitHub and Forgejo ids are decimal integers written as strings (`"812736451"`).
/// Unlike a name, it survives renames and transfers, which is why rights and bindings
/// are keyed by it.
typedef ForgeId = String;

/// A person's account on one forge. `id` is authoritative; `login` is for display
/// only, because logins can be renamed and re-registered.
class ForgeAccount {
  const ForgeAccount({
    required this.forge,
    required this.id,
    required this.login,
  });

  /// Read this payload from a decoded JSON object.
  factory ForgeAccount.fromJson(Map<String, dynamic> json) => ForgeAccount(
        forge: json['forge'] as String,
        id: json['id'] as String,
        login: json['login'] as String,
      );

  final ForgeHost forge;
  final ForgeId id;

  /// The account's current login, as the forge reported it when last seen. Display only.
  final String login;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'forge': forge,
        'id': id,
        'login': login,
      };
}

/// One of the five git rights. Each string is also the TRQP `action` the VTC publishes
/// the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin`
/// and `git.repo.create` apply to a namespace resource; `git.repo.own` and
/// `git.repo.maintain` to a repository resource; `git.commit.sign` to either.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const Right(String value) {
  static const Right gitNsAdmin = Right('git.ns.admin');
  static const Right gitRepoCreate = Right('git.repo.create');
  static const Right gitRepoOwn = Right('git.repo.own');
  static const Right gitRepoMaintain = Right('git.repo.maintain');
  static const Right gitCommitSign = Right('git.commit.sign');

  /// Every value this specification's schema permits.
  static const List<Right> values = <Right>[
    gitNsAdmin,
    gitRepoCreate,
    gitRepoOwn,
    gitRepoMaintain,
    gitCommitSign
  ];
}

/// DesiredRole, generated from its schema.
class DesiredRole {
  const DesiredRole({
    required this.subject,
    required this.account,
    required this.right,
  });

  /// Read this payload from a decoded JSON object.
  factory DesiredRole.fromJson(Map<String, dynamic> json) => DesiredRole(
        subject: json['subject'] as String,
        account: ForgeAccount.fromJson(json['account'] as Map<String, dynamic>),
        right: Right(json['right'] as String),
      );

  final Did subject;

  /// The subject's linked account on this namespace's forge.
  final ForgeAccount account;

  /// The subject's highest effective right on the target, which the forge adapter maps
  /// to one of its roles.
  final Right right;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'subject': subject,
        'account': account.toJson(),
        'right': right.value,
      };
}

/// Repository visibility on the forge.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const RepoVisibility(String value) {
  static const RepoVisibility public = RepoVisibility('public');
  static const RepoVisibility private = RepoVisibility('private');

  /// Every value this specification's schema permits.
  static const List<RepoVisibility> values = <RepoVisibility>[public, private];
}

/// RepoSpec, generated from its schema.
class RepoSpec {
  const RepoSpec({
    required this.visibility,
    this.description,
  });

  /// Read this payload from a decoded JSON object.
  factory RepoSpec.fromJson(Map<String, dynamic> json) => RepoSpec(
        visibility: RepoVisibility(json['visibility'] as String),
        description: json['description'] as String?,
      );

  final RepoVisibility visibility;
  final String? description;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'visibility': visibility.value,
        if (description != null) 'description': description!,
      };
}

/// One lowercased owner or repository name. Forges compare these case-insensitively,
/// so the wire form is always lowercase and a producer lowercases before sending. A
/// leading `.` is refused, which rules out `.` and `..`.
typedef Segment = String;

/// The forge owner whose control the binding admin is to prove.
class BindTarget {
  const BindTarget({
    required this.forge,
    required this.owner,
  });

  /// Read this payload from a decoded JSON object.
  factory BindTarget.fromJson(Map<String, dynamic> json) => BindTarget(
        forge: json['forge'] as String,
        owner: json['owner'] as String,
      );

  final ForgeHost forge;
  final Segment owner;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'forge': forge,
        'owner': owner,
      };
}

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC
/// sends the person.
class ResponseNext {
  const ResponseNext({
    required this.url,
    this.userCode,
    required this.expiresAt,
  });

  /// Read this payload from a decoded JSON object.
  factory ResponseNext.fromJson(Map<String, dynamic> json) => ResponseNext(
        url: json['url'] as String,
        userCode: json['userCode'] as String?,
        expiresAt: json['expiresAt'] as String,
      );

  final String url;

  /// The device-flow code, for a forge that uses one.
  final String? userCode;
  final String expiresAt;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'url': url,
        if (userCode != null) 'userCode': userCode!,
        'expiresAt': expiresAt,
      };
}

/// Whether the bridge took the job.
class Response {
  const Response({
    required this.jobId,
    required this.accepted,
    this.next,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        jobId: json['jobId'] as String,
        accepted: json['accepted'] as bool,
        next: json['next'] == null
            ? null
            : ResponseNext.fromJson(json['next'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final JobId jobId;

  /// `true` — the job is durably queued, or was already queued or running under this
  /// `jobId`; a git-ns/bridge/result follows. `false` — the bridge already finished this
  /// `jobId`; it does not run it again, and sends its git-ns/bridge/result again.
  final bool accepted;

  /// For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC
  /// sends the person.
  final ResponseNext? next;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jobId': jobId,
        'accepted': accepted,
        if (next != null) 'next': next!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// A forge-qualified resource naming exactly one repository:
/// `&lt;forge-host&gt;/&lt;owner&gt;/&lt;repo&gt;`, lowercase.
typedef RepoResource = String;

/// The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for
/// the life of the binding; never reused for another binding.
typedef NamespaceId = String;

/// What to do. `projectRoles` — converge forge roles to `desiredRoles`, and remove
/// whatever role each of `removeAccounts` holds on `repo`. `createRepo` — create
/// `repo`, run the bootstrap plan, and project `desiredRoles`. `bootstrap` — run the
/// bootstrap plan, or the listed `steps` of it, on an existing `repo`. `archive` —
/// archive `repo`. `inspect` — compare the forge with the projection for `repo`, or
/// for the whole namespace. `beginBind` — start the proof that the binding admin
/// controls `target`. `beginAccountLink` — start linking `subject`'s account on this
/// namespace's forge.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadKind(String value) {
  static const PayloadKind projectRoles = PayloadKind('projectRoles');
  static const PayloadKind createRepo = PayloadKind('createRepo');
  static const PayloadKind bootstrap = PayloadKind('bootstrap');
  static const PayloadKind archive = PayloadKind('archive');
  static const PayloadKind inspect = PayloadKind('inspect');
  static const PayloadKind beginBind = PayloadKind('beginBind');
  static const PayloadKind beginAccountLink = PayloadKind('beginAccountLink');

  /// Every value this specification's schema permits.
  static const List<PayloadKind> values = <PayloadKind>[
    projectRoles,
    createRepo,
    bootstrap,
    archive,
    inspect,
    beginBind,
    beginAccountLink
  ];
}

/// The VTC asks its bridge to do one piece of forge work in a namespace. The job names
/// what to do (`kind`) and carries the desired state, never forge credentials — the
/// bridge holds those. Which optional members a job carries is fixed by its `kind`
/// (see the specification's kind table); a bridge refuses a job that carries a member
/// its kind does not use, or lacks one its kind requires.
class Payload {
  const Payload({
    required this.jobId,
    required this.namespace,
    required this.kind,
    this.repo,
    this.spec,
    this.desiredRoles,
    this.removeAccounts,
    this.steps,
    this.target,
    this.subject,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        jobId: json['jobId'] as String,
        namespace: json['namespace'] as String,
        kind: PayloadKind(json['kind'] as String),
        repo: json['repo'] as String?,
        spec: json['spec'] == null
            ? null
            : RepoSpec.fromJson(json['spec'] as Map<String, dynamic>),
        desiredRoles: json['desiredRoles'] == null
            ? null
            : (json['desiredRoles'] as List<dynamic>)
                .map((e) => DesiredRole.fromJson(e as Map<String, dynamic>))
                .toList(),
        removeAccounts: json['removeAccounts'] == null
            ? null
            : (json['removeAccounts'] as List<dynamic>)
                .map((e) => ForgeAccount.fromJson(e as Map<String, dynamic>))
                .toList(),
        steps: json['steps'] == null
            ? null
            : (json['steps'] as List<dynamic>).map((e) => e as String).toList(),
        target: json['target'] == null
            ? null
            : BindTarget.fromJson(json['target'] as Map<String, dynamic>),
        subject: json['subject'] as String?,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The VTC's identifier for this job. The bridge reports on it with
  /// git-ns/bridge/result.
  final JobId jobId;

  /// The namespace the job acts in. It also selects the bridge's forge credentials for
  /// it.
  final NamespaceId namespace;

  /// What to do. `projectRoles` — converge forge roles to `desiredRoles`, and remove
  /// whatever role each of `removeAccounts` holds on `repo`. `createRepo` — create
  /// `repo`, run the bootstrap plan, and project `desiredRoles`. `bootstrap` — run the
  /// bootstrap plan, or the listed `steps` of it, on an existing `repo`. `archive` —
  /// archive `repo`. `inspect` — compare the forge with the projection for `repo`, or
  /// for the whole namespace. `beginBind` — start the proof that the binding admin
  /// controls `target`. `beginAccountLink` — start linking `subject`'s account on this
  /// namespace's forge.
  final PayloadKind kind;

  /// The repository. Required for `createRepo`, `bootstrap` and `archive`. For
  /// `projectRoles`, absent means the namespace-level roles (organisation owners), where
  /// only `git.ns.admin` is projected. For `inspect`, absent means a sweep of every
  /// repository in the namespace.
  final RepoResource? repo;

  /// How to create the repository. `createRepo` only.
  final RepoSpec? spec;

  /// The complete set of people who should hold a forge role on the target. The bridge
  /// converges to exactly this set among the roles it manages: it adds or changes roles
  /// to match and removes managed roles not listed. `projectRoles` and `createRepo`
  /// only.
  final List<DesiredRole>? desiredRoles;

  /// Accounts whose direct role on `repo` the bridge removes, whatever that role is —
  /// including a role the bridge does not manage and would otherwise only report as
  /// drift. Matched by `forge` and `id`; `login` is display only. No account listed here
  /// may also appear in `desiredRoles`. `projectRoles` with `repo` only.
  final List<ForgeAccount>? removeAccounts;

  /// Run only these steps of the adapter's bootstrap plan, in the plan's order. Absent:
  /// the whole plan. `bootstrap` only.
  final List<StepName>? steps;

  /// `beginBind` only.
  final BindTarget? target;

  /// The member whose forge account is being linked. `beginAccountLink` only.
  final Did? subject;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jobId': jobId,
        'namespace': namespace,
        'kind': kind.value,
        if (repo != null) 'repo': repo!,
        if (spec != null) 'spec': spec!.toJson(),
        if (desiredRoles != null)
          'desiredRoles': desiredRoles!.map((e) => e.toJson()).toList(),
        if (removeAccounts != null)
          'removeAccounts': removeAccounts!.map((e) => e.toJson()).toList(),
        if (steps != null) 'steps': steps!,
        if (target != null) 'target': target!.toJson(),
        if (subject != null) 'subject': subject!,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/git-ns/bridge/job/0.2';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/git-ns/bridge/job/0.2#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/git-ns/bridge/job/0.2","title":"Git Namespaces — Bridge Job — payload","description":"The VTC asks its bridge to do one piece of forge work in a namespace. The job names what to do (`kind`) and carries the desired state, never forge credentials — the bridge holds those. Which optional members a job carries is fixed by its `kind` (see the specification\'s kind table); a bridge refuses a job that carries a member its kind does not use, or lacks one its kind requires.","type":"object","additionalProperties":false,"required":["jobId","namespace","kind"],"properties":{"jobId":{"\$ref":"#/\$defs/JobId","description":"The VTC\'s identifier for this job. The bridge reports on it with git-ns/bridge/result."},"namespace":{"\$ref":"#/\$defs/NamespaceId","description":"The namespace the job acts in. It also selects the bridge\'s forge credentials for it."},"kind":{"type":"string","enum":["projectRoles","createRepo","bootstrap","archive","inspect","beginBind","beginAccountLink"],"description":"What to do. `projectRoles` — converge forge roles to `desiredRoles`, and remove whatever role each of `removeAccounts` holds on `repo`. `createRepo` — create `repo`, run the bootstrap plan, and project `desiredRoles`. `bootstrap` — run the bootstrap plan, or the listed `steps` of it, on an existing `repo`. `archive` — archive `repo`. `inspect` — compare the forge with the projection for `repo`, or for the whole namespace. `beginBind` — start the proof that the binding admin controls `target`. `beginAccountLink` — start linking `subject`\'s account on this namespace\'s forge."},"repo":{"\$ref":"#/\$defs/RepoResource","description":"The repository. Required for `createRepo`, `bootstrap` and `archive`. For `projectRoles`, absent means the namespace-level roles (organisation owners), where only `git.ns.admin` is projected. For `inspect`, absent means a sweep of every repository in the namespace."},"spec":{"\$ref":"#/\$defs/RepoSpec","description":"How to create the repository. `createRepo` only."},"desiredRoles":{"type":"array","uniqueItems":true,"items":{"\$ref":"#/\$defs/DesiredRole"},"description":"The complete set of people who should hold a forge role on the target. The bridge converges to exactly this set among the roles it manages: it adds or changes roles to match and removes managed roles not listed. `projectRoles` and `createRepo` only."},"removeAccounts":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/ForgeAccount"},"description":"Accounts whose direct role on `repo` the bridge removes, whatever that role is — including a role the bridge does not manage and would otherwise only report as drift. Matched by `forge` and `id`; `login` is display only. No account listed here may also appear in `desiredRoles`. `projectRoles` with `repo` only."},"steps":{"type":"array","minItems":1,"uniqueItems":true,"items":{"\$ref":"#/\$defs/StepName"},"description":"Run only these steps of the adapter\'s bootstrap plan, in the plan\'s order. Absent: the whole plan. `bootstrap` only."},"target":{"\$ref":"#/\$defs/BindTarget","description":"`beginBind` only."},"subject":{"\$ref":"#/\$defs/Did","description":"The member whose forge account is being linked. `beginAccountLink` only."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"JobId":{"type":"string","minLength":1,"maxLength":128,"description":"The VTC\'s identifier for a job, unique across the VTC\'s jobs."},"StepName":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z][A-Za-z0-9]*\$","description":"One step of a forge adapter\'s plan. Forge-neutral names: `create`, `workflow`, `keyring`, `variables`, `requiredCheck`, `roles`, `archive`. An adapter MAY add its own (`mergeStyle`, `runner`), which a VTC displays but need not understand."},"DesiredRole":{"type":"object","additionalProperties":false,"required":["subject","account","right"],"properties":{"subject":{"\$ref":"#/\$defs/Did"},"account":{"\$ref":"#/\$defs/ForgeAccount","description":"The subject\'s linked account on this namespace\'s forge."},"right":{"\$ref":"#/\$defs/Right","description":"The subject\'s highest effective right on the target, which the forge adapter maps to one of its roles."}}},"RepoSpec":{"type":"object","additionalProperties":false,"required":["visibility"],"properties":{"visibility":{"\$ref":"#/\$defs/RepoVisibility"},"description":{"type":"string","maxLength":350}}},"BindTarget":{"title":"BindTarget","description":"The forge owner whose control the binding admin is to prove.","type":"object","additionalProperties":false,"required":["forge","owner"],"properties":{"forge":{"\$ref":"#/\$defs/ForgeHost"},"owner":{"\$ref":"#/\$defs/Segment"}}},"Response":{"\$anchor":"response","title":"Git Namespaces — Bridge Job — response payload","description":"Whether the bridge took the job.","type":"object","additionalProperties":false,"required":["jobId","accepted"],"properties":{"jobId":{"\$ref":"#/\$defs/JobId"},"accepted":{"type":"boolean","description":"`true` — the job is durably queued, or was already queued or running under this `jobId`; a git-ns/bridge/result follows. `false` — the bridge already finished this `jobId`; it does not run it again, and sends its git-ns/bridge/result again."},"next":{"type":"object","additionalProperties":false,"required":["url","expiresAt"],"description":"For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC sends the person.","properties":{"url":{"type":"string","format":"uri","maxLength":2048,"pattern":"^https://"},"userCode":{"type":"string","minLength":1,"maxLength":64,"description":"The device-flow code, for a forge that uses one."},"expiresAt":{"type":"string","format":"date-time"}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Segment":{"title":"Segment","description":"One lowercased owner or repository name. Forges compare these case-insensitively, so the wire form is always lowercase and a producer lowercases before sending. A leading `.` is refused, which rules out `.` and `..`.","type":"string","minLength":1,"maxLength":100,"pattern":"^[a-z0-9_-][a-z0-9._-]*\$"},"ForgeHost":{"title":"ForgeHost","description":"The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No scheme, no port, no path. The host is a segment of every resource, so a right never crosses forges.","type":"string","minLength":3,"maxLength":253,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+\$"},"RepoVisibility":{"title":"RepoVisibility","type":"string","enum":["public","private"],"description":"Repository visibility on the forge."},"Right":{"title":"Right","description":"One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.","type":"string","enum":["git.ns.admin","git.repo.create","git.repo.own","git.repo.maintain","git.commit.sign"]},"ForgeAccount":{"title":"ForgeAccount","description":"A person\'s account on one forge. `id` is authoritative; `login` is for display only, because logins can be renamed and re-registered.","type":"object","additionalProperties":false,"required":["forge","id","login"],"properties":{"forge":{"\$ref":"#/\$defs/ForgeHost"},"id":{"\$ref":"#/\$defs/ForgeId"},"login":{"type":"string","minLength":1,"maxLength":100,"description":"The account\'s current login, as the forge reported it when last seen. Display only."}}},"ForgeId":{"title":"ForgeId","description":"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\"812736451\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.","type":"string","minLength":1,"maxLength":64},"Did":{"title":"Did","description":"A DID, compared by exact string equality.","type":"string","maxLength":2048,"pattern":"^did:[a-z0-9]+:\\\\S+\$"},"RepoResource":{"title":"RepoResource","description":"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.","type":"string","maxLength":455,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}\$"},"NamespaceId":{"title":"NamespaceId","description":"The VTC\'s opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.","type":"string","minLength":1,"maxLength":128}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"JobId":{"type":"string","minLength":1,"maxLength":128,"description":"The VTC\'s identifier for a job, unique across the VTC\'s jobs."},"StepName":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z][A-Za-z0-9]*\$","description":"One step of a forge adapter\'s plan. Forge-neutral names: `create`, `workflow`, `keyring`, `variables`, `requiredCheck`, `roles`, `archive`. An adapter MAY add its own (`mergeStyle`, `runner`), which a VTC displays but need not understand."},"DesiredRole":{"type":"object","additionalProperties":false,"required":["subject","account","right"],"properties":{"subject":{"\$ref":"#/\$defs/Did"},"account":{"\$ref":"#/\$defs/ForgeAccount","description":"The subject\'s linked account on this namespace\'s forge."},"right":{"\$ref":"#/\$defs/Right","description":"The subject\'s highest effective right on the target, which the forge adapter maps to one of its roles."}}},"RepoSpec":{"type":"object","additionalProperties":false,"required":["visibility"],"properties":{"visibility":{"\$ref":"#/\$defs/RepoVisibility"},"description":{"type":"string","maxLength":350}}},"BindTarget":{"title":"BindTarget","description":"The forge owner whose control the binding admin is to prove.","type":"object","additionalProperties":false,"required":["forge","owner"],"properties":{"forge":{"\$ref":"#/\$defs/ForgeHost"},"owner":{"\$ref":"#/\$defs/Segment"}}},"Response":{"\$anchor":"response","title":"Git Namespaces — Bridge Job — response payload","description":"Whether the bridge took the job.","type":"object","additionalProperties":false,"required":["jobId","accepted"],"properties":{"jobId":{"\$ref":"#/\$defs/JobId"},"accepted":{"type":"boolean","description":"`true` — the job is durably queued, or was already queued or running under this `jobId`; a git-ns/bridge/result follows. `false` — the bridge already finished this `jobId`; it does not run it again, and sends its git-ns/bridge/result again."},"next":{"type":"object","additionalProperties":false,"required":["url","expiresAt"],"description":"For `beginBind` and `beginAccountLink` when `accepted` is `true`: where the VTC sends the person.","properties":{"url":{"type":"string","format":"uri","maxLength":2048,"pattern":"^https://"},"userCode":{"type":"string","minLength":1,"maxLength":64,"description":"The device-flow code, for a forge that uses one."},"expiresAt":{"type":"string","format":"date-time"}}},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"Segment":{"title":"Segment","description":"One lowercased owner or repository name. Forges compare these case-insensitively, so the wire form is always lowercase and a producer lowercases before sending. A leading `.` is refused, which rules out `.` and `..`.","type":"string","minLength":1,"maxLength":100,"pattern":"^[a-z0-9_-][a-z0-9._-]*\$"},"ForgeHost":{"title":"ForgeHost","description":"The lowercased DNS host of a forge: `github.com`, a GitHub Enterprise Server host, `codeberg.org`, or a self-hosted Forgejo instance such as `git.example.org`. No scheme, no port, no path. The host is a segment of every resource, so a right never crosses forges.","type":"string","minLength":3,"maxLength":253,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+\$"},"RepoVisibility":{"title":"RepoVisibility","type":"string","enum":["public","private"],"description":"Repository visibility on the forge."},"Right":{"title":"Right","description":"One of the five git rights. Each string is also the TRQP `action` the VTC publishes the right under in its Trust Registry, so it is carried verbatim. `git.ns.admin` and `git.repo.create` apply to a namespace resource; `git.repo.own` and `git.repo.maintain` to a repository resource; `git.commit.sign` to either.","type":"string","enum":["git.ns.admin","git.repo.create","git.repo.own","git.repo.maintain","git.commit.sign"]},"ForgeAccount":{"title":"ForgeAccount","description":"A person\'s account on one forge. `id` is authoritative; `login` is for display only, because logins can be renamed and re-registered.","type":"object","additionalProperties":false,"required":["forge","id","login"],"properties":{"forge":{"\$ref":"#/\$defs/ForgeHost"},"id":{"\$ref":"#/\$defs/ForgeId"},"login":{"type":"string","minLength":1,"maxLength":100,"description":"The account\'s current login, as the forge reported it when last seen. Display only."}}},"ForgeId":{"title":"ForgeId","description":"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\"812736451\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.","type":"string","minLength":1,"maxLength":64},"Did":{"title":"Did","description":"A DID, compared by exact string equality.","type":"string","maxLength":2048,"pattern":"^did:[a-z0-9]+:\\\\S+\$"},"RepoResource":{"title":"RepoResource","description":"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.","type":"string","maxLength":455,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}\$"},"NamespaceId":{"title":"NamespaceId","description":"The VTC\'s opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.","type":"string","minLength":1,"maxLength":128}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
