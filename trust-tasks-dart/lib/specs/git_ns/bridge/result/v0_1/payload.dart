// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/bridge/result/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// The VTC has recorded the result.
class Response {
  const Response({
    required this.jobId,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        jobId: json['jobId'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final String jobId;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jobId': jobId,
        if (ext != null) 'ext': ext!,
      };
}

/// An identifier the forge itself assigns — a repository id, a user or organisation id
/// — carried as a string so a forge whose ids are not numbers needs no new version.
/// GitHub and Forgejo ids are decimal integers written as strings (`"812736451"`).
/// Unlike a name, it survives renames and transfers, which is why rights and bindings
/// are keyed by it.
typedef ForgeId = String;

/// A forge-qualified resource naming exactly one repository:
/// `&lt;forge-host&gt;/&lt;owner&gt;/&lt;repo&gt;`, lowercase.
typedef RepoResource = String;

/// `succeeded` — every step was applied. `failed` — nothing was applied. `partial` —
/// some steps were applied and at least one failed; `steps` says which. Every step is
/// check-then-apply, so the VTC may send the job again.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadOutcome(String value) {
  static const PayloadOutcome succeeded = PayloadOutcome('succeeded');
  static const PayloadOutcome failed = PayloadOutcome('failed');
  static const PayloadOutcome partial = PayloadOutcome('partial');

  /// Every value this specification's schema permits.
  static const List<PayloadOutcome> values = <PayloadOutcome>[
    succeeded,
    failed,
    partial
  ];
}

/// The repository the job touched, as the forge now reports it. Present whenever the
/// job reached a repository.
class PayloadRepo {
  const PayloadRepo({
    required this.resource,
    required this.forgeId,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadRepo.fromJson(Map<String, dynamic> json) => PayloadRepo(
        resource: json['resource'] as String,
        forgeId: json['forgeId'] as String,
      );

  final RepoResource resource;
  final ForgeId forgeId;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'resource': resource,
        'forgeId': forgeId,
      };
}

/// `applied` — changed the forge. `unchanged` — already as required. `failed` — could
/// not be applied. `skipped` — not attempted because an earlier step failed.
///
/// An extension type rather than an enum: a value from a newer MINOR of this
/// specification must not crash the parse (SPEC §5.2), and an enum would throw on one.
/// Compare against the constants below, and treat anything else as unrecognised.
extension type const PayloadStepsItemOutcome(String value) {
  static const PayloadStepsItemOutcome applied =
      PayloadStepsItemOutcome('applied');
  static const PayloadStepsItemOutcome unchanged =
      PayloadStepsItemOutcome('unchanged');
  static const PayloadStepsItemOutcome failed =
      PayloadStepsItemOutcome('failed');
  static const PayloadStepsItemOutcome skipped =
      PayloadStepsItemOutcome('skipped');

  /// Every value this specification's schema permits.
  static const List<PayloadStepsItemOutcome> values = <PayloadStepsItemOutcome>[
    applied,
    unchanged,
    failed,
    skipped
  ];
}

/// PayloadStepsItem, generated from its schema.
class PayloadStepsItem {
  const PayloadStepsItem({
    required this.step,
    required this.outcome,
    this.detail,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadStepsItem.fromJson(Map<String, dynamic> json) =>
      PayloadStepsItem(
        step: json['step'] as String,
        outcome: PayloadStepsItemOutcome(json['outcome'] as String),
        detail: json['detail'] as String?,
      );

  /// The step name, from the adapter's plan.
  final String step;

  /// `applied` — changed the forge. `unchanged` — already as required. `failed` — could
  /// not be applied. `skipped` — not attempted because an earlier step failed.
  final PayloadStepsItemOutcome outcome;

  /// Human-readable detail, typically for a failure.
  final String? detail;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'step': step,
        'outcome': outcome.value,
        if (detail != null) 'detail': detail!,
      };
}

/// Why the job failed or stopped partway. Present when `outcome` is not `succeeded`.
class PayloadError {
  const PayloadError({
    required this.code,
    this.message,
  });

  /// Read this payload from a decoded JSON object.
  factory PayloadError.fromJson(Map<String, dynamic> json) => PayloadError(
        code: json['code'] as String,
        message: json['message'] as String?,
      );

  /// Forge-neutral: `nameTaken`, `notFound`, `forbidden` (the bridge's forge credentials
  /// lack a permission), `notCapable` (this forge or namespace cannot do it),
  /// `rateLimited`, `expired` (a begin* job's person never completed it), `forgeError`.
  /// A bridge MAY use others.
  final String code;
  final String? message;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'code': code,
        if (message != null) 'message': message!,
      };
}

/// The bridge reports how a job ended. Exactly one result is sent per job; the VTC
/// updates the repository's state, bootstrap status and forge id from it.
class Payload {
  const Payload({
    required this.jobId,
    required this.outcome,
    this.repo,
    this.steps,
    this.error,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        jobId: json['jobId'] as String,
        outcome: PayloadOutcome(json['outcome'] as String),
        repo: json['repo'] == null
            ? null
            : PayloadRepo.fromJson(json['repo'] as Map<String, dynamic>),
        steps: json['steps'] == null
            ? null
            : (json['steps'] as List<dynamic>)
                .map(
                    (e) => PayloadStepsItem.fromJson(e as Map<String, dynamic>))
                .toList(),
        error: json['error'] == null
            ? null
            : PayloadError.fromJson(json['error'] as Map<String, dynamic>),
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The job, as git-ns/bridge/job named it.
  final String jobId;

  /// `succeeded` — every step was applied. `failed` — nothing was applied. `partial` —
  /// some steps were applied and at least one failed; `steps` says which. Every step is
  /// check-then-apply, so the VTC may send the job again.
  final PayloadOutcome outcome;

  /// The repository the job touched, as the forge now reports it. Present whenever the
  /// job reached a repository.
  final PayloadRepo? repo;

  /// Per-step outcomes, in the order they ran.
  final List<PayloadStepsItem>? steps;

  /// Why the job failed or stopped partway. Present when `outcome` is not `succeeded`.
  final PayloadError? error;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'jobId': jobId,
        'outcome': outcome.value,
        if (repo != null) 'repo': repo!.toJson(),
        if (steps != null) 'steps': steps!.map((e) => e.toJson()).toList(),
        if (error != null) 'error': error!.toJson(),
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri = 'https://trusttasks.org/spec/git-ns/bridge/result/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/git-ns/bridge/result/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/git-ns/bridge/result/0.1","title":"Git Namespaces — Bridge Job Result — payload","description":"The bridge reports how a job ended. Exactly one result is sent per job; the VTC updates the repository\'s state, bootstrap status and forge id from it.","type":"object","additionalProperties":false,"required":["jobId","outcome"],"properties":{"jobId":{"type":"string","minLength":1,"maxLength":128,"description":"The job, as git-ns/bridge/job named it."},"outcome":{"type":"string","enum":["succeeded","failed","partial"],"description":"`succeeded` — every step was applied. `failed` — nothing was applied. `partial` — some steps were applied and at least one failed; `steps` says which. Every step is check-then-apply, so the VTC may send the job again."},"repo":{"type":"object","additionalProperties":false,"required":["resource","forgeId"],"description":"The repository the job touched, as the forge now reports it. Present whenever the job reached a repository.","properties":{"resource":{"\$ref":"#/\$defs/RepoResource"},"forgeId":{"\$ref":"#/\$defs/ForgeId"}}},"steps":{"type":"array","items":{"type":"object","additionalProperties":false,"required":["step","outcome"],"properties":{"step":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z][A-Za-z0-9]*\$","description":"The step name, from the adapter\'s plan."},"outcome":{"type":"string","enum":["applied","unchanged","failed","skipped"],"description":"`applied` — changed the forge. `unchanged` — already as required. `failed` — could not be applied. `skipped` — not attempted because an earlier step failed."},"detail":{"type":"string","maxLength":1024,"description":"Human-readable detail, typically for a failure."}}},"description":"Per-step outcomes, in the order they ran."},"error":{"type":"object","additionalProperties":false,"required":["code"],"description":"Why the job failed or stopped partway. Present when `outcome` is not `succeeded`.","properties":{"code":{"type":"string","minLength":1,"maxLength":64,"pattern":"^[a-z][A-Za-z0-9]*\$","description":"Forge-neutral: `nameTaken`, `notFound`, `forbidden` (the bridge\'s forge credentials lack a permission), `notCapable` (this forge or namespace cannot do it), `rateLimited`, `expired` (a begin* job\'s person never completed it), `forgeError`. A bridge MAY use others."},"message":{"type":"string","maxLength":1024}}},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Git Namespaces — Bridge Job Result — response payload","description":"The VTC has recorded the result.","type":"object","additionalProperties":false,"required":["jobId"],"properties":{"jobId":{"type":"string","minLength":1,"maxLength":128},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ForgeId":{"title":"ForgeId","description":"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\"812736451\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.","type":"string","minLength":1,"maxLength":64},"RepoResource":{"title":"RepoResource","description":"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.","type":"string","maxLength":455,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}\$"}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Git Namespaces — Bridge Job Result — response payload","description":"The VTC has recorded the result.","type":"object","additionalProperties":false,"required":["jobId"],"properties":{"jobId":{"type":"string","minLength":1,"maxLength":128},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"ForgeId":{"title":"ForgeId","description":"An identifier the forge itself assigns — a repository id, a user or organisation id — carried as a string so a forge whose ids are not numbers needs no new version. GitHub and Forgejo ids are decimal integers written as strings (`\\"812736451\\"`). Unlike a name, it survives renames and transfers, which is why rights and bindings are keyed by it.","type":"string","minLength":1,"maxLength":64},"RepoResource":{"title":"RepoResource","description":"A forge-qualified resource naming exactly one repository: `<forge-host>/<owner>/<repo>`, lowercase.","type":"string","maxLength":455,"pattern":"^[a-z0-9](?:[a-z0-9-]*[a-z0-9])?(?:\\\\.[a-z0-9](?:[a-z0-9-]*[a-z0-9])?)+/[a-z0-9_-][a-z0-9._-]{0,99}/[a-z0-9_-][a-z0-9._-]{0,99}\$"}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
