// Code generated by scripts/build-dart-bindings.mjs. DO NOT EDIT.
// Source: specs/git-ns/namespace/unbind/0.1/payload.schema.json

import 'package:trust_tasks/trust_tasks.dart';

/// The VTC's opaque identifier for a namespace, assigned when it is bound. Stable for
/// the life of the binding; never reused for another binding.
typedef NamespaceId = String;

/// Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a
/// reverse-DNS namespace; structure under each namespace is opaque to the framework.
typedef Ext = Map<String, dynamic>;

/// What unbinding removed.
class Response {
  const Response({
    required this.namespace,
    required this.rightsRevoked,
    required this.reposDetached,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Response.fromJson(Map<String, dynamic> json) => Response(
        namespace: json['namespace'] as String,
        rightsRevoked: json['rightsRevoked'] as int,
        reposDetached: json['reposDetached'] as int,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  final NamespaceId namespace;

  /// How many recorded rights were revoked.
  final int rightsRevoked;

  /// How many repositories became `detached`.
  final int reposDetached;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'namespace': namespace,
        'rightsRevoked': rightsRevoked,
        'reposDetached': reposDetached,
        if (ext != null) 'ext': ext!,
      };
}

/// A namespace admin ends the VTC's governance of a namespace. Every right recorded on
/// a resource inside it is revoked and withdrawn from the Trust Registry, and every
/// repository in it becomes `detached`.
class Payload {
  const Payload({
    required this.namespace,
    this.ext,
  });

  /// Read this payload from a decoded JSON object.
  factory Payload.fromJson(Map<String, dynamic> json) => Payload(
        namespace: json['namespace'] as String,
        ext: json['ext'] as Map<String, dynamic>?,
      );

  /// The namespace to unbind.
  final NamespaceId namespace;
  final Ext? ext;

  /// Serialize to a JSON-encodable map, omitting absent members.
  Map<String, dynamic> toJson() => <String, dynamic>{
        'namespace': namespace,
        if (ext != null) 'ext': ext!,
      };
}

/// The Trust Task type URI this library's [Payload] is carried under.
const String typeUri =
    'https://trusttasks.org/spec/git-ns/namespace/unbind/0.1';

/// The success-response form of [typeUri] (SPEC §4.4.1).
const String responseTypeUri =
    'https://trusttasks.org/spec/git-ns/namespace/unbind/0.1#response';

/// This specification's payload schema, as JSON text.
///
/// SPEC §7.2 item 2 is performed against this. It ships with the library rather
/// than only as a file under specs/ because a Dart class carries none of the
/// schema's constraints — not minLength, not pattern, not the oneOf mutual
/// exclusion — so without it every such rule is unenforced. Cross-file \$refs are
/// already inlined, so it needs no resolver.
const String payloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$id":"https://trusttasks.org/spec/git-ns/namespace/unbind/0.1","title":"Git Namespaces — Unbind Namespace — payload","description":"A namespace admin ends the VTC\'s governance of a namespace. Every right recorded on a resource inside it is revoked and withdrawn from the Trust Registry, and every repository in it becomes `detached`.","type":"object","additionalProperties":false,"required":["namespace"],"properties":{"namespace":{"\$ref":"#/\$defs/NamespaceId","description":"The namespace to unbind."},"ext":{"\$ref":"#/\$defs/Ext"}},"\$defs":{"Response":{"\$anchor":"response","title":"Git Namespaces — Unbind Namespace — response payload","description":"What unbinding removed.","type":"object","additionalProperties":false,"required":["namespace","rightsRevoked","reposDetached"],"properties":{"namespace":{"\$ref":"#/\$defs/NamespaceId"},"rightsRevoked":{"type":"integer","minimum":0,"description":"How many recorded rights were revoked."},"reposDetached":{"type":"integer","minimum":0,"description":"How many repositories became `detached`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"NamespaceId":{"title":"NamespaceId","description":"The VTC\'s opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.","type":"string","minLength":1,"maxLength":128}}}';

/// As [payloadSchemaJson], for the success-response variant.
const String responsePayloadSchemaJson =
    '{"\$schema":"https://json-schema.org/draft/2020-12/schema","\$ref":"#/\$defs/Response","\$defs":{"Response":{"\$anchor":"response","title":"Git Namespaces — Unbind Namespace — response payload","description":"What unbinding removed.","type":"object","additionalProperties":false,"required":["namespace","rightsRevoked","reposDetached"],"properties":{"namespace":{"\$ref":"#/\$defs/NamespaceId"},"rightsRevoked":{"type":"integer","minimum":0,"description":"How many recorded rights were revoked."},"reposDetached":{"type":"integer","minimum":0,"description":"How many repositories became `detached`."},"ext":{"\$ref":"#/\$defs/Ext"}}},"Ext":{"title":"Ext","description":"Vendor-namespaced extension object per SPEC.md §4.5.1. Each immediate key MUST be a reverse-DNS namespace; structure under each namespace is opaque to the framework.","type":"object","minProperties":1,"additionalProperties":true,"propertyNames":{"pattern":"^[a-z][a-z0-9-]*(\\\\.[a-z0-9-]+)+\$"}},"NamespaceId":{"title":"NamespaceId","description":"The VTC\'s opaque identifier for a namespace, assigned when it is bound. Stable for the life of the binding; never reused for another binding.","type":"string","minLength":1,"maxLength":128}}}';

/// The SPEC §7.2 policy for the request variant, taken from this
/// specification's front matter.
///
/// Pass it to [consumeInbound] — items 5b, 7 and 8 are per-specification and
/// cannot be derived from the document alone, and item 2 needs the schema it
/// carries.
const SpecPolicy spec = SpecPolicy(
  typeUri: typeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: payloadSchemaJson,
);

/// The SPEC §7.2 policy for the success-response variant.
///
/// `isRecipientRequired` tracks the *issuer* party's requirement, because a
/// response swaps the parties (§7.3 item 5).
const SpecPolicy responseSpec = SpecPolicy(
  typeUri: responseTypeUri,
  isBearer: false,
  isProofRequired: true,
  isRecipientRequired: true,
  isIssuedAtRequired: true,
  payloadSchema: responsePayloadSchemaJson,
);
