/// The Trust Tasks HTTPS transport binding (`bindings/https/0.2`).
///
/// [HttpsClient] sends documents; [HttpsServer] answers them and runs on any
/// HTTP server — `package:trust_tasks_https/io.dart` serves it with `dart:io`.
library;

export 'src/binding.dart';
export 'src/client.dart';
export 'src/server.dart';
